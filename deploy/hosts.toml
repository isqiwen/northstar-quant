# user 仅用于 init-host 登录及提权；后续应用管理固定使用 northstar。
[database]
host = "core.local"
user = "qiwen"
port = 22

[data_hub]
host = "core.local"
user = "qiwen"
port = 22

[research]
host = "research.local"
user = "qiwen"
port = 22

[live]
host = "quant.wangqiwen.me"
user = "qiwen"
port = 22

# 仅共享行情目录；core/research/external，默认 Research 提供 NFSv4。
[nfs]
server = "research"
enabled = true

# server = "external" 时填写可通过 SSH 管理的 Ubuntu/Debian 主机。
# [nfs_server]
# host = "storage.local"
# user = "qiwen"
# port = 22

# 系统架构

Northstar 是面向中国期货的个人量化框架：**三个独立应用，共享业务规则，一个账户一个事实所有者。**
首轮交付一个真实合约、一个策略的跨日回测，以及同一业务规则下的 SimNow 开平仓与恢复。
多品种、换月、多策略、机器学习和真实资金分阶段加入。运行方法见 [README](../README.md)，任务见 [ROADMAP](ROADMAP.md)。

## 0. 设计取舍

参考项目及官方资料见 [REFERENCES](REFERENCES.md)。本次核对日期：2026-09-10。
采用 NautilusTrader 的组件边界、VeighNa 的 CTP/开平转换思路、RQAlpha/LEAN 的账户与模拟模型分工；
参考 Qlib/MLflow 组织研究，ArcticDB 组织固定版本。具体取舍是 Northstar 的设计，不代表这些项目共同规定的标准。

- 保留 Python、Next.js、Protobuf、PostgreSQL、SQLite、Parquet/Arrow 与 DuckDB；本轮不引入另一套交易框架、任务平台或存储引擎。
- Research/Live 通过进程内 `messaging` 总线路由明确类型的命令与事件，计算仍使用直接调用；跨进程只经过所属 API、客户端或固定产物。
- 类型及不变量放在业务所有者旁，不增加 `core/`、`contracts/`、`schemas/` 等统一外层；接口有真实调用者后才抽象。
- 成交、资金与数据可复核优先；关键持久化的成本需要测量，不能靠取消可靠落盘来获得“低延迟”。
- 每次替换同时修改调用、协议、测试与文档，删除旧实现。历史证据可以保留，旧架构不继续运行。

## 1. 三个应用与存储

| 应用 | 长期运行进程 | 权威状态 | 文件与外部接口 |
|---|---|---|---|
| Data Hub（core） | Next.js、Python API、独立数据 worker | core 本地 PostgreSQL：目录、任务、质量、发布、引用 | Tushare；原文与固定 Parquet；发布清单 API |
| Research（research） | Next.js、Python API、独立研究 worker/计算子进程 | 本机 SQLite：计划、任务、尝试、结果、候选 | API 取清单、只读市场文件；DuckDB 查询；本机产物/临时目录 |
| Live（独立主机） | Next.js、管理 API、每账户实例独立内核及 CTP 子进程 | 每实例本机 SQLite：绑定、回报、账本、订单、授权、恢复 | 独立行情/柜台；本地固定材料；后续独立异步归档 |

前端、API、worker/内核分别监管；浏览器或管理 API 退出不停止持久任务与交易内核。
Data Hub 与 Research 通过固定清单和只读文件协作，不共享可写业务表。DuckDB 是查询器，不是另一份任务库。
Live 运行与本地恢复不等待 core、research、NAS 或 WAN 数据库；远端断线只影响可选的材料传输或归档。

目录固定在 `/opt/northstar/`：`apps`、`config`、`files/{source,market,research,backup}`、`state`、`credentials`、`logs`、`work`。
应用只感知目录、内容/存储身份和权限；NFS 服务及 `/quant` 导出由外部准备，部署层仅在 Data Hub/Research 主机挂载到 `/opt/northstar/files/market`，分别读写/只读（同机时主机读写、Research 容器只读）。
应用主机依赖由外部预先准备，首次 deploy 使用 SSH 默认账号提权并准备 northstar 部署账号；hosts.toml 仅保存 host，部署使用固定 northstar，SSH 端口由本机 SSH 配置决定。数据库固定使用 Data Hub 主机配置，独立容器及本机运行目录。NFS 的前置要求和客户端挂载规则见 [部署说明](../deploy/README.md)，本机运行数据与 Live SQLite 不放在该共享上。
每个发布文件必须核对身份与哈希；目录存在不代表引用的数据存在。

当前同一受管 Live 主机用共享账户进程锁保证唯一占用；身份为环境、BrokerID、账户。
不同环境/账户可并行，同账户不得同时运行两个所有者。锁无超时自动接管，跨主机主备不在当前范围。
接收线程在既有半秒监测周期检查实例、账户和接收锁的文件身份，不依赖 HTTP 请求触发；
锁或账户目录被替换即终止接收，已提交事实保留，不自动重连。这是所有权故障检测，不是发送授权。
`simnow_trading`、`simnow_dev` 是 SANDBOX 的柜台 profile；`ctp_production` 对应 LIVE，目前拒绝启动。实例/数据库隔离，账户锁按柜台 profile、BrokerID、账户确定，不能将两个 SimNow 柜台混成同一个资金账户。

## 2. 当前实现与目标的差距

| 已有实现 | 下一步必须完成 |
|---|---|
| 全市场 Tushare 持久同步、原文留存、精确数值/Decimal Parquet、补缺/失败状态、固定查询 | 真实样本抽核、规范化跨日研究清单 |
| 固定配置、研究 worker、回测/Paper、共享内核、多空数量投影；本地批次新增固定日结算、今昨仓计价和量参与率部分成交 | 真实有效条款、费用调整、保证金、柜台资金投影及完整执行 |
| Live 独立内核/SQLite/账户锁、CTP 只读查询与有界接收、影子信号和账本观察 | 夜盘持续行情、完整材料绑定、发送授权/预占、报撤单、恢复与独立告警 |
| 安装、浏览器、备份与容器故障验收；#53 跨主机实机证据 | 用真实固定数据完成跨日研究，并在明确授权的 SimNow 完成非空交易闭环 |

当前已共享内核入口、行情窗口、策略运行时及部分风险/账户能力，**尚未共享完整期货账户与执行实现**。
Research 与 Live 已共用多空数量规则和 FIFO 计价；Live 查询工作台从已确认成交重放同一 Account，
报告费用前已实现盈亏。柜台完整资金与跨日计价仍需接入统一事实模型，
不能通过重命名或并列保存两套余额宣称统一完成。研究已支持固定日结算、有效费用/保证金/限价和受量参与率限制的部分成交；真实来源装配及柜台资金闭环仍待完成。

## 3. 模块、接口与状态所有权

以下为 `backend/src/northstar_quant/` 内的职责；“核心接口”描述业务边界，不要求新增抽象基类。

| 模块 | 唯一拥有的规则或状态 | 核心接口 / 不可越界 |
|---|---|---|
| `apps/`、`web/`、`cli/` | 装配、页面/API、会话、参数/输出 | 调用业务接口；不实现导入、记账、风险或发单规则 |
| `data_management/` | 来源、同步、加工、质量、清单、引用与生命周期 | 固定发布/读取；不维护交易账户 |
| `trading/` | 交易内核的单线程入口、生命周期、失败策略与完成通知 | 装配所属事件处理器；不保存账户事实或授予发送权 |
| `messaging/` | 实例内同步路由、订阅与生命周期 | 不拥有业务状态、事务、持久日志或执行权限 |
| `persistence/` | UTC 数据库时间、非过期文件锁与显式写事务 | 只提供已复用的存储机制；表、事实修改规则和完整事务由业务所有者负责 |
| `market_data/` | 合约/行情/时段的值与时间数值规则 | 适配器转为值；不导入 ORM、页面或 Research |
| `factors/` | 有界输入、预热、因子计算 | 仅使用决策时可得信息；不含未来标签 |
| `strategies/` | 固定配置、内部状态、目标暴露及理由 | 行情/特征 → 账户无关目标；不下单或改持仓 |
| `risk/` | 定仓、限额、资金/仓位预算与拒绝原因 | 目标 + 只读账户 + 条款 + 预占 → 许可；不保存另一份余额 |
| `execution/` | 开平订单计划、身份、状态、发送尝试、撤单、未决预占 | 许可 → 有界订单；确认回报推进状态，UNKNOWN 保留预占 |
| `broker/` | CTP 协议映射、连接、查询、原始回报与接收证据 | 请求/回报适配；不决定目标、风险许可或通用账户规则 |
| `accounting/` | 资金、成交、费用、结算事实与持仓/权益投影 | 应用已确认事实并核对；请求不能提前改变持仓 |
| `simulation/` | 固定假设下的可成交条件、成交量/价格、费用估计 | 接收订单和后续市场事件；不拥有账户或研究任务 |
| `research/` | 计划、任务、尝试、评价、策略版本和候选 | 组织历史时钟与共享能力；模拟资金/成交明确标识 |
| `live/` | 账户实例、材料绑定、事件顺序、授权、控制和恢复 | 协調本地业务操作；真实/仿真成交只来自柜台 |

一个模块拥有唯一修改规则，不要求一个模块一个数据库或进程。
成交去重、入账、订单/预占推进与处理游标属于同一个完整业务提交，不能因拆包变成几次独立提交。
只读账户视图由账本及订单预占组合，缓存/检查点均可重建，不拥有独立事实。

`risk.RiskEngine` 固定合约、风险参数和条款修订，拥有下单前定仓、条款价格/有效期收紧及费用和保证金预算。
回测主循环及报告核对调用同一业务入口；已绑定条款不能通过省略参数或同名替换绕开。
风险结果不等于 Live 执行授权，柜台当前资金覆盖和未决订单仍须在生产会话执行处确认。

`execution.ExecutionEngine` 接管当前单目标路径的工作订单投影及保留/替换计划。
保留前由 Risk 按原订单价格上界重新计算费用和保证金预算；计划替换不会释放预占。
只有已确认的提交、撤单、失效或与本次已入账成交数量一致的更新才能推进投影；累计回报不制造成交。
回测的同步撤单属于历史模拟；Live 由 OrderJournal 持久保存订单、发送尝试和预占，CTP 适配器关联真实回报。此内存组件不替代持久 OMS，当前账户/风险与新增报单的完整接入仍未完成。

### 进程内消息总线

2026-09-10 按用户确认引入，参考 [Nautilus MessageBus](https://nautilustrader.io/docs/latest/concepts/message_bus/)
及其 [typed_router.rs](https://github.com/nautechsystems/nautilus_trader/blob/0608ce241d3ab3e2e4b5d66115c9b3cc71dbf439/crates/common/src/msgbus/typed_router.rs)
（审阅提交 `0608ce241d3ab3e2e4b5d66115c9b3cc71dbf439`）。借鉴类型路由和同步分发，独立 Python 实现；没有复制源码或新增依赖。

- 每个 Research/Paper 计算会话、每个 Live 接收循环通过 `TradingKernel` 独立创建 `MessageBus`，只能在创建它的线程/进程使用。Live 沿用 CTP 有界接收通道进入核心线程。
- `Endpoint[输入,结果]` 是唯一负责人命令/查询，调用者导入所属模块的同一个端点定义；`Topic[事件]` 按订阅顺序同步通知。主题精确匹配，定义放所属业务旁；不增加通配符、优先级或异步请求跟踪。
- 消息为所属模块的不可变值。总线检查精确类型，业务模块保证嵌套内容不变；Broker 回调字段只读，研究步骤的嵌套报告用不可变 JSON 保存。
- 嵌套调用按深度优先执行，禁止路由循环及分发中修改订阅。命令异常原样传回，业务负责人负责回滚/恢复；事件订阅者异常使总线故障，禁止继续分发，已提交事实不回滚也不自动重发。
- Research `advance` 经端点执行整步计算，成功后发布 `STEP_COMPLETED`；批量报告订阅该事件。暂存状态及未平仓批次，失败撤销新成交，不复制累计历史账本。此事件表示内存计算完成，Paper 的数据库提交仍由所属操作负责。
- Live 柜台回调经唯一接收端点进入原有原文保存、账户应用、影子推进。不广播未提交成交；撤单命令已接入同一核心，新增报单的账户准入仍在 #49/#32 中推进。
- 总线不保存历史、不负责幂等/交易授权、不连接三应用；Data Hub 继续用持久任务。关闭释放处理器；恢复重新装配，不能从路由状态推导发送权。

### 共享交易内核与环境适配

2026-09-10 再次核对 [Nautilus Architecture](https://nautilustrader.io/docs/latest/concepts/architecture/)
和 [Event sourcing](https://nautilustrader.io/docs/latest/concepts/event_sourcing/) 的 `latest` 文档；
沿用上文源码审阅身份，独立 Python 实现，无新增依赖。采用共同内核、单线程状态推进和环境适配；
不照搬进程全局注册表、无界接收队列或异步缓存持久化。

```text
Research/Paper runner                 Live 账户接收 runner
          └──────────────┬────────────────────┘
                    TradingKernel
                单线程入口 + MessageBus
                生命周期 + 失败/完成边界
                         │
               所属环境的事件处理器
                         │
      行情 → 策略 → Risk → Execution（含 OMS）
                           ↓
                       Accounting
                           ↓
                  Portfolio / Cache 投影
```

`Environment` 在内核构造时固定：`BACKTEST`、`SANDBOX`、`LIVE`。Research/文件驱动 Paper 为 BACKTEST，
两个 SimNow profile 为 SANDBOX，真实资金为 LIVE；三类共享 TradingKernel 和所属业务能力。
这是 Northstar 按资金用途采用的定义：2026-09-10 核对的 Nautilus latest 将外部柜台模拟账户也归入 Live，我们保留其适配器边界但不照搬该分类。
部署实例列表使用 `id:broker_profile`，自动推导不可变环境；独立启动使用 `NORTHSTAR_ENVIRONMENT` 与
`NORTHSTAR_BROKER_PROFILE`，不匹配即拒绝，真实资金仍未准入。协议/实例状态分别返回 environment 和 broker_profile。
环境进入研究结果、检查点和柜台接收绑定；SANDBOX/LIVE 不允许采用研究整步回滚策略。实际账户锁继续绑定柜台与账户，不能靠换环境标签取得第二份发送权。

图为组件关系；具体业务顺序仍遵循第 5 节，先应用已发生事实再计算新决策。
Kernel 不实现公式/撮合/账本，不增加独立账户缓存权威。当前处理器为 Research `TradingSession._process`
和 Live `LiveStreams.accept`：前者装配已有模拟/账户/策略/Risk，后者装配可靠接收/账本/行情/影子策略。
完整账户与执行模型按 #19/#20/#49/#32 替换所属组件，不能以共同入口宣称其业务已统一。

- 内核为 READY → RUNNING → CLOSED；无法安全继续时为 FAULTED，只能关闭并从事实重新装配，不提供自动重试/重新启用。
- 一次输入及其完成通知不可重入，跨线程/进程拒绝；关闭释放路由。进程内诊断计数不充当持久事件身份。
- Research 处理器显式承诺整步回滚，计算异常后允许调用者重试；完成通知在处理器成功返回后发布，通知失败使内核故障，不能撤销已经完成的事实。
- Live 默认任何处理异常均使内核故障，后续回调不得继续写入；原始接收/账户提交仍由原来的可靠事务负责。关闭内核不表示订单终态或账户空仓。
- `market_data.Instrument` 只保存合约身份、币种、最小价位与乘数等经济属性，不包含 K 线周期。固定研究输入、模拟适配器、策略订阅和检查点显式绑定周期；同合约不同周期使用同一账户定义，错误周期的输入或恢复在执行前拒绝。2026-09-11 查阅 [Nautilus Data latest](https://nautilustrader.io/docs/latest/concepts/data/) 的 Instrument/BarType 分离原则；没有引入其依赖，当前只实现固定秒数的完成 Bar，不把月线或交易日等同固定秒数。
- `market_data.DataEngine` 按合约、周期和固定来源登记行情订阅，路由完成 Bar，并持有每个流的不可变 `MarketWindow`。未订阅流、身份冲突、倒退时钟及迟到输入在策略前拒绝；重复输入不再分发。
  `strategies.Trader` 使用返回的因果行情帧计算，只在成功后一起提交 DataEngine 候选状态和策略状态；Research 整步失败时原投影保持不变。历史回放和 Live 完成行情均接入此路径，缓存不保存账户事实。
  2026-09-11 再查阅 Nautilus Data/Execution latest：采用按标的/周期/来源区分订阅、处理后分发的原则。当前接入的是已完成 Bar，CTP 原始行情采样与交易日规则仍由其适配入口负责，不宣称已具备订单簿或全部聚合类型。
- Trader 以固定实例标识、配置和行情流绑定策略；同一输入按实例标识稳定排序，任一策略异常时所有候选状态不提交。重复输入不重复产生意图，实例绑定进入意图身份；跨线程/进程、重入及关闭后推进均拒绝。Research 和 Live 完成行情已使用此所有者，未增加 Actor 或执行算法空接口。
- 当前窗口面向单合约完整 Bar，CTP 采样仍由柜台适配器产生标准化 Bar；Trader 已管理固定策略实例的共享订阅与原子状态提交；这不代表多策略账户预算、归因、统一执行算法或完整 Live 跨日账户已完成。
- 回测和当前文件驱动 Paper 都从历史输入推进，Environment 为 BACKTEST；SimNow 为 SANDBOX，真实资金为 LIVE。SimNow 始终采用外部柜台成交，不能以本地撮合替代。环境名称不授予交易。
- 一个 Live 账户所有者独立进程与本地 SQLite；Research 用独立计算进程并行，Web/API 继续独立监管。串行处理不保证外部输入天然可重放，仍需固定输入顺序、时钟与配置身份。
- 网络接收、普通日志和归档可在外围；发送前身份/预占必须可靠提交，成交去重与账本更新保持事务一致，不能统一改成异步后置。

## 4. 数据、条款与研究输入

```text
Tushare → 原始响应 → 响应校验 → 规范化/质量 → 固定研究清单
                                                   ↓
Research：清单 + 只读 Parquet → 固定计划 → 任务/尝试 → 结果/候选
```

Data Hub 仅自动同步 Tushare 全部期货历史数据；没有手工上传、下载范围选择、Tick 或实时 Recorder。
同步持久排队、分片、限流/重试、补缺与修订；全市场未完成不阻塞选择已有一个合约做研究验收。
空响应、截断、权限不足、缺字段与质量异常不能表示已完成覆盖。原文不改写，失败原因与请求身份保留。
供应商错误先脱敏分类：明确限频进入有限退避，明确权限不足才暂停同类请求；未确认原因（包括仅有 40203）仅阻塞失败分片，不能推断整个接口无权访问。旧失败保持待核查，不自动记为成功。
2026-09-10 对照 Tushare 官方 ft_mins 文档及获准的 RB2610.SHF 三次请求，确认现有 REST 参数可获取真实分钟；继续有界 HTTPS 接收，不新增 SDK 或运行依赖。固定样本与单位核对见 SOURCES.md。

区分三种身份：**供应商响应**记录收到什么；**加工版本**记录用何规则处理；**研究清单**固定本次使用哪些结果。
内容相同但规则变更可以形成新校验证据。研究不能通过扫描目录或跟随“最新数据”隐式改变输入。
2026-09-12 对照 [Nautilus Data latest](https://nautilustrader.io/docs/latest/concepts/data/) 的标的与数据时间边界，将多时段组合从验收辅助代码落实到 `DataLibrary.assemble_research`：只接收已确认的固定清单，统一截止时间重新检查每段质量，保留原始观察身份及已有结算/条款，不以新版本替换旧输入。逻辑清单提交后再原子发布文件；中断重试复用同一清单，文件未完成时不出现在可运行数据列表中。此接口不推断 Tushare 标签或交易日历，也不将合成时段验收等同真实数据装配完成。


当前 `tushare-response/11` 对行情接口强制 OHLCV 和完整时间标签，固定 `tushare-numbers/2` 数值规则及参数进入校验内容摘要；
部署不会追溯改写旧 receipt，已有数据按后续刷新/重试接受新规则，旧规则结果不冒充已重新核验。
Parquet 物理布局服务于按数据类型、合约、周期和时间范围读取；内容哈希与清单负责身份，不依赖文件名猜语义。
小文件合并生成新清单，被研究/候选/备份引用的旧对象先保护再清理，不原地覆盖。

一份可运行的研究清单必须固定：

- 真实期货合约 UUID、交易所/品种、tick、乘数、币种、量额单位；连续/复权价格只能作为研究特征，不能成为执行合约。
- 每段实际交易时段与交易日，包括夜盘跨自然日、周末/节假日和休市；不得以 `自然日 + 1` 猜交易日。
- bar 开始、完成、可得时间与交易日；固定周期及缺失/修订政策，不以下载时间伪造历史首次可得。
- 区间适用的结算事实、费用/保证金条款及其有效时间、可得时间、来源与修订；缺少必要事实拒绝所需计算。
- 原文、响应、规则、质量与输出文件身份；读取再次核对哈希、冲突和有界范围。

共享 `MarketWindow` 在策略计算前拒绝区间重叠和交易日倒退：新 bar 开始不得早于上一根完成时刻，显式交易日不得小于上一根；检查点恢复执行相同检查。
相邻 bar 可以首尾相接，也可以存在休市间隔；不据间隔补造行情，不按自然日期改写声明交易日。该检查不验证交易所日历或21:00标签语义；跨日运行另外要求固定且当时已可得的结算事实。
2026-09-11 对照 Nautilus Data/Bar 的时间区间与交易输入边界，沿用本项目共享行情窗口，未新增框架依赖。Research 在任何撮合/记账前调用同一检查，拒绝输入不消耗待成交订单。

Tushare 最终历史值使用 `FINAL_REVISED`，只能在明确时钟假设下做事后研究。没有证据不能升级为严格时点回测。
有首次可得/修订证据时按当时知识选择；晚到数据不得改写旧决策或补造过去成交。标签和训练拟合结果单独管理。
数值规范化由 `data_management/tushare/normalization.py` 拥有，接入既有下载 worker → 质量 → 发布 → 页面查询。
行情价格、量、金额及已提供的结算等数值列使用 Decimal(38,12)，去掉无意义尾零，使 `100`、`100.0` 和 `1e2` 的数值身份一致；超范围拒绝而非舍入，缺失保持缺失。
成交额按固定接口倍率转为 CNY，数量保留供应商口径，不推断单双边换算；原始响应字节独立留存，清单固定其哈希/长度及规范化规则，Parquet 保存相同规则元数据。
同值重试不产生另一经济版本，但原始请求尝试仍可追溯；规则变更产生新校验证据，既有清单不改写。Web/API 返回精确十进制字符串，不通过浮点数往返。
这不是完整期货领域规范化：合约条款、交易日/时段、首次可得及有效结算事实仍需明确证据，不能直接将响应行当成可执行行情。
页面同步记录可对最新留存响应提交重处理，固定来源尝试身份并持久排入原同步分片；同一 worker 执行，不另建导入或下载路径。
暂停时保留排队，恢复或进程中断后继续同一原文；执行读取核对哈希/长度，不使用新下载替代缺失或损坏原文。重处理不消耗下载次数和供应商限流额度，不推进下载新鲜度时间，也不能替代已排队下载。
只允许最新已留存响应，拒绝过期页面、运行中或已拆分分片；成功原子更新当前覆盖及尝试的 receipt 引用，旧 receipt/清单不改写。失败保留已有覆盖。
尝试记录来源父尝试、实际执行 Git 身份和结果 receipt；数据库初始化追加这些证据列，已有历史尝试保持空值，不伪造旧执行身份。
失败尝试保存结构化质量报告：原始响应 1 起始行号、字段、原因和冲突关联行；缺字段等响应级错误无行号。不回显非法原始值。
扫描有界响应，每行记录首个失败，保存总问题行数及最多 100 项明细并明确截断；整份响应仍拒绝发布，不丢掉坏行后发布其余数据。
报告与失败状态在同一事务保存，成功重试保留原失败报告；旧尝试未记录的明细不追溯伪造。
版本页显式选择基准和对照两个同请求分片的固定 receipt，核验清单/Parquet 哈希后按记录身份比较新增、删除、修改与未变行。
`response-diff/1` 为只读派生查询，比较身份绑定规则及有方向的两个清单；分页固定这两个版本，不读取当前最新覆盖，不另存一份数据。
字段变化保留精确值与字段存在性（缺字段不同于空值），每页 100 项，超过 512 字符的文本明确标记为预览；同时显示原文与处理规则是否不同，不推断修订原因或历史首次可得时间。
完整期货领域规范化、时段和有效条款仍由 #18 完成；#46 的本轮工程验收状态以 Issue/CI 为准。

参考核对（2026-09-10）：[Nautilus Data/Catalog](https://nautilustrader.io/docs/latest/concepts/data/) 的类型化数据与固定读取边界、[ArcticDB Library](https://docs.arcticdb.io/latest/api/library/) 的版本保留语义；重处理追加结果并保留旧引用，不原地修订。2026-09-10 再次核对上述 latest 文档，本轮不引入其运行库。
[Tushare 分钟](https://tushare.pro/document/2?doc_id=313)和[日线](https://tushare.pro/document/2?doc_id=138)分别声明成交额为元和万元；仅执行固定单位换算，不补造首次可得时间。
[Arrow decimal128](https://arrow.apache.org/docs/python/generated/pyarrow.decimal128.html) 文档为 25.0.1，实际仓库依赖 PyArrow 21.0.0 已验证 Decimal(38,12) 写入与重读；没有新增依赖或复制框架代码。

当前供应商响应发布尚不等于上述研究清单，相关落地由 #46、#18 完成。

当前固定输入声明已区分 `DAY/NIGHT`，夜盘显式绑定交易日，周五夜盘可以声明归属周一；
时段种类进入目录和来源身份，分钟开始、完成、可得时间保持独立，发布后的 Parquet 读取保留这些事实。
`data_management/research_input.py` 拥有声明和结构校验，发布流程不再把全部时段注册成 DAY。
固定研究快照现可读取同一合约的多个已发布分区，每个分区分别验证完整分钟时段、来源归档与质量 pin；
跨分区拒绝合约/经济含义不一致、时段重叠、交易日倒退、混合可得时间假设和可得时间倒退。
清单显式提供 `trading_days`、`import_specs` 和各时段质量结果；休市间隔不补行情，原快照不变。
Data Library 读取已提交清单时，要求每个原始导入均被已完成的发布尝试引用；仅写入清单但处理未确认不能被列出。
跨时段清单还需与已完成的固定 Parquet 导出完全一致，未导出清单不开放；
单时段处理继续要求当前尝试已确认。没有伪造组合数据的单来源导入，归档与内容校验继续生效。
`research_reader.py` 单独拥有固定读取，现有发布 API、Parquet 和 Research 客户端沿用同一数据结构。
组合发布必须在统一截止时间重新评估各分区质量，不能复用不同截止时间的质量结论。

2026-09-11 核对 [Nautilus Data](https://nautilustrader.io/docs/latest/concepts/data/) 的 `latest` 文档，
采用独立合约身份、明确数据时间和固定目录读取的原则，不引入其运行库或复制源码。
直接下载和保存 Tushare 官方提供的 1min、5min、15min、30min、60min 数据，各周期独立校验与版本化。
不从 1min 重建供应商大周期数据，不提供跨周期比较，也不以不同周期 OHLCV 是否相等作为同步、发布或研究准入条件。
Research 使用所需周期的官方固定版本；时间标签、可得时间、交易日和有效条款仍须明确，不能提前使用尚未完成的 Bar。

当前多时段证据仍是合成工程数据，不验证交易所节假日。真实 Tushare 响应装配、21:00 边界解释、
有效条款引用与真实合约联合验收仍由 #18 完成；没有增加手工导入或下载范围选择。
Research 跨交易日运行要求清单同时绑定结算事实；仅有多日行情仍不足以运行跨日账户（#19）。

## 5. 国内期货账户与事件顺序

目标账户保留多/空、今/昨仓，不用相反方向开仓自动抵消持仓。订单与成交分别明确开仓、平今、平昨或经验证的平仓规则。
Execution 根据账户仓位、未决冻结和交易所规则生成开平计划；SHFE/INE 等差异经独立验证，不硬套单一净仓模型。

Accounting 的输入为明确身份的成交、费用/调整、资金流与逐日结算事实。费用未知不填零，也不拒收已确认成交。
共享 Account 接受已确认的零余额或负余额，不因欠款拒收成交或无法重放；
模拟初始资金必须为正的约束留在 SimulationConfig，账户记账不替代 Risk 准入。
必要的费用、资金、结算或持仓事实未确立时，账户保持未完整核对，禁止新增风险，不能靠默认值恢复许可。
Accounting 拥有费用与结算计价规则；Simulation 根据固定条款生成模拟费用事实，Risk 用相同规则计算预算。柜台费用以确认事实为准。
日结算更新持仓结算基准并确认对应盈亏，不等于平仓；今昨仓迁移、费用、保证金与权益必须逐项勾稽，重复事件不重复入账。
今昨仓迁移按确认的交易日结算边界推进，不按自然日零点。
柜台余额/持仓查询是独立核对证据，不能覆盖本地账本以制造“核对成功”。账户累计费用不能重复当作新增费用。

资金观察的读取、重复命令和恢复校验从原始查询重建金额、期间变化、累计变化及未知原因；
同时检查账户/环境和查询时间顺序。派生结果即使具有一致哈希，若与来源不符仍拒绝。
2026-09-12 核对 [Nautilus Reconciliation latest](https://nautilustrader.io/docs/latest/concepts/reconciliation/)，
采用保留外部事实并核对重建状态的原则；不引入依赖，不以合成调整补齐资金缺口。
累计资金观察仍不证明逐笔费用覆盖、原子账户截面或发送权限。
2026-09-13 对照 VeighNa `vnpy_ctp` 提交
`ad76250cf87cf5b03604336fde8c7489bdc0d0d7` 的
[CTP Trader API 头文件](https://github.com/vnpy/vnpy_ctp/blob/ad76250cf87cf5b03604336fde8c7489bdc0d0d7/vnpy_ctp/api/include/ctp/ThostFtdcTraderApi.h)
及相邻结构定义，原生接收器登记银行/期货发起的银期转账与冲正通知。
只复制期货账户、金额、状态、平台流水和冲正关联；不复制银行账号、密码、证件、姓名或自由错误文本。
查询期间出现这些通知也属于账户活动，不能作为无变化的平仓基线。
资金观察保留账户身份与活动不确定性的区别；区间任一端点存在查询期间活动，
该区间保持 UNKNOWN，不因后一份查询成功而清除前一端点的不确定性。
查询来源中的资金通知与流通知一样进入持仓证据链的账户待核对原因。
流接收先保留原始证据，再将账户标记为资金流待核对；恢复重算同一结论。
仅有资金流缺口时，继续展示可从成交重建的持仓和费用前盈亏，现金仍未核定。
错账户、成交冲突或未知持仓影响仍阻止持仓估值，不能一概忽略未知事实。
不因收到一条通知就推断转账成功、手续费归属、唯一性或可用余额，也不提供转账请求功能。
这是接口字段与通知边界的参考，未复制上游实现或增加依赖。

资金观察引用的持仓必须属于同一账户基线、具有一致序号，并在该观察记录前已存在。
写入事务和恢复读取使用同一关联校验；时钟倒退造成因果关系不成立时，在提交前拒绝。
后来的持仓不能回填为过去资金观察的依据，后续入账也不改变已固定的引用。

当前 #19 已接通共享数量投影：Research `Account` 与 Live `BrokerLedger` 都使用
`accounting.positions.Position` 的多空/今昨仓规则；订单及模拟成交显式携带开平，反向开仓保留独立持仓。
Research 用同方向 FIFO 成本计算明确平仓的盈亏，超量平仓不能自动变成开仓；Risk 先将反向目标降为平仓，
Execution 再按今昨仓生成明确平仓请求，之后的新决策才能开反向仓。成交结果保存开平及成交后的多空仓位，恢复从完整成交重建并核验投影。
Live 继续先保存柜台原始事实；同一报告时间的成交按组核对数量，不捏造组内顺序，未知费用不填成零。
Research 已支持显式日结算及成本批次的今昨仓迁移；柜台资金计价、费用调整、冻结/保证金及完整 Portfolio 仍待 #18/#19/#20。

参考核对：2026-09-10 的 [Nautilus Architecture](https://nautilustrader.io/docs/latest/concepts/architecture/)（latest）
及 [VeighNa converter](https://github.com/vnpy/vnpy/blob/master/vnpy/trader/converter.py)（master）。
采用派生账户投影和明确开平边界，独立实现，无新增依赖；未把交易所平仓转换或原框架冻结逻辑当作本次已完成能力。

2026-09-11 本地重构接通日结算：`accounting.settlement.SettlementFact` 固定合约、交易日、下一交易日、
结算时刻、可得时刻、价格与来源声明，进入 `dataset_snapshot_manifest/4.0.0` 内容哈希及固定 Parquet 清单。
发布拒绝重复合约/交易日、超截止时间及不属于选定行情的结算。Research 要求结算位于两段行情之间且在下一段开始前已可得，
不从收盘价或自然日推断结算。账户逐成本批次确认盯市盈亏并重置结算基准，保留开仓交易日，不生成虚假平仓成交。
批量回测与 SQLite Paper 使用同一路径，持久化结算和成交后从事实重建账户；失败步骤包括结算一起回滚。
数据存储基线为 `20260913_01`，旧结构不原地转换；任何个人数据重建/转换需独立安排，本批不操作已部署数据。
报告单独列出结算依据、价格、盯市盈亏与现金。当前验证使用合成事实；来源声明不等于独立验证的交易所结算条款。
报告逐项核对账户已入账的成交与结算，缺失结算或修改成交金额不能仅凭相同记录数通过。
跨日预检和执行使用相同的可得时间顺序；固定快照拒绝未纳入规范内容的额外结算字段。

本次对照 [RQAlpha 6.3.x Position](https://rqalpha.readthedocs.io/zh-cn/latest/_modules/rqalpha/mod/rqalpha_mod_sys_accounts/position_model.html)
及 [LEAN Settlement](https://www.quantconnect.com/docs/v2/writing-algorithms/reality-modeling/settlement/key-concepts)
（2026-09-11 在线版本），吸收持仓年龄、结算前后计价及模型职责分离；代码独立实现，无复制或新增依赖。
不采用股票 T+N 资金交收语义代替中国期货日盯市，不以参考实现证明国内费用、保证金或交易日口径。

一次业务推进遵循：

1. 核验事件身份、可得时间和固定绑定；Live 先可靠保存原始接收事实。
2. 应用已可得账户事实和有效条款；Research 让之前许可的订单在后续可执行市场事件上尝试模拟成交。
3. 形成成交、账本、订单剩余量/预占与游标的一致变化；Live 提交已发生事实，Research 暂存到整步成功后提交。派生失败不能丢失原始事实。
4. 更新可用行情，计算策略目标；Risk 用更新后的账户及所有未决风险计算许可。
5. Execution 固定订单、授权引用、发送尝试与预占，可靠提交后才交给 Broker。确认结果另作为事件处理。

Research 单事件计算失败应不留下半更新的账户、订单、策略或指标，同一输入重试结果一致；不复制整段历史来实现每步事务。
Live 已发生的成交不能因随后策略失败撤销入账：策略故障停止新增风险，原始事实留存并支持幂等补记。
回放不触发外部发送。账户所有者按确定顺序串行推进，SDK 接收和普通日志通过有界队列交付，不等待远端。

2026-09-11 对照 [Nautilus Execution](https://nautilustrader.io/docs/latest/concepts/execution/)（latest）
及 [LEAN Fill](https://www.quantconnect.com/docs/v2/writing-algorithms/reality-modeling/trade-fills/key-concepts)
的事件与成交模型职责分离，独立实现以下历史执行规则，无新增框架依赖。
本地模拟实现按 `max_volume_participation`（默认 0.1）限制完整后续 bar 的每手成交量，
数量向下取整；委托在 bar 开始后才生效时，不使用该 bar 的累计成交量。零量、超许可价格、过期分别保留原因。
每步保存订单提交、部分/全部成交、剩余量和显式目标替换撤销，账户与订单变化使用同一事务。
新风险边界仍包含原授权、方向/开平不变且剩余量获准时保留原订单身份及到期时间，避免延迟 bar 导致反复撤建而永远不能成交。
报告提供订单过程；固定有效条款约束价格与期限，但历史 bar 假设不证明真实队列优先级，真实历史条款仍需验证。

Simulation 明确价格模型、滑点、费用、成交量参与限制、部分成交和未成交原因；一根 OHLC 不能证明 bar 内先后成交路径。
成交量参与约束不能使用委托生效前累计的成交量。停市、无成交量、超价、过期及缺条款均有明确结果；数据结束不编造平仓。模拟成交带独立身份，永不冒充柜台事实。

## 6. 因子、策略、研究与发布

新增因子/策略使用所属模块的定义、参数、输入要求、注册和测试，见 [开发指南](FACTOR_STRATEGY_DEVELOPMENT.md)。
先服务实际策略计算，不为了框架形式增加强制继承树。`strategies.Trader` 管理有界预热、因果顺序和策略内部状态。

计划固定数据清单、策略/因子/Risk/模拟配置、代码 SHA、评价窗口、基准及比较规则。任务与尝试分别记录，失败/取消也留证。
独立 worker 先持久认领后执行；SQLite 写事务先取得写权，读事务保持一致快照。 Research 和 Live 使用同一 `northstar_write` 连接标记，跨业务模块调用不会退化为先读后争抢写锁；应用分别保留本地存储准入与初始化。UTC 时间编解码也独立于 Broker，Data Hub 不为通用存储机制导入 Live 或柜台模块。计算子进程可取消，管理响应有资源余量。
按实测内存、磁盘、队列与管理响应调度 CPU/算法支持的 GPU；不靠固定核数或容器资源百分比代替测量。

结果由同一账本导出成交、费用、结算、保证金、权益与回撤。报表库可消费核对后的收益序列，不能重算另一份账户真相。
样本外区间、训练范围、成本和信息时钟不一致时，结果比较必须拒绝或明确区分；向量化筛选不替代事件回测。

```text
固定策略版本 + 评价证据 → Research 候选
候选 + 合约/条款/预热资料 → Live 核验并绑定实例
已核对的实例 + 操作者限额/时窗授权 → 可发送（仍逐次执行风控）
```

名称、默认模板和“最新版”不改变已绑定内容。候选接收、实例启动、核对通过和交易授权是四个不同事实。
当前 Live 接收候选不会自动创建可交易部署；生产账户和权限留待单独验收。

## 7. Live 控制、身份与恢复

公网/局域网可访问不等于操作者已认证；Protobuf、Host/Origin 和 CSRF 不证明用户身份。
三个工作台各自验证单一操作者 `owner`，不限制访问 IP，也不共享账号数据库或会话。
`web` 负责 Protobuf 登录、注销和会话查询；所有管理 API 在进入业务前校验服务器会话，写操作再校验 CSRF。
只读发布接口供跨主机机器访问，保持独立于管理身份；登录不连接柜台，不授予交易执行许可。
首次访问通过 `/api/setup` 创建唯一用户名与 scrypt 密码摘要，私有文件以排他方式原子发布，重复注册返回冲突。
文件位于 `/opt/northstar/credentials/<应用>/workspace/`，容器只挂载其所属目录；无默认密码和摘要环境参数。
普通重启和重新 deploy 均保留持久账号；API 重启撤销旧会话，用户使用原用户名和密码重新登录。只有未创建账号的首次安装进入注册流程，部署不得删除账号凭据。
会话为服务端随机身份，30 分钟绝对有效期；Cookie 为 HttpOnly、SameSite=Strict，HTTPS 使用 Secure。
注销、轮换登录和 API 重启均使对应旧身份失效。操作日志仅记录服务器确认的操作者、路由模板、方法和状态，不记录请求体或凭据。
浏览器未知命令只保存命令身份、路径和实例，不保存请求体；登录失效不删除 UNKNOWN 身份，也不重发操作。
2026-09-11 参考 [OWASP Password Storage](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html)：
使用 Python 标准库 scrypt（N=2^17、r=8、p=1、随机盐）；校验固定摘要格式，单应用同时只执行一次密码计算，每分钟至多 5 次尝试。
哈希计算在 Web 工作线程，不进入交易内核；不引入中心认证服务。
前端仅选择固定实例，不能修改其环境、账户或凭据；旧请求/未确认命令不能转发到另一个实例。

管理命令通过当前内部协议 5 传递可信操作人：Web 从已认证会话绑定 `owner`，本机 CLI 为 `maintenance`，浏览器自报字段不参与身份判断。命令回执可靠保存操作人、输入、时限与运行实例，同一命令身份不能更换操作人；历史未记录身份的事实不补造归属。回执查询使用统一 `request_id`，重启后查询不重复执行。此身份记录不等于交易发送授权。 SQLite 与 PostgreSQL 同时约束命令身份及终态不可改写/删除，状态只能从受理中的 RUNNING 变为最终回执；重启读出的 UNKNOWN 不会重新获得执行权。

默认无发送。授权绑定环境、账户、合约、策略/配置、代码、时窗和限额；重启、重连或恢复必须重新核对后显式启用。
暂停新增风险不等于撤单，撤单请求不等于确认撤销，请求平仓不等于已经空仓；既有订单继续接收回报。
未知发送结果保留订单身份与预占，只查询核对，不盲重发。所有权过期、复制数据库或连接恢复均不能自动产生第二发送者。

2026-09-11 再对照 Nautilus `latest` Execution/Orders 的命令、订单状态与执行事实边界，
Northstar 将模拟订单的费用/新保证金预算在 Risk 中计算并固定到 Execution 订单。
采用按单手向上取整的费用/保证金上界覆盖部分成交；平仓仅预占相应今昨仓数量及费用，不先释放持仓保证金。
残量、预算和订单身份随 Research/Paper 的整步事务与检查点保存；部分成交消耗残量，确认终态才释放。
报告复核初始预算和完整订单过程，分别显示持仓保证金、剩余费用/保证金预占及扣除后的资金。
没有固定条款时只展示明确模拟预算，不捏造历史可用资金；不将这些预算当作柜台冻结事实。
Live 已有独立限额同意、SQLite 发送尝试与预占事务；现有撤单进入原接收核心，新增报单仍须接通当前账户/行情/风险准入，#49 未完成。

同日参考 RQAlpha `a5fb4e43879c381e61131399dcc094d495c7080a` 的
`rqalpha/mod/rqalpha_mod_sys_simulation/matcher/base.py` 方向性涨跌停约束，独立实现当前 Bar 模型：
固定条款下，涨停价买入/跌停价卖出返回 `LIMIT_QUEUE_UNOBSERVED`，保留订单及预占，
不能以成交量替代排队证据；后续价格离开边界时仍需符合原量价/期限约束。
这是一项明确的保守模拟假设，不声称这些委托在真实交易所必然无法成交；未引入 RQAlpha 依赖或源码。

固定候选格式 2 的 `same_clean_revision` 只说明配置、因子、策略和研究证据引用同一干净提交，
不再使用暗示生产准入的 `production_eligible` 名称。Live 接收时另核对已安装提交，SANDBOX 同样适用；
接收结果保持 `execution_authorized=false`，材料检查不创建执行授权、资金或订单。

SQLite（WAL/FULL）和来源文件在实例本地，关键事实与普通异步 `logging_` 日志分开。
备份将数据库一致快照与引用文件联合保存，恢复到独立空目录，先核验内容与账户绑定，再只读核对柜台。
后续独立上传器只传封存备份，失败积压不阻塞内核；不把 Live 行情作为 Data Hub 的第二种研究数据入口。
按实测吞吐、延迟和本地剩余容量声明离线保证窗口；本地持久化故障或安全容量耗尽停止新增风险，不承诺无限离线。

实例健康至少说明输入新鲜度、账户差异、未决/UNKNOWN、积压、磁盘和运行身份。主机失联告警需要独立故障域观察者。
2026-09-12 将内核/本地存储的只读观察落实为每实例独立 `live-monitor` 进程和容器：从当前内核协议检查运行身份与新鲜响应，输出结构化故障、恢复、重启与一分钟心跳。凭据目录仅包含 read token，不挂载数据库或柜台凭据；观察者故障不能控制内核。JSON stdout 是默认采集出口，可配置 HTTPS 告警接收端；个人主机外部接收/失联检测仍未验收；执行模块另提供全量 UNKNOWN、事实冲突和未确认费用统计，不受网页分页限制。
输入健康由 LiveStreams 读取已有状态和接收边界：陈旧行情、连接失败、未处理回调积压、未知账户回报及旧运行实例均可报告。
只读取最近的活动接收或最后一个终止结果，不重放全量行情；查询使用状态/时间索引，
不会调用会修改暂停状态的轮询逻辑。四个只读响应必须属于同一内核身份。
完整资金覆盖与外部观察者仍待验收，不能把 HEALTHY 当作执行就绪。
实际告警通道、外部柜台和生产操作按相应任务提供明确环境与授权；代码/合成故障验收不冒充外部运行证据。

## 8. 交付原则

任务只保留一个所有者和可验收结果，直接前置写 GitHub 原生依赖；历史已交付 Issue 保留证据并归档。
先走通固定真实数据 → 跨日账户/模拟 → 报告，再走固定材料 → SimNow 执行 → 控制/恢复，最后联合验收。
每次交付记录源码 SHA、安装/镜像身份、数据库版本、存储 UUID、输入与关键检查；复用现有验收入口。
正式构建要求干净工作区；开发标记不能冒充可重建制品。更换实现不自动迁移/删除个人数据或操作柜台。

### 固定费用、保证金与限价条款（本地批次 2026-09-11）

`accounting.terms` 拥有绝对按金额/按手数计价、显式舍入、多空保证金与开/平今/平昨费用。
`FuturesTerms` 固定合约、版本、来源、生效/失效和可得时间；清单选择不重叠修订并纳入内容哈希。
外部消息由 `proto/accounting.proto` 唯一定义费用率、有效条款和结算事实，三个应用共同消费生成代码；
固定字段不再使用任意 JSON 对象，计算实现不依赖 Protobuf。
Research 只在整个输入区间条款可得且有效时使用它，缺口不回退到默认费率；订单到期不越过条款失效。
没有固定条款的工程/探索输入继续明确使用配置声明的假设，其结果不冒充真实历史条款回测。

Simulation 使用实际成交数量及对应开平费用，拒绝涨跌停边界外价格；Risk 对固定价格区间的最高费用
及多空保证金建立保守预算，包含逐笔舍入，预计算到实例以免每根行情重复计算。
Live 现有只读开仓预算和 Research 费用/保证金共用 `ChargeRate` 的绝对计价规则；
预算固定原策略步骤、同一接收连接的已结束查询与账本记录身份；直接核对原始账户观察的
账户/币种/业务/交易日/结算范围。查询结束不能刷新早先资金回报的时间；资金和行情分别
检查时效。数值不变只表示两次观察一致，不代表事件覆盖、完整资金核对或发送许可。
预算与最终发送之间仍需原子准入、预占和当前账户检查，不能直接把预算结果用于报单。
报告显示每步条款身份、按观察价格计的保证金和扣除保证金后的资金；后者未减未决订单冻结，
未采用组合抵扣，也不是柜台实际 Available。条款来源声明仍需独立验收，不能由类型校验代替。

2026-09-11 查阅 [Nautilus Instruments](https://nautilustrader.io/docs/latest/concepts/instruments/)（latest）
与 [LEAN Buying Power](https://www.quantconnect.com/docs/v2/writing-algorithms/reality-modeling/buying-power)
的当前官方文档，采用合约经济参数统一引用、风险预算与账户事实分离；未复制代码或增加依赖。
不把 maker/taker 费用或海外保证金政策套作中国期货开平今昨条款。

### 订单状态归属补强（本地重构，2026-09-11）

核对 [Nautilus Orders](https://nautilustrader.io/docs/latest/concepts/orders/) 的当前文档，
采用订单数量/终态由 Execution 管理、取消请求与取消事实分离的原则，不复制实现或增加依赖。
Research/Paper 调用 `PendingOrder` 的成交推进、工作状态、已确认取消和过期操作；
Accounting 仍在同一整步事务内接收独立成交，负责事实去重与账户变化。
保留工作订单必须同时满足新许可的方向、开平、残量、价格范围及截止时间；
缩短许可不能继续沿用寿命更长的旧订单。模拟环境能确认撤销/过期，
外部柜台必须等待自己的确认，不能调用这些操作伪装成已经完成报撤单或预占释放。

同日核对 [Nautilus Portfolio](https://nautilustrader.io/docs/latest/concepts/portfolio/) 的当前文档，
采用从账户和持仓推导只读估值的边界；Northstar 暂只处理单合约同币种，不复制其跨币种/多账户聚合。
账户估值由 `accounting.portfolio` 从已接受账本和明确标记价格推导，不能倒置到账本事实之前。
Research/Paper 不另算保证金；报告发布前按同一账户规则顺序重放成交/结算，
逐行核对资金、持仓、费用、权益、保证金及回撤，再核对最终账户。
此校验按报告生成执行，不进入每根行情的累计历史复制；市场来源身份仍由固定输入和持久步骤哈希证明。

模拟订单报告由 `execution.OrderHistory` 按独立成交核对提交、部分成交、终态与残量，
拒绝凭累计数量冒充成交、终态重复推进和丢失提交记录。该校验按事件数量线性执行；
不是柜台回报接收器，不能直接套用为外部乱序回报或成交更正的恢复规则。

Portfolio 估值同时保留多头/空头手数、带方向的净名义敞口与双边总名义敞口；
净持仓为零不代表没有总敞口或保证金。累计实现盈亏分解为日结算盈亏和结算成本重置后的平仓盈亏，
不能把这两项再次相加到已包含它们的余额。报告的费用、盈亏、敞口均通过明确 Protobuf 字段传递，
前端只展示，不用浮点数重算账本。

### 固定探索性评价（本地重构，2026-09-11）

参考 [empyrical stats](https://github.com/quantopian/empyrical/blob/master/empyrical/stats.py) 的显式周期/年化参数边界，
只参考方法约束，不复制公式实现或引入该库。当前唯一评价方案在会话创建时绑定完整固定快照及其行情区间、数量；
进入 Paper 的初始检查点，恢复不能改动计划。基准明确为不交易、无利息的初始现金，
窗口未走完及无来源输入分别标记，年化/夏普为空。全部结果均为探索性研究，不能自行称为样本外。
比较还要求相同评价方案；此版本不提供可编辑评价窗口、无风险曲线或训练/验证划分，后续按实际研究需求扩展。


2026-09-11：对照 [Python 3.12 SQLite URI 说明](https://docs.python.org/3.12/library/sqlite3.html#how-to-work-with-sqlite-uris)，
Research/Live 的连接池使用 `mode=rw` 打开应用已创建的文件，在新连接、连接配置及事务开始时核对文件身份。
运行中丢失或替换数据库会失败，不因查询或连接池重连创建空库。Live 诊断分别显示本地数据库盘空闲量、
数据库/WAL 大小及来源盘状态，不执行 checkpoint、修复或交易；无可用空间或观测失败明确降级。
这些观测不代替执行授权或独立告警送达。

### 固定参数实验（本地批次 2026-09-11）

对照 [Qlib Workflow 0.9.8.dev11](https://qlib.readthedocs.io/en/latest/component/workflow.html)
与 [MLflow Tracking latest](https://mlflow.org/docs/latest/ml/tracking/)（查阅 2026-09-11），
采用运行前固定数据分段、候选、选择规则与全部尝试的组织方式，不引入这两个框架或复制源码。
`research.experiments` 接入现有 SQLite、TaskStore、worker、RunStore 和研究工作台。
三个已发布快照必须按时间有序、互不重叠且合约/经济/时钟语义一致；2–64 个候选保持相同账户、风险与成本。
训练及验证各自从空账户/空预热运行，按验证净收益最高、同分按配置身份选定。
选择记录不可修改，并在胜出配置的测试任务排队前提交；重启沿用固定任务 UUID 和已保存选择。
中断须通过原任务显式重试；失败/取消候选保留，不用失败后的临时参数代替。测试快照在计划提交时即登记保留引用。
网页展示全部阶段任务与对应账本报告。测试收益不进入此计划的选择逻辑；跨实验重复使用测试区间仍不能宣称未使用样本。
另支持固定双收益因子的岭回归：仅加载训练快照，DuckDB 向量化计算过去收益与独立未来收益标签，剔除预热和无未来标签的尾部。
训练标准化统计和 2–16 个预定惩罚参数仅使用训练数据；系数固定为 12 位十进制，并先持久保存拟合身份再排队回测。
共享 `learned.linear_return` 策略只接收过去因子和固定系数，训练误差按固定系数计算，不进入交易账本。
三个窗口还要求前一窗口的可得截止不晚于下一窗口开始；失败拟合保存终态，重启不暗中重新拟合。
当前限单合约一分钟、最多 10,000 根训练 Bar。它提供可复核的小型学习流程；多因子模型、滚动稳定性和组合筛选仍需补齐。

### 数据浏览发现（2026-09-14）

查阅 [Nautilus Data latest](https://nautilustrader.io/docs/latest/concepts/data/)（2026-09-14，动态文档）。
沿用市场数据与合约定义分离、数据范围查询由数据所有者负责的原则，不新增依赖或存储。
`data_management.exploration.discovery` 从当前 receipt 指针发现非空发布版本，排除 SPLIT；
合约目录只补充交易所/品种，不以合约存在推断有行情。列表分页只读 PostgreSQL 元数据，
按合约聚合展示可用周期和发布窗口外界；元数据不代表连续覆盖、历史首次可得或已通过研究准入。
选择合约/周期后，按最新非空发布窗口选择一个当前版本，仅读取该版本的固定文件，复用范围查询的清单/哈希校验，从实际记录日期
选取末尾最多31个自然日，再由既有精确查询返回分页、字段、来源及导出权限。长请求区间
或休市末尾不会令默认查询落在空白日期；打开后新修订不会替换所选版本。
三应用协议中的 Data Hub API 以只读 available/select 提供合约发现及打开；open 保留明确版本入口；浏览工作台优先呈现已有数据，
保留手动筛选及空结果返回入口。日期含首尾，分钟按上海供应商自然日期，日线按供应商
交易日期，周/月按既有双日期范围键；不在展示层另行解释夜盘交易日。

2026-09-14 图表与中文合约：查阅 [Apache ECharts Axis](https://echarts.apache.org/handbook/en/concepts/axis/)
及 [dataZoom](https://echarts.apache.org/en/option.html#dataZoom)，继续使用已锁定 ECharts 6.1.0，
无新依赖。左侧已发布数据列表和右侧暗色行情图共用原 Data API；合约中文名取已同步
`details.name`，展示及中文搜索保留供应商代码。部分仅返回代码的中金所品种名称按
[中金所产品目录](https://www.cffex.com.cn/cp/)和[首页](https://www.cffex.com.cn/)（本日查阅）
补充展示标签；未知名称仍保留原名/代码，不写回行情身份或研究材料。

图表使用明确 receipt 集合的只读 `explorer/chart`，复用同一精确读取及32MiB/20,000行边界；
返回的 view_id 必须与明细相同。图表完整读取固定范围，明细继续200行分页，翻页不重置
图表或截断均线。原生周期按钮由同合约实际非空发布种类决定；切换自动定位该周期最近发布中的有记录日期，不从
其他周期重采样；手工日期查询的空范围仍明确为空。MA5/10/20/60及MACD(12,26,9)仅作展示，
使用当前及此前固定记录，从范围起点预热；MACD EMA以首个有效收盘初始化，直方图为
2×(DIF−DEA)，缺失值重置预热。不调用策略配置或产生交易输入，精确悬浮信息仍用原值。
当前合并产物详情保留其分页读取边界并注明当前明细页，不将其冒充供应商全范围图表。

### 固定范围读取（本地批次 2026-09-11）

查阅 [Arrow Parquet 25.0.1](https://arrow.apache.org/docs/python/parquet.html)、
[Nautilus Data latest](https://nautilustrader.io/docs/latest/concepts/data/) 与
[ArcticDB Library latest](https://docs.arcticdb.io/latest/api/library/)（2026-09-11）。
沿用仓库 PyArrow 21.0.0：供应商固定文件按 512 行写行组，目录查询按供应商时间统计裁剪行组。
统计缺失/含空值时完整解码；合约统计不符仍拒绝。查询后的冲突检测、精确十进制、来源引用和分页身份不变。
`exploration.parquet.ResponseScan` 拥有扫描，页面展示文件数、行组数、解码行数与完整核验字节数。
合成四日 2,048 行输入中，单日范围解码 512 行，结果与完整扫描后过滤一致；完整文件哈希仍必须验证，
不能声称减少了这些核验 I/O。

`data_management.compaction` 将 2–32 个明确固定版本、日期范围和实现身份持久化后交给现有 Data worker。
沿用上述范围查询的重叠冲突拒绝与逐行来源，生成新的内容寻址清单和分片；原供应商版本不覆盖、不删除。
计划最多 32 MiB/20,000 行，产物分片不超过来源归档文件上限，512 行一组。
读取只核验命中分页的完整分片并裁剪行组，检查原输入身份并重新计算来源导出权限。
后台中断留下明确 FAILED，不能以同一请求重新执行；新请求仍绑定固定输入。
合并文件同时进入来源归档和发布目录，并纳入现有数据库/文件联合备份及恢复。
Data Hub 的固定结果可提交合并，“版本与来源”展示持久计划，详情支持进度、分页与按权限导出。
`data_management.cleanup` 通过维护 CLI 预览并执行最多 500 个孤立对象的固定清单。
引用根包含全部来源、供应商版本、合并产物和历史备份；研究依赖的旧版本全部保留。
执行取得 Data 排他维护锁，重新核对引用与对象清单，先提交意图再核验哈希、删除并 fsync。
中断留下 STARTED，重复旧身份不重试；再次预览引用及既有操作后才能生成新清单。
失败保留已删除列表和原因，不把部分清理声明成完整成功。该入口不删除任何有引用版本、不处理 staging。

### 因子前瞻诊断（本地批次 2026-09-11）

对照 [Alphalens Reloaded API 0.4.1post1+8.g3348e92](https://alphalens.ml4trading.io/api-reference.html)
的前瞻收益、秩相关和分组报告组织方式；不引入该依赖或复制源码。Northstar 当前为单合约时间序列，
不能把这里的相关性称为多标的横截面 IC。`research.factor_analysis` 在固定因子结果之外计算
1/5/15 根连续 Bar 的价格收益标签；晚到因子、跨休市缺口、跨交易日与越出固定快照的标签逐项排除。
DuckDB 使用显式有序归约、平均秩处理同值，报告数值固定 12 位小数；因子及交易金额精度不受影响。
全窗口分组只用于事后描述，逐日相关展示稳定性，不提供独立样本显著性或样本外结论。
结果与输入、实现身份一起保存在原因子任务/产物中，工作台展示有效样本、排除原因、分组与逐日统计。
标签与分组不进入策略输入；粗筛仍需经过固定训练/验证及完整成本、撮合和账户验证。

因子计算同样先固定请求身份、参数、完整快照证据和实现身份，再保存 QUEUED。
Research 现有排他执行器按可用 CPU、内存与磁盘启动独立计算进程，API 不持有计算生命周期。
进度持久保存，取消请求先进入 CANCEL_REQUESTED；终态和结果不可修改。
进程退出或执行器恢复后留下 INTERRUPTED，需要明确提交新身份，不能自动重试旧任务。
提交阶段仍验证固定数据的来源与文件；计算与前瞻诊断在子进程执行，浏览器轮询所属 API。

训练拟合也在提交实验时保存 QUEUED，领取后由同一资源调度器启动独立子进程。
监督进程只编排固定候选任务与选择，不运行模型计算；拟合仅加载训练输入。
拟合进程丢失留下 INTERRUPTED，旧实验不自动重新拟合；终态系数和候选集合不可改写。

`market_data.SessionWindow` 是不依赖 ORM 的显式时段值，来源读取通过同一半开区间解析交易日。
2026-09-12 对照 [Nautilus Data latest](https://nautilustrader.io/docs/latest/concepts/data/) 的输入时间与聚合边界，
Live 接收也使用该时段值：`SessionSchedule` 固定来源依据、可得时间和最多 64 个实际时段，
经工作台选择 JSON 文件、Protobuf、Live 客户端和内核启动命令进入不可变接收绑定。
CTP `ActionDay` 只确定自然时钟，`TradingDay` 必须匹配声明时段；不推算周末或节假日。
采样、休市诊断与影子开仓预算共用这份绑定，跨午夜保留交易日；时段重叠、未来可得、
更换绑定或行情缺口拒绝。休市后的首分钟重新预热，不补造末端 Bar。
未提供时段仍限定既有 SHFE 日盘；声明时段可支持同交易日夜盘/日盘，但不是交易所日历认证。
交易日变化仍暂停，须先补齐账户结算与恢复核对，不能把时段文件当作持续交易或发送许可。
未复制参考代码或增加框架依赖，真实夜盘接收和跨交易日运行仍待外部验收。
原仅供测试调用的 catalog 时间辅助实现已移除；夜盘归属来自固定声明，不按自然日加一。
此边界检查不认证交易所节假日或供应商时间口径，真实日历证据仍必须由 Data 提供。

账户按一组固定同币种合约建账：现金、费用、成交身份及结算流水只有一个所有者，FIFO 批次和今昨仓按合约隔离。
`value_portfolio` 汇总全部持仓的显式价格与有效保证金条款，缺少持仓腿不会按零估值；不推断汇率、套利保证金或组合优惠。
当前单合约回测/报告通过同一账户和组合估值计算获得单合约视图；多合约研究任务编排仍需接通。
账户检查点用于重放比较，不用于注入持仓；一次事件内的多个合约变化共享回滚边界。


RiskEngine 从同一时刻的组合估值计算目标合约允许数量，同时扣除其他合约的持仓保证金、总敞口和未决订单预算。
订单固定合约 UUID 及每手手续费、保证金、新增总敞口和成交滑点损失预算，剩余手数由 Execution 管理。
到期授权不等于终态，外部未决订单仍占用预算；同一合约已有工作单时拒绝另行定仓。
历史模拟可以计算确认撤单后的替代计划，但仍先记录取消再提交新单；柜台场景不得假定立即撤单。
成交路由、订单保留和报告核对均验证合约身份；报告逐项显示预算，扣除资金预算时包含滑点。
组合估值不净抵多空保证金；当前净目标定仓拒绝同时多空持仓，以免隐藏库存。
此计算不是外部发送授权或完整多合约运行验收，Live 的持久发送和核对仍需独立完成。

ExecutionEngine 的内存投影按合约保存工作单，订单身份在整个账户投影内唯一。
同一合约一次净目标只保留一张工作单；跨合约撤单、部分成交和替代计划互不释放预占。
报告 OrderHistory 重放逐笔成交和订单通知，要求每个成交都属于同合约的已授权工作单。
持久订单历史仍由运行所有者提交，投影可重建；这里不代替外部柜台 OMS 的发送及恢复证据。

本次组合设计继续查阅 NautilusTrader latest Execution 与 pysystemtrade develop backtesting
（2026-09-11，后者的 Forecast combine / Position scaling / Creating portfolios）。
采用策略组合、定仓、订单和账户分责，以及显式组合权重；不复制源码、不增加运行依赖。
Northstar 不因为部分策略缺少数据而重新分配它的权重，不用虚拟策略账户替代柜台账户。
多合约研究下一步统一排列行情、结算与有效条款事件；一个 TradingKernel 驱动一个 Account/ExecutionEngine。
各合约只拥有行情窗口和策略状态；报告必须列出全部持仓腿，不能相加不同合约的手数形成净仓位。

Live 接收候选后，配置列表和会话读取均来自本地 StrategyMaterials，而不查询 Research 的配置或因子表。
会话同时绑定配置身份与确切候选身份；接收同配置的新证据不会替换原会话材料。
读取核验内容哈希、配置索引与已安装代码，接收及复用都不授予发送权限。
Live 初始化不再创建 Research 因子任务、配置目录或 Paper 会话表；共享策略计算直接消费 StrategyConfig。


### 外部订单的本地提交与恢复（本地重构，2026-09-11）

再次核对 Nautilus `latest` Execution / Execution Reconciliation（2026-09-11）。
采用执行事实重建订单、累计回报不能替代逐笔成交的边界；不复制源码或引入依赖。
`execution.OrderJournal` 由 LiveOwner 装配到同一实例 SQLite，启动重放检查本地投影。
订单固定身份、合约、预算、授权引用与唯一发送尝试在同一 FULL/WAL 写事务中提交。
实际发送回调只在提交后执行一次；返回、异常或进程死亡都不证明柜台接受或拒绝，
相同身份在新进程中也不能重发。撤单尝试同样先落盘且不释放预算，只有明确的柜台撤单拒绝才能允许新的撤单尝试。业务所有者在同一事务中校验授权及账户，传输处再检查存活。

逐笔成交通过同一数据库连接完成账户写入与订单残量更新；重复身份不重复入账，失败整体回滚。
撤销累计回报若仍缺逐笔成交，保留全部尚未入账数量的预算；矛盾或倒退回报留为 UNKNOWN。
已确认发生的迟到成交不因授权过期而拒绝，终态后矛盾成交入账后保持核对要求。
原始柜台接收证据先于派生处理保存；这里的 FillFact 需要明确费用，不能把未知费用填零。

这是持久订单组件，不是已完成的发送授权或 CTP 报撤单入口：当前管理 API 通过原接收核心处理已有订单撤单与显式授权的一手空仓开仓，
Live 的明确授权、真实费用/账户衔接、独立柜台核对继续由 #49/#32/#33 完成。
Journal 的本地重放不等于外部 reconciliation，也不授予恢复后发送权。

Live HTTP 测试使用与账户锁相同的真实 SQLite 文件，不再借用 PostgreSQL 假装 Live。
同时修复合约登记在成交事务内重复取得 SQLite 写锁的问题：合约登记借用所属连接的保存点，
不独立提交；影子决策查询显式声明提交时间类型，避免 SQLite 文本时间导致预算接口失败。


本地订单查询通过 Live 内核 → 管理 API → Protobuf → Next.js 工作台；管理端不读 SQLite。
订单及事件以固定序号游标分页，每页最多 100 条，保留所有历史事实；金额使用 Decimal 字符串。
页面区分固定授权引用、实际发送尝试、逐笔入账、累计回报和剩余预算。
查询不调用 SDK、不创建授权、不重放发送；无当前观察时明确报错。


### 按所有者核验恢复（本地重构，2026-09-11）

`apps.storage` 只分发 Data Hub PostgreSQL、Research SQLite、Live SQLite，默认数据管理；
删除统一 `all` 数据库和 PostgreSQL 恢复时安装缺失业务表的分支。
Data Hub 当前基线的安装、检查与备份属于 `data_management`，Research/Live 不进入其恢复链。
PG 适配回归测试可以显式装配独立模块，但不是任何应用可部署的数据库组织方式。

Live 的实际 SQLite 备份和恢复核验完整本地查询、材料、持仓、资金观察、订单比较、
行情归档来源和持久订单事件链，不使用界面的最近 100 条限制。备份只有核验完成才写完成清单；
恢复先在备份快照上核验，再写入空目标。缺失前序、父证据或损坏身份不因外层文件哈希正确而通过。
完成状态为 `RECONCILIATION_REQUIRED`，本地证据完整不能代替当前柜台核对或授予执行权限。
普通 Live 启动与离线恢复共用 `live.recovery.verify`，在开放内核接口前核验全部本地事实；
损坏账户不能因未经备份恢复流程而绕过检查。此项为启动时检查，不在行情热路径重复执行，
也不会建立柜台连接、重开接收会话或恢复旧授权。2026-09-12 对照 Nautilus Reconciliation
的启动核对失败不运行原则；本地完整性验证与仍待完成的外部账户核对分别记录。
历史会话核验候选内容和来源身份，不要求其 Git 提交等于当前安装；它不执行历史代码。
新会话仍要求候选与当前安装一致。这样升级后可以读取已有事实，而不会自动恢复旧策略资格。
共享持久模块只提供有界普通文件读取、哈希和完成记录刷盘，业务验证仍在各所有者内。
Live/Research 使用 SQLAlchemy `Uuid` 的 SQLite 文本表示，避免全数字 UUID 的数字亲和转换损坏身份。


### CTP 订单传输身份（本地重构，2026-09-11）

核对 vnpy_ctp `main` 的 CTP gateway 报撤单实现及 MIT 许可（2026-09-11），
并再次对照 Nautilus latest Execution。借鉴领域订单与具体柜台字段分离，不复制源码或新增依赖。
`broker.CtpExecution` 把 SHFE/INE 普通期货限价单映射为明确开、平今、平昨；
真实期货公司资料和其他交易所平仓语义仍需独立核实，不能把普通平仓自动解释成平今。

CTP 账号范围内的 OrderRef 在本地持久高水位与已验证 TD 登录 MaxOrderRef 之上递增。
原始登录 FrontID/SessionID、合约、限价和报撤单请求在 OrderJournal 的同一事务内固定；
提交后才执行注入的传输，发送前再核验所有权与订单有效期。
重连不能重写原订单身份；重复请求不能换价格或重发，SDK 非零返回仍保留不确定性与预占。
报单价格仅在原生结构边界转为 double，不能保留十进制价格时拒绝，不擅自改价。

Live 启动及恢复核验 CTP 身份与其订单、发送和撤单父记录；接收端复制报撤单拒绝的有限字段。
网络隔离 SDK 自检增加 InputOrder/InputOrderAction 的构造，不调用发送。
这一步接通本地订单与传输回调，不等于管理端已允许报单：完整运行授权、原生双向接收进程、
柜台回报关联及真实费用账本仍需接入，现有只读/影子运行不因这些组件出现而获得发送权限。

固定依赖 `ctpwrapper 6.7.13` 的 PyPI 源码发布包也已核对（2026-09-11），SHA-256
`94be58b8360e26f6c3b57f5a991a6c5bdac0f7707234e41ceb69c42c006e778e`；
通过 AST 核实登录、InputOrder、InputOrderAction 构造参数，未执行下载源码或连接柜台。
这项检查不替代 Linux 原生 ABI 和明确授权的外部验收。


CTP `OnRtnOrder` 原始回调先持久保存，再由 Broker 依据账号、原登录
FrontID/SessionID 与 OrderRef 关联固定请求。价格、开平、合约和数量必须一致；
交易所 OrderSysID 及来源引用与 OMS 报告在同一事务内固定。冲突保留原始证据，
不推进本地订单；只有 OrderRef 相同的其他登录回报不关联。
未确认提交保持 UNKNOWN，可由后续确认推进；累计成交不是逐笔入账，
部分成交后撤单仍等待已发生的成交事实，不能立即释放全部预占。
重放幂等；启动及联合恢复重新核对源回调、OMS 父事件和交易所身份，
缺失关联也必须拒绝。这里不将订单回报伪装为费用或现金账本。

明确报单拒绝与撤单拒绝也使用原始回调及持久请求编号关联；原生结构 RequestID
与调用参数由同一持久序号产生。回调参数或错误回传结构中的编号必须指向同一
账号、操作与固定字段。报单拒绝不能覆盖已发生的成交；撤单拒绝仅确认该次撤单失败，
不关闭订单、不释放预占。缺少可验证编号的异步错误保留为待核对证据，不能猜测尝试。

原生 CTP 接收进程现在具有显式注入的私有订单通道；未提供通道的查询/接收
没有报撤单输入。核心线程仅向容量为一的队列提交，不同步等待 SDK；接收循环
非阻塞核对请求编号、返回和超时。原生线程在登录/查询/订阅确认后才处理请求，
检查账号、操作、编号与有界截止时间，拒绝重复或过期请求，再构造原生结构。
本地排队返回不代表 SDK 返回，更不代表柜台受理；原生返回码由 RequestSent 原始证据保存。
不确定传输封闭该通道并停止会话，不重发；报撤单拒绝本身不关闭行情/回报接收。
脚本化 SDK 的实际 spawn 进程已验证有界发送、撤单拒绝、超时和子进程回收；
没有外部柜台连接。管理端运行授权与当前账户事实检查仍需接入，不能直接调用此通道。

### 当前运行实例的执行限额同意

`live.ExecutionAuthority` 保存 owner 明确同意的账号、接收会话、合约、固定配置、
截止时间、单笔手数/金额和累计委托手数。会话固定其创建运行实例；新内核不能
对遗留 RECEIVING 行重新授权。授权与撤销不可改写，恢复后的许可属于旧运行实例，
不能自动生效。维护客户端没有同意权限。

`admit` 在 OrderJournal 的订单/预占事务内核验当前同意、运行实例、会话状态、
合约、期限及限额，然后必须执行所属账户/风险的当前就绪检查。许可不证明
账户已核对，也不能覆盖市场、条款、未决委托和入账完整性要求。累计委托额度
按已持久承诺的数量消耗；拒单不自动返还，避免一次同意被无限重复使用。

独立 Live Web 通过拥有的 Protobuf 接口展示、提交和撤销许可，运行时接口版本为 5；
页面明确“同意限额”与“可以下单”的区别。管理端可请求撤销已有订单；固定预算页面的一手 SimNow 空仓开仓已接入当前账户/风险与持久订单事务，完整持仓后开平仓与跨日准入继续实现。

### 成交发生时间与接收时间

2026-09-11 再次核对 NautilusTrader latest Architecture / Event Sourcing，
继续采用不可变事实重放及接入边界，未引入运行依赖。Northstar 的
`accounting.fills.FillFact` 同时固定 `filled_at` 和 `available_at`；
适配器必须提供明确 UTC 接收时间且不早于成交时间。账户按可得顺序消费，
不同合约的迟到回报不因另一个合约先发生而被拒绝。每个合约保留发生顺序，
倒序成交及结算边界冲突要求核对，不能事后重排 FIFO 来隐藏缺失事实。
模拟器目前在执行事件可得时确认成交，两种时间相同，不能冒充 Tick 级实际成交时间。
Research 会话实现版本同步为 20；订单日志和研究账本恢复读取完整的新事实格式。

### 成交与费用的分别确认

成交事实的费用可以明确为未知，不能填零。`Account` 立即保存成交数量与 FIFO
计价，但只要存在未确认费用，现金与组合估值接口就拒绝提供可用于新增风险的余额；
检查点明确返回未知现金，并列出待确认成交。实际已知费用、平仓与结算仍可记账，
不会因为等待另一笔费用而丢弃已经发生的事实。

`FeeFact` 固定费用身份、币种、总额、明确覆盖的成交集合、发生/可得时间及来源。
同一费用只扣一次；不得覆盖已计费成交，不向每笔成交猜测分摊金额。部分覆盖后仍
保持未知现金；明确核实的零费用是一笔事实，不能由缺失数据自动产生。
费用修订通过 `supersedes_fee_id` 指向当前收费事实，保持同一成交集合与币种，
以新的非负总额替代旧总额，只将差额计入账户；负差额表示退款。每笔修订有独立
身份、来源和因果时间，拒绝缺失前项、分叉、倒序及范围变更，旧事实永久保留。
OMS 在同一写事务保存修订和账户变动，恢复按事件顺序校验完整修订链。退款不会
自动解除此前超预算留下的冲突，也不改变成交事实。当前修订覆盖独立确认的
聚合费用，不支持更改成交内已固定的费用或用累计 Commission 猜测成交覆盖。

2026-09-12 对照 NautilusTrader `latest` [Execution](https://nautilustrader.io/docs/latest/concepts/execution/)
的执行事实与历史修正边界：Northstar 采用保留原事实、显式修订和重放核对，
费用差额与订单预占由各自所有者在一个事务内协调；未引入其运行库或照搬成交撤销规则。

OMS 对已成交但费用未知的手数保留手续费预算。确认费用、对应订单预占释放及
所属账户扣账使用同一 SQLite 写事务，任一失败共同回滚。费用超过原覆盖预算时
仍记实际扣费，同时将关联订单保留为待核对；成交价格或已知费用越界也不丢弃事实。
重启逐笔核验完整费用覆盖与所有订单关联，缺失关联不可当成已完成。Live 订单页面
分别显示成交与费用待确认手数，成交终态不表示现金账本已完整。

CTP 的累计 Commission 观测本身不能创建 FeeFact，必须先由 Broker/Accounting
核实账户范围、执行覆盖与核对截面。这项共享账本能力不宣称外部资金核对已经完成，
也没有开放从页面输入一个费用数就修正生产账本的通道。

### CTP 逐笔成交与订单/账户的共同提交

2026-09-11 核对 vnpy_ctp main 的 `onRtnOrder` / `onRtnTrade`：以交易所
OrderSysID 关联委托再发布成交。Northstar 借鉴此边界，额外固定账号、环境、
交易日、交易所和已验证的本地请求；不依赖进程内字典恢复身份，也未复制网关实现。
参考：https://github.com/vnpy/vnpy_ctp/blob/main/vnpy_ctp/gateway/ctp_gateway.py 。

`broker.execution_fills` 接收已持久保存的原始回报，仅在交易所订单身份已确认时
关联本地订单。先到的成交保留原始账户证据，后续订单身份建立时本地补处理；
未知外部订单不猜成本地订单。首次匹配固定成交可得时间，后续重复接收只增加
来源证据；经济字段冲突保留原始冲突回报并停止推进，不修改先前成交。

匹配后的 OMS 逐笔数量、费用待确认预占、CTP 成交关联及账户回报处理进度
共用 SQLite 写事务。账户失败共同回滚，原始接收记录仍在；策略暂停不丢弃
实际成交。启动和联合恢复核对原始回报、交易所身份、OMS 事件及所属账户的
完整固定前缀，缺少任何关联不能宣称完成。

当前原始 BrokerLedger 仍只证明逐笔持仓事实；它不是已经核对的现金账本。
CTP 成交的费用保持未知，完整现金核对与当前有效条款仍是开启执行的前置条件。
这些本地检查不替代真实 SimNow 接入及明确授权的非空交易验收。

研究报告从逐步核对后的权益轨迹生成按交易日/月份的观察区间收益、20 个已观察交易日滚动收益和回撤区间。夜盘按显式交易日归属；缺日期不补零，未恢复回撤保留至最后观察，资本非正后的比例留空。2026-09-11 查阅 [QuantStats stats.py main](https://github.com/ranaroussi/quantstats/blob/main/quantstats/stats.py) 的分期复合收益与回撤报告思路；只参考职责，Northstar 使用现有 Decimal 账本，不新增依赖或复制实现。月份和日末都是输入覆盖内的最后观察，不冒充完整收盘结算；不推算年化、夏普或完整交易日历。

### 恢复时重建账户观察与持仓核对

柜台查询的完整性、字段投影和权限状态先从原始回调重建，拒绝修改投影后重新
计算哈希的结果；账户基线、基线比较、持仓入账和持仓比较再从保留的查询或流前缀重新推导；
哈希只证明字节对应，不能替代这些业务核验。`position_projection` 由入账与恢复
共同调用，重新解码成交、去重、检查问题、计算今昨仓，并重算独立查询的差异。
来源范围、账户、合约和因果时间一并核验；不能通过同时修改 JSON 和哈希隐藏
成交、价格、持仓差异或把只读观察改成执行许可。恢复仅验证，不修复来源和目录。
资金字段以固定顺序比较，避免数据库 JSON 对象的键顺序影响跨存储恢复。
这仍是同日、初始空仓的柜台观察和持仓核对，不代表完整资金或跨日结算已完成。

### Live 柜台事实的共享 FIFO 投影

`BrokerLedger.context` 从经重建验证的初始空仓基线、成交前缀及本机合约信息，
调用 `accounting.broker_account` 转换成明确的外部 `FillFact`，复用 Research 的
`Account` 计价；外部订单身份不伪装成本地发送尝试。行情元数据以 `Instrument`
值交付，不把 ORM 行交给计算模块。读取不修改原始成交或另存一份可变账户。

工作台通过 `BrokerAccountProjection` Protobuf 显示费用前已实现盈亏与费用待确认
笔数，Decimal 金额以字符串传输。当前 CTP 成交没有逐笔费用、完整资金流和结算
覆盖，因此现金始终不提供为可交易余额，总费用保持未知；计价失败也不丢弃柜台事实。
该投影只补齐共同计价调用，不是当前账户就绪证明或完整跨日生产账户。


### 柜台查询解释与持久事实（2026-09-13）

`broker.query_projection.project_query` 是首次查询保存和冷恢复共用的纯解释入口，
从复制后的原生回报计算身份、请求完成情况与账户活动不确定性，不读取数据库。
原生接收与重建使用同一份有序查询计划，避免新增查询只修改一侧。
`BrokerRecords` 负责不可变绑定、来源完整性及持久化；解释完成不等于账户核对完成，
也不授予发送权限。后续会话内查询复用该边界，不通过另一次登录伪造当前账户快照。

查询身份必须关联正确方法、原生成功返回和唯一结束回报；缺失、空回复、
重复结束或请求复用保留不确定性。持续接收的账户推进与冷恢复也要求明确的
登录结束标志，不以仅包含账号字段的回报建立 READY。该判断只是身份证据，
不能代替实时账户、持仓、资金及未决订单的核对。


### Data Hub 单一下载入口（2026-09-13）

Data Hub 上游下载仅由 `data_management.tushare` 自动同步。移除旧的手工 SHFE
日线 HTTP 客户端、审批/恢复服务、专用配置与四张关联模型，质量评估不再检查
不存在的旧下载父记录。当前数据库基线直接创建这一种结构；不保留旧下载实现。
标准化、来源原文、逐行处理证据、版本修订、质量评估和不可变研究发布继续保留。
导入完整性规则更新为 `import_integrity_quality/3.0.0`，计算指纹、数据库约束
和快照质量引用共用该版本定义，旧规则不能被标记成新规则的评估结果。
本地代码清理没有操作个人部署或删除已存行情文件。


部署提供显式 `purge-host <应用> --yes`，按应用选择主机，清除该主机全部项目部署与本地事实。
它不是交易撤单/平仓指令，不操作外部 NAS；NFS 客户端先卸载，失败不删除文件。
共享 Docker/主机依赖不属于项目卸载范围。实现与使用边界见 deploy/README.md。

2026-09-13：#18 沿用已有固定清单流程，将原先写死的 1m 改为显式 1/5/15/30/60 分钟。周期随原始声明、DataSeries、快照和 Protobuf 明细固定，完成/可得时间及质量网格按该周期计算；Research/Paper 不要求同时存在其他周期。此层只接收明确 BAR_START 且完整连续的时段，不替 Tushare 推断标签或跨休市分段规则；供应商响应的真实时间装配仍待验收。无新增依赖、重采样或跨周期比较。

2026-09-13：Data Hub 的 `/api/research-inputs` 仅从已发布 Tushare receipt 准备研究输入，核对固定 manifest/Parquet/原文身份、合约和官方周期。显式固定完整时段与经核实的 BAR_START/BAR_END 解释及依据，只允许 FINAL_REVISED；不推断历史首次可得，不丢弃时段开盘处无法解释的结束标签。转为既有内部 CONVERTED_CSV 并引用原始 TUSHARE_RESPONSE，持久排队交独立 worker 处理，关闭 API 不影响已受理任务。该接口不接收上传行情、不下载、不改变全市场同步配置。真实时标/交易日历及条款证据仍独立验收。

### 已识别柜台资金流水（2026-09-13）

参考当日 NautilusTrader 官方 Event sourcing 文档的有序事实/可重建投影边界；
核对锁定的 ctpwrapper 6.7.13 官方分发包 CTP 头文件中 TransferStatus、
BrokerRepealFlag、BankRepealFlag 与 FutureRepealSerial。未增加依赖。
共享 Account 接受具名入出金及一次性精确冲正，现金恒等式包含净资金流，
成交、补记费用和跨日结算均复用它；资金流不计为策略损益。
Live 在原有 SQLite 回报入账事务内保存流水身份、原始来源和处理进度，
重启从同一来源重算。只确认同账户、同日、成功、零显式转账费用、基准之后
的回报；冲正必须引用已知未冲正流水并具有完成标志。重复回报不重复记账。
未知费用、缺失原流水、断线覆盖或错误身份保留问题，不用累计余额差倒推流水。
页面显示已识别净额/笔数；完整费用、连续资金流覆盖与跨日外部核对仍未完成，
因此不把这些投影当成可交易余额或开启发送的依据。测试为合成回报与本地重启，
不代表真实 CTP 入出金验收。

资金流水明细通过 Accounting 所属 `CashFlowFact` Protobuf 传输，金额保持十进制字符串，
原流水为空与明确冲正关系分别编码。Live 的资金页只读显示发生/接收时间、金额及来源
回报链接；不增加人工修改账本入口。2026-09-13 再核对 NautilusTrader 官方
Execution reconciliation 指南：借鉴保留来源和先核对再恢复的边界，不采用其合成
缺失成交来修齐经济事实的路径，也不以异步缓存替代发送前持久记录。

### 柜台结算原文接收

2026-09-13 核对当前锁定的 ctpwrapper 6.7.13 sdist（PyPI SHA256
`94be58b8360e26f6c3b57f5a991a6c5bdac0f7707234e41ceb69c42c006e778e`）中
`QrySettlementInfoField`、`SettlementInfoField` 和 Base 的 GBK 解码行为。
沿用 NautilusTrader Event Sourcing 的来源保留边界，不添加运行依赖或照搬账务解释。

现有只读查询可明确指定 `settlement_day`，在同一固定请求中增加
`ReqQrySettlementInfo`；不默认查询，不扩展持续接收范围，不调用结算确认。
原始正文按 CTP 500 字节分段保存为 Base64，先校验成功请求与唯一结束、账户、币种、
日期、结算编号及连续序号，再拼接解码并计算原始字节 SHA256。中文跨段不会被逐段
解码损坏；缺段、冲突、错误编码或未返回保留原始回报并明确不可用。

同一 SQLite 原始查询存储负责持久化与冷恢复，不建立第二个结算数据库。
Live 独立页面通过所属 Protobuf 展示原文及内容身份；文本转义展示，不解释为 HTML。
结算文档可能含个人信息，只通过既有工作台身份认证访问，不写入应用日志。
收到原文不代表已核实其版式、成交覆盖或费用含义；仍须确认真实来源样本后才能
生成 FeeFact/SettlementFact。累计 Commission 仍不能直接修改共享账户。

### 原接收核心处理撤单

2026-09-13 核对 NautilusTrader latest Execution 的命令路由及 HALTED 状态：
撤单与查询可直接进入执行组件，暂停新增风险不等于停止管理已有订单。
Northstar 保留同一 `TradingKernel`，启动前登记所属命令端点，行情与撤单共用
单核顺序和故障边界；控制响应不会发布为行情完成事件。

Live 工作台选择当前接收会话后，运行时先保存带操作者、实例及期限的控制命令。
有界队列只将请求递交给原接收线程，HTTP 不调用 SDK。未开始的超时请求明确取消，
不在恢复后补发；已经开始而结果未知仍按既有命令身份查询，不重试。
原核心检查当前所有权、原 CTP 账户/环境/交易日/合约及已验证登录，
在 OMS 与 CTP 同一 SQLite 事务中保存撤单身份后，才允许原生通道发送。
发送前再次检查所有权和原请求期限。原 OrderRef/FrontID/SessionID 不重写。

缺接收器、忙碌、错订单和未解决的先前撤单返回明确拒绝，不为这些请求另建连接。
策略暂停与未核定现金不阻止撤销同账户既有订单，但仍不允许新增风险。
撤单请求、IPC 返回和柜台撤单确认分别保留，预占仅由订单事实推进。
当前仅开放 SANDBOX 既有订单撤销，新增报单及完整账户准入尚未接通。
新的 TD 登录先清除上一连接的撤单就绪状态；只有该接收流程再次完成对应订阅后才恢复，
不能因丢失断线通知而继承旧连接能力。幂等启动与状态读取共用可重入的本机管理锁。

### 统一账户事实链

2026-09-13 再次核对 NautilusTrader latest Event Sourcing 的有序事实、重放和缓存边界。
Northstar 在所属 SQLite 中保存 `account_journal`，由现有账户入账事务写入，
不增加异步账户权威或独立数据库。每个批次固定原始账户证据身份/哈希、连续序号、
前序哈希、合约经济属性及计算后的校验投影；不可修改或删除。

`FillFact`、`FeeFact`、`CashFlowFact`、`SettlementFact` 共用这一事实链和共享 `Account`
规则。费用未知仍不能读取可用现金；收到结算不伪装成平仓。固定前缀重放不查询柜台，
新合约不能修改已登记合约属性，冷恢复比较事实与投影并核验柜台来源。
已有柜台成交/资金流水已在原入账事务中接入，OMS 失败会共同回滚；原始回报仍独立保留。
不支持的经济含义明确未入账；已经入账的事实链损坏必须报错，不能降级为普通未知状态。

该账本具备费用与跨日结算的持久处理规则，但真实结算正文到事实的供应商解释仍需独立验证。
没有完整外部覆盖与核对时，工作台仍报告资金不完整，不因此开放新的订单发送。

### 费用扣账与订单预占的统一提交

2026-09-13 再核对 NautilusTrader latest Execution 的执行事实与账户边界，
Northstar 将已核实的 `FeeFact` 交给 `OrderJournal.confirm_fee(account_id=...)`。
该操作直接核对所属账户与 OMS 的同一成交经济事实，将费用/修订写入账户事实链，
并在同一个 SQLite 写事务释放对应手续费预占。删除可空实现的费用扣账回调，
测试也使用真正的 Account 重放，不再维护测试专用现金表。

费用首次入账和修订固定同一账户，重复调用核验原有扣账而不重复写入。
恢复双向核对账户费用来源、OMS 费用与覆盖事件；任一侧缺失均拒绝恢复。
费用超预算仍记真实费用并保留风险冲突，退款不自动解除冲突。
这是已确认经济事实的持久操作，不能从累计 Commission 推导收费，
也没有新增供页面手工填写费用的接口；实际结算版式和来源覆盖仍须核实。

### 已知事实与新增风险分别处理

OrderJournal 在新开仓事务中检查整个实例账户的 UNKNOWN、事实冲突和未确认费用，
不能换一个合约绕过已有问题；同合约仍只允许一个未决目标订单。
这项底层约束不替代每次当前账户/市场/条款的 Live 准入。撤单及确认回报仍可处理。

原始接收或核对存在历史异常时，已识别、合约/交易日/投机范围明确的新成交和资金流
仍尝试按原账户事实链入账；不能因为账户当前不允许交易就忽略事实。
异常原因保留，工作台不因此宣称完整资金或允许下单。缺少合约、未知开平、未支持的
套保类别或尚未结算的新交易日不会被转换成普通投机成交。

恢复不仅核对每条柜台来源是否入账，也反向核对每个金额批次的来源所有者。
当前 Live 金额批次只能来自已核验的柜台账本，或由 Execution 双向核验的已确认费用；
完整哈希链和可重放余额不能代替柜台来源。来源缺失的额外金额使恢复失败。

同批柜台回报包含暂不支持的合约、套保类别或开平类别时，已明确识别且可计价的
成交仍进入同一来源批次的账户事实链，金额覆盖标为 PARTIAL；原始记录和未知原因
完整保留。部分入账不提供可用现金或执行许可，恢复重新推导相同的事实子集。
若已支持的事实本身无法按账户持仓应用，整个金额批次回滚，原始回报仍保留。

2026-09-13 核对 [上期所螺纹钢业务细则](https://www.shfe.com.cn/regulation/exchangerules/productrules/202512/t20251231_829962.html)
及 [交易时间](https://www.shfe.cn/services/calenderandholidays/tradinghours/)，确认 RB 合约
单位 10 吨/手、最小价格变动 1 元/吨及分段日盘。Tushare [ft_mins](https://tushare.pro/document/2?doc_id=313)
明确五个原生周期，但该接口说明不足以核实历史首次可得时间和夜盘开盘标签。
留存 RB2610 的 15 分钟数据按显式 BAR_END/FINAL_REVISED 假设通过日盘装配；
夜盘 21:00 的额外记录仍拒绝完整 Bar 准入。未将此假设提升为已确认供应商时标，
也未用当前合约最低保证金替代历史账户的有效保证金与费用。

恢复的金额来源检查在完整重放之后使用唯一来源索引核验对应批次，避免每个来源都
重复读取整本账。Execution 的费用审计在同一只读事务内，每个账户重放一次并建立
临时费用身份索引；检查结束即丢弃，不进入交易授权或替代持久账本。完整重放仍
核验全部事实、顺序和投影。新增金额写入仍采用现有完整重放路径，未据恢复优化
宣称实时写入成本已恒定。

接收进程明细从自身留存回报重建首次启动查询，截止于第一次行情订阅结束响应。
读取绑定固定接收前缀，核对逐条顺序和内容身份；之后的成交、断线或重连不会改写这份
历史观察。未完成、柜台错误、损坏和读取上限分别明确呈现，不回退到启动前查询。
它复用柜台查询投影，不新增 SDK 连接、账本或账户权威；完整查询仍不是原子账户快照，
不建立当前资金覆盖、完成恢复核对或授予发送权限。版本信息未作为流回报保存时保持未知。

2026-09-13 再核对 NautilusTrader latest Execution / Execution Reconciliation：
新订单与撤单走各自的准入路径。Northstar 在订单/预占事务内核对接收与处理前缀一致，
原始回报已落盘但尚未处理时，不使用旧账户观察继续新增风险；先拒绝新单，保留原始事实
供恢复处理。该检查补充当前账户/行情准入，不能代替它，也不阻止已有订单撤销。

同一事务还直接核验本接收进程首次查询的原始前缀，禁止用另一连接的成功查询替代。
首次查询后出现重登录、断线或心跳异常时，旧查询保留作历史证据，不能继续作为该会话的
执行起点；即使缺失断线回调也检查新登录。当前账户/行情/风险检查仍是另外的必需条件。


### 行情身份贯穿计算与恢复（2026-09-13）

参考 NautilusTrader [Data / Bar types](https://nautilustrader.io/docs/latest/concepts/data/#bar-types)
当日 latest 文档：行情属于明确标的，Bar 类型同时区分标的、周期与来源。Northstar 的
`MarketBar` 强制携带规范合约 UUID；现有 `BarStream` 继续绑定周期和固定来源。
DataEngine 在接收与预热时核验合约，MarketWindow 拒绝混合合约；研究输入、检查点与
模拟成交使用同一身份校验。发布的 Parquet 与检查点保留该字段，数据适配器在业务边界
提供明确身份，不由调用者根据价格或订阅名猜测。Tushare 官方各周期仍独立使用，
不增加跨周期比较或重采样功能。不引入框架依赖或第二套行情模型。


### 账户展示与账本版本（2026-09-13）

参考 NautilusTrader [Portfolio / Python query boundary](https://nautilustrader.io/docs/latest/concepts/portfolio/#python-query-boundary)
当日 latest 文档的独立账户读视图。Northstar 管理端从同一次账本读取重建账户，
返回序号与内容哈希；当前柜台前缀之外的已确认费用及修订可进入该读视图。
柜台持仓/资金事实来源必须与该账本版本一致，读取期间新增柜台前缀时返回不可用，
不混合新旧状态。固定历史重放仍按原来源截止；视图不持久保存另一份账户，也不进入
发送热路径。已确认费用可展示，但累计 Commission、查询成功或投影一致都不等于
外部完整覆盖、可交易余额或执行授权。

### 接收连接内账户刷新（2026-09-13，本地批次）

接收进程复用已登录的 CTP 连接执行原来的七类只读查询。工作台请求经持久命令、
所属运行实例和单线程内核进入有界通道；原生查询等待期间继续处理撤单，查询 UUID
防止重复提交。刷新先查询合约/条款/订单等，最后查询资金，避免其他查询等待让资金观察过期；
仍保留非原子窗口与期间账户活动拒绝。查询请求身份与订单请求身份分开，超时/断线保持不完整，不新建连接。

查询窗口保留原回报序号与内容哈希，显式引用经过核验的启动会话；不会把旧查询行
混入新结果或重新编号成一次独立登录。中途重登录、缺项、损坏和回报错误均显式拒绝。
完成七类响应也只是非原子的固定观察，尚需账户与订单核对；不提供发送授权。
沿用上述 NautilusTrader Execution/Reconciliation 的职责划分，不增加运行时依赖。

同日重新查阅 NautilusTrader 官方 `latest` Execution Reconciliation：Northstar 继续
采用订单/成交/持仓分别核对、明确报告覆盖范围的原则；保留未知历史，不合成缺失成交。
账户刷新中或失败时，同一订单事务拒绝新单，不以旧账户检查代替；查询窗口的接收时间
回退同样拒绝。核对完成与同意限额仍是不同事实。工作台刷新保留正在查看的标签。

原独立查询与接收窗口复用 `account_reports` 的资金解释规则，核验唯一账户回包、
账户/币种/结算身份、精确金额和查询期间活动。独立查询引用 batch，接收窗口引用
stream/query/原序号及哈希；不制造虚假独立 capture。该解释不修改资金，不把
累计手续费当作逐笔费用，也不把 Available 展示值升级成风险许可。

结束的接收查询可按 stream/query 身份独立读取；后来查询不改变旧窗口。开始身份重复、
缺失来源或校验损坏拒绝读取，未结束窗口不提供固定结果入口。API 与工作台使用同一
只读重建路径，仍由原回报存储拥有事实，不另建可修改账户快照表。

已完成计算中的领域 `UNKNOWN` 与命令执行结果未知分开：LiveClient 仅返回持久命令
`COMPLETED` 的业务结果；未确认命令通过 HTTP 503 返回。浏览器展示已保存预算的未知条件，
不因该领域状态锁住后续操作；断线、无效回包和 503 保留固定命令身份，继续禁止盲重发。

2026-09-13 再查阅 [Nautilus Execution](https://nautilustrader.io/docs/latest/concepts/execution/)
（latest）：沿用新报单经过风险、平仓必须检查减仓数量、撤单独立处理的分工。Northstar
区分仍在工作的订单和终结后尚未确认的手续费；后者保留费用预占并阻止开仓，不当成
仍占用同合约执行槽位的订单。平仓仍需所属账户/今昨仓/授权检查，UNKNOWN 的平仓发送
继续占用槽位，不重复下发。未复制实现或增加依赖，也未据此开放最终柜台报单入口。

### 同一接收核心的一手开仓（2026-09-13，本地批次）

再次查阅 [Nautilus Execution](https://nautilustrader.io/docs/latest/concepts/execution/)（latest，2026-09-13）：
采用“新单先经风险、普通拒绝保留运行、撤单独立路由”的职责边界；无新增依赖或复制代码。
`live.opening_execution` 将固定预算和 owner 执行限额送入既有接收核心的同步端点。
当前仅准入同一 SimNow 连接、同一交易日、已入账的空仓账户的一手普通期货限价开仓。

最终准入在订单事务内重查固定查询身份、5 秒内资金/行情、完整启动与刷新、
账本固定切面、持续账户入账绑定与追赶进度、无历史成交/费用/资金流/结算、
全账户无未结委托、账户范围和金额一致。
新的完成 Bar、暂停后恢复及关键报价变化使旧目标不可继续使用。
任何未定事实、其他合约的未结委托或权限/时窗问题都拒绝新增风险；拒绝不停止接收。
预算的 WITHIN_BUDGET 本身仍不提供发送权限，也不当作实际费用或保证金事实。

固定预算/查询/账本/最新行情身份及检查时间，与 CTP OrderRef、领域订单、授权引用、
发送尝试和预占在同一 SQLite 事务提交，然后才交付已有原生通道。
恢复按发送时固定账本序号重放并核对预算、查询和行情引用；后来的成交不会替换原准入依据。
SDK 返回和业务 UNKNOWN 均不表示成交；持久命令的重复请求返回原结果，不二次发送。
网页明确操作一手开仓，必须选择当前许可；启动接收、计算预算、保存许可都不自动下单。
`order_sending`/`cancel_sending` 展示已拥有传输通道的就绪能力，不表示某张订单已经获准。
完整平仓准入、费用/资金/结算覆盖、重启重新核对和真实柜台往返仍按 #31/#32/#33 验收。

### 同一接收核心的明确平仓（2026-09-13，本地批次）

复核 NautilusTrader latest [Execution](https://nautilustrader.io/docs/latest/concepts/execution/)
的 REDUCING 与 HALTED 分离：Northstar 主动 PAUSE 阻止开仓，但允许经过持仓核对的平仓；
故障暂停、停止、会话中断仍拒绝发送。两者均保留许可、时限、账户所有权和原生通道检查。
不复制其净持仓规则，SHFE 使用已确认的多空今昨仓。

首轮平仓引用同一连接的一手已确认开仓、本地账本和新的完整账户查询；
核对一笔成交、一手今仓、柜台终结订单及新报价，按当前平今手续费率计算上限。
显式价格与执行授权由用户提交，不接受用户填写持仓或余额。
费用预占不等于实际费用，未知开仓费用不按零入账，也不阻止已核对数量的受限平仓。
持久保存核对依据、平仓数量预占与 CTP 身份之后才发送；确认成交后才减持仓。
恢复核对发送时的固定账本切面、原开仓、查询和报价，不重新发送。
该边界尚不涵盖连续开仓、跨日平昨仓或完整费用/结算核对；合成开平回报不替代外部验收。

### 独立健康通知（2026-09-13，本地批次）

查阅 NautilusTrader latest [Architecture](https://nautilustrader.io/docs/latest/concepts/architecture/)
的核心/外部生命周期边界，以及 Freqtrade stable [Webhook](https://www.freqtrade.io/en/stable/webhook-config/)
的独立通知出口。沿用现有 `live-monitor`，不在交易线程发送通知，不增加通知服务或依赖。

可配置 HTTPS 接收端及独立 Bearer token；只传实例、运行身份、故障代码和观察时间，
不传账户/委托细节、柜台或内核凭据。接收方按 event_id 去重，成功保存后回复 2xx。
单次网络超时 2 秒，不跟随重定向或读取接收端响应正文；失败每 5 秒重试同一事件，
重复失败只记录一次不含地址/凭据的诊断。确认送达后才推进已通知状态。

待送事件只占一个内存槽，等待期间不生成新观察；这是当前状态通知，不是完整故障审计。
接收恢复后继续查询当前内核。监控重启产生新 monitor_id/INITIAL，不承诺重放旧待送事件。
外部接收端必须按 observed_at/心跳检测迟到与失联，不能用迟到 HEALTHY 消除当前失联告警。
实际子进程/HTTP 检查覆盖确认丢失、重复身份、故障与恢复；不代表个人主机已有接收服务。

### 报撤单拒绝与连接故障分离（2026-09-13，本地批次）

沿用 NautilusTrader latest Architecture 的业务拒绝与运行故障分离，
CTP 的 OnRsp/OnErrRtn OrderInsert/OrderAction 错误由具体订单发送尝试解释。
`broker.events` 只判定同账户的协议类别；`execution_reports` 仍核验原始请求身份、
字段和来源，在 OMS 事务保存拒绝。Live 仅对成功匹配的拒绝继续接收；无法匹配则暂停，
错误身份、字段冲突、断线和心跳异常保持故障路径。

账户进度与固定查询使用同一类别规则，不把普通报撤单拒绝误判为资金连接失效。
查询 COMPLETE 仍不等于账户核对或交易准入。撤单被拒不改变原订单状态、不释放预占；
后续确认成交仍按实际回报入账。恢复从原始回报重算分类，不创造费用或重发订单。

PAUSE 是停止新增风险的操作，不能解除此前故障：已有暂停原因保留，
故障只能沿原有恢复/重新核对路径处理。平仓准入及持久化后的发送前检查
都拒绝 connection_error，不能靠重写显示原因或再次点击暂停获得发送权限。

2026-09-13：对照 [Tushare fut_settle 官方字段说明](https://tushare.pro/document/2?doc_id=141)，结算参数同步显式请求默认字段以及非默认 `offset_today_fee`、`exchange`。字段选择随任务参数参与身份，分片、重试和不可变发布保留该选择；HTTP 发送时提取为顶层 `fields`。响应缺少要求的列时保留原文并阻止发布；列中的 null 仍表示未知。供应商原始费率不推断单位，也不作为账户实际手续费或有效柜台条款。没有新增供应商请求或运行依赖。

结算参数使用 Decimal(38,12) Parquet 列：非空值必须精确、有限且在范围内；结算价为正，手续费和保证金率非负。null 保持未知，不填零，不把百分比或万分比自行换算成账户费率。数值拼写改变不会制造经济修订，原始响应分别保留；质量/规范化规则分别升级到 `tushare-response/6`、`tushare-numbers/2`。

结算参数沿用 Data Hub 的固定范围读取、版本选择、逐字段修订比较和现有合并入口；工作台单独展示结算价曲线，不构造 OHLC，也不将供应商费率默认为账户有效条款。空值断开图线，精确明细继续使用十进制字符串。

2026-09-14：再次查阅 [Nautilus Data latest](https://nautilustrader.io/docs/latest/concepts/data/)，沿用标的、周期和数据时间的明确边界。Data Hub 的研究快照页通过所属 Protobuf 装配接口调用 `DataLibrary.assemble_research`，选择 2–32 个固定快照，继承已固定的来源、结算和条款；不提供手工行情或经济事实输入。请求重试和顺序变化复用同一内容身份，原子文件发布完成后才返回详情；跨日缺失经济依据仍由研究准入拒绝。页面不把装配成功等同于可交易或可信收益验收。无新增运行依赖或代码复制。

2026-09-14：按 Nautilus Data 的固定 Bar 周期边界修正因子准入：按 Bar 数计算的收益与区间位置因子消费调用方固定的原生周期，不再隐含要求 60 秒。时间专用公式仍可声明精确周期；输入周期必须为正整数，完成/可得时间与新鲜度检查保留。Tushare 的 1/5/15/30/60 分钟数据直接使用，不重采样、不增加跨周期比较。回归通过实际研究计算检查五种周期的成交和费用，并通过固定发布与 SQLite 逐 Bar 重启检查 15 分钟跨日结算。无新增依赖或代码复制。

2026-09-14：Tushare 合约目录发生新增或修订时，在接收事务内使范围规划失效；连续、品种和市场范围都依赖实际合约，重复响应不触发重规划，已存在任务按身份复用。对照官方 [ft_mins](https://tushare.pro/document/2?doc_id=313) 与[分钟数据说明](https://tushare.pro/document/1?doc_id=234)，不把“超过十年”转换成统一历史起点。超过近期窗口的空响应每 7 天复核，保持未覆盖；日历不完整仍走正常退避，不能冒充空响应或休市。供应商错误仅按明确文本分类，不回显消息或凭据，也不按 40203/50101 数字猜测权限。同步页优先展示受阻及等待任务和重试时间。


2026-09-14 Tushare 实机响应修正：以官方专属文档 [南华指数 468](https://tushare.pro/document/2?doc_id=468) 为准，接口仍为 `fut_index_daily`；实际服务要求 `ts_code`，按文档指数清单逐代码规划，包含综合指数和历史别名，不按当前期货品种猜代码。旧权限总表的 `index_daily` 不作为改名依据。[周/月线 337](https://tushare.pro/document/2?doc_id=337) 的 `trade_date` 与 `end_date` 分别留存；实机1995年行情存在2026年计算日期，不能要求计算日期早于行情标签。分片及浏览范围使用两者较早日期，支持未结束周期的期末标签，也支持历史重算；该范围键不代表首次可得时间，不自动赋予研究时点资格。响应质量规则升为7，旧原文通过重处理形成新固定版本。权限/必填参数错误按已观察到的消息结构脱敏分类；升级积分后仍以接口实际授权结果为准。

2026-09-14 历史范围采用已退市真实合约全生命周期，目录保留关联元数据。

### 批量部署与整套卸载（2026-09-14）

northstarctl 的所有操作均要求明确对象，具体应用或 all。all 按已配置应用展开，数据库归 Data Hub；
单应用生命周期不变。purge-host all 是显式删除系统数据的操作，包含已配置 Live，不能从普通部署推断授权。
所有目标持有部署锁并通过预检查后，先停止全部容器，再经 Data Hub 挂载清空项目 /quant 活跃行情，
最后按主机去重清理本地部署。单机 purge-host 保留共享数据；两者均不管理外部 NAS 服务或其快照。

### 首次历史补齐与日常更新调度（2026-09-14）

首次持久任务的创建时间（上海时区，沿用18点盘后边界）固定初始历史截止日，重启、暂停和配置修订不改变它。
不增加第二套任务或下载器；合约全生命周期按月从旧到新推进，同月按数据类型最近执行时间轮转，
类型内按区间从旧到新。新增截止日之后的任务优先，近期任务进入退避后立即允许历史补齐。
目录/日历优先准备，留存响应重处理仍不受供应商限频影响；权限、退避和任务去重规则保留。
进度以完整合约发布计数；内部请求统计供诊断，SPLIT不重复计数，WAITING/BLOCKED不算完成；最早未完成日期不代表此前覆盖完整。
目录尚在规划时明确总数会增加。此调度不保证供应商覆盖，也不增加请求权限或新数据来源。

### 数据起点证据（2026-09-14）

任务入口与领取器按已结束合约归属执行。合约仍按目录上市/到期范围规划；
指数不以当前合约目录的最早上市日期截断自身历史。每个数据集/标的分别从允许范围
按时间探测，分钟各周期互不推导。通过字段、范围和数值校验的响应将最早日期记录在
原有下载尝试的质量证据中，关联原文身份；覆盖校验失败也不抹去已观察记录。
任务详情展示当前最早观察日期，较早修订可将其前移，不能宣称已经证明供应商的绝对起点。
周报日期代表统计周范围，不能当成精确首笔成交时间。

首次有数据前的历史空窗口继续向后探测，90天复核；已观察起点之后的历史空洞7天复核，
近期空响应及日历不完整保持原有退避。均不计完成、不删除证据、不把一次空响应推断为
未上市或永久无数据。尚未发现有效记录的范围保持“探测中”，不编造起点。
已留存旧响应可通过原有重处理补充起点证据，不为填充新字段额外全量下载。

核对 Tushare 官方 `index_basic`（doc94）和期货指数（doc468）、分钟接口（doc313）：
指数基期/发布日期不等于供应商可下载历史起点，分钟“超过十年”也不是所有合约共同边界。
因此没有以指数发布日期、商品名称推算或统一供应商年限删掉可能的回溯历史。
来源：https://tushare.pro/document/2?doc_id=94 、https://tushare.pro/document/2?doc_id=468 。

2026-09-14 #47：同步进度标签进入同页任务筛选，数据类型/状态和页码共同定义请求；
通过 Protobuf `SyncJobQuery/SyncJobPage` 查询持久队列全部记录。每次计数和取页使用同一
PostgreSQL只读操作的REPEATABLE READ快照，按状态优先级、更新时间和唯一身份排序。
独立轮询中旧筛选的迟到响应被丢弃；筛选切换回第一页。概览/CLI仍可保留最近50条摘要，
工作台列表不从此摘要分页；没有新增修改任务状态或绕过重处理规则的入口。

## 共享存储与备份规划（2026-09-14）

目标布局：

```text
nas.local:/quant/
├── market/                       # 活跃的固定行情，不是备份
└── backups/
    ├── data-hub/<UTC时间-UUID>/   # PostgreSQL、原始响应、引用文件、恢复清单
    └── research/<UTC时间-UUID>/   # SQLite一致快照、研究产物、固定输入、恢复清单
```

Data Hub 的来源归档仍在本机 `/opt/northstar/files/source`，PostgreSQL 在本机
`state/data-hub/postgresql`；Research 的 SQLite、产物和临时计算仍在本机。
目标挂载为 `market/` → 两机的 `files/market`（Hub读写、Research只读），
各应用的备份子目录 → 所属主机的 `files/backup`（读写，业务容器不挂载）。
同机部署时备份挂载必须按应用分开，不能把两个不同导出覆盖到同一挂载点。
不在 NAS 上部署服务或运行 SQLite/PostgreSQL；Live 不依赖这些挂载。

备份由主机定时任务调用既有备份入口，采用独立批次。Research 使用 SQLite Backup API，
不能复制正在运行的主文件来代替一致快照。DB 快照与它引用的原文、固定输入、研究产物联合封存，
恢复前核对哈希、存储身份与实现身份，完成后才发布完成清单。不能只备份结果图表。
配置、工作台账户与供应商凭据需要单独加密；解密材料保存在备份之外，不与密文放在同一目录。
每日一次，保留最近7个每日成功批次与最近4周各一个成功批次；失败批次不挤掉成功备份。
仅完成清单通过验证的批次进入保留轮换。引用数据保留必须先建立引用保护，不能因旧备份到期删除在用行情。
NAS自身快照或异地副本由外部设施负责，同一NAS上的行情与备份不能覆盖NAS整体损坏。

**卸载规则**：`purge-host all --yes` 是清空所有配置目标的整套卸载，停止所有目标的写入者后，
同时清空项目共享行情与 `backups/data-hub`、`backups/research`，再卸载和删除本机数据/配置。
`stop`、普通部署与重新部署不清理数据或账户。指定单个对象的整机卸载不擅自删除另一主机仍在使用的共享行情。
不SSH登录NAS、不删除NAS服务、外部快照或其他项目目录；无法确认所有权、错误挂载或尚有使用者时明确失败。

**容量规则（已实现）**：原始归档默认无固定总量配额，不再使用10 GiB默认上限。
写入前按实际文件系统可用空间检查：至少预留 `max(1 GiB, 总容量5%)`，
剩余低于 `max(预留×2, 总容量10%)` 提前告警；无可用inode或触及预留时拒绝新增写入。
既有内容可继续读取、去重重用。无总量配额时写入不扫描全归档；界面容量读取只查询文件系统。
低空间不触发原始证据自动删除；清理必须走已有引用保护与保留业务。容量告警是扩容信号，不能承诺无限存储。
显式的归档配额和最小剩余字节仍可用于受约束环境，但部署默认不设置。

**实施状态**：当前部署仍把 `/quant` 直接挂到 `files/market`，备份目前仍在各机本地；
上面的分目录挂载、定时备份、加密和轮换尚未完成，不代表已有NAS备份保障。
迁移须先停写并联合备份，保留市场UUID和完整文件，把原市场内容移到 `market/`，
创建独立备份目录，再切换两端挂载并实测备份恢复。普通deploy不得覆盖非空本地目录或不同的现有挂载。
本轮没有执行远程迁移、卸载或重部署。

参考核对（2026-09-14）：[Nautilus Data](https://nautilustrader.io/docs/latest/concepts/data/)
沿用固定市场身份及来源边界；[SQLite Backup API](https://www.sqlite.org/backup.html)
采用在线一致快照；[PostgreSQL 17 Backup](https://www.postgresql.org/docs/17/backup.html)
沿用逻辑备份与引用文件联合恢复。未新增框架或数据库依赖；目录、保留天数和容量阈值为Northstar自身决策。

### 零成交日线（2026-09-14）

对已留存的2012年DCE/SHFE日线及复权日线响应，确认存在成交量0、成交额0、开高低价null、
收盘/结算价仍有值的记录。`tushare-response/11` 按供应商观测保留这些null，明确标记
`observation_status=ZERO_VOLUME`，不补零、不以前收盘或结算价填造OHLC。
仅日线/复权日线/周线/月线、零成交量、成交额零或缺失、开高低价全部缺失且收盘价有效时允许这一形态；
正成交量缺价、部分缺价、负值、非法数值仍隔离。有完整价格的零成交记录也标为ZERO_VOLUME。
该标记为本地派生，不能由供应商额外字段覆盖。固定研究行情装配拒绝ZERO_VOLUME，
不把参考收盘价解释为可执行成交；既有模拟成交量约束仍生效。规则修订产生新质量证据和固定版本，
旧原始响应及历史发布不变，可通过现有留存响应重处理完成，无需新增供应商请求。
核对[Tushare期货日线字段](https://tushare.pro/document/2?doc_id=138)及本机实际留存响应；
官方字段说明没有确认空值形成原因，因此不把保留参考价宣称为交易所特定结转规则。

同步任务列表的POST筛选属于只读查询，使用独立查询入口和CSRF验证，不写入浏览器未确认命令记录。
轮询取消、超时或失败不创建操作锁，也不能清除真正的未确认写操作。周/月线零成交缺价沿用
保留null规则；成交量非零而收盘超出高低区间仍隔离，不擅自修改供应商OHLC。

2026-09-14 再次对照 NautilusTrader Data latest 对 Bar 与结算/参考价格的分离：Northstar 不以结算价填造 OHLC。零成交、OHLC 全空而结算价有效的记录标为 SETTLEMENT_ONLY；它可供结算事实核对，不可作为可执行行情。
行情响应按行核查，任一异常行拒绝整个新响应的发布，不剔除坏行后发布好行。失败行号、原因和原始响应保留。此前已固定的部分发布仍保持其原始身份，但不能用来证明合约完整；Research 仍独立核对所选实际时段与有效条款。

同步调度按数据类型与历史/增量两条链路使用索引领取候选，只对有限候选排序；原文重处理仍优先，限频、权限暂停、同月轮转和单接收者事务不变。Data Hub 部署阶段通过同主机数据库管理账号幂等准备物理索引，worker 不执行 DDL，不改写业务身份；重启遵守已保存的目录刷新时刻，不强制重下载目录。日志记录规划、领取、获取、归档、校验、发布、提交和读取耗时，不记录凭据或原文。增加并发前先依据实际阶段耗时消除存储/查询瓶颈。

worker 在自己的生命周期内复用一个有界 HTTP 连接池，请求 token 仍按每次调用读取并仅放在请求体；关闭 worker 时关闭客户端。保留 HTTPS、禁用环境代理/重定向、响应大小和超时检查、持久限频及供应商退避，不以并发绕过接口额度。2026-09-14 hub.local 百万级任务队列下第一阶段实测102项约32.8项/分钟，旧样本约4项/分钟；连接复用后的173项窗口约41.4项/分钟、单次处理平均0.917秒。统计包含等待/异常结果，不等于可用数据发布数；样本窗口与数据类型不同，不将其作为长期吞吐保证。

### Tushare 接口额度与批量下载（2026-09-14）

按 [Tushare 权限表](https://tushare.pro/document/1?doc_id=290) 的已确认 5000 积分及期货历史分钟独立权限，总请求上限从项目旧默认 60 改为 500 次/分钟；各 API 单独持久保存下一可请求时刻，五种分钟周期共用 `ft_mins` 额度，周/月线共用其 API 额度。收到官方限频或 HTTP 429 时整个 API 退避，其他 API 仍可继续。额度不证明账号权限，权限拒绝不绕过。

[ft_mins](https://tushare.pro/document/2?doc_id=313) 单次 8000 行；[日线](https://tushare.pro/document/2?doc_id=138) 2000、[结算](https://tushare.pro/document/2?doc_id=141) 1600、[周/月线](https://tushare.pro/document/2?doc_id=337) 6000，各自保留上限。领取时合并同来源/合约/周期、参数一致、连续且从未请求的待下载范围，不跨初始历史/增量边界。优先取同周期最近32份完整发布响应的最大日均行数，按接口行数上限的90%估算批量预算；暂无样本时，分钟按每天600分钟除以原生周期估计。预算只是请求大小估计，不是交易时段或覆盖证据，实际密度变化仍由截断保护处理。其他单行/日数据按各自上限扩大范围，多行/日的品种报表保留原分片。范围受实际队列、合约生命周期限制，不为了凑满行数重复请求或补造数据。

对照 Nautilus Data catalog latest（2026-09-14）的固定来源原则，批量请求生成新身份，原未执行分片保留为已重新分片并指向批量请求。已经下载/发布/需要重处理的分片不合并；范围有间断时停止合并。返回行数达到上限或超过响应体积限制仍必须拆分，拆分重新激活必要的未执行子分片，不能把满额响应当作完整覆盖。原响应及已发布版本保持不变。没有新增供应商或运行依赖。

### Data Hub 多核流水线（2026-09-14）

Tushare worker 由一个监督进程与多个 `spawn` 子进程组成。监督进程推进持久规划、处理任务与合并任务；每个子进程拥有自己的数据库连接池和 HTTP 客户端，独立完成一个分片的下载、校验、Parquet 生成和留存内部响应。不同请求的网络等待和 CPU 处理可以重叠，不增加第二套任务队列或内存任务权威。每个子进程的 Arrow CPU 池为1，避免嵌套线程争抢；并行发生在多个 Python 进程之间。

对照 [Python multiprocessing 3.14 文档](https://docs.python.org/3/library/multiprocessing.html) 与 [PostgreSQL 18 advisory locks](https://www.postgresql.org/docs/18/explicit-locking.html)（2026-09-14 查阅；项目运行版本由自身依赖决定）：使用 spawn 避免继承连接与日志线程；短事务串行领取任务、合并范围和分配供应商配额，任务独立连接上的事务锁覆盖整个处理过程。只有取得已失去连接的任务锁后才恢复 RUNNING，不按超时抢占慢任务。已留存响应从其固定身份恢复，不重复请求供应商；最终提交继续检查 generation，并将 receipt、coverage 和任务结果放在同一事务。规划与目录更新另行串行，文件写入及备份继续遵守原有共享/排他维护锁。

监督进程按 CPU affinity/cgroup、可用内存、子进程实测 RSS 与数据库连接余量渐进扩容，内存不足时让多余进程完成当前任务后退出；存储容量仍由写入准入检查并明确暂停。所有进程共享持久全局/API 配额与退避，不能以并行突破供应商限额。子进程日志先按现有规则净化，再通过有界队列交给监督进程唯一文件写入器，避免争抢轮转锁。停止时先等待已领取任务，超过有界退出等待则终止子进程，后续依赖数据库连接锁与原文恢复；管理 Web 不拥有这一生命周期。

同日 hub.local 实测百万级队列的领取查询触发 PostgreSQL JIT：EXPLAIN 显示约470ms用于编译，实际索引探测/排序约12ms。按 [PostgreSQL JIT 适用范围](https://www.postgresql.org/docs/18/jit-decision.html)，仅在领取事务使用 `SET LOCAL jit=off`，不修改数据库全局配置；同机三次只读领取查询为29.8/16.9/16.2ms。已启动12个同步子进程，任务可同时处于下载和校验阶段；总吞吐仍受供应商额度、网络与持久文件写入约束，不以CPU满载作为成功标准。

### 不可变文件并行发布（2026-09-14）

对照 NautilusTrader Data catalog latest 的不可变来源原则及 Linux man-pages 6.19 的 [link](https://man7.org/linux/man-pages/man2/link.2.html)/[fsync](https://man7.org/linux/man-pages/man2/fsync.2.html) 语义：未配置归档总量上限时，各写入者持有共享文件门闩，并行写各自临时文件；内容写入、fsync、摘要核验完成后使用不覆盖的硬链接发布。同一摘要竞争时验证胜者的字节和类型，不覆盖损坏文件或符号链接。文件目录及其父目录同步后才返回，复用刚由其他进程链接的文件也执行目录同步，避免将尚未持久化的名称当作成功。保留暂存清理及同步，不改变固定内容身份或receipt/coverage事务。

配置总容量上限的归档继续使用独占锁执行容量检查和写入；删除始终独占，不能与任何写入交错。可用空间检查仍是准入时的文件系统观测，不能保证其他进程不会消耗磁盘；OS写入/同步失败不产生成功确认。备份与清理仍由数据库维护门闩冻结完整业务写操作。容量观测容许暂存文件在并发完成时消失，不把正常清理报告为故障。

增加按阶段的有界运行日志：锁等待/持有、文件写入及fsync、验证、链接及目录同步、暂存清理，以及Parquet编码/持久保存；不记录内容或凭据。优化前hub.local最近180秒517个任务：下载平均0.167秒、校验0.198秒、发布4.244秒，发布占累计阶段耗时约90%（包含等待），12进程未耗尽CPU/内存。发布优化按实际部署后新窗口验收，不以本地文件系统测试推断NFS性能。

共享锁上线后的稳定60秒窗口显示：锁等待约1.7ms，但临时文件清理约0.57秒/文件，共用staging目录的创建/删除成为新争用点。因此临时文件同样按摘要前两位分散到最多256个暂存子目录，各自创建、清理与同步；正式objects路径、内容身份和发布协议不变。容量观测统计暂存树中的实际文件，忽略空目录，拒绝符号链接；未完成的临时字节仍计入显式归档额度，不把它们当作成功发布。

### 合约工作台交互（2026-09-14）

按用户指定参考本机同花顺期货通 1.2.0（build 202608191330）的截图与辅助功能控件。
实查市场、自选、合约与自定义模板导航；行点击与周期切换的原软件联动未确认，不冒充已验收。
采用紧凑导航、对象列表与主图同屏、低频信息抽屉的交互原则，不复制代码或引入依赖。
三个 Web 共用紧凑可折叠导航，展开偏好按应用保存在本机浏览器；业务入口和事实所有者不变。
Data Hub 列表每个供应商合约只出现一次，展示中文名、代码、已有周期和发布窗口范围。
`exploration.discovery.open_instrument` 精确按合约和原生周期选择当前非空 receipt，
沿用固定文件验证和实际日期定位；未发布周期拒绝，SPLIT 和空响应不用于打开。
默认只加载最近一个发布的末尾最多31自然日，不能称为整个合约全历史；完整范围指本次固定查询。
明细、来源、字段统计和合并入口在抽屉，主图不随明细表滚走；手动日期筛选也在抽屉。
同标签页记住最近合约/周期，再次普通进入浏览页按当前发布重新打开；显式版本链接优先，
不保存账户、凭据或原始行情到浏览器。Research/Live 的领域布局继续各自演进，未接入行情盘口或下单新能力。


## 2026-09-14 完整已退市合约架构

组织及发布单位为交易所 → 品种 → 真实合约。只下载已结束合约：实际上市日、最后交易日和最后交割日
必须有效且顺序一致，最后交割日必须早于当前日期；连续序列不能作为真实合约。搜索最后交易日
不早于 2012-01-01 的历史合约，按最后交易日升序，同日按交易所/品种/代码稳定排序；跨越起点的合约
仍下载真实上市日起全部行情，不截断为 2012 年。按合约类型核验全部适用必需数据，包括结算参数；已确认不适用及独立研究序列不阻塞合约发布。
品种、连续及市场数据保持原生身份。

2026-09-15 核对 [Tushare fut_basic](https://tushare.pro/document/2?doc_id=135) 与
[Nautilus Data latest](https://nautilustrader.io/docs/latest/concepts/data/)：`delist_date` 是最后交易日，
`last_ddate` 是最后交割日，`d_month` 是交割月份。保留供应商原文和固定来源身份，不根据代码或月份
推算生命周期。供应商没有提供首次交割日时明确返回未知。生命周期由元数据派生，与采集/核验/发布
状态分离；缺失或冲突只能等待核实，不能据此拒绝并清理已有原文。未增加依赖或另一个目录权威。
行情及现有逐交易日采集截止最后交易日；交割/结算后续适用区间仍需历史有效规则证据，完整报告
明确保留该缺口，不能要求最后交易日后的 K 线，也不能将当前采集区间等同全部交割业务覆盖。
品种级历史规则版本与首次交割日的外部证据尚未接通，不能把当前交易所规则倒推应用到历史合约。
规划和实际领取均优先较早合约，旧近期队列不能越过尚未规划的较早候选；保留既有请求及来源身份。

`contract_data` 拥有持久合约采集、子请求关联、验收状态和不可变包目录。规划每次展开一个合约，
供应商限额所需的拆分/合并请求继承所属合约；请求窗口是内部执行细节。合约状态为采集、核验、
拒绝或已发布，下载成功不增加发布数。目录刷新只刷新发现，不自动重开已结束合约的请求。

正式产物为 `<exchange>/<product>/<contract>/<publication_id>.zip`：一个不可变文件包含固定清单、
Parquet 与来源证据。内容身份、精确输入和实现版本固定；写入核验路径、拒绝符号链接，原子落盘后
才登记发布。普通发现、图表、明细和研究桥接均要求完整合约发布成员身份，单个响应不再可直接进入。
旧固定包成员仍可读取，后台当前响应变化不能改写固定输入。联合备份核验包完整性；恢复由已备份
清单及精确原始对象确定性重建同一包，校验路径、大小和哈希。

被拒绝合约的原始响应仅在不存在固定版本、研究处理、上游依赖、重处理和备份引用时释放。
持有来源排他门锁，先持久化释放事实再删除核验过的文件；恢复重处理不能使用已释放原文。
锁被采集占用时本轮清理让步。用户另行明确授权本次清空旧 Data Hub 数据；账号、密码哈希、token、
配置和其他应用不属于此清理范围。普通拒绝清理不继承这次全量删除例外。

当前完整性报告仍阻止正式发布：历史分钟交易时段与供应商标签、周月及品种报表应有记录、
关联适用性证据尚未核实。未知不是确认缺失，不会凭空形成合格事实或触发原文清理。
测试中的预先固定合约包仅验证读取、不可变性和恢复，不代表供应商全生命周期验收。

2026-09-14 查阅 [Nautilus Data latest](https://nautilustrader.io/docs/latest/concepts/data/)
及 [Tushare 历史分钟官方文档](https://tushare.pro/document/2?doc_id=313)：采用标的身份与物理文件分离、
固定目录读取和来源保留原则。分钟接口单次最多8000行，内部有界请求不等于业务发布单位。
官方该页未提供覆盖全历史的时段及分钟开始/结束标签承诺，不能据示例认证整生命周期。
未引入参考项目运行依赖或第二个存储权威。


2026-09-15 合约工作台呈现：首页及同步主视图仅展示合约总数、合约状态和一行一个合约的列表。
交易所/品种/中文名称或代码/状态筛选及分页由 `contract_data.collection_query` 在同一数据库
快照中读取总数和明细，取消“仅前100个合约”的静默截断。各周期和结算资料进入合约验收抽屉，
原全局数据集/内部请求进度表删除。请求诊断移入独立抽屉，合约诊断通过持久归属关系筛选，
包含同合约关联的品种级请求；服务级诊断仅供技术排障。响应校验不标为发布；完整包发布
仍由原有合约准入决定。本次没有改变采集、质量、发布或数据保留规则。


### 2026-09-15 分钟异常归因与诊断证据

对照 Tushare HTTP 文档（https://tushare.pro/document/1?doc_id=130，查阅 2026-09-15）：
POST 的 fields 位于顶层，响应按 data.fields 映射 items；权限 code=2002 独立于 msg 识别。
继续参考 Nautilus Data latest（https://nautilustrader.io/docs/latest/concepts/data/，同日查阅）的
来源与市场事实区分，不增加依赖或改变原生周期。响应规则 /12 保持零价拒绝，
增加最多100条异常的校验前时间及数值白名单，防止规范化改变诊断中的源值。
原始文件依既有引用规则清理时保留有界诊断，API/UI显示清理事实；诊断样本不冒充完整原文。
合约原因优先列出具体异常数据集、请求区间和数量，已拒绝状态也定期复核。
真实只读复查 JD2608.DCE / 1min / 2026-08-01..26 返回4068条，
2026-08-26 15:00:00 原响应开高低收均0、vol=14、amount=661080、oi=0。
这是供应商返回值，不能作为可成交价；没有以填价、删异常行或强制放行替代完整性验证。
全生命周期时段与资料适用性、真实整合约发布仍未验收。

生产复查另发现拒绝来源清理的并行哈希连接耗尽 PostgreSQL 容器共享内存（32MiB段申请失败），
导致整个collector父进程重启。仅该清理事务使用 `SET LOCAL max_parallel_workers_per_gather=0`；
不修改全局数据库参数或清理条件。生产同一只读候选查询串行返回16项，单次约1.21秒。


### 2026-09-15 清理不阻塞采集调度

生产执行计划暴露来源/发布/重处理外键查找缺少物理索引，清理SQL反复扫描约8万条发布记录；
串行查询仍可能运行超过一分钟。依据 PostgreSQL 17 EXPLAIN 官方指南
（https://www.postgresql.org/docs/17/using-explain.html，查阅2026-09-15）添加定向索引，
用每个来源的一条合约归属替代多归属展开后全局去重。Nautilus Data latest（同日查阅）
仍只作为不可变来源/目录原则参考，不引入依赖或另一个存储权威。

候选发现不持有采集排他门锁；每次最多16个来源，在非阻塞取得门锁后重新验证生命周期、
全部所属合约状态、固定发布、研究处理、上游依赖、重处理和备份引用，才记录释放事实。
最终只读取候选哈希的保留清单，保护同内容别名及固定/合并产物，不物化整个目录。
SQL采用事务内2秒超时、串行执行；超时不授权删除。文件删除仍在排他门锁内并验证内容。

Data Hub worker监督一个独立 `data-retention` 子进程，每30秒尝试一次低优先级清理，
单独写retention日志。慢查询或失败只推迟该项维护，不阻塞父进程扩缩采集并发。
子进程随父进程退出，异常退出有重启退避；账户、供应商限频和原有删除权限不变。
CPU/总内存不添加人为比例限制，也不通过扩大共享内存掩盖无界查询。


### 2026-09-15 按合约类型确定采集和验收要求

用户确认清空 Data Hub 数据后按类型重新采集。`contract_data.requirements` 是下载规划、
验收报告与包输入选择共同使用的唯一策略，版本 `contract-requirements/1`；不再为每个合约
下载所有南华指数、主力/连续及复权序列。这些资料保持独立研究身份，本轮不另建独立采集器。

分类分别表达真实合约/序列、品种类型和交割方式，依据交易所、供应商品种代码、名称和
`d_mode_desc` 元数据交叉核对。DCE 的 L_F/V_F/PP_F 要求月均价名称和现金交割一致；
CFFEX 区分股指与国债，INE EC 区分航运指数，其他商品交易所实物交割合约按商品类型处理。
缺失、未知或冲突保持 UNKNOWN；不靠合约后缀推断，更不将空响应当作不适用证明。

| 数据 | 实物商品 | 月均价商品 | 股指 | 国债 | 航运指数 |
|---|---|---|---|---|---|
| 元数据、日历、原生分钟/日/周/月、结算和涨跌停/保证金 | 必需 | 必需 | 必需 | 必需 | 必需 |
| 商品仓单 | 必需 | 不适用 | 不适用 | 不适用 | 不适用 |
| 持仓排名、品种周报 | 必需，披露范围待逐项核实 | 代码/适用范围待核实 | 必需，披露范围待逐项核实 | 必需，披露范围待逐项核实 | 代码/适用范围待核实 |
| 主力/连续、复权、南华指数 | 关联研究资料 | 关联研究资料 | 关联研究资料 | 关联研究资料 | 关联研究资料 |

类型 UNKNOWN 仍可采集通用合约行情，但未知的品种资料不发起猜测请求，未知规则继续阻止发布。
REQUIRED、NOT_APPLICABLE、RELATED、UNKNOWN 是适用关系，不是响应质量。报告逐数据集列出
请求未完成、已收到但记录待核验、响应异常和规则未知，取消对所有合约添加同一组笼统阻塞理由。
只有所有必需项 VERIFIED 且没有未知依据才允许发布；本轮没有把请求窗口完整冒充应有记录完整。
历史分钟时段、供应商标签、无成交分钟政策和周月线/品种报表的应有记录仍需真实证据，
结算后的业务区间和月均价最终结算依据仍需核实；真实完整合约验收继续 #18/#47。

2026-09-15 查阅 Nautilus Data `latest`，采用标的语义与独立研究序列分离原则，无新增依赖。
查阅 Tushare [fut_basic](https://tushare.pro/document/2?doc_id=135)、
[仓单](https://tushare.pro/document/2?doc_id=140)、[持仓](https://tushare.pro/document/2?doc_id=139)、
[周报](https://tushare.pro/document/2?doc_id=216)、[结算](https://tushare.pro/document/2?doc_id=141)
和 [涨跌停](https://tushare.pro/document/2?doc_id=368)，仅用文档确认字段/身份范围，
不将接口起始年份当作所有品种完整覆盖。国债的证券交割不适用商品仓单接口；现金交割无商品仓单义务。
[能源中心交割细则](https://www.shfe.cn/regulation/ineregulation/businessmethods/delivery/202606/t20260626_832297.html)
确认 EC 现金交割；当前规则不倒推认证历史时段、费率或最终结算算法。

# 共用决策与账本，按事实来源分别执行

研究、内部 Paper、柜台仿真和实盘共用 Strategy、Risk 与基于事实的 Accounting，
但只有 Simulation 从行情生成模拟成交；真实成交与账户变化必须来自柜台回报并核对，
不能复用“后续 bar 全量成交”的假设。
研究冻结输入后保存完整结果，持续会话按账户串行提交事实、策略状态、委托和游标；
外发委托先持久化身份与预占，再在事务外发送，结果未知时查询对账而非盲重发。
这让首个受限实盘闭环可以先于高级研究平台交付，同时承担真实外部执行不可原子回滚的恢复成本，
不为全部历史计算引入逐事件持久化或通用事件平台。

# 共用交易计算，分别提交研究结果与 Paper 事件

历史研究与持续 Paper 使用同一因果顺序、账户计算和风险规则，避免两个模式产生不同交易含义。
研究对固定 Snapshot 计算后保存结果；Paper 按输入在 PostgreSQL 中原子提交账本、策略状态与处理游标，
并从已提交事实恢复。这样将恢复复杂性留在实际需要持续账户的地方，承担按账户串行处理的代价，
同时避免为全部研究计算引入逐事件持久化或通用事件平台。

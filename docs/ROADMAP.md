# 开发路线

#18 直接接入 Tushare 官方 1/5/15/30/60 分钟固定数据，各周期独立同步、校验和发布。已删除跨周期比较功能，不要求由 1min 合成或核对大周期；保留时间、交易日与有效条款的必要语义。


2026-09-13：#18 已将 1/5/15/30/60 分钟周期贯穿现有规范化、质量、固定快照、离线语义及 Research/Paper 输入；保留默认 1m 的内部研究声明，发布后周期不可变。五周期发布/重读、提前可得拒绝与 15 分钟持久回放对账通过本地 PostgreSQL 验证。已接入已发布 Tushare receipt → 经核实时标和时段 → 持久准备队列 → 固定研究清单；API 关闭、重复受理和原文血缘验证通过。尚未完成真实跨日时标、日历和条款验收；不推断供应商时间标签，不接受跨休市或不完整时段作为完整 Bar。

2026-09-13：工作台改为首次访问创建独立单用户账号；普通启停保留，deploy 停止 API 后清除账号。
使用私有持久文件、scrypt 摘要及排他发布，重复注册不能替换账号；移除部署密码摘要与随机密码生成。
修复容器验收对首页大写品牌文本的错误断言，改以响应和注册/登录的真实协议验证。新提交 CI 结果单独记录。
`5891ebf` 新安装包的三工作台 Chromium 首次注册、密码确认、重复注册拒绝、退出及重启身份验证通过。
完整数据/研究/Live 管理浏览器流程随后通过，browser_errors=[]，broker_connected=false；临时 PostgreSQL，Live 控制为合成 HTTP，无真实柜台验收。

2026-09-13：新增 `purge-host <应用> --yes`，清理目标主机全部 Northstar 部署与本地数据。
仅卸载 NFS 客户端，不操作 NAS；挂载失败、异类容器占用及活动部署锁阻止删除。
本地部署/卸载回归 53 项通过，后续新增外部卷、防火墙隔离及 NAS 目标拒绝测试也通过；
已按用户授权实际卸载 hub.local 和 research.local；Research 的旧 NFS 导出及行情亦经单独确认删除，未操作 nas.local。hub 两个无法归属的匿名 Docker 卷保留。


2026-09-13 部署边界调整（本地批次）：应用主机依赖由外部预置，主机配置仅 host；首次 deploy 用 SSH 默认账号提权并自动准备 northstar。
不提供单独 init-host，账号准备集成在 deploy，依赖/镜像源不自动安装；部署使用 northstar，SSH 端口由本机 SSH 配置决定。NFS 服务及 `/quant` 导出改为外部前置条件；
`[nfs]` 仅配置 host，项目仅管理 Data Hub/Research 的固定客户端挂载。
数据库目标固定继承 `[data_hub]`，仍独立部署；数据库初始化前在该主机准备行情挂载。
移除 NFS 服务端安装、导出、账号、防火墙和启停管理。部署传输与挂载故障回归通过；未执行 NAS/主机现场迁移验收。


设计与边界见 [ARCHITECTURE](ARCHITECTURE.md)，参考依据见 [REFERENCES](REFERENCES.md)。
[Project](https://github.com/users/isqiwen/projects/1) 管理顺序/状态，[Issues](https://github.com/isqiwen/northstar-quant/issues) 保存可执行步骤、直接依赖和验收。
**首轮：一份真实固定数据 → 单合约跨日回测 → 同一业务规则下的 SimNow 开平仓与恢复。**
全市场下载完成、多品种、ML、云主机和真实资金都不是首轮前置。

## 第一轮：数据与研究

| 建议顺序 | Issue | 完成后得到什么 |
|---|---|---|
| 1 | [#46 响应规范化与质量版本](https://github.com/isqiwen/northstar-quant/issues/46) | 响应校验、Decimal Parquet、留存原文重处理、逐项质量与固定版本差异已通过完整 CI，已关闭 |
| 并行核实 | [#41 Tushare真实来源验收](https://github.com/isqiwen/northstar-quant/issues/41) | 已取得 RB2610 真实分钟/日线固定样本并逐项核对；请求异常隔离修复已通过 CI，已关闭 |
| 2 | [#18 合约、时段与信息时钟](https://github.com/isqiwen/northstar-quant/issues/18) | 已接通固定多时段/多交易日读取与时间顺序拒绝；待真实 Tushare 装配、日历及条款 |
| 3 | [#19 统一期货账户](https://github.com/isqiwen/northstar-quant/issues/19) | 多空今昨仓、费用、结算、保证金和权益逐项勾稽 |
| 4 | [#20 事件原子性与受约束成交](https://github.com/isqiwen/northstar-quant/issues/20) | 失败不留半状态，成交遵守量价/时段，部分成交与残量可解释 |
| 5 | [#21 可核对报告与固定评价](https://github.com/isqiwen/northstar-quant/issues/21) | 同一账本报告、固定窗口/基准和明确样本外边界 |
| 并行工程 | [#23 持久研究负载验收](https://github.com/isqiwen/northstar-quant/issues/23) | 现有SQLite/worker的并发、取消、重启与实测资源管理 |
| 6 | [#47 数据检查工作台](https://github.com/isqiwen/northstar-quant/issues/47) | 图表、覆盖、异常行和修订差异同源可查 |

已交付 #46：响应校验、Decimal Parquet、留存原文重处理、逐项质量报告与固定版本差异均已通过完整 CI 与浏览器验收；完整领域时间与条款继续 #18。
已交付 [#56 共享消息总线](https://github.com/isqiwen/northstar-quant/issues/56)：Research/Live 实际接入、事件隔离与研究整步回滚。
已交付 [#57 共享交易内核](https://github.com/isqiwen/northstar-quant/issues/57)：统一事件入口、生命周期、失败策略和行情窗口；完整账户/执行业务继续按下表推进。
#48 已将 `Environment`（BACKTEST/SANDBOX/LIVE）接入共享内核、实例绑定、配置与管理协议。SimNow 为 SANDBOX，柜台 profile 单独固定；完整材料、健康告警仍待交付。
#19 已接入 Research/Live 共用的多空持仓数量规则及显式开平、FIFO 平仓计价与恢复核验；费用调整、跨日结算和保证金仍待完成。
#20 的单事件异常回滚已随 #56 实现；完整成交模型仍需 #19。#41 的外部权限问题不阻止无凭据工程验证。

## 第一轮：SimNow Sandbox

| 建议顺序 | Issue | 完成后得到什么 |
|---|---|---|
| 并行基座 | [#26 三工作台操作者认证](https://github.com/isqiwen/northstar-quant/issues/26) | 独立登录/会话与审计；不限制IP，登录不授予交易 |
| 并行基座 | [#48 固定材料与健康告警](https://github.com/isqiwen/northstar-quant/issues/48) | 本地完整实例绑定、独立内核、可观察故障 |
| 1 | [#31 SimNow账户与条款](https://github.com/isqiwen/northstar-quant/issues/31) | 只读可信起点，缺项与差异明确 |
| 2 | [#25 持续行情与交易日](https://github.com/isqiwen/northstar-quant/issues/25) | 夜盘/日盘独立行情、陈旧/缺口与预热处理 |
| 3 | [#49 授权、订单与预占](https://github.com/isqiwen/northstar-quant/issues/49) | 先可靠保存再发送，账户唯一所有者，UNKNOWN不盲重发 |
| 4 | [#32 柜台报撤单与控制](https://github.com/isqiwen/northstar-quant/issues/32) | 仿真开平仓、确认成交/账本、暂停/撤单/请求平仓 |
| 5 | [#33 断线与联合恢复](https://github.com/isqiwen/northstar-quant/issues/33) | 本地恢复、外部核对与重新授权完整验收 |
| 联合出口 | [#50 首轮验收](https://github.com/isqiwen/northstar-quant/issues/50) | 真实跨日研究和真实柜台仿真闭环均通过 |

顺序不是依赖闭包；硬性前置以 Issue 的原生 Blocked by 为准。每次选择一个可观察结果交付。
历史 #53 跨主机固定文件与联合恢复已有实机证据，保留历史，不重开成首轮阻塞。

## 后续独立交付

| 优先级 | Issue | 范围 |
|---|---|---|
| P1 | [#43 范围查询与引用保留](https://github.com/isqiwen/northstar-quant/issues/43) | 按实测优化读取与小文件合并，不破坏固定版本 |
| P1 | [#54 Live异步备份与容量](https://github.com/isqiwen/northstar-quant/issues/54) | SQLite封存包独立传输，远端失败不阻塞内核；不增加Data Hub数据来源 |
| P2 | [#28 组合与多策略预算](https://github.com/isqiwen/northstar-quant/issues/28) | 多合约、多策略仍共享单账户风险与发送所有者 |
| P2 | [#29 真实合约换月](https://github.com/isqiwen/northstar-quant/issues/29) | 固定映射、平旧开新、成本及残余敞口 |
| P2 | [#45 有限因子实验](https://github.com/isqiwen/northstar-quant/issues/45) | 假设、候选、训练/评价与复核；复用现有任务与产物 |
| P2 | [#34 生产准入与只读核对](https://github.com/isqiwen/northstar-quant/issues/34) | 期货公司/用户明确资料与权限，仍无发送 |
| P2 | [#35 生产部署与受限实盘](https://github.com/isqiwen/northstar-quant/issues/35) | 实机/云端故障验收后，由用户本人授权并操作真实资金 |

## 后续 AI 执行规则

1. 先读架构、Issue 当前基线、直接依赖与相关参考；不要重做已交付能力或跟随旧评论中的拓扑。
2. 使用既有业务入口推进一个完整切片，修改所有受影响调用/协议；不搭第二套演示流程或兼容架构。
3. 验证金钱、因果、持久性、权限与真实集成。外部样本、柜台交易、实机和合成故障证据分别记录。
4. 提交并检查该SHA的CI，更新Issue验收与Project；外部条件未满足的任务保持开放。

本次整理保留有交付证据的关闭事项并从当前Project归档；纯计划重复内容迁入唯一所有者后删除。
旧手工导入、NAS三库和Live PostgreSQL不再成为当前开发任务；当前已维护的部署与数据不因任务整理自动删除。

## 当前本地重构批次（2026-09-11）

用户于 2026-09-13 授权先提交并推送当前进度，不再等待整批完成。分支 `refactor/futures-system`，远端基线
`5ec856270a15eb4a28e1305d813fac2b39e7b68e`。上述已交付事项指远端证据；
下表是当前分支工作；推送不等于完成验收，不据此关闭 Issue 或将 Project 标为 Done。

| 范围 | 当前已实现 | 整批剩余条件 |
|---|---|---|
| #18 固定研究输入 | 多时段/交易日、结算与有效条款进入不可变清单；时间重叠、晚可得和条款缺口拒绝 | 真实 Tushare 数据装配，交易日历与历史条款核实 |
| #19 共享账户 | 多合约共用现金、分合约 FIFO/日结算；未知费用、明确费用覆盖及修订差额、重放/回滚 | Live 完整资金账本与外部核对 |
| #20 受约束订单 | 受约束模拟成交；Live 持久订单/发送/撤单尝试、部分成交、费用预占与恢复 | 接入当前账户就绪检查的原生发送及外部闭环 |
| #21 报告与评价 | 同一账本逐项核对；执行前固定窗口、现金基准；显式探索性结果，不冒充样本外或生产资格 | 真实固定输入的最终研究验收 |
| #26 操作者身份 | 三应用首次访问创建单用户、会话/退出、CSRF；普通重启保留、deploy 清除账号；可信操作人进入 Live 命令回执 | 随整批交付验证 |
| #48/#49 运行边界 | 候选/账号唯一锁、当前实例 owner 执行限额、撤销、预占、有界 CTP 私有通道；UNKNOWN 不重发 | 许可与当前账户/市场/风险的整体接入，独立告警 |
| #31/#25/#32/#33 Live Sim | 独立 SQLite/内核、既有只读柜台证据和影子流程保持可用 | 可信账户/条款、持续夜盘行情、报撤单、资金与联合恢复闭环 |
| #43 固定范围读取 | 按时间裁剪 Parquet 行组、精确值/来源不变、持久固定合并/逐行来源/恢复及读取成本；无引用清单清理 | 随整批交付验证；进一步列裁剪按实测需要 |
| #45 有限参数实验 | 固定训练/验证/测试、参数搜索、训练期岭回归与固定系数；因子前瞻粗筛、分组和逐日统计 | 多合约、多因子与滚动稳定性研究 |
| #50 验收 | 合成数据的安装、浏览器、后台生命周期与联合恢复已多次验证 | 真实固定研究与明确授权的外部 SimNow 非空交易闭环 |

共同机制：实例内 `TradingKernel`/消息总线；`persistence` 仅复用 UTC 数据库时间、
本地文件锁和显式写事务，各业务仍拥有表与完整提交。Data Hub 不因这类机制依赖 Live/Broker。
订单计算保留完整精度，损坏金额在恢复时拒绝，不以舍入改变既有风险边界。

## 本地验收基线（2026-09-13）

历史切片的实现和检查记录保留在本分支 Git 提交及所属 Issue；此处只记录当前可用证据。

| 检查 | 提交与结果 | 证据边界 |
|---|---|---|
| 完整后端 | `aa54e92`：878 项通过，266 个源文件类型检查通过 | 包含真实本机 PostgreSQL 与 Live/Research SQLite；不等于外部柜台验收 |
| 安装与联合恢复 | `aa54e92` 新 wheel：固定研究、Paper 重启、独立进程、空库恢复与六端日志通过 | 新 Data Hub 基线 `20260913_01`；未操作个人部署 |
| 三工作台浏览器 | `31e3447` 对应前端：登录/退出、账户报告、会话文件及页面操作通过，无浏览器错误 | 后续仅改后端；交易控制使用明确标记的合成拦截，没有下单 |

合成账户基准：现金 97370、权益 97320、空仓 5 手、费用 30；不是收益保证或真实历史验收。

本轮清理：`9eefced` 移除旧手工 SHFE 下载、审批/恢复、四张关联模型及专属质量分支，
约减少 4,000 行旧实现和专属测试。Tushare 是唯一自动下载入口；标准化与固定发布保留。
导入完整性规则 3.0.0 的计算指纹、数据库约束和快照引用共用版本定义。
`aa54e92` 对旧基线明确拒绝启动，不自动修改版本、转换或删除数据。
878 项与清理前 `0bb8a8f` 的 906 项差异来自旧流程专属测试删除及新基线保护检查。

柜台查询的新接收与冷恢复共用纯解释和有序查询计划；登录/认证必须匹配成功请求及唯一结束回报。
持续接收与恢复也拒绝未完成登录。资金活动不确定性贯穿观察区间两端，但不抹掉已经确认的持仓和 FIFO 盈亏。
这些改动均不授予订单发送权限。

## 整批尚未完成的主路径

1. **#18**：将真实 Tushare 来源按已核实的分钟标签、交易日/时段及历史条款装配成固定研究输入。不能丢弃不便解释的开盘记录或用默认费率冒充历史事实。
2. **#19/#31**：建立 Live 完整资金、费用、资金流与跨日结算的同一账本及外部核对。当前累计资金观察和有限 FIFO 投影不是完整现金权威。
3. **#49/#32**：在实际接收核心中连接当前账户/行情/风险检查、运行授权、持久订单/预占和原生报撤单通道；现有 `ExecutionAuthority.admit` 的当前账户回调尚未接入最终发送者，影子流程仍不发送。
4. **#25/#33/#48**：跨交易日的条款/结算切换、外部核对与重新授权，及实际故障告警接收。当前同一交易日的夜盘/日盘声明和有限接收不代表长期跨日运行已完成。
5. **#50**：真实固定输入的可重复跨日回测，以及另行明确授权的 SimNow 非空开平仓、断线和恢复验收。

继续按上述依赖推进实际调用闭环，缺少外部证据不阻止独立代码工作。
不以更多只读投影、诊断、局部单测或候选接收代替尚未接通的执行与账户业务。
组合、换月、高级 ML 和真实资金准入保留后续阶段。

本批按用户新授权推送至 refactor/futures-system；Issue/Project 按实际进度和 CI/外部验收更新，未完成事项保持开放。
真实来源样本保留在私有验收目录；新供应商请求、柜台连接或实机操作须有明确范围。
不将合成成交、假定历史条款或源码测试当作真实外部验收。

2026-09-13 账户增量：共享 Account 与 Live 原始回报入账接通已识别入出金/精确冲正，
重复流水、事务回滚、SQLite 重启以及入金→成交→费用→跨日结算→平仓均有验证。
Live 显示已识别净资金流与笔数；未知或冲突回报保留待核对，完整资金权威仍未完成。

本地全量回归首次为 898 通过、6 失败：空仓普通查询的只读计价误走合约登记导致
4 项 SQLite 写锁失败，已移除该写入路径并补充“读取不创建合约”检查；另 2 项恢复
测试遇到测试期间 Git 提交改变，由实现身份保护拒绝不同版本。固定版本后的相关
PostgreSQL/SQLite 资金、恢复与任务测试 24 项全部通过。后续全量及当前 CI 仍需核实。

`01a073d` 固定 SHA 全量后端 905 项通过；新 wheel 重建合成资金流水 SQLite 副本一致。
安装后的 CLI/API、Web 与工作进程隔离、Paper 重启、联合恢复和文件日志检查通过。
验收恢复库已显式使用 template0/UTF-8，避免继承本地 SQL_ASCII 模板。
资金流水明细增加所属 Protobuf 与只读表格，原流水/冲正关系及精确金额有跨语言检查。

`cff3926` 新 wheel 与三个 Next.js 构建完成浏览器流程：三工作台注册/登录及前后端隔离、
Research 持久任务和 Live 明细均通过，浏览器错误为零。资金表使用合成拦截回包，
没有柜台连接。旧 CI 的“未知”文本定位不唯一已改为定位总费用字段并重跑通过。

结算来源链路继续补齐：Live 已有只读查询可附带明确日期，保存原生分段、完整正文及
字节身份，页面/CLI 共用运行时请求，重试与重启读取固定证据，不自动连接或确认结算。
该能力准备取得实际版式，尚未将原文猜测成费用/结算事实；真实来源范围待确认。

`b675d76` 新 wheel/三应用构建及浏览器验收通过，结算正文精确字符串、普通文本转义、
资金流水、身份和进程隔离检查均通过，浏览器错误为零；没有连接柜台。

执行主线接通既有订单撤销：拥有者命令→原接收核心的 MessageBus→OMS 持久尝试→
现有 CTP 私有通道；缺会话不重连、过期队列不补发、未确认撤单不重复发送，预占不提前释放。
相关 Live/订单/交易核心/恢复回归 207 项通过，额外过期与重复请求检查继续补齐。
新增报单的当前账户/行情/风险准入和外部非空往返仍未完成。

撤单链路验收：`60a09af` 修复幂等启动读取状态时的锁重入死锁，并移除当前定义已覆盖的
旧表结构补丁；完整后端 928 项通过（实际本机 PostgreSQL/SQLite，无跳过），273 个源文件
类型检查通过。三个前端构建与 20 项测试通过。`11ac992` 新 wheel 的三工作台浏览器验收
通过，包含撤单请求后保留 UNKNOWN/预占、结算原文、资金明细和持久任务，浏览器错误为零。
其 Live 控制使用合成回包，没有连接柜台。额外的接收/SQLite/撤单 26 项检查通过，确认
新的 TD 登录不会继承上一连接的撤单就绪状态，即使未收到断线回调。

资金主线仍需要已确认来源与格式的结算证据，将其中明确覆盖的费用和结算生成所属账户事实，
再接通当前账户准入与最终新报单；不能把接收到正文、累计 Commission 或余额差额当成这些事实。
本地测试通过不代表整批重构、真实跨日数据或 SimNow 外部往返已完成。

继续 #19/#31：已将现有柜台成交/资金流水接入同一事务中的账户事实链，移除金额视图自行
重建账户的路径。费用、资金流水、结算和跨日平仓均可按原顺序持久重放，固定前缀可复查；
已保存的投影不作为恢复输入。初步账户/柜台成交/SQLite 集成 158 项通过，额外回滚与
来源缺失检查通过；完整回归继续。真实结算解释与资金覆盖仍独立保留，缺样本不暂停代码实现。

`10d3975` 固定提交完整后端 930 项通过，274 文件 Mypy 通过；对应新 wheel
三工作台浏览器验收通过，错误为零、无柜台连接。费用主线随后删除任意扣账回调：
OMS 费用确认直接核对并写入统一账户事实链，与预占释放共同提交；恢复双向核验。
已将原先仅模拟 OMS 的费用备份检查替换为原始 CTP 合成回报→账户→OMS→费用→联合恢复。

费用共同提交及双向恢复核验的账户/OMS/CTP/Live 回归 316 项通过（本机 PostgreSQL
与 SQLite，无跳过），275 文件 Mypy 通过。费用覆盖来自显式合成事实，未把柜台累计
Commission 或未知正文转换为费用；整批剩余主路径仍按上方清单推进。

`dd1460e` 固定提交完整后端 933 项通过；此前 `10d3975` 的 GitHub CI 已通过。
后续本地继续收紧新开仓事务的账户范围约束，保留未知费用/发送结果时的撤单与事实入账；
历史接收异常不再跳过后来已识别的经济事实。相关账户/OMS/CTP/Live 回归 337 项通过。
Live 原始持仓来源链仍按单交易日约束，不能据通用 Account 支持结算宣称跨日会话完成。

`5cc8299` 新 wheel 的三工作台浏览器验收通过，错误为零、无柜台连接；
Research 在前端/API/Data Hub 停止后完成持久任务。后续补齐柜台来源与金额入账的
反向核验：能重放但找不到所属原始来源的新增金额在恢复时拒绝。账户/OMS/柜台/恢复
75 项集成检查通过，275 文件 Mypy 通过。该检查不建立尚缺失的外部资金覆盖证明。

同批回报的部分金额覆盖已接入真实入账与恢复路径：可确认成交不会被同批不支持的
合约/套保/开平记录丢弃，PARTIAL 明确保留未知，不暴露可用现金或新增风险权限。
78 项账户/OMS/柜台/恢复集成测试通过，全部使用合成柜台事实。

#18 的真实留存原文装配检查：原始 15min SHA256
`86912f3dd781e061ad17f35527223835051f42bb73e2025fb4442dbb603c6f96`，
重新经过现有处理/持久准备/发布，2026-09-08、09 两个日盘共 6 时段、30 根原生 Bar，
固定清单 `6727b47c-bde5-4e6f-af63-c38bdc020d84`；离线复制后内容身份一致。
私有证据 `.northstar/acceptance/issue18-retained/report.json`，未新增供应商请求。
BAR_END 为明确条件解释、FINAL_REVISED 不宣称历史首次可得；夜盘开盘记录拒绝准入。
未绑定结算事实时，实际 Research 入口拒绝跨日运行；不作为可信收益或 #50 验收。

`46f4b6c` 完整回归首次结果为 936 通过、4 失败，失败均为 CTP 测试在 UNKNOWN 后
继续新开仓的旧前提。夹具已增加明确柜台确认，11 项对应检查通过；完整回归待复验。

`330b808` 完整后端复验 940 项通过、无跳过。后续优化金额恢复核验：完整重放后
按来源索引核对批次，费用在同一审计事务内每账户仅重放一次；不引入可写余额缓存。
同一本机 SQLite 的 300 个合成批次，重放加来源核验由约 1.337 秒降至 0.027 秒，
仅代表此场景。记录于 `.northstar/acceptance/account-recovery-cost/`。79 项账户/OMS/
柜台/恢复检查和 275 文件 Mypy 通过，包含重新计算哈希后金额仍与来源不一致的拒绝。

`cbbaa99` 新 wheel 的安装联合恢复通过，私有 `recovery-cbbaa99` 证据记录安装提交、
PostgreSQL/SQLite 版本、两份存储 UUID 和检查结果；空库恢复后固定数据与研究结果
一致，六类日志和 Web/worker 隔离通过，无柜台连接。随后接通接收进程自身的启动查询
明细，固定原始回报前缀；成功、未完成、失败、冷读取和损坏拒绝通过集成验证，276 文件
Mypy 通过。新前端与安装包验收继续，不把查询成功当作当前账户准入或整批完成。

本次完整后端初跑 940 通过，因命令遗漏 PostgreSQL 客户端 PATH 导致 1 失败、2 跳过；
补正环境后相关维护/备份 5 项全部通过，原失败和跳过均已实际执行通过。三个前端构建、
20 项前端测试及协议生成一致性检查通过；安装浏览器验收仍需记录新提交。

`296da4c` 新 wheel 三工作台浏览器检查通过，错误为零、无柜台连接；启动查询场景为
合成 HTTP。视觉复查发现该合成场景遗漏账本读取回包，已补齐并增加错误横幅检查，继续复验。
新单准入补充持久接收/处理前缀一致性检查，防止未处理回报被旧账户结果掩盖；31 项授权/
订单/撤单测试和 276 文件 Mypy 通过。最终新报单与真实 SimNow 往返仍未完成。

`8e29ca6` 新 wheel 的三工作台浏览器复验通过，启动查询截图无错误横幅，浏览器错误为零。
后续订单事务直接核验本连接的完整启动查询，新增无断线回调的重登录拒绝，旧查询仅保留
历史解释。35 项授权/来源/订单/撤单检查通过；这仍不替代当前账户风险检查或真实发送验收。


`6f5b3d1` 完整后端验证 946 项通过，无跳过；启动查询的同事务核验与重登录拒绝已覆盖。
后续 #18/#20 补齐 MarketBar 规范合约身份，贯穿数据适配、订阅、研究输入、检查点和
模拟成交，错误合约拒绝且不消耗订单或修改账户。276 文件 Mypy 与 Ruff 通过；
完整回归 946 项通过，1 项策略路由测试仍把旧合约行情投给另一合约而失败；修正输入并增加拒绝断言后，57 项相关检查通过，原失败已通过。新 wheel 联合恢复继续执行；本地切片不代表最终原生报单或整批重构完成。


`298d751` 新 wheel 联合恢复通过：固定行情、研究重放、空库恢复、来源核验、独立 Live
进程与六类日志均通过；私有 `contract-298d751` 证据记录提交、数据库版本和存储 UUID，
无柜台连接。后续修正账户投影遗漏最新已确认费用的问题，展示固定账本版本与哈希；
写入新柜台事实期间不拼接旧持仓和新资金。49 项含 PostgreSQL 的账户/执行/恢复检查、
13 项原生合成回报检查、276 文件 Mypy、20 项前端测试与 TypeScript 检查通过；
三个前端构建及新版本安装浏览器验收继续。最终当前账户准入/原生新报单仍待接通。

`0938721` 完整后端 948 项通过，无跳过；`69446eb` 新 wheel 三工作台浏览器验收
通过，费用刷新显示新的固定账本身份，未知余额不变，浏览器错误为零，无柜台连接。
接收进程内账户刷新已贯穿持久命令、原连接七类查询与查询窗口显示；165 项柜台/
授权/接收控制检查、278 文件 Mypy、20 项前端测试通过。该查询不等于账户核对或
新增报单权限；安装浏览器验收继续。真实 SimNow 往返及当前账户报单准入仍未完成。

`bb65170` 接收内刷新已落入本地提交。安装浏览器首次发现刷新后标签页重置，已修正并
继续复验。完整后端初跑 952 项通过、1 项新增夹具使用旧接收时间失败；修正正常时间并
保留时间回退拒绝后，20 项相关检查通过。随后将接收窗口资金观察接入现有金额解释规则，
保留流/查询/原回报身份，不伪装成独立查询批次；160 项含 PostgreSQL 的账户与授权
检查通过。工作台展示固定观察，未建立当前资金核对或开放报单。

`f5372c7` 新 wheel 三工作台浏览器复验通过，错误为零、无柜台连接；账户刷新保留当前
标签，截图已检查。接收窗口新增按固定查询身份读取的 API 与页面；仅结束的查询提供
固定入口，后来刷新不替换旧结果。20 项接收/授权检查及 278 文件 Mypy 通过，新的完整
回归和安装页面验收继续。

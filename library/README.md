# 本地资料库

此目录用于存放量化研究相关的本地书籍与论文，供阅读和研究参考；它不参与程序运行、数据下载或交易决策。

```text
library/
├── books/   # 书籍、课程讲义等
└── papers/  # 论文、研究报告等
```

直接放在 `books/` 或 `papers/` 下的 PDF 会纳入 Git LFS 管理，不进入普通 Git 对象；其他类型的
本地资料默认忽略。添加前请确认资料的授权允许团队或远程仓库保存。需要在项目文档中引用资料时，仍应
记录书名、作者、发表年份和公开链接。

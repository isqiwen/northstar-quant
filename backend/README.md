# Northstar Quant 后端

一个 Python 包提供 Data Hub、Research、Live 三个应用的 API 与业务能力。
前端在仓库的 `frontend/`，公共协议在 `proto/`。完整启动和维护说明见仓库根目录 README.md。

在仓库根目录运行 `uv sync --project backend --locked` 安装依赖，使用 `make verify` 验证。

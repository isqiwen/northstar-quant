/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-mixed-operators, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars, default-case, jsdoc/require-param*/
import $protobuf from "protobufjs/minimal.js";

// Common aliases
const $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;
const $Object = $util.global.Object, $undefined = $util.global.undefined, $Error = $util.global.Error, $RangeError = $util.global.RangeError, $TypeError = $util.global.TypeError, $Number = $util.global.Number, $String = $util.global.String, $Boolean = $util.global.Boolean, $isFinite = $util.global.isFinite, $Array = $util.global.Array, $parseInt = $util.global.parseInt, $BigInt = $util.global.BigInt;

// Exported root namespace
const $root = $protobuf.roots["northstar_research"] || ($protobuf.roots["northstar_research"] = {});

export const google = $root.google = (() => {

    /**
     * Namespace google.
     * @exports google
     * @namespace
     */
    const google = {};

    google.protobuf = (function() {

        /**
         * Namespace protobuf.
         * @memberof google
         * @namespace
         */
        const protobuf = {};

        protobuf.Struct = (function() {

            /**
             * Properties of a Struct.
             * @typedef {Object} google.protobuf.Struct.$Properties
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [fields] Struct fields
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Struct.
             * @memberof google.protobuf
             * @interface IStruct
             * @augments google.protobuf.Struct.$Properties
             * @deprecated Use google.protobuf.Struct.$Properties instead.
             */

            /**
             * Shape of a Struct.
             * @typedef {{
             *   fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} google.protobuf.Struct.$Shape
             */

            /**
             * Constructs a new Struct.
             * @memberof google.protobuf
             * @classdesc Represents a Struct.
             * @constructor
             * @param {google.protobuf.Struct.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const Struct = function (properties) {
                this.fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Struct fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} fields
             * @memberof google.protobuf.Struct
             * @instance
             */
            Struct.prototype.fields = $util.emptyObject;

            /**
             * Creates a new Struct instance using the specified properties.
             * @function create
             * @memberof google.protobuf.Struct
             * @static
             * @param {google.protobuf.Struct.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.Struct} Struct instance
             * @type {{
             *   (properties: google.protobuf.Struct.$Shape): google.protobuf.Struct & google.protobuf.Struct.$Shape;
             *   (properties?: google.protobuf.Struct.$Properties): google.protobuf.Struct;
             * }}
             */
            Struct.create = function(properties) {
                return new Struct(properties);
            };

            /**
             * Encodes the specified Struct message. Does not implicitly {@link google.protobuf.Struct.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.Struct
             * @static
             * @param {google.protobuf.Struct.$Properties} message Struct message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Struct.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.fields != null && $Object.hasOwnProperty.call(message, "fields"))
                    for (let keys = $Object.keys(message.fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a Struct message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.Struct
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.Struct & google.protobuf.Struct.$Shape} Struct
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Struct.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.google.protobuf.Struct();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (message.fields === $util.emptyObject)
                                message.fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.fields, key);
                            message.fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a Struct message.
             * @function verify
             * @memberof google.protobuf.Struct
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Struct.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.fields != null && $Object.hasOwnProperty.call(message, "fields")) {
                    if (!$util.isObject(message.fields))
                        return "fields: object expected";
                    let key = $Object.keys(message.fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.fields[key[i]], _depth + 1);
                        if (error)
                            return "fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a Struct message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.Struct
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.Struct} Struct
             */
            Struct.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.Struct)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.Struct: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.Struct();
                if (object.fields) {
                    if (!$util.isObject(object.fields))
                        throw $TypeError(".google.protobuf.Struct.fields: object expected");
                    message.fields = {};
                    for (let keys = $Object.keys(object.fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.fields, keys[i]);
                        if (!$util.isObject(object.fields[keys[i]]))
                            throw $TypeError(".google.protobuf.Struct.fields: object expected");
                        message.fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a Struct message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.Struct
             * @static
             * @param {google.protobuf.Struct} message Struct
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Struct.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.fields = {};
                let keys2;
                if (message.fields && (keys2 = $Object.keys(message.fields)).length) {
                    object.fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.fields, keys2[j]);
                        object.fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this Struct to JSON.
             * @function toJSON
             * @memberof google.protobuf.Struct
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Struct.prototype.toJSON = function() {
                return Struct.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Struct
             * @function getTypeUrl
             * @memberof google.protobuf.Struct
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Struct.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.Struct";
            };

            return Struct;
        })();

        protobuf.Value = (function() {

            /**
             * Properties of a Value.
             * @typedef {Object} google.protobuf.Value.$Properties
             * @property {google.protobuf.NullValue|null} [nullValue] Value nullValue
             * @property {number|null} [numberValue] Value numberValue
             * @property {string|null} [stringValue] Value stringValue
             * @property {boolean|null} [boolValue] Value boolValue
             * @property {google.protobuf.Struct.$Properties|null} [structValue] Value structValue
             * @property {google.protobuf.ListValue.$Properties|null} [listValue] Value listValue
             * @property {"nullValue"|"numberValue"|"stringValue"|"boolValue"|"structValue"|"listValue"} [kind] Value kind
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Value.
             * @memberof google.protobuf
             * @interface IValue
             * @augments google.protobuf.Value.$Properties
             * @deprecated Use google.protobuf.Value.$Properties instead.
             */

            /**
             * Narrowed shape of a Value.
             * @typedef {{
             *   nullValue?: google.protobuf.NullValue|null;
             *   numberValue?: number|null;
             *   stringValue?: string|null;
             *   boolValue?: boolean|null;
             *   structValue?: google.protobuf.Struct.$Shape|null;
             *   listValue?: google.protobuf.ListValue.$Shape|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ kind?: undefined; nullValue?: null; numberValue?: null; stringValue?: null; boolValue?: null; structValue?: null; listValue?: null }|{ kind?: "nullValue"; nullValue: google.protobuf.NullValue; numberValue?: null; stringValue?: null; boolValue?: null; structValue?: null; listValue?: null }|{ kind?: "numberValue"; nullValue?: null; numberValue: number; stringValue?: null; boolValue?: null; structValue?: null; listValue?: null }|{ kind?: "stringValue"; nullValue?: null; numberValue?: null; stringValue: string; boolValue?: null; structValue?: null; listValue?: null }|{ kind?: "boolValue"; nullValue?: null; numberValue?: null; stringValue?: null; boolValue: boolean; structValue?: null; listValue?: null }|{ kind?: "structValue"; nullValue?: null; numberValue?: null; stringValue?: null; boolValue?: null; structValue: google.protobuf.Struct.$Shape; listValue?: null }|{ kind?: "listValue"; nullValue?: null; numberValue?: null; stringValue?: null; boolValue?: null; structValue?: null; listValue: google.protobuf.ListValue.$Shape })
             * )} google.protobuf.Value.$Shape
             */

            /**
             * Constructs a new Value.
             * @memberof google.protobuf
             * @classdesc Represents a Value.
             * @constructor
             * @param {google.protobuf.Value.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const Value = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Value nullValue.
             * @member {google.protobuf.NullValue|null|undefined} nullValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.nullValue = null;

            /**
             * Value numberValue.
             * @member {number|null|undefined} numberValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.numberValue = null;

            /**
             * Value stringValue.
             * @member {string|null|undefined} stringValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.stringValue = null;

            /**
             * Value boolValue.
             * @member {boolean|null|undefined} boolValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.boolValue = null;

            /**
             * Value structValue.
             * @member {google.protobuf.Struct.$Properties|null|undefined} structValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.structValue = null;

            /**
             * Value listValue.
             * @member {google.protobuf.ListValue.$Properties|null|undefined} listValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.listValue = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * Value kind.
             * @member {"nullValue"|"numberValue"|"stringValue"|"boolValue"|"structValue"|"listValue"|undefined} kind
             * @memberof google.protobuf.Value
             * @instance
             */
            $Object.defineProperty(Value.prototype, "kind", {
                get: $util.oneOfGetter($oneOfFields = ["nullValue", "numberValue", "stringValue", "boolValue", "structValue", "listValue"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new Value instance using the specified properties.
             * @function create
             * @memberof google.protobuf.Value
             * @static
             * @param {google.protobuf.Value.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.Value} Value instance
             * @type {{
             *   (properties: google.protobuf.Value.$Shape): google.protobuf.Value & google.protobuf.Value.$Shape;
             *   (properties?: google.protobuf.Value.$Properties): google.protobuf.Value;
             * }}
             */
            Value.create = function(properties) {
                return new Value(properties);
            };

            /**
             * Encodes the specified Value message. Does not implicitly {@link google.protobuf.Value.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.Value
             * @static
             * @param {google.protobuf.Value.$Properties} message Value message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Value.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.nullValue != null && $Object.hasOwnProperty.call(message, "nullValue"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.nullValue);
                if (message.numberValue != null && $Object.hasOwnProperty.call(message, "numberValue"))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.numberValue);
                if (message.stringValue != null && $Object.hasOwnProperty.call(message, "stringValue"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.stringValue);
                if (message.boolValue != null && $Object.hasOwnProperty.call(message, "boolValue"))
                    writer.uint32(/* id 4, wireType 0 =*/32).bool(message.boolValue);
                if (message.structValue != null && $Object.hasOwnProperty.call(message, "structValue"))
                    $root.google.protobuf.Struct.encode(message.structValue, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                if (message.listValue != null && $Object.hasOwnProperty.call(message, "listValue"))
                    $root.google.protobuf.ListValue.encode(message.listValue, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a Value message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.Value
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.Value & google.protobuf.Value.$Shape} Value
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Value.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.google.protobuf.Value();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            message.nullValue = reader.int32();
                            message.kind = "nullValue";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 1)
                                break;
                            message.numberValue = reader.double();
                            message.kind = "numberValue";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.stringValue = reader.stringVerify();
                            message.kind = "stringValue";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            message.boolValue = reader.bool();
                            message.kind = "boolValue";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.structValue = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.structValue);
                            message.kind = "structValue";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.listValue = $root.google.protobuf.ListValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.listValue);
                            message.kind = "listValue";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a Value message.
             * @function verify
             * @memberof google.protobuf.Value
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Value.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.nullValue != null && $Object.hasOwnProperty.call(message, "nullValue")) {
                    properties.kind = 1;
                    if (typeof message.nullValue !== "number" || (message.nullValue | 0) !== message.nullValue)
                        return "nullValue: enum value expected";
                }
                if (message.numberValue != null && $Object.hasOwnProperty.call(message, "numberValue")) {
                    if (properties.kind === 1)
                        return "kind: multiple values";
                    properties.kind = 1;
                    if (typeof message.numberValue !== "number")
                        return "numberValue: number expected";
                }
                if (message.stringValue != null && $Object.hasOwnProperty.call(message, "stringValue")) {
                    if (properties.kind === 1)
                        return "kind: multiple values";
                    properties.kind = 1;
                    if (!$util.isString(message.stringValue))
                        return "stringValue: string expected";
                }
                if (message.boolValue != null && $Object.hasOwnProperty.call(message, "boolValue")) {
                    if (properties.kind === 1)
                        return "kind: multiple values";
                    properties.kind = 1;
                    if (typeof message.boolValue !== "boolean")
                        return "boolValue: boolean expected";
                }
                if (message.structValue != null && $Object.hasOwnProperty.call(message, "structValue")) {
                    if (properties.kind === 1)
                        return "kind: multiple values";
                    properties.kind = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.structValue, _depth + 1);
                        if (error)
                            return "structValue." + error;
                    }
                }
                if (message.listValue != null && $Object.hasOwnProperty.call(message, "listValue")) {
                    if (properties.kind === 1)
                        return "kind: multiple values";
                    properties.kind = 1;
                    {
                        let error = $root.google.protobuf.ListValue.verify(message.listValue, _depth + 1);
                        if (error)
                            return "listValue." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a Value message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.Value
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.Value} Value
             */
            Value.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.Value)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.Value: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.Value();
                switch (object.nullValue) {
                case "NULL_VALUE":
                case 0:
                    message.nullValue = 0;
                    break;
                default:
                    if (typeof object.nullValue === "number" && (object.nullValue | 0) === object.nullValue)
                        message.nullValue = object.nullValue;
                }
                if (object.numberValue != null)
                    message.numberValue = $Number(object.numberValue);
                if (object.stringValue != null)
                    message.stringValue = $String(object.stringValue);
                if (object.boolValue != null)
                    message.boolValue = $Boolean(object.boolValue);
                if (object.structValue != null) {
                    if (!$util.isObject(object.structValue))
                        throw $TypeError(".google.protobuf.Value.structValue: object expected");
                    message.structValue = $root.google.protobuf.Struct.fromObject(object.structValue, _depth + 1);
                }
                if (object.listValue != null) {
                    if (!$util.isObject(object.listValue))
                        throw $TypeError(".google.protobuf.Value.listValue: object expected");
                    message.listValue = $root.google.protobuf.ListValue.fromObject(object.listValue, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a Value message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.Value
             * @static
             * @param {google.protobuf.Value} message Value
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Value.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.nullValue != null && $Object.hasOwnProperty.call(message, "nullValue")) {
                    object.nullValue = options.enums === $String ? $root.google.protobuf.NullValue[message.nullValue] === $undefined ? message.nullValue : $root.google.protobuf.NullValue[message.nullValue] : message.nullValue;
                    if (options.oneofs)
                        object.kind = "nullValue";
                }
                if (message.numberValue != null && $Object.hasOwnProperty.call(message, "numberValue")) {
                    object.numberValue = options.json && !$isFinite(message.numberValue) ? $String(message.numberValue) : message.numberValue;
                    if (options.oneofs)
                        object.kind = "numberValue";
                }
                if (message.stringValue != null && $Object.hasOwnProperty.call(message, "stringValue")) {
                    object.stringValue = message.stringValue;
                    if (options.oneofs)
                        object.kind = "stringValue";
                }
                if (message.boolValue != null && $Object.hasOwnProperty.call(message, "boolValue")) {
                    object.boolValue = message.boolValue;
                    if (options.oneofs)
                        object.kind = "boolValue";
                }
                if (message.structValue != null && $Object.hasOwnProperty.call(message, "structValue")) {
                    object.structValue = $root.google.protobuf.Struct.toObject(message.structValue, options, _depth + 1);
                    if (options.oneofs)
                        object.kind = "structValue";
                }
                if (message.listValue != null && $Object.hasOwnProperty.call(message, "listValue")) {
                    object.listValue = $root.google.protobuf.ListValue.toObject(message.listValue, options, _depth + 1);
                    if (options.oneofs)
                        object.kind = "listValue";
                }
                return object;
            };

            /**
             * Converts this Value to JSON.
             * @function toJSON
             * @memberof google.protobuf.Value
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Value.prototype.toJSON = function() {
                return Value.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Value
             * @function getTypeUrl
             * @memberof google.protobuf.Value
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Value.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.Value";
            };

            return Value;
        })();

        /**
         * NullValue enum.
         * @name google.protobuf.NullValue
         * @enum {number}
         * @property {number} NULL_VALUE=0 NULL_VALUE value
         */
        protobuf.NullValue = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "NULL_VALUE"] = 0;
            return values;
        })();

        protobuf.ListValue = (function() {

            /**
             * Properties of a ListValue.
             * @typedef {Object} google.protobuf.ListValue.$Properties
             * @property {Array.<google.protobuf.Value.$Properties>|null} [values] ListValue values
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ListValue.
             * @memberof google.protobuf
             * @interface IListValue
             * @augments google.protobuf.ListValue.$Properties
             * @deprecated Use google.protobuf.ListValue.$Properties instead.
             */

            /**
             * Shape of a ListValue.
             * @typedef {{
             *   values?: Array.<google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} google.protobuf.ListValue.$Shape
             */

            /**
             * Constructs a new ListValue.
             * @memberof google.protobuf
             * @classdesc Represents a ListValue.
             * @constructor
             * @param {google.protobuf.ListValue.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ListValue = function (properties) {
                this.values = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ListValue values.
             * @member {Array.<google.protobuf.Value.$Properties>} values
             * @memberof google.protobuf.ListValue
             * @instance
             */
            ListValue.prototype.values = $util.emptyArray;

            /**
             * Creates a new ListValue instance using the specified properties.
             * @function create
             * @memberof google.protobuf.ListValue
             * @static
             * @param {google.protobuf.ListValue.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.ListValue} ListValue instance
             * @type {{
             *   (properties: google.protobuf.ListValue.$Shape): google.protobuf.ListValue & google.protobuf.ListValue.$Shape;
             *   (properties?: google.protobuf.ListValue.$Properties): google.protobuf.ListValue;
             * }}
             */
            ListValue.create = function(properties) {
                return new ListValue(properties);
            };

            /**
             * Encodes the specified ListValue message. Does not implicitly {@link google.protobuf.ListValue.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.ListValue
             * @static
             * @param {google.protobuf.ListValue.$Properties} message ListValue message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ListValue.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.values != null && message.values.length)
                    for (let i = 0; i < message.values.length; ++i)
                        $root.google.protobuf.Value.encode(message.values[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a ListValue message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.ListValue
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.ListValue & google.protobuf.ListValue.$Shape} ListValue
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ListValue.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.google.protobuf.ListValue();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.values && message.values.length))
                                message.values = [];
                            message.values.push($root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a ListValue message.
             * @function verify
             * @memberof google.protobuf.ListValue
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ListValue.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.values != null && $Object.hasOwnProperty.call(message, "values")) {
                    if (!$Array.isArray(message.values))
                        return "values: array expected";
                    for (let i = 0; i < message.values.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.values[i], _depth + 1);
                        if (error)
                            return "values." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a ListValue message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.ListValue
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.ListValue} ListValue
             */
            ListValue.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.ListValue)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.ListValue: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.ListValue();
                if (object.values) {
                    if (!$Array.isArray(object.values))
                        throw $TypeError(".google.protobuf.ListValue.values: array expected");
                    message.values = $Array(object.values.length);
                    for (let i = 0; i < object.values.length; ++i) {
                        if (!$util.isObject(object.values[i]))
                            throw $TypeError(".google.protobuf.ListValue.values: object expected");
                        message.values[i] = $root.google.protobuf.Value.fromObject(object.values[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a ListValue message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.ListValue
             * @static
             * @param {google.protobuf.ListValue} message ListValue
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ListValue.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.values = [];
                if (message.values && message.values.length) {
                    object.values = $Array(message.values.length);
                    for (let j = 0; j < message.values.length; ++j)
                        object.values[j] = $root.google.protobuf.Value.toObject(message.values[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this ListValue to JSON.
             * @function toJSON
             * @memberof google.protobuf.ListValue
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ListValue.prototype.toJSON = function() {
                return ListValue.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ListValue
             * @function getTypeUrl
             * @memberof google.protobuf.ListValue
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ListValue.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.ListValue";
            };

            return ListValue;
        })();

        return protobuf;
    })();

    return google;
})();

export const northstar = $root.northstar = (() => {

    /**
     * Namespace northstar.
     * @exports northstar
     * @namespace
     */
    const northstar = {};

    northstar.research = (function() {

        /**
         * Namespace research.
         * @memberof northstar
         * @namespace
         */
        const research = {};

        research.AdvanceRequest = (function() {

            /**
             * Properties of an AdvanceRequest.
             * @typedef {Object} northstar.research.AdvanceRequest.$Properties
             * @property {string|null} [request_id] AdvanceRequest request_id
             * @property {"request_id"} [_request_id] AdvanceRequest _request_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an AdvanceRequest.
             * @memberof northstar.research
             * @interface IAdvanceRequest
             * @augments northstar.research.AdvanceRequest.$Properties
             * @deprecated Use northstar.research.AdvanceRequest.$Properties instead.
             */

            /**
             * Narrowed shape of an AdvanceRequest.
             * @typedef {{
             *   request_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * )} northstar.research.AdvanceRequest.$Shape
             */

            /**
             * Constructs a new AdvanceRequest.
             * @memberof northstar.research
             * @classdesc Represents an AdvanceRequest.
             * @constructor
             * @param {northstar.research.AdvanceRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const AdvanceRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * AdvanceRequest request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.research.AdvanceRequest
             * @instance
             */
            AdvanceRequest.prototype.request_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * AdvanceRequest _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.research.AdvanceRequest
             * @instance
             */
            $Object.defineProperty(AdvanceRequest.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AdvanceRequest instance using the specified properties.
             * @function create
             * @memberof northstar.research.AdvanceRequest
             * @static
             * @param {northstar.research.AdvanceRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.research.AdvanceRequest} AdvanceRequest instance
             * @type {{
             *   (properties: northstar.research.AdvanceRequest.$Shape): northstar.research.AdvanceRequest & northstar.research.AdvanceRequest.$Shape;
             *   (properties?: northstar.research.AdvanceRequest.$Properties): northstar.research.AdvanceRequest;
             * }}
             */
            AdvanceRequest.create = function(properties) {
                return new AdvanceRequest(properties);
            };

            /**
             * Encodes the specified AdvanceRequest message. Does not implicitly {@link northstar.research.AdvanceRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.AdvanceRequest
             * @static
             * @param {northstar.research.AdvanceRequest.$Properties} message AdvanceRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AdvanceRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.request_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes an AdvanceRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.AdvanceRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.AdvanceRequest & northstar.research.AdvanceRequest.$Shape} AdvanceRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AdvanceRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.AdvanceRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies an AdvanceRequest message.
             * @function verify
             * @memberof northstar.research.AdvanceRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AdvanceRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                return null;
            };

            /**
             * Creates an AdvanceRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.AdvanceRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.AdvanceRequest} AdvanceRequest
             */
            AdvanceRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.AdvanceRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.AdvanceRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.AdvanceRequest();
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                return message;
            };

            /**
             * Creates a plain object from an AdvanceRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.AdvanceRequest
             * @static
             * @param {northstar.research.AdvanceRequest} message AdvanceRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AdvanceRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                return object;
            };

            /**
             * Converts this AdvanceRequest to JSON.
             * @function toJSON
             * @memberof northstar.research.AdvanceRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AdvanceRequest.prototype.toJSON = function() {
                return AdvanceRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for AdvanceRequest
             * @function getTypeUrl
             * @memberof northstar.research.AdvanceRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            AdvanceRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.AdvanceRequest";
            };

            return AdvanceRequest;
        })();

        research.Annotation = (function() {

            /**
             * Properties of an Annotation.
             * @typedef {Object} northstar.research.Annotation.$Properties
             * @property {string|null} [at] Annotation at
             * @property {string|null} [description] Annotation description
             * @property {"at"} [_at] Annotation _at
             * @property {"description"} [_description] Annotation _description
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an Annotation.
             * @memberof northstar.research
             * @interface IAnnotation
             * @augments northstar.research.Annotation.$Properties
             * @deprecated Use northstar.research.Annotation.$Properties instead.
             */

            /**
             * Narrowed shape of an Annotation.
             * @typedef {{
             *   at?: string|null;
             *   description?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _at?: undefined; at?: null }|{ _at?: "at"; at: string })
             * ) & (
             *   ({ _description?: undefined; description?: null }|{ _description?: "description"; description: string })
             * )} northstar.research.Annotation.$Shape
             */

            /**
             * Constructs a new Annotation.
             * @memberof northstar.research
             * @classdesc Represents an Annotation.
             * @constructor
             * @param {northstar.research.Annotation.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const Annotation = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Annotation at.
             * @member {string|null|undefined} at
             * @memberof northstar.research.Annotation
             * @instance
             */
            Annotation.prototype.at = null;

            /**
             * Annotation description.
             * @member {string|null|undefined} description
             * @memberof northstar.research.Annotation
             * @instance
             */
            Annotation.prototype.description = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * Annotation _at.
             * @member {"at"|undefined} _at
             * @memberof northstar.research.Annotation
             * @instance
             */
            $Object.defineProperty(Annotation.prototype, "_at", {
                get: $util.oneOfGetter($oneOfFields = ["at"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Annotation _description.
             * @member {"description"|undefined} _description
             * @memberof northstar.research.Annotation
             * @instance
             */
            $Object.defineProperty(Annotation.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new Annotation instance using the specified properties.
             * @function create
             * @memberof northstar.research.Annotation
             * @static
             * @param {northstar.research.Annotation.$Properties=} [properties] Properties to set
             * @returns {northstar.research.Annotation} Annotation instance
             * @type {{
             *   (properties: northstar.research.Annotation.$Shape): northstar.research.Annotation & northstar.research.Annotation.$Shape;
             *   (properties?: northstar.research.Annotation.$Properties): northstar.research.Annotation;
             * }}
             */
            Annotation.create = function(properties) {
                return new Annotation(properties);
            };

            /**
             * Encodes the specified Annotation message. Does not implicitly {@link northstar.research.Annotation.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.Annotation
             * @static
             * @param {northstar.research.Annotation.$Properties} message Annotation message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Annotation.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.at != null && $Object.hasOwnProperty.call(message, "at"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.at);
                if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.description);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes an Annotation message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.Annotation
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.Annotation & northstar.research.Annotation.$Shape} Annotation
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Annotation.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.Annotation();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.at = reader.stringVerify();
                            message._at = "at";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.description = reader.stringVerify();
                            message._description = "description";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies an Annotation message.
             * @function verify
             * @memberof northstar.research.Annotation
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Annotation.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.at != null && $Object.hasOwnProperty.call(message, "at")) {
                    properties._at = 1;
                    if (!$util.isString(message.at))
                        return "at: string expected";
                }
                if (message.description != null && $Object.hasOwnProperty.call(message, "description")) {
                    properties._description = 1;
                    if (!$util.isString(message.description))
                        return "description: string expected";
                }
                return null;
            };

            /**
             * Creates an Annotation message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.Annotation
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.Annotation} Annotation
             */
            Annotation.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.Annotation)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.Annotation: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.Annotation();
                if (object.at != null)
                    message.at = $String(object.at);
                if (object.description != null)
                    message.description = $String(object.description);
                return message;
            };

            /**
             * Creates a plain object from an Annotation message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.Annotation
             * @static
             * @param {northstar.research.Annotation} message Annotation
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Annotation.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.at != null && $Object.hasOwnProperty.call(message, "at")) {
                    object.at = message.at;
                    if (options.oneofs)
                        object._at = "at";
                }
                if (message.description != null && $Object.hasOwnProperty.call(message, "description")) {
                    object.description = message.description;
                    if (options.oneofs)
                        object._description = "description";
                }
                return object;
            };

            /**
             * Converts this Annotation to JSON.
             * @function toJSON
             * @memberof northstar.research.Annotation
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Annotation.prototype.toJSON = function() {
                return Annotation.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Annotation
             * @function getTypeUrl
             * @memberof northstar.research.Annotation
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Annotation.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.Annotation";
            };

            return Annotation;
        })();

        research.AnnotationRequest = (function() {

            /**
             * Properties of an AnnotationRequest.
             * @typedef {Object} northstar.research.AnnotationRequest.$Properties
             * @property {string|null} [description] AnnotationRequest description
             * @property {"description"} [_description] AnnotationRequest _description
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an AnnotationRequest.
             * @memberof northstar.research
             * @interface IAnnotationRequest
             * @augments northstar.research.AnnotationRequest.$Properties
             * @deprecated Use northstar.research.AnnotationRequest.$Properties instead.
             */

            /**
             * Narrowed shape of an AnnotationRequest.
             * @typedef {{
             *   description?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _description?: undefined; description?: null }|{ _description?: "description"; description: string })
             * )} northstar.research.AnnotationRequest.$Shape
             */

            /**
             * Constructs a new AnnotationRequest.
             * @memberof northstar.research
             * @classdesc Represents an AnnotationRequest.
             * @constructor
             * @param {northstar.research.AnnotationRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const AnnotationRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * AnnotationRequest description.
             * @member {string|null|undefined} description
             * @memberof northstar.research.AnnotationRequest
             * @instance
             */
            AnnotationRequest.prototype.description = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * AnnotationRequest _description.
             * @member {"description"|undefined} _description
             * @memberof northstar.research.AnnotationRequest
             * @instance
             */
            $Object.defineProperty(AnnotationRequest.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AnnotationRequest instance using the specified properties.
             * @function create
             * @memberof northstar.research.AnnotationRequest
             * @static
             * @param {northstar.research.AnnotationRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.research.AnnotationRequest} AnnotationRequest instance
             * @type {{
             *   (properties: northstar.research.AnnotationRequest.$Shape): northstar.research.AnnotationRequest & northstar.research.AnnotationRequest.$Shape;
             *   (properties?: northstar.research.AnnotationRequest.$Properties): northstar.research.AnnotationRequest;
             * }}
             */
            AnnotationRequest.create = function(properties) {
                return new AnnotationRequest(properties);
            };

            /**
             * Encodes the specified AnnotationRequest message. Does not implicitly {@link northstar.research.AnnotationRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.AnnotationRequest
             * @static
             * @param {northstar.research.AnnotationRequest.$Properties} message AnnotationRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AnnotationRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.description);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes an AnnotationRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.AnnotationRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.AnnotationRequest & northstar.research.AnnotationRequest.$Shape} AnnotationRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AnnotationRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.AnnotationRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.description = reader.stringVerify();
                            message._description = "description";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies an AnnotationRequest message.
             * @function verify
             * @memberof northstar.research.AnnotationRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AnnotationRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.description != null && $Object.hasOwnProperty.call(message, "description")) {
                    properties._description = 1;
                    if (!$util.isString(message.description))
                        return "description: string expected";
                }
                return null;
            };

            /**
             * Creates an AnnotationRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.AnnotationRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.AnnotationRequest} AnnotationRequest
             */
            AnnotationRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.AnnotationRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.AnnotationRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.AnnotationRequest();
                if (object.description != null)
                    message.description = $String(object.description);
                return message;
            };

            /**
             * Creates a plain object from an AnnotationRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.AnnotationRequest
             * @static
             * @param {northstar.research.AnnotationRequest} message AnnotationRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AnnotationRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.description != null && $Object.hasOwnProperty.call(message, "description")) {
                    object.description = message.description;
                    if (options.oneofs)
                        object._description = "description";
                }
                return object;
            };

            /**
             * Converts this AnnotationRequest to JSON.
             * @function toJSON
             * @memberof northstar.research.AnnotationRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AnnotationRequest.prototype.toJSON = function() {
                return AnnotationRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for AnnotationRequest
             * @function getTypeUrl
             * @memberof northstar.research.AnnotationRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            AnnotationRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.AnnotationRequest";
            };

            return AnnotationRequest;
        })();

        research.BrowserSession = (function() {

            /**
             * Properties of a BrowserSession.
             * @typedef {Object} northstar.research.BrowserSession.$Properties
             * @property {string|null} [csrf] BrowserSession csrf
             * @property {"csrf"} [_csrf] BrowserSession _csrf
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a BrowserSession.
             * @memberof northstar.research
             * @interface IBrowserSession
             * @augments northstar.research.BrowserSession.$Properties
             * @deprecated Use northstar.research.BrowserSession.$Properties instead.
             */

            /**
             * Narrowed shape of a BrowserSession.
             * @typedef {{
             *   csrf?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _csrf?: undefined; csrf?: null }|{ _csrf?: "csrf"; csrf: string })
             * )} northstar.research.BrowserSession.$Shape
             */

            /**
             * Constructs a new BrowserSession.
             * @memberof northstar.research
             * @classdesc Represents a BrowserSession.
             * @constructor
             * @param {northstar.research.BrowserSession.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const BrowserSession = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * BrowserSession csrf.
             * @member {string|null|undefined} csrf
             * @memberof northstar.research.BrowserSession
             * @instance
             */
            BrowserSession.prototype.csrf = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * BrowserSession _csrf.
             * @member {"csrf"|undefined} _csrf
             * @memberof northstar.research.BrowserSession
             * @instance
             */
            $Object.defineProperty(BrowserSession.prototype, "_csrf", {
                get: $util.oneOfGetter($oneOfFields = ["csrf"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BrowserSession instance using the specified properties.
             * @function create
             * @memberof northstar.research.BrowserSession
             * @static
             * @param {northstar.research.BrowserSession.$Properties=} [properties] Properties to set
             * @returns {northstar.research.BrowserSession} BrowserSession instance
             * @type {{
             *   (properties: northstar.research.BrowserSession.$Shape): northstar.research.BrowserSession & northstar.research.BrowserSession.$Shape;
             *   (properties?: northstar.research.BrowserSession.$Properties): northstar.research.BrowserSession;
             * }}
             */
            BrowserSession.create = function(properties) {
                return new BrowserSession(properties);
            };

            /**
             * Encodes the specified BrowserSession message. Does not implicitly {@link northstar.research.BrowserSession.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.BrowserSession
             * @static
             * @param {northstar.research.BrowserSession.$Properties} message BrowserSession message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BrowserSession.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.csrf != null && $Object.hasOwnProperty.call(message, "csrf"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.csrf);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a BrowserSession message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.BrowserSession
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.BrowserSession & northstar.research.BrowserSession.$Shape} BrowserSession
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BrowserSession.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.BrowserSession();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.csrf = reader.stringVerify();
                            message._csrf = "csrf";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a BrowserSession message.
             * @function verify
             * @memberof northstar.research.BrowserSession
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BrowserSession.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.csrf != null && $Object.hasOwnProperty.call(message, "csrf")) {
                    properties._csrf = 1;
                    if (!$util.isString(message.csrf))
                        return "csrf: string expected";
                }
                return null;
            };

            /**
             * Creates a BrowserSession message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.BrowserSession
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.BrowserSession} BrowserSession
             */
            BrowserSession.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.BrowserSession)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.BrowserSession: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.BrowserSession();
                if (object.csrf != null)
                    message.csrf = $String(object.csrf);
                return message;
            };

            /**
             * Creates a plain object from a BrowserSession message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.BrowserSession
             * @static
             * @param {northstar.research.BrowserSession} message BrowserSession
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BrowserSession.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.csrf != null && $Object.hasOwnProperty.call(message, "csrf")) {
                    object.csrf = message.csrf;
                    if (options.oneofs)
                        object._csrf = "csrf";
                }
                return object;
            };

            /**
             * Converts this BrowserSession to JSON.
             * @function toJSON
             * @memberof northstar.research.BrowserSession
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BrowserSession.prototype.toJSON = function() {
                return BrowserSession.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for BrowserSession
             * @function getTypeUrl
             * @memberof northstar.research.BrowserSession
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            BrowserSession.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.BrowserSession";
            };

            return BrowserSession;
        })();

        research.Catalog = (function() {

            /**
             * Properties of a Catalog.
             * @typedef {Object} northstar.research.Catalog.$Properties
             * @property {Array.<northstar.research.FactorSummary.$Properties>|null} [factors] Catalog factors
             * @property {Array.<northstar.research.StrategySummary.$Properties>|null} [strategies] Catalog strategies
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Catalog.
             * @memberof northstar.research
             * @interface ICatalog
             * @augments northstar.research.Catalog.$Properties
             * @deprecated Use northstar.research.Catalog.$Properties instead.
             */

            /**
             * Shape of a Catalog.
             * @typedef {{
             *   factors?: Array.<northstar.research.FactorSummary.$Shape>|null;
             *   strategies?: Array.<northstar.research.StrategySummary.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} northstar.research.Catalog.$Shape
             */

            /**
             * Constructs a new Catalog.
             * @memberof northstar.research
             * @classdesc Represents a Catalog.
             * @constructor
             * @param {northstar.research.Catalog.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const Catalog = function (properties) {
                this.factors = [];
                this.strategies = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Catalog factors.
             * @member {Array.<northstar.research.FactorSummary.$Properties>} factors
             * @memberof northstar.research.Catalog
             * @instance
             */
            Catalog.prototype.factors = $util.emptyArray;

            /**
             * Catalog strategies.
             * @member {Array.<northstar.research.StrategySummary.$Properties>} strategies
             * @memberof northstar.research.Catalog
             * @instance
             */
            Catalog.prototype.strategies = $util.emptyArray;

            /**
             * Creates a new Catalog instance using the specified properties.
             * @function create
             * @memberof northstar.research.Catalog
             * @static
             * @param {northstar.research.Catalog.$Properties=} [properties] Properties to set
             * @returns {northstar.research.Catalog} Catalog instance
             * @type {{
             *   (properties: northstar.research.Catalog.$Shape): northstar.research.Catalog & northstar.research.Catalog.$Shape;
             *   (properties?: northstar.research.Catalog.$Properties): northstar.research.Catalog;
             * }}
             */
            Catalog.create = function(properties) {
                return new Catalog(properties);
            };

            /**
             * Encodes the specified Catalog message. Does not implicitly {@link northstar.research.Catalog.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.Catalog
             * @static
             * @param {northstar.research.Catalog.$Properties} message Catalog message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Catalog.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.factors != null && message.factors.length)
                    for (let i = 0; i < message.factors.length; ++i)
                        $root.northstar.research.FactorSummary.encode(message.factors[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.strategies != null && message.strategies.length)
                    for (let i = 0; i < message.strategies.length; ++i)
                        $root.northstar.research.StrategySummary.encode(message.strategies[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a Catalog message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.Catalog
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.Catalog & northstar.research.Catalog.$Shape} Catalog
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Catalog.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.Catalog();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.factors && message.factors.length))
                                message.factors = [];
                            message.factors.push($root.northstar.research.FactorSummary.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.strategies && message.strategies.length))
                                message.strategies = [];
                            message.strategies.push($root.northstar.research.StrategySummary.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a Catalog message.
             * @function verify
             * @memberof northstar.research.Catalog
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Catalog.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.factors != null && $Object.hasOwnProperty.call(message, "factors")) {
                    if (!$Array.isArray(message.factors))
                        return "factors: array expected";
                    for (let i = 0; i < message.factors.length; ++i) {
                        let error = $root.northstar.research.FactorSummary.verify(message.factors[i], _depth + 1);
                        if (error)
                            return "factors." + error;
                    }
                }
                if (message.strategies != null && $Object.hasOwnProperty.call(message, "strategies")) {
                    if (!$Array.isArray(message.strategies))
                        return "strategies: array expected";
                    for (let i = 0; i < message.strategies.length; ++i) {
                        let error = $root.northstar.research.StrategySummary.verify(message.strategies[i], _depth + 1);
                        if (error)
                            return "strategies." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a Catalog message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.Catalog
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.Catalog} Catalog
             */
            Catalog.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.Catalog)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.Catalog: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.Catalog();
                if (object.factors) {
                    if (!$Array.isArray(object.factors))
                        throw $TypeError(".northstar.research.Catalog.factors: array expected");
                    message.factors = $Array(object.factors.length);
                    for (let i = 0; i < object.factors.length; ++i) {
                        if (!$util.isObject(object.factors[i]))
                            throw $TypeError(".northstar.research.Catalog.factors: object expected");
                        message.factors[i] = $root.northstar.research.FactorSummary.fromObject(object.factors[i], _depth + 1);
                    }
                }
                if (object.strategies) {
                    if (!$Array.isArray(object.strategies))
                        throw $TypeError(".northstar.research.Catalog.strategies: array expected");
                    message.strategies = $Array(object.strategies.length);
                    for (let i = 0; i < object.strategies.length; ++i) {
                        if (!$util.isObject(object.strategies[i]))
                            throw $TypeError(".northstar.research.Catalog.strategies: object expected");
                        message.strategies[i] = $root.northstar.research.StrategySummary.fromObject(object.strategies[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a Catalog message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.Catalog
             * @static
             * @param {northstar.research.Catalog} message Catalog
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Catalog.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.factors = [];
                    object.strategies = [];
                }
                if (message.factors && message.factors.length) {
                    object.factors = $Array(message.factors.length);
                    for (let j = 0; j < message.factors.length; ++j)
                        object.factors[j] = $root.northstar.research.FactorSummary.toObject(message.factors[j], options, _depth + 1);
                }
                if (message.strategies && message.strategies.length) {
                    object.strategies = $Array(message.strategies.length);
                    for (let j = 0; j < message.strategies.length; ++j)
                        object.strategies[j] = $root.northstar.research.StrategySummary.toObject(message.strategies[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this Catalog to JSON.
             * @function toJSON
             * @memberof northstar.research.Catalog
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Catalog.prototype.toJSON = function() {
                return Catalog.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Catalog
             * @function getTypeUrl
             * @memberof northstar.research.Catalog
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Catalog.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.Catalog";
            };

            return Catalog;
        })();

        research.Comparison = (function() {

            /**
             * Properties of a Comparison.
             * @typedef {Object} northstar.research.Comparison.$Properties
             * @property {number|Long|null} [bar_count] Comparison bar_count
             * @property {number|Long|null} [decision_count] Comparison decision_count
             * @property {string|null} [ending_cash] Comparison ending_cash
             * @property {string|null} [ending_equity] Comparison ending_equity
             * @property {number|Long|null} [ending_position_lots] Comparison ending_position_lots
             * @property {number|Long|null} [fill_count] Comparison fill_count
             * @property {string|null} [initial_cash] Comparison initial_cash
             * @property {string|null} [max_drawdown] Comparison max_drawdown
             * @property {string|null} [max_drawdown_fraction] Comparison max_drawdown_fraction
             * @property {string|null} [realized_pnl] Comparison realized_pnl
             * @property {string|null} [run_id] Comparison run_id
             * @property {string|null} [strategy] Comparison strategy
             * @property {string|null} [total_fees] Comparison total_fees
             * @property {string|null} [total_return] Comparison total_return
             * @property {string|null} [unrealized_pnl] Comparison unrealized_pnl
             * @property {"bar_count"} [_bar_count] Comparison _bar_count
             * @property {"decision_count"} [_decision_count] Comparison _decision_count
             * @property {"ending_cash"} [_ending_cash] Comparison _ending_cash
             * @property {"ending_equity"} [_ending_equity] Comparison _ending_equity
             * @property {"ending_position_lots"} [_ending_position_lots] Comparison _ending_position_lots
             * @property {"fill_count"} [_fill_count] Comparison _fill_count
             * @property {"initial_cash"} [_initial_cash] Comparison _initial_cash
             * @property {"max_drawdown"} [_max_drawdown] Comparison _max_drawdown
             * @property {"max_drawdown_fraction"} [_max_drawdown_fraction] Comparison _max_drawdown_fraction
             * @property {"realized_pnl"} [_realized_pnl] Comparison _realized_pnl
             * @property {"run_id"} [_run_id] Comparison _run_id
             * @property {"strategy"} [_strategy] Comparison _strategy
             * @property {"total_fees"} [_total_fees] Comparison _total_fees
             * @property {"total_return"} [_total_return] Comparison _total_return
             * @property {"unrealized_pnl"} [_unrealized_pnl] Comparison _unrealized_pnl
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Comparison.
             * @memberof northstar.research
             * @interface IComparison
             * @augments northstar.research.Comparison.$Properties
             * @deprecated Use northstar.research.Comparison.$Properties instead.
             */

            /**
             * Narrowed shape of a Comparison.
             * @typedef {{
             *   bar_count?: number|Long|null;
             *   decision_count?: number|Long|null;
             *   ending_cash?: string|null;
             *   ending_equity?: string|null;
             *   ending_position_lots?: number|Long|null;
             *   fill_count?: number|Long|null;
             *   initial_cash?: string|null;
             *   max_drawdown?: string|null;
             *   max_drawdown_fraction?: string|null;
             *   realized_pnl?: string|null;
             *   run_id?: string|null;
             *   strategy?: string|null;
             *   total_fees?: string|null;
             *   total_return?: string|null;
             *   unrealized_pnl?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _bar_count?: undefined; bar_count?: null }|{ _bar_count?: "bar_count"; bar_count: number|Long })
             * ) & (
             *   ({ _decision_count?: undefined; decision_count?: null }|{ _decision_count?: "decision_count"; decision_count: number|Long })
             * ) & (
             *   ({ _ending_cash?: undefined; ending_cash?: null }|{ _ending_cash?: "ending_cash"; ending_cash: string })
             * ) & (
             *   ({ _ending_equity?: undefined; ending_equity?: null }|{ _ending_equity?: "ending_equity"; ending_equity: string })
             * ) & (
             *   ({ _ending_position_lots?: undefined; ending_position_lots?: null }|{ _ending_position_lots?: "ending_position_lots"; ending_position_lots: number|Long })
             * ) & (
             *   ({ _fill_count?: undefined; fill_count?: null }|{ _fill_count?: "fill_count"; fill_count: number|Long })
             * ) & (
             *   ({ _initial_cash?: undefined; initial_cash?: null }|{ _initial_cash?: "initial_cash"; initial_cash: string })
             * ) & (
             *   ({ _max_drawdown?: undefined; max_drawdown?: null }|{ _max_drawdown?: "max_drawdown"; max_drawdown: string })
             * ) & (
             *   ({ _max_drawdown_fraction?: undefined; max_drawdown_fraction?: null }|{ _max_drawdown_fraction?: "max_drawdown_fraction"; max_drawdown_fraction: string })
             * ) & (
             *   ({ _realized_pnl?: undefined; realized_pnl?: null }|{ _realized_pnl?: "realized_pnl"; realized_pnl: string })
             * ) & (
             *   ({ _run_id?: undefined; run_id?: null }|{ _run_id?: "run_id"; run_id: string })
             * ) & (
             *   ({ _strategy?: undefined; strategy?: null }|{ _strategy?: "strategy"; strategy: string })
             * ) & (
             *   ({ _total_fees?: undefined; total_fees?: null }|{ _total_fees?: "total_fees"; total_fees: string })
             * ) & (
             *   ({ _total_return?: undefined; total_return?: null }|{ _total_return?: "total_return"; total_return: string })
             * ) & (
             *   ({ _unrealized_pnl?: undefined; unrealized_pnl?: null }|{ _unrealized_pnl?: "unrealized_pnl"; unrealized_pnl: string })
             * )} northstar.research.Comparison.$Shape
             */

            /**
             * Constructs a new Comparison.
             * @memberof northstar.research
             * @classdesc Represents a Comparison.
             * @constructor
             * @param {northstar.research.Comparison.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const Comparison = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Comparison bar_count.
             * @member {number|Long|null|undefined} bar_count
             * @memberof northstar.research.Comparison
             * @instance
             */
            Comparison.prototype.bar_count = null;

            /**
             * Comparison decision_count.
             * @member {number|Long|null|undefined} decision_count
             * @memberof northstar.research.Comparison
             * @instance
             */
            Comparison.prototype.decision_count = null;

            /**
             * Comparison ending_cash.
             * @member {string|null|undefined} ending_cash
             * @memberof northstar.research.Comparison
             * @instance
             */
            Comparison.prototype.ending_cash = null;

            /**
             * Comparison ending_equity.
             * @member {string|null|undefined} ending_equity
             * @memberof northstar.research.Comparison
             * @instance
             */
            Comparison.prototype.ending_equity = null;

            /**
             * Comparison ending_position_lots.
             * @member {number|Long|null|undefined} ending_position_lots
             * @memberof northstar.research.Comparison
             * @instance
             */
            Comparison.prototype.ending_position_lots = null;

            /**
             * Comparison fill_count.
             * @member {number|Long|null|undefined} fill_count
             * @memberof northstar.research.Comparison
             * @instance
             */
            Comparison.prototype.fill_count = null;

            /**
             * Comparison initial_cash.
             * @member {string|null|undefined} initial_cash
             * @memberof northstar.research.Comparison
             * @instance
             */
            Comparison.prototype.initial_cash = null;

            /**
             * Comparison max_drawdown.
             * @member {string|null|undefined} max_drawdown
             * @memberof northstar.research.Comparison
             * @instance
             */
            Comparison.prototype.max_drawdown = null;

            /**
             * Comparison max_drawdown_fraction.
             * @member {string|null|undefined} max_drawdown_fraction
             * @memberof northstar.research.Comparison
             * @instance
             */
            Comparison.prototype.max_drawdown_fraction = null;

            /**
             * Comparison realized_pnl.
             * @member {string|null|undefined} realized_pnl
             * @memberof northstar.research.Comparison
             * @instance
             */
            Comparison.prototype.realized_pnl = null;

            /**
             * Comparison run_id.
             * @member {string|null|undefined} run_id
             * @memberof northstar.research.Comparison
             * @instance
             */
            Comparison.prototype.run_id = null;

            /**
             * Comparison strategy.
             * @member {string|null|undefined} strategy
             * @memberof northstar.research.Comparison
             * @instance
             */
            Comparison.prototype.strategy = null;

            /**
             * Comparison total_fees.
             * @member {string|null|undefined} total_fees
             * @memberof northstar.research.Comparison
             * @instance
             */
            Comparison.prototype.total_fees = null;

            /**
             * Comparison total_return.
             * @member {string|null|undefined} total_return
             * @memberof northstar.research.Comparison
             * @instance
             */
            Comparison.prototype.total_return = null;

            /**
             * Comparison unrealized_pnl.
             * @member {string|null|undefined} unrealized_pnl
             * @memberof northstar.research.Comparison
             * @instance
             */
            Comparison.prototype.unrealized_pnl = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * Comparison _bar_count.
             * @member {"bar_count"|undefined} _bar_count
             * @memberof northstar.research.Comparison
             * @instance
             */
            $Object.defineProperty(Comparison.prototype, "_bar_count", {
                get: $util.oneOfGetter($oneOfFields = ["bar_count"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Comparison _decision_count.
             * @member {"decision_count"|undefined} _decision_count
             * @memberof northstar.research.Comparison
             * @instance
             */
            $Object.defineProperty(Comparison.prototype, "_decision_count", {
                get: $util.oneOfGetter($oneOfFields = ["decision_count"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Comparison _ending_cash.
             * @member {"ending_cash"|undefined} _ending_cash
             * @memberof northstar.research.Comparison
             * @instance
             */
            $Object.defineProperty(Comparison.prototype, "_ending_cash", {
                get: $util.oneOfGetter($oneOfFields = ["ending_cash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Comparison _ending_equity.
             * @member {"ending_equity"|undefined} _ending_equity
             * @memberof northstar.research.Comparison
             * @instance
             */
            $Object.defineProperty(Comparison.prototype, "_ending_equity", {
                get: $util.oneOfGetter($oneOfFields = ["ending_equity"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Comparison _ending_position_lots.
             * @member {"ending_position_lots"|undefined} _ending_position_lots
             * @memberof northstar.research.Comparison
             * @instance
             */
            $Object.defineProperty(Comparison.prototype, "_ending_position_lots", {
                get: $util.oneOfGetter($oneOfFields = ["ending_position_lots"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Comparison _fill_count.
             * @member {"fill_count"|undefined} _fill_count
             * @memberof northstar.research.Comparison
             * @instance
             */
            $Object.defineProperty(Comparison.prototype, "_fill_count", {
                get: $util.oneOfGetter($oneOfFields = ["fill_count"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Comparison _initial_cash.
             * @member {"initial_cash"|undefined} _initial_cash
             * @memberof northstar.research.Comparison
             * @instance
             */
            $Object.defineProperty(Comparison.prototype, "_initial_cash", {
                get: $util.oneOfGetter($oneOfFields = ["initial_cash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Comparison _max_drawdown.
             * @member {"max_drawdown"|undefined} _max_drawdown
             * @memberof northstar.research.Comparison
             * @instance
             */
            $Object.defineProperty(Comparison.prototype, "_max_drawdown", {
                get: $util.oneOfGetter($oneOfFields = ["max_drawdown"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Comparison _max_drawdown_fraction.
             * @member {"max_drawdown_fraction"|undefined} _max_drawdown_fraction
             * @memberof northstar.research.Comparison
             * @instance
             */
            $Object.defineProperty(Comparison.prototype, "_max_drawdown_fraction", {
                get: $util.oneOfGetter($oneOfFields = ["max_drawdown_fraction"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Comparison _realized_pnl.
             * @member {"realized_pnl"|undefined} _realized_pnl
             * @memberof northstar.research.Comparison
             * @instance
             */
            $Object.defineProperty(Comparison.prototype, "_realized_pnl", {
                get: $util.oneOfGetter($oneOfFields = ["realized_pnl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Comparison _run_id.
             * @member {"run_id"|undefined} _run_id
             * @memberof northstar.research.Comparison
             * @instance
             */
            $Object.defineProperty(Comparison.prototype, "_run_id", {
                get: $util.oneOfGetter($oneOfFields = ["run_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Comparison _strategy.
             * @member {"strategy"|undefined} _strategy
             * @memberof northstar.research.Comparison
             * @instance
             */
            $Object.defineProperty(Comparison.prototype, "_strategy", {
                get: $util.oneOfGetter($oneOfFields = ["strategy"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Comparison _total_fees.
             * @member {"total_fees"|undefined} _total_fees
             * @memberof northstar.research.Comparison
             * @instance
             */
            $Object.defineProperty(Comparison.prototype, "_total_fees", {
                get: $util.oneOfGetter($oneOfFields = ["total_fees"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Comparison _total_return.
             * @member {"total_return"|undefined} _total_return
             * @memberof northstar.research.Comparison
             * @instance
             */
            $Object.defineProperty(Comparison.prototype, "_total_return", {
                get: $util.oneOfGetter($oneOfFields = ["total_return"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Comparison _unrealized_pnl.
             * @member {"unrealized_pnl"|undefined} _unrealized_pnl
             * @memberof northstar.research.Comparison
             * @instance
             */
            $Object.defineProperty(Comparison.prototype, "_unrealized_pnl", {
                get: $util.oneOfGetter($oneOfFields = ["unrealized_pnl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new Comparison instance using the specified properties.
             * @function create
             * @memberof northstar.research.Comparison
             * @static
             * @param {northstar.research.Comparison.$Properties=} [properties] Properties to set
             * @returns {northstar.research.Comparison} Comparison instance
             * @type {{
             *   (properties: northstar.research.Comparison.$Shape): northstar.research.Comparison & northstar.research.Comparison.$Shape;
             *   (properties?: northstar.research.Comparison.$Properties): northstar.research.Comparison;
             * }}
             */
            Comparison.create = function(properties) {
                return new Comparison(properties);
            };

            /**
             * Encodes the specified Comparison message. Does not implicitly {@link northstar.research.Comparison.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.Comparison
             * @static
             * @param {northstar.research.Comparison.$Properties} message Comparison message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Comparison.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.bar_count != null && $Object.hasOwnProperty.call(message, "bar_count"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int64(message.bar_count);
                if (message.decision_count != null && $Object.hasOwnProperty.call(message, "decision_count"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.decision_count);
                if (message.ending_cash != null && $Object.hasOwnProperty.call(message, "ending_cash"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.ending_cash);
                if (message.ending_equity != null && $Object.hasOwnProperty.call(message, "ending_equity"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.ending_equity);
                if (message.ending_position_lots != null && $Object.hasOwnProperty.call(message, "ending_position_lots"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.ending_position_lots);
                if (message.fill_count != null && $Object.hasOwnProperty.call(message, "fill_count"))
                    writer.uint32(/* id 6, wireType 0 =*/48).int64(message.fill_count);
                if (message.initial_cash != null && $Object.hasOwnProperty.call(message, "initial_cash"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.initial_cash);
                if (message.max_drawdown != null && $Object.hasOwnProperty.call(message, "max_drawdown"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.max_drawdown);
                if (message.max_drawdown_fraction != null && $Object.hasOwnProperty.call(message, "max_drawdown_fraction"))
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.max_drawdown_fraction);
                if (message.realized_pnl != null && $Object.hasOwnProperty.call(message, "realized_pnl"))
                    writer.uint32(/* id 10, wireType 2 =*/82).string(message.realized_pnl);
                if (message.run_id != null && $Object.hasOwnProperty.call(message, "run_id"))
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.run_id);
                if (message.strategy != null && $Object.hasOwnProperty.call(message, "strategy"))
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.strategy);
                if (message.total_fees != null && $Object.hasOwnProperty.call(message, "total_fees"))
                    writer.uint32(/* id 13, wireType 2 =*/106).string(message.total_fees);
                if (message.total_return != null && $Object.hasOwnProperty.call(message, "total_return"))
                    writer.uint32(/* id 14, wireType 2 =*/114).string(message.total_return);
                if (message.unrealized_pnl != null && $Object.hasOwnProperty.call(message, "unrealized_pnl"))
                    writer.uint32(/* id 15, wireType 2 =*/122).string(message.unrealized_pnl);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a Comparison message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.Comparison
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.Comparison & northstar.research.Comparison.$Shape} Comparison
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Comparison.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.Comparison();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            message.bar_count = reader.int64();
                            message._bar_count = "bar_count";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            message.decision_count = reader.int64();
                            message._decision_count = "decision_count";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.ending_cash = reader.stringVerify();
                            message._ending_cash = "ending_cash";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.ending_equity = reader.stringVerify();
                            message._ending_equity = "ending_equity";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            message.ending_position_lots = reader.int64();
                            message._ending_position_lots = "ending_position_lots";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            message.fill_count = reader.int64();
                            message._fill_count = "fill_count";
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            message.initial_cash = reader.stringVerify();
                            message._initial_cash = "initial_cash";
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            message.max_drawdown = reader.stringVerify();
                            message._max_drawdown = "max_drawdown";
                            continue;
                        }
                    case 9: {
                            if (wireType !== 2)
                                break;
                            message.max_drawdown_fraction = reader.stringVerify();
                            message._max_drawdown_fraction = "max_drawdown_fraction";
                            continue;
                        }
                    case 10: {
                            if (wireType !== 2)
                                break;
                            message.realized_pnl = reader.stringVerify();
                            message._realized_pnl = "realized_pnl";
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            message.run_id = reader.stringVerify();
                            message._run_id = "run_id";
                            continue;
                        }
                    case 12: {
                            if (wireType !== 2)
                                break;
                            message.strategy = reader.stringVerify();
                            message._strategy = "strategy";
                            continue;
                        }
                    case 13: {
                            if (wireType !== 2)
                                break;
                            message.total_fees = reader.stringVerify();
                            message._total_fees = "total_fees";
                            continue;
                        }
                    case 14: {
                            if (wireType !== 2)
                                break;
                            message.total_return = reader.stringVerify();
                            message._total_return = "total_return";
                            continue;
                        }
                    case 15: {
                            if (wireType !== 2)
                                break;
                            message.unrealized_pnl = reader.stringVerify();
                            message._unrealized_pnl = "unrealized_pnl";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a Comparison message.
             * @function verify
             * @memberof northstar.research.Comparison
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Comparison.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.bar_count != null && $Object.hasOwnProperty.call(message, "bar_count")) {
                    properties._bar_count = 1;
                    if (!$util.isInteger(message.bar_count) && !(message.bar_count && $util.isInteger(message.bar_count.low) && $util.isInteger(message.bar_count.high)))
                        return "bar_count: integer|Long expected";
                }
                if (message.decision_count != null && $Object.hasOwnProperty.call(message, "decision_count")) {
                    properties._decision_count = 1;
                    if (!$util.isInteger(message.decision_count) && !(message.decision_count && $util.isInteger(message.decision_count.low) && $util.isInteger(message.decision_count.high)))
                        return "decision_count: integer|Long expected";
                }
                if (message.ending_cash != null && $Object.hasOwnProperty.call(message, "ending_cash")) {
                    properties._ending_cash = 1;
                    if (!$util.isString(message.ending_cash))
                        return "ending_cash: string expected";
                }
                if (message.ending_equity != null && $Object.hasOwnProperty.call(message, "ending_equity")) {
                    properties._ending_equity = 1;
                    if (!$util.isString(message.ending_equity))
                        return "ending_equity: string expected";
                }
                if (message.ending_position_lots != null && $Object.hasOwnProperty.call(message, "ending_position_lots")) {
                    properties._ending_position_lots = 1;
                    if (!$util.isInteger(message.ending_position_lots) && !(message.ending_position_lots && $util.isInteger(message.ending_position_lots.low) && $util.isInteger(message.ending_position_lots.high)))
                        return "ending_position_lots: integer|Long expected";
                }
                if (message.fill_count != null && $Object.hasOwnProperty.call(message, "fill_count")) {
                    properties._fill_count = 1;
                    if (!$util.isInteger(message.fill_count) && !(message.fill_count && $util.isInteger(message.fill_count.low) && $util.isInteger(message.fill_count.high)))
                        return "fill_count: integer|Long expected";
                }
                if (message.initial_cash != null && $Object.hasOwnProperty.call(message, "initial_cash")) {
                    properties._initial_cash = 1;
                    if (!$util.isString(message.initial_cash))
                        return "initial_cash: string expected";
                }
                if (message.max_drawdown != null && $Object.hasOwnProperty.call(message, "max_drawdown")) {
                    properties._max_drawdown = 1;
                    if (!$util.isString(message.max_drawdown))
                        return "max_drawdown: string expected";
                }
                if (message.max_drawdown_fraction != null && $Object.hasOwnProperty.call(message, "max_drawdown_fraction")) {
                    properties._max_drawdown_fraction = 1;
                    if (!$util.isString(message.max_drawdown_fraction))
                        return "max_drawdown_fraction: string expected";
                }
                if (message.realized_pnl != null && $Object.hasOwnProperty.call(message, "realized_pnl")) {
                    properties._realized_pnl = 1;
                    if (!$util.isString(message.realized_pnl))
                        return "realized_pnl: string expected";
                }
                if (message.run_id != null && $Object.hasOwnProperty.call(message, "run_id")) {
                    properties._run_id = 1;
                    if (!$util.isString(message.run_id))
                        return "run_id: string expected";
                }
                if (message.strategy != null && $Object.hasOwnProperty.call(message, "strategy")) {
                    properties._strategy = 1;
                    if (!$util.isString(message.strategy))
                        return "strategy: string expected";
                }
                if (message.total_fees != null && $Object.hasOwnProperty.call(message, "total_fees")) {
                    properties._total_fees = 1;
                    if (!$util.isString(message.total_fees))
                        return "total_fees: string expected";
                }
                if (message.total_return != null && $Object.hasOwnProperty.call(message, "total_return")) {
                    properties._total_return = 1;
                    if (!$util.isString(message.total_return))
                        return "total_return: string expected";
                }
                if (message.unrealized_pnl != null && $Object.hasOwnProperty.call(message, "unrealized_pnl")) {
                    properties._unrealized_pnl = 1;
                    if (!$util.isString(message.unrealized_pnl))
                        return "unrealized_pnl: string expected";
                }
                return null;
            };

            /**
             * Creates a Comparison message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.Comparison
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.Comparison} Comparison
             */
            Comparison.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.Comparison)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.Comparison: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.Comparison();
                if (object.bar_count != null)
                    if ($util.Long)
                        message.bar_count = $util.Long.fromValue(object.bar_count, false);
                    else if (typeof object.bar_count === "string")
                        message.bar_count = $parseInt(object.bar_count, 10);
                    else if (typeof object.bar_count === "number")
                        message.bar_count = object.bar_count;
                    else if (typeof object.bar_count === "object")
                        message.bar_count = new $util.LongBits(object.bar_count.low >>> 0, object.bar_count.high >>> 0).toNumber();
                if (object.decision_count != null)
                    if ($util.Long)
                        message.decision_count = $util.Long.fromValue(object.decision_count, false);
                    else if (typeof object.decision_count === "string")
                        message.decision_count = $parseInt(object.decision_count, 10);
                    else if (typeof object.decision_count === "number")
                        message.decision_count = object.decision_count;
                    else if (typeof object.decision_count === "object")
                        message.decision_count = new $util.LongBits(object.decision_count.low >>> 0, object.decision_count.high >>> 0).toNumber();
                if (object.ending_cash != null)
                    message.ending_cash = $String(object.ending_cash);
                if (object.ending_equity != null)
                    message.ending_equity = $String(object.ending_equity);
                if (object.ending_position_lots != null)
                    if ($util.Long)
                        message.ending_position_lots = $util.Long.fromValue(object.ending_position_lots, false);
                    else if (typeof object.ending_position_lots === "string")
                        message.ending_position_lots = $parseInt(object.ending_position_lots, 10);
                    else if (typeof object.ending_position_lots === "number")
                        message.ending_position_lots = object.ending_position_lots;
                    else if (typeof object.ending_position_lots === "object")
                        message.ending_position_lots = new $util.LongBits(object.ending_position_lots.low >>> 0, object.ending_position_lots.high >>> 0).toNumber();
                if (object.fill_count != null)
                    if ($util.Long)
                        message.fill_count = $util.Long.fromValue(object.fill_count, false);
                    else if (typeof object.fill_count === "string")
                        message.fill_count = $parseInt(object.fill_count, 10);
                    else if (typeof object.fill_count === "number")
                        message.fill_count = object.fill_count;
                    else if (typeof object.fill_count === "object")
                        message.fill_count = new $util.LongBits(object.fill_count.low >>> 0, object.fill_count.high >>> 0).toNumber();
                if (object.initial_cash != null)
                    message.initial_cash = $String(object.initial_cash);
                if (object.max_drawdown != null)
                    message.max_drawdown = $String(object.max_drawdown);
                if (object.max_drawdown_fraction != null)
                    message.max_drawdown_fraction = $String(object.max_drawdown_fraction);
                if (object.realized_pnl != null)
                    message.realized_pnl = $String(object.realized_pnl);
                if (object.run_id != null)
                    message.run_id = $String(object.run_id);
                if (object.strategy != null)
                    message.strategy = $String(object.strategy);
                if (object.total_fees != null)
                    message.total_fees = $String(object.total_fees);
                if (object.total_return != null)
                    message.total_return = $String(object.total_return);
                if (object.unrealized_pnl != null)
                    message.unrealized_pnl = $String(object.unrealized_pnl);
                return message;
            };

            /**
             * Creates a plain object from a Comparison message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.Comparison
             * @static
             * @param {northstar.research.Comparison} message Comparison
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Comparison.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.bar_count != null && $Object.hasOwnProperty.call(message, "bar_count")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.bar_count = typeof message.bar_count === "number" ? $BigInt(message.bar_count) : $util.Long.fromBits(message.bar_count.low >>> 0, message.bar_count.high >>> 0, false).toBigInt();
                    else if (typeof message.bar_count === "number")
                        object.bar_count = options.longs === $String ? $String(message.bar_count) : message.bar_count;
                    else
                        object.bar_count = options.longs === $String ? $util.Long.prototype.toString.call(message.bar_count) : options.longs === $Number ? new $util.LongBits(message.bar_count.low >>> 0, message.bar_count.high >>> 0).toNumber() : message.bar_count;
                    if (options.oneofs)
                        object._bar_count = "bar_count";
                }
                if (message.decision_count != null && $Object.hasOwnProperty.call(message, "decision_count")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.decision_count = typeof message.decision_count === "number" ? $BigInt(message.decision_count) : $util.Long.fromBits(message.decision_count.low >>> 0, message.decision_count.high >>> 0, false).toBigInt();
                    else if (typeof message.decision_count === "number")
                        object.decision_count = options.longs === $String ? $String(message.decision_count) : message.decision_count;
                    else
                        object.decision_count = options.longs === $String ? $util.Long.prototype.toString.call(message.decision_count) : options.longs === $Number ? new $util.LongBits(message.decision_count.low >>> 0, message.decision_count.high >>> 0).toNumber() : message.decision_count;
                    if (options.oneofs)
                        object._decision_count = "decision_count";
                }
                if (message.ending_cash != null && $Object.hasOwnProperty.call(message, "ending_cash")) {
                    object.ending_cash = message.ending_cash;
                    if (options.oneofs)
                        object._ending_cash = "ending_cash";
                }
                if (message.ending_equity != null && $Object.hasOwnProperty.call(message, "ending_equity")) {
                    object.ending_equity = message.ending_equity;
                    if (options.oneofs)
                        object._ending_equity = "ending_equity";
                }
                if (message.ending_position_lots != null && $Object.hasOwnProperty.call(message, "ending_position_lots")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.ending_position_lots = typeof message.ending_position_lots === "number" ? $BigInt(message.ending_position_lots) : $util.Long.fromBits(message.ending_position_lots.low >>> 0, message.ending_position_lots.high >>> 0, false).toBigInt();
                    else if (typeof message.ending_position_lots === "number")
                        object.ending_position_lots = options.longs === $String ? $String(message.ending_position_lots) : message.ending_position_lots;
                    else
                        object.ending_position_lots = options.longs === $String ? $util.Long.prototype.toString.call(message.ending_position_lots) : options.longs === $Number ? new $util.LongBits(message.ending_position_lots.low >>> 0, message.ending_position_lots.high >>> 0).toNumber() : message.ending_position_lots;
                    if (options.oneofs)
                        object._ending_position_lots = "ending_position_lots";
                }
                if (message.fill_count != null && $Object.hasOwnProperty.call(message, "fill_count")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.fill_count = typeof message.fill_count === "number" ? $BigInt(message.fill_count) : $util.Long.fromBits(message.fill_count.low >>> 0, message.fill_count.high >>> 0, false).toBigInt();
                    else if (typeof message.fill_count === "number")
                        object.fill_count = options.longs === $String ? $String(message.fill_count) : message.fill_count;
                    else
                        object.fill_count = options.longs === $String ? $util.Long.prototype.toString.call(message.fill_count) : options.longs === $Number ? new $util.LongBits(message.fill_count.low >>> 0, message.fill_count.high >>> 0).toNumber() : message.fill_count;
                    if (options.oneofs)
                        object._fill_count = "fill_count";
                }
                if (message.initial_cash != null && $Object.hasOwnProperty.call(message, "initial_cash")) {
                    object.initial_cash = message.initial_cash;
                    if (options.oneofs)
                        object._initial_cash = "initial_cash";
                }
                if (message.max_drawdown != null && $Object.hasOwnProperty.call(message, "max_drawdown")) {
                    object.max_drawdown = message.max_drawdown;
                    if (options.oneofs)
                        object._max_drawdown = "max_drawdown";
                }
                if (message.max_drawdown_fraction != null && $Object.hasOwnProperty.call(message, "max_drawdown_fraction")) {
                    object.max_drawdown_fraction = message.max_drawdown_fraction;
                    if (options.oneofs)
                        object._max_drawdown_fraction = "max_drawdown_fraction";
                }
                if (message.realized_pnl != null && $Object.hasOwnProperty.call(message, "realized_pnl")) {
                    object.realized_pnl = message.realized_pnl;
                    if (options.oneofs)
                        object._realized_pnl = "realized_pnl";
                }
                if (message.run_id != null && $Object.hasOwnProperty.call(message, "run_id")) {
                    object.run_id = message.run_id;
                    if (options.oneofs)
                        object._run_id = "run_id";
                }
                if (message.strategy != null && $Object.hasOwnProperty.call(message, "strategy")) {
                    object.strategy = message.strategy;
                    if (options.oneofs)
                        object._strategy = "strategy";
                }
                if (message.total_fees != null && $Object.hasOwnProperty.call(message, "total_fees")) {
                    object.total_fees = message.total_fees;
                    if (options.oneofs)
                        object._total_fees = "total_fees";
                }
                if (message.total_return != null && $Object.hasOwnProperty.call(message, "total_return")) {
                    object.total_return = message.total_return;
                    if (options.oneofs)
                        object._total_return = "total_return";
                }
                if (message.unrealized_pnl != null && $Object.hasOwnProperty.call(message, "unrealized_pnl")) {
                    object.unrealized_pnl = message.unrealized_pnl;
                    if (options.oneofs)
                        object._unrealized_pnl = "unrealized_pnl";
                }
                return object;
            };

            /**
             * Converts this Comparison to JSON.
             * @function toJSON
             * @memberof northstar.research.Comparison
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Comparison.prototype.toJSON = function() {
                return Comparison.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Comparison
             * @function getTypeUrl
             * @memberof northstar.research.Comparison
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Comparison.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.Comparison";
            };

            return Comparison;
        })();

        research.ComparisonRequest = (function() {

            /**
             * Properties of a ComparisonRequest.
             * @typedef {Object} northstar.research.ComparisonRequest.$Properties
             * @property {Array.<string>|null} [run_ids] ComparisonRequest run_ids
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ComparisonRequest.
             * @memberof northstar.research
             * @interface IComparisonRequest
             * @augments northstar.research.ComparisonRequest.$Properties
             * @deprecated Use northstar.research.ComparisonRequest.$Properties instead.
             */

            /**
             * Shape of a ComparisonRequest.
             * @typedef {northstar.research.ComparisonRequest.$Properties} northstar.research.ComparisonRequest.$Shape
             */

            /**
             * Constructs a new ComparisonRequest.
             * @memberof northstar.research
             * @classdesc Represents a ComparisonRequest.
             * @constructor
             * @param {northstar.research.ComparisonRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ComparisonRequest = function (properties) {
                this.run_ids = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ComparisonRequest run_ids.
             * @member {Array.<string>} run_ids
             * @memberof northstar.research.ComparisonRequest
             * @instance
             */
            ComparisonRequest.prototype.run_ids = $util.emptyArray;

            /**
             * Creates a new ComparisonRequest instance using the specified properties.
             * @function create
             * @memberof northstar.research.ComparisonRequest
             * @static
             * @param {northstar.research.ComparisonRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.research.ComparisonRequest} ComparisonRequest instance
             * @type {{
             *   (properties: northstar.research.ComparisonRequest.$Shape): northstar.research.ComparisonRequest & northstar.research.ComparisonRequest.$Shape;
             *   (properties?: northstar.research.ComparisonRequest.$Properties): northstar.research.ComparisonRequest;
             * }}
             */
            ComparisonRequest.create = function(properties) {
                return new ComparisonRequest(properties);
            };

            /**
             * Encodes the specified ComparisonRequest message. Does not implicitly {@link northstar.research.ComparisonRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.ComparisonRequest
             * @static
             * @param {northstar.research.ComparisonRequest.$Properties} message ComparisonRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ComparisonRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.run_ids != null && message.run_ids.length)
                    for (let i = 0; i < message.run_ids.length; ++i)
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.run_ids[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a ComparisonRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.ComparisonRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.ComparisonRequest & northstar.research.ComparisonRequest.$Shape} ComparisonRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ComparisonRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.ComparisonRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.run_ids && message.run_ids.length))
                                message.run_ids = [];
                            message.run_ids.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a ComparisonRequest message.
             * @function verify
             * @memberof northstar.research.ComparisonRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ComparisonRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.run_ids != null && $Object.hasOwnProperty.call(message, "run_ids")) {
                    if (!$Array.isArray(message.run_ids))
                        return "run_ids: array expected";
                    for (let i = 0; i < message.run_ids.length; ++i)
                        if (!$util.isString(message.run_ids[i]))
                            return "run_ids: string[] expected";
                }
                return null;
            };

            /**
             * Creates a ComparisonRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.ComparisonRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.ComparisonRequest} ComparisonRequest
             */
            ComparisonRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.ComparisonRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.ComparisonRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.ComparisonRequest();
                if (object.run_ids) {
                    if (!$Array.isArray(object.run_ids))
                        throw $TypeError(".northstar.research.ComparisonRequest.run_ids: array expected");
                    message.run_ids = $Array(object.run_ids.length);
                    for (let i = 0; i < object.run_ids.length; ++i)
                        message.run_ids[i] = $String(object.run_ids[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a ComparisonRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.ComparisonRequest
             * @static
             * @param {northstar.research.ComparisonRequest} message ComparisonRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ComparisonRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.run_ids = [];
                if (message.run_ids && message.run_ids.length) {
                    object.run_ids = $Array(message.run_ids.length);
                    for (let j = 0; j < message.run_ids.length; ++j)
                        object.run_ids[j] = message.run_ids[j];
                }
                return object;
            };

            /**
             * Converts this ComparisonRequest to JSON.
             * @function toJSON
             * @memberof northstar.research.ComparisonRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ComparisonRequest.prototype.toJSON = function() {
                return ComparisonRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ComparisonRequest
             * @function getTypeUrl
             * @memberof northstar.research.ComparisonRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ComparisonRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.ComparisonRequest";
            };

            return ComparisonRequest;
        })();

        research.ConfigurationRequest = (function() {

            /**
             * Properties of a ConfigurationRequest.
             * @typedef {Object} northstar.research.ConfigurationRequest.$Properties
             * @property {northstar.research.ResearchConfigurationInput.$Properties|null} [config] ConfigurationRequest config
             * @property {string|null} [name] ConfigurationRequest name
             * @property {"config"} [_config] ConfigurationRequest _config
             * @property {"name"} [_name] ConfigurationRequest _name
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ConfigurationRequest.
             * @memberof northstar.research
             * @interface IConfigurationRequest
             * @augments northstar.research.ConfigurationRequest.$Properties
             * @deprecated Use northstar.research.ConfigurationRequest.$Properties instead.
             */

            /**
             * Narrowed shape of a ConfigurationRequest.
             * @typedef {{
             *   config?: northstar.research.ResearchConfigurationInput.$Shape|null;
             *   name?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _config?: undefined; config?: null }|{ _config?: "config"; config: northstar.research.ResearchConfigurationInput.$Shape })
             * ) & (
             *   ({ _name?: undefined; name?: null }|{ _name?: "name"; name: string })
             * )} northstar.research.ConfigurationRequest.$Shape
             */

            /**
             * Constructs a new ConfigurationRequest.
             * @memberof northstar.research
             * @classdesc Represents a ConfigurationRequest.
             * @constructor
             * @param {northstar.research.ConfigurationRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ConfigurationRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ConfigurationRequest config.
             * @member {northstar.research.ResearchConfigurationInput.$Properties|null|undefined} config
             * @memberof northstar.research.ConfigurationRequest
             * @instance
             */
            ConfigurationRequest.prototype.config = null;

            /**
             * ConfigurationRequest name.
             * @member {string|null|undefined} name
             * @memberof northstar.research.ConfigurationRequest
             * @instance
             */
            ConfigurationRequest.prototype.name = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * ConfigurationRequest _config.
             * @member {"config"|undefined} _config
             * @memberof northstar.research.ConfigurationRequest
             * @instance
             */
            $Object.defineProperty(ConfigurationRequest.prototype, "_config", {
                get: $util.oneOfGetter($oneOfFields = ["config"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ConfigurationRequest _name.
             * @member {"name"|undefined} _name
             * @memberof northstar.research.ConfigurationRequest
             * @instance
             */
            $Object.defineProperty(ConfigurationRequest.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ConfigurationRequest instance using the specified properties.
             * @function create
             * @memberof northstar.research.ConfigurationRequest
             * @static
             * @param {northstar.research.ConfigurationRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.research.ConfigurationRequest} ConfigurationRequest instance
             * @type {{
             *   (properties: northstar.research.ConfigurationRequest.$Shape): northstar.research.ConfigurationRequest & northstar.research.ConfigurationRequest.$Shape;
             *   (properties?: northstar.research.ConfigurationRequest.$Properties): northstar.research.ConfigurationRequest;
             * }}
             */
            ConfigurationRequest.create = function(properties) {
                return new ConfigurationRequest(properties);
            };

            /**
             * Encodes the specified ConfigurationRequest message. Does not implicitly {@link northstar.research.ConfigurationRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.ConfigurationRequest
             * @static
             * @param {northstar.research.ConfigurationRequest.$Properties} message ConfigurationRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ConfigurationRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.config != null && $Object.hasOwnProperty.call(message, "config"))
                    $root.northstar.research.ResearchConfigurationInput.encode(message.config, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a ConfigurationRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.ConfigurationRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.ConfigurationRequest & northstar.research.ConfigurationRequest.$Shape} ConfigurationRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ConfigurationRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.ConfigurationRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.config = $root.northstar.research.ResearchConfigurationInput.decode(reader, reader.uint32(), $undefined, _depth + 1, message.config);
                            message._config = "config";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a ConfigurationRequest message.
             * @function verify
             * @memberof northstar.research.ConfigurationRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ConfigurationRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.config != null && $Object.hasOwnProperty.call(message, "config")) {
                    properties._config = 1;
                    {
                        let error = $root.northstar.research.ResearchConfigurationInput.verify(message.config, _depth + 1);
                        if (error)
                            return "config." + error;
                    }
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                return null;
            };

            /**
             * Creates a ConfigurationRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.ConfigurationRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.ConfigurationRequest} ConfigurationRequest
             */
            ConfigurationRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.ConfigurationRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.ConfigurationRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.ConfigurationRequest();
                if (object.config != null) {
                    if (!$util.isObject(object.config))
                        throw $TypeError(".northstar.research.ConfigurationRequest.config: object expected");
                    message.config = $root.northstar.research.ResearchConfigurationInput.fromObject(object.config, _depth + 1);
                }
                if (object.name != null)
                    message.name = $String(object.name);
                return message;
            };

            /**
             * Creates a plain object from a ConfigurationRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.ConfigurationRequest
             * @static
             * @param {northstar.research.ConfigurationRequest} message ConfigurationRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ConfigurationRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.config != null && $Object.hasOwnProperty.call(message, "config")) {
                    object.config = $root.northstar.research.ResearchConfigurationInput.toObject(message.config, options, _depth + 1);
                    if (options.oneofs)
                        object._config = "config";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    object.name = message.name;
                    if (options.oneofs)
                        object._name = "name";
                }
                return object;
            };

            /**
             * Converts this ConfigurationRequest to JSON.
             * @function toJSON
             * @memberof northstar.research.ConfigurationRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ConfigurationRequest.prototype.toJSON = function() {
                return ConfigurationRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ConfigurationRequest
             * @function getTypeUrl
             * @memberof northstar.research.ConfigurationRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ConfigurationRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.ConfigurationRequest";
            };

            return ConfigurationRequest;
        })();

        research.DatasetDetails = (function() {

            /**
             * Properties of a DatasetDetails.
             * @typedef {Object} northstar.research.DatasetDetails.$Properties
             * @property {string|null} [availability_basis] DatasetDetails availability_basis
             * @property {string|null} [availability_note] DatasetDetails availability_note
             * @property {number|Long|null} [bar_count] DatasetDetails bar_count
             * @property {string|null} [content_hash] DatasetDetails content_hash
             * @property {string|null} [exchange] DatasetDetails exchange
             * @property {northstar.research.ImportSpecification.$Properties|null} [import_spec] DatasetDetails import_spec
             * @property {Array.<string>|null} [limitations] DatasetDetails limitations
             * @property {google.protobuf.Struct.$Properties|null} [processing_provenance] DatasetDetails processing_provenance
             * @property {string|null} [product] DatasetDetails product
             * @property {string|null} [published_at] DatasetDetails published_at
             * @property {google.protobuf.Struct.$Properties|null} [quality] DatasetDetails quality
             * @property {google.protobuf.Struct.$Properties|null} [semantics] DatasetDetails semantics
             * @property {string|null} [session_close] DatasetDetails session_close
             * @property {string|null} [session_open] DatasetDetails session_open
             * @property {string|null} [snapshot_id] DatasetDetails snapshot_id
             * @property {string|null} [source_reference] DatasetDetails source_reference
             * @property {Array.<google.protobuf.Struct.$Properties>|null} [sources] DatasetDetails sources
             * @property {string|null} [symbol] DatasetDetails symbol
             * @property {string|null} [trading_day] DatasetDetails trading_day
             * @property {Array.<string>|null} [null_fields] DatasetDetails null_fields
             * @property {"availability_basis"} [_availability_basis] DatasetDetails _availability_basis
             * @property {"availability_note"} [_availability_note] DatasetDetails _availability_note
             * @property {"bar_count"} [_bar_count] DatasetDetails _bar_count
             * @property {"content_hash"} [_content_hash] DatasetDetails _content_hash
             * @property {"exchange"} [_exchange] DatasetDetails _exchange
             * @property {"import_spec"} [_import_spec] DatasetDetails _import_spec
             * @property {"processing_provenance"} [_processing_provenance] DatasetDetails _processing_provenance
             * @property {"product"} [_product] DatasetDetails _product
             * @property {"published_at"} [_published_at] DatasetDetails _published_at
             * @property {"quality"} [_quality] DatasetDetails _quality
             * @property {"semantics"} [_semantics] DatasetDetails _semantics
             * @property {"session_close"} [_session_close] DatasetDetails _session_close
             * @property {"session_open"} [_session_open] DatasetDetails _session_open
             * @property {"snapshot_id"} [_snapshot_id] DatasetDetails _snapshot_id
             * @property {"source_reference"} [_source_reference] DatasetDetails _source_reference
             * @property {"symbol"} [_symbol] DatasetDetails _symbol
             * @property {"trading_day"} [_trading_day] DatasetDetails _trading_day
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a DatasetDetails.
             * @memberof northstar.research
             * @interface IDatasetDetails
             * @augments northstar.research.DatasetDetails.$Properties
             * @deprecated Use northstar.research.DatasetDetails.$Properties instead.
             */

            /**
             * Narrowed shape of a DatasetDetails.
             * @typedef {{
             *   availability_basis?: string|null;
             *   availability_note?: string|null;
             *   bar_count?: number|Long|null;
             *   content_hash?: string|null;
             *   exchange?: string|null;
             *   import_spec?: northstar.research.ImportSpecification.$Shape|null;
             *   limitations?: Array.<string>|null;
             *   processing_provenance?: google.protobuf.Struct.$Shape|null;
             *   product?: string|null;
             *   published_at?: string|null;
             *   quality?: google.protobuf.Struct.$Shape|null;
             *   semantics?: google.protobuf.Struct.$Shape|null;
             *   session_close?: string|null;
             *   session_open?: string|null;
             *   snapshot_id?: string|null;
             *   source_reference?: string|null;
             *   sources?: Array.<google.protobuf.Struct.$Shape>|null;
             *   symbol?: string|null;
             *   trading_day?: string|null;
             *   null_fields?: Array.<string>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _availability_basis?: undefined; availability_basis?: null }|{ _availability_basis?: "availability_basis"; availability_basis: string })
             * ) & (
             *   ({ _availability_note?: undefined; availability_note?: null }|{ _availability_note?: "availability_note"; availability_note: string })
             * ) & (
             *   ({ _bar_count?: undefined; bar_count?: null }|{ _bar_count?: "bar_count"; bar_count: number|Long })
             * ) & (
             *   ({ _content_hash?: undefined; content_hash?: null }|{ _content_hash?: "content_hash"; content_hash: string })
             * ) & (
             *   ({ _exchange?: undefined; exchange?: null }|{ _exchange?: "exchange"; exchange: string })
             * ) & (
             *   ({ _import_spec?: undefined; import_spec?: null }|{ _import_spec?: "import_spec"; import_spec: northstar.research.ImportSpecification.$Shape })
             * ) & (
             *   ({ _processing_provenance?: undefined; processing_provenance?: null }|{ _processing_provenance?: "processing_provenance"; processing_provenance: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _product?: undefined; product?: null }|{ _product?: "product"; product: string })
             * ) & (
             *   ({ _published_at?: undefined; published_at?: null }|{ _published_at?: "published_at"; published_at: string })
             * ) & (
             *   ({ _quality?: undefined; quality?: null }|{ _quality?: "quality"; quality: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _semantics?: undefined; semantics?: null }|{ _semantics?: "semantics"; semantics: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _session_close?: undefined; session_close?: null }|{ _session_close?: "session_close"; session_close: string })
             * ) & (
             *   ({ _session_open?: undefined; session_open?: null }|{ _session_open?: "session_open"; session_open: string })
             * ) & (
             *   ({ _snapshot_id?: undefined; snapshot_id?: null }|{ _snapshot_id?: "snapshot_id"; snapshot_id: string })
             * ) & (
             *   ({ _source_reference?: undefined; source_reference?: null }|{ _source_reference?: "source_reference"; source_reference: string })
             * ) & (
             *   ({ _symbol?: undefined; symbol?: null }|{ _symbol?: "symbol"; symbol: string })
             * ) & (
             *   ({ _trading_day?: undefined; trading_day?: null }|{ _trading_day?: "trading_day"; trading_day: string })
             * )} northstar.research.DatasetDetails.$Shape
             */

            /**
             * Constructs a new DatasetDetails.
             * @memberof northstar.research
             * @classdesc Represents a DatasetDetails.
             * @constructor
             * @param {northstar.research.DatasetDetails.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const DatasetDetails = function (properties) {
                this.limitations = [];
                this.sources = [];
                this.null_fields = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * DatasetDetails availability_basis.
             * @member {string|null|undefined} availability_basis
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            DatasetDetails.prototype.availability_basis = null;

            /**
             * DatasetDetails availability_note.
             * @member {string|null|undefined} availability_note
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            DatasetDetails.prototype.availability_note = null;

            /**
             * DatasetDetails bar_count.
             * @member {number|Long|null|undefined} bar_count
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            DatasetDetails.prototype.bar_count = null;

            /**
             * DatasetDetails content_hash.
             * @member {string|null|undefined} content_hash
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            DatasetDetails.prototype.content_hash = null;

            /**
             * DatasetDetails exchange.
             * @member {string|null|undefined} exchange
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            DatasetDetails.prototype.exchange = null;

            /**
             * DatasetDetails import_spec.
             * @member {northstar.research.ImportSpecification.$Properties|null|undefined} import_spec
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            DatasetDetails.prototype.import_spec = null;

            /**
             * DatasetDetails limitations.
             * @member {Array.<string>} limitations
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            DatasetDetails.prototype.limitations = $util.emptyArray;

            /**
             * DatasetDetails processing_provenance.
             * @member {google.protobuf.Struct.$Properties|null|undefined} processing_provenance
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            DatasetDetails.prototype.processing_provenance = null;

            /**
             * DatasetDetails product.
             * @member {string|null|undefined} product
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            DatasetDetails.prototype.product = null;

            /**
             * DatasetDetails published_at.
             * @member {string|null|undefined} published_at
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            DatasetDetails.prototype.published_at = null;

            /**
             * DatasetDetails quality.
             * @member {google.protobuf.Struct.$Properties|null|undefined} quality
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            DatasetDetails.prototype.quality = null;

            /**
             * DatasetDetails semantics.
             * @member {google.protobuf.Struct.$Properties|null|undefined} semantics
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            DatasetDetails.prototype.semantics = null;

            /**
             * DatasetDetails session_close.
             * @member {string|null|undefined} session_close
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            DatasetDetails.prototype.session_close = null;

            /**
             * DatasetDetails session_open.
             * @member {string|null|undefined} session_open
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            DatasetDetails.prototype.session_open = null;

            /**
             * DatasetDetails snapshot_id.
             * @member {string|null|undefined} snapshot_id
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            DatasetDetails.prototype.snapshot_id = null;

            /**
             * DatasetDetails source_reference.
             * @member {string|null|undefined} source_reference
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            DatasetDetails.prototype.source_reference = null;

            /**
             * DatasetDetails sources.
             * @member {Array.<google.protobuf.Struct.$Properties>} sources
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            DatasetDetails.prototype.sources = $util.emptyArray;

            /**
             * DatasetDetails symbol.
             * @member {string|null|undefined} symbol
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            DatasetDetails.prototype.symbol = null;

            /**
             * DatasetDetails trading_day.
             * @member {string|null|undefined} trading_day
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            DatasetDetails.prototype.trading_day = null;

            /**
             * DatasetDetails null_fields.
             * @member {Array.<string>} null_fields
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            DatasetDetails.prototype.null_fields = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * DatasetDetails _availability_basis.
             * @member {"availability_basis"|undefined} _availability_basis
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            $Object.defineProperty(DatasetDetails.prototype, "_availability_basis", {
                get: $util.oneOfGetter($oneOfFields = ["availability_basis"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetDetails _availability_note.
             * @member {"availability_note"|undefined} _availability_note
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            $Object.defineProperty(DatasetDetails.prototype, "_availability_note", {
                get: $util.oneOfGetter($oneOfFields = ["availability_note"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetDetails _bar_count.
             * @member {"bar_count"|undefined} _bar_count
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            $Object.defineProperty(DatasetDetails.prototype, "_bar_count", {
                get: $util.oneOfGetter($oneOfFields = ["bar_count"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetDetails _content_hash.
             * @member {"content_hash"|undefined} _content_hash
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            $Object.defineProperty(DatasetDetails.prototype, "_content_hash", {
                get: $util.oneOfGetter($oneOfFields = ["content_hash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetDetails _exchange.
             * @member {"exchange"|undefined} _exchange
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            $Object.defineProperty(DatasetDetails.prototype, "_exchange", {
                get: $util.oneOfGetter($oneOfFields = ["exchange"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetDetails _import_spec.
             * @member {"import_spec"|undefined} _import_spec
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            $Object.defineProperty(DatasetDetails.prototype, "_import_spec", {
                get: $util.oneOfGetter($oneOfFields = ["import_spec"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetDetails _processing_provenance.
             * @member {"processing_provenance"|undefined} _processing_provenance
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            $Object.defineProperty(DatasetDetails.prototype, "_processing_provenance", {
                get: $util.oneOfGetter($oneOfFields = ["processing_provenance"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetDetails _product.
             * @member {"product"|undefined} _product
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            $Object.defineProperty(DatasetDetails.prototype, "_product", {
                get: $util.oneOfGetter($oneOfFields = ["product"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetDetails _published_at.
             * @member {"published_at"|undefined} _published_at
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            $Object.defineProperty(DatasetDetails.prototype, "_published_at", {
                get: $util.oneOfGetter($oneOfFields = ["published_at"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetDetails _quality.
             * @member {"quality"|undefined} _quality
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            $Object.defineProperty(DatasetDetails.prototype, "_quality", {
                get: $util.oneOfGetter($oneOfFields = ["quality"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetDetails _semantics.
             * @member {"semantics"|undefined} _semantics
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            $Object.defineProperty(DatasetDetails.prototype, "_semantics", {
                get: $util.oneOfGetter($oneOfFields = ["semantics"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetDetails _session_close.
             * @member {"session_close"|undefined} _session_close
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            $Object.defineProperty(DatasetDetails.prototype, "_session_close", {
                get: $util.oneOfGetter($oneOfFields = ["session_close"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetDetails _session_open.
             * @member {"session_open"|undefined} _session_open
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            $Object.defineProperty(DatasetDetails.prototype, "_session_open", {
                get: $util.oneOfGetter($oneOfFields = ["session_open"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetDetails _snapshot_id.
             * @member {"snapshot_id"|undefined} _snapshot_id
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            $Object.defineProperty(DatasetDetails.prototype, "_snapshot_id", {
                get: $util.oneOfGetter($oneOfFields = ["snapshot_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetDetails _source_reference.
             * @member {"source_reference"|undefined} _source_reference
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            $Object.defineProperty(DatasetDetails.prototype, "_source_reference", {
                get: $util.oneOfGetter($oneOfFields = ["source_reference"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetDetails _symbol.
             * @member {"symbol"|undefined} _symbol
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            $Object.defineProperty(DatasetDetails.prototype, "_symbol", {
                get: $util.oneOfGetter($oneOfFields = ["symbol"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetDetails _trading_day.
             * @member {"trading_day"|undefined} _trading_day
             * @memberof northstar.research.DatasetDetails
             * @instance
             */
            $Object.defineProperty(DatasetDetails.prototype, "_trading_day", {
                get: $util.oneOfGetter($oneOfFields = ["trading_day"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new DatasetDetails instance using the specified properties.
             * @function create
             * @memberof northstar.research.DatasetDetails
             * @static
             * @param {northstar.research.DatasetDetails.$Properties=} [properties] Properties to set
             * @returns {northstar.research.DatasetDetails} DatasetDetails instance
             * @type {{
             *   (properties: northstar.research.DatasetDetails.$Shape): northstar.research.DatasetDetails & northstar.research.DatasetDetails.$Shape;
             *   (properties?: northstar.research.DatasetDetails.$Properties): northstar.research.DatasetDetails;
             * }}
             */
            DatasetDetails.create = function(properties) {
                return new DatasetDetails(properties);
            };

            /**
             * Encodes the specified DatasetDetails message. Does not implicitly {@link northstar.research.DatasetDetails.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.DatasetDetails
             * @static
             * @param {northstar.research.DatasetDetails.$Properties} message DatasetDetails message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DatasetDetails.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.availability_basis != null && $Object.hasOwnProperty.call(message, "availability_basis"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.availability_basis);
                if (message.availability_note != null && $Object.hasOwnProperty.call(message, "availability_note"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.availability_note);
                if (message.bar_count != null && $Object.hasOwnProperty.call(message, "bar_count"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.bar_count);
                if (message.content_hash != null && $Object.hasOwnProperty.call(message, "content_hash"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.content_hash);
                if (message.exchange != null && $Object.hasOwnProperty.call(message, "exchange"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.exchange);
                if (message.import_spec != null && $Object.hasOwnProperty.call(message, "import_spec"))
                    $root.northstar.research.ImportSpecification.encode(message.import_spec, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
                if (message.limitations != null && message.limitations.length)
                    for (let i = 0; i < message.limitations.length; ++i)
                        writer.uint32(/* id 7, wireType 2 =*/58).string(message.limitations[i]);
                if (message.processing_provenance != null && $Object.hasOwnProperty.call(message, "processing_provenance"))
                    $root.google.protobuf.Struct.encode(message.processing_provenance, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
                if (message.product != null && $Object.hasOwnProperty.call(message, "product"))
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.product);
                if (message.published_at != null && $Object.hasOwnProperty.call(message, "published_at"))
                    writer.uint32(/* id 10, wireType 2 =*/82).string(message.published_at);
                if (message.quality != null && $Object.hasOwnProperty.call(message, "quality"))
                    $root.google.protobuf.Struct.encode(message.quality, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
                if (message.semantics != null && $Object.hasOwnProperty.call(message, "semantics"))
                    $root.google.protobuf.Struct.encode(message.semantics, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
                if (message.session_close != null && $Object.hasOwnProperty.call(message, "session_close"))
                    writer.uint32(/* id 13, wireType 2 =*/106).string(message.session_close);
                if (message.session_open != null && $Object.hasOwnProperty.call(message, "session_open"))
                    writer.uint32(/* id 14, wireType 2 =*/114).string(message.session_open);
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id"))
                    writer.uint32(/* id 15, wireType 2 =*/122).string(message.snapshot_id);
                if (message.source_reference != null && $Object.hasOwnProperty.call(message, "source_reference"))
                    writer.uint32(/* id 16, wireType 2 =*/130).string(message.source_reference);
                if (message.sources != null && message.sources.length)
                    for (let i = 0; i < message.sources.length; ++i)
                        $root.google.protobuf.Struct.encode(message.sources[i], writer.uint32(/* id 17, wireType 2 =*/138).fork(), _depth + 1).ldelim();
                if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol"))
                    writer.uint32(/* id 18, wireType 2 =*/146).string(message.symbol);
                if (message.trading_day != null && $Object.hasOwnProperty.call(message, "trading_day"))
                    writer.uint32(/* id 19, wireType 2 =*/154).string(message.trading_day);
                if (message.null_fields != null && message.null_fields.length)
                    for (let i = 0; i < message.null_fields.length; ++i)
                        writer.uint32(/* id 2046, wireType 2 =*/16370).string(message.null_fields[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a DatasetDetails message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.DatasetDetails
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.DatasetDetails & northstar.research.DatasetDetails.$Shape} DatasetDetails
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DatasetDetails.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.DatasetDetails();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.availability_basis = reader.stringVerify();
                            message._availability_basis = "availability_basis";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.availability_note = reader.stringVerify();
                            message._availability_note = "availability_note";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            message.bar_count = reader.int64();
                            message._bar_count = "bar_count";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.content_hash = reader.stringVerify();
                            message._content_hash = "content_hash";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.exchange = reader.stringVerify();
                            message._exchange = "exchange";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.import_spec = $root.northstar.research.ImportSpecification.decode(reader, reader.uint32(), $undefined, _depth + 1, message.import_spec);
                            message._import_spec = "import_spec";
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            if (!(message.limitations && message.limitations.length))
                                message.limitations = [];
                            message.limitations.push(reader.stringVerify());
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            message.processing_provenance = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.processing_provenance);
                            message._processing_provenance = "processing_provenance";
                            continue;
                        }
                    case 9: {
                            if (wireType !== 2)
                                break;
                            message.product = reader.stringVerify();
                            message._product = "product";
                            continue;
                        }
                    case 10: {
                            if (wireType !== 2)
                                break;
                            message.published_at = reader.stringVerify();
                            message._published_at = "published_at";
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            message.quality = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.quality);
                            message._quality = "quality";
                            continue;
                        }
                    case 12: {
                            if (wireType !== 2)
                                break;
                            message.semantics = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.semantics);
                            message._semantics = "semantics";
                            continue;
                        }
                    case 13: {
                            if (wireType !== 2)
                                break;
                            message.session_close = reader.stringVerify();
                            message._session_close = "session_close";
                            continue;
                        }
                    case 14: {
                            if (wireType !== 2)
                                break;
                            message.session_open = reader.stringVerify();
                            message._session_open = "session_open";
                            continue;
                        }
                    case 15: {
                            if (wireType !== 2)
                                break;
                            message.snapshot_id = reader.stringVerify();
                            message._snapshot_id = "snapshot_id";
                            continue;
                        }
                    case 16: {
                            if (wireType !== 2)
                                break;
                            message.source_reference = reader.stringVerify();
                            message._source_reference = "source_reference";
                            continue;
                        }
                    case 17: {
                            if (wireType !== 2)
                                break;
                            if (!(message.sources && message.sources.length))
                                message.sources = [];
                            message.sources.push($root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 18: {
                            if (wireType !== 2)
                                break;
                            message.symbol = reader.stringVerify();
                            message._symbol = "symbol";
                            continue;
                        }
                    case 19: {
                            if (wireType !== 2)
                                break;
                            message.trading_day = reader.stringVerify();
                            message._trading_day = "trading_day";
                            continue;
                        }
                    case 2046: {
                            if (wireType !== 2)
                                break;
                            if (!(message.null_fields && message.null_fields.length))
                                message.null_fields = [];
                            message.null_fields.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a DatasetDetails message.
             * @function verify
             * @memberof northstar.research.DatasetDetails
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DatasetDetails.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.availability_basis != null && $Object.hasOwnProperty.call(message, "availability_basis")) {
                    properties._availability_basis = 1;
                    if (!$util.isString(message.availability_basis))
                        return "availability_basis: string expected";
                }
                if (message.availability_note != null && $Object.hasOwnProperty.call(message, "availability_note")) {
                    properties._availability_note = 1;
                    if (!$util.isString(message.availability_note))
                        return "availability_note: string expected";
                }
                if (message.bar_count != null && $Object.hasOwnProperty.call(message, "bar_count")) {
                    properties._bar_count = 1;
                    if (!$util.isInteger(message.bar_count) && !(message.bar_count && $util.isInteger(message.bar_count.low) && $util.isInteger(message.bar_count.high)))
                        return "bar_count: integer|Long expected";
                }
                if (message.content_hash != null && $Object.hasOwnProperty.call(message, "content_hash")) {
                    properties._content_hash = 1;
                    if (!$util.isString(message.content_hash))
                        return "content_hash: string expected";
                }
                if (message.exchange != null && $Object.hasOwnProperty.call(message, "exchange")) {
                    properties._exchange = 1;
                    if (!$util.isString(message.exchange))
                        return "exchange: string expected";
                }
                if (message.import_spec != null && $Object.hasOwnProperty.call(message, "import_spec")) {
                    properties._import_spec = 1;
                    {
                        let error = $root.northstar.research.ImportSpecification.verify(message.import_spec, _depth + 1);
                        if (error)
                            return "import_spec." + error;
                    }
                }
                if (message.limitations != null && $Object.hasOwnProperty.call(message, "limitations")) {
                    if (!$Array.isArray(message.limitations))
                        return "limitations: array expected";
                    for (let i = 0; i < message.limitations.length; ++i)
                        if (!$util.isString(message.limitations[i]))
                            return "limitations: string[] expected";
                }
                if (message.processing_provenance != null && $Object.hasOwnProperty.call(message, "processing_provenance")) {
                    properties._processing_provenance = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.processing_provenance, _depth + 1);
                        if (error)
                            return "processing_provenance." + error;
                    }
                }
                if (message.product != null && $Object.hasOwnProperty.call(message, "product")) {
                    properties._product = 1;
                    if (!$util.isString(message.product))
                        return "product: string expected";
                }
                if (message.published_at != null && $Object.hasOwnProperty.call(message, "published_at")) {
                    properties._published_at = 1;
                    if (!$util.isString(message.published_at))
                        return "published_at: string expected";
                }
                if (message.quality != null && $Object.hasOwnProperty.call(message, "quality")) {
                    properties._quality = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.quality, _depth + 1);
                        if (error)
                            return "quality." + error;
                    }
                }
                if (message.semantics != null && $Object.hasOwnProperty.call(message, "semantics")) {
                    properties._semantics = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.semantics, _depth + 1);
                        if (error)
                            return "semantics." + error;
                    }
                }
                if (message.session_close != null && $Object.hasOwnProperty.call(message, "session_close")) {
                    properties._session_close = 1;
                    if (!$util.isString(message.session_close))
                        return "session_close: string expected";
                }
                if (message.session_open != null && $Object.hasOwnProperty.call(message, "session_open")) {
                    properties._session_open = 1;
                    if (!$util.isString(message.session_open))
                        return "session_open: string expected";
                }
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id")) {
                    properties._snapshot_id = 1;
                    if (!$util.isString(message.snapshot_id))
                        return "snapshot_id: string expected";
                }
                if (message.source_reference != null && $Object.hasOwnProperty.call(message, "source_reference")) {
                    properties._source_reference = 1;
                    if (!$util.isString(message.source_reference))
                        return "source_reference: string expected";
                }
                if (message.sources != null && $Object.hasOwnProperty.call(message, "sources")) {
                    if (!$Array.isArray(message.sources))
                        return "sources: array expected";
                    for (let i = 0; i < message.sources.length; ++i) {
                        let error = $root.google.protobuf.Struct.verify(message.sources[i], _depth + 1);
                        if (error)
                            return "sources." + error;
                    }
                }
                if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol")) {
                    properties._symbol = 1;
                    if (!$util.isString(message.symbol))
                        return "symbol: string expected";
                }
                if (message.trading_day != null && $Object.hasOwnProperty.call(message, "trading_day")) {
                    properties._trading_day = 1;
                    if (!$util.isString(message.trading_day))
                        return "trading_day: string expected";
                }
                if (message.null_fields != null && $Object.hasOwnProperty.call(message, "null_fields")) {
                    if (!$Array.isArray(message.null_fields))
                        return "null_fields: array expected";
                    for (let i = 0; i < message.null_fields.length; ++i)
                        if (!$util.isString(message.null_fields[i]))
                            return "null_fields: string[] expected";
                }
                return null;
            };

            /**
             * Creates a DatasetDetails message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.DatasetDetails
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.DatasetDetails} DatasetDetails
             */
            DatasetDetails.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.DatasetDetails)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.DatasetDetails: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.DatasetDetails();
                if (object.availability_basis != null)
                    message.availability_basis = $String(object.availability_basis);
                if (object.availability_note != null)
                    message.availability_note = $String(object.availability_note);
                if (object.bar_count != null)
                    if ($util.Long)
                        message.bar_count = $util.Long.fromValue(object.bar_count, false);
                    else if (typeof object.bar_count === "string")
                        message.bar_count = $parseInt(object.bar_count, 10);
                    else if (typeof object.bar_count === "number")
                        message.bar_count = object.bar_count;
                    else if (typeof object.bar_count === "object")
                        message.bar_count = new $util.LongBits(object.bar_count.low >>> 0, object.bar_count.high >>> 0).toNumber();
                if (object.content_hash != null)
                    message.content_hash = $String(object.content_hash);
                if (object.exchange != null)
                    message.exchange = $String(object.exchange);
                if (object.import_spec != null) {
                    if (!$util.isObject(object.import_spec))
                        throw $TypeError(".northstar.research.DatasetDetails.import_spec: object expected");
                    message.import_spec = $root.northstar.research.ImportSpecification.fromObject(object.import_spec, _depth + 1);
                }
                if (object.limitations) {
                    if (!$Array.isArray(object.limitations))
                        throw $TypeError(".northstar.research.DatasetDetails.limitations: array expected");
                    message.limitations = $Array(object.limitations.length);
                    for (let i = 0; i < object.limitations.length; ++i)
                        message.limitations[i] = $String(object.limitations[i]);
                }
                if (object.processing_provenance != null) {
                    if (!$util.isObject(object.processing_provenance))
                        throw $TypeError(".northstar.research.DatasetDetails.processing_provenance: object expected");
                    message.processing_provenance = $root.google.protobuf.Struct.fromObject(object.processing_provenance, _depth + 1);
                }
                if (object.product != null)
                    message.product = $String(object.product);
                if (object.published_at != null)
                    message.published_at = $String(object.published_at);
                if (object.quality != null) {
                    if (!$util.isObject(object.quality))
                        throw $TypeError(".northstar.research.DatasetDetails.quality: object expected");
                    message.quality = $root.google.protobuf.Struct.fromObject(object.quality, _depth + 1);
                }
                if (object.semantics != null) {
                    if (!$util.isObject(object.semantics))
                        throw $TypeError(".northstar.research.DatasetDetails.semantics: object expected");
                    message.semantics = $root.google.protobuf.Struct.fromObject(object.semantics, _depth + 1);
                }
                if (object.session_close != null)
                    message.session_close = $String(object.session_close);
                if (object.session_open != null)
                    message.session_open = $String(object.session_open);
                if (object.snapshot_id != null)
                    message.snapshot_id = $String(object.snapshot_id);
                if (object.source_reference != null)
                    message.source_reference = $String(object.source_reference);
                if (object.sources) {
                    if (!$Array.isArray(object.sources))
                        throw $TypeError(".northstar.research.DatasetDetails.sources: array expected");
                    message.sources = $Array(object.sources.length);
                    for (let i = 0; i < object.sources.length; ++i) {
                        if (!$util.isObject(object.sources[i]))
                            throw $TypeError(".northstar.research.DatasetDetails.sources: object expected");
                        message.sources[i] = $root.google.protobuf.Struct.fromObject(object.sources[i], _depth + 1);
                    }
                }
                if (object.symbol != null)
                    message.symbol = $String(object.symbol);
                if (object.trading_day != null)
                    message.trading_day = $String(object.trading_day);
                if (object.null_fields) {
                    if (!$Array.isArray(object.null_fields))
                        throw $TypeError(".northstar.research.DatasetDetails.null_fields: array expected");
                    message.null_fields = $Array(object.null_fields.length);
                    for (let i = 0; i < object.null_fields.length; ++i)
                        message.null_fields[i] = $String(object.null_fields[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a DatasetDetails message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.DatasetDetails
             * @static
             * @param {northstar.research.DatasetDetails} message DatasetDetails
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DatasetDetails.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.limitations = [];
                    object.sources = [];
                    object.null_fields = [];
                }
                if (message.availability_basis != null && $Object.hasOwnProperty.call(message, "availability_basis")) {
                    object.availability_basis = message.availability_basis;
                    if (options.oneofs)
                        object._availability_basis = "availability_basis";
                }
                if (message.availability_note != null && $Object.hasOwnProperty.call(message, "availability_note")) {
                    object.availability_note = message.availability_note;
                    if (options.oneofs)
                        object._availability_note = "availability_note";
                }
                if (message.bar_count != null && $Object.hasOwnProperty.call(message, "bar_count")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.bar_count = typeof message.bar_count === "number" ? $BigInt(message.bar_count) : $util.Long.fromBits(message.bar_count.low >>> 0, message.bar_count.high >>> 0, false).toBigInt();
                    else if (typeof message.bar_count === "number")
                        object.bar_count = options.longs === $String ? $String(message.bar_count) : message.bar_count;
                    else
                        object.bar_count = options.longs === $String ? $util.Long.prototype.toString.call(message.bar_count) : options.longs === $Number ? new $util.LongBits(message.bar_count.low >>> 0, message.bar_count.high >>> 0).toNumber() : message.bar_count;
                    if (options.oneofs)
                        object._bar_count = "bar_count";
                }
                if (message.content_hash != null && $Object.hasOwnProperty.call(message, "content_hash")) {
                    object.content_hash = message.content_hash;
                    if (options.oneofs)
                        object._content_hash = "content_hash";
                }
                if (message.exchange != null && $Object.hasOwnProperty.call(message, "exchange")) {
                    object.exchange = message.exchange;
                    if (options.oneofs)
                        object._exchange = "exchange";
                }
                if (message.import_spec != null && $Object.hasOwnProperty.call(message, "import_spec")) {
                    object.import_spec = $root.northstar.research.ImportSpecification.toObject(message.import_spec, options, _depth + 1);
                    if (options.oneofs)
                        object._import_spec = "import_spec";
                }
                if (message.limitations && message.limitations.length) {
                    object.limitations = $Array(message.limitations.length);
                    for (let j = 0; j < message.limitations.length; ++j)
                        object.limitations[j] = message.limitations[j];
                }
                if (message.processing_provenance != null && $Object.hasOwnProperty.call(message, "processing_provenance")) {
                    object.processing_provenance = $root.google.protobuf.Struct.toObject(message.processing_provenance, options, _depth + 1);
                    if (options.oneofs)
                        object._processing_provenance = "processing_provenance";
                }
                if (message.product != null && $Object.hasOwnProperty.call(message, "product")) {
                    object.product = message.product;
                    if (options.oneofs)
                        object._product = "product";
                }
                if (message.published_at != null && $Object.hasOwnProperty.call(message, "published_at")) {
                    object.published_at = message.published_at;
                    if (options.oneofs)
                        object._published_at = "published_at";
                }
                if (message.quality != null && $Object.hasOwnProperty.call(message, "quality")) {
                    object.quality = $root.google.protobuf.Struct.toObject(message.quality, options, _depth + 1);
                    if (options.oneofs)
                        object._quality = "quality";
                }
                if (message.semantics != null && $Object.hasOwnProperty.call(message, "semantics")) {
                    object.semantics = $root.google.protobuf.Struct.toObject(message.semantics, options, _depth + 1);
                    if (options.oneofs)
                        object._semantics = "semantics";
                }
                if (message.session_close != null && $Object.hasOwnProperty.call(message, "session_close")) {
                    object.session_close = message.session_close;
                    if (options.oneofs)
                        object._session_close = "session_close";
                }
                if (message.session_open != null && $Object.hasOwnProperty.call(message, "session_open")) {
                    object.session_open = message.session_open;
                    if (options.oneofs)
                        object._session_open = "session_open";
                }
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id")) {
                    object.snapshot_id = message.snapshot_id;
                    if (options.oneofs)
                        object._snapshot_id = "snapshot_id";
                }
                if (message.source_reference != null && $Object.hasOwnProperty.call(message, "source_reference")) {
                    object.source_reference = message.source_reference;
                    if (options.oneofs)
                        object._source_reference = "source_reference";
                }
                if (message.sources && message.sources.length) {
                    object.sources = $Array(message.sources.length);
                    for (let j = 0; j < message.sources.length; ++j)
                        object.sources[j] = $root.google.protobuf.Struct.toObject(message.sources[j], options, _depth + 1);
                }
                if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol")) {
                    object.symbol = message.symbol;
                    if (options.oneofs)
                        object._symbol = "symbol";
                }
                if (message.trading_day != null && $Object.hasOwnProperty.call(message, "trading_day")) {
                    object.trading_day = message.trading_day;
                    if (options.oneofs)
                        object._trading_day = "trading_day";
                }
                if (message.null_fields && message.null_fields.length) {
                    object.null_fields = $Array(message.null_fields.length);
                    for (let j = 0; j < message.null_fields.length; ++j)
                        object.null_fields[j] = message.null_fields[j];
                }
                return object;
            };

            /**
             * Converts this DatasetDetails to JSON.
             * @function toJSON
             * @memberof northstar.research.DatasetDetails
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DatasetDetails.prototype.toJSON = function() {
                return DatasetDetails.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for DatasetDetails
             * @function getTypeUrl
             * @memberof northstar.research.DatasetDetails
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            DatasetDetails.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.DatasetDetails";
            };

            return DatasetDetails;
        })();

        research.DatasetLineage = (function() {

            /**
             * Properties of a DatasetLineage.
             * @typedef {Object} northstar.research.DatasetLineage.$Properties
             * @property {Array.<google.protobuf.Struct.$Properties>|null} [attempts] DatasetLineage attempts
             * @property {string|null} [snapshot_id] DatasetLineage snapshot_id
             * @property {Array.<google.protobuf.Struct.$Properties>|null} [sources] DatasetLineage sources
             * @property {Array.<google.protobuf.Struct.$Properties>|null} [usages] DatasetLineage usages
             * @property {"snapshot_id"} [_snapshot_id] DatasetLineage _snapshot_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a DatasetLineage.
             * @memberof northstar.research
             * @interface IDatasetLineage
             * @augments northstar.research.DatasetLineage.$Properties
             * @deprecated Use northstar.research.DatasetLineage.$Properties instead.
             */

            /**
             * Narrowed shape of a DatasetLineage.
             * @typedef {{
             *   attempts?: Array.<google.protobuf.Struct.$Shape>|null;
             *   snapshot_id?: string|null;
             *   sources?: Array.<google.protobuf.Struct.$Shape>|null;
             *   usages?: Array.<google.protobuf.Struct.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _snapshot_id?: undefined; snapshot_id?: null }|{ _snapshot_id?: "snapshot_id"; snapshot_id: string })
             * )} northstar.research.DatasetLineage.$Shape
             */

            /**
             * Constructs a new DatasetLineage.
             * @memberof northstar.research
             * @classdesc Represents a DatasetLineage.
             * @constructor
             * @param {northstar.research.DatasetLineage.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const DatasetLineage = function (properties) {
                this.attempts = [];
                this.sources = [];
                this.usages = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * DatasetLineage attempts.
             * @member {Array.<google.protobuf.Struct.$Properties>} attempts
             * @memberof northstar.research.DatasetLineage
             * @instance
             */
            DatasetLineage.prototype.attempts = $util.emptyArray;

            /**
             * DatasetLineage snapshot_id.
             * @member {string|null|undefined} snapshot_id
             * @memberof northstar.research.DatasetLineage
             * @instance
             */
            DatasetLineage.prototype.snapshot_id = null;

            /**
             * DatasetLineage sources.
             * @member {Array.<google.protobuf.Struct.$Properties>} sources
             * @memberof northstar.research.DatasetLineage
             * @instance
             */
            DatasetLineage.prototype.sources = $util.emptyArray;

            /**
             * DatasetLineage usages.
             * @member {Array.<google.protobuf.Struct.$Properties>} usages
             * @memberof northstar.research.DatasetLineage
             * @instance
             */
            DatasetLineage.prototype.usages = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * DatasetLineage _snapshot_id.
             * @member {"snapshot_id"|undefined} _snapshot_id
             * @memberof northstar.research.DatasetLineage
             * @instance
             */
            $Object.defineProperty(DatasetLineage.prototype, "_snapshot_id", {
                get: $util.oneOfGetter($oneOfFields = ["snapshot_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new DatasetLineage instance using the specified properties.
             * @function create
             * @memberof northstar.research.DatasetLineage
             * @static
             * @param {northstar.research.DatasetLineage.$Properties=} [properties] Properties to set
             * @returns {northstar.research.DatasetLineage} DatasetLineage instance
             * @type {{
             *   (properties: northstar.research.DatasetLineage.$Shape): northstar.research.DatasetLineage & northstar.research.DatasetLineage.$Shape;
             *   (properties?: northstar.research.DatasetLineage.$Properties): northstar.research.DatasetLineage;
             * }}
             */
            DatasetLineage.create = function(properties) {
                return new DatasetLineage(properties);
            };

            /**
             * Encodes the specified DatasetLineage message. Does not implicitly {@link northstar.research.DatasetLineage.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.DatasetLineage
             * @static
             * @param {northstar.research.DatasetLineage.$Properties} message DatasetLineage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DatasetLineage.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.attempts != null && message.attempts.length)
                    for (let i = 0; i < message.attempts.length; ++i)
                        $root.google.protobuf.Struct.encode(message.attempts[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.snapshot_id);
                if (message.sources != null && message.sources.length)
                    for (let i = 0; i < message.sources.length; ++i)
                        $root.google.protobuf.Struct.encode(message.sources[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.usages != null && message.usages.length)
                    for (let i = 0; i < message.usages.length; ++i)
                        $root.google.protobuf.Struct.encode(message.usages[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a DatasetLineage message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.DatasetLineage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.DatasetLineage & northstar.research.DatasetLineage.$Shape} DatasetLineage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DatasetLineage.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.DatasetLineage();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.attempts && message.attempts.length))
                                message.attempts = [];
                            message.attempts.push($root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.snapshot_id = reader.stringVerify();
                            message._snapshot_id = "snapshot_id";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if (!(message.sources && message.sources.length))
                                message.sources = [];
                            message.sources.push($root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            if (!(message.usages && message.usages.length))
                                message.usages = [];
                            message.usages.push($root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a DatasetLineage message.
             * @function verify
             * @memberof northstar.research.DatasetLineage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DatasetLineage.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.attempts != null && $Object.hasOwnProperty.call(message, "attempts")) {
                    if (!$Array.isArray(message.attempts))
                        return "attempts: array expected";
                    for (let i = 0; i < message.attempts.length; ++i) {
                        let error = $root.google.protobuf.Struct.verify(message.attempts[i], _depth + 1);
                        if (error)
                            return "attempts." + error;
                    }
                }
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id")) {
                    properties._snapshot_id = 1;
                    if (!$util.isString(message.snapshot_id))
                        return "snapshot_id: string expected";
                }
                if (message.sources != null && $Object.hasOwnProperty.call(message, "sources")) {
                    if (!$Array.isArray(message.sources))
                        return "sources: array expected";
                    for (let i = 0; i < message.sources.length; ++i) {
                        let error = $root.google.protobuf.Struct.verify(message.sources[i], _depth + 1);
                        if (error)
                            return "sources." + error;
                    }
                }
                if (message.usages != null && $Object.hasOwnProperty.call(message, "usages")) {
                    if (!$Array.isArray(message.usages))
                        return "usages: array expected";
                    for (let i = 0; i < message.usages.length; ++i) {
                        let error = $root.google.protobuf.Struct.verify(message.usages[i], _depth + 1);
                        if (error)
                            return "usages." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a DatasetLineage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.DatasetLineage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.DatasetLineage} DatasetLineage
             */
            DatasetLineage.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.DatasetLineage)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.DatasetLineage: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.DatasetLineage();
                if (object.attempts) {
                    if (!$Array.isArray(object.attempts))
                        throw $TypeError(".northstar.research.DatasetLineage.attempts: array expected");
                    message.attempts = $Array(object.attempts.length);
                    for (let i = 0; i < object.attempts.length; ++i) {
                        if (!$util.isObject(object.attempts[i]))
                            throw $TypeError(".northstar.research.DatasetLineage.attempts: object expected");
                        message.attempts[i] = $root.google.protobuf.Struct.fromObject(object.attempts[i], _depth + 1);
                    }
                }
                if (object.snapshot_id != null)
                    message.snapshot_id = $String(object.snapshot_id);
                if (object.sources) {
                    if (!$Array.isArray(object.sources))
                        throw $TypeError(".northstar.research.DatasetLineage.sources: array expected");
                    message.sources = $Array(object.sources.length);
                    for (let i = 0; i < object.sources.length; ++i) {
                        if (!$util.isObject(object.sources[i]))
                            throw $TypeError(".northstar.research.DatasetLineage.sources: object expected");
                        message.sources[i] = $root.google.protobuf.Struct.fromObject(object.sources[i], _depth + 1);
                    }
                }
                if (object.usages) {
                    if (!$Array.isArray(object.usages))
                        throw $TypeError(".northstar.research.DatasetLineage.usages: array expected");
                    message.usages = $Array(object.usages.length);
                    for (let i = 0; i < object.usages.length; ++i) {
                        if (!$util.isObject(object.usages[i]))
                            throw $TypeError(".northstar.research.DatasetLineage.usages: object expected");
                        message.usages[i] = $root.google.protobuf.Struct.fromObject(object.usages[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a DatasetLineage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.DatasetLineage
             * @static
             * @param {northstar.research.DatasetLineage} message DatasetLineage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DatasetLineage.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.attempts = [];
                    object.sources = [];
                    object.usages = [];
                }
                if (message.attempts && message.attempts.length) {
                    object.attempts = $Array(message.attempts.length);
                    for (let j = 0; j < message.attempts.length; ++j)
                        object.attempts[j] = $root.google.protobuf.Struct.toObject(message.attempts[j], options, _depth + 1);
                }
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id")) {
                    object.snapshot_id = message.snapshot_id;
                    if (options.oneofs)
                        object._snapshot_id = "snapshot_id";
                }
                if (message.sources && message.sources.length) {
                    object.sources = $Array(message.sources.length);
                    for (let j = 0; j < message.sources.length; ++j)
                        object.sources[j] = $root.google.protobuf.Struct.toObject(message.sources[j], options, _depth + 1);
                }
                if (message.usages && message.usages.length) {
                    object.usages = $Array(message.usages.length);
                    for (let j = 0; j < message.usages.length; ++j)
                        object.usages[j] = $root.google.protobuf.Struct.toObject(message.usages[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this DatasetLineage to JSON.
             * @function toJSON
             * @memberof northstar.research.DatasetLineage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DatasetLineage.prototype.toJSON = function() {
                return DatasetLineage.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for DatasetLineage
             * @function getTypeUrl
             * @memberof northstar.research.DatasetLineage
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            DatasetLineage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.DatasetLineage";
            };

            return DatasetLineage;
        })();

        research.DatasetSummary = (function() {

            /**
             * Properties of a DatasetSummary.
             * @typedef {Object} northstar.research.DatasetSummary.$Properties
             * @property {number|Long|null} [bar_count] DatasetSummary bar_count
             * @property {string|null} [content_hash] DatasetSummary content_hash
             * @property {string|null} [exchange] DatasetSummary exchange
             * @property {string|null} [product] DatasetSummary product
             * @property {string|null} [published_at] DatasetSummary published_at
             * @property {string|null} [session_close] DatasetSummary session_close
             * @property {string|null} [session_open] DatasetSummary session_open
             * @property {string|null} [snapshot_id] DatasetSummary snapshot_id
             * @property {string|null} [symbol] DatasetSummary symbol
             * @property {string|null} [trading_day] DatasetSummary trading_day
             * @property {"bar_count"} [_bar_count] DatasetSummary _bar_count
             * @property {"content_hash"} [_content_hash] DatasetSummary _content_hash
             * @property {"exchange"} [_exchange] DatasetSummary _exchange
             * @property {"product"} [_product] DatasetSummary _product
             * @property {"published_at"} [_published_at] DatasetSummary _published_at
             * @property {"session_close"} [_session_close] DatasetSummary _session_close
             * @property {"session_open"} [_session_open] DatasetSummary _session_open
             * @property {"snapshot_id"} [_snapshot_id] DatasetSummary _snapshot_id
             * @property {"symbol"} [_symbol] DatasetSummary _symbol
             * @property {"trading_day"} [_trading_day] DatasetSummary _trading_day
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a DatasetSummary.
             * @memberof northstar.research
             * @interface IDatasetSummary
             * @augments northstar.research.DatasetSummary.$Properties
             * @deprecated Use northstar.research.DatasetSummary.$Properties instead.
             */

            /**
             * Narrowed shape of a DatasetSummary.
             * @typedef {{
             *   bar_count?: number|Long|null;
             *   content_hash?: string|null;
             *   exchange?: string|null;
             *   product?: string|null;
             *   published_at?: string|null;
             *   session_close?: string|null;
             *   session_open?: string|null;
             *   snapshot_id?: string|null;
             *   symbol?: string|null;
             *   trading_day?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _bar_count?: undefined; bar_count?: null }|{ _bar_count?: "bar_count"; bar_count: number|Long })
             * ) & (
             *   ({ _content_hash?: undefined; content_hash?: null }|{ _content_hash?: "content_hash"; content_hash: string })
             * ) & (
             *   ({ _exchange?: undefined; exchange?: null }|{ _exchange?: "exchange"; exchange: string })
             * ) & (
             *   ({ _product?: undefined; product?: null }|{ _product?: "product"; product: string })
             * ) & (
             *   ({ _published_at?: undefined; published_at?: null }|{ _published_at?: "published_at"; published_at: string })
             * ) & (
             *   ({ _session_close?: undefined; session_close?: null }|{ _session_close?: "session_close"; session_close: string })
             * ) & (
             *   ({ _session_open?: undefined; session_open?: null }|{ _session_open?: "session_open"; session_open: string })
             * ) & (
             *   ({ _snapshot_id?: undefined; snapshot_id?: null }|{ _snapshot_id?: "snapshot_id"; snapshot_id: string })
             * ) & (
             *   ({ _symbol?: undefined; symbol?: null }|{ _symbol?: "symbol"; symbol: string })
             * ) & (
             *   ({ _trading_day?: undefined; trading_day?: null }|{ _trading_day?: "trading_day"; trading_day: string })
             * )} northstar.research.DatasetSummary.$Shape
             */

            /**
             * Constructs a new DatasetSummary.
             * @memberof northstar.research
             * @classdesc Represents a DatasetSummary.
             * @constructor
             * @param {northstar.research.DatasetSummary.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const DatasetSummary = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * DatasetSummary bar_count.
             * @member {number|Long|null|undefined} bar_count
             * @memberof northstar.research.DatasetSummary
             * @instance
             */
            DatasetSummary.prototype.bar_count = null;

            /**
             * DatasetSummary content_hash.
             * @member {string|null|undefined} content_hash
             * @memberof northstar.research.DatasetSummary
             * @instance
             */
            DatasetSummary.prototype.content_hash = null;

            /**
             * DatasetSummary exchange.
             * @member {string|null|undefined} exchange
             * @memberof northstar.research.DatasetSummary
             * @instance
             */
            DatasetSummary.prototype.exchange = null;

            /**
             * DatasetSummary product.
             * @member {string|null|undefined} product
             * @memberof northstar.research.DatasetSummary
             * @instance
             */
            DatasetSummary.prototype.product = null;

            /**
             * DatasetSummary published_at.
             * @member {string|null|undefined} published_at
             * @memberof northstar.research.DatasetSummary
             * @instance
             */
            DatasetSummary.prototype.published_at = null;

            /**
             * DatasetSummary session_close.
             * @member {string|null|undefined} session_close
             * @memberof northstar.research.DatasetSummary
             * @instance
             */
            DatasetSummary.prototype.session_close = null;

            /**
             * DatasetSummary session_open.
             * @member {string|null|undefined} session_open
             * @memberof northstar.research.DatasetSummary
             * @instance
             */
            DatasetSummary.prototype.session_open = null;

            /**
             * DatasetSummary snapshot_id.
             * @member {string|null|undefined} snapshot_id
             * @memberof northstar.research.DatasetSummary
             * @instance
             */
            DatasetSummary.prototype.snapshot_id = null;

            /**
             * DatasetSummary symbol.
             * @member {string|null|undefined} symbol
             * @memberof northstar.research.DatasetSummary
             * @instance
             */
            DatasetSummary.prototype.symbol = null;

            /**
             * DatasetSummary trading_day.
             * @member {string|null|undefined} trading_day
             * @memberof northstar.research.DatasetSummary
             * @instance
             */
            DatasetSummary.prototype.trading_day = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * DatasetSummary _bar_count.
             * @member {"bar_count"|undefined} _bar_count
             * @memberof northstar.research.DatasetSummary
             * @instance
             */
            $Object.defineProperty(DatasetSummary.prototype, "_bar_count", {
                get: $util.oneOfGetter($oneOfFields = ["bar_count"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetSummary _content_hash.
             * @member {"content_hash"|undefined} _content_hash
             * @memberof northstar.research.DatasetSummary
             * @instance
             */
            $Object.defineProperty(DatasetSummary.prototype, "_content_hash", {
                get: $util.oneOfGetter($oneOfFields = ["content_hash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetSummary _exchange.
             * @member {"exchange"|undefined} _exchange
             * @memberof northstar.research.DatasetSummary
             * @instance
             */
            $Object.defineProperty(DatasetSummary.prototype, "_exchange", {
                get: $util.oneOfGetter($oneOfFields = ["exchange"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetSummary _product.
             * @member {"product"|undefined} _product
             * @memberof northstar.research.DatasetSummary
             * @instance
             */
            $Object.defineProperty(DatasetSummary.prototype, "_product", {
                get: $util.oneOfGetter($oneOfFields = ["product"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetSummary _published_at.
             * @member {"published_at"|undefined} _published_at
             * @memberof northstar.research.DatasetSummary
             * @instance
             */
            $Object.defineProperty(DatasetSummary.prototype, "_published_at", {
                get: $util.oneOfGetter($oneOfFields = ["published_at"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetSummary _session_close.
             * @member {"session_close"|undefined} _session_close
             * @memberof northstar.research.DatasetSummary
             * @instance
             */
            $Object.defineProperty(DatasetSummary.prototype, "_session_close", {
                get: $util.oneOfGetter($oneOfFields = ["session_close"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetSummary _session_open.
             * @member {"session_open"|undefined} _session_open
             * @memberof northstar.research.DatasetSummary
             * @instance
             */
            $Object.defineProperty(DatasetSummary.prototype, "_session_open", {
                get: $util.oneOfGetter($oneOfFields = ["session_open"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetSummary _snapshot_id.
             * @member {"snapshot_id"|undefined} _snapshot_id
             * @memberof northstar.research.DatasetSummary
             * @instance
             */
            $Object.defineProperty(DatasetSummary.prototype, "_snapshot_id", {
                get: $util.oneOfGetter($oneOfFields = ["snapshot_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetSummary _symbol.
             * @member {"symbol"|undefined} _symbol
             * @memberof northstar.research.DatasetSummary
             * @instance
             */
            $Object.defineProperty(DatasetSummary.prototype, "_symbol", {
                get: $util.oneOfGetter($oneOfFields = ["symbol"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * DatasetSummary _trading_day.
             * @member {"trading_day"|undefined} _trading_day
             * @memberof northstar.research.DatasetSummary
             * @instance
             */
            $Object.defineProperty(DatasetSummary.prototype, "_trading_day", {
                get: $util.oneOfGetter($oneOfFields = ["trading_day"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new DatasetSummary instance using the specified properties.
             * @function create
             * @memberof northstar.research.DatasetSummary
             * @static
             * @param {northstar.research.DatasetSummary.$Properties=} [properties] Properties to set
             * @returns {northstar.research.DatasetSummary} DatasetSummary instance
             * @type {{
             *   (properties: northstar.research.DatasetSummary.$Shape): northstar.research.DatasetSummary & northstar.research.DatasetSummary.$Shape;
             *   (properties?: northstar.research.DatasetSummary.$Properties): northstar.research.DatasetSummary;
             * }}
             */
            DatasetSummary.create = function(properties) {
                return new DatasetSummary(properties);
            };

            /**
             * Encodes the specified DatasetSummary message. Does not implicitly {@link northstar.research.DatasetSummary.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.DatasetSummary
             * @static
             * @param {northstar.research.DatasetSummary.$Properties} message DatasetSummary message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DatasetSummary.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.bar_count != null && $Object.hasOwnProperty.call(message, "bar_count"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int64(message.bar_count);
                if (message.content_hash != null && $Object.hasOwnProperty.call(message, "content_hash"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.content_hash);
                if (message.exchange != null && $Object.hasOwnProperty.call(message, "exchange"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.exchange);
                if (message.product != null && $Object.hasOwnProperty.call(message, "product"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.product);
                if (message.published_at != null && $Object.hasOwnProperty.call(message, "published_at"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.published_at);
                if (message.session_close != null && $Object.hasOwnProperty.call(message, "session_close"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.session_close);
                if (message.session_open != null && $Object.hasOwnProperty.call(message, "session_open"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.session_open);
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.snapshot_id);
                if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol"))
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.symbol);
                if (message.trading_day != null && $Object.hasOwnProperty.call(message, "trading_day"))
                    writer.uint32(/* id 10, wireType 2 =*/82).string(message.trading_day);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a DatasetSummary message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.DatasetSummary
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.DatasetSummary & northstar.research.DatasetSummary.$Shape} DatasetSummary
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DatasetSummary.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.DatasetSummary();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            message.bar_count = reader.int64();
                            message._bar_count = "bar_count";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.content_hash = reader.stringVerify();
                            message._content_hash = "content_hash";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.exchange = reader.stringVerify();
                            message._exchange = "exchange";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.product = reader.stringVerify();
                            message._product = "product";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.published_at = reader.stringVerify();
                            message._published_at = "published_at";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.session_close = reader.stringVerify();
                            message._session_close = "session_close";
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            message.session_open = reader.stringVerify();
                            message._session_open = "session_open";
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            message.snapshot_id = reader.stringVerify();
                            message._snapshot_id = "snapshot_id";
                            continue;
                        }
                    case 9: {
                            if (wireType !== 2)
                                break;
                            message.symbol = reader.stringVerify();
                            message._symbol = "symbol";
                            continue;
                        }
                    case 10: {
                            if (wireType !== 2)
                                break;
                            message.trading_day = reader.stringVerify();
                            message._trading_day = "trading_day";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a DatasetSummary message.
             * @function verify
             * @memberof northstar.research.DatasetSummary
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DatasetSummary.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.bar_count != null && $Object.hasOwnProperty.call(message, "bar_count")) {
                    properties._bar_count = 1;
                    if (!$util.isInteger(message.bar_count) && !(message.bar_count && $util.isInteger(message.bar_count.low) && $util.isInteger(message.bar_count.high)))
                        return "bar_count: integer|Long expected";
                }
                if (message.content_hash != null && $Object.hasOwnProperty.call(message, "content_hash")) {
                    properties._content_hash = 1;
                    if (!$util.isString(message.content_hash))
                        return "content_hash: string expected";
                }
                if (message.exchange != null && $Object.hasOwnProperty.call(message, "exchange")) {
                    properties._exchange = 1;
                    if (!$util.isString(message.exchange))
                        return "exchange: string expected";
                }
                if (message.product != null && $Object.hasOwnProperty.call(message, "product")) {
                    properties._product = 1;
                    if (!$util.isString(message.product))
                        return "product: string expected";
                }
                if (message.published_at != null && $Object.hasOwnProperty.call(message, "published_at")) {
                    properties._published_at = 1;
                    if (!$util.isString(message.published_at))
                        return "published_at: string expected";
                }
                if (message.session_close != null && $Object.hasOwnProperty.call(message, "session_close")) {
                    properties._session_close = 1;
                    if (!$util.isString(message.session_close))
                        return "session_close: string expected";
                }
                if (message.session_open != null && $Object.hasOwnProperty.call(message, "session_open")) {
                    properties._session_open = 1;
                    if (!$util.isString(message.session_open))
                        return "session_open: string expected";
                }
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id")) {
                    properties._snapshot_id = 1;
                    if (!$util.isString(message.snapshot_id))
                        return "snapshot_id: string expected";
                }
                if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol")) {
                    properties._symbol = 1;
                    if (!$util.isString(message.symbol))
                        return "symbol: string expected";
                }
                if (message.trading_day != null && $Object.hasOwnProperty.call(message, "trading_day")) {
                    properties._trading_day = 1;
                    if (!$util.isString(message.trading_day))
                        return "trading_day: string expected";
                }
                return null;
            };

            /**
             * Creates a DatasetSummary message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.DatasetSummary
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.DatasetSummary} DatasetSummary
             */
            DatasetSummary.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.DatasetSummary)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.DatasetSummary: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.DatasetSummary();
                if (object.bar_count != null)
                    if ($util.Long)
                        message.bar_count = $util.Long.fromValue(object.bar_count, false);
                    else if (typeof object.bar_count === "string")
                        message.bar_count = $parseInt(object.bar_count, 10);
                    else if (typeof object.bar_count === "number")
                        message.bar_count = object.bar_count;
                    else if (typeof object.bar_count === "object")
                        message.bar_count = new $util.LongBits(object.bar_count.low >>> 0, object.bar_count.high >>> 0).toNumber();
                if (object.content_hash != null)
                    message.content_hash = $String(object.content_hash);
                if (object.exchange != null)
                    message.exchange = $String(object.exchange);
                if (object.product != null)
                    message.product = $String(object.product);
                if (object.published_at != null)
                    message.published_at = $String(object.published_at);
                if (object.session_close != null)
                    message.session_close = $String(object.session_close);
                if (object.session_open != null)
                    message.session_open = $String(object.session_open);
                if (object.snapshot_id != null)
                    message.snapshot_id = $String(object.snapshot_id);
                if (object.symbol != null)
                    message.symbol = $String(object.symbol);
                if (object.trading_day != null)
                    message.trading_day = $String(object.trading_day);
                return message;
            };

            /**
             * Creates a plain object from a DatasetSummary message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.DatasetSummary
             * @static
             * @param {northstar.research.DatasetSummary} message DatasetSummary
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DatasetSummary.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.bar_count != null && $Object.hasOwnProperty.call(message, "bar_count")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.bar_count = typeof message.bar_count === "number" ? $BigInt(message.bar_count) : $util.Long.fromBits(message.bar_count.low >>> 0, message.bar_count.high >>> 0, false).toBigInt();
                    else if (typeof message.bar_count === "number")
                        object.bar_count = options.longs === $String ? $String(message.bar_count) : message.bar_count;
                    else
                        object.bar_count = options.longs === $String ? $util.Long.prototype.toString.call(message.bar_count) : options.longs === $Number ? new $util.LongBits(message.bar_count.low >>> 0, message.bar_count.high >>> 0).toNumber() : message.bar_count;
                    if (options.oneofs)
                        object._bar_count = "bar_count";
                }
                if (message.content_hash != null && $Object.hasOwnProperty.call(message, "content_hash")) {
                    object.content_hash = message.content_hash;
                    if (options.oneofs)
                        object._content_hash = "content_hash";
                }
                if (message.exchange != null && $Object.hasOwnProperty.call(message, "exchange")) {
                    object.exchange = message.exchange;
                    if (options.oneofs)
                        object._exchange = "exchange";
                }
                if (message.product != null && $Object.hasOwnProperty.call(message, "product")) {
                    object.product = message.product;
                    if (options.oneofs)
                        object._product = "product";
                }
                if (message.published_at != null && $Object.hasOwnProperty.call(message, "published_at")) {
                    object.published_at = message.published_at;
                    if (options.oneofs)
                        object._published_at = "published_at";
                }
                if (message.session_close != null && $Object.hasOwnProperty.call(message, "session_close")) {
                    object.session_close = message.session_close;
                    if (options.oneofs)
                        object._session_close = "session_close";
                }
                if (message.session_open != null && $Object.hasOwnProperty.call(message, "session_open")) {
                    object.session_open = message.session_open;
                    if (options.oneofs)
                        object._session_open = "session_open";
                }
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id")) {
                    object.snapshot_id = message.snapshot_id;
                    if (options.oneofs)
                        object._snapshot_id = "snapshot_id";
                }
                if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol")) {
                    object.symbol = message.symbol;
                    if (options.oneofs)
                        object._symbol = "symbol";
                }
                if (message.trading_day != null && $Object.hasOwnProperty.call(message, "trading_day")) {
                    object.trading_day = message.trading_day;
                    if (options.oneofs)
                        object._trading_day = "trading_day";
                }
                return object;
            };

            /**
             * Converts this DatasetSummary to JSON.
             * @function toJSON
             * @memberof northstar.research.DatasetSummary
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DatasetSummary.prototype.toJSON = function() {
                return DatasetSummary.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for DatasetSummary
             * @function getTypeUrl
             * @memberof northstar.research.DatasetSummary
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            DatasetSummary.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.DatasetSummary";
            };

            return DatasetSummary;
        })();

        research.EquityPoint = (function() {

            /**
             * Properties of an EquityPoint.
             * @typedef {Object} northstar.research.EquityPoint.$Properties
             * @property {string|null} [at] EquityPoint at
             * @property {string|null} [equity] EquityPoint equity
             * @property {string|null} [observation_id] EquityPoint observation_id
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] EquityPoint evidence_fields
             * @property {"at"} [_at] EquityPoint _at
             * @property {"equity"} [_equity] EquityPoint _equity
             * @property {"observation_id"} [_observation_id] EquityPoint _observation_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an EquityPoint.
             * @memberof northstar.research
             * @interface IEquityPoint
             * @augments northstar.research.EquityPoint.$Properties
             * @deprecated Use northstar.research.EquityPoint.$Properties instead.
             */

            /**
             * Narrowed shape of an EquityPoint.
             * @typedef {{
             *   at?: string|null;
             *   equity?: string|null;
             *   observation_id?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _at?: undefined; at?: null }|{ _at?: "at"; at: string })
             * ) & (
             *   ({ _equity?: undefined; equity?: null }|{ _equity?: "equity"; equity: string })
             * ) & (
             *   ({ _observation_id?: undefined; observation_id?: null }|{ _observation_id?: "observation_id"; observation_id: string })
             * )} northstar.research.EquityPoint.$Shape
             */

            /**
             * Constructs a new EquityPoint.
             * @memberof northstar.research
             * @classdesc Represents an EquityPoint.
             * @constructor
             * @param {northstar.research.EquityPoint.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const EquityPoint = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * EquityPoint at.
             * @member {string|null|undefined} at
             * @memberof northstar.research.EquityPoint
             * @instance
             */
            EquityPoint.prototype.at = null;

            /**
             * EquityPoint equity.
             * @member {string|null|undefined} equity
             * @memberof northstar.research.EquityPoint
             * @instance
             */
            EquityPoint.prototype.equity = null;

            /**
             * EquityPoint observation_id.
             * @member {string|null|undefined} observation_id
             * @memberof northstar.research.EquityPoint
             * @instance
             */
            EquityPoint.prototype.observation_id = null;

            /**
             * EquityPoint evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.research.EquityPoint
             * @instance
             */
            EquityPoint.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * EquityPoint _at.
             * @member {"at"|undefined} _at
             * @memberof northstar.research.EquityPoint
             * @instance
             */
            $Object.defineProperty(EquityPoint.prototype, "_at", {
                get: $util.oneOfGetter($oneOfFields = ["at"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * EquityPoint _equity.
             * @member {"equity"|undefined} _equity
             * @memberof northstar.research.EquityPoint
             * @instance
             */
            $Object.defineProperty(EquityPoint.prototype, "_equity", {
                get: $util.oneOfGetter($oneOfFields = ["equity"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * EquityPoint _observation_id.
             * @member {"observation_id"|undefined} _observation_id
             * @memberof northstar.research.EquityPoint
             * @instance
             */
            $Object.defineProperty(EquityPoint.prototype, "_observation_id", {
                get: $util.oneOfGetter($oneOfFields = ["observation_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new EquityPoint instance using the specified properties.
             * @function create
             * @memberof northstar.research.EquityPoint
             * @static
             * @param {northstar.research.EquityPoint.$Properties=} [properties] Properties to set
             * @returns {northstar.research.EquityPoint} EquityPoint instance
             * @type {{
             *   (properties: northstar.research.EquityPoint.$Shape): northstar.research.EquityPoint & northstar.research.EquityPoint.$Shape;
             *   (properties?: northstar.research.EquityPoint.$Properties): northstar.research.EquityPoint;
             * }}
             */
            EquityPoint.create = function(properties) {
                return new EquityPoint(properties);
            };

            /**
             * Encodes the specified EquityPoint message. Does not implicitly {@link northstar.research.EquityPoint.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.EquityPoint
             * @static
             * @param {northstar.research.EquityPoint.$Properties} message EquityPoint message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EquityPoint.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.at != null && $Object.hasOwnProperty.call(message, "at"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.at);
                if (message.equity != null && $Object.hasOwnProperty.call(message, "equity"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.equity);
                if (message.observation_id != null && $Object.hasOwnProperty.call(message, "observation_id"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.observation_id);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes an EquityPoint message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.EquityPoint
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.EquityPoint & northstar.research.EquityPoint.$Shape} EquityPoint
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EquityPoint.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.EquityPoint();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.at = reader.stringVerify();
                            message._at = "at";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.equity = reader.stringVerify();
                            message._equity = "equity";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.observation_id = reader.stringVerify();
                            message._observation_id = "observation_id";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies an EquityPoint message.
             * @function verify
             * @memberof northstar.research.EquityPoint
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            EquityPoint.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.at != null && $Object.hasOwnProperty.call(message, "at")) {
                    properties._at = 1;
                    if (!$util.isString(message.at))
                        return "at: string expected";
                }
                if (message.equity != null && $Object.hasOwnProperty.call(message, "equity")) {
                    properties._equity = 1;
                    if (!$util.isString(message.equity))
                        return "equity: string expected";
                }
                if (message.observation_id != null && $Object.hasOwnProperty.call(message, "observation_id")) {
                    properties._observation_id = 1;
                    if (!$util.isString(message.observation_id))
                        return "observation_id: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an EquityPoint message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.EquityPoint
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.EquityPoint} EquityPoint
             */
            EquityPoint.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.EquityPoint)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.EquityPoint: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.EquityPoint();
                if (object.at != null)
                    message.at = $String(object.at);
                if (object.equity != null)
                    message.equity = $String(object.equity);
                if (object.observation_id != null)
                    message.observation_id = $String(object.observation_id);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.research.EquityPoint.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.research.EquityPoint.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from an EquityPoint message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.EquityPoint
             * @static
             * @param {northstar.research.EquityPoint} message EquityPoint
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            EquityPoint.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.at != null && $Object.hasOwnProperty.call(message, "at")) {
                    object.at = message.at;
                    if (options.oneofs)
                        object._at = "at";
                }
                if (message.equity != null && $Object.hasOwnProperty.call(message, "equity")) {
                    object.equity = message.equity;
                    if (options.oneofs)
                        object._equity = "equity";
                }
                if (message.observation_id != null && $Object.hasOwnProperty.call(message, "observation_id")) {
                    object.observation_id = message.observation_id;
                    if (options.oneofs)
                        object._observation_id = "observation_id";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this EquityPoint to JSON.
             * @function toJSON
             * @memberof northstar.research.EquityPoint
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            EquityPoint.prototype.toJSON = function() {
                return EquityPoint.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for EquityPoint
             * @function getTypeUrl
             * @memberof northstar.research.EquityPoint
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            EquityPoint.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.EquityPoint";
            };

            return EquityPoint;
        })();

        research.FactorBinding = (function() {

            /**
             * Properties of a FactorBinding.
             * @typedef {Object} northstar.research.FactorBinding.$Properties
             * @property {string|null} [code_revision] FactorBinding code_revision
             * @property {string|null} [factor_id] FactorBinding factor_id
             * @property {google.protobuf.Struct.$Properties|null} [parameters] FactorBinding parameters
             * @property {string|null} [revision] FactorBinding revision
             * @property {"code_revision"} [_code_revision] FactorBinding _code_revision
             * @property {"factor_id"} [_factor_id] FactorBinding _factor_id
             * @property {"parameters"} [_parameters] FactorBinding _parameters
             * @property {"revision"} [_revision] FactorBinding _revision
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FactorBinding.
             * @memberof northstar.research
             * @interface IFactorBinding
             * @augments northstar.research.FactorBinding.$Properties
             * @deprecated Use northstar.research.FactorBinding.$Properties instead.
             */

            /**
             * Narrowed shape of a FactorBinding.
             * @typedef {{
             *   code_revision?: string|null;
             *   factor_id?: string|null;
             *   parameters?: google.protobuf.Struct.$Shape|null;
             *   revision?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _code_revision?: undefined; code_revision?: null }|{ _code_revision?: "code_revision"; code_revision: string })
             * ) & (
             *   ({ _factor_id?: undefined; factor_id?: null }|{ _factor_id?: "factor_id"; factor_id: string })
             * ) & (
             *   ({ _parameters?: undefined; parameters?: null }|{ _parameters?: "parameters"; parameters: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _revision?: undefined; revision?: null }|{ _revision?: "revision"; revision: string })
             * )} northstar.research.FactorBinding.$Shape
             */

            /**
             * Constructs a new FactorBinding.
             * @memberof northstar.research
             * @classdesc Represents a FactorBinding.
             * @constructor
             * @param {northstar.research.FactorBinding.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FactorBinding = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FactorBinding code_revision.
             * @member {string|null|undefined} code_revision
             * @memberof northstar.research.FactorBinding
             * @instance
             */
            FactorBinding.prototype.code_revision = null;

            /**
             * FactorBinding factor_id.
             * @member {string|null|undefined} factor_id
             * @memberof northstar.research.FactorBinding
             * @instance
             */
            FactorBinding.prototype.factor_id = null;

            /**
             * FactorBinding parameters.
             * @member {google.protobuf.Struct.$Properties|null|undefined} parameters
             * @memberof northstar.research.FactorBinding
             * @instance
             */
            FactorBinding.prototype.parameters = null;

            /**
             * FactorBinding revision.
             * @member {string|null|undefined} revision
             * @memberof northstar.research.FactorBinding
             * @instance
             */
            FactorBinding.prototype.revision = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * FactorBinding _code_revision.
             * @member {"code_revision"|undefined} _code_revision
             * @memberof northstar.research.FactorBinding
             * @instance
             */
            $Object.defineProperty(FactorBinding.prototype, "_code_revision", {
                get: $util.oneOfGetter($oneOfFields = ["code_revision"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorBinding _factor_id.
             * @member {"factor_id"|undefined} _factor_id
             * @memberof northstar.research.FactorBinding
             * @instance
             */
            $Object.defineProperty(FactorBinding.prototype, "_factor_id", {
                get: $util.oneOfGetter($oneOfFields = ["factor_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorBinding _parameters.
             * @member {"parameters"|undefined} _parameters
             * @memberof northstar.research.FactorBinding
             * @instance
             */
            $Object.defineProperty(FactorBinding.prototype, "_parameters", {
                get: $util.oneOfGetter($oneOfFields = ["parameters"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorBinding _revision.
             * @member {"revision"|undefined} _revision
             * @memberof northstar.research.FactorBinding
             * @instance
             */
            $Object.defineProperty(FactorBinding.prototype, "_revision", {
                get: $util.oneOfGetter($oneOfFields = ["revision"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new FactorBinding instance using the specified properties.
             * @function create
             * @memberof northstar.research.FactorBinding
             * @static
             * @param {northstar.research.FactorBinding.$Properties=} [properties] Properties to set
             * @returns {northstar.research.FactorBinding} FactorBinding instance
             * @type {{
             *   (properties: northstar.research.FactorBinding.$Shape): northstar.research.FactorBinding & northstar.research.FactorBinding.$Shape;
             *   (properties?: northstar.research.FactorBinding.$Properties): northstar.research.FactorBinding;
             * }}
             */
            FactorBinding.create = function(properties) {
                return new FactorBinding(properties);
            };

            /**
             * Encodes the specified FactorBinding message. Does not implicitly {@link northstar.research.FactorBinding.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.FactorBinding
             * @static
             * @param {northstar.research.FactorBinding.$Properties} message FactorBinding message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FactorBinding.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.code_revision);
                if (message.factor_id != null && $Object.hasOwnProperty.call(message, "factor_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.factor_id);
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters"))
                    $root.google.protobuf.Struct.encode(message.parameters, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.revision != null && $Object.hasOwnProperty.call(message, "revision"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.revision);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a FactorBinding message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.FactorBinding
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.FactorBinding & northstar.research.FactorBinding.$Shape} FactorBinding
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FactorBinding.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.FactorBinding();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.code_revision = reader.stringVerify();
                            message._code_revision = "code_revision";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.factor_id = reader.stringVerify();
                            message._factor_id = "factor_id";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.parameters = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.parameters);
                            message._parameters = "parameters";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.revision = reader.stringVerify();
                            message._revision = "revision";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a FactorBinding message.
             * @function verify
             * @memberof northstar.research.FactorBinding
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FactorBinding.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision")) {
                    properties._code_revision = 1;
                    if (!$util.isString(message.code_revision))
                        return "code_revision: string expected";
                }
                if (message.factor_id != null && $Object.hasOwnProperty.call(message, "factor_id")) {
                    properties._factor_id = 1;
                    if (!$util.isString(message.factor_id))
                        return "factor_id: string expected";
                }
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters")) {
                    properties._parameters = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.parameters, _depth + 1);
                        if (error)
                            return "parameters." + error;
                    }
                }
                if (message.revision != null && $Object.hasOwnProperty.call(message, "revision")) {
                    properties._revision = 1;
                    if (!$util.isString(message.revision))
                        return "revision: string expected";
                }
                return null;
            };

            /**
             * Creates a FactorBinding message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.FactorBinding
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.FactorBinding} FactorBinding
             */
            FactorBinding.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.FactorBinding)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.FactorBinding: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.FactorBinding();
                if (object.code_revision != null)
                    message.code_revision = $String(object.code_revision);
                if (object.factor_id != null)
                    message.factor_id = $String(object.factor_id);
                if (object.parameters != null) {
                    if (!$util.isObject(object.parameters))
                        throw $TypeError(".northstar.research.FactorBinding.parameters: object expected");
                    message.parameters = $root.google.protobuf.Struct.fromObject(object.parameters, _depth + 1);
                }
                if (object.revision != null)
                    message.revision = $String(object.revision);
                return message;
            };

            /**
             * Creates a plain object from a FactorBinding message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.FactorBinding
             * @static
             * @param {northstar.research.FactorBinding} message FactorBinding
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FactorBinding.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision")) {
                    object.code_revision = message.code_revision;
                    if (options.oneofs)
                        object._code_revision = "code_revision";
                }
                if (message.factor_id != null && $Object.hasOwnProperty.call(message, "factor_id")) {
                    object.factor_id = message.factor_id;
                    if (options.oneofs)
                        object._factor_id = "factor_id";
                }
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters")) {
                    object.parameters = $root.google.protobuf.Struct.toObject(message.parameters, options, _depth + 1);
                    if (options.oneofs)
                        object._parameters = "parameters";
                }
                if (message.revision != null && $Object.hasOwnProperty.call(message, "revision")) {
                    object.revision = message.revision;
                    if (options.oneofs)
                        object._revision = "revision";
                }
                return object;
            };

            /**
             * Converts this FactorBinding to JSON.
             * @function toJSON
             * @memberof northstar.research.FactorBinding
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FactorBinding.prototype.toJSON = function() {
                return FactorBinding.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FactorBinding
             * @function getTypeUrl
             * @memberof northstar.research.FactorBinding
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FactorBinding.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.FactorBinding";
            };

            return FactorBinding;
        })();

        research.FactorDescription = (function() {

            /**
             * Properties of a FactorDescription.
             * @typedef {Object} northstar.research.FactorDescription.$Properties
             * @property {Array.<string>|null} [capabilities] FactorDescription capabilities
             * @property {string|null} [category] FactorDescription category
             * @property {string|null} [description] FactorDescription description
             * @property {string|null} [factor_id] FactorDescription factor_id
             * @property {string|null} [limitations] FactorDescription limitations
             * @property {string|null} [name] FactorDescription name
             * @property {Array.<northstar.research.ParameterDescription.$Properties>|null} [parameters] FactorDescription parameters
             * @property {string|null} [revision] FactorDescription revision
             * @property {"category"} [_category] FactorDescription _category
             * @property {"description"} [_description] FactorDescription _description
             * @property {"factor_id"} [_factor_id] FactorDescription _factor_id
             * @property {"limitations"} [_limitations] FactorDescription _limitations
             * @property {"name"} [_name] FactorDescription _name
             * @property {"revision"} [_revision] FactorDescription _revision
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FactorDescription.
             * @memberof northstar.research
             * @interface IFactorDescription
             * @augments northstar.research.FactorDescription.$Properties
             * @deprecated Use northstar.research.FactorDescription.$Properties instead.
             */

            /**
             * Narrowed shape of a FactorDescription.
             * @typedef {{
             *   capabilities?: Array.<string>|null;
             *   category?: string|null;
             *   description?: string|null;
             *   factor_id?: string|null;
             *   limitations?: string|null;
             *   name?: string|null;
             *   parameters?: Array.<northstar.research.ParameterDescription.$Shape>|null;
             *   revision?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _category?: undefined; category?: null }|{ _category?: "category"; category: string })
             * ) & (
             *   ({ _description?: undefined; description?: null }|{ _description?: "description"; description: string })
             * ) & (
             *   ({ _factor_id?: undefined; factor_id?: null }|{ _factor_id?: "factor_id"; factor_id: string })
             * ) & (
             *   ({ _limitations?: undefined; limitations?: null }|{ _limitations?: "limitations"; limitations: string })
             * ) & (
             *   ({ _name?: undefined; name?: null }|{ _name?: "name"; name: string })
             * ) & (
             *   ({ _revision?: undefined; revision?: null }|{ _revision?: "revision"; revision: string })
             * )} northstar.research.FactorDescription.$Shape
             */

            /**
             * Constructs a new FactorDescription.
             * @memberof northstar.research
             * @classdesc Represents a FactorDescription.
             * @constructor
             * @param {northstar.research.FactorDescription.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FactorDescription = function (properties) {
                this.capabilities = [];
                this.parameters = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FactorDescription capabilities.
             * @member {Array.<string>} capabilities
             * @memberof northstar.research.FactorDescription
             * @instance
             */
            FactorDescription.prototype.capabilities = $util.emptyArray;

            /**
             * FactorDescription category.
             * @member {string|null|undefined} category
             * @memberof northstar.research.FactorDescription
             * @instance
             */
            FactorDescription.prototype.category = null;

            /**
             * FactorDescription description.
             * @member {string|null|undefined} description
             * @memberof northstar.research.FactorDescription
             * @instance
             */
            FactorDescription.prototype.description = null;

            /**
             * FactorDescription factor_id.
             * @member {string|null|undefined} factor_id
             * @memberof northstar.research.FactorDescription
             * @instance
             */
            FactorDescription.prototype.factor_id = null;

            /**
             * FactorDescription limitations.
             * @member {string|null|undefined} limitations
             * @memberof northstar.research.FactorDescription
             * @instance
             */
            FactorDescription.prototype.limitations = null;

            /**
             * FactorDescription name.
             * @member {string|null|undefined} name
             * @memberof northstar.research.FactorDescription
             * @instance
             */
            FactorDescription.prototype.name = null;

            /**
             * FactorDescription parameters.
             * @member {Array.<northstar.research.ParameterDescription.$Properties>} parameters
             * @memberof northstar.research.FactorDescription
             * @instance
             */
            FactorDescription.prototype.parameters = $util.emptyArray;

            /**
             * FactorDescription revision.
             * @member {string|null|undefined} revision
             * @memberof northstar.research.FactorDescription
             * @instance
             */
            FactorDescription.prototype.revision = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * FactorDescription _category.
             * @member {"category"|undefined} _category
             * @memberof northstar.research.FactorDescription
             * @instance
             */
            $Object.defineProperty(FactorDescription.prototype, "_category", {
                get: $util.oneOfGetter($oneOfFields = ["category"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorDescription _description.
             * @member {"description"|undefined} _description
             * @memberof northstar.research.FactorDescription
             * @instance
             */
            $Object.defineProperty(FactorDescription.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorDescription _factor_id.
             * @member {"factor_id"|undefined} _factor_id
             * @memberof northstar.research.FactorDescription
             * @instance
             */
            $Object.defineProperty(FactorDescription.prototype, "_factor_id", {
                get: $util.oneOfGetter($oneOfFields = ["factor_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorDescription _limitations.
             * @member {"limitations"|undefined} _limitations
             * @memberof northstar.research.FactorDescription
             * @instance
             */
            $Object.defineProperty(FactorDescription.prototype, "_limitations", {
                get: $util.oneOfGetter($oneOfFields = ["limitations"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorDescription _name.
             * @member {"name"|undefined} _name
             * @memberof northstar.research.FactorDescription
             * @instance
             */
            $Object.defineProperty(FactorDescription.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorDescription _revision.
             * @member {"revision"|undefined} _revision
             * @memberof northstar.research.FactorDescription
             * @instance
             */
            $Object.defineProperty(FactorDescription.prototype, "_revision", {
                get: $util.oneOfGetter($oneOfFields = ["revision"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new FactorDescription instance using the specified properties.
             * @function create
             * @memberof northstar.research.FactorDescription
             * @static
             * @param {northstar.research.FactorDescription.$Properties=} [properties] Properties to set
             * @returns {northstar.research.FactorDescription} FactorDescription instance
             * @type {{
             *   (properties: northstar.research.FactorDescription.$Shape): northstar.research.FactorDescription & northstar.research.FactorDescription.$Shape;
             *   (properties?: northstar.research.FactorDescription.$Properties): northstar.research.FactorDescription;
             * }}
             */
            FactorDescription.create = function(properties) {
                return new FactorDescription(properties);
            };

            /**
             * Encodes the specified FactorDescription message. Does not implicitly {@link northstar.research.FactorDescription.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.FactorDescription
             * @static
             * @param {northstar.research.FactorDescription.$Properties} message FactorDescription message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FactorDescription.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.capabilities != null && message.capabilities.length)
                    for (let i = 0; i < message.capabilities.length; ++i)
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.capabilities[i]);
                if (message.category != null && $Object.hasOwnProperty.call(message, "category"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.category);
                if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.description);
                if (message.factor_id != null && $Object.hasOwnProperty.call(message, "factor_id"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.factor_id);
                if (message.limitations != null && $Object.hasOwnProperty.call(message, "limitations"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.limitations);
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.name);
                if (message.parameters != null && message.parameters.length)
                    for (let i = 0; i < message.parameters.length; ++i)
                        $root.northstar.research.ParameterDescription.encode(message.parameters[i], writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
                if (message.revision != null && $Object.hasOwnProperty.call(message, "revision"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.revision);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a FactorDescription message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.FactorDescription
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.FactorDescription & northstar.research.FactorDescription.$Shape} FactorDescription
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FactorDescription.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.FactorDescription();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.capabilities && message.capabilities.length))
                                message.capabilities = [];
                            message.capabilities.push(reader.stringVerify());
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.category = reader.stringVerify();
                            message._category = "category";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.description = reader.stringVerify();
                            message._description = "description";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.factor_id = reader.stringVerify();
                            message._factor_id = "factor_id";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.limitations = reader.stringVerify();
                            message._limitations = "limitations";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            if (!(message.parameters && message.parameters.length))
                                message.parameters = [];
                            message.parameters.push($root.northstar.research.ParameterDescription.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            message.revision = reader.stringVerify();
                            message._revision = "revision";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a FactorDescription message.
             * @function verify
             * @memberof northstar.research.FactorDescription
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FactorDescription.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.capabilities != null && $Object.hasOwnProperty.call(message, "capabilities")) {
                    if (!$Array.isArray(message.capabilities))
                        return "capabilities: array expected";
                    for (let i = 0; i < message.capabilities.length; ++i)
                        if (!$util.isString(message.capabilities[i]))
                            return "capabilities: string[] expected";
                }
                if (message.category != null && $Object.hasOwnProperty.call(message, "category")) {
                    properties._category = 1;
                    if (!$util.isString(message.category))
                        return "category: string expected";
                }
                if (message.description != null && $Object.hasOwnProperty.call(message, "description")) {
                    properties._description = 1;
                    if (!$util.isString(message.description))
                        return "description: string expected";
                }
                if (message.factor_id != null && $Object.hasOwnProperty.call(message, "factor_id")) {
                    properties._factor_id = 1;
                    if (!$util.isString(message.factor_id))
                        return "factor_id: string expected";
                }
                if (message.limitations != null && $Object.hasOwnProperty.call(message, "limitations")) {
                    properties._limitations = 1;
                    if (!$util.isString(message.limitations))
                        return "limitations: string expected";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters")) {
                    if (!$Array.isArray(message.parameters))
                        return "parameters: array expected";
                    for (let i = 0; i < message.parameters.length; ++i) {
                        let error = $root.northstar.research.ParameterDescription.verify(message.parameters[i], _depth + 1);
                        if (error)
                            return "parameters." + error;
                    }
                }
                if (message.revision != null && $Object.hasOwnProperty.call(message, "revision")) {
                    properties._revision = 1;
                    if (!$util.isString(message.revision))
                        return "revision: string expected";
                }
                return null;
            };

            /**
             * Creates a FactorDescription message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.FactorDescription
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.FactorDescription} FactorDescription
             */
            FactorDescription.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.FactorDescription)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.FactorDescription: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.FactorDescription();
                if (object.capabilities) {
                    if (!$Array.isArray(object.capabilities))
                        throw $TypeError(".northstar.research.FactorDescription.capabilities: array expected");
                    message.capabilities = $Array(object.capabilities.length);
                    for (let i = 0; i < object.capabilities.length; ++i)
                        message.capabilities[i] = $String(object.capabilities[i]);
                }
                if (object.category != null)
                    message.category = $String(object.category);
                if (object.description != null)
                    message.description = $String(object.description);
                if (object.factor_id != null)
                    message.factor_id = $String(object.factor_id);
                if (object.limitations != null)
                    message.limitations = $String(object.limitations);
                if (object.name != null)
                    message.name = $String(object.name);
                if (object.parameters) {
                    if (!$Array.isArray(object.parameters))
                        throw $TypeError(".northstar.research.FactorDescription.parameters: array expected");
                    message.parameters = $Array(object.parameters.length);
                    for (let i = 0; i < object.parameters.length; ++i) {
                        if (!$util.isObject(object.parameters[i]))
                            throw $TypeError(".northstar.research.FactorDescription.parameters: object expected");
                        message.parameters[i] = $root.northstar.research.ParameterDescription.fromObject(object.parameters[i], _depth + 1);
                    }
                }
                if (object.revision != null)
                    message.revision = $String(object.revision);
                return message;
            };

            /**
             * Creates a plain object from a FactorDescription message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.FactorDescription
             * @static
             * @param {northstar.research.FactorDescription} message FactorDescription
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FactorDescription.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.capabilities = [];
                    object.parameters = [];
                }
                if (message.capabilities && message.capabilities.length) {
                    object.capabilities = $Array(message.capabilities.length);
                    for (let j = 0; j < message.capabilities.length; ++j)
                        object.capabilities[j] = message.capabilities[j];
                }
                if (message.category != null && $Object.hasOwnProperty.call(message, "category")) {
                    object.category = message.category;
                    if (options.oneofs)
                        object._category = "category";
                }
                if (message.description != null && $Object.hasOwnProperty.call(message, "description")) {
                    object.description = message.description;
                    if (options.oneofs)
                        object._description = "description";
                }
                if (message.factor_id != null && $Object.hasOwnProperty.call(message, "factor_id")) {
                    object.factor_id = message.factor_id;
                    if (options.oneofs)
                        object._factor_id = "factor_id";
                }
                if (message.limitations != null && $Object.hasOwnProperty.call(message, "limitations")) {
                    object.limitations = message.limitations;
                    if (options.oneofs)
                        object._limitations = "limitations";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    object.name = message.name;
                    if (options.oneofs)
                        object._name = "name";
                }
                if (message.parameters && message.parameters.length) {
                    object.parameters = $Array(message.parameters.length);
                    for (let j = 0; j < message.parameters.length; ++j)
                        object.parameters[j] = $root.northstar.research.ParameterDescription.toObject(message.parameters[j], options, _depth + 1);
                }
                if (message.revision != null && $Object.hasOwnProperty.call(message, "revision")) {
                    object.revision = message.revision;
                    if (options.oneofs)
                        object._revision = "revision";
                }
                return object;
            };

            /**
             * Converts this FactorDescription to JSON.
             * @function toJSON
             * @memberof northstar.research.FactorDescription
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FactorDescription.prototype.toJSON = function() {
                return FactorDescription.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FactorDescription
             * @function getTypeUrl
             * @memberof northstar.research.FactorDescription
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FactorDescription.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.FactorDescription";
            };

            return FactorDescription;
        })();

        research.FactorInput = (function() {

            /**
             * Properties of a FactorInput.
             * @typedef {Object} northstar.research.FactorInput.$Properties
             * @property {string|null} [factor_id] FactorInput factor_id
             * @property {google.protobuf.Struct.$Properties|null} [parameters] FactorInput parameters
             * @property {"factor_id"} [_factor_id] FactorInput _factor_id
             * @property {"parameters"} [_parameters] FactorInput _parameters
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FactorInput.
             * @memberof northstar.research
             * @interface IFactorInput
             * @augments northstar.research.FactorInput.$Properties
             * @deprecated Use northstar.research.FactorInput.$Properties instead.
             */

            /**
             * Narrowed shape of a FactorInput.
             * @typedef {{
             *   factor_id?: string|null;
             *   parameters?: google.protobuf.Struct.$Shape|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _factor_id?: undefined; factor_id?: null }|{ _factor_id?: "factor_id"; factor_id: string })
             * ) & (
             *   ({ _parameters?: undefined; parameters?: null }|{ _parameters?: "parameters"; parameters: google.protobuf.Struct.$Shape })
             * )} northstar.research.FactorInput.$Shape
             */

            /**
             * Constructs a new FactorInput.
             * @memberof northstar.research
             * @classdesc Represents a FactorInput.
             * @constructor
             * @param {northstar.research.FactorInput.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FactorInput = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FactorInput factor_id.
             * @member {string|null|undefined} factor_id
             * @memberof northstar.research.FactorInput
             * @instance
             */
            FactorInput.prototype.factor_id = null;

            /**
             * FactorInput parameters.
             * @member {google.protobuf.Struct.$Properties|null|undefined} parameters
             * @memberof northstar.research.FactorInput
             * @instance
             */
            FactorInput.prototype.parameters = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * FactorInput _factor_id.
             * @member {"factor_id"|undefined} _factor_id
             * @memberof northstar.research.FactorInput
             * @instance
             */
            $Object.defineProperty(FactorInput.prototype, "_factor_id", {
                get: $util.oneOfGetter($oneOfFields = ["factor_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorInput _parameters.
             * @member {"parameters"|undefined} _parameters
             * @memberof northstar.research.FactorInput
             * @instance
             */
            $Object.defineProperty(FactorInput.prototype, "_parameters", {
                get: $util.oneOfGetter($oneOfFields = ["parameters"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new FactorInput instance using the specified properties.
             * @function create
             * @memberof northstar.research.FactorInput
             * @static
             * @param {northstar.research.FactorInput.$Properties=} [properties] Properties to set
             * @returns {northstar.research.FactorInput} FactorInput instance
             * @type {{
             *   (properties: northstar.research.FactorInput.$Shape): northstar.research.FactorInput & northstar.research.FactorInput.$Shape;
             *   (properties?: northstar.research.FactorInput.$Properties): northstar.research.FactorInput;
             * }}
             */
            FactorInput.create = function(properties) {
                return new FactorInput(properties);
            };

            /**
             * Encodes the specified FactorInput message. Does not implicitly {@link northstar.research.FactorInput.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.FactorInput
             * @static
             * @param {northstar.research.FactorInput.$Properties} message FactorInput message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FactorInput.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.factor_id != null && $Object.hasOwnProperty.call(message, "factor_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.factor_id);
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters"))
                    $root.google.protobuf.Struct.encode(message.parameters, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a FactorInput message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.FactorInput
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.FactorInput & northstar.research.FactorInput.$Shape} FactorInput
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FactorInput.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.FactorInput();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.factor_id = reader.stringVerify();
                            message._factor_id = "factor_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.parameters = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.parameters);
                            message._parameters = "parameters";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a FactorInput message.
             * @function verify
             * @memberof northstar.research.FactorInput
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FactorInput.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.factor_id != null && $Object.hasOwnProperty.call(message, "factor_id")) {
                    properties._factor_id = 1;
                    if (!$util.isString(message.factor_id))
                        return "factor_id: string expected";
                }
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters")) {
                    properties._parameters = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.parameters, _depth + 1);
                        if (error)
                            return "parameters." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a FactorInput message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.FactorInput
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.FactorInput} FactorInput
             */
            FactorInput.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.FactorInput)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.FactorInput: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.FactorInput();
                if (object.factor_id != null)
                    message.factor_id = $String(object.factor_id);
                if (object.parameters != null) {
                    if (!$util.isObject(object.parameters))
                        throw $TypeError(".northstar.research.FactorInput.parameters: object expected");
                    message.parameters = $root.google.protobuf.Struct.fromObject(object.parameters, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a FactorInput message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.FactorInput
             * @static
             * @param {northstar.research.FactorInput} message FactorInput
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FactorInput.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.factor_id != null && $Object.hasOwnProperty.call(message, "factor_id")) {
                    object.factor_id = message.factor_id;
                    if (options.oneofs)
                        object._factor_id = "factor_id";
                }
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters")) {
                    object.parameters = $root.google.protobuf.Struct.toObject(message.parameters, options, _depth + 1);
                    if (options.oneofs)
                        object._parameters = "parameters";
                }
                return object;
            };

            /**
             * Converts this FactorInput to JSON.
             * @function toJSON
             * @memberof northstar.research.FactorInput
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FactorInput.prototype.toJSON = function() {
                return FactorInput.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FactorInput
             * @function getTypeUrl
             * @memberof northstar.research.FactorInput
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FactorInput.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.FactorInput";
            };

            return FactorInput;
        })();

        research.FactorResult = (function() {

            /**
             * Properties of a FactorResult.
             * @typedef {Object} northstar.research.FactorResult.$Properties
             * @property {google.protobuf.Struct.$Properties|null} [evaluation] FactorResult evaluation
             * @property {google.protobuf.Struct.$Properties|null} [inputs] FactorResult inputs
             * @property {Array.<northstar.research.FactorValue.$Properties>|null} [values] FactorResult values
             * @property {"evaluation"} [_evaluation] FactorResult _evaluation
             * @property {"inputs"} [_inputs] FactorResult _inputs
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FactorResult.
             * @memberof northstar.research
             * @interface IFactorResult
             * @augments northstar.research.FactorResult.$Properties
             * @deprecated Use northstar.research.FactorResult.$Properties instead.
             */

            /**
             * Narrowed shape of a FactorResult.
             * @typedef {{
             *   evaluation?: google.protobuf.Struct.$Shape|null;
             *   inputs?: google.protobuf.Struct.$Shape|null;
             *   values?: Array.<northstar.research.FactorValue.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _evaluation?: undefined; evaluation?: null }|{ _evaluation?: "evaluation"; evaluation: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _inputs?: undefined; inputs?: null }|{ _inputs?: "inputs"; inputs: google.protobuf.Struct.$Shape })
             * )} northstar.research.FactorResult.$Shape
             */

            /**
             * Constructs a new FactorResult.
             * @memberof northstar.research
             * @classdesc Represents a FactorResult.
             * @constructor
             * @param {northstar.research.FactorResult.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FactorResult = function (properties) {
                this.values = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FactorResult evaluation.
             * @member {google.protobuf.Struct.$Properties|null|undefined} evaluation
             * @memberof northstar.research.FactorResult
             * @instance
             */
            FactorResult.prototype.evaluation = null;

            /**
             * FactorResult inputs.
             * @member {google.protobuf.Struct.$Properties|null|undefined} inputs
             * @memberof northstar.research.FactorResult
             * @instance
             */
            FactorResult.prototype.inputs = null;

            /**
             * FactorResult values.
             * @member {Array.<northstar.research.FactorValue.$Properties>} values
             * @memberof northstar.research.FactorResult
             * @instance
             */
            FactorResult.prototype.values = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * FactorResult _evaluation.
             * @member {"evaluation"|undefined} _evaluation
             * @memberof northstar.research.FactorResult
             * @instance
             */
            $Object.defineProperty(FactorResult.prototype, "_evaluation", {
                get: $util.oneOfGetter($oneOfFields = ["evaluation"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorResult _inputs.
             * @member {"inputs"|undefined} _inputs
             * @memberof northstar.research.FactorResult
             * @instance
             */
            $Object.defineProperty(FactorResult.prototype, "_inputs", {
                get: $util.oneOfGetter($oneOfFields = ["inputs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new FactorResult instance using the specified properties.
             * @function create
             * @memberof northstar.research.FactorResult
             * @static
             * @param {northstar.research.FactorResult.$Properties=} [properties] Properties to set
             * @returns {northstar.research.FactorResult} FactorResult instance
             * @type {{
             *   (properties: northstar.research.FactorResult.$Shape): northstar.research.FactorResult & northstar.research.FactorResult.$Shape;
             *   (properties?: northstar.research.FactorResult.$Properties): northstar.research.FactorResult;
             * }}
             */
            FactorResult.create = function(properties) {
                return new FactorResult(properties);
            };

            /**
             * Encodes the specified FactorResult message. Does not implicitly {@link northstar.research.FactorResult.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.FactorResult
             * @static
             * @param {northstar.research.FactorResult.$Properties} message FactorResult message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FactorResult.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.evaluation != null && $Object.hasOwnProperty.call(message, "evaluation"))
                    $root.google.protobuf.Struct.encode(message.evaluation, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.inputs != null && $Object.hasOwnProperty.call(message, "inputs"))
                    $root.google.protobuf.Struct.encode(message.inputs, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.values != null && message.values.length)
                    for (let i = 0; i < message.values.length; ++i)
                        $root.northstar.research.FactorValue.encode(message.values[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a FactorResult message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.FactorResult
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.FactorResult & northstar.research.FactorResult.$Shape} FactorResult
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FactorResult.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.FactorResult();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.evaluation = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.evaluation);
                            message._evaluation = "evaluation";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.inputs = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.inputs);
                            message._inputs = "inputs";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if (!(message.values && message.values.length))
                                message.values = [];
                            message.values.push($root.northstar.research.FactorValue.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a FactorResult message.
             * @function verify
             * @memberof northstar.research.FactorResult
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FactorResult.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.evaluation != null && $Object.hasOwnProperty.call(message, "evaluation")) {
                    properties._evaluation = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.evaluation, _depth + 1);
                        if (error)
                            return "evaluation." + error;
                    }
                }
                if (message.inputs != null && $Object.hasOwnProperty.call(message, "inputs")) {
                    properties._inputs = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.inputs, _depth + 1);
                        if (error)
                            return "inputs." + error;
                    }
                }
                if (message.values != null && $Object.hasOwnProperty.call(message, "values")) {
                    if (!$Array.isArray(message.values))
                        return "values: array expected";
                    for (let i = 0; i < message.values.length; ++i) {
                        let error = $root.northstar.research.FactorValue.verify(message.values[i], _depth + 1);
                        if (error)
                            return "values." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a FactorResult message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.FactorResult
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.FactorResult} FactorResult
             */
            FactorResult.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.FactorResult)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.FactorResult: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.FactorResult();
                if (object.evaluation != null) {
                    if (!$util.isObject(object.evaluation))
                        throw $TypeError(".northstar.research.FactorResult.evaluation: object expected");
                    message.evaluation = $root.google.protobuf.Struct.fromObject(object.evaluation, _depth + 1);
                }
                if (object.inputs != null) {
                    if (!$util.isObject(object.inputs))
                        throw $TypeError(".northstar.research.FactorResult.inputs: object expected");
                    message.inputs = $root.google.protobuf.Struct.fromObject(object.inputs, _depth + 1);
                }
                if (object.values) {
                    if (!$Array.isArray(object.values))
                        throw $TypeError(".northstar.research.FactorResult.values: array expected");
                    message.values = $Array(object.values.length);
                    for (let i = 0; i < object.values.length; ++i) {
                        if (!$util.isObject(object.values[i]))
                            throw $TypeError(".northstar.research.FactorResult.values: object expected");
                        message.values[i] = $root.northstar.research.FactorValue.fromObject(object.values[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a FactorResult message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.FactorResult
             * @static
             * @param {northstar.research.FactorResult} message FactorResult
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FactorResult.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.values = [];
                if (message.evaluation != null && $Object.hasOwnProperty.call(message, "evaluation")) {
                    object.evaluation = $root.google.protobuf.Struct.toObject(message.evaluation, options, _depth + 1);
                    if (options.oneofs)
                        object._evaluation = "evaluation";
                }
                if (message.inputs != null && $Object.hasOwnProperty.call(message, "inputs")) {
                    object.inputs = $root.google.protobuf.Struct.toObject(message.inputs, options, _depth + 1);
                    if (options.oneofs)
                        object._inputs = "inputs";
                }
                if (message.values && message.values.length) {
                    object.values = $Array(message.values.length);
                    for (let j = 0; j < message.values.length; ++j)
                        object.values[j] = $root.northstar.research.FactorValue.toObject(message.values[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this FactorResult to JSON.
             * @function toJSON
             * @memberof northstar.research.FactorResult
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FactorResult.prototype.toJSON = function() {
                return FactorResult.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FactorResult
             * @function getTypeUrl
             * @memberof northstar.research.FactorResult
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FactorResult.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.FactorResult";
            };

            return FactorResult;
        })();

        research.FactorRevision = (function() {

            /**
             * Properties of a FactorRevision.
             * @typedef {Object} northstar.research.FactorRevision.$Properties
             * @property {Array.<northstar.research.Annotation.$Properties>|null} [annotations] FactorRevision annotations
             * @property {northstar.research.FactorBinding.$Properties|null} [binding] FactorRevision binding
             * @property {string|null} [revision_id] FactorRevision revision_id
             * @property {"binding"} [_binding] FactorRevision _binding
             * @property {"revision_id"} [_revision_id] FactorRevision _revision_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FactorRevision.
             * @memberof northstar.research
             * @interface IFactorRevision
             * @augments northstar.research.FactorRevision.$Properties
             * @deprecated Use northstar.research.FactorRevision.$Properties instead.
             */

            /**
             * Narrowed shape of a FactorRevision.
             * @typedef {{
             *   annotations?: Array.<northstar.research.Annotation.$Shape>|null;
             *   binding?: northstar.research.FactorBinding.$Shape|null;
             *   revision_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _binding?: undefined; binding?: null }|{ _binding?: "binding"; binding: northstar.research.FactorBinding.$Shape })
             * ) & (
             *   ({ _revision_id?: undefined; revision_id?: null }|{ _revision_id?: "revision_id"; revision_id: string })
             * )} northstar.research.FactorRevision.$Shape
             */

            /**
             * Constructs a new FactorRevision.
             * @memberof northstar.research
             * @classdesc Represents a FactorRevision.
             * @constructor
             * @param {northstar.research.FactorRevision.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FactorRevision = function (properties) {
                this.annotations = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FactorRevision annotations.
             * @member {Array.<northstar.research.Annotation.$Properties>} annotations
             * @memberof northstar.research.FactorRevision
             * @instance
             */
            FactorRevision.prototype.annotations = $util.emptyArray;

            /**
             * FactorRevision binding.
             * @member {northstar.research.FactorBinding.$Properties|null|undefined} binding
             * @memberof northstar.research.FactorRevision
             * @instance
             */
            FactorRevision.prototype.binding = null;

            /**
             * FactorRevision revision_id.
             * @member {string|null|undefined} revision_id
             * @memberof northstar.research.FactorRevision
             * @instance
             */
            FactorRevision.prototype.revision_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * FactorRevision _binding.
             * @member {"binding"|undefined} _binding
             * @memberof northstar.research.FactorRevision
             * @instance
             */
            $Object.defineProperty(FactorRevision.prototype, "_binding", {
                get: $util.oneOfGetter($oneOfFields = ["binding"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorRevision _revision_id.
             * @member {"revision_id"|undefined} _revision_id
             * @memberof northstar.research.FactorRevision
             * @instance
             */
            $Object.defineProperty(FactorRevision.prototype, "_revision_id", {
                get: $util.oneOfGetter($oneOfFields = ["revision_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new FactorRevision instance using the specified properties.
             * @function create
             * @memberof northstar.research.FactorRevision
             * @static
             * @param {northstar.research.FactorRevision.$Properties=} [properties] Properties to set
             * @returns {northstar.research.FactorRevision} FactorRevision instance
             * @type {{
             *   (properties: northstar.research.FactorRevision.$Shape): northstar.research.FactorRevision & northstar.research.FactorRevision.$Shape;
             *   (properties?: northstar.research.FactorRevision.$Properties): northstar.research.FactorRevision;
             * }}
             */
            FactorRevision.create = function(properties) {
                return new FactorRevision(properties);
            };

            /**
             * Encodes the specified FactorRevision message. Does not implicitly {@link northstar.research.FactorRevision.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.FactorRevision
             * @static
             * @param {northstar.research.FactorRevision.$Properties} message FactorRevision message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FactorRevision.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.annotations != null && message.annotations.length)
                    for (let i = 0; i < message.annotations.length; ++i)
                        $root.northstar.research.Annotation.encode(message.annotations[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.binding != null && $Object.hasOwnProperty.call(message, "binding"))
                    $root.northstar.research.FactorBinding.encode(message.binding, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.revision_id != null && $Object.hasOwnProperty.call(message, "revision_id"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.revision_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a FactorRevision message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.FactorRevision
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.FactorRevision & northstar.research.FactorRevision.$Shape} FactorRevision
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FactorRevision.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.FactorRevision();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.annotations && message.annotations.length))
                                message.annotations = [];
                            message.annotations.push($root.northstar.research.Annotation.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.binding = $root.northstar.research.FactorBinding.decode(reader, reader.uint32(), $undefined, _depth + 1, message.binding);
                            message._binding = "binding";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.revision_id = reader.stringVerify();
                            message._revision_id = "revision_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a FactorRevision message.
             * @function verify
             * @memberof northstar.research.FactorRevision
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FactorRevision.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.annotations != null && $Object.hasOwnProperty.call(message, "annotations")) {
                    if (!$Array.isArray(message.annotations))
                        return "annotations: array expected";
                    for (let i = 0; i < message.annotations.length; ++i) {
                        let error = $root.northstar.research.Annotation.verify(message.annotations[i], _depth + 1);
                        if (error)
                            return "annotations." + error;
                    }
                }
                if (message.binding != null && $Object.hasOwnProperty.call(message, "binding")) {
                    properties._binding = 1;
                    {
                        let error = $root.northstar.research.FactorBinding.verify(message.binding, _depth + 1);
                        if (error)
                            return "binding." + error;
                    }
                }
                if (message.revision_id != null && $Object.hasOwnProperty.call(message, "revision_id")) {
                    properties._revision_id = 1;
                    if (!$util.isString(message.revision_id))
                        return "revision_id: string expected";
                }
                return null;
            };

            /**
             * Creates a FactorRevision message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.FactorRevision
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.FactorRevision} FactorRevision
             */
            FactorRevision.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.FactorRevision)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.FactorRevision: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.FactorRevision();
                if (object.annotations) {
                    if (!$Array.isArray(object.annotations))
                        throw $TypeError(".northstar.research.FactorRevision.annotations: array expected");
                    message.annotations = $Array(object.annotations.length);
                    for (let i = 0; i < object.annotations.length; ++i) {
                        if (!$util.isObject(object.annotations[i]))
                            throw $TypeError(".northstar.research.FactorRevision.annotations: object expected");
                        message.annotations[i] = $root.northstar.research.Annotation.fromObject(object.annotations[i], _depth + 1);
                    }
                }
                if (object.binding != null) {
                    if (!$util.isObject(object.binding))
                        throw $TypeError(".northstar.research.FactorRevision.binding: object expected");
                    message.binding = $root.northstar.research.FactorBinding.fromObject(object.binding, _depth + 1);
                }
                if (object.revision_id != null)
                    message.revision_id = $String(object.revision_id);
                return message;
            };

            /**
             * Creates a plain object from a FactorRevision message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.FactorRevision
             * @static
             * @param {northstar.research.FactorRevision} message FactorRevision
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FactorRevision.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.annotations = [];
                if (message.annotations && message.annotations.length) {
                    object.annotations = $Array(message.annotations.length);
                    for (let j = 0; j < message.annotations.length; ++j)
                        object.annotations[j] = $root.northstar.research.Annotation.toObject(message.annotations[j], options, _depth + 1);
                }
                if (message.binding != null && $Object.hasOwnProperty.call(message, "binding")) {
                    object.binding = $root.northstar.research.FactorBinding.toObject(message.binding, options, _depth + 1);
                    if (options.oneofs)
                        object._binding = "binding";
                }
                if (message.revision_id != null && $Object.hasOwnProperty.call(message, "revision_id")) {
                    object.revision_id = message.revision_id;
                    if (options.oneofs)
                        object._revision_id = "revision_id";
                }
                return object;
            };

            /**
             * Converts this FactorRevision to JSON.
             * @function toJSON
             * @memberof northstar.research.FactorRevision
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FactorRevision.prototype.toJSON = function() {
                return FactorRevision.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FactorRevision
             * @function getTypeUrl
             * @memberof northstar.research.FactorRevision
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FactorRevision.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.FactorRevision";
            };

            return FactorRevision;
        })();

        research.FactorRevisionRequest = (function() {

            /**
             * Properties of a FactorRevisionRequest.
             * @typedef {Object} northstar.research.FactorRevisionRequest.$Properties
             * @property {string|null} [factor_id] FactorRevisionRequest factor_id
             * @property {google.protobuf.Struct.$Properties|null} [parameters] FactorRevisionRequest parameters
             * @property {"factor_id"} [_factor_id] FactorRevisionRequest _factor_id
             * @property {"parameters"} [_parameters] FactorRevisionRequest _parameters
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FactorRevisionRequest.
             * @memberof northstar.research
             * @interface IFactorRevisionRequest
             * @augments northstar.research.FactorRevisionRequest.$Properties
             * @deprecated Use northstar.research.FactorRevisionRequest.$Properties instead.
             */

            /**
             * Narrowed shape of a FactorRevisionRequest.
             * @typedef {{
             *   factor_id?: string|null;
             *   parameters?: google.protobuf.Struct.$Shape|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _factor_id?: undefined; factor_id?: null }|{ _factor_id?: "factor_id"; factor_id: string })
             * ) & (
             *   ({ _parameters?: undefined; parameters?: null }|{ _parameters?: "parameters"; parameters: google.protobuf.Struct.$Shape })
             * )} northstar.research.FactorRevisionRequest.$Shape
             */

            /**
             * Constructs a new FactorRevisionRequest.
             * @memberof northstar.research
             * @classdesc Represents a FactorRevisionRequest.
             * @constructor
             * @param {northstar.research.FactorRevisionRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FactorRevisionRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FactorRevisionRequest factor_id.
             * @member {string|null|undefined} factor_id
             * @memberof northstar.research.FactorRevisionRequest
             * @instance
             */
            FactorRevisionRequest.prototype.factor_id = null;

            /**
             * FactorRevisionRequest parameters.
             * @member {google.protobuf.Struct.$Properties|null|undefined} parameters
             * @memberof northstar.research.FactorRevisionRequest
             * @instance
             */
            FactorRevisionRequest.prototype.parameters = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * FactorRevisionRequest _factor_id.
             * @member {"factor_id"|undefined} _factor_id
             * @memberof northstar.research.FactorRevisionRequest
             * @instance
             */
            $Object.defineProperty(FactorRevisionRequest.prototype, "_factor_id", {
                get: $util.oneOfGetter($oneOfFields = ["factor_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorRevisionRequest _parameters.
             * @member {"parameters"|undefined} _parameters
             * @memberof northstar.research.FactorRevisionRequest
             * @instance
             */
            $Object.defineProperty(FactorRevisionRequest.prototype, "_parameters", {
                get: $util.oneOfGetter($oneOfFields = ["parameters"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new FactorRevisionRequest instance using the specified properties.
             * @function create
             * @memberof northstar.research.FactorRevisionRequest
             * @static
             * @param {northstar.research.FactorRevisionRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.research.FactorRevisionRequest} FactorRevisionRequest instance
             * @type {{
             *   (properties: northstar.research.FactorRevisionRequest.$Shape): northstar.research.FactorRevisionRequest & northstar.research.FactorRevisionRequest.$Shape;
             *   (properties?: northstar.research.FactorRevisionRequest.$Properties): northstar.research.FactorRevisionRequest;
             * }}
             */
            FactorRevisionRequest.create = function(properties) {
                return new FactorRevisionRequest(properties);
            };

            /**
             * Encodes the specified FactorRevisionRequest message. Does not implicitly {@link northstar.research.FactorRevisionRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.FactorRevisionRequest
             * @static
             * @param {northstar.research.FactorRevisionRequest.$Properties} message FactorRevisionRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FactorRevisionRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.factor_id != null && $Object.hasOwnProperty.call(message, "factor_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.factor_id);
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters"))
                    $root.google.protobuf.Struct.encode(message.parameters, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a FactorRevisionRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.FactorRevisionRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.FactorRevisionRequest & northstar.research.FactorRevisionRequest.$Shape} FactorRevisionRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FactorRevisionRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.FactorRevisionRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.factor_id = reader.stringVerify();
                            message._factor_id = "factor_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.parameters = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.parameters);
                            message._parameters = "parameters";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a FactorRevisionRequest message.
             * @function verify
             * @memberof northstar.research.FactorRevisionRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FactorRevisionRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.factor_id != null && $Object.hasOwnProperty.call(message, "factor_id")) {
                    properties._factor_id = 1;
                    if (!$util.isString(message.factor_id))
                        return "factor_id: string expected";
                }
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters")) {
                    properties._parameters = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.parameters, _depth + 1);
                        if (error)
                            return "parameters." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a FactorRevisionRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.FactorRevisionRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.FactorRevisionRequest} FactorRevisionRequest
             */
            FactorRevisionRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.FactorRevisionRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.FactorRevisionRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.FactorRevisionRequest();
                if (object.factor_id != null)
                    message.factor_id = $String(object.factor_id);
                if (object.parameters != null) {
                    if (!$util.isObject(object.parameters))
                        throw $TypeError(".northstar.research.FactorRevisionRequest.parameters: object expected");
                    message.parameters = $root.google.protobuf.Struct.fromObject(object.parameters, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a FactorRevisionRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.FactorRevisionRequest
             * @static
             * @param {northstar.research.FactorRevisionRequest} message FactorRevisionRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FactorRevisionRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.factor_id != null && $Object.hasOwnProperty.call(message, "factor_id")) {
                    object.factor_id = message.factor_id;
                    if (options.oneofs)
                        object._factor_id = "factor_id";
                }
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters")) {
                    object.parameters = $root.google.protobuf.Struct.toObject(message.parameters, options, _depth + 1);
                    if (options.oneofs)
                        object._parameters = "parameters";
                }
                return object;
            };

            /**
             * Converts this FactorRevisionRequest to JSON.
             * @function toJSON
             * @memberof northstar.research.FactorRevisionRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FactorRevisionRequest.prototype.toJSON = function() {
                return FactorRevisionRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FactorRevisionRequest
             * @function getTypeUrl
             * @memberof northstar.research.FactorRevisionRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FactorRevisionRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.FactorRevisionRequest";
            };

            return FactorRevisionRequest;
        })();

        research.FactorRun = (function() {

            /**
             * Properties of a FactorRun.
             * @typedef {Object} northstar.research.FactorRun.$Properties
             * @property {string|null} [attempt_id] FactorRun attempt_id
             * @property {string|null} [code_revision] FactorRun code_revision
             * @property {string|null} [completed_at] FactorRun completed_at
             * @property {string|null} [created_at] FactorRun created_at
             * @property {string|null} [error] FactorRun error
             * @property {northstar.research.FactorResult.$Properties|null} [result] FactorRun result
             * @property {string|null} [revision_id] FactorRun revision_id
             * @property {string|null} [snapshot_id] FactorRun snapshot_id
             * @property {string|null} [status] FactorRun status
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] FactorRun evidence_fields
             * @property {Array.<string>|null} [null_fields] FactorRun null_fields
             * @property {"attempt_id"} [_attempt_id] FactorRun _attempt_id
             * @property {"code_revision"} [_code_revision] FactorRun _code_revision
             * @property {"completed_at"} [_completed_at] FactorRun _completed_at
             * @property {"created_at"} [_created_at] FactorRun _created_at
             * @property {"error"} [_error] FactorRun _error
             * @property {"result"} [_result] FactorRun _result
             * @property {"revision_id"} [_revision_id] FactorRun _revision_id
             * @property {"snapshot_id"} [_snapshot_id] FactorRun _snapshot_id
             * @property {"status"} [_status] FactorRun _status
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FactorRun.
             * @memberof northstar.research
             * @interface IFactorRun
             * @augments northstar.research.FactorRun.$Properties
             * @deprecated Use northstar.research.FactorRun.$Properties instead.
             */

            /**
             * Narrowed shape of a FactorRun.
             * @typedef {{
             *   attempt_id?: string|null;
             *   code_revision?: string|null;
             *   completed_at?: string|null;
             *   created_at?: string|null;
             *   error?: string|null;
             *   result?: northstar.research.FactorResult.$Shape|null;
             *   revision_id?: string|null;
             *   snapshot_id?: string|null;
             *   status?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   null_fields?: Array.<string>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _attempt_id?: undefined; attempt_id?: null }|{ _attempt_id?: "attempt_id"; attempt_id: string })
             * ) & (
             *   ({ _code_revision?: undefined; code_revision?: null }|{ _code_revision?: "code_revision"; code_revision: string })
             * ) & (
             *   ({ _completed_at?: undefined; completed_at?: null }|{ _completed_at?: "completed_at"; completed_at: string })
             * ) & (
             *   ({ _created_at?: undefined; created_at?: null }|{ _created_at?: "created_at"; created_at: string })
             * ) & (
             *   ({ _error?: undefined; error?: null }|{ _error?: "error"; error: string })
             * ) & (
             *   ({ _result?: undefined; result?: null }|{ _result?: "result"; result: northstar.research.FactorResult.$Shape })
             * ) & (
             *   ({ _revision_id?: undefined; revision_id?: null }|{ _revision_id?: "revision_id"; revision_id: string })
             * ) & (
             *   ({ _snapshot_id?: undefined; snapshot_id?: null }|{ _snapshot_id?: "snapshot_id"; snapshot_id: string })
             * ) & (
             *   ({ _status?: undefined; status?: null }|{ _status?: "status"; status: string })
             * )} northstar.research.FactorRun.$Shape
             */

            /**
             * Constructs a new FactorRun.
             * @memberof northstar.research
             * @classdesc Represents a FactorRun.
             * @constructor
             * @param {northstar.research.FactorRun.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FactorRun = function (properties) {
                this.evidence_fields = {};
                this.null_fields = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FactorRun attempt_id.
             * @member {string|null|undefined} attempt_id
             * @memberof northstar.research.FactorRun
             * @instance
             */
            FactorRun.prototype.attempt_id = null;

            /**
             * FactorRun code_revision.
             * @member {string|null|undefined} code_revision
             * @memberof northstar.research.FactorRun
             * @instance
             */
            FactorRun.prototype.code_revision = null;

            /**
             * FactorRun completed_at.
             * @member {string|null|undefined} completed_at
             * @memberof northstar.research.FactorRun
             * @instance
             */
            FactorRun.prototype.completed_at = null;

            /**
             * FactorRun created_at.
             * @member {string|null|undefined} created_at
             * @memberof northstar.research.FactorRun
             * @instance
             */
            FactorRun.prototype.created_at = null;

            /**
             * FactorRun error.
             * @member {string|null|undefined} error
             * @memberof northstar.research.FactorRun
             * @instance
             */
            FactorRun.prototype.error = null;

            /**
             * FactorRun result.
             * @member {northstar.research.FactorResult.$Properties|null|undefined} result
             * @memberof northstar.research.FactorRun
             * @instance
             */
            FactorRun.prototype.result = null;

            /**
             * FactorRun revision_id.
             * @member {string|null|undefined} revision_id
             * @memberof northstar.research.FactorRun
             * @instance
             */
            FactorRun.prototype.revision_id = null;

            /**
             * FactorRun snapshot_id.
             * @member {string|null|undefined} snapshot_id
             * @memberof northstar.research.FactorRun
             * @instance
             */
            FactorRun.prototype.snapshot_id = null;

            /**
             * FactorRun status.
             * @member {string|null|undefined} status
             * @memberof northstar.research.FactorRun
             * @instance
             */
            FactorRun.prototype.status = null;

            /**
             * FactorRun evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.research.FactorRun
             * @instance
             */
            FactorRun.prototype.evidence_fields = $util.emptyObject;

            /**
             * FactorRun null_fields.
             * @member {Array.<string>} null_fields
             * @memberof northstar.research.FactorRun
             * @instance
             */
            FactorRun.prototype.null_fields = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * FactorRun _attempt_id.
             * @member {"attempt_id"|undefined} _attempt_id
             * @memberof northstar.research.FactorRun
             * @instance
             */
            $Object.defineProperty(FactorRun.prototype, "_attempt_id", {
                get: $util.oneOfGetter($oneOfFields = ["attempt_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorRun _code_revision.
             * @member {"code_revision"|undefined} _code_revision
             * @memberof northstar.research.FactorRun
             * @instance
             */
            $Object.defineProperty(FactorRun.prototype, "_code_revision", {
                get: $util.oneOfGetter($oneOfFields = ["code_revision"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorRun _completed_at.
             * @member {"completed_at"|undefined} _completed_at
             * @memberof northstar.research.FactorRun
             * @instance
             */
            $Object.defineProperty(FactorRun.prototype, "_completed_at", {
                get: $util.oneOfGetter($oneOfFields = ["completed_at"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorRun _created_at.
             * @member {"created_at"|undefined} _created_at
             * @memberof northstar.research.FactorRun
             * @instance
             */
            $Object.defineProperty(FactorRun.prototype, "_created_at", {
                get: $util.oneOfGetter($oneOfFields = ["created_at"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorRun _error.
             * @member {"error"|undefined} _error
             * @memberof northstar.research.FactorRun
             * @instance
             */
            $Object.defineProperty(FactorRun.prototype, "_error", {
                get: $util.oneOfGetter($oneOfFields = ["error"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorRun _result.
             * @member {"result"|undefined} _result
             * @memberof northstar.research.FactorRun
             * @instance
             */
            $Object.defineProperty(FactorRun.prototype, "_result", {
                get: $util.oneOfGetter($oneOfFields = ["result"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorRun _revision_id.
             * @member {"revision_id"|undefined} _revision_id
             * @memberof northstar.research.FactorRun
             * @instance
             */
            $Object.defineProperty(FactorRun.prototype, "_revision_id", {
                get: $util.oneOfGetter($oneOfFields = ["revision_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorRun _snapshot_id.
             * @member {"snapshot_id"|undefined} _snapshot_id
             * @memberof northstar.research.FactorRun
             * @instance
             */
            $Object.defineProperty(FactorRun.prototype, "_snapshot_id", {
                get: $util.oneOfGetter($oneOfFields = ["snapshot_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorRun _status.
             * @member {"status"|undefined} _status
             * @memberof northstar.research.FactorRun
             * @instance
             */
            $Object.defineProperty(FactorRun.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new FactorRun instance using the specified properties.
             * @function create
             * @memberof northstar.research.FactorRun
             * @static
             * @param {northstar.research.FactorRun.$Properties=} [properties] Properties to set
             * @returns {northstar.research.FactorRun} FactorRun instance
             * @type {{
             *   (properties: northstar.research.FactorRun.$Shape): northstar.research.FactorRun & northstar.research.FactorRun.$Shape;
             *   (properties?: northstar.research.FactorRun.$Properties): northstar.research.FactorRun;
             * }}
             */
            FactorRun.create = function(properties) {
                return new FactorRun(properties);
            };

            /**
             * Encodes the specified FactorRun message. Does not implicitly {@link northstar.research.FactorRun.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.FactorRun
             * @static
             * @param {northstar.research.FactorRun.$Properties} message FactorRun message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FactorRun.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.attempt_id != null && $Object.hasOwnProperty.call(message, "attempt_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.attempt_id);
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.code_revision);
                if (message.completed_at != null && $Object.hasOwnProperty.call(message, "completed_at"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.completed_at);
                if (message.created_at != null && $Object.hasOwnProperty.call(message, "created_at"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.created_at);
                if (message.error != null && $Object.hasOwnProperty.call(message, "error"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.error);
                if (message.result != null && $Object.hasOwnProperty.call(message, "result"))
                    $root.northstar.research.FactorResult.encode(message.result, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
                if (message.revision_id != null && $Object.hasOwnProperty.call(message, "revision_id"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.revision_id);
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.snapshot_id);
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.status);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.null_fields != null && message.null_fields.length)
                    for (let i = 0; i < message.null_fields.length; ++i)
                        writer.uint32(/* id 2046, wireType 2 =*/16370).string(message.null_fields[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a FactorRun message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.FactorRun
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.FactorRun & northstar.research.FactorRun.$Shape} FactorRun
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FactorRun.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.FactorRun();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.attempt_id = reader.stringVerify();
                            message._attempt_id = "attempt_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.code_revision = reader.stringVerify();
                            message._code_revision = "code_revision";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.completed_at = reader.stringVerify();
                            message._completed_at = "completed_at";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.created_at = reader.stringVerify();
                            message._created_at = "created_at";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.error = reader.stringVerify();
                            message._error = "error";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.result = $root.northstar.research.FactorResult.decode(reader, reader.uint32(), $undefined, _depth + 1, message.result);
                            message._result = "result";
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            message.revision_id = reader.stringVerify();
                            message._revision_id = "revision_id";
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            message.snapshot_id = reader.stringVerify();
                            message._snapshot_id = "snapshot_id";
                            continue;
                        }
                    case 9: {
                            if (wireType !== 2)
                                break;
                            message.status = reader.stringVerify();
                            message._status = "status";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    case 2046: {
                            if (wireType !== 2)
                                break;
                            if (!(message.null_fields && message.null_fields.length))
                                message.null_fields = [];
                            message.null_fields.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a FactorRun message.
             * @function verify
             * @memberof northstar.research.FactorRun
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FactorRun.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.attempt_id != null && $Object.hasOwnProperty.call(message, "attempt_id")) {
                    properties._attempt_id = 1;
                    if (!$util.isString(message.attempt_id))
                        return "attempt_id: string expected";
                }
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision")) {
                    properties._code_revision = 1;
                    if (!$util.isString(message.code_revision))
                        return "code_revision: string expected";
                }
                if (message.completed_at != null && $Object.hasOwnProperty.call(message, "completed_at")) {
                    properties._completed_at = 1;
                    if (!$util.isString(message.completed_at))
                        return "completed_at: string expected";
                }
                if (message.created_at != null && $Object.hasOwnProperty.call(message, "created_at")) {
                    properties._created_at = 1;
                    if (!$util.isString(message.created_at))
                        return "created_at: string expected";
                }
                if (message.error != null && $Object.hasOwnProperty.call(message, "error")) {
                    properties._error = 1;
                    if (!$util.isString(message.error))
                        return "error: string expected";
                }
                if (message.result != null && $Object.hasOwnProperty.call(message, "result")) {
                    properties._result = 1;
                    {
                        let error = $root.northstar.research.FactorResult.verify(message.result, _depth + 1);
                        if (error)
                            return "result." + error;
                    }
                }
                if (message.revision_id != null && $Object.hasOwnProperty.call(message, "revision_id")) {
                    properties._revision_id = 1;
                    if (!$util.isString(message.revision_id))
                        return "revision_id: string expected";
                }
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id")) {
                    properties._snapshot_id = 1;
                    if (!$util.isString(message.snapshot_id))
                        return "snapshot_id: string expected";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (!$util.isString(message.status))
                        return "status: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                if (message.null_fields != null && $Object.hasOwnProperty.call(message, "null_fields")) {
                    if (!$Array.isArray(message.null_fields))
                        return "null_fields: array expected";
                    for (let i = 0; i < message.null_fields.length; ++i)
                        if (!$util.isString(message.null_fields[i]))
                            return "null_fields: string[] expected";
                }
                return null;
            };

            /**
             * Creates a FactorRun message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.FactorRun
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.FactorRun} FactorRun
             */
            FactorRun.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.FactorRun)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.FactorRun: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.FactorRun();
                if (object.attempt_id != null)
                    message.attempt_id = $String(object.attempt_id);
                if (object.code_revision != null)
                    message.code_revision = $String(object.code_revision);
                if (object.completed_at != null)
                    message.completed_at = $String(object.completed_at);
                if (object.created_at != null)
                    message.created_at = $String(object.created_at);
                if (object.error != null)
                    message.error = $String(object.error);
                if (object.result != null) {
                    if (!$util.isObject(object.result))
                        throw $TypeError(".northstar.research.FactorRun.result: object expected");
                    message.result = $root.northstar.research.FactorResult.fromObject(object.result, _depth + 1);
                }
                if (object.revision_id != null)
                    message.revision_id = $String(object.revision_id);
                if (object.snapshot_id != null)
                    message.snapshot_id = $String(object.snapshot_id);
                if (object.status != null)
                    message.status = $String(object.status);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.research.FactorRun.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.research.FactorRun.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                if (object.null_fields) {
                    if (!$Array.isArray(object.null_fields))
                        throw $TypeError(".northstar.research.FactorRun.null_fields: array expected");
                    message.null_fields = $Array(object.null_fields.length);
                    for (let i = 0; i < object.null_fields.length; ++i)
                        message.null_fields[i] = $String(object.null_fields[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a FactorRun message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.FactorRun
             * @static
             * @param {northstar.research.FactorRun} message FactorRun
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FactorRun.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.null_fields = [];
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.attempt_id != null && $Object.hasOwnProperty.call(message, "attempt_id")) {
                    object.attempt_id = message.attempt_id;
                    if (options.oneofs)
                        object._attempt_id = "attempt_id";
                }
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision")) {
                    object.code_revision = message.code_revision;
                    if (options.oneofs)
                        object._code_revision = "code_revision";
                }
                if (message.completed_at != null && $Object.hasOwnProperty.call(message, "completed_at")) {
                    object.completed_at = message.completed_at;
                    if (options.oneofs)
                        object._completed_at = "completed_at";
                }
                if (message.created_at != null && $Object.hasOwnProperty.call(message, "created_at")) {
                    object.created_at = message.created_at;
                    if (options.oneofs)
                        object._created_at = "created_at";
                }
                if (message.error != null && $Object.hasOwnProperty.call(message, "error")) {
                    object.error = message.error;
                    if (options.oneofs)
                        object._error = "error";
                }
                if (message.result != null && $Object.hasOwnProperty.call(message, "result")) {
                    object.result = $root.northstar.research.FactorResult.toObject(message.result, options, _depth + 1);
                    if (options.oneofs)
                        object._result = "result";
                }
                if (message.revision_id != null && $Object.hasOwnProperty.call(message, "revision_id")) {
                    object.revision_id = message.revision_id;
                    if (options.oneofs)
                        object._revision_id = "revision_id";
                }
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id")) {
                    object.snapshot_id = message.snapshot_id;
                    if (options.oneofs)
                        object._snapshot_id = "snapshot_id";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    object.status = message.status;
                    if (options.oneofs)
                        object._status = "status";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                if (message.null_fields && message.null_fields.length) {
                    object.null_fields = $Array(message.null_fields.length);
                    for (let j = 0; j < message.null_fields.length; ++j)
                        object.null_fields[j] = message.null_fields[j];
                }
                return object;
            };

            /**
             * Converts this FactorRun to JSON.
             * @function toJSON
             * @memberof northstar.research.FactorRun
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FactorRun.prototype.toJSON = function() {
                return FactorRun.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FactorRun
             * @function getTypeUrl
             * @memberof northstar.research.FactorRun
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FactorRun.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.FactorRun";
            };

            return FactorRun;
        })();

        research.FactorRunRequest = (function() {

            /**
             * Properties of a FactorRunRequest.
             * @typedef {Object} northstar.research.FactorRunRequest.$Properties
             * @property {string|null} [revision_id] FactorRunRequest revision_id
             * @property {string|null} [snapshot_id] FactorRunRequest snapshot_id
             * @property {"revision_id"} [_revision_id] FactorRunRequest _revision_id
             * @property {"snapshot_id"} [_snapshot_id] FactorRunRequest _snapshot_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FactorRunRequest.
             * @memberof northstar.research
             * @interface IFactorRunRequest
             * @augments northstar.research.FactorRunRequest.$Properties
             * @deprecated Use northstar.research.FactorRunRequest.$Properties instead.
             */

            /**
             * Narrowed shape of a FactorRunRequest.
             * @typedef {{
             *   revision_id?: string|null;
             *   snapshot_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _revision_id?: undefined; revision_id?: null }|{ _revision_id?: "revision_id"; revision_id: string })
             * ) & (
             *   ({ _snapshot_id?: undefined; snapshot_id?: null }|{ _snapshot_id?: "snapshot_id"; snapshot_id: string })
             * )} northstar.research.FactorRunRequest.$Shape
             */

            /**
             * Constructs a new FactorRunRequest.
             * @memberof northstar.research
             * @classdesc Represents a FactorRunRequest.
             * @constructor
             * @param {northstar.research.FactorRunRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FactorRunRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FactorRunRequest revision_id.
             * @member {string|null|undefined} revision_id
             * @memberof northstar.research.FactorRunRequest
             * @instance
             */
            FactorRunRequest.prototype.revision_id = null;

            /**
             * FactorRunRequest snapshot_id.
             * @member {string|null|undefined} snapshot_id
             * @memberof northstar.research.FactorRunRequest
             * @instance
             */
            FactorRunRequest.prototype.snapshot_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * FactorRunRequest _revision_id.
             * @member {"revision_id"|undefined} _revision_id
             * @memberof northstar.research.FactorRunRequest
             * @instance
             */
            $Object.defineProperty(FactorRunRequest.prototype, "_revision_id", {
                get: $util.oneOfGetter($oneOfFields = ["revision_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorRunRequest _snapshot_id.
             * @member {"snapshot_id"|undefined} _snapshot_id
             * @memberof northstar.research.FactorRunRequest
             * @instance
             */
            $Object.defineProperty(FactorRunRequest.prototype, "_snapshot_id", {
                get: $util.oneOfGetter($oneOfFields = ["snapshot_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new FactorRunRequest instance using the specified properties.
             * @function create
             * @memberof northstar.research.FactorRunRequest
             * @static
             * @param {northstar.research.FactorRunRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.research.FactorRunRequest} FactorRunRequest instance
             * @type {{
             *   (properties: northstar.research.FactorRunRequest.$Shape): northstar.research.FactorRunRequest & northstar.research.FactorRunRequest.$Shape;
             *   (properties?: northstar.research.FactorRunRequest.$Properties): northstar.research.FactorRunRequest;
             * }}
             */
            FactorRunRequest.create = function(properties) {
                return new FactorRunRequest(properties);
            };

            /**
             * Encodes the specified FactorRunRequest message. Does not implicitly {@link northstar.research.FactorRunRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.FactorRunRequest
             * @static
             * @param {northstar.research.FactorRunRequest.$Properties} message FactorRunRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FactorRunRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.revision_id != null && $Object.hasOwnProperty.call(message, "revision_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.revision_id);
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.snapshot_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a FactorRunRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.FactorRunRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.FactorRunRequest & northstar.research.FactorRunRequest.$Shape} FactorRunRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FactorRunRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.FactorRunRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.revision_id = reader.stringVerify();
                            message._revision_id = "revision_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.snapshot_id = reader.stringVerify();
                            message._snapshot_id = "snapshot_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a FactorRunRequest message.
             * @function verify
             * @memberof northstar.research.FactorRunRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FactorRunRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.revision_id != null && $Object.hasOwnProperty.call(message, "revision_id")) {
                    properties._revision_id = 1;
                    if (!$util.isString(message.revision_id))
                        return "revision_id: string expected";
                }
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id")) {
                    properties._snapshot_id = 1;
                    if (!$util.isString(message.snapshot_id))
                        return "snapshot_id: string expected";
                }
                return null;
            };

            /**
             * Creates a FactorRunRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.FactorRunRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.FactorRunRequest} FactorRunRequest
             */
            FactorRunRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.FactorRunRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.FactorRunRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.FactorRunRequest();
                if (object.revision_id != null)
                    message.revision_id = $String(object.revision_id);
                if (object.snapshot_id != null)
                    message.snapshot_id = $String(object.snapshot_id);
                return message;
            };

            /**
             * Creates a plain object from a FactorRunRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.FactorRunRequest
             * @static
             * @param {northstar.research.FactorRunRequest} message FactorRunRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FactorRunRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.revision_id != null && $Object.hasOwnProperty.call(message, "revision_id")) {
                    object.revision_id = message.revision_id;
                    if (options.oneofs)
                        object._revision_id = "revision_id";
                }
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id")) {
                    object.snapshot_id = message.snapshot_id;
                    if (options.oneofs)
                        object._snapshot_id = "snapshot_id";
                }
                return object;
            };

            /**
             * Converts this FactorRunRequest to JSON.
             * @function toJSON
             * @memberof northstar.research.FactorRunRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FactorRunRequest.prototype.toJSON = function() {
                return FactorRunRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FactorRunRequest
             * @function getTypeUrl
             * @memberof northstar.research.FactorRunRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FactorRunRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.FactorRunRequest";
            };

            return FactorRunRequest;
        })();

        research.FactorSummary = (function() {

            /**
             * Properties of a FactorSummary.
             * @typedef {Object} northstar.research.FactorSummary.$Properties
             * @property {Array.<string>|null} [capabilities] FactorSummary capabilities
             * @property {string|null} [category] FactorSummary category
             * @property {string|null} [description] FactorSummary description
             * @property {string|null} [factor_id] FactorSummary factor_id
             * @property {string|null} [limitations] FactorSummary limitations
             * @property {string|null} [name] FactorSummary name
             * @property {"category"} [_category] FactorSummary _category
             * @property {"description"} [_description] FactorSummary _description
             * @property {"factor_id"} [_factor_id] FactorSummary _factor_id
             * @property {"limitations"} [_limitations] FactorSummary _limitations
             * @property {"name"} [_name] FactorSummary _name
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FactorSummary.
             * @memberof northstar.research
             * @interface IFactorSummary
             * @augments northstar.research.FactorSummary.$Properties
             * @deprecated Use northstar.research.FactorSummary.$Properties instead.
             */

            /**
             * Narrowed shape of a FactorSummary.
             * @typedef {{
             *   capabilities?: Array.<string>|null;
             *   category?: string|null;
             *   description?: string|null;
             *   factor_id?: string|null;
             *   limitations?: string|null;
             *   name?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _category?: undefined; category?: null }|{ _category?: "category"; category: string })
             * ) & (
             *   ({ _description?: undefined; description?: null }|{ _description?: "description"; description: string })
             * ) & (
             *   ({ _factor_id?: undefined; factor_id?: null }|{ _factor_id?: "factor_id"; factor_id: string })
             * ) & (
             *   ({ _limitations?: undefined; limitations?: null }|{ _limitations?: "limitations"; limitations: string })
             * ) & (
             *   ({ _name?: undefined; name?: null }|{ _name?: "name"; name: string })
             * )} northstar.research.FactorSummary.$Shape
             */

            /**
             * Constructs a new FactorSummary.
             * @memberof northstar.research
             * @classdesc Represents a FactorSummary.
             * @constructor
             * @param {northstar.research.FactorSummary.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FactorSummary = function (properties) {
                this.capabilities = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FactorSummary capabilities.
             * @member {Array.<string>} capabilities
             * @memberof northstar.research.FactorSummary
             * @instance
             */
            FactorSummary.prototype.capabilities = $util.emptyArray;

            /**
             * FactorSummary category.
             * @member {string|null|undefined} category
             * @memberof northstar.research.FactorSummary
             * @instance
             */
            FactorSummary.prototype.category = null;

            /**
             * FactorSummary description.
             * @member {string|null|undefined} description
             * @memberof northstar.research.FactorSummary
             * @instance
             */
            FactorSummary.prototype.description = null;

            /**
             * FactorSummary factor_id.
             * @member {string|null|undefined} factor_id
             * @memberof northstar.research.FactorSummary
             * @instance
             */
            FactorSummary.prototype.factor_id = null;

            /**
             * FactorSummary limitations.
             * @member {string|null|undefined} limitations
             * @memberof northstar.research.FactorSummary
             * @instance
             */
            FactorSummary.prototype.limitations = null;

            /**
             * FactorSummary name.
             * @member {string|null|undefined} name
             * @memberof northstar.research.FactorSummary
             * @instance
             */
            FactorSummary.prototype.name = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * FactorSummary _category.
             * @member {"category"|undefined} _category
             * @memberof northstar.research.FactorSummary
             * @instance
             */
            $Object.defineProperty(FactorSummary.prototype, "_category", {
                get: $util.oneOfGetter($oneOfFields = ["category"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorSummary _description.
             * @member {"description"|undefined} _description
             * @memberof northstar.research.FactorSummary
             * @instance
             */
            $Object.defineProperty(FactorSummary.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorSummary _factor_id.
             * @member {"factor_id"|undefined} _factor_id
             * @memberof northstar.research.FactorSummary
             * @instance
             */
            $Object.defineProperty(FactorSummary.prototype, "_factor_id", {
                get: $util.oneOfGetter($oneOfFields = ["factor_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorSummary _limitations.
             * @member {"limitations"|undefined} _limitations
             * @memberof northstar.research.FactorSummary
             * @instance
             */
            $Object.defineProperty(FactorSummary.prototype, "_limitations", {
                get: $util.oneOfGetter($oneOfFields = ["limitations"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorSummary _name.
             * @member {"name"|undefined} _name
             * @memberof northstar.research.FactorSummary
             * @instance
             */
            $Object.defineProperty(FactorSummary.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new FactorSummary instance using the specified properties.
             * @function create
             * @memberof northstar.research.FactorSummary
             * @static
             * @param {northstar.research.FactorSummary.$Properties=} [properties] Properties to set
             * @returns {northstar.research.FactorSummary} FactorSummary instance
             * @type {{
             *   (properties: northstar.research.FactorSummary.$Shape): northstar.research.FactorSummary & northstar.research.FactorSummary.$Shape;
             *   (properties?: northstar.research.FactorSummary.$Properties): northstar.research.FactorSummary;
             * }}
             */
            FactorSummary.create = function(properties) {
                return new FactorSummary(properties);
            };

            /**
             * Encodes the specified FactorSummary message. Does not implicitly {@link northstar.research.FactorSummary.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.FactorSummary
             * @static
             * @param {northstar.research.FactorSummary.$Properties} message FactorSummary message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FactorSummary.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.capabilities != null && message.capabilities.length)
                    for (let i = 0; i < message.capabilities.length; ++i)
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.capabilities[i]);
                if (message.category != null && $Object.hasOwnProperty.call(message, "category"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.category);
                if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.description);
                if (message.factor_id != null && $Object.hasOwnProperty.call(message, "factor_id"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.factor_id);
                if (message.limitations != null && $Object.hasOwnProperty.call(message, "limitations"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.limitations);
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.name);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a FactorSummary message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.FactorSummary
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.FactorSummary & northstar.research.FactorSummary.$Shape} FactorSummary
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FactorSummary.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.FactorSummary();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.capabilities && message.capabilities.length))
                                message.capabilities = [];
                            message.capabilities.push(reader.stringVerify());
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.category = reader.stringVerify();
                            message._category = "category";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.description = reader.stringVerify();
                            message._description = "description";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.factor_id = reader.stringVerify();
                            message._factor_id = "factor_id";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.limitations = reader.stringVerify();
                            message._limitations = "limitations";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a FactorSummary message.
             * @function verify
             * @memberof northstar.research.FactorSummary
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FactorSummary.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.capabilities != null && $Object.hasOwnProperty.call(message, "capabilities")) {
                    if (!$Array.isArray(message.capabilities))
                        return "capabilities: array expected";
                    for (let i = 0; i < message.capabilities.length; ++i)
                        if (!$util.isString(message.capabilities[i]))
                            return "capabilities: string[] expected";
                }
                if (message.category != null && $Object.hasOwnProperty.call(message, "category")) {
                    properties._category = 1;
                    if (!$util.isString(message.category))
                        return "category: string expected";
                }
                if (message.description != null && $Object.hasOwnProperty.call(message, "description")) {
                    properties._description = 1;
                    if (!$util.isString(message.description))
                        return "description: string expected";
                }
                if (message.factor_id != null && $Object.hasOwnProperty.call(message, "factor_id")) {
                    properties._factor_id = 1;
                    if (!$util.isString(message.factor_id))
                        return "factor_id: string expected";
                }
                if (message.limitations != null && $Object.hasOwnProperty.call(message, "limitations")) {
                    properties._limitations = 1;
                    if (!$util.isString(message.limitations))
                        return "limitations: string expected";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                return null;
            };

            /**
             * Creates a FactorSummary message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.FactorSummary
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.FactorSummary} FactorSummary
             */
            FactorSummary.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.FactorSummary)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.FactorSummary: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.FactorSummary();
                if (object.capabilities) {
                    if (!$Array.isArray(object.capabilities))
                        throw $TypeError(".northstar.research.FactorSummary.capabilities: array expected");
                    message.capabilities = $Array(object.capabilities.length);
                    for (let i = 0; i < object.capabilities.length; ++i)
                        message.capabilities[i] = $String(object.capabilities[i]);
                }
                if (object.category != null)
                    message.category = $String(object.category);
                if (object.description != null)
                    message.description = $String(object.description);
                if (object.factor_id != null)
                    message.factor_id = $String(object.factor_id);
                if (object.limitations != null)
                    message.limitations = $String(object.limitations);
                if (object.name != null)
                    message.name = $String(object.name);
                return message;
            };

            /**
             * Creates a plain object from a FactorSummary message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.FactorSummary
             * @static
             * @param {northstar.research.FactorSummary} message FactorSummary
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FactorSummary.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.capabilities = [];
                if (message.capabilities && message.capabilities.length) {
                    object.capabilities = $Array(message.capabilities.length);
                    for (let j = 0; j < message.capabilities.length; ++j)
                        object.capabilities[j] = message.capabilities[j];
                }
                if (message.category != null && $Object.hasOwnProperty.call(message, "category")) {
                    object.category = message.category;
                    if (options.oneofs)
                        object._category = "category";
                }
                if (message.description != null && $Object.hasOwnProperty.call(message, "description")) {
                    object.description = message.description;
                    if (options.oneofs)
                        object._description = "description";
                }
                if (message.factor_id != null && $Object.hasOwnProperty.call(message, "factor_id")) {
                    object.factor_id = message.factor_id;
                    if (options.oneofs)
                        object._factor_id = "factor_id";
                }
                if (message.limitations != null && $Object.hasOwnProperty.call(message, "limitations")) {
                    object.limitations = message.limitations;
                    if (options.oneofs)
                        object._limitations = "limitations";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    object.name = message.name;
                    if (options.oneofs)
                        object._name = "name";
                }
                return object;
            };

            /**
             * Converts this FactorSummary to JSON.
             * @function toJSON
             * @memberof northstar.research.FactorSummary
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FactorSummary.prototype.toJSON = function() {
                return FactorSummary.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FactorSummary
             * @function getTypeUrl
             * @memberof northstar.research.FactorSummary
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FactorSummary.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.FactorSummary";
            };

            return FactorSummary;
        })();

        research.FactorValue = (function() {

            /**
             * Properties of a FactorValue.
             * @typedef {Object} northstar.research.FactorValue.$Properties
             * @property {string|null} [at] FactorValue at
             * @property {string|null} [observation_id] FactorValue observation_id
             * @property {string|null} [reason] FactorValue reason
             * @property {string|null} [status] FactorValue status
             * @property {string|null} [value] FactorValue value
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] FactorValue evidence_fields
             * @property {Array.<string>|null} [null_fields] FactorValue null_fields
             * @property {"at"} [_at] FactorValue _at
             * @property {"observation_id"} [_observation_id] FactorValue _observation_id
             * @property {"reason"} [_reason] FactorValue _reason
             * @property {"status"} [_status] FactorValue _status
             * @property {"value"} [_value] FactorValue _value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FactorValue.
             * @memberof northstar.research
             * @interface IFactorValue
             * @augments northstar.research.FactorValue.$Properties
             * @deprecated Use northstar.research.FactorValue.$Properties instead.
             */

            /**
             * Narrowed shape of a FactorValue.
             * @typedef {{
             *   at?: string|null;
             *   observation_id?: string|null;
             *   reason?: string|null;
             *   status?: string|null;
             *   value?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   null_fields?: Array.<string>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _at?: undefined; at?: null }|{ _at?: "at"; at: string })
             * ) & (
             *   ({ _observation_id?: undefined; observation_id?: null }|{ _observation_id?: "observation_id"; observation_id: string })
             * ) & (
             *   ({ _reason?: undefined; reason?: null }|{ _reason?: "reason"; reason: string })
             * ) & (
             *   ({ _status?: undefined; status?: null }|{ _status?: "status"; status: string })
             * ) & (
             *   ({ _value?: undefined; value?: null }|{ _value?: "value"; value: string })
             * )} northstar.research.FactorValue.$Shape
             */

            /**
             * Constructs a new FactorValue.
             * @memberof northstar.research
             * @classdesc Represents a FactorValue.
             * @constructor
             * @param {northstar.research.FactorValue.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FactorValue = function (properties) {
                this.evidence_fields = {};
                this.null_fields = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FactorValue at.
             * @member {string|null|undefined} at
             * @memberof northstar.research.FactorValue
             * @instance
             */
            FactorValue.prototype.at = null;

            /**
             * FactorValue observation_id.
             * @member {string|null|undefined} observation_id
             * @memberof northstar.research.FactorValue
             * @instance
             */
            FactorValue.prototype.observation_id = null;

            /**
             * FactorValue reason.
             * @member {string|null|undefined} reason
             * @memberof northstar.research.FactorValue
             * @instance
             */
            FactorValue.prototype.reason = null;

            /**
             * FactorValue status.
             * @member {string|null|undefined} status
             * @memberof northstar.research.FactorValue
             * @instance
             */
            FactorValue.prototype.status = null;

            /**
             * FactorValue value.
             * @member {string|null|undefined} value
             * @memberof northstar.research.FactorValue
             * @instance
             */
            FactorValue.prototype.value = null;

            /**
             * FactorValue evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.research.FactorValue
             * @instance
             */
            FactorValue.prototype.evidence_fields = $util.emptyObject;

            /**
             * FactorValue null_fields.
             * @member {Array.<string>} null_fields
             * @memberof northstar.research.FactorValue
             * @instance
             */
            FactorValue.prototype.null_fields = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * FactorValue _at.
             * @member {"at"|undefined} _at
             * @memberof northstar.research.FactorValue
             * @instance
             */
            $Object.defineProperty(FactorValue.prototype, "_at", {
                get: $util.oneOfGetter($oneOfFields = ["at"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorValue _observation_id.
             * @member {"observation_id"|undefined} _observation_id
             * @memberof northstar.research.FactorValue
             * @instance
             */
            $Object.defineProperty(FactorValue.prototype, "_observation_id", {
                get: $util.oneOfGetter($oneOfFields = ["observation_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorValue _reason.
             * @member {"reason"|undefined} _reason
             * @memberof northstar.research.FactorValue
             * @instance
             */
            $Object.defineProperty(FactorValue.prototype, "_reason", {
                get: $util.oneOfGetter($oneOfFields = ["reason"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorValue _status.
             * @member {"status"|undefined} _status
             * @memberof northstar.research.FactorValue
             * @instance
             */
            $Object.defineProperty(FactorValue.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FactorValue _value.
             * @member {"value"|undefined} _value
             * @memberof northstar.research.FactorValue
             * @instance
             */
            $Object.defineProperty(FactorValue.prototype, "_value", {
                get: $util.oneOfGetter($oneOfFields = ["value"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new FactorValue instance using the specified properties.
             * @function create
             * @memberof northstar.research.FactorValue
             * @static
             * @param {northstar.research.FactorValue.$Properties=} [properties] Properties to set
             * @returns {northstar.research.FactorValue} FactorValue instance
             * @type {{
             *   (properties: northstar.research.FactorValue.$Shape): northstar.research.FactorValue & northstar.research.FactorValue.$Shape;
             *   (properties?: northstar.research.FactorValue.$Properties): northstar.research.FactorValue;
             * }}
             */
            FactorValue.create = function(properties) {
                return new FactorValue(properties);
            };

            /**
             * Encodes the specified FactorValue message. Does not implicitly {@link northstar.research.FactorValue.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.FactorValue
             * @static
             * @param {northstar.research.FactorValue.$Properties} message FactorValue message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FactorValue.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.at != null && $Object.hasOwnProperty.call(message, "at"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.at);
                if (message.observation_id != null && $Object.hasOwnProperty.call(message, "observation_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.observation_id);
                if (message.reason != null && $Object.hasOwnProperty.call(message, "reason"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.reason);
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.status);
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.value);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.null_fields != null && message.null_fields.length)
                    for (let i = 0; i < message.null_fields.length; ++i)
                        writer.uint32(/* id 2046, wireType 2 =*/16370).string(message.null_fields[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a FactorValue message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.FactorValue
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.FactorValue & northstar.research.FactorValue.$Shape} FactorValue
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FactorValue.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.FactorValue();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.at = reader.stringVerify();
                            message._at = "at";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.observation_id = reader.stringVerify();
                            message._observation_id = "observation_id";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.reason = reader.stringVerify();
                            message._reason = "reason";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.status = reader.stringVerify();
                            message._status = "status";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.value = reader.stringVerify();
                            message._value = "value";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    case 2046: {
                            if (wireType !== 2)
                                break;
                            if (!(message.null_fields && message.null_fields.length))
                                message.null_fields = [];
                            message.null_fields.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a FactorValue message.
             * @function verify
             * @memberof northstar.research.FactorValue
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FactorValue.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.at != null && $Object.hasOwnProperty.call(message, "at")) {
                    properties._at = 1;
                    if (!$util.isString(message.at))
                        return "at: string expected";
                }
                if (message.observation_id != null && $Object.hasOwnProperty.call(message, "observation_id")) {
                    properties._observation_id = 1;
                    if (!$util.isString(message.observation_id))
                        return "observation_id: string expected";
                }
                if (message.reason != null && $Object.hasOwnProperty.call(message, "reason")) {
                    properties._reason = 1;
                    if (!$util.isString(message.reason))
                        return "reason: string expected";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (!$util.isString(message.status))
                        return "status: string expected";
                }
                if (message.value != null && $Object.hasOwnProperty.call(message, "value")) {
                    properties._value = 1;
                    if (!$util.isString(message.value))
                        return "value: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                if (message.null_fields != null && $Object.hasOwnProperty.call(message, "null_fields")) {
                    if (!$Array.isArray(message.null_fields))
                        return "null_fields: array expected";
                    for (let i = 0; i < message.null_fields.length; ++i)
                        if (!$util.isString(message.null_fields[i]))
                            return "null_fields: string[] expected";
                }
                return null;
            };

            /**
             * Creates a FactorValue message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.FactorValue
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.FactorValue} FactorValue
             */
            FactorValue.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.FactorValue)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.FactorValue: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.FactorValue();
                if (object.at != null)
                    message.at = $String(object.at);
                if (object.observation_id != null)
                    message.observation_id = $String(object.observation_id);
                if (object.reason != null)
                    message.reason = $String(object.reason);
                if (object.status != null)
                    message.status = $String(object.status);
                if (object.value != null)
                    message.value = $String(object.value);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.research.FactorValue.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.research.FactorValue.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                if (object.null_fields) {
                    if (!$Array.isArray(object.null_fields))
                        throw $TypeError(".northstar.research.FactorValue.null_fields: array expected");
                    message.null_fields = $Array(object.null_fields.length);
                    for (let i = 0; i < object.null_fields.length; ++i)
                        message.null_fields[i] = $String(object.null_fields[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a FactorValue message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.FactorValue
             * @static
             * @param {northstar.research.FactorValue} message FactorValue
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FactorValue.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.null_fields = [];
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.at != null && $Object.hasOwnProperty.call(message, "at")) {
                    object.at = message.at;
                    if (options.oneofs)
                        object._at = "at";
                }
                if (message.observation_id != null && $Object.hasOwnProperty.call(message, "observation_id")) {
                    object.observation_id = message.observation_id;
                    if (options.oneofs)
                        object._observation_id = "observation_id";
                }
                if (message.reason != null && $Object.hasOwnProperty.call(message, "reason")) {
                    object.reason = message.reason;
                    if (options.oneofs)
                        object._reason = "reason";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    object.status = message.status;
                    if (options.oneofs)
                        object._status = "status";
                }
                if (message.value != null && $Object.hasOwnProperty.call(message, "value")) {
                    object.value = message.value;
                    if (options.oneofs)
                        object._value = "value";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                if (message.null_fields && message.null_fields.length) {
                    object.null_fields = $Array(message.null_fields.length);
                    for (let j = 0; j < message.null_fields.length; ++j)
                        object.null_fields[j] = message.null_fields[j];
                }
                return object;
            };

            /**
             * Converts this FactorValue to JSON.
             * @function toJSON
             * @memberof northstar.research.FactorValue
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FactorValue.prototype.toJSON = function() {
                return FactorValue.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FactorValue
             * @function getTypeUrl
             * @memberof northstar.research.FactorValue
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FactorValue.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.FactorValue";
            };

            return FactorValue;
        })();

        research.FixedFactor = (function() {

            /**
             * Properties of a FixedFactor.
             * @typedef {Object} northstar.research.FixedFactor.$Properties
             * @property {string|null} [code_revision] FixedFactor code_revision
             * @property {string|null} [factor_id] FixedFactor factor_id
             * @property {google.protobuf.Struct.$Properties|null} [parameters] FixedFactor parameters
             * @property {string|null} [revision] FixedFactor revision
             * @property {"code_revision"} [_code_revision] FixedFactor _code_revision
             * @property {"factor_id"} [_factor_id] FixedFactor _factor_id
             * @property {"parameters"} [_parameters] FixedFactor _parameters
             * @property {"revision"} [_revision] FixedFactor _revision
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FixedFactor.
             * @memberof northstar.research
             * @interface IFixedFactor
             * @augments northstar.research.FixedFactor.$Properties
             * @deprecated Use northstar.research.FixedFactor.$Properties instead.
             */

            /**
             * Narrowed shape of a FixedFactor.
             * @typedef {{
             *   code_revision?: string|null;
             *   factor_id?: string|null;
             *   parameters?: google.protobuf.Struct.$Shape|null;
             *   revision?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _code_revision?: undefined; code_revision?: null }|{ _code_revision?: "code_revision"; code_revision: string })
             * ) & (
             *   ({ _factor_id?: undefined; factor_id?: null }|{ _factor_id?: "factor_id"; factor_id: string })
             * ) & (
             *   ({ _parameters?: undefined; parameters?: null }|{ _parameters?: "parameters"; parameters: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _revision?: undefined; revision?: null }|{ _revision?: "revision"; revision: string })
             * )} northstar.research.FixedFactor.$Shape
             */

            /**
             * Constructs a new FixedFactor.
             * @memberof northstar.research
             * @classdesc Represents a FixedFactor.
             * @constructor
             * @param {northstar.research.FixedFactor.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FixedFactor = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FixedFactor code_revision.
             * @member {string|null|undefined} code_revision
             * @memberof northstar.research.FixedFactor
             * @instance
             */
            FixedFactor.prototype.code_revision = null;

            /**
             * FixedFactor factor_id.
             * @member {string|null|undefined} factor_id
             * @memberof northstar.research.FixedFactor
             * @instance
             */
            FixedFactor.prototype.factor_id = null;

            /**
             * FixedFactor parameters.
             * @member {google.protobuf.Struct.$Properties|null|undefined} parameters
             * @memberof northstar.research.FixedFactor
             * @instance
             */
            FixedFactor.prototype.parameters = null;

            /**
             * FixedFactor revision.
             * @member {string|null|undefined} revision
             * @memberof northstar.research.FixedFactor
             * @instance
             */
            FixedFactor.prototype.revision = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * FixedFactor _code_revision.
             * @member {"code_revision"|undefined} _code_revision
             * @memberof northstar.research.FixedFactor
             * @instance
             */
            $Object.defineProperty(FixedFactor.prototype, "_code_revision", {
                get: $util.oneOfGetter($oneOfFields = ["code_revision"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FixedFactor _factor_id.
             * @member {"factor_id"|undefined} _factor_id
             * @memberof northstar.research.FixedFactor
             * @instance
             */
            $Object.defineProperty(FixedFactor.prototype, "_factor_id", {
                get: $util.oneOfGetter($oneOfFields = ["factor_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FixedFactor _parameters.
             * @member {"parameters"|undefined} _parameters
             * @memberof northstar.research.FixedFactor
             * @instance
             */
            $Object.defineProperty(FixedFactor.prototype, "_parameters", {
                get: $util.oneOfGetter($oneOfFields = ["parameters"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FixedFactor _revision.
             * @member {"revision"|undefined} _revision
             * @memberof northstar.research.FixedFactor
             * @instance
             */
            $Object.defineProperty(FixedFactor.prototype, "_revision", {
                get: $util.oneOfGetter($oneOfFields = ["revision"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new FixedFactor instance using the specified properties.
             * @function create
             * @memberof northstar.research.FixedFactor
             * @static
             * @param {northstar.research.FixedFactor.$Properties=} [properties] Properties to set
             * @returns {northstar.research.FixedFactor} FixedFactor instance
             * @type {{
             *   (properties: northstar.research.FixedFactor.$Shape): northstar.research.FixedFactor & northstar.research.FixedFactor.$Shape;
             *   (properties?: northstar.research.FixedFactor.$Properties): northstar.research.FixedFactor;
             * }}
             */
            FixedFactor.create = function(properties) {
                return new FixedFactor(properties);
            };

            /**
             * Encodes the specified FixedFactor message. Does not implicitly {@link northstar.research.FixedFactor.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.FixedFactor
             * @static
             * @param {northstar.research.FixedFactor.$Properties} message FixedFactor message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FixedFactor.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.code_revision);
                if (message.factor_id != null && $Object.hasOwnProperty.call(message, "factor_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.factor_id);
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters"))
                    $root.google.protobuf.Struct.encode(message.parameters, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.revision != null && $Object.hasOwnProperty.call(message, "revision"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.revision);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a FixedFactor message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.FixedFactor
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.FixedFactor & northstar.research.FixedFactor.$Shape} FixedFactor
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FixedFactor.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.FixedFactor();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.code_revision = reader.stringVerify();
                            message._code_revision = "code_revision";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.factor_id = reader.stringVerify();
                            message._factor_id = "factor_id";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.parameters = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.parameters);
                            message._parameters = "parameters";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.revision = reader.stringVerify();
                            message._revision = "revision";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a FixedFactor message.
             * @function verify
             * @memberof northstar.research.FixedFactor
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FixedFactor.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision")) {
                    properties._code_revision = 1;
                    if (!$util.isString(message.code_revision))
                        return "code_revision: string expected";
                }
                if (message.factor_id != null && $Object.hasOwnProperty.call(message, "factor_id")) {
                    properties._factor_id = 1;
                    if (!$util.isString(message.factor_id))
                        return "factor_id: string expected";
                }
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters")) {
                    properties._parameters = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.parameters, _depth + 1);
                        if (error)
                            return "parameters." + error;
                    }
                }
                if (message.revision != null && $Object.hasOwnProperty.call(message, "revision")) {
                    properties._revision = 1;
                    if (!$util.isString(message.revision))
                        return "revision: string expected";
                }
                return null;
            };

            /**
             * Creates a FixedFactor message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.FixedFactor
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.FixedFactor} FixedFactor
             */
            FixedFactor.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.FixedFactor)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.FixedFactor: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.FixedFactor();
                if (object.code_revision != null)
                    message.code_revision = $String(object.code_revision);
                if (object.factor_id != null)
                    message.factor_id = $String(object.factor_id);
                if (object.parameters != null) {
                    if (!$util.isObject(object.parameters))
                        throw $TypeError(".northstar.research.FixedFactor.parameters: object expected");
                    message.parameters = $root.google.protobuf.Struct.fromObject(object.parameters, _depth + 1);
                }
                if (object.revision != null)
                    message.revision = $String(object.revision);
                return message;
            };

            /**
             * Creates a plain object from a FixedFactor message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.FixedFactor
             * @static
             * @param {northstar.research.FixedFactor} message FixedFactor
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FixedFactor.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision")) {
                    object.code_revision = message.code_revision;
                    if (options.oneofs)
                        object._code_revision = "code_revision";
                }
                if (message.factor_id != null && $Object.hasOwnProperty.call(message, "factor_id")) {
                    object.factor_id = message.factor_id;
                    if (options.oneofs)
                        object._factor_id = "factor_id";
                }
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters")) {
                    object.parameters = $root.google.protobuf.Struct.toObject(message.parameters, options, _depth + 1);
                    if (options.oneofs)
                        object._parameters = "parameters";
                }
                if (message.revision != null && $Object.hasOwnProperty.call(message, "revision")) {
                    object.revision = message.revision;
                    if (options.oneofs)
                        object._revision = "revision";
                }
                return object;
            };

            /**
             * Converts this FixedFactor to JSON.
             * @function toJSON
             * @memberof northstar.research.FixedFactor
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FixedFactor.prototype.toJSON = function() {
                return FixedFactor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FixedFactor
             * @function getTypeUrl
             * @memberof northstar.research.FixedFactor
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FixedFactor.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.FixedFactor";
            };

            return FixedFactor;
        })();

        research.FixedStrategy = (function() {

            /**
             * Properties of a FixedStrategy.
             * @typedef {Object} northstar.research.FixedStrategy.$Properties
             * @property {string|null} [code_revision] FixedStrategy code_revision
             * @property {Object.<string,northstar.research.FixedFactor.$Properties>|null} [factor_bindings] FixedStrategy factor_bindings
             * @property {google.protobuf.Struct.$Properties|null} [parameters] FixedStrategy parameters
             * @property {string|null} [revision] FixedStrategy revision
             * @property {string|null} [strategy_id] FixedStrategy strategy_id
             * @property {"code_revision"} [_code_revision] FixedStrategy _code_revision
             * @property {"parameters"} [_parameters] FixedStrategy _parameters
             * @property {"revision"} [_revision] FixedStrategy _revision
             * @property {"strategy_id"} [_strategy_id] FixedStrategy _strategy_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FixedStrategy.
             * @memberof northstar.research
             * @interface IFixedStrategy
             * @augments northstar.research.FixedStrategy.$Properties
             * @deprecated Use northstar.research.FixedStrategy.$Properties instead.
             */

            /**
             * Narrowed shape of a FixedStrategy.
             * @typedef {{
             *   code_revision?: string|null;
             *   factor_bindings?: Object.<string,northstar.research.FixedFactor.$Shape>|null;
             *   parameters?: google.protobuf.Struct.$Shape|null;
             *   revision?: string|null;
             *   strategy_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _code_revision?: undefined; code_revision?: null }|{ _code_revision?: "code_revision"; code_revision: string })
             * ) & (
             *   ({ _parameters?: undefined; parameters?: null }|{ _parameters?: "parameters"; parameters: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _revision?: undefined; revision?: null }|{ _revision?: "revision"; revision: string })
             * ) & (
             *   ({ _strategy_id?: undefined; strategy_id?: null }|{ _strategy_id?: "strategy_id"; strategy_id: string })
             * )} northstar.research.FixedStrategy.$Shape
             */

            /**
             * Constructs a new FixedStrategy.
             * @memberof northstar.research
             * @classdesc Represents a FixedStrategy.
             * @constructor
             * @param {northstar.research.FixedStrategy.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FixedStrategy = function (properties) {
                this.factor_bindings = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FixedStrategy code_revision.
             * @member {string|null|undefined} code_revision
             * @memberof northstar.research.FixedStrategy
             * @instance
             */
            FixedStrategy.prototype.code_revision = null;

            /**
             * FixedStrategy factor_bindings.
             * @member {Object.<string,northstar.research.FixedFactor.$Properties>} factor_bindings
             * @memberof northstar.research.FixedStrategy
             * @instance
             */
            FixedStrategy.prototype.factor_bindings = $util.emptyObject;

            /**
             * FixedStrategy parameters.
             * @member {google.protobuf.Struct.$Properties|null|undefined} parameters
             * @memberof northstar.research.FixedStrategy
             * @instance
             */
            FixedStrategy.prototype.parameters = null;

            /**
             * FixedStrategy revision.
             * @member {string|null|undefined} revision
             * @memberof northstar.research.FixedStrategy
             * @instance
             */
            FixedStrategy.prototype.revision = null;

            /**
             * FixedStrategy strategy_id.
             * @member {string|null|undefined} strategy_id
             * @memberof northstar.research.FixedStrategy
             * @instance
             */
            FixedStrategy.prototype.strategy_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * FixedStrategy _code_revision.
             * @member {"code_revision"|undefined} _code_revision
             * @memberof northstar.research.FixedStrategy
             * @instance
             */
            $Object.defineProperty(FixedStrategy.prototype, "_code_revision", {
                get: $util.oneOfGetter($oneOfFields = ["code_revision"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FixedStrategy _parameters.
             * @member {"parameters"|undefined} _parameters
             * @memberof northstar.research.FixedStrategy
             * @instance
             */
            $Object.defineProperty(FixedStrategy.prototype, "_parameters", {
                get: $util.oneOfGetter($oneOfFields = ["parameters"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FixedStrategy _revision.
             * @member {"revision"|undefined} _revision
             * @memberof northstar.research.FixedStrategy
             * @instance
             */
            $Object.defineProperty(FixedStrategy.prototype, "_revision", {
                get: $util.oneOfGetter($oneOfFields = ["revision"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FixedStrategy _strategy_id.
             * @member {"strategy_id"|undefined} _strategy_id
             * @memberof northstar.research.FixedStrategy
             * @instance
             */
            $Object.defineProperty(FixedStrategy.prototype, "_strategy_id", {
                get: $util.oneOfGetter($oneOfFields = ["strategy_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new FixedStrategy instance using the specified properties.
             * @function create
             * @memberof northstar.research.FixedStrategy
             * @static
             * @param {northstar.research.FixedStrategy.$Properties=} [properties] Properties to set
             * @returns {northstar.research.FixedStrategy} FixedStrategy instance
             * @type {{
             *   (properties: northstar.research.FixedStrategy.$Shape): northstar.research.FixedStrategy & northstar.research.FixedStrategy.$Shape;
             *   (properties?: northstar.research.FixedStrategy.$Properties): northstar.research.FixedStrategy;
             * }}
             */
            FixedStrategy.create = function(properties) {
                return new FixedStrategy(properties);
            };

            /**
             * Encodes the specified FixedStrategy message. Does not implicitly {@link northstar.research.FixedStrategy.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.FixedStrategy
             * @static
             * @param {northstar.research.FixedStrategy.$Properties} message FixedStrategy message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FixedStrategy.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.code_revision);
                if (message.factor_bindings != null && $Object.hasOwnProperty.call(message, "factor_bindings"))
                    for (let keys = $Object.keys(message.factor_bindings), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 2, wireType 2 =*/18).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.northstar.research.FixedFactor.encode(message.factor_bindings[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters"))
                    $root.google.protobuf.Struct.encode(message.parameters, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.revision != null && $Object.hasOwnProperty.call(message, "revision"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.revision);
                if (message.strategy_id != null && $Object.hasOwnProperty.call(message, "strategy_id"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.strategy_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a FixedStrategy message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.FixedStrategy
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.FixedStrategy & northstar.research.FixedStrategy.$Shape} FixedStrategy
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FixedStrategy.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.FixedStrategy();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.code_revision = reader.stringVerify();
                            message._code_revision = "code_revision";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (message.factor_bindings === $util.emptyObject)
                                message.factor_bindings = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.northstar.research.FixedFactor.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.factor_bindings, key);
                            message.factor_bindings[key] = value || new $root.northstar.research.FixedFactor();
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.parameters = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.parameters);
                            message._parameters = "parameters";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.revision = reader.stringVerify();
                            message._revision = "revision";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.strategy_id = reader.stringVerify();
                            message._strategy_id = "strategy_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a FixedStrategy message.
             * @function verify
             * @memberof northstar.research.FixedStrategy
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FixedStrategy.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision")) {
                    properties._code_revision = 1;
                    if (!$util.isString(message.code_revision))
                        return "code_revision: string expected";
                }
                if (message.factor_bindings != null && $Object.hasOwnProperty.call(message, "factor_bindings")) {
                    if (!$util.isObject(message.factor_bindings))
                        return "factor_bindings: object expected";
                    let key = $Object.keys(message.factor_bindings);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.northstar.research.FixedFactor.verify(message.factor_bindings[key[i]], _depth + 1);
                        if (error)
                            return "factor_bindings." + error;
                    }
                }
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters")) {
                    properties._parameters = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.parameters, _depth + 1);
                        if (error)
                            return "parameters." + error;
                    }
                }
                if (message.revision != null && $Object.hasOwnProperty.call(message, "revision")) {
                    properties._revision = 1;
                    if (!$util.isString(message.revision))
                        return "revision: string expected";
                }
                if (message.strategy_id != null && $Object.hasOwnProperty.call(message, "strategy_id")) {
                    properties._strategy_id = 1;
                    if (!$util.isString(message.strategy_id))
                        return "strategy_id: string expected";
                }
                return null;
            };

            /**
             * Creates a FixedStrategy message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.FixedStrategy
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.FixedStrategy} FixedStrategy
             */
            FixedStrategy.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.FixedStrategy)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.FixedStrategy: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.FixedStrategy();
                if (object.code_revision != null)
                    message.code_revision = $String(object.code_revision);
                if (object.factor_bindings) {
                    if (!$util.isObject(object.factor_bindings))
                        throw $TypeError(".northstar.research.FixedStrategy.factor_bindings: object expected");
                    message.factor_bindings = {};
                    for (let keys = $Object.keys(object.factor_bindings), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.factor_bindings, keys[i]);
                        if (!$util.isObject(object.factor_bindings[keys[i]]))
                            throw $TypeError(".northstar.research.FixedStrategy.factor_bindings: object expected");
                        message.factor_bindings[keys[i]] = $root.northstar.research.FixedFactor.fromObject(object.factor_bindings[keys[i]], _depth + 1);
                    }
                }
                if (object.parameters != null) {
                    if (!$util.isObject(object.parameters))
                        throw $TypeError(".northstar.research.FixedStrategy.parameters: object expected");
                    message.parameters = $root.google.protobuf.Struct.fromObject(object.parameters, _depth + 1);
                }
                if (object.revision != null)
                    message.revision = $String(object.revision);
                if (object.strategy_id != null)
                    message.strategy_id = $String(object.strategy_id);
                return message;
            };

            /**
             * Creates a plain object from a FixedStrategy message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.FixedStrategy
             * @static
             * @param {northstar.research.FixedStrategy} message FixedStrategy
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FixedStrategy.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.factor_bindings = {};
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision")) {
                    object.code_revision = message.code_revision;
                    if (options.oneofs)
                        object._code_revision = "code_revision";
                }
                let keys2;
                if (message.factor_bindings && (keys2 = $Object.keys(message.factor_bindings)).length) {
                    object.factor_bindings = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.factor_bindings, keys2[j]);
                        object.factor_bindings[keys2[j]] = $root.northstar.research.FixedFactor.toObject(message.factor_bindings[keys2[j]], options, _depth + 1);
                    }
                }
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters")) {
                    object.parameters = $root.google.protobuf.Struct.toObject(message.parameters, options, _depth + 1);
                    if (options.oneofs)
                        object._parameters = "parameters";
                }
                if (message.revision != null && $Object.hasOwnProperty.call(message, "revision")) {
                    object.revision = message.revision;
                    if (options.oneofs)
                        object._revision = "revision";
                }
                if (message.strategy_id != null && $Object.hasOwnProperty.call(message, "strategy_id")) {
                    object.strategy_id = message.strategy_id;
                    if (options.oneofs)
                        object._strategy_id = "strategy_id";
                }
                return object;
            };

            /**
             * Converts this FixedStrategy to JSON.
             * @function toJSON
             * @memberof northstar.research.FixedStrategy
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FixedStrategy.prototype.toJSON = function() {
                return FixedStrategy.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FixedStrategy
             * @function getTypeUrl
             * @memberof northstar.research.FixedStrategy
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FixedStrategy.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.FixedStrategy";
            };

            return FixedStrategy;
        })();

        research.HttpError = (function() {

            /**
             * Properties of a HttpError.
             * @typedef {Object} northstar.research.HttpError.$Properties
             * @property {string|null} [detail] HttpError detail
             * @property {string|null} [rejection_id] HttpError rejection_id
             * @property {string|null} [request_id] HttpError request_id
             * @property {string|null} [runtime_id] HttpError runtime_id
             * @property {string|null} [status] HttpError status
             * @property {string|null} [url] HttpError url
             * @property {Array.<string>|null} [null_fields] HttpError null_fields
             * @property {"detail"} [_detail] HttpError _detail
             * @property {"rejection_id"} [_rejection_id] HttpError _rejection_id
             * @property {"request_id"} [_request_id] HttpError _request_id
             * @property {"runtime_id"} [_runtime_id] HttpError _runtime_id
             * @property {"status"} [_status] HttpError _status
             * @property {"url"} [_url] HttpError _url
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a HttpError.
             * @memberof northstar.research
             * @interface IHttpError
             * @augments northstar.research.HttpError.$Properties
             * @deprecated Use northstar.research.HttpError.$Properties instead.
             */

            /**
             * Narrowed shape of a HttpError.
             * @typedef {{
             *   detail?: string|null;
             *   rejection_id?: string|null;
             *   request_id?: string|null;
             *   runtime_id?: string|null;
             *   status?: string|null;
             *   url?: string|null;
             *   null_fields?: Array.<string>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _detail?: undefined; detail?: null }|{ _detail?: "detail"; detail: string })
             * ) & (
             *   ({ _rejection_id?: undefined; rejection_id?: null }|{ _rejection_id?: "rejection_id"; rejection_id: string })
             * ) & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * ) & (
             *   ({ _runtime_id?: undefined; runtime_id?: null }|{ _runtime_id?: "runtime_id"; runtime_id: string })
             * ) & (
             *   ({ _status?: undefined; status?: null }|{ _status?: "status"; status: string })
             * ) & (
             *   ({ _url?: undefined; url?: null }|{ _url?: "url"; url: string })
             * )} northstar.research.HttpError.$Shape
             */

            /**
             * Constructs a new HttpError.
             * @memberof northstar.research
             * @classdesc Represents a HttpError.
             * @constructor
             * @param {northstar.research.HttpError.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const HttpError = function (properties) {
                this.null_fields = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * HttpError detail.
             * @member {string|null|undefined} detail
             * @memberof northstar.research.HttpError
             * @instance
             */
            HttpError.prototype.detail = null;

            /**
             * HttpError rejection_id.
             * @member {string|null|undefined} rejection_id
             * @memberof northstar.research.HttpError
             * @instance
             */
            HttpError.prototype.rejection_id = null;

            /**
             * HttpError request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.research.HttpError
             * @instance
             */
            HttpError.prototype.request_id = null;

            /**
             * HttpError runtime_id.
             * @member {string|null|undefined} runtime_id
             * @memberof northstar.research.HttpError
             * @instance
             */
            HttpError.prototype.runtime_id = null;

            /**
             * HttpError status.
             * @member {string|null|undefined} status
             * @memberof northstar.research.HttpError
             * @instance
             */
            HttpError.prototype.status = null;

            /**
             * HttpError url.
             * @member {string|null|undefined} url
             * @memberof northstar.research.HttpError
             * @instance
             */
            HttpError.prototype.url = null;

            /**
             * HttpError null_fields.
             * @member {Array.<string>} null_fields
             * @memberof northstar.research.HttpError
             * @instance
             */
            HttpError.prototype.null_fields = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * HttpError _detail.
             * @member {"detail"|undefined} _detail
             * @memberof northstar.research.HttpError
             * @instance
             */
            $Object.defineProperty(HttpError.prototype, "_detail", {
                get: $util.oneOfGetter($oneOfFields = ["detail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * HttpError _rejection_id.
             * @member {"rejection_id"|undefined} _rejection_id
             * @memberof northstar.research.HttpError
             * @instance
             */
            $Object.defineProperty(HttpError.prototype, "_rejection_id", {
                get: $util.oneOfGetter($oneOfFields = ["rejection_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * HttpError _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.research.HttpError
             * @instance
             */
            $Object.defineProperty(HttpError.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * HttpError _runtime_id.
             * @member {"runtime_id"|undefined} _runtime_id
             * @memberof northstar.research.HttpError
             * @instance
             */
            $Object.defineProperty(HttpError.prototype, "_runtime_id", {
                get: $util.oneOfGetter($oneOfFields = ["runtime_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * HttpError _status.
             * @member {"status"|undefined} _status
             * @memberof northstar.research.HttpError
             * @instance
             */
            $Object.defineProperty(HttpError.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * HttpError _url.
             * @member {"url"|undefined} _url
             * @memberof northstar.research.HttpError
             * @instance
             */
            $Object.defineProperty(HttpError.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new HttpError instance using the specified properties.
             * @function create
             * @memberof northstar.research.HttpError
             * @static
             * @param {northstar.research.HttpError.$Properties=} [properties] Properties to set
             * @returns {northstar.research.HttpError} HttpError instance
             * @type {{
             *   (properties: northstar.research.HttpError.$Shape): northstar.research.HttpError & northstar.research.HttpError.$Shape;
             *   (properties?: northstar.research.HttpError.$Properties): northstar.research.HttpError;
             * }}
             */
            HttpError.create = function(properties) {
                return new HttpError(properties);
            };

            /**
             * Encodes the specified HttpError message. Does not implicitly {@link northstar.research.HttpError.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.HttpError
             * @static
             * @param {northstar.research.HttpError.$Properties} message HttpError message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HttpError.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.detail != null && $Object.hasOwnProperty.call(message, "detail"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.detail);
                if (message.rejection_id != null && $Object.hasOwnProperty.call(message, "rejection_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.rejection_id);
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.request_id);
                if (message.runtime_id != null && $Object.hasOwnProperty.call(message, "runtime_id"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.runtime_id);
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.status);
                if (message.url != null && $Object.hasOwnProperty.call(message, "url"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.url);
                if (message.null_fields != null && message.null_fields.length)
                    for (let i = 0; i < message.null_fields.length; ++i)
                        writer.uint32(/* id 2046, wireType 2 =*/16370).string(message.null_fields[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a HttpError message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.HttpError
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.HttpError & northstar.research.HttpError.$Shape} HttpError
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HttpError.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.HttpError();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.detail = reader.stringVerify();
                            message._detail = "detail";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.rejection_id = reader.stringVerify();
                            message._rejection_id = "rejection_id";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.runtime_id = reader.stringVerify();
                            message._runtime_id = "runtime_id";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.status = reader.stringVerify();
                            message._status = "status";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.url = reader.stringVerify();
                            message._url = "url";
                            continue;
                        }
                    case 2046: {
                            if (wireType !== 2)
                                break;
                            if (!(message.null_fields && message.null_fields.length))
                                message.null_fields = [];
                            message.null_fields.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a HttpError message.
             * @function verify
             * @memberof northstar.research.HttpError
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            HttpError.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.detail != null && $Object.hasOwnProperty.call(message, "detail")) {
                    properties._detail = 1;
                    if (!$util.isString(message.detail))
                        return "detail: string expected";
                }
                if (message.rejection_id != null && $Object.hasOwnProperty.call(message, "rejection_id")) {
                    properties._rejection_id = 1;
                    if (!$util.isString(message.rejection_id))
                        return "rejection_id: string expected";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                if (message.runtime_id != null && $Object.hasOwnProperty.call(message, "runtime_id")) {
                    properties._runtime_id = 1;
                    if (!$util.isString(message.runtime_id))
                        return "runtime_id: string expected";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (!$util.isString(message.status))
                        return "status: string expected";
                }
                if (message.url != null && $Object.hasOwnProperty.call(message, "url")) {
                    properties._url = 1;
                    if (!$util.isString(message.url))
                        return "url: string expected";
                }
                if (message.null_fields != null && $Object.hasOwnProperty.call(message, "null_fields")) {
                    if (!$Array.isArray(message.null_fields))
                        return "null_fields: array expected";
                    for (let i = 0; i < message.null_fields.length; ++i)
                        if (!$util.isString(message.null_fields[i]))
                            return "null_fields: string[] expected";
                }
                return null;
            };

            /**
             * Creates a HttpError message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.HttpError
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.HttpError} HttpError
             */
            HttpError.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.HttpError)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.HttpError: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.HttpError();
                if (object.detail != null)
                    message.detail = $String(object.detail);
                if (object.rejection_id != null)
                    message.rejection_id = $String(object.rejection_id);
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                if (object.runtime_id != null)
                    message.runtime_id = $String(object.runtime_id);
                if (object.status != null)
                    message.status = $String(object.status);
                if (object.url != null)
                    message.url = $String(object.url);
                if (object.null_fields) {
                    if (!$Array.isArray(object.null_fields))
                        throw $TypeError(".northstar.research.HttpError.null_fields: array expected");
                    message.null_fields = $Array(object.null_fields.length);
                    for (let i = 0; i < object.null_fields.length; ++i)
                        message.null_fields[i] = $String(object.null_fields[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a HttpError message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.HttpError
             * @static
             * @param {northstar.research.HttpError} message HttpError
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            HttpError.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.null_fields = [];
                if (message.detail != null && $Object.hasOwnProperty.call(message, "detail")) {
                    object.detail = message.detail;
                    if (options.oneofs)
                        object._detail = "detail";
                }
                if (message.rejection_id != null && $Object.hasOwnProperty.call(message, "rejection_id")) {
                    object.rejection_id = message.rejection_id;
                    if (options.oneofs)
                        object._rejection_id = "rejection_id";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                if (message.runtime_id != null && $Object.hasOwnProperty.call(message, "runtime_id")) {
                    object.runtime_id = message.runtime_id;
                    if (options.oneofs)
                        object._runtime_id = "runtime_id";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    object.status = message.status;
                    if (options.oneofs)
                        object._status = "status";
                }
                if (message.url != null && $Object.hasOwnProperty.call(message, "url")) {
                    object.url = message.url;
                    if (options.oneofs)
                        object._url = "url";
                }
                if (message.null_fields && message.null_fields.length) {
                    object.null_fields = $Array(message.null_fields.length);
                    for (let j = 0; j < message.null_fields.length; ++j)
                        object.null_fields[j] = message.null_fields[j];
                }
                return object;
            };

            /**
             * Converts this HttpError to JSON.
             * @function toJSON
             * @memberof northstar.research.HttpError
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            HttpError.prototype.toJSON = function() {
                return HttpError.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for HttpError
             * @function getTypeUrl
             * @memberof northstar.research.HttpError
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            HttpError.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.HttpError";
            };

            return HttpError;
        })();

        research.ImportSpecification = (function() {

            /**
             * Properties of an ImportSpecification.
             * @typedef {Object} northstar.research.ImportSpecification.$Properties
             * @property {string|null} [availability_basis] ImportSpecification availability_basis
             * @property {string|null} [availability_note] ImportSpecification availability_note
             * @property {string|null} [currency] ImportSpecification currency
             * @property {string|null} [exchange] ImportSpecification exchange
             * @property {string|null} [multiplier] ImportSpecification multiplier
             * @property {string|null} [price_tick] ImportSpecification price_tick
             * @property {string|null} [product] ImportSpecification product
             * @property {string|null} [quantity_unit] ImportSpecification quantity_unit
             * @property {string|null} [session_close] ImportSpecification session_close
             * @property {string|null} [session_open] ImportSpecification session_open
             * @property {string|null} [source_name] ImportSpecification source_name
             * @property {string|null} [source_reference] ImportSpecification source_reference
             * @property {string|null} [symbol] ImportSpecification symbol
             * @property {string|null} [timezone] ImportSpecification timezone
             * @property {string|null} [trading_day] ImportSpecification trading_day
             * @property {"availability_basis"} [_availability_basis] ImportSpecification _availability_basis
             * @property {"availability_note"} [_availability_note] ImportSpecification _availability_note
             * @property {"currency"} [_currency] ImportSpecification _currency
             * @property {"exchange"} [_exchange] ImportSpecification _exchange
             * @property {"multiplier"} [_multiplier] ImportSpecification _multiplier
             * @property {"price_tick"} [_price_tick] ImportSpecification _price_tick
             * @property {"product"} [_product] ImportSpecification _product
             * @property {"quantity_unit"} [_quantity_unit] ImportSpecification _quantity_unit
             * @property {"session_close"} [_session_close] ImportSpecification _session_close
             * @property {"session_open"} [_session_open] ImportSpecification _session_open
             * @property {"source_name"} [_source_name] ImportSpecification _source_name
             * @property {"source_reference"} [_source_reference] ImportSpecification _source_reference
             * @property {"symbol"} [_symbol] ImportSpecification _symbol
             * @property {"timezone"} [_timezone] ImportSpecification _timezone
             * @property {"trading_day"} [_trading_day] ImportSpecification _trading_day
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an ImportSpecification.
             * @memberof northstar.research
             * @interface IImportSpecification
             * @augments northstar.research.ImportSpecification.$Properties
             * @deprecated Use northstar.research.ImportSpecification.$Properties instead.
             */

            /**
             * Narrowed shape of an ImportSpecification.
             * @typedef {{
             *   availability_basis?: string|null;
             *   availability_note?: string|null;
             *   currency?: string|null;
             *   exchange?: string|null;
             *   multiplier?: string|null;
             *   price_tick?: string|null;
             *   product?: string|null;
             *   quantity_unit?: string|null;
             *   session_close?: string|null;
             *   session_open?: string|null;
             *   source_name?: string|null;
             *   source_reference?: string|null;
             *   symbol?: string|null;
             *   timezone?: string|null;
             *   trading_day?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _availability_basis?: undefined; availability_basis?: null }|{ _availability_basis?: "availability_basis"; availability_basis: string })
             * ) & (
             *   ({ _availability_note?: undefined; availability_note?: null }|{ _availability_note?: "availability_note"; availability_note: string })
             * ) & (
             *   ({ _currency?: undefined; currency?: null }|{ _currency?: "currency"; currency: string })
             * ) & (
             *   ({ _exchange?: undefined; exchange?: null }|{ _exchange?: "exchange"; exchange: string })
             * ) & (
             *   ({ _multiplier?: undefined; multiplier?: null }|{ _multiplier?: "multiplier"; multiplier: string })
             * ) & (
             *   ({ _price_tick?: undefined; price_tick?: null }|{ _price_tick?: "price_tick"; price_tick: string })
             * ) & (
             *   ({ _product?: undefined; product?: null }|{ _product?: "product"; product: string })
             * ) & (
             *   ({ _quantity_unit?: undefined; quantity_unit?: null }|{ _quantity_unit?: "quantity_unit"; quantity_unit: string })
             * ) & (
             *   ({ _session_close?: undefined; session_close?: null }|{ _session_close?: "session_close"; session_close: string })
             * ) & (
             *   ({ _session_open?: undefined; session_open?: null }|{ _session_open?: "session_open"; session_open: string })
             * ) & (
             *   ({ _source_name?: undefined; source_name?: null }|{ _source_name?: "source_name"; source_name: string })
             * ) & (
             *   ({ _source_reference?: undefined; source_reference?: null }|{ _source_reference?: "source_reference"; source_reference: string })
             * ) & (
             *   ({ _symbol?: undefined; symbol?: null }|{ _symbol?: "symbol"; symbol: string })
             * ) & (
             *   ({ _timezone?: undefined; timezone?: null }|{ _timezone?: "timezone"; timezone: string })
             * ) & (
             *   ({ _trading_day?: undefined; trading_day?: null }|{ _trading_day?: "trading_day"; trading_day: string })
             * )} northstar.research.ImportSpecification.$Shape
             */

            /**
             * Constructs a new ImportSpecification.
             * @memberof northstar.research
             * @classdesc Represents an ImportSpecification.
             * @constructor
             * @param {northstar.research.ImportSpecification.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ImportSpecification = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ImportSpecification availability_basis.
             * @member {string|null|undefined} availability_basis
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.availability_basis = null;

            /**
             * ImportSpecification availability_note.
             * @member {string|null|undefined} availability_note
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.availability_note = null;

            /**
             * ImportSpecification currency.
             * @member {string|null|undefined} currency
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.currency = null;

            /**
             * ImportSpecification exchange.
             * @member {string|null|undefined} exchange
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.exchange = null;

            /**
             * ImportSpecification multiplier.
             * @member {string|null|undefined} multiplier
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.multiplier = null;

            /**
             * ImportSpecification price_tick.
             * @member {string|null|undefined} price_tick
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.price_tick = null;

            /**
             * ImportSpecification product.
             * @member {string|null|undefined} product
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.product = null;

            /**
             * ImportSpecification quantity_unit.
             * @member {string|null|undefined} quantity_unit
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.quantity_unit = null;

            /**
             * ImportSpecification session_close.
             * @member {string|null|undefined} session_close
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.session_close = null;

            /**
             * ImportSpecification session_open.
             * @member {string|null|undefined} session_open
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.session_open = null;

            /**
             * ImportSpecification source_name.
             * @member {string|null|undefined} source_name
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.source_name = null;

            /**
             * ImportSpecification source_reference.
             * @member {string|null|undefined} source_reference
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.source_reference = null;

            /**
             * ImportSpecification symbol.
             * @member {string|null|undefined} symbol
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.symbol = null;

            /**
             * ImportSpecification timezone.
             * @member {string|null|undefined} timezone
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.timezone = null;

            /**
             * ImportSpecification trading_day.
             * @member {string|null|undefined} trading_day
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.trading_day = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * ImportSpecification _availability_basis.
             * @member {"availability_basis"|undefined} _availability_basis
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_availability_basis", {
                get: $util.oneOfGetter($oneOfFields = ["availability_basis"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _availability_note.
             * @member {"availability_note"|undefined} _availability_note
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_availability_note", {
                get: $util.oneOfGetter($oneOfFields = ["availability_note"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _currency.
             * @member {"currency"|undefined} _currency
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_currency", {
                get: $util.oneOfGetter($oneOfFields = ["currency"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _exchange.
             * @member {"exchange"|undefined} _exchange
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_exchange", {
                get: $util.oneOfGetter($oneOfFields = ["exchange"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _multiplier.
             * @member {"multiplier"|undefined} _multiplier
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_multiplier", {
                get: $util.oneOfGetter($oneOfFields = ["multiplier"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _price_tick.
             * @member {"price_tick"|undefined} _price_tick
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_price_tick", {
                get: $util.oneOfGetter($oneOfFields = ["price_tick"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _product.
             * @member {"product"|undefined} _product
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_product", {
                get: $util.oneOfGetter($oneOfFields = ["product"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _quantity_unit.
             * @member {"quantity_unit"|undefined} _quantity_unit
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_quantity_unit", {
                get: $util.oneOfGetter($oneOfFields = ["quantity_unit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _session_close.
             * @member {"session_close"|undefined} _session_close
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_session_close", {
                get: $util.oneOfGetter($oneOfFields = ["session_close"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _session_open.
             * @member {"session_open"|undefined} _session_open
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_session_open", {
                get: $util.oneOfGetter($oneOfFields = ["session_open"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _source_name.
             * @member {"source_name"|undefined} _source_name
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_source_name", {
                get: $util.oneOfGetter($oneOfFields = ["source_name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _source_reference.
             * @member {"source_reference"|undefined} _source_reference
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_source_reference", {
                get: $util.oneOfGetter($oneOfFields = ["source_reference"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _symbol.
             * @member {"symbol"|undefined} _symbol
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_symbol", {
                get: $util.oneOfGetter($oneOfFields = ["symbol"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _timezone.
             * @member {"timezone"|undefined} _timezone
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_timezone", {
                get: $util.oneOfGetter($oneOfFields = ["timezone"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _trading_day.
             * @member {"trading_day"|undefined} _trading_day
             * @memberof northstar.research.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_trading_day", {
                get: $util.oneOfGetter($oneOfFields = ["trading_day"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ImportSpecification instance using the specified properties.
             * @function create
             * @memberof northstar.research.ImportSpecification
             * @static
             * @param {northstar.research.ImportSpecification.$Properties=} [properties] Properties to set
             * @returns {northstar.research.ImportSpecification} ImportSpecification instance
             * @type {{
             *   (properties: northstar.research.ImportSpecification.$Shape): northstar.research.ImportSpecification & northstar.research.ImportSpecification.$Shape;
             *   (properties?: northstar.research.ImportSpecification.$Properties): northstar.research.ImportSpecification;
             * }}
             */
            ImportSpecification.create = function(properties) {
                return new ImportSpecification(properties);
            };

            /**
             * Encodes the specified ImportSpecification message. Does not implicitly {@link northstar.research.ImportSpecification.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.ImportSpecification
             * @static
             * @param {northstar.research.ImportSpecification.$Properties} message ImportSpecification message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ImportSpecification.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.availability_basis != null && $Object.hasOwnProperty.call(message, "availability_basis"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.availability_basis);
                if (message.availability_note != null && $Object.hasOwnProperty.call(message, "availability_note"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.availability_note);
                if (message.currency != null && $Object.hasOwnProperty.call(message, "currency"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.currency);
                if (message.exchange != null && $Object.hasOwnProperty.call(message, "exchange"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.exchange);
                if (message.multiplier != null && $Object.hasOwnProperty.call(message, "multiplier"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.multiplier);
                if (message.price_tick != null && $Object.hasOwnProperty.call(message, "price_tick"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.price_tick);
                if (message.product != null && $Object.hasOwnProperty.call(message, "product"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.product);
                if (message.quantity_unit != null && $Object.hasOwnProperty.call(message, "quantity_unit"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.quantity_unit);
                if (message.session_close != null && $Object.hasOwnProperty.call(message, "session_close"))
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.session_close);
                if (message.session_open != null && $Object.hasOwnProperty.call(message, "session_open"))
                    writer.uint32(/* id 10, wireType 2 =*/82).string(message.session_open);
                if (message.source_name != null && $Object.hasOwnProperty.call(message, "source_name"))
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.source_name);
                if (message.source_reference != null && $Object.hasOwnProperty.call(message, "source_reference"))
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.source_reference);
                if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol"))
                    writer.uint32(/* id 13, wireType 2 =*/106).string(message.symbol);
                if (message.timezone != null && $Object.hasOwnProperty.call(message, "timezone"))
                    writer.uint32(/* id 14, wireType 2 =*/114).string(message.timezone);
                if (message.trading_day != null && $Object.hasOwnProperty.call(message, "trading_day"))
                    writer.uint32(/* id 15, wireType 2 =*/122).string(message.trading_day);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes an ImportSpecification message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.ImportSpecification
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.ImportSpecification & northstar.research.ImportSpecification.$Shape} ImportSpecification
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ImportSpecification.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.ImportSpecification();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.availability_basis = reader.stringVerify();
                            message._availability_basis = "availability_basis";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.availability_note = reader.stringVerify();
                            message._availability_note = "availability_note";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.currency = reader.stringVerify();
                            message._currency = "currency";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.exchange = reader.stringVerify();
                            message._exchange = "exchange";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.multiplier = reader.stringVerify();
                            message._multiplier = "multiplier";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.price_tick = reader.stringVerify();
                            message._price_tick = "price_tick";
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            message.product = reader.stringVerify();
                            message._product = "product";
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            message.quantity_unit = reader.stringVerify();
                            message._quantity_unit = "quantity_unit";
                            continue;
                        }
                    case 9: {
                            if (wireType !== 2)
                                break;
                            message.session_close = reader.stringVerify();
                            message._session_close = "session_close";
                            continue;
                        }
                    case 10: {
                            if (wireType !== 2)
                                break;
                            message.session_open = reader.stringVerify();
                            message._session_open = "session_open";
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            message.source_name = reader.stringVerify();
                            message._source_name = "source_name";
                            continue;
                        }
                    case 12: {
                            if (wireType !== 2)
                                break;
                            message.source_reference = reader.stringVerify();
                            message._source_reference = "source_reference";
                            continue;
                        }
                    case 13: {
                            if (wireType !== 2)
                                break;
                            message.symbol = reader.stringVerify();
                            message._symbol = "symbol";
                            continue;
                        }
                    case 14: {
                            if (wireType !== 2)
                                break;
                            message.timezone = reader.stringVerify();
                            message._timezone = "timezone";
                            continue;
                        }
                    case 15: {
                            if (wireType !== 2)
                                break;
                            message.trading_day = reader.stringVerify();
                            message._trading_day = "trading_day";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies an ImportSpecification message.
             * @function verify
             * @memberof northstar.research.ImportSpecification
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ImportSpecification.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.availability_basis != null && $Object.hasOwnProperty.call(message, "availability_basis")) {
                    properties._availability_basis = 1;
                    if (!$util.isString(message.availability_basis))
                        return "availability_basis: string expected";
                }
                if (message.availability_note != null && $Object.hasOwnProperty.call(message, "availability_note")) {
                    properties._availability_note = 1;
                    if (!$util.isString(message.availability_note))
                        return "availability_note: string expected";
                }
                if (message.currency != null && $Object.hasOwnProperty.call(message, "currency")) {
                    properties._currency = 1;
                    if (!$util.isString(message.currency))
                        return "currency: string expected";
                }
                if (message.exchange != null && $Object.hasOwnProperty.call(message, "exchange")) {
                    properties._exchange = 1;
                    if (!$util.isString(message.exchange))
                        return "exchange: string expected";
                }
                if (message.multiplier != null && $Object.hasOwnProperty.call(message, "multiplier")) {
                    properties._multiplier = 1;
                    if (!$util.isString(message.multiplier))
                        return "multiplier: string expected";
                }
                if (message.price_tick != null && $Object.hasOwnProperty.call(message, "price_tick")) {
                    properties._price_tick = 1;
                    if (!$util.isString(message.price_tick))
                        return "price_tick: string expected";
                }
                if (message.product != null && $Object.hasOwnProperty.call(message, "product")) {
                    properties._product = 1;
                    if (!$util.isString(message.product))
                        return "product: string expected";
                }
                if (message.quantity_unit != null && $Object.hasOwnProperty.call(message, "quantity_unit")) {
                    properties._quantity_unit = 1;
                    if (!$util.isString(message.quantity_unit))
                        return "quantity_unit: string expected";
                }
                if (message.session_close != null && $Object.hasOwnProperty.call(message, "session_close")) {
                    properties._session_close = 1;
                    if (!$util.isString(message.session_close))
                        return "session_close: string expected";
                }
                if (message.session_open != null && $Object.hasOwnProperty.call(message, "session_open")) {
                    properties._session_open = 1;
                    if (!$util.isString(message.session_open))
                        return "session_open: string expected";
                }
                if (message.source_name != null && $Object.hasOwnProperty.call(message, "source_name")) {
                    properties._source_name = 1;
                    if (!$util.isString(message.source_name))
                        return "source_name: string expected";
                }
                if (message.source_reference != null && $Object.hasOwnProperty.call(message, "source_reference")) {
                    properties._source_reference = 1;
                    if (!$util.isString(message.source_reference))
                        return "source_reference: string expected";
                }
                if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol")) {
                    properties._symbol = 1;
                    if (!$util.isString(message.symbol))
                        return "symbol: string expected";
                }
                if (message.timezone != null && $Object.hasOwnProperty.call(message, "timezone")) {
                    properties._timezone = 1;
                    if (!$util.isString(message.timezone))
                        return "timezone: string expected";
                }
                if (message.trading_day != null && $Object.hasOwnProperty.call(message, "trading_day")) {
                    properties._trading_day = 1;
                    if (!$util.isString(message.trading_day))
                        return "trading_day: string expected";
                }
                return null;
            };

            /**
             * Creates an ImportSpecification message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.ImportSpecification
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.ImportSpecification} ImportSpecification
             */
            ImportSpecification.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.ImportSpecification)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.ImportSpecification: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.ImportSpecification();
                if (object.availability_basis != null)
                    message.availability_basis = $String(object.availability_basis);
                if (object.availability_note != null)
                    message.availability_note = $String(object.availability_note);
                if (object.currency != null)
                    message.currency = $String(object.currency);
                if (object.exchange != null)
                    message.exchange = $String(object.exchange);
                if (object.multiplier != null)
                    message.multiplier = $String(object.multiplier);
                if (object.price_tick != null)
                    message.price_tick = $String(object.price_tick);
                if (object.product != null)
                    message.product = $String(object.product);
                if (object.quantity_unit != null)
                    message.quantity_unit = $String(object.quantity_unit);
                if (object.session_close != null)
                    message.session_close = $String(object.session_close);
                if (object.session_open != null)
                    message.session_open = $String(object.session_open);
                if (object.source_name != null)
                    message.source_name = $String(object.source_name);
                if (object.source_reference != null)
                    message.source_reference = $String(object.source_reference);
                if (object.symbol != null)
                    message.symbol = $String(object.symbol);
                if (object.timezone != null)
                    message.timezone = $String(object.timezone);
                if (object.trading_day != null)
                    message.trading_day = $String(object.trading_day);
                return message;
            };

            /**
             * Creates a plain object from an ImportSpecification message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.ImportSpecification
             * @static
             * @param {northstar.research.ImportSpecification} message ImportSpecification
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ImportSpecification.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.availability_basis != null && $Object.hasOwnProperty.call(message, "availability_basis")) {
                    object.availability_basis = message.availability_basis;
                    if (options.oneofs)
                        object._availability_basis = "availability_basis";
                }
                if (message.availability_note != null && $Object.hasOwnProperty.call(message, "availability_note")) {
                    object.availability_note = message.availability_note;
                    if (options.oneofs)
                        object._availability_note = "availability_note";
                }
                if (message.currency != null && $Object.hasOwnProperty.call(message, "currency")) {
                    object.currency = message.currency;
                    if (options.oneofs)
                        object._currency = "currency";
                }
                if (message.exchange != null && $Object.hasOwnProperty.call(message, "exchange")) {
                    object.exchange = message.exchange;
                    if (options.oneofs)
                        object._exchange = "exchange";
                }
                if (message.multiplier != null && $Object.hasOwnProperty.call(message, "multiplier")) {
                    object.multiplier = message.multiplier;
                    if (options.oneofs)
                        object._multiplier = "multiplier";
                }
                if (message.price_tick != null && $Object.hasOwnProperty.call(message, "price_tick")) {
                    object.price_tick = message.price_tick;
                    if (options.oneofs)
                        object._price_tick = "price_tick";
                }
                if (message.product != null && $Object.hasOwnProperty.call(message, "product")) {
                    object.product = message.product;
                    if (options.oneofs)
                        object._product = "product";
                }
                if (message.quantity_unit != null && $Object.hasOwnProperty.call(message, "quantity_unit")) {
                    object.quantity_unit = message.quantity_unit;
                    if (options.oneofs)
                        object._quantity_unit = "quantity_unit";
                }
                if (message.session_close != null && $Object.hasOwnProperty.call(message, "session_close")) {
                    object.session_close = message.session_close;
                    if (options.oneofs)
                        object._session_close = "session_close";
                }
                if (message.session_open != null && $Object.hasOwnProperty.call(message, "session_open")) {
                    object.session_open = message.session_open;
                    if (options.oneofs)
                        object._session_open = "session_open";
                }
                if (message.source_name != null && $Object.hasOwnProperty.call(message, "source_name")) {
                    object.source_name = message.source_name;
                    if (options.oneofs)
                        object._source_name = "source_name";
                }
                if (message.source_reference != null && $Object.hasOwnProperty.call(message, "source_reference")) {
                    object.source_reference = message.source_reference;
                    if (options.oneofs)
                        object._source_reference = "source_reference";
                }
                if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol")) {
                    object.symbol = message.symbol;
                    if (options.oneofs)
                        object._symbol = "symbol";
                }
                if (message.timezone != null && $Object.hasOwnProperty.call(message, "timezone")) {
                    object.timezone = message.timezone;
                    if (options.oneofs)
                        object._timezone = "timezone";
                }
                if (message.trading_day != null && $Object.hasOwnProperty.call(message, "trading_day")) {
                    object.trading_day = message.trading_day;
                    if (options.oneofs)
                        object._trading_day = "trading_day";
                }
                return object;
            };

            /**
             * Converts this ImportSpecification to JSON.
             * @function toJSON
             * @memberof northstar.research.ImportSpecification
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ImportSpecification.prototype.toJSON = function() {
                return ImportSpecification.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ImportSpecification
             * @function getTypeUrl
             * @memberof northstar.research.ImportSpecification
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ImportSpecification.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.ImportSpecification";
            };

            return ImportSpecification;
        })();

        research.PaperAdvanced = (function() {

            /**
             * Properties of a PaperAdvanced.
             * @typedef {Object} northstar.research.PaperAdvanced.$Properties
             * @property {number|Long|null} [sequence] PaperAdvanced sequence
             * @property {string|null} [session_id] PaperAdvanced session_id
             * @property {google.protobuf.Struct.$Properties|null} [step] PaperAdvanced step
             * @property {string|null} [url] PaperAdvanced url
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] PaperAdvanced evidence_fields
             * @property {"sequence"} [_sequence] PaperAdvanced _sequence
             * @property {"session_id"} [_session_id] PaperAdvanced _session_id
             * @property {"step"} [_step] PaperAdvanced _step
             * @property {"url"} [_url] PaperAdvanced _url
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a PaperAdvanced.
             * @memberof northstar.research
             * @interface IPaperAdvanced
             * @augments northstar.research.PaperAdvanced.$Properties
             * @deprecated Use northstar.research.PaperAdvanced.$Properties instead.
             */

            /**
             * Narrowed shape of a PaperAdvanced.
             * @typedef {{
             *   sequence?: number|Long|null;
             *   session_id?: string|null;
             *   step?: google.protobuf.Struct.$Shape|null;
             *   url?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _sequence?: undefined; sequence?: null }|{ _sequence?: "sequence"; sequence: number|Long })
             * ) & (
             *   ({ _session_id?: undefined; session_id?: null }|{ _session_id?: "session_id"; session_id: string })
             * ) & (
             *   ({ _step?: undefined; step?: null }|{ _step?: "step"; step: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _url?: undefined; url?: null }|{ _url?: "url"; url: string })
             * )} northstar.research.PaperAdvanced.$Shape
             */

            /**
             * Constructs a new PaperAdvanced.
             * @memberof northstar.research
             * @classdesc Represents a PaperAdvanced.
             * @constructor
             * @param {northstar.research.PaperAdvanced.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const PaperAdvanced = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * PaperAdvanced sequence.
             * @member {number|Long|null|undefined} sequence
             * @memberof northstar.research.PaperAdvanced
             * @instance
             */
            PaperAdvanced.prototype.sequence = null;

            /**
             * PaperAdvanced session_id.
             * @member {string|null|undefined} session_id
             * @memberof northstar.research.PaperAdvanced
             * @instance
             */
            PaperAdvanced.prototype.session_id = null;

            /**
             * PaperAdvanced step.
             * @member {google.protobuf.Struct.$Properties|null|undefined} step
             * @memberof northstar.research.PaperAdvanced
             * @instance
             */
            PaperAdvanced.prototype.step = null;

            /**
             * PaperAdvanced url.
             * @member {string|null|undefined} url
             * @memberof northstar.research.PaperAdvanced
             * @instance
             */
            PaperAdvanced.prototype.url = null;

            /**
             * PaperAdvanced evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.research.PaperAdvanced
             * @instance
             */
            PaperAdvanced.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * PaperAdvanced _sequence.
             * @member {"sequence"|undefined} _sequence
             * @memberof northstar.research.PaperAdvanced
             * @instance
             */
            $Object.defineProperty(PaperAdvanced.prototype, "_sequence", {
                get: $util.oneOfGetter($oneOfFields = ["sequence"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * PaperAdvanced _session_id.
             * @member {"session_id"|undefined} _session_id
             * @memberof northstar.research.PaperAdvanced
             * @instance
             */
            $Object.defineProperty(PaperAdvanced.prototype, "_session_id", {
                get: $util.oneOfGetter($oneOfFields = ["session_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * PaperAdvanced _step.
             * @member {"step"|undefined} _step
             * @memberof northstar.research.PaperAdvanced
             * @instance
             */
            $Object.defineProperty(PaperAdvanced.prototype, "_step", {
                get: $util.oneOfGetter($oneOfFields = ["step"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * PaperAdvanced _url.
             * @member {"url"|undefined} _url
             * @memberof northstar.research.PaperAdvanced
             * @instance
             */
            $Object.defineProperty(PaperAdvanced.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PaperAdvanced instance using the specified properties.
             * @function create
             * @memberof northstar.research.PaperAdvanced
             * @static
             * @param {northstar.research.PaperAdvanced.$Properties=} [properties] Properties to set
             * @returns {northstar.research.PaperAdvanced} PaperAdvanced instance
             * @type {{
             *   (properties: northstar.research.PaperAdvanced.$Shape): northstar.research.PaperAdvanced & northstar.research.PaperAdvanced.$Shape;
             *   (properties?: northstar.research.PaperAdvanced.$Properties): northstar.research.PaperAdvanced;
             * }}
             */
            PaperAdvanced.create = function(properties) {
                return new PaperAdvanced(properties);
            };

            /**
             * Encodes the specified PaperAdvanced message. Does not implicitly {@link northstar.research.PaperAdvanced.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.PaperAdvanced
             * @static
             * @param {northstar.research.PaperAdvanced.$Properties} message PaperAdvanced message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PaperAdvanced.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.sequence != null && $Object.hasOwnProperty.call(message, "sequence"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int64(message.sequence);
                if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.session_id);
                if (message.step != null && $Object.hasOwnProperty.call(message, "step"))
                    $root.google.protobuf.Struct.encode(message.step, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.url != null && $Object.hasOwnProperty.call(message, "url"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.url);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a PaperAdvanced message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.PaperAdvanced
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.PaperAdvanced & northstar.research.PaperAdvanced.$Shape} PaperAdvanced
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PaperAdvanced.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.PaperAdvanced();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            message.sequence = reader.int64();
                            message._sequence = "sequence";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.session_id = reader.stringVerify();
                            message._session_id = "session_id";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.step = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.step);
                            message._step = "step";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.url = reader.stringVerify();
                            message._url = "url";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a PaperAdvanced message.
             * @function verify
             * @memberof northstar.research.PaperAdvanced
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PaperAdvanced.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.sequence != null && $Object.hasOwnProperty.call(message, "sequence")) {
                    properties._sequence = 1;
                    if (!$util.isInteger(message.sequence) && !(message.sequence && $util.isInteger(message.sequence.low) && $util.isInteger(message.sequence.high)))
                        return "sequence: integer|Long expected";
                }
                if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id")) {
                    properties._session_id = 1;
                    if (!$util.isString(message.session_id))
                        return "session_id: string expected";
                }
                if (message.step != null && $Object.hasOwnProperty.call(message, "step")) {
                    properties._step = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.step, _depth + 1);
                        if (error)
                            return "step." + error;
                    }
                }
                if (message.url != null && $Object.hasOwnProperty.call(message, "url")) {
                    properties._url = 1;
                    if (!$util.isString(message.url))
                        return "url: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a PaperAdvanced message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.PaperAdvanced
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.PaperAdvanced} PaperAdvanced
             */
            PaperAdvanced.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.PaperAdvanced)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.PaperAdvanced: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.PaperAdvanced();
                if (object.sequence != null)
                    if ($util.Long)
                        message.sequence = $util.Long.fromValue(object.sequence, false);
                    else if (typeof object.sequence === "string")
                        message.sequence = $parseInt(object.sequence, 10);
                    else if (typeof object.sequence === "number")
                        message.sequence = object.sequence;
                    else if (typeof object.sequence === "object")
                        message.sequence = new $util.LongBits(object.sequence.low >>> 0, object.sequence.high >>> 0).toNumber();
                if (object.session_id != null)
                    message.session_id = $String(object.session_id);
                if (object.step != null) {
                    if (!$util.isObject(object.step))
                        throw $TypeError(".northstar.research.PaperAdvanced.step: object expected");
                    message.step = $root.google.protobuf.Struct.fromObject(object.step, _depth + 1);
                }
                if (object.url != null)
                    message.url = $String(object.url);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.research.PaperAdvanced.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.research.PaperAdvanced.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a PaperAdvanced message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.PaperAdvanced
             * @static
             * @param {northstar.research.PaperAdvanced} message PaperAdvanced
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PaperAdvanced.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.sequence != null && $Object.hasOwnProperty.call(message, "sequence")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.sequence = typeof message.sequence === "number" ? $BigInt(message.sequence) : $util.Long.fromBits(message.sequence.low >>> 0, message.sequence.high >>> 0, false).toBigInt();
                    else if (typeof message.sequence === "number")
                        object.sequence = options.longs === $String ? $String(message.sequence) : message.sequence;
                    else
                        object.sequence = options.longs === $String ? $util.Long.prototype.toString.call(message.sequence) : options.longs === $Number ? new $util.LongBits(message.sequence.low >>> 0, message.sequence.high >>> 0).toNumber() : message.sequence;
                    if (options.oneofs)
                        object._sequence = "sequence";
                }
                if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id")) {
                    object.session_id = message.session_id;
                    if (options.oneofs)
                        object._session_id = "session_id";
                }
                if (message.step != null && $Object.hasOwnProperty.call(message, "step")) {
                    object.step = $root.google.protobuf.Struct.toObject(message.step, options, _depth + 1);
                    if (options.oneofs)
                        object._step = "step";
                }
                if (message.url != null && $Object.hasOwnProperty.call(message, "url")) {
                    object.url = message.url;
                    if (options.oneofs)
                        object._url = "url";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this PaperAdvanced to JSON.
             * @function toJSON
             * @memberof northstar.research.PaperAdvanced
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PaperAdvanced.prototype.toJSON = function() {
                return PaperAdvanced.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for PaperAdvanced
             * @function getTypeUrl
             * @memberof northstar.research.PaperAdvanced
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            PaperAdvanced.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.PaperAdvanced";
            };

            return PaperAdvanced;
        })();

        research.PaperRequest = (function() {

            /**
             * Properties of a PaperRequest.
             * @typedef {Object} northstar.research.PaperRequest.$Properties
             * @property {string|null} [configuration_id] PaperRequest configuration_id
             * @property {string|null} [request_id] PaperRequest request_id
             * @property {string|null} [snapshot_id] PaperRequest snapshot_id
             * @property {"configuration_id"} [_configuration_id] PaperRequest _configuration_id
             * @property {"request_id"} [_request_id] PaperRequest _request_id
             * @property {"snapshot_id"} [_snapshot_id] PaperRequest _snapshot_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a PaperRequest.
             * @memberof northstar.research
             * @interface IPaperRequest
             * @augments northstar.research.PaperRequest.$Properties
             * @deprecated Use northstar.research.PaperRequest.$Properties instead.
             */

            /**
             * Narrowed shape of a PaperRequest.
             * @typedef {{
             *   configuration_id?: string|null;
             *   request_id?: string|null;
             *   snapshot_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _configuration_id?: undefined; configuration_id?: null }|{ _configuration_id?: "configuration_id"; configuration_id: string })
             * ) & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * ) & (
             *   ({ _snapshot_id?: undefined; snapshot_id?: null }|{ _snapshot_id?: "snapshot_id"; snapshot_id: string })
             * )} northstar.research.PaperRequest.$Shape
             */

            /**
             * Constructs a new PaperRequest.
             * @memberof northstar.research
             * @classdesc Represents a PaperRequest.
             * @constructor
             * @param {northstar.research.PaperRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const PaperRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * PaperRequest configuration_id.
             * @member {string|null|undefined} configuration_id
             * @memberof northstar.research.PaperRequest
             * @instance
             */
            PaperRequest.prototype.configuration_id = null;

            /**
             * PaperRequest request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.research.PaperRequest
             * @instance
             */
            PaperRequest.prototype.request_id = null;

            /**
             * PaperRequest snapshot_id.
             * @member {string|null|undefined} snapshot_id
             * @memberof northstar.research.PaperRequest
             * @instance
             */
            PaperRequest.prototype.snapshot_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * PaperRequest _configuration_id.
             * @member {"configuration_id"|undefined} _configuration_id
             * @memberof northstar.research.PaperRequest
             * @instance
             */
            $Object.defineProperty(PaperRequest.prototype, "_configuration_id", {
                get: $util.oneOfGetter($oneOfFields = ["configuration_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * PaperRequest _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.research.PaperRequest
             * @instance
             */
            $Object.defineProperty(PaperRequest.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * PaperRequest _snapshot_id.
             * @member {"snapshot_id"|undefined} _snapshot_id
             * @memberof northstar.research.PaperRequest
             * @instance
             */
            $Object.defineProperty(PaperRequest.prototype, "_snapshot_id", {
                get: $util.oneOfGetter($oneOfFields = ["snapshot_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PaperRequest instance using the specified properties.
             * @function create
             * @memberof northstar.research.PaperRequest
             * @static
             * @param {northstar.research.PaperRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.research.PaperRequest} PaperRequest instance
             * @type {{
             *   (properties: northstar.research.PaperRequest.$Shape): northstar.research.PaperRequest & northstar.research.PaperRequest.$Shape;
             *   (properties?: northstar.research.PaperRequest.$Properties): northstar.research.PaperRequest;
             * }}
             */
            PaperRequest.create = function(properties) {
                return new PaperRequest(properties);
            };

            /**
             * Encodes the specified PaperRequest message. Does not implicitly {@link northstar.research.PaperRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.PaperRequest
             * @static
             * @param {northstar.research.PaperRequest.$Properties} message PaperRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PaperRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.configuration_id != null && $Object.hasOwnProperty.call(message, "configuration_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.configuration_id);
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.request_id);
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.snapshot_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a PaperRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.PaperRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.PaperRequest & northstar.research.PaperRequest.$Shape} PaperRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PaperRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.PaperRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.configuration_id = reader.stringVerify();
                            message._configuration_id = "configuration_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.snapshot_id = reader.stringVerify();
                            message._snapshot_id = "snapshot_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a PaperRequest message.
             * @function verify
             * @memberof northstar.research.PaperRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PaperRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.configuration_id != null && $Object.hasOwnProperty.call(message, "configuration_id")) {
                    properties._configuration_id = 1;
                    if (!$util.isString(message.configuration_id))
                        return "configuration_id: string expected";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id")) {
                    properties._snapshot_id = 1;
                    if (!$util.isString(message.snapshot_id))
                        return "snapshot_id: string expected";
                }
                return null;
            };

            /**
             * Creates a PaperRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.PaperRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.PaperRequest} PaperRequest
             */
            PaperRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.PaperRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.PaperRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.PaperRequest();
                if (object.configuration_id != null)
                    message.configuration_id = $String(object.configuration_id);
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                if (object.snapshot_id != null)
                    message.snapshot_id = $String(object.snapshot_id);
                return message;
            };

            /**
             * Creates a plain object from a PaperRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.PaperRequest
             * @static
             * @param {northstar.research.PaperRequest} message PaperRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PaperRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.configuration_id != null && $Object.hasOwnProperty.call(message, "configuration_id")) {
                    object.configuration_id = message.configuration_id;
                    if (options.oneofs)
                        object._configuration_id = "configuration_id";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id")) {
                    object.snapshot_id = message.snapshot_id;
                    if (options.oneofs)
                        object._snapshot_id = "snapshot_id";
                }
                return object;
            };

            /**
             * Converts this PaperRequest to JSON.
             * @function toJSON
             * @memberof northstar.research.PaperRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PaperRequest.prototype.toJSON = function() {
                return PaperRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for PaperRequest
             * @function getTypeUrl
             * @memberof northstar.research.PaperRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            PaperRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.PaperRequest";
            };

            return PaperRequest;
        })();

        research.PaperSession = (function() {

            /**
             * Properties of a PaperSession.
             * @typedef {Object} northstar.research.PaperSession.$Properties
             * @property {northstar.research.SavedConfiguration.$Properties|null} [configuration] PaperSession configuration
             * @property {number|Long|null} [cursor] PaperSession cursor
             * @property {string|null} [session_id] PaperSession session_id
             * @property {string|null} [status] PaperSession status
             * @property {northstar.research.ResearchSummary.$Properties|null} [summary] PaperSession summary
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] PaperSession evidence_fields
             * @property {"configuration"} [_configuration] PaperSession _configuration
             * @property {"cursor"} [_cursor] PaperSession _cursor
             * @property {"session_id"} [_session_id] PaperSession _session_id
             * @property {"status"} [_status] PaperSession _status
             * @property {"summary"} [_summary] PaperSession _summary
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a PaperSession.
             * @memberof northstar.research
             * @interface IPaperSession
             * @augments northstar.research.PaperSession.$Properties
             * @deprecated Use northstar.research.PaperSession.$Properties instead.
             */

            /**
             * Narrowed shape of a PaperSession.
             * @typedef {{
             *   configuration?: northstar.research.SavedConfiguration.$Shape|null;
             *   cursor?: number|Long|null;
             *   session_id?: string|null;
             *   status?: string|null;
             *   summary?: northstar.research.ResearchSummary.$Shape|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _configuration?: undefined; configuration?: null }|{ _configuration?: "configuration"; configuration: northstar.research.SavedConfiguration.$Shape })
             * ) & (
             *   ({ _cursor?: undefined; cursor?: null }|{ _cursor?: "cursor"; cursor: number|Long })
             * ) & (
             *   ({ _session_id?: undefined; session_id?: null }|{ _session_id?: "session_id"; session_id: string })
             * ) & (
             *   ({ _status?: undefined; status?: null }|{ _status?: "status"; status: string })
             * ) & (
             *   ({ _summary?: undefined; summary?: null }|{ _summary?: "summary"; summary: northstar.research.ResearchSummary.$Shape })
             * )} northstar.research.PaperSession.$Shape
             */

            /**
             * Constructs a new PaperSession.
             * @memberof northstar.research
             * @classdesc Represents a PaperSession.
             * @constructor
             * @param {northstar.research.PaperSession.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const PaperSession = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * PaperSession configuration.
             * @member {northstar.research.SavedConfiguration.$Properties|null|undefined} configuration
             * @memberof northstar.research.PaperSession
             * @instance
             */
            PaperSession.prototype.configuration = null;

            /**
             * PaperSession cursor.
             * @member {number|Long|null|undefined} cursor
             * @memberof northstar.research.PaperSession
             * @instance
             */
            PaperSession.prototype.cursor = null;

            /**
             * PaperSession session_id.
             * @member {string|null|undefined} session_id
             * @memberof northstar.research.PaperSession
             * @instance
             */
            PaperSession.prototype.session_id = null;

            /**
             * PaperSession status.
             * @member {string|null|undefined} status
             * @memberof northstar.research.PaperSession
             * @instance
             */
            PaperSession.prototype.status = null;

            /**
             * PaperSession summary.
             * @member {northstar.research.ResearchSummary.$Properties|null|undefined} summary
             * @memberof northstar.research.PaperSession
             * @instance
             */
            PaperSession.prototype.summary = null;

            /**
             * PaperSession evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.research.PaperSession
             * @instance
             */
            PaperSession.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * PaperSession _configuration.
             * @member {"configuration"|undefined} _configuration
             * @memberof northstar.research.PaperSession
             * @instance
             */
            $Object.defineProperty(PaperSession.prototype, "_configuration", {
                get: $util.oneOfGetter($oneOfFields = ["configuration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * PaperSession _cursor.
             * @member {"cursor"|undefined} _cursor
             * @memberof northstar.research.PaperSession
             * @instance
             */
            $Object.defineProperty(PaperSession.prototype, "_cursor", {
                get: $util.oneOfGetter($oneOfFields = ["cursor"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * PaperSession _session_id.
             * @member {"session_id"|undefined} _session_id
             * @memberof northstar.research.PaperSession
             * @instance
             */
            $Object.defineProperty(PaperSession.prototype, "_session_id", {
                get: $util.oneOfGetter($oneOfFields = ["session_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * PaperSession _status.
             * @member {"status"|undefined} _status
             * @memberof northstar.research.PaperSession
             * @instance
             */
            $Object.defineProperty(PaperSession.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * PaperSession _summary.
             * @member {"summary"|undefined} _summary
             * @memberof northstar.research.PaperSession
             * @instance
             */
            $Object.defineProperty(PaperSession.prototype, "_summary", {
                get: $util.oneOfGetter($oneOfFields = ["summary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PaperSession instance using the specified properties.
             * @function create
             * @memberof northstar.research.PaperSession
             * @static
             * @param {northstar.research.PaperSession.$Properties=} [properties] Properties to set
             * @returns {northstar.research.PaperSession} PaperSession instance
             * @type {{
             *   (properties: northstar.research.PaperSession.$Shape): northstar.research.PaperSession & northstar.research.PaperSession.$Shape;
             *   (properties?: northstar.research.PaperSession.$Properties): northstar.research.PaperSession;
             * }}
             */
            PaperSession.create = function(properties) {
                return new PaperSession(properties);
            };

            /**
             * Encodes the specified PaperSession message. Does not implicitly {@link northstar.research.PaperSession.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.PaperSession
             * @static
             * @param {northstar.research.PaperSession.$Properties} message PaperSession message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PaperSession.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.configuration != null && $Object.hasOwnProperty.call(message, "configuration"))
                    $root.northstar.research.SavedConfiguration.encode(message.configuration, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.cursor != null && $Object.hasOwnProperty.call(message, "cursor"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.cursor);
                if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.session_id);
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.status);
                if (message.summary != null && $Object.hasOwnProperty.call(message, "summary"))
                    $root.northstar.research.ResearchSummary.encode(message.summary, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a PaperSession message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.PaperSession
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.PaperSession & northstar.research.PaperSession.$Shape} PaperSession
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PaperSession.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.PaperSession();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.configuration = $root.northstar.research.SavedConfiguration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.configuration);
                            message._configuration = "configuration";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            message.cursor = reader.int64();
                            message._cursor = "cursor";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.session_id = reader.stringVerify();
                            message._session_id = "session_id";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.status = reader.stringVerify();
                            message._status = "status";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.summary = $root.northstar.research.ResearchSummary.decode(reader, reader.uint32(), $undefined, _depth + 1, message.summary);
                            message._summary = "summary";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a PaperSession message.
             * @function verify
             * @memberof northstar.research.PaperSession
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PaperSession.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.configuration != null && $Object.hasOwnProperty.call(message, "configuration")) {
                    properties._configuration = 1;
                    {
                        let error = $root.northstar.research.SavedConfiguration.verify(message.configuration, _depth + 1);
                        if (error)
                            return "configuration." + error;
                    }
                }
                if (message.cursor != null && $Object.hasOwnProperty.call(message, "cursor")) {
                    properties._cursor = 1;
                    if (!$util.isInteger(message.cursor) && !(message.cursor && $util.isInteger(message.cursor.low) && $util.isInteger(message.cursor.high)))
                        return "cursor: integer|Long expected";
                }
                if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id")) {
                    properties._session_id = 1;
                    if (!$util.isString(message.session_id))
                        return "session_id: string expected";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (!$util.isString(message.status))
                        return "status: string expected";
                }
                if (message.summary != null && $Object.hasOwnProperty.call(message, "summary")) {
                    properties._summary = 1;
                    {
                        let error = $root.northstar.research.ResearchSummary.verify(message.summary, _depth + 1);
                        if (error)
                            return "summary." + error;
                    }
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a PaperSession message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.PaperSession
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.PaperSession} PaperSession
             */
            PaperSession.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.PaperSession)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.PaperSession: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.PaperSession();
                if (object.configuration != null) {
                    if (!$util.isObject(object.configuration))
                        throw $TypeError(".northstar.research.PaperSession.configuration: object expected");
                    message.configuration = $root.northstar.research.SavedConfiguration.fromObject(object.configuration, _depth + 1);
                }
                if (object.cursor != null)
                    if ($util.Long)
                        message.cursor = $util.Long.fromValue(object.cursor, false);
                    else if (typeof object.cursor === "string")
                        message.cursor = $parseInt(object.cursor, 10);
                    else if (typeof object.cursor === "number")
                        message.cursor = object.cursor;
                    else if (typeof object.cursor === "object")
                        message.cursor = new $util.LongBits(object.cursor.low >>> 0, object.cursor.high >>> 0).toNumber();
                if (object.session_id != null)
                    message.session_id = $String(object.session_id);
                if (object.status != null)
                    message.status = $String(object.status);
                if (object.summary != null) {
                    if (!$util.isObject(object.summary))
                        throw $TypeError(".northstar.research.PaperSession.summary: object expected");
                    message.summary = $root.northstar.research.ResearchSummary.fromObject(object.summary, _depth + 1);
                }
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.research.PaperSession.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.research.PaperSession.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a PaperSession message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.PaperSession
             * @static
             * @param {northstar.research.PaperSession} message PaperSession
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PaperSession.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.configuration != null && $Object.hasOwnProperty.call(message, "configuration")) {
                    object.configuration = $root.northstar.research.SavedConfiguration.toObject(message.configuration, options, _depth + 1);
                    if (options.oneofs)
                        object._configuration = "configuration";
                }
                if (message.cursor != null && $Object.hasOwnProperty.call(message, "cursor")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.cursor = typeof message.cursor === "number" ? $BigInt(message.cursor) : $util.Long.fromBits(message.cursor.low >>> 0, message.cursor.high >>> 0, false).toBigInt();
                    else if (typeof message.cursor === "number")
                        object.cursor = options.longs === $String ? $String(message.cursor) : message.cursor;
                    else
                        object.cursor = options.longs === $String ? $util.Long.prototype.toString.call(message.cursor) : options.longs === $Number ? new $util.LongBits(message.cursor.low >>> 0, message.cursor.high >>> 0).toNumber() : message.cursor;
                    if (options.oneofs)
                        object._cursor = "cursor";
                }
                if (message.session_id != null && $Object.hasOwnProperty.call(message, "session_id")) {
                    object.session_id = message.session_id;
                    if (options.oneofs)
                        object._session_id = "session_id";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    object.status = message.status;
                    if (options.oneofs)
                        object._status = "status";
                }
                if (message.summary != null && $Object.hasOwnProperty.call(message, "summary")) {
                    object.summary = $root.northstar.research.ResearchSummary.toObject(message.summary, options, _depth + 1);
                    if (options.oneofs)
                        object._summary = "summary";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this PaperSession to JSON.
             * @function toJSON
             * @memberof northstar.research.PaperSession
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PaperSession.prototype.toJSON = function() {
                return PaperSession.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for PaperSession
             * @function getTypeUrl
             * @memberof northstar.research.PaperSession
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            PaperSession.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.PaperSession";
            };

            return PaperSession;
        })();

        research.ParameterDescription = (function() {

            /**
             * Properties of a ParameterDescription.
             * @typedef {Object} northstar.research.ParameterDescription.$Properties
             * @property {google.protobuf.Value.$Properties|null} ["default"] ParameterDescription default
             * @property {string|null} [label] ParameterDescription label
             * @property {google.protobuf.Value.$Properties|null} [maximum] ParameterDescription maximum
             * @property {google.protobuf.Value.$Properties|null} [minimum] ParameterDescription minimum
             * @property {string|null} [name] ParameterDescription name
             * @property {string|null} [unit] ParameterDescription unit
             * @property {"default"} [_default] ParameterDescription _default
             * @property {"label"} [_label] ParameterDescription _label
             * @property {"maximum"} [_maximum] ParameterDescription _maximum
             * @property {"minimum"} [_minimum] ParameterDescription _minimum
             * @property {"name"} [_name] ParameterDescription _name
             * @property {"unit"} [_unit] ParameterDescription _unit
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ParameterDescription.
             * @memberof northstar.research
             * @interface IParameterDescription
             * @augments northstar.research.ParameterDescription.$Properties
             * @deprecated Use northstar.research.ParameterDescription.$Properties instead.
             */

            /**
             * Narrowed shape of a ParameterDescription.
             * @typedef {{
             *   "default"?: google.protobuf.Value.$Shape|null;
             *   label?: string|null;
             *   maximum?: google.protobuf.Value.$Shape|null;
             *   minimum?: google.protobuf.Value.$Shape|null;
             *   name?: string|null;
             *   unit?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _default?: undefined; "default"?: null }|{ _default?: "default"; "default": google.protobuf.Value.$Shape })
             * ) & (
             *   ({ _label?: undefined; label?: null }|{ _label?: "label"; label: string })
             * ) & (
             *   ({ _maximum?: undefined; maximum?: null }|{ _maximum?: "maximum"; maximum: google.protobuf.Value.$Shape })
             * ) & (
             *   ({ _minimum?: undefined; minimum?: null }|{ _minimum?: "minimum"; minimum: google.protobuf.Value.$Shape })
             * ) & (
             *   ({ _name?: undefined; name?: null }|{ _name?: "name"; name: string })
             * ) & (
             *   ({ _unit?: undefined; unit?: null }|{ _unit?: "unit"; unit: string })
             * )} northstar.research.ParameterDescription.$Shape
             */

            /**
             * Constructs a new ParameterDescription.
             * @memberof northstar.research
             * @classdesc Represents a ParameterDescription.
             * @constructor
             * @param {northstar.research.ParameterDescription.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ParameterDescription = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ParameterDescription default.
             * @member {google.protobuf.Value.$Properties|null|undefined} default
             * @memberof northstar.research.ParameterDescription
             * @instance
             */
            ParameterDescription.prototype["default"] = null;

            /**
             * ParameterDescription label.
             * @member {string|null|undefined} label
             * @memberof northstar.research.ParameterDescription
             * @instance
             */
            ParameterDescription.prototype.label = null;

            /**
             * ParameterDescription maximum.
             * @member {google.protobuf.Value.$Properties|null|undefined} maximum
             * @memberof northstar.research.ParameterDescription
             * @instance
             */
            ParameterDescription.prototype.maximum = null;

            /**
             * ParameterDescription minimum.
             * @member {google.protobuf.Value.$Properties|null|undefined} minimum
             * @memberof northstar.research.ParameterDescription
             * @instance
             */
            ParameterDescription.prototype.minimum = null;

            /**
             * ParameterDescription name.
             * @member {string|null|undefined} name
             * @memberof northstar.research.ParameterDescription
             * @instance
             */
            ParameterDescription.prototype.name = null;

            /**
             * ParameterDescription unit.
             * @member {string|null|undefined} unit
             * @memberof northstar.research.ParameterDescription
             * @instance
             */
            ParameterDescription.prototype.unit = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * ParameterDescription _default.
             * @member {"default"|undefined} _default
             * @memberof northstar.research.ParameterDescription
             * @instance
             */
            $Object.defineProperty(ParameterDescription.prototype, "_default", {
                get: $util.oneOfGetter($oneOfFields = ["default"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ParameterDescription _label.
             * @member {"label"|undefined} _label
             * @memberof northstar.research.ParameterDescription
             * @instance
             */
            $Object.defineProperty(ParameterDescription.prototype, "_label", {
                get: $util.oneOfGetter($oneOfFields = ["label"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ParameterDescription _maximum.
             * @member {"maximum"|undefined} _maximum
             * @memberof northstar.research.ParameterDescription
             * @instance
             */
            $Object.defineProperty(ParameterDescription.prototype, "_maximum", {
                get: $util.oneOfGetter($oneOfFields = ["maximum"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ParameterDescription _minimum.
             * @member {"minimum"|undefined} _minimum
             * @memberof northstar.research.ParameterDescription
             * @instance
             */
            $Object.defineProperty(ParameterDescription.prototype, "_minimum", {
                get: $util.oneOfGetter($oneOfFields = ["minimum"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ParameterDescription _name.
             * @member {"name"|undefined} _name
             * @memberof northstar.research.ParameterDescription
             * @instance
             */
            $Object.defineProperty(ParameterDescription.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ParameterDescription _unit.
             * @member {"unit"|undefined} _unit
             * @memberof northstar.research.ParameterDescription
             * @instance
             */
            $Object.defineProperty(ParameterDescription.prototype, "_unit", {
                get: $util.oneOfGetter($oneOfFields = ["unit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ParameterDescription instance using the specified properties.
             * @function create
             * @memberof northstar.research.ParameterDescription
             * @static
             * @param {northstar.research.ParameterDescription.$Properties=} [properties] Properties to set
             * @returns {northstar.research.ParameterDescription} ParameterDescription instance
             * @type {{
             *   (properties: northstar.research.ParameterDescription.$Shape): northstar.research.ParameterDescription & northstar.research.ParameterDescription.$Shape;
             *   (properties?: northstar.research.ParameterDescription.$Properties): northstar.research.ParameterDescription;
             * }}
             */
            ParameterDescription.create = function(properties) {
                return new ParameterDescription(properties);
            };

            /**
             * Encodes the specified ParameterDescription message. Does not implicitly {@link northstar.research.ParameterDescription.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.ParameterDescription
             * @static
             * @param {northstar.research.ParameterDescription.$Properties} message ParameterDescription message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ParameterDescription.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message["default"] != null && $Object.hasOwnProperty.call(message, "default"))
                    $root.google.protobuf.Value.encode(message["default"], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.label);
                if (message.maximum != null && $Object.hasOwnProperty.call(message, "maximum"))
                    $root.google.protobuf.Value.encode(message.maximum, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.minimum != null && $Object.hasOwnProperty.call(message, "minimum"))
                    $root.google.protobuf.Value.encode(message.minimum, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.name);
                if (message.unit != null && $Object.hasOwnProperty.call(message, "unit"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.unit);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a ParameterDescription message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.ParameterDescription
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.ParameterDescription & northstar.research.ParameterDescription.$Shape} ParameterDescription
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ParameterDescription.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.ParameterDescription();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message["default"] = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, message["default"]);
                            message._default = "default";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.label = reader.stringVerify();
                            message._label = "label";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.maximum = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, message.maximum);
                            message._maximum = "maximum";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.minimum = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, message.minimum);
                            message._minimum = "minimum";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.unit = reader.stringVerify();
                            message._unit = "unit";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a ParameterDescription message.
             * @function verify
             * @memberof northstar.research.ParameterDescription
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ParameterDescription.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message["default"] != null && $Object.hasOwnProperty.call(message, "default")) {
                    properties._default = 1;
                    {
                        let error = $root.google.protobuf.Value.verify(message["default"], _depth + 1);
                        if (error)
                            return "default." + error;
                    }
                }
                if (message.label != null && $Object.hasOwnProperty.call(message, "label")) {
                    properties._label = 1;
                    if (!$util.isString(message.label))
                        return "label: string expected";
                }
                if (message.maximum != null && $Object.hasOwnProperty.call(message, "maximum")) {
                    properties._maximum = 1;
                    {
                        let error = $root.google.protobuf.Value.verify(message.maximum, _depth + 1);
                        if (error)
                            return "maximum." + error;
                    }
                }
                if (message.minimum != null && $Object.hasOwnProperty.call(message, "minimum")) {
                    properties._minimum = 1;
                    {
                        let error = $root.google.protobuf.Value.verify(message.minimum, _depth + 1);
                        if (error)
                            return "minimum." + error;
                    }
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                if (message.unit != null && $Object.hasOwnProperty.call(message, "unit")) {
                    properties._unit = 1;
                    if (!$util.isString(message.unit))
                        return "unit: string expected";
                }
                return null;
            };

            /**
             * Creates a ParameterDescription message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.ParameterDescription
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.ParameterDescription} ParameterDescription
             */
            ParameterDescription.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.ParameterDescription)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.ParameterDescription: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.ParameterDescription();
                if (object["default"] != null) {
                    if (!$util.isObject(object["default"]))
                        throw $TypeError(".northstar.research.ParameterDescription.default: object expected");
                    message["default"] = $root.google.protobuf.Value.fromObject(object["default"], _depth + 1);
                }
                if (object.label != null)
                    message.label = $String(object.label);
                if (object.maximum != null) {
                    if (!$util.isObject(object.maximum))
                        throw $TypeError(".northstar.research.ParameterDescription.maximum: object expected");
                    message.maximum = $root.google.protobuf.Value.fromObject(object.maximum, _depth + 1);
                }
                if (object.minimum != null) {
                    if (!$util.isObject(object.minimum))
                        throw $TypeError(".northstar.research.ParameterDescription.minimum: object expected");
                    message.minimum = $root.google.protobuf.Value.fromObject(object.minimum, _depth + 1);
                }
                if (object.name != null)
                    message.name = $String(object.name);
                if (object.unit != null)
                    message.unit = $String(object.unit);
                return message;
            };

            /**
             * Creates a plain object from a ParameterDescription message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.ParameterDescription
             * @static
             * @param {northstar.research.ParameterDescription} message ParameterDescription
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ParameterDescription.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message["default"] != null && $Object.hasOwnProperty.call(message, "default")) {
                    object["default"] = $root.google.protobuf.Value.toObject(message["default"], options, _depth + 1);
                    if (options.oneofs)
                        object._default = "default";
                }
                if (message.label != null && $Object.hasOwnProperty.call(message, "label")) {
                    object.label = message.label;
                    if (options.oneofs)
                        object._label = "label";
                }
                if (message.maximum != null && $Object.hasOwnProperty.call(message, "maximum")) {
                    object.maximum = $root.google.protobuf.Value.toObject(message.maximum, options, _depth + 1);
                    if (options.oneofs)
                        object._maximum = "maximum";
                }
                if (message.minimum != null && $Object.hasOwnProperty.call(message, "minimum")) {
                    object.minimum = $root.google.protobuf.Value.toObject(message.minimum, options, _depth + 1);
                    if (options.oneofs)
                        object._minimum = "minimum";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    object.name = message.name;
                    if (options.oneofs)
                        object._name = "name";
                }
                if (message.unit != null && $Object.hasOwnProperty.call(message, "unit")) {
                    object.unit = message.unit;
                    if (options.oneofs)
                        object._unit = "unit";
                }
                return object;
            };

            /**
             * Converts this ParameterDescription to JSON.
             * @function toJSON
             * @memberof northstar.research.ParameterDescription
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ParameterDescription.prototype.toJSON = function() {
                return ParameterDescription.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ParameterDescription
             * @function getTypeUrl
             * @memberof northstar.research.ParameterDescription
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ParameterDescription.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.ParameterDescription";
            };

            return ParameterDescription;
        })();

        research.PublishRequest = (function() {

            /**
             * Properties of a PublishRequest.
             * @typedef {Object} northstar.research.PublishRequest.$Properties
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a PublishRequest.
             * @memberof northstar.research
             * @interface IPublishRequest
             * @augments northstar.research.PublishRequest.$Properties
             * @deprecated Use northstar.research.PublishRequest.$Properties instead.
             */

            /**
             * Shape of a PublishRequest.
             * @typedef {northstar.research.PublishRequest.$Properties} northstar.research.PublishRequest.$Shape
             */

            /**
             * Constructs a new PublishRequest.
             * @memberof northstar.research
             * @classdesc Represents a PublishRequest.
             * @constructor
             * @param {northstar.research.PublishRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const PublishRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Creates a new PublishRequest instance using the specified properties.
             * @function create
             * @memberof northstar.research.PublishRequest
             * @static
             * @param {northstar.research.PublishRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.research.PublishRequest} PublishRequest instance
             * @type {{
             *   (properties: northstar.research.PublishRequest.$Shape): northstar.research.PublishRequest & northstar.research.PublishRequest.$Shape;
             *   (properties?: northstar.research.PublishRequest.$Properties): northstar.research.PublishRequest;
             * }}
             */
            PublishRequest.create = function(properties) {
                return new PublishRequest(properties);
            };

            /**
             * Encodes the specified PublishRequest message. Does not implicitly {@link northstar.research.PublishRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.PublishRequest
             * @static
             * @param {northstar.research.PublishRequest.$Properties} message PublishRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PublishRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a PublishRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.PublishRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.PublishRequest & northstar.research.PublishRequest.$Shape} PublishRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PublishRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.PublishRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    reader.skipType(tag & 7, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a PublishRequest message.
             * @function verify
             * @memberof northstar.research.PublishRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PublishRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                return null;
            };

            /**
             * Creates a PublishRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.PublishRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.PublishRequest} PublishRequest
             */
            PublishRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.PublishRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.PublishRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                return new $root.northstar.research.PublishRequest();
            };

            /**
             * Creates a plain object from a PublishRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.PublishRequest
             * @static
             * @param {northstar.research.PublishRequest} message PublishRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PublishRequest.toObject = function () {
                return {};
            };

            /**
             * Converts this PublishRequest to JSON.
             * @function toJSON
             * @memberof northstar.research.PublishRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PublishRequest.prototype.toJSON = function() {
                return PublishRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for PublishRequest
             * @function getTypeUrl
             * @memberof northstar.research.PublishRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            PublishRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.PublishRequest";
            };

            return PublishRequest;
        })();

        research.Readiness = (function() {

            /**
             * Properties of a Readiness.
             * @typedef {Object} northstar.research.Readiness.$Properties
             * @property {string|null} [status] Readiness status
             * @property {"status"} [_status] Readiness _status
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Readiness.
             * @memberof northstar.research
             * @interface IReadiness
             * @augments northstar.research.Readiness.$Properties
             * @deprecated Use northstar.research.Readiness.$Properties instead.
             */

            /**
             * Narrowed shape of a Readiness.
             * @typedef {{
             *   status?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _status?: undefined; status?: null }|{ _status?: "status"; status: string })
             * )} northstar.research.Readiness.$Shape
             */

            /**
             * Constructs a new Readiness.
             * @memberof northstar.research
             * @classdesc Represents a Readiness.
             * @constructor
             * @param {northstar.research.Readiness.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const Readiness = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Readiness status.
             * @member {string|null|undefined} status
             * @memberof northstar.research.Readiness
             * @instance
             */
            Readiness.prototype.status = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * Readiness _status.
             * @member {"status"|undefined} _status
             * @memberof northstar.research.Readiness
             * @instance
             */
            $Object.defineProperty(Readiness.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new Readiness instance using the specified properties.
             * @function create
             * @memberof northstar.research.Readiness
             * @static
             * @param {northstar.research.Readiness.$Properties=} [properties] Properties to set
             * @returns {northstar.research.Readiness} Readiness instance
             * @type {{
             *   (properties: northstar.research.Readiness.$Shape): northstar.research.Readiness & northstar.research.Readiness.$Shape;
             *   (properties?: northstar.research.Readiness.$Properties): northstar.research.Readiness;
             * }}
             */
            Readiness.create = function(properties) {
                return new Readiness(properties);
            };

            /**
             * Encodes the specified Readiness message. Does not implicitly {@link northstar.research.Readiness.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.Readiness
             * @static
             * @param {northstar.research.Readiness.$Properties} message Readiness message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Readiness.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a Readiness message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.Readiness
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.Readiness & northstar.research.Readiness.$Shape} Readiness
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Readiness.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.Readiness();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.status = reader.stringVerify();
                            message._status = "status";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a Readiness message.
             * @function verify
             * @memberof northstar.research.Readiness
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Readiness.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (!$util.isString(message.status))
                        return "status: string expected";
                }
                return null;
            };

            /**
             * Creates a Readiness message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.Readiness
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.Readiness} Readiness
             */
            Readiness.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.Readiness)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.Readiness: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.Readiness();
                if (object.status != null)
                    message.status = $String(object.status);
                return message;
            };

            /**
             * Creates a plain object from a Readiness message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.Readiness
             * @static
             * @param {northstar.research.Readiness} message Readiness
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Readiness.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    object.status = message.status;
                    if (options.oneofs)
                        object._status = "status";
                }
                return object;
            };

            /**
             * Converts this Readiness to JSON.
             * @function toJSON
             * @memberof northstar.research.Readiness
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Readiness.prototype.toJSON = function() {
                return Readiness.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Readiness
             * @function getTypeUrl
             * @memberof northstar.research.Readiness
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Readiness.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.Readiness";
            };

            return Readiness;
        })();

        research.ResearchAttempt = (function() {

            /**
             * Properties of a ResearchAttempt.
             * @typedef {Object} northstar.research.ResearchAttempt.$Properties
             * @property {string|null} [attempt_id] ResearchAttempt attempt_id
             * @property {string|null} [created_at] ResearchAttempt created_at
             * @property {string|null} [error] ResearchAttempt error
             * @property {string|null} [run_id] ResearchAttempt run_id
             * @property {string|null} [status] ResearchAttempt status
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] ResearchAttempt evidence_fields
             * @property {Array.<string>|null} [null_fields] ResearchAttempt null_fields
             * @property {"attempt_id"} [_attempt_id] ResearchAttempt _attempt_id
             * @property {"created_at"} [_created_at] ResearchAttempt _created_at
             * @property {"error"} [_error] ResearchAttempt _error
             * @property {"run_id"} [_run_id] ResearchAttempt _run_id
             * @property {"status"} [_status] ResearchAttempt _status
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ResearchAttempt.
             * @memberof northstar.research
             * @interface IResearchAttempt
             * @augments northstar.research.ResearchAttempt.$Properties
             * @deprecated Use northstar.research.ResearchAttempt.$Properties instead.
             */

            /**
             * Narrowed shape of a ResearchAttempt.
             * @typedef {{
             *   attempt_id?: string|null;
             *   created_at?: string|null;
             *   error?: string|null;
             *   run_id?: string|null;
             *   status?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   null_fields?: Array.<string>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _attempt_id?: undefined; attempt_id?: null }|{ _attempt_id?: "attempt_id"; attempt_id: string })
             * ) & (
             *   ({ _created_at?: undefined; created_at?: null }|{ _created_at?: "created_at"; created_at: string })
             * ) & (
             *   ({ _error?: undefined; error?: null }|{ _error?: "error"; error: string })
             * ) & (
             *   ({ _run_id?: undefined; run_id?: null }|{ _run_id?: "run_id"; run_id: string })
             * ) & (
             *   ({ _status?: undefined; status?: null }|{ _status?: "status"; status: string })
             * )} northstar.research.ResearchAttempt.$Shape
             */

            /**
             * Constructs a new ResearchAttempt.
             * @memberof northstar.research
             * @classdesc Represents a ResearchAttempt.
             * @constructor
             * @param {northstar.research.ResearchAttempt.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ResearchAttempt = function (properties) {
                this.evidence_fields = {};
                this.null_fields = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ResearchAttempt attempt_id.
             * @member {string|null|undefined} attempt_id
             * @memberof northstar.research.ResearchAttempt
             * @instance
             */
            ResearchAttempt.prototype.attempt_id = null;

            /**
             * ResearchAttempt created_at.
             * @member {string|null|undefined} created_at
             * @memberof northstar.research.ResearchAttempt
             * @instance
             */
            ResearchAttempt.prototype.created_at = null;

            /**
             * ResearchAttempt error.
             * @member {string|null|undefined} error
             * @memberof northstar.research.ResearchAttempt
             * @instance
             */
            ResearchAttempt.prototype.error = null;

            /**
             * ResearchAttempt run_id.
             * @member {string|null|undefined} run_id
             * @memberof northstar.research.ResearchAttempt
             * @instance
             */
            ResearchAttempt.prototype.run_id = null;

            /**
             * ResearchAttempt status.
             * @member {string|null|undefined} status
             * @memberof northstar.research.ResearchAttempt
             * @instance
             */
            ResearchAttempt.prototype.status = null;

            /**
             * ResearchAttempt evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.research.ResearchAttempt
             * @instance
             */
            ResearchAttempt.prototype.evidence_fields = $util.emptyObject;

            /**
             * ResearchAttempt null_fields.
             * @member {Array.<string>} null_fields
             * @memberof northstar.research.ResearchAttempt
             * @instance
             */
            ResearchAttempt.prototype.null_fields = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * ResearchAttempt _attempt_id.
             * @member {"attempt_id"|undefined} _attempt_id
             * @memberof northstar.research.ResearchAttempt
             * @instance
             */
            $Object.defineProperty(ResearchAttempt.prototype, "_attempt_id", {
                get: $util.oneOfGetter($oneOfFields = ["attempt_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchAttempt _created_at.
             * @member {"created_at"|undefined} _created_at
             * @memberof northstar.research.ResearchAttempt
             * @instance
             */
            $Object.defineProperty(ResearchAttempt.prototype, "_created_at", {
                get: $util.oneOfGetter($oneOfFields = ["created_at"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchAttempt _error.
             * @member {"error"|undefined} _error
             * @memberof northstar.research.ResearchAttempt
             * @instance
             */
            $Object.defineProperty(ResearchAttempt.prototype, "_error", {
                get: $util.oneOfGetter($oneOfFields = ["error"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchAttempt _run_id.
             * @member {"run_id"|undefined} _run_id
             * @memberof northstar.research.ResearchAttempt
             * @instance
             */
            $Object.defineProperty(ResearchAttempt.prototype, "_run_id", {
                get: $util.oneOfGetter($oneOfFields = ["run_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchAttempt _status.
             * @member {"status"|undefined} _status
             * @memberof northstar.research.ResearchAttempt
             * @instance
             */
            $Object.defineProperty(ResearchAttempt.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ResearchAttempt instance using the specified properties.
             * @function create
             * @memberof northstar.research.ResearchAttempt
             * @static
             * @param {northstar.research.ResearchAttempt.$Properties=} [properties] Properties to set
             * @returns {northstar.research.ResearchAttempt} ResearchAttempt instance
             * @type {{
             *   (properties: northstar.research.ResearchAttempt.$Shape): northstar.research.ResearchAttempt & northstar.research.ResearchAttempt.$Shape;
             *   (properties?: northstar.research.ResearchAttempt.$Properties): northstar.research.ResearchAttempt;
             * }}
             */
            ResearchAttempt.create = function(properties) {
                return new ResearchAttempt(properties);
            };

            /**
             * Encodes the specified ResearchAttempt message. Does not implicitly {@link northstar.research.ResearchAttempt.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.ResearchAttempt
             * @static
             * @param {northstar.research.ResearchAttempt.$Properties} message ResearchAttempt message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ResearchAttempt.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.attempt_id != null && $Object.hasOwnProperty.call(message, "attempt_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.attempt_id);
                if (message.created_at != null && $Object.hasOwnProperty.call(message, "created_at"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.created_at);
                if (message.error != null && $Object.hasOwnProperty.call(message, "error"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.error);
                if (message.run_id != null && $Object.hasOwnProperty.call(message, "run_id"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.run_id);
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.status);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.null_fields != null && message.null_fields.length)
                    for (let i = 0; i < message.null_fields.length; ++i)
                        writer.uint32(/* id 2046, wireType 2 =*/16370).string(message.null_fields[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a ResearchAttempt message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.ResearchAttempt
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.ResearchAttempt & northstar.research.ResearchAttempt.$Shape} ResearchAttempt
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ResearchAttempt.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.ResearchAttempt();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.attempt_id = reader.stringVerify();
                            message._attempt_id = "attempt_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.created_at = reader.stringVerify();
                            message._created_at = "created_at";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.error = reader.stringVerify();
                            message._error = "error";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.run_id = reader.stringVerify();
                            message._run_id = "run_id";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.status = reader.stringVerify();
                            message._status = "status";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    case 2046: {
                            if (wireType !== 2)
                                break;
                            if (!(message.null_fields && message.null_fields.length))
                                message.null_fields = [];
                            message.null_fields.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a ResearchAttempt message.
             * @function verify
             * @memberof northstar.research.ResearchAttempt
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ResearchAttempt.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.attempt_id != null && $Object.hasOwnProperty.call(message, "attempt_id")) {
                    properties._attempt_id = 1;
                    if (!$util.isString(message.attempt_id))
                        return "attempt_id: string expected";
                }
                if (message.created_at != null && $Object.hasOwnProperty.call(message, "created_at")) {
                    properties._created_at = 1;
                    if (!$util.isString(message.created_at))
                        return "created_at: string expected";
                }
                if (message.error != null && $Object.hasOwnProperty.call(message, "error")) {
                    properties._error = 1;
                    if (!$util.isString(message.error))
                        return "error: string expected";
                }
                if (message.run_id != null && $Object.hasOwnProperty.call(message, "run_id")) {
                    properties._run_id = 1;
                    if (!$util.isString(message.run_id))
                        return "run_id: string expected";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (!$util.isString(message.status))
                        return "status: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                if (message.null_fields != null && $Object.hasOwnProperty.call(message, "null_fields")) {
                    if (!$Array.isArray(message.null_fields))
                        return "null_fields: array expected";
                    for (let i = 0; i < message.null_fields.length; ++i)
                        if (!$util.isString(message.null_fields[i]))
                            return "null_fields: string[] expected";
                }
                return null;
            };

            /**
             * Creates a ResearchAttempt message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.ResearchAttempt
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.ResearchAttempt} ResearchAttempt
             */
            ResearchAttempt.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.ResearchAttempt)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.ResearchAttempt: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.ResearchAttempt();
                if (object.attempt_id != null)
                    message.attempt_id = $String(object.attempt_id);
                if (object.created_at != null)
                    message.created_at = $String(object.created_at);
                if (object.error != null)
                    message.error = $String(object.error);
                if (object.run_id != null)
                    message.run_id = $String(object.run_id);
                if (object.status != null)
                    message.status = $String(object.status);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.research.ResearchAttempt.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.research.ResearchAttempt.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                if (object.null_fields) {
                    if (!$Array.isArray(object.null_fields))
                        throw $TypeError(".northstar.research.ResearchAttempt.null_fields: array expected");
                    message.null_fields = $Array(object.null_fields.length);
                    for (let i = 0; i < object.null_fields.length; ++i)
                        message.null_fields[i] = $String(object.null_fields[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a ResearchAttempt message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.ResearchAttempt
             * @static
             * @param {northstar.research.ResearchAttempt} message ResearchAttempt
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ResearchAttempt.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.null_fields = [];
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.attempt_id != null && $Object.hasOwnProperty.call(message, "attempt_id")) {
                    object.attempt_id = message.attempt_id;
                    if (options.oneofs)
                        object._attempt_id = "attempt_id";
                }
                if (message.created_at != null && $Object.hasOwnProperty.call(message, "created_at")) {
                    object.created_at = message.created_at;
                    if (options.oneofs)
                        object._created_at = "created_at";
                }
                if (message.error != null && $Object.hasOwnProperty.call(message, "error")) {
                    object.error = message.error;
                    if (options.oneofs)
                        object._error = "error";
                }
                if (message.run_id != null && $Object.hasOwnProperty.call(message, "run_id")) {
                    object.run_id = message.run_id;
                    if (options.oneofs)
                        object._run_id = "run_id";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    object.status = message.status;
                    if (options.oneofs)
                        object._status = "status";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                if (message.null_fields && message.null_fields.length) {
                    object.null_fields = $Array(message.null_fields.length);
                    for (let j = 0; j < message.null_fields.length; ++j)
                        object.null_fields[j] = message.null_fields[j];
                }
                return object;
            };

            /**
             * Converts this ResearchAttempt to JSON.
             * @function toJSON
             * @memberof northstar.research.ResearchAttempt
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ResearchAttempt.prototype.toJSON = function() {
                return ResearchAttempt.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ResearchAttempt
             * @function getTypeUrl
             * @memberof northstar.research.ResearchAttempt
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ResearchAttempt.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.ResearchAttempt";
            };

            return ResearchAttempt;
        })();

        research.ResearchConfiguration = (function() {

            /**
             * Properties of a ResearchConfiguration.
             * @typedef {Object} northstar.research.ResearchConfiguration.$Properties
             * @property {northstar.research.RiskInput.$Properties|null} [risk] ResearchConfiguration risk
             * @property {northstar.research.SimulationInput.$Properties|null} [simulation] ResearchConfiguration simulation
             * @property {northstar.research.FixedStrategy.$Properties|null} [strategy] ResearchConfiguration strategy
             * @property {"risk"} [_risk] ResearchConfiguration _risk
             * @property {"simulation"} [_simulation] ResearchConfiguration _simulation
             * @property {"strategy"} [_strategy] ResearchConfiguration _strategy
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ResearchConfiguration.
             * @memberof northstar.research
             * @interface IResearchConfiguration
             * @augments northstar.research.ResearchConfiguration.$Properties
             * @deprecated Use northstar.research.ResearchConfiguration.$Properties instead.
             */

            /**
             * Narrowed shape of a ResearchConfiguration.
             * @typedef {{
             *   risk?: northstar.research.RiskInput.$Shape|null;
             *   simulation?: northstar.research.SimulationInput.$Shape|null;
             *   strategy?: northstar.research.FixedStrategy.$Shape|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _risk?: undefined; risk?: null }|{ _risk?: "risk"; risk: northstar.research.RiskInput.$Shape })
             * ) & (
             *   ({ _simulation?: undefined; simulation?: null }|{ _simulation?: "simulation"; simulation: northstar.research.SimulationInput.$Shape })
             * ) & (
             *   ({ _strategy?: undefined; strategy?: null }|{ _strategy?: "strategy"; strategy: northstar.research.FixedStrategy.$Shape })
             * )} northstar.research.ResearchConfiguration.$Shape
             */

            /**
             * Constructs a new ResearchConfiguration.
             * @memberof northstar.research
             * @classdesc Represents a ResearchConfiguration.
             * @constructor
             * @param {northstar.research.ResearchConfiguration.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ResearchConfiguration = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ResearchConfiguration risk.
             * @member {northstar.research.RiskInput.$Properties|null|undefined} risk
             * @memberof northstar.research.ResearchConfiguration
             * @instance
             */
            ResearchConfiguration.prototype.risk = null;

            /**
             * ResearchConfiguration simulation.
             * @member {northstar.research.SimulationInput.$Properties|null|undefined} simulation
             * @memberof northstar.research.ResearchConfiguration
             * @instance
             */
            ResearchConfiguration.prototype.simulation = null;

            /**
             * ResearchConfiguration strategy.
             * @member {northstar.research.FixedStrategy.$Properties|null|undefined} strategy
             * @memberof northstar.research.ResearchConfiguration
             * @instance
             */
            ResearchConfiguration.prototype.strategy = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * ResearchConfiguration _risk.
             * @member {"risk"|undefined} _risk
             * @memberof northstar.research.ResearchConfiguration
             * @instance
             */
            $Object.defineProperty(ResearchConfiguration.prototype, "_risk", {
                get: $util.oneOfGetter($oneOfFields = ["risk"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchConfiguration _simulation.
             * @member {"simulation"|undefined} _simulation
             * @memberof northstar.research.ResearchConfiguration
             * @instance
             */
            $Object.defineProperty(ResearchConfiguration.prototype, "_simulation", {
                get: $util.oneOfGetter($oneOfFields = ["simulation"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchConfiguration _strategy.
             * @member {"strategy"|undefined} _strategy
             * @memberof northstar.research.ResearchConfiguration
             * @instance
             */
            $Object.defineProperty(ResearchConfiguration.prototype, "_strategy", {
                get: $util.oneOfGetter($oneOfFields = ["strategy"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ResearchConfiguration instance using the specified properties.
             * @function create
             * @memberof northstar.research.ResearchConfiguration
             * @static
             * @param {northstar.research.ResearchConfiguration.$Properties=} [properties] Properties to set
             * @returns {northstar.research.ResearchConfiguration} ResearchConfiguration instance
             * @type {{
             *   (properties: northstar.research.ResearchConfiguration.$Shape): northstar.research.ResearchConfiguration & northstar.research.ResearchConfiguration.$Shape;
             *   (properties?: northstar.research.ResearchConfiguration.$Properties): northstar.research.ResearchConfiguration;
             * }}
             */
            ResearchConfiguration.create = function(properties) {
                return new ResearchConfiguration(properties);
            };

            /**
             * Encodes the specified ResearchConfiguration message. Does not implicitly {@link northstar.research.ResearchConfiguration.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.ResearchConfiguration
             * @static
             * @param {northstar.research.ResearchConfiguration.$Properties} message ResearchConfiguration message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ResearchConfiguration.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.risk != null && $Object.hasOwnProperty.call(message, "risk"))
                    $root.northstar.research.RiskInput.encode(message.risk, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.simulation != null && $Object.hasOwnProperty.call(message, "simulation"))
                    $root.northstar.research.SimulationInput.encode(message.simulation, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.strategy != null && $Object.hasOwnProperty.call(message, "strategy"))
                    $root.northstar.research.FixedStrategy.encode(message.strategy, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a ResearchConfiguration message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.ResearchConfiguration
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.ResearchConfiguration & northstar.research.ResearchConfiguration.$Shape} ResearchConfiguration
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ResearchConfiguration.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.ResearchConfiguration();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.risk = $root.northstar.research.RiskInput.decode(reader, reader.uint32(), $undefined, _depth + 1, message.risk);
                            message._risk = "risk";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.simulation = $root.northstar.research.SimulationInput.decode(reader, reader.uint32(), $undefined, _depth + 1, message.simulation);
                            message._simulation = "simulation";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.strategy = $root.northstar.research.FixedStrategy.decode(reader, reader.uint32(), $undefined, _depth + 1, message.strategy);
                            message._strategy = "strategy";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a ResearchConfiguration message.
             * @function verify
             * @memberof northstar.research.ResearchConfiguration
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ResearchConfiguration.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.risk != null && $Object.hasOwnProperty.call(message, "risk")) {
                    properties._risk = 1;
                    {
                        let error = $root.northstar.research.RiskInput.verify(message.risk, _depth + 1);
                        if (error)
                            return "risk." + error;
                    }
                }
                if (message.simulation != null && $Object.hasOwnProperty.call(message, "simulation")) {
                    properties._simulation = 1;
                    {
                        let error = $root.northstar.research.SimulationInput.verify(message.simulation, _depth + 1);
                        if (error)
                            return "simulation." + error;
                    }
                }
                if (message.strategy != null && $Object.hasOwnProperty.call(message, "strategy")) {
                    properties._strategy = 1;
                    {
                        let error = $root.northstar.research.FixedStrategy.verify(message.strategy, _depth + 1);
                        if (error)
                            return "strategy." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a ResearchConfiguration message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.ResearchConfiguration
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.ResearchConfiguration} ResearchConfiguration
             */
            ResearchConfiguration.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.ResearchConfiguration)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.ResearchConfiguration: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.ResearchConfiguration();
                if (object.risk != null) {
                    if (!$util.isObject(object.risk))
                        throw $TypeError(".northstar.research.ResearchConfiguration.risk: object expected");
                    message.risk = $root.northstar.research.RiskInput.fromObject(object.risk, _depth + 1);
                }
                if (object.simulation != null) {
                    if (!$util.isObject(object.simulation))
                        throw $TypeError(".northstar.research.ResearchConfiguration.simulation: object expected");
                    message.simulation = $root.northstar.research.SimulationInput.fromObject(object.simulation, _depth + 1);
                }
                if (object.strategy != null) {
                    if (!$util.isObject(object.strategy))
                        throw $TypeError(".northstar.research.ResearchConfiguration.strategy: object expected");
                    message.strategy = $root.northstar.research.FixedStrategy.fromObject(object.strategy, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a ResearchConfiguration message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.ResearchConfiguration
             * @static
             * @param {northstar.research.ResearchConfiguration} message ResearchConfiguration
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ResearchConfiguration.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.risk != null && $Object.hasOwnProperty.call(message, "risk")) {
                    object.risk = $root.northstar.research.RiskInput.toObject(message.risk, options, _depth + 1);
                    if (options.oneofs)
                        object._risk = "risk";
                }
                if (message.simulation != null && $Object.hasOwnProperty.call(message, "simulation")) {
                    object.simulation = $root.northstar.research.SimulationInput.toObject(message.simulation, options, _depth + 1);
                    if (options.oneofs)
                        object._simulation = "simulation";
                }
                if (message.strategy != null && $Object.hasOwnProperty.call(message, "strategy")) {
                    object.strategy = $root.northstar.research.FixedStrategy.toObject(message.strategy, options, _depth + 1);
                    if (options.oneofs)
                        object._strategy = "strategy";
                }
                return object;
            };

            /**
             * Converts this ResearchConfiguration to JSON.
             * @function toJSON
             * @memberof northstar.research.ResearchConfiguration
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ResearchConfiguration.prototype.toJSON = function() {
                return ResearchConfiguration.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ResearchConfiguration
             * @function getTypeUrl
             * @memberof northstar.research.ResearchConfiguration
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ResearchConfiguration.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.ResearchConfiguration";
            };

            return ResearchConfiguration;
        })();

        research.ResearchConfigurationInput = (function() {

            /**
             * Properties of a ResearchConfigurationInput.
             * @typedef {Object} northstar.research.ResearchConfigurationInput.$Properties
             * @property {northstar.research.RiskInput.$Properties|null} [risk] ResearchConfigurationInput risk
             * @property {northstar.research.SimulationInput.$Properties|null} [simulation] ResearchConfigurationInput simulation
             * @property {google.protobuf.Value.$Properties|null} [strategy] ResearchConfigurationInput strategy
             * @property {Array.<string>|null} [null_fields] ResearchConfigurationInput null_fields
             * @property {"risk"} [_risk] ResearchConfigurationInput _risk
             * @property {"simulation"} [_simulation] ResearchConfigurationInput _simulation
             * @property {"strategy"} [_strategy] ResearchConfigurationInput _strategy
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ResearchConfigurationInput.
             * @memberof northstar.research
             * @interface IResearchConfigurationInput
             * @augments northstar.research.ResearchConfigurationInput.$Properties
             * @deprecated Use northstar.research.ResearchConfigurationInput.$Properties instead.
             */

            /**
             * Narrowed shape of a ResearchConfigurationInput.
             * @typedef {{
             *   risk?: northstar.research.RiskInput.$Shape|null;
             *   simulation?: northstar.research.SimulationInput.$Shape|null;
             *   strategy?: google.protobuf.Value.$Shape|null;
             *   null_fields?: Array.<string>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _risk?: undefined; risk?: null }|{ _risk?: "risk"; risk: northstar.research.RiskInput.$Shape })
             * ) & (
             *   ({ _simulation?: undefined; simulation?: null }|{ _simulation?: "simulation"; simulation: northstar.research.SimulationInput.$Shape })
             * ) & (
             *   ({ _strategy?: undefined; strategy?: null }|{ _strategy?: "strategy"; strategy: google.protobuf.Value.$Shape })
             * )} northstar.research.ResearchConfigurationInput.$Shape
             */

            /**
             * Constructs a new ResearchConfigurationInput.
             * @memberof northstar.research
             * @classdesc Represents a ResearchConfigurationInput.
             * @constructor
             * @param {northstar.research.ResearchConfigurationInput.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ResearchConfigurationInput = function (properties) {
                this.null_fields = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ResearchConfigurationInput risk.
             * @member {northstar.research.RiskInput.$Properties|null|undefined} risk
             * @memberof northstar.research.ResearchConfigurationInput
             * @instance
             */
            ResearchConfigurationInput.prototype.risk = null;

            /**
             * ResearchConfigurationInput simulation.
             * @member {northstar.research.SimulationInput.$Properties|null|undefined} simulation
             * @memberof northstar.research.ResearchConfigurationInput
             * @instance
             */
            ResearchConfigurationInput.prototype.simulation = null;

            /**
             * ResearchConfigurationInput strategy.
             * @member {google.protobuf.Value.$Properties|null|undefined} strategy
             * @memberof northstar.research.ResearchConfigurationInput
             * @instance
             */
            ResearchConfigurationInput.prototype.strategy = null;

            /**
             * ResearchConfigurationInput null_fields.
             * @member {Array.<string>} null_fields
             * @memberof northstar.research.ResearchConfigurationInput
             * @instance
             */
            ResearchConfigurationInput.prototype.null_fields = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * ResearchConfigurationInput _risk.
             * @member {"risk"|undefined} _risk
             * @memberof northstar.research.ResearchConfigurationInput
             * @instance
             */
            $Object.defineProperty(ResearchConfigurationInput.prototype, "_risk", {
                get: $util.oneOfGetter($oneOfFields = ["risk"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchConfigurationInput _simulation.
             * @member {"simulation"|undefined} _simulation
             * @memberof northstar.research.ResearchConfigurationInput
             * @instance
             */
            $Object.defineProperty(ResearchConfigurationInput.prototype, "_simulation", {
                get: $util.oneOfGetter($oneOfFields = ["simulation"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchConfigurationInput _strategy.
             * @member {"strategy"|undefined} _strategy
             * @memberof northstar.research.ResearchConfigurationInput
             * @instance
             */
            $Object.defineProperty(ResearchConfigurationInput.prototype, "_strategy", {
                get: $util.oneOfGetter($oneOfFields = ["strategy"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ResearchConfigurationInput instance using the specified properties.
             * @function create
             * @memberof northstar.research.ResearchConfigurationInput
             * @static
             * @param {northstar.research.ResearchConfigurationInput.$Properties=} [properties] Properties to set
             * @returns {northstar.research.ResearchConfigurationInput} ResearchConfigurationInput instance
             * @type {{
             *   (properties: northstar.research.ResearchConfigurationInput.$Shape): northstar.research.ResearchConfigurationInput & northstar.research.ResearchConfigurationInput.$Shape;
             *   (properties?: northstar.research.ResearchConfigurationInput.$Properties): northstar.research.ResearchConfigurationInput;
             * }}
             */
            ResearchConfigurationInput.create = function(properties) {
                return new ResearchConfigurationInput(properties);
            };

            /**
             * Encodes the specified ResearchConfigurationInput message. Does not implicitly {@link northstar.research.ResearchConfigurationInput.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.ResearchConfigurationInput
             * @static
             * @param {northstar.research.ResearchConfigurationInput.$Properties} message ResearchConfigurationInput message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ResearchConfigurationInput.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.risk != null && $Object.hasOwnProperty.call(message, "risk"))
                    $root.northstar.research.RiskInput.encode(message.risk, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.simulation != null && $Object.hasOwnProperty.call(message, "simulation"))
                    $root.northstar.research.SimulationInput.encode(message.simulation, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.strategy != null && $Object.hasOwnProperty.call(message, "strategy"))
                    $root.google.protobuf.Value.encode(message.strategy, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.null_fields != null && message.null_fields.length)
                    for (let i = 0; i < message.null_fields.length; ++i)
                        writer.uint32(/* id 2046, wireType 2 =*/16370).string(message.null_fields[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a ResearchConfigurationInput message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.ResearchConfigurationInput
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.ResearchConfigurationInput & northstar.research.ResearchConfigurationInput.$Shape} ResearchConfigurationInput
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ResearchConfigurationInput.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.ResearchConfigurationInput();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.risk = $root.northstar.research.RiskInput.decode(reader, reader.uint32(), $undefined, _depth + 1, message.risk);
                            message._risk = "risk";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.simulation = $root.northstar.research.SimulationInput.decode(reader, reader.uint32(), $undefined, _depth + 1, message.simulation);
                            message._simulation = "simulation";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.strategy = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, message.strategy);
                            message._strategy = "strategy";
                            continue;
                        }
                    case 2046: {
                            if (wireType !== 2)
                                break;
                            if (!(message.null_fields && message.null_fields.length))
                                message.null_fields = [];
                            message.null_fields.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a ResearchConfigurationInput message.
             * @function verify
             * @memberof northstar.research.ResearchConfigurationInput
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ResearchConfigurationInput.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.risk != null && $Object.hasOwnProperty.call(message, "risk")) {
                    properties._risk = 1;
                    {
                        let error = $root.northstar.research.RiskInput.verify(message.risk, _depth + 1);
                        if (error)
                            return "risk." + error;
                    }
                }
                if (message.simulation != null && $Object.hasOwnProperty.call(message, "simulation")) {
                    properties._simulation = 1;
                    {
                        let error = $root.northstar.research.SimulationInput.verify(message.simulation, _depth + 1);
                        if (error)
                            return "simulation." + error;
                    }
                }
                if (message.strategy != null && $Object.hasOwnProperty.call(message, "strategy")) {
                    properties._strategy = 1;
                    {
                        let error = $root.google.protobuf.Value.verify(message.strategy, _depth + 1);
                        if (error)
                            return "strategy." + error;
                    }
                }
                if (message.null_fields != null && $Object.hasOwnProperty.call(message, "null_fields")) {
                    if (!$Array.isArray(message.null_fields))
                        return "null_fields: array expected";
                    for (let i = 0; i < message.null_fields.length; ++i)
                        if (!$util.isString(message.null_fields[i]))
                            return "null_fields: string[] expected";
                }
                return null;
            };

            /**
             * Creates a ResearchConfigurationInput message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.ResearchConfigurationInput
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.ResearchConfigurationInput} ResearchConfigurationInput
             */
            ResearchConfigurationInput.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.ResearchConfigurationInput)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.ResearchConfigurationInput: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.ResearchConfigurationInput();
                if (object.risk != null) {
                    if (!$util.isObject(object.risk))
                        throw $TypeError(".northstar.research.ResearchConfigurationInput.risk: object expected");
                    message.risk = $root.northstar.research.RiskInput.fromObject(object.risk, _depth + 1);
                }
                if (object.simulation != null) {
                    if (!$util.isObject(object.simulation))
                        throw $TypeError(".northstar.research.ResearchConfigurationInput.simulation: object expected");
                    message.simulation = $root.northstar.research.SimulationInput.fromObject(object.simulation, _depth + 1);
                }
                if (object.strategy != null) {
                    if (!$util.isObject(object.strategy))
                        throw $TypeError(".northstar.research.ResearchConfigurationInput.strategy: object expected");
                    message.strategy = $root.google.protobuf.Value.fromObject(object.strategy, _depth + 1);
                }
                if (object.null_fields) {
                    if (!$Array.isArray(object.null_fields))
                        throw $TypeError(".northstar.research.ResearchConfigurationInput.null_fields: array expected");
                    message.null_fields = $Array(object.null_fields.length);
                    for (let i = 0; i < object.null_fields.length; ++i)
                        message.null_fields[i] = $String(object.null_fields[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a ResearchConfigurationInput message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.ResearchConfigurationInput
             * @static
             * @param {northstar.research.ResearchConfigurationInput} message ResearchConfigurationInput
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ResearchConfigurationInput.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.null_fields = [];
                if (message.risk != null && $Object.hasOwnProperty.call(message, "risk")) {
                    object.risk = $root.northstar.research.RiskInput.toObject(message.risk, options, _depth + 1);
                    if (options.oneofs)
                        object._risk = "risk";
                }
                if (message.simulation != null && $Object.hasOwnProperty.call(message, "simulation")) {
                    object.simulation = $root.northstar.research.SimulationInput.toObject(message.simulation, options, _depth + 1);
                    if (options.oneofs)
                        object._simulation = "simulation";
                }
                if (message.strategy != null && $Object.hasOwnProperty.call(message, "strategy")) {
                    object.strategy = $root.google.protobuf.Value.toObject(message.strategy, options, _depth + 1);
                    if (options.oneofs)
                        object._strategy = "strategy";
                }
                if (message.null_fields && message.null_fields.length) {
                    object.null_fields = $Array(message.null_fields.length);
                    for (let j = 0; j < message.null_fields.length; ++j)
                        object.null_fields[j] = message.null_fields[j];
                }
                return object;
            };

            /**
             * Converts this ResearchConfigurationInput to JSON.
             * @function toJSON
             * @memberof northstar.research.ResearchConfigurationInput
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ResearchConfigurationInput.prototype.toJSON = function() {
                return ResearchConfigurationInput.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ResearchConfigurationInput
             * @function getTypeUrl
             * @memberof northstar.research.ResearchConfigurationInput
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ResearchConfigurationInput.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.ResearchConfigurationInput";
            };

            return ResearchConfigurationInput;
        })();

        research.ResearchResultDocument = (function() {

            /**
             * Properties of a ResearchResultDocument.
             * @typedef {Object} northstar.research.ResearchResultDocument.$Properties
             * @property {google.protobuf.Struct.$Properties|null} [data] ResearchResultDocument data
             * @property {Array.<google.protobuf.Struct.$Properties>|null} [decisions] ResearchResultDocument decisions
             * @property {Array.<northstar.research.EquityPoint.$Properties>|null} [equity_curve] ResearchResultDocument equity_curve
             * @property {Array.<google.protobuf.Struct.$Properties>|null} [fills] ResearchResultDocument fills
             * @property {northstar.research.ResearchSummary.$Properties|null} [summary] ResearchResultDocument summary
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] ResearchResultDocument evidence_fields
             * @property {Array.<string>|null} [null_fields] ResearchResultDocument null_fields
             * @property {"data"} [_data] ResearchResultDocument _data
             * @property {"summary"} [_summary] ResearchResultDocument _summary
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ResearchResultDocument.
             * @memberof northstar.research
             * @interface IResearchResultDocument
             * @augments northstar.research.ResearchResultDocument.$Properties
             * @deprecated Use northstar.research.ResearchResultDocument.$Properties instead.
             */

            /**
             * Narrowed shape of a ResearchResultDocument.
             * @typedef {{
             *   data?: google.protobuf.Struct.$Shape|null;
             *   decisions?: Array.<google.protobuf.Struct.$Shape>|null;
             *   equity_curve?: Array.<northstar.research.EquityPoint.$Shape>|null;
             *   fills?: Array.<google.protobuf.Struct.$Shape>|null;
             *   summary?: northstar.research.ResearchSummary.$Shape|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   null_fields?: Array.<string>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _data?: undefined; data?: null }|{ _data?: "data"; data: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _summary?: undefined; summary?: null }|{ _summary?: "summary"; summary: northstar.research.ResearchSummary.$Shape })
             * )} northstar.research.ResearchResultDocument.$Shape
             */

            /**
             * Constructs a new ResearchResultDocument.
             * @memberof northstar.research
             * @classdesc Represents a ResearchResultDocument.
             * @constructor
             * @param {northstar.research.ResearchResultDocument.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ResearchResultDocument = function (properties) {
                this.decisions = [];
                this.equity_curve = [];
                this.fills = [];
                this.evidence_fields = {};
                this.null_fields = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ResearchResultDocument data.
             * @member {google.protobuf.Struct.$Properties|null|undefined} data
             * @memberof northstar.research.ResearchResultDocument
             * @instance
             */
            ResearchResultDocument.prototype.data = null;

            /**
             * ResearchResultDocument decisions.
             * @member {Array.<google.protobuf.Struct.$Properties>} decisions
             * @memberof northstar.research.ResearchResultDocument
             * @instance
             */
            ResearchResultDocument.prototype.decisions = $util.emptyArray;

            /**
             * ResearchResultDocument equity_curve.
             * @member {Array.<northstar.research.EquityPoint.$Properties>} equity_curve
             * @memberof northstar.research.ResearchResultDocument
             * @instance
             */
            ResearchResultDocument.prototype.equity_curve = $util.emptyArray;

            /**
             * ResearchResultDocument fills.
             * @member {Array.<google.protobuf.Struct.$Properties>} fills
             * @memberof northstar.research.ResearchResultDocument
             * @instance
             */
            ResearchResultDocument.prototype.fills = $util.emptyArray;

            /**
             * ResearchResultDocument summary.
             * @member {northstar.research.ResearchSummary.$Properties|null|undefined} summary
             * @memberof northstar.research.ResearchResultDocument
             * @instance
             */
            ResearchResultDocument.prototype.summary = null;

            /**
             * ResearchResultDocument evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.research.ResearchResultDocument
             * @instance
             */
            ResearchResultDocument.prototype.evidence_fields = $util.emptyObject;

            /**
             * ResearchResultDocument null_fields.
             * @member {Array.<string>} null_fields
             * @memberof northstar.research.ResearchResultDocument
             * @instance
             */
            ResearchResultDocument.prototype.null_fields = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * ResearchResultDocument _data.
             * @member {"data"|undefined} _data
             * @memberof northstar.research.ResearchResultDocument
             * @instance
             */
            $Object.defineProperty(ResearchResultDocument.prototype, "_data", {
                get: $util.oneOfGetter($oneOfFields = ["data"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchResultDocument _summary.
             * @member {"summary"|undefined} _summary
             * @memberof northstar.research.ResearchResultDocument
             * @instance
             */
            $Object.defineProperty(ResearchResultDocument.prototype, "_summary", {
                get: $util.oneOfGetter($oneOfFields = ["summary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ResearchResultDocument instance using the specified properties.
             * @function create
             * @memberof northstar.research.ResearchResultDocument
             * @static
             * @param {northstar.research.ResearchResultDocument.$Properties=} [properties] Properties to set
             * @returns {northstar.research.ResearchResultDocument} ResearchResultDocument instance
             * @type {{
             *   (properties: northstar.research.ResearchResultDocument.$Shape): northstar.research.ResearchResultDocument & northstar.research.ResearchResultDocument.$Shape;
             *   (properties?: northstar.research.ResearchResultDocument.$Properties): northstar.research.ResearchResultDocument;
             * }}
             */
            ResearchResultDocument.create = function(properties) {
                return new ResearchResultDocument(properties);
            };

            /**
             * Encodes the specified ResearchResultDocument message. Does not implicitly {@link northstar.research.ResearchResultDocument.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.ResearchResultDocument
             * @static
             * @param {northstar.research.ResearchResultDocument.$Properties} message ResearchResultDocument message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ResearchResultDocument.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    $root.google.protobuf.Struct.encode(message.data, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.decisions != null && message.decisions.length)
                    for (let i = 0; i < message.decisions.length; ++i)
                        $root.google.protobuf.Struct.encode(message.decisions[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.equity_curve != null && message.equity_curve.length)
                    for (let i = 0; i < message.equity_curve.length; ++i)
                        $root.northstar.research.EquityPoint.encode(message.equity_curve[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.fills != null && message.fills.length)
                    for (let i = 0; i < message.fills.length; ++i)
                        $root.google.protobuf.Struct.encode(message.fills[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                if (message.summary != null && $Object.hasOwnProperty.call(message, "summary"))
                    $root.northstar.research.ResearchSummary.encode(message.summary, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.null_fields != null && message.null_fields.length)
                    for (let i = 0; i < message.null_fields.length; ++i)
                        writer.uint32(/* id 2046, wireType 2 =*/16370).string(message.null_fields[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a ResearchResultDocument message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.ResearchResultDocument
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.ResearchResultDocument & northstar.research.ResearchResultDocument.$Shape} ResearchResultDocument
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ResearchResultDocument.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.ResearchResultDocument();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.data = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.data);
                            message._data = "data";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.decisions && message.decisions.length))
                                message.decisions = [];
                            message.decisions.push($root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if (!(message.equity_curve && message.equity_curve.length))
                                message.equity_curve = [];
                            message.equity_curve.push($root.northstar.research.EquityPoint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            if (!(message.fills && message.fills.length))
                                message.fills = [];
                            message.fills.push($root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.summary = $root.northstar.research.ResearchSummary.decode(reader, reader.uint32(), $undefined, _depth + 1, message.summary);
                            message._summary = "summary";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    case 2046: {
                            if (wireType !== 2)
                                break;
                            if (!(message.null_fields && message.null_fields.length))
                                message.null_fields = [];
                            message.null_fields.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a ResearchResultDocument message.
             * @function verify
             * @memberof northstar.research.ResearchResultDocument
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ResearchResultDocument.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.data != null && $Object.hasOwnProperty.call(message, "data")) {
                    properties._data = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.data, _depth + 1);
                        if (error)
                            return "data." + error;
                    }
                }
                if (message.decisions != null && $Object.hasOwnProperty.call(message, "decisions")) {
                    if (!$Array.isArray(message.decisions))
                        return "decisions: array expected";
                    for (let i = 0; i < message.decisions.length; ++i) {
                        let error = $root.google.protobuf.Struct.verify(message.decisions[i], _depth + 1);
                        if (error)
                            return "decisions." + error;
                    }
                }
                if (message.equity_curve != null && $Object.hasOwnProperty.call(message, "equity_curve")) {
                    if (!$Array.isArray(message.equity_curve))
                        return "equity_curve: array expected";
                    for (let i = 0; i < message.equity_curve.length; ++i) {
                        let error = $root.northstar.research.EquityPoint.verify(message.equity_curve[i], _depth + 1);
                        if (error)
                            return "equity_curve." + error;
                    }
                }
                if (message.fills != null && $Object.hasOwnProperty.call(message, "fills")) {
                    if (!$Array.isArray(message.fills))
                        return "fills: array expected";
                    for (let i = 0; i < message.fills.length; ++i) {
                        let error = $root.google.protobuf.Struct.verify(message.fills[i], _depth + 1);
                        if (error)
                            return "fills." + error;
                    }
                }
                if (message.summary != null && $Object.hasOwnProperty.call(message, "summary")) {
                    properties._summary = 1;
                    {
                        let error = $root.northstar.research.ResearchSummary.verify(message.summary, _depth + 1);
                        if (error)
                            return "summary." + error;
                    }
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                if (message.null_fields != null && $Object.hasOwnProperty.call(message, "null_fields")) {
                    if (!$Array.isArray(message.null_fields))
                        return "null_fields: array expected";
                    for (let i = 0; i < message.null_fields.length; ++i)
                        if (!$util.isString(message.null_fields[i]))
                            return "null_fields: string[] expected";
                }
                return null;
            };

            /**
             * Creates a ResearchResultDocument message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.ResearchResultDocument
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.ResearchResultDocument} ResearchResultDocument
             */
            ResearchResultDocument.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.ResearchResultDocument)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.ResearchResultDocument: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.ResearchResultDocument();
                if (object.data != null) {
                    if (!$util.isObject(object.data))
                        throw $TypeError(".northstar.research.ResearchResultDocument.data: object expected");
                    message.data = $root.google.protobuf.Struct.fromObject(object.data, _depth + 1);
                }
                if (object.decisions) {
                    if (!$Array.isArray(object.decisions))
                        throw $TypeError(".northstar.research.ResearchResultDocument.decisions: array expected");
                    message.decisions = $Array(object.decisions.length);
                    for (let i = 0; i < object.decisions.length; ++i) {
                        if (!$util.isObject(object.decisions[i]))
                            throw $TypeError(".northstar.research.ResearchResultDocument.decisions: object expected");
                        message.decisions[i] = $root.google.protobuf.Struct.fromObject(object.decisions[i], _depth + 1);
                    }
                }
                if (object.equity_curve) {
                    if (!$Array.isArray(object.equity_curve))
                        throw $TypeError(".northstar.research.ResearchResultDocument.equity_curve: array expected");
                    message.equity_curve = $Array(object.equity_curve.length);
                    for (let i = 0; i < object.equity_curve.length; ++i) {
                        if (!$util.isObject(object.equity_curve[i]))
                            throw $TypeError(".northstar.research.ResearchResultDocument.equity_curve: object expected");
                        message.equity_curve[i] = $root.northstar.research.EquityPoint.fromObject(object.equity_curve[i], _depth + 1);
                    }
                }
                if (object.fills) {
                    if (!$Array.isArray(object.fills))
                        throw $TypeError(".northstar.research.ResearchResultDocument.fills: array expected");
                    message.fills = $Array(object.fills.length);
                    for (let i = 0; i < object.fills.length; ++i) {
                        if (!$util.isObject(object.fills[i]))
                            throw $TypeError(".northstar.research.ResearchResultDocument.fills: object expected");
                        message.fills[i] = $root.google.protobuf.Struct.fromObject(object.fills[i], _depth + 1);
                    }
                }
                if (object.summary != null) {
                    if (!$util.isObject(object.summary))
                        throw $TypeError(".northstar.research.ResearchResultDocument.summary: object expected");
                    message.summary = $root.northstar.research.ResearchSummary.fromObject(object.summary, _depth + 1);
                }
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.research.ResearchResultDocument.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.research.ResearchResultDocument.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                if (object.null_fields) {
                    if (!$Array.isArray(object.null_fields))
                        throw $TypeError(".northstar.research.ResearchResultDocument.null_fields: array expected");
                    message.null_fields = $Array(object.null_fields.length);
                    for (let i = 0; i < object.null_fields.length; ++i)
                        message.null_fields[i] = $String(object.null_fields[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a ResearchResultDocument message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.ResearchResultDocument
             * @static
             * @param {northstar.research.ResearchResultDocument} message ResearchResultDocument
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ResearchResultDocument.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.decisions = [];
                    object.equity_curve = [];
                    object.fills = [];
                    object.null_fields = [];
                }
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.data != null && $Object.hasOwnProperty.call(message, "data")) {
                    object.data = $root.google.protobuf.Struct.toObject(message.data, options, _depth + 1);
                    if (options.oneofs)
                        object._data = "data";
                }
                if (message.decisions && message.decisions.length) {
                    object.decisions = $Array(message.decisions.length);
                    for (let j = 0; j < message.decisions.length; ++j)
                        object.decisions[j] = $root.google.protobuf.Struct.toObject(message.decisions[j], options, _depth + 1);
                }
                if (message.equity_curve && message.equity_curve.length) {
                    object.equity_curve = $Array(message.equity_curve.length);
                    for (let j = 0; j < message.equity_curve.length; ++j)
                        object.equity_curve[j] = $root.northstar.research.EquityPoint.toObject(message.equity_curve[j], options, _depth + 1);
                }
                if (message.fills && message.fills.length) {
                    object.fills = $Array(message.fills.length);
                    for (let j = 0; j < message.fills.length; ++j)
                        object.fills[j] = $root.google.protobuf.Struct.toObject(message.fills[j], options, _depth + 1);
                }
                if (message.summary != null && $Object.hasOwnProperty.call(message, "summary")) {
                    object.summary = $root.northstar.research.ResearchSummary.toObject(message.summary, options, _depth + 1);
                    if (options.oneofs)
                        object._summary = "summary";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                if (message.null_fields && message.null_fields.length) {
                    object.null_fields = $Array(message.null_fields.length);
                    for (let j = 0; j < message.null_fields.length; ++j)
                        object.null_fields[j] = message.null_fields[j];
                }
                return object;
            };

            /**
             * Converts this ResearchResultDocument to JSON.
             * @function toJSON
             * @memberof northstar.research.ResearchResultDocument
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ResearchResultDocument.prototype.toJSON = function() {
                return ResearchResultDocument.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ResearchResultDocument
             * @function getTypeUrl
             * @memberof northstar.research.ResearchResultDocument
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ResearchResultDocument.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.ResearchResultDocument";
            };

            return ResearchResultDocument;
        })();

        research.ResearchSummary = (function() {

            /**
             * Properties of a ResearchSummary.
             * @typedef {Object} northstar.research.ResearchSummary.$Properties
             * @property {number|Long|null} [bar_count] ResearchSummary bar_count
             * @property {number|Long|null} [decision_count] ResearchSummary decision_count
             * @property {string|null} [ending_cash] ResearchSummary ending_cash
             * @property {string|null} [ending_equity] ResearchSummary ending_equity
             * @property {number|Long|null} [ending_position_lots] ResearchSummary ending_position_lots
             * @property {number|Long|null} [fill_count] ResearchSummary fill_count
             * @property {string|null} [initial_cash] ResearchSummary initial_cash
             * @property {string|null} [max_drawdown] ResearchSummary max_drawdown
             * @property {string|null} [max_drawdown_fraction] ResearchSummary max_drawdown_fraction
             * @property {string|null} [realized_pnl] ResearchSummary realized_pnl
             * @property {string|null} [total_fees] ResearchSummary total_fees
             * @property {string|null} [total_return] ResearchSummary total_return
             * @property {string|null} [unrealized_pnl] ResearchSummary unrealized_pnl
             * @property {"bar_count"} [_bar_count] ResearchSummary _bar_count
             * @property {"decision_count"} [_decision_count] ResearchSummary _decision_count
             * @property {"ending_cash"} [_ending_cash] ResearchSummary _ending_cash
             * @property {"ending_equity"} [_ending_equity] ResearchSummary _ending_equity
             * @property {"ending_position_lots"} [_ending_position_lots] ResearchSummary _ending_position_lots
             * @property {"fill_count"} [_fill_count] ResearchSummary _fill_count
             * @property {"initial_cash"} [_initial_cash] ResearchSummary _initial_cash
             * @property {"max_drawdown"} [_max_drawdown] ResearchSummary _max_drawdown
             * @property {"max_drawdown_fraction"} [_max_drawdown_fraction] ResearchSummary _max_drawdown_fraction
             * @property {"realized_pnl"} [_realized_pnl] ResearchSummary _realized_pnl
             * @property {"total_fees"} [_total_fees] ResearchSummary _total_fees
             * @property {"total_return"} [_total_return] ResearchSummary _total_return
             * @property {"unrealized_pnl"} [_unrealized_pnl] ResearchSummary _unrealized_pnl
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ResearchSummary.
             * @memberof northstar.research
             * @interface IResearchSummary
             * @augments northstar.research.ResearchSummary.$Properties
             * @deprecated Use northstar.research.ResearchSummary.$Properties instead.
             */

            /**
             * Narrowed shape of a ResearchSummary.
             * @typedef {{
             *   bar_count?: number|Long|null;
             *   decision_count?: number|Long|null;
             *   ending_cash?: string|null;
             *   ending_equity?: string|null;
             *   ending_position_lots?: number|Long|null;
             *   fill_count?: number|Long|null;
             *   initial_cash?: string|null;
             *   max_drawdown?: string|null;
             *   max_drawdown_fraction?: string|null;
             *   realized_pnl?: string|null;
             *   total_fees?: string|null;
             *   total_return?: string|null;
             *   unrealized_pnl?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _bar_count?: undefined; bar_count?: null }|{ _bar_count?: "bar_count"; bar_count: number|Long })
             * ) & (
             *   ({ _decision_count?: undefined; decision_count?: null }|{ _decision_count?: "decision_count"; decision_count: number|Long })
             * ) & (
             *   ({ _ending_cash?: undefined; ending_cash?: null }|{ _ending_cash?: "ending_cash"; ending_cash: string })
             * ) & (
             *   ({ _ending_equity?: undefined; ending_equity?: null }|{ _ending_equity?: "ending_equity"; ending_equity: string })
             * ) & (
             *   ({ _ending_position_lots?: undefined; ending_position_lots?: null }|{ _ending_position_lots?: "ending_position_lots"; ending_position_lots: number|Long })
             * ) & (
             *   ({ _fill_count?: undefined; fill_count?: null }|{ _fill_count?: "fill_count"; fill_count: number|Long })
             * ) & (
             *   ({ _initial_cash?: undefined; initial_cash?: null }|{ _initial_cash?: "initial_cash"; initial_cash: string })
             * ) & (
             *   ({ _max_drawdown?: undefined; max_drawdown?: null }|{ _max_drawdown?: "max_drawdown"; max_drawdown: string })
             * ) & (
             *   ({ _max_drawdown_fraction?: undefined; max_drawdown_fraction?: null }|{ _max_drawdown_fraction?: "max_drawdown_fraction"; max_drawdown_fraction: string })
             * ) & (
             *   ({ _realized_pnl?: undefined; realized_pnl?: null }|{ _realized_pnl?: "realized_pnl"; realized_pnl: string })
             * ) & (
             *   ({ _total_fees?: undefined; total_fees?: null }|{ _total_fees?: "total_fees"; total_fees: string })
             * ) & (
             *   ({ _total_return?: undefined; total_return?: null }|{ _total_return?: "total_return"; total_return: string })
             * ) & (
             *   ({ _unrealized_pnl?: undefined; unrealized_pnl?: null }|{ _unrealized_pnl?: "unrealized_pnl"; unrealized_pnl: string })
             * )} northstar.research.ResearchSummary.$Shape
             */

            /**
             * Constructs a new ResearchSummary.
             * @memberof northstar.research
             * @classdesc Represents a ResearchSummary.
             * @constructor
             * @param {northstar.research.ResearchSummary.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ResearchSummary = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ResearchSummary bar_count.
             * @member {number|Long|null|undefined} bar_count
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            ResearchSummary.prototype.bar_count = null;

            /**
             * ResearchSummary decision_count.
             * @member {number|Long|null|undefined} decision_count
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            ResearchSummary.prototype.decision_count = null;

            /**
             * ResearchSummary ending_cash.
             * @member {string|null|undefined} ending_cash
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            ResearchSummary.prototype.ending_cash = null;

            /**
             * ResearchSummary ending_equity.
             * @member {string|null|undefined} ending_equity
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            ResearchSummary.prototype.ending_equity = null;

            /**
             * ResearchSummary ending_position_lots.
             * @member {number|Long|null|undefined} ending_position_lots
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            ResearchSummary.prototype.ending_position_lots = null;

            /**
             * ResearchSummary fill_count.
             * @member {number|Long|null|undefined} fill_count
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            ResearchSummary.prototype.fill_count = null;

            /**
             * ResearchSummary initial_cash.
             * @member {string|null|undefined} initial_cash
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            ResearchSummary.prototype.initial_cash = null;

            /**
             * ResearchSummary max_drawdown.
             * @member {string|null|undefined} max_drawdown
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            ResearchSummary.prototype.max_drawdown = null;

            /**
             * ResearchSummary max_drawdown_fraction.
             * @member {string|null|undefined} max_drawdown_fraction
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            ResearchSummary.prototype.max_drawdown_fraction = null;

            /**
             * ResearchSummary realized_pnl.
             * @member {string|null|undefined} realized_pnl
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            ResearchSummary.prototype.realized_pnl = null;

            /**
             * ResearchSummary total_fees.
             * @member {string|null|undefined} total_fees
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            ResearchSummary.prototype.total_fees = null;

            /**
             * ResearchSummary total_return.
             * @member {string|null|undefined} total_return
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            ResearchSummary.prototype.total_return = null;

            /**
             * ResearchSummary unrealized_pnl.
             * @member {string|null|undefined} unrealized_pnl
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            ResearchSummary.prototype.unrealized_pnl = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * ResearchSummary _bar_count.
             * @member {"bar_count"|undefined} _bar_count
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            $Object.defineProperty(ResearchSummary.prototype, "_bar_count", {
                get: $util.oneOfGetter($oneOfFields = ["bar_count"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchSummary _decision_count.
             * @member {"decision_count"|undefined} _decision_count
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            $Object.defineProperty(ResearchSummary.prototype, "_decision_count", {
                get: $util.oneOfGetter($oneOfFields = ["decision_count"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchSummary _ending_cash.
             * @member {"ending_cash"|undefined} _ending_cash
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            $Object.defineProperty(ResearchSummary.prototype, "_ending_cash", {
                get: $util.oneOfGetter($oneOfFields = ["ending_cash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchSummary _ending_equity.
             * @member {"ending_equity"|undefined} _ending_equity
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            $Object.defineProperty(ResearchSummary.prototype, "_ending_equity", {
                get: $util.oneOfGetter($oneOfFields = ["ending_equity"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchSummary _ending_position_lots.
             * @member {"ending_position_lots"|undefined} _ending_position_lots
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            $Object.defineProperty(ResearchSummary.prototype, "_ending_position_lots", {
                get: $util.oneOfGetter($oneOfFields = ["ending_position_lots"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchSummary _fill_count.
             * @member {"fill_count"|undefined} _fill_count
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            $Object.defineProperty(ResearchSummary.prototype, "_fill_count", {
                get: $util.oneOfGetter($oneOfFields = ["fill_count"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchSummary _initial_cash.
             * @member {"initial_cash"|undefined} _initial_cash
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            $Object.defineProperty(ResearchSummary.prototype, "_initial_cash", {
                get: $util.oneOfGetter($oneOfFields = ["initial_cash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchSummary _max_drawdown.
             * @member {"max_drawdown"|undefined} _max_drawdown
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            $Object.defineProperty(ResearchSummary.prototype, "_max_drawdown", {
                get: $util.oneOfGetter($oneOfFields = ["max_drawdown"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchSummary _max_drawdown_fraction.
             * @member {"max_drawdown_fraction"|undefined} _max_drawdown_fraction
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            $Object.defineProperty(ResearchSummary.prototype, "_max_drawdown_fraction", {
                get: $util.oneOfGetter($oneOfFields = ["max_drawdown_fraction"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchSummary _realized_pnl.
             * @member {"realized_pnl"|undefined} _realized_pnl
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            $Object.defineProperty(ResearchSummary.prototype, "_realized_pnl", {
                get: $util.oneOfGetter($oneOfFields = ["realized_pnl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchSummary _total_fees.
             * @member {"total_fees"|undefined} _total_fees
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            $Object.defineProperty(ResearchSummary.prototype, "_total_fees", {
                get: $util.oneOfGetter($oneOfFields = ["total_fees"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchSummary _total_return.
             * @member {"total_return"|undefined} _total_return
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            $Object.defineProperty(ResearchSummary.prototype, "_total_return", {
                get: $util.oneOfGetter($oneOfFields = ["total_return"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ResearchSummary _unrealized_pnl.
             * @member {"unrealized_pnl"|undefined} _unrealized_pnl
             * @memberof northstar.research.ResearchSummary
             * @instance
             */
            $Object.defineProperty(ResearchSummary.prototype, "_unrealized_pnl", {
                get: $util.oneOfGetter($oneOfFields = ["unrealized_pnl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ResearchSummary instance using the specified properties.
             * @function create
             * @memberof northstar.research.ResearchSummary
             * @static
             * @param {northstar.research.ResearchSummary.$Properties=} [properties] Properties to set
             * @returns {northstar.research.ResearchSummary} ResearchSummary instance
             * @type {{
             *   (properties: northstar.research.ResearchSummary.$Shape): northstar.research.ResearchSummary & northstar.research.ResearchSummary.$Shape;
             *   (properties?: northstar.research.ResearchSummary.$Properties): northstar.research.ResearchSummary;
             * }}
             */
            ResearchSummary.create = function(properties) {
                return new ResearchSummary(properties);
            };

            /**
             * Encodes the specified ResearchSummary message. Does not implicitly {@link northstar.research.ResearchSummary.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.ResearchSummary
             * @static
             * @param {northstar.research.ResearchSummary.$Properties} message ResearchSummary message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ResearchSummary.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.bar_count != null && $Object.hasOwnProperty.call(message, "bar_count"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int64(message.bar_count);
                if (message.decision_count != null && $Object.hasOwnProperty.call(message, "decision_count"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.decision_count);
                if (message.ending_cash != null && $Object.hasOwnProperty.call(message, "ending_cash"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.ending_cash);
                if (message.ending_equity != null && $Object.hasOwnProperty.call(message, "ending_equity"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.ending_equity);
                if (message.ending_position_lots != null && $Object.hasOwnProperty.call(message, "ending_position_lots"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.ending_position_lots);
                if (message.fill_count != null && $Object.hasOwnProperty.call(message, "fill_count"))
                    writer.uint32(/* id 6, wireType 0 =*/48).int64(message.fill_count);
                if (message.initial_cash != null && $Object.hasOwnProperty.call(message, "initial_cash"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.initial_cash);
                if (message.max_drawdown != null && $Object.hasOwnProperty.call(message, "max_drawdown"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.max_drawdown);
                if (message.max_drawdown_fraction != null && $Object.hasOwnProperty.call(message, "max_drawdown_fraction"))
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.max_drawdown_fraction);
                if (message.realized_pnl != null && $Object.hasOwnProperty.call(message, "realized_pnl"))
                    writer.uint32(/* id 10, wireType 2 =*/82).string(message.realized_pnl);
                if (message.total_fees != null && $Object.hasOwnProperty.call(message, "total_fees"))
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.total_fees);
                if (message.total_return != null && $Object.hasOwnProperty.call(message, "total_return"))
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.total_return);
                if (message.unrealized_pnl != null && $Object.hasOwnProperty.call(message, "unrealized_pnl"))
                    writer.uint32(/* id 13, wireType 2 =*/106).string(message.unrealized_pnl);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a ResearchSummary message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.ResearchSummary
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.ResearchSummary & northstar.research.ResearchSummary.$Shape} ResearchSummary
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ResearchSummary.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.ResearchSummary();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            message.bar_count = reader.int64();
                            message._bar_count = "bar_count";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            message.decision_count = reader.int64();
                            message._decision_count = "decision_count";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.ending_cash = reader.stringVerify();
                            message._ending_cash = "ending_cash";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.ending_equity = reader.stringVerify();
                            message._ending_equity = "ending_equity";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            message.ending_position_lots = reader.int64();
                            message._ending_position_lots = "ending_position_lots";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            message.fill_count = reader.int64();
                            message._fill_count = "fill_count";
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            message.initial_cash = reader.stringVerify();
                            message._initial_cash = "initial_cash";
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            message.max_drawdown = reader.stringVerify();
                            message._max_drawdown = "max_drawdown";
                            continue;
                        }
                    case 9: {
                            if (wireType !== 2)
                                break;
                            message.max_drawdown_fraction = reader.stringVerify();
                            message._max_drawdown_fraction = "max_drawdown_fraction";
                            continue;
                        }
                    case 10: {
                            if (wireType !== 2)
                                break;
                            message.realized_pnl = reader.stringVerify();
                            message._realized_pnl = "realized_pnl";
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            message.total_fees = reader.stringVerify();
                            message._total_fees = "total_fees";
                            continue;
                        }
                    case 12: {
                            if (wireType !== 2)
                                break;
                            message.total_return = reader.stringVerify();
                            message._total_return = "total_return";
                            continue;
                        }
                    case 13: {
                            if (wireType !== 2)
                                break;
                            message.unrealized_pnl = reader.stringVerify();
                            message._unrealized_pnl = "unrealized_pnl";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a ResearchSummary message.
             * @function verify
             * @memberof northstar.research.ResearchSummary
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ResearchSummary.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.bar_count != null && $Object.hasOwnProperty.call(message, "bar_count")) {
                    properties._bar_count = 1;
                    if (!$util.isInteger(message.bar_count) && !(message.bar_count && $util.isInteger(message.bar_count.low) && $util.isInteger(message.bar_count.high)))
                        return "bar_count: integer|Long expected";
                }
                if (message.decision_count != null && $Object.hasOwnProperty.call(message, "decision_count")) {
                    properties._decision_count = 1;
                    if (!$util.isInteger(message.decision_count) && !(message.decision_count && $util.isInteger(message.decision_count.low) && $util.isInteger(message.decision_count.high)))
                        return "decision_count: integer|Long expected";
                }
                if (message.ending_cash != null && $Object.hasOwnProperty.call(message, "ending_cash")) {
                    properties._ending_cash = 1;
                    if (!$util.isString(message.ending_cash))
                        return "ending_cash: string expected";
                }
                if (message.ending_equity != null && $Object.hasOwnProperty.call(message, "ending_equity")) {
                    properties._ending_equity = 1;
                    if (!$util.isString(message.ending_equity))
                        return "ending_equity: string expected";
                }
                if (message.ending_position_lots != null && $Object.hasOwnProperty.call(message, "ending_position_lots")) {
                    properties._ending_position_lots = 1;
                    if (!$util.isInteger(message.ending_position_lots) && !(message.ending_position_lots && $util.isInteger(message.ending_position_lots.low) && $util.isInteger(message.ending_position_lots.high)))
                        return "ending_position_lots: integer|Long expected";
                }
                if (message.fill_count != null && $Object.hasOwnProperty.call(message, "fill_count")) {
                    properties._fill_count = 1;
                    if (!$util.isInteger(message.fill_count) && !(message.fill_count && $util.isInteger(message.fill_count.low) && $util.isInteger(message.fill_count.high)))
                        return "fill_count: integer|Long expected";
                }
                if (message.initial_cash != null && $Object.hasOwnProperty.call(message, "initial_cash")) {
                    properties._initial_cash = 1;
                    if (!$util.isString(message.initial_cash))
                        return "initial_cash: string expected";
                }
                if (message.max_drawdown != null && $Object.hasOwnProperty.call(message, "max_drawdown")) {
                    properties._max_drawdown = 1;
                    if (!$util.isString(message.max_drawdown))
                        return "max_drawdown: string expected";
                }
                if (message.max_drawdown_fraction != null && $Object.hasOwnProperty.call(message, "max_drawdown_fraction")) {
                    properties._max_drawdown_fraction = 1;
                    if (!$util.isString(message.max_drawdown_fraction))
                        return "max_drawdown_fraction: string expected";
                }
                if (message.realized_pnl != null && $Object.hasOwnProperty.call(message, "realized_pnl")) {
                    properties._realized_pnl = 1;
                    if (!$util.isString(message.realized_pnl))
                        return "realized_pnl: string expected";
                }
                if (message.total_fees != null && $Object.hasOwnProperty.call(message, "total_fees")) {
                    properties._total_fees = 1;
                    if (!$util.isString(message.total_fees))
                        return "total_fees: string expected";
                }
                if (message.total_return != null && $Object.hasOwnProperty.call(message, "total_return")) {
                    properties._total_return = 1;
                    if (!$util.isString(message.total_return))
                        return "total_return: string expected";
                }
                if (message.unrealized_pnl != null && $Object.hasOwnProperty.call(message, "unrealized_pnl")) {
                    properties._unrealized_pnl = 1;
                    if (!$util.isString(message.unrealized_pnl))
                        return "unrealized_pnl: string expected";
                }
                return null;
            };

            /**
             * Creates a ResearchSummary message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.ResearchSummary
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.ResearchSummary} ResearchSummary
             */
            ResearchSummary.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.ResearchSummary)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.ResearchSummary: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.ResearchSummary();
                if (object.bar_count != null)
                    if ($util.Long)
                        message.bar_count = $util.Long.fromValue(object.bar_count, false);
                    else if (typeof object.bar_count === "string")
                        message.bar_count = $parseInt(object.bar_count, 10);
                    else if (typeof object.bar_count === "number")
                        message.bar_count = object.bar_count;
                    else if (typeof object.bar_count === "object")
                        message.bar_count = new $util.LongBits(object.bar_count.low >>> 0, object.bar_count.high >>> 0).toNumber();
                if (object.decision_count != null)
                    if ($util.Long)
                        message.decision_count = $util.Long.fromValue(object.decision_count, false);
                    else if (typeof object.decision_count === "string")
                        message.decision_count = $parseInt(object.decision_count, 10);
                    else if (typeof object.decision_count === "number")
                        message.decision_count = object.decision_count;
                    else if (typeof object.decision_count === "object")
                        message.decision_count = new $util.LongBits(object.decision_count.low >>> 0, object.decision_count.high >>> 0).toNumber();
                if (object.ending_cash != null)
                    message.ending_cash = $String(object.ending_cash);
                if (object.ending_equity != null)
                    message.ending_equity = $String(object.ending_equity);
                if (object.ending_position_lots != null)
                    if ($util.Long)
                        message.ending_position_lots = $util.Long.fromValue(object.ending_position_lots, false);
                    else if (typeof object.ending_position_lots === "string")
                        message.ending_position_lots = $parseInt(object.ending_position_lots, 10);
                    else if (typeof object.ending_position_lots === "number")
                        message.ending_position_lots = object.ending_position_lots;
                    else if (typeof object.ending_position_lots === "object")
                        message.ending_position_lots = new $util.LongBits(object.ending_position_lots.low >>> 0, object.ending_position_lots.high >>> 0).toNumber();
                if (object.fill_count != null)
                    if ($util.Long)
                        message.fill_count = $util.Long.fromValue(object.fill_count, false);
                    else if (typeof object.fill_count === "string")
                        message.fill_count = $parseInt(object.fill_count, 10);
                    else if (typeof object.fill_count === "number")
                        message.fill_count = object.fill_count;
                    else if (typeof object.fill_count === "object")
                        message.fill_count = new $util.LongBits(object.fill_count.low >>> 0, object.fill_count.high >>> 0).toNumber();
                if (object.initial_cash != null)
                    message.initial_cash = $String(object.initial_cash);
                if (object.max_drawdown != null)
                    message.max_drawdown = $String(object.max_drawdown);
                if (object.max_drawdown_fraction != null)
                    message.max_drawdown_fraction = $String(object.max_drawdown_fraction);
                if (object.realized_pnl != null)
                    message.realized_pnl = $String(object.realized_pnl);
                if (object.total_fees != null)
                    message.total_fees = $String(object.total_fees);
                if (object.total_return != null)
                    message.total_return = $String(object.total_return);
                if (object.unrealized_pnl != null)
                    message.unrealized_pnl = $String(object.unrealized_pnl);
                return message;
            };

            /**
             * Creates a plain object from a ResearchSummary message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.ResearchSummary
             * @static
             * @param {northstar.research.ResearchSummary} message ResearchSummary
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ResearchSummary.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.bar_count != null && $Object.hasOwnProperty.call(message, "bar_count")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.bar_count = typeof message.bar_count === "number" ? $BigInt(message.bar_count) : $util.Long.fromBits(message.bar_count.low >>> 0, message.bar_count.high >>> 0, false).toBigInt();
                    else if (typeof message.bar_count === "number")
                        object.bar_count = options.longs === $String ? $String(message.bar_count) : message.bar_count;
                    else
                        object.bar_count = options.longs === $String ? $util.Long.prototype.toString.call(message.bar_count) : options.longs === $Number ? new $util.LongBits(message.bar_count.low >>> 0, message.bar_count.high >>> 0).toNumber() : message.bar_count;
                    if (options.oneofs)
                        object._bar_count = "bar_count";
                }
                if (message.decision_count != null && $Object.hasOwnProperty.call(message, "decision_count")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.decision_count = typeof message.decision_count === "number" ? $BigInt(message.decision_count) : $util.Long.fromBits(message.decision_count.low >>> 0, message.decision_count.high >>> 0, false).toBigInt();
                    else if (typeof message.decision_count === "number")
                        object.decision_count = options.longs === $String ? $String(message.decision_count) : message.decision_count;
                    else
                        object.decision_count = options.longs === $String ? $util.Long.prototype.toString.call(message.decision_count) : options.longs === $Number ? new $util.LongBits(message.decision_count.low >>> 0, message.decision_count.high >>> 0).toNumber() : message.decision_count;
                    if (options.oneofs)
                        object._decision_count = "decision_count";
                }
                if (message.ending_cash != null && $Object.hasOwnProperty.call(message, "ending_cash")) {
                    object.ending_cash = message.ending_cash;
                    if (options.oneofs)
                        object._ending_cash = "ending_cash";
                }
                if (message.ending_equity != null && $Object.hasOwnProperty.call(message, "ending_equity")) {
                    object.ending_equity = message.ending_equity;
                    if (options.oneofs)
                        object._ending_equity = "ending_equity";
                }
                if (message.ending_position_lots != null && $Object.hasOwnProperty.call(message, "ending_position_lots")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.ending_position_lots = typeof message.ending_position_lots === "number" ? $BigInt(message.ending_position_lots) : $util.Long.fromBits(message.ending_position_lots.low >>> 0, message.ending_position_lots.high >>> 0, false).toBigInt();
                    else if (typeof message.ending_position_lots === "number")
                        object.ending_position_lots = options.longs === $String ? $String(message.ending_position_lots) : message.ending_position_lots;
                    else
                        object.ending_position_lots = options.longs === $String ? $util.Long.prototype.toString.call(message.ending_position_lots) : options.longs === $Number ? new $util.LongBits(message.ending_position_lots.low >>> 0, message.ending_position_lots.high >>> 0).toNumber() : message.ending_position_lots;
                    if (options.oneofs)
                        object._ending_position_lots = "ending_position_lots";
                }
                if (message.fill_count != null && $Object.hasOwnProperty.call(message, "fill_count")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.fill_count = typeof message.fill_count === "number" ? $BigInt(message.fill_count) : $util.Long.fromBits(message.fill_count.low >>> 0, message.fill_count.high >>> 0, false).toBigInt();
                    else if (typeof message.fill_count === "number")
                        object.fill_count = options.longs === $String ? $String(message.fill_count) : message.fill_count;
                    else
                        object.fill_count = options.longs === $String ? $util.Long.prototype.toString.call(message.fill_count) : options.longs === $Number ? new $util.LongBits(message.fill_count.low >>> 0, message.fill_count.high >>> 0).toNumber() : message.fill_count;
                    if (options.oneofs)
                        object._fill_count = "fill_count";
                }
                if (message.initial_cash != null && $Object.hasOwnProperty.call(message, "initial_cash")) {
                    object.initial_cash = message.initial_cash;
                    if (options.oneofs)
                        object._initial_cash = "initial_cash";
                }
                if (message.max_drawdown != null && $Object.hasOwnProperty.call(message, "max_drawdown")) {
                    object.max_drawdown = message.max_drawdown;
                    if (options.oneofs)
                        object._max_drawdown = "max_drawdown";
                }
                if (message.max_drawdown_fraction != null && $Object.hasOwnProperty.call(message, "max_drawdown_fraction")) {
                    object.max_drawdown_fraction = message.max_drawdown_fraction;
                    if (options.oneofs)
                        object._max_drawdown_fraction = "max_drawdown_fraction";
                }
                if (message.realized_pnl != null && $Object.hasOwnProperty.call(message, "realized_pnl")) {
                    object.realized_pnl = message.realized_pnl;
                    if (options.oneofs)
                        object._realized_pnl = "realized_pnl";
                }
                if (message.total_fees != null && $Object.hasOwnProperty.call(message, "total_fees")) {
                    object.total_fees = message.total_fees;
                    if (options.oneofs)
                        object._total_fees = "total_fees";
                }
                if (message.total_return != null && $Object.hasOwnProperty.call(message, "total_return")) {
                    object.total_return = message.total_return;
                    if (options.oneofs)
                        object._total_return = "total_return";
                }
                if (message.unrealized_pnl != null && $Object.hasOwnProperty.call(message, "unrealized_pnl")) {
                    object.unrealized_pnl = message.unrealized_pnl;
                    if (options.oneofs)
                        object._unrealized_pnl = "unrealized_pnl";
                }
                return object;
            };

            /**
             * Converts this ResearchSummary to JSON.
             * @function toJSON
             * @memberof northstar.research.ResearchSummary
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ResearchSummary.prototype.toJSON = function() {
                return ResearchSummary.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ResearchSummary
             * @function getTypeUrl
             * @memberof northstar.research.ResearchSummary
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ResearchSummary.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.ResearchSummary";
            };

            return ResearchSummary;
        })();

        research.RevisionCreated = (function() {

            /**
             * Properties of a RevisionCreated.
             * @typedef {Object} northstar.research.RevisionCreated.$Properties
             * @property {string|null} [revision_id] RevisionCreated revision_id
             * @property {"revision_id"} [_revision_id] RevisionCreated _revision_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a RevisionCreated.
             * @memberof northstar.research
             * @interface IRevisionCreated
             * @augments northstar.research.RevisionCreated.$Properties
             * @deprecated Use northstar.research.RevisionCreated.$Properties instead.
             */

            /**
             * Narrowed shape of a RevisionCreated.
             * @typedef {{
             *   revision_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _revision_id?: undefined; revision_id?: null }|{ _revision_id?: "revision_id"; revision_id: string })
             * )} northstar.research.RevisionCreated.$Shape
             */

            /**
             * Constructs a new RevisionCreated.
             * @memberof northstar.research
             * @classdesc Represents a RevisionCreated.
             * @constructor
             * @param {northstar.research.RevisionCreated.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const RevisionCreated = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * RevisionCreated revision_id.
             * @member {string|null|undefined} revision_id
             * @memberof northstar.research.RevisionCreated
             * @instance
             */
            RevisionCreated.prototype.revision_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * RevisionCreated _revision_id.
             * @member {"revision_id"|undefined} _revision_id
             * @memberof northstar.research.RevisionCreated
             * @instance
             */
            $Object.defineProperty(RevisionCreated.prototype, "_revision_id", {
                get: $util.oneOfGetter($oneOfFields = ["revision_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new RevisionCreated instance using the specified properties.
             * @function create
             * @memberof northstar.research.RevisionCreated
             * @static
             * @param {northstar.research.RevisionCreated.$Properties=} [properties] Properties to set
             * @returns {northstar.research.RevisionCreated} RevisionCreated instance
             * @type {{
             *   (properties: northstar.research.RevisionCreated.$Shape): northstar.research.RevisionCreated & northstar.research.RevisionCreated.$Shape;
             *   (properties?: northstar.research.RevisionCreated.$Properties): northstar.research.RevisionCreated;
             * }}
             */
            RevisionCreated.create = function(properties) {
                return new RevisionCreated(properties);
            };

            /**
             * Encodes the specified RevisionCreated message. Does not implicitly {@link northstar.research.RevisionCreated.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.RevisionCreated
             * @static
             * @param {northstar.research.RevisionCreated.$Properties} message RevisionCreated message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RevisionCreated.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.revision_id != null && $Object.hasOwnProperty.call(message, "revision_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.revision_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a RevisionCreated message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.RevisionCreated
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.RevisionCreated & northstar.research.RevisionCreated.$Shape} RevisionCreated
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RevisionCreated.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.RevisionCreated();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.revision_id = reader.stringVerify();
                            message._revision_id = "revision_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a RevisionCreated message.
             * @function verify
             * @memberof northstar.research.RevisionCreated
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RevisionCreated.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.revision_id != null && $Object.hasOwnProperty.call(message, "revision_id")) {
                    properties._revision_id = 1;
                    if (!$util.isString(message.revision_id))
                        return "revision_id: string expected";
                }
                return null;
            };

            /**
             * Creates a RevisionCreated message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.RevisionCreated
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.RevisionCreated} RevisionCreated
             */
            RevisionCreated.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.RevisionCreated)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.RevisionCreated: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.RevisionCreated();
                if (object.revision_id != null)
                    message.revision_id = $String(object.revision_id);
                return message;
            };

            /**
             * Creates a plain object from a RevisionCreated message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.RevisionCreated
             * @static
             * @param {northstar.research.RevisionCreated} message RevisionCreated
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RevisionCreated.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.revision_id != null && $Object.hasOwnProperty.call(message, "revision_id")) {
                    object.revision_id = message.revision_id;
                    if (options.oneofs)
                        object._revision_id = "revision_id";
                }
                return object;
            };

            /**
             * Converts this RevisionCreated to JSON.
             * @function toJSON
             * @memberof northstar.research.RevisionCreated
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RevisionCreated.prototype.toJSON = function() {
                return RevisionCreated.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for RevisionCreated
             * @function getTypeUrl
             * @memberof northstar.research.RevisionCreated
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            RevisionCreated.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.RevisionCreated";
            };

            return RevisionCreated;
        })();

        research.RiskInput = (function() {

            /**
             * Properties of a RiskInput.
             * @typedef {Object} northstar.research.RiskInput.$Properties
             * @property {string|null} [initial_margin_fraction] RiskInput initial_margin_fraction
             * @property {string|null} [max_adverse_price_move_fraction] RiskInput max_adverse_price_move_fraction
             * @property {string|null} [max_gross_notional] RiskInput max_gross_notional
             * @property {number|Long|null} [max_lots] RiskInput max_lots
             * @property {string|null} [max_margin_fraction] RiskInput max_margin_fraction
             * @property {"initial_margin_fraction"} [_initial_margin_fraction] RiskInput _initial_margin_fraction
             * @property {"max_adverse_price_move_fraction"} [_max_adverse_price_move_fraction] RiskInput _max_adverse_price_move_fraction
             * @property {"max_gross_notional"} [_max_gross_notional] RiskInput _max_gross_notional
             * @property {"max_lots"} [_max_lots] RiskInput _max_lots
             * @property {"max_margin_fraction"} [_max_margin_fraction] RiskInput _max_margin_fraction
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a RiskInput.
             * @memberof northstar.research
             * @interface IRiskInput
             * @augments northstar.research.RiskInput.$Properties
             * @deprecated Use northstar.research.RiskInput.$Properties instead.
             */

            /**
             * Narrowed shape of a RiskInput.
             * @typedef {{
             *   initial_margin_fraction?: string|null;
             *   max_adverse_price_move_fraction?: string|null;
             *   max_gross_notional?: string|null;
             *   max_lots?: number|Long|null;
             *   max_margin_fraction?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _initial_margin_fraction?: undefined; initial_margin_fraction?: null }|{ _initial_margin_fraction?: "initial_margin_fraction"; initial_margin_fraction: string })
             * ) & (
             *   ({ _max_adverse_price_move_fraction?: undefined; max_adverse_price_move_fraction?: null }|{ _max_adverse_price_move_fraction?: "max_adverse_price_move_fraction"; max_adverse_price_move_fraction: string })
             * ) & (
             *   ({ _max_gross_notional?: undefined; max_gross_notional?: null }|{ _max_gross_notional?: "max_gross_notional"; max_gross_notional: string })
             * ) & (
             *   ({ _max_lots?: undefined; max_lots?: null }|{ _max_lots?: "max_lots"; max_lots: number|Long })
             * ) & (
             *   ({ _max_margin_fraction?: undefined; max_margin_fraction?: null }|{ _max_margin_fraction?: "max_margin_fraction"; max_margin_fraction: string })
             * )} northstar.research.RiskInput.$Shape
             */

            /**
             * Constructs a new RiskInput.
             * @memberof northstar.research
             * @classdesc Represents a RiskInput.
             * @constructor
             * @param {northstar.research.RiskInput.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const RiskInput = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * RiskInput initial_margin_fraction.
             * @member {string|null|undefined} initial_margin_fraction
             * @memberof northstar.research.RiskInput
             * @instance
             */
            RiskInput.prototype.initial_margin_fraction = null;

            /**
             * RiskInput max_adverse_price_move_fraction.
             * @member {string|null|undefined} max_adverse_price_move_fraction
             * @memberof northstar.research.RiskInput
             * @instance
             */
            RiskInput.prototype.max_adverse_price_move_fraction = null;

            /**
             * RiskInput max_gross_notional.
             * @member {string|null|undefined} max_gross_notional
             * @memberof northstar.research.RiskInput
             * @instance
             */
            RiskInput.prototype.max_gross_notional = null;

            /**
             * RiskInput max_lots.
             * @member {number|Long|null|undefined} max_lots
             * @memberof northstar.research.RiskInput
             * @instance
             */
            RiskInput.prototype.max_lots = null;

            /**
             * RiskInput max_margin_fraction.
             * @member {string|null|undefined} max_margin_fraction
             * @memberof northstar.research.RiskInput
             * @instance
             */
            RiskInput.prototype.max_margin_fraction = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * RiskInput _initial_margin_fraction.
             * @member {"initial_margin_fraction"|undefined} _initial_margin_fraction
             * @memberof northstar.research.RiskInput
             * @instance
             */
            $Object.defineProperty(RiskInput.prototype, "_initial_margin_fraction", {
                get: $util.oneOfGetter($oneOfFields = ["initial_margin_fraction"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RiskInput _max_adverse_price_move_fraction.
             * @member {"max_adverse_price_move_fraction"|undefined} _max_adverse_price_move_fraction
             * @memberof northstar.research.RiskInput
             * @instance
             */
            $Object.defineProperty(RiskInput.prototype, "_max_adverse_price_move_fraction", {
                get: $util.oneOfGetter($oneOfFields = ["max_adverse_price_move_fraction"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RiskInput _max_gross_notional.
             * @member {"max_gross_notional"|undefined} _max_gross_notional
             * @memberof northstar.research.RiskInput
             * @instance
             */
            $Object.defineProperty(RiskInput.prototype, "_max_gross_notional", {
                get: $util.oneOfGetter($oneOfFields = ["max_gross_notional"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RiskInput _max_lots.
             * @member {"max_lots"|undefined} _max_lots
             * @memberof northstar.research.RiskInput
             * @instance
             */
            $Object.defineProperty(RiskInput.prototype, "_max_lots", {
                get: $util.oneOfGetter($oneOfFields = ["max_lots"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RiskInput _max_margin_fraction.
             * @member {"max_margin_fraction"|undefined} _max_margin_fraction
             * @memberof northstar.research.RiskInput
             * @instance
             */
            $Object.defineProperty(RiskInput.prototype, "_max_margin_fraction", {
                get: $util.oneOfGetter($oneOfFields = ["max_margin_fraction"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new RiskInput instance using the specified properties.
             * @function create
             * @memberof northstar.research.RiskInput
             * @static
             * @param {northstar.research.RiskInput.$Properties=} [properties] Properties to set
             * @returns {northstar.research.RiskInput} RiskInput instance
             * @type {{
             *   (properties: northstar.research.RiskInput.$Shape): northstar.research.RiskInput & northstar.research.RiskInput.$Shape;
             *   (properties?: northstar.research.RiskInput.$Properties): northstar.research.RiskInput;
             * }}
             */
            RiskInput.create = function(properties) {
                return new RiskInput(properties);
            };

            /**
             * Encodes the specified RiskInput message. Does not implicitly {@link northstar.research.RiskInput.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.RiskInput
             * @static
             * @param {northstar.research.RiskInput.$Properties} message RiskInput message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RiskInput.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.initial_margin_fraction != null && $Object.hasOwnProperty.call(message, "initial_margin_fraction"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.initial_margin_fraction);
                if (message.max_adverse_price_move_fraction != null && $Object.hasOwnProperty.call(message, "max_adverse_price_move_fraction"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.max_adverse_price_move_fraction);
                if (message.max_gross_notional != null && $Object.hasOwnProperty.call(message, "max_gross_notional"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.max_gross_notional);
                if (message.max_lots != null && $Object.hasOwnProperty.call(message, "max_lots"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int64(message.max_lots);
                if (message.max_margin_fraction != null && $Object.hasOwnProperty.call(message, "max_margin_fraction"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.max_margin_fraction);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a RiskInput message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.RiskInput
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.RiskInput & northstar.research.RiskInput.$Shape} RiskInput
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RiskInput.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.RiskInput();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.initial_margin_fraction = reader.stringVerify();
                            message._initial_margin_fraction = "initial_margin_fraction";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.max_adverse_price_move_fraction = reader.stringVerify();
                            message._max_adverse_price_move_fraction = "max_adverse_price_move_fraction";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.max_gross_notional = reader.stringVerify();
                            message._max_gross_notional = "max_gross_notional";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            message.max_lots = reader.int64();
                            message._max_lots = "max_lots";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.max_margin_fraction = reader.stringVerify();
                            message._max_margin_fraction = "max_margin_fraction";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a RiskInput message.
             * @function verify
             * @memberof northstar.research.RiskInput
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RiskInput.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.initial_margin_fraction != null && $Object.hasOwnProperty.call(message, "initial_margin_fraction")) {
                    properties._initial_margin_fraction = 1;
                    if (!$util.isString(message.initial_margin_fraction))
                        return "initial_margin_fraction: string expected";
                }
                if (message.max_adverse_price_move_fraction != null && $Object.hasOwnProperty.call(message, "max_adverse_price_move_fraction")) {
                    properties._max_adverse_price_move_fraction = 1;
                    if (!$util.isString(message.max_adverse_price_move_fraction))
                        return "max_adverse_price_move_fraction: string expected";
                }
                if (message.max_gross_notional != null && $Object.hasOwnProperty.call(message, "max_gross_notional")) {
                    properties._max_gross_notional = 1;
                    if (!$util.isString(message.max_gross_notional))
                        return "max_gross_notional: string expected";
                }
                if (message.max_lots != null && $Object.hasOwnProperty.call(message, "max_lots")) {
                    properties._max_lots = 1;
                    if (!$util.isInteger(message.max_lots) && !(message.max_lots && $util.isInteger(message.max_lots.low) && $util.isInteger(message.max_lots.high)))
                        return "max_lots: integer|Long expected";
                }
                if (message.max_margin_fraction != null && $Object.hasOwnProperty.call(message, "max_margin_fraction")) {
                    properties._max_margin_fraction = 1;
                    if (!$util.isString(message.max_margin_fraction))
                        return "max_margin_fraction: string expected";
                }
                return null;
            };

            /**
             * Creates a RiskInput message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.RiskInput
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.RiskInput} RiskInput
             */
            RiskInput.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.RiskInput)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.RiskInput: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.RiskInput();
                if (object.initial_margin_fraction != null)
                    message.initial_margin_fraction = $String(object.initial_margin_fraction);
                if (object.max_adverse_price_move_fraction != null)
                    message.max_adverse_price_move_fraction = $String(object.max_adverse_price_move_fraction);
                if (object.max_gross_notional != null)
                    message.max_gross_notional = $String(object.max_gross_notional);
                if (object.max_lots != null)
                    if ($util.Long)
                        message.max_lots = $util.Long.fromValue(object.max_lots, false);
                    else if (typeof object.max_lots === "string")
                        message.max_lots = $parseInt(object.max_lots, 10);
                    else if (typeof object.max_lots === "number")
                        message.max_lots = object.max_lots;
                    else if (typeof object.max_lots === "object")
                        message.max_lots = new $util.LongBits(object.max_lots.low >>> 0, object.max_lots.high >>> 0).toNumber();
                if (object.max_margin_fraction != null)
                    message.max_margin_fraction = $String(object.max_margin_fraction);
                return message;
            };

            /**
             * Creates a plain object from a RiskInput message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.RiskInput
             * @static
             * @param {northstar.research.RiskInput} message RiskInput
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RiskInput.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.initial_margin_fraction != null && $Object.hasOwnProperty.call(message, "initial_margin_fraction")) {
                    object.initial_margin_fraction = message.initial_margin_fraction;
                    if (options.oneofs)
                        object._initial_margin_fraction = "initial_margin_fraction";
                }
                if (message.max_adverse_price_move_fraction != null && $Object.hasOwnProperty.call(message, "max_adverse_price_move_fraction")) {
                    object.max_adverse_price_move_fraction = message.max_adverse_price_move_fraction;
                    if (options.oneofs)
                        object._max_adverse_price_move_fraction = "max_adverse_price_move_fraction";
                }
                if (message.max_gross_notional != null && $Object.hasOwnProperty.call(message, "max_gross_notional")) {
                    object.max_gross_notional = message.max_gross_notional;
                    if (options.oneofs)
                        object._max_gross_notional = "max_gross_notional";
                }
                if (message.max_lots != null && $Object.hasOwnProperty.call(message, "max_lots")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.max_lots = typeof message.max_lots === "number" ? $BigInt(message.max_lots) : $util.Long.fromBits(message.max_lots.low >>> 0, message.max_lots.high >>> 0, false).toBigInt();
                    else if (typeof message.max_lots === "number")
                        object.max_lots = options.longs === $String ? $String(message.max_lots) : message.max_lots;
                    else
                        object.max_lots = options.longs === $String ? $util.Long.prototype.toString.call(message.max_lots) : options.longs === $Number ? new $util.LongBits(message.max_lots.low >>> 0, message.max_lots.high >>> 0).toNumber() : message.max_lots;
                    if (options.oneofs)
                        object._max_lots = "max_lots";
                }
                if (message.max_margin_fraction != null && $Object.hasOwnProperty.call(message, "max_margin_fraction")) {
                    object.max_margin_fraction = message.max_margin_fraction;
                    if (options.oneofs)
                        object._max_margin_fraction = "max_margin_fraction";
                }
                return object;
            };

            /**
             * Converts this RiskInput to JSON.
             * @function toJSON
             * @memberof northstar.research.RiskInput
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RiskInput.prototype.toJSON = function() {
                return RiskInput.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for RiskInput
             * @function getTypeUrl
             * @memberof northstar.research.RiskInput
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            RiskInput.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.RiskInput";
            };

            return RiskInput;
        })();

        research.RunCreated = (function() {

            /**
             * Properties of a RunCreated.
             * @typedef {Object} northstar.research.RunCreated.$Properties
             * @property {string|null} [run_id] RunCreated run_id
             * @property {string|null} [url] RunCreated url
             * @property {"run_id"} [_run_id] RunCreated _run_id
             * @property {"url"} [_url] RunCreated _url
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a RunCreated.
             * @memberof northstar.research
             * @interface IRunCreated
             * @augments northstar.research.RunCreated.$Properties
             * @deprecated Use northstar.research.RunCreated.$Properties instead.
             */

            /**
             * Narrowed shape of a RunCreated.
             * @typedef {{
             *   run_id?: string|null;
             *   url?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _run_id?: undefined; run_id?: null }|{ _run_id?: "run_id"; run_id: string })
             * ) & (
             *   ({ _url?: undefined; url?: null }|{ _url?: "url"; url: string })
             * )} northstar.research.RunCreated.$Shape
             */

            /**
             * Constructs a new RunCreated.
             * @memberof northstar.research
             * @classdesc Represents a RunCreated.
             * @constructor
             * @param {northstar.research.RunCreated.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const RunCreated = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * RunCreated run_id.
             * @member {string|null|undefined} run_id
             * @memberof northstar.research.RunCreated
             * @instance
             */
            RunCreated.prototype.run_id = null;

            /**
             * RunCreated url.
             * @member {string|null|undefined} url
             * @memberof northstar.research.RunCreated
             * @instance
             */
            RunCreated.prototype.url = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * RunCreated _run_id.
             * @member {"run_id"|undefined} _run_id
             * @memberof northstar.research.RunCreated
             * @instance
             */
            $Object.defineProperty(RunCreated.prototype, "_run_id", {
                get: $util.oneOfGetter($oneOfFields = ["run_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RunCreated _url.
             * @member {"url"|undefined} _url
             * @memberof northstar.research.RunCreated
             * @instance
             */
            $Object.defineProperty(RunCreated.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new RunCreated instance using the specified properties.
             * @function create
             * @memberof northstar.research.RunCreated
             * @static
             * @param {northstar.research.RunCreated.$Properties=} [properties] Properties to set
             * @returns {northstar.research.RunCreated} RunCreated instance
             * @type {{
             *   (properties: northstar.research.RunCreated.$Shape): northstar.research.RunCreated & northstar.research.RunCreated.$Shape;
             *   (properties?: northstar.research.RunCreated.$Properties): northstar.research.RunCreated;
             * }}
             */
            RunCreated.create = function(properties) {
                return new RunCreated(properties);
            };

            /**
             * Encodes the specified RunCreated message. Does not implicitly {@link northstar.research.RunCreated.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.RunCreated
             * @static
             * @param {northstar.research.RunCreated.$Properties} message RunCreated message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RunCreated.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.run_id != null && $Object.hasOwnProperty.call(message, "run_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.run_id);
                if (message.url != null && $Object.hasOwnProperty.call(message, "url"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.url);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a RunCreated message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.RunCreated
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.RunCreated & northstar.research.RunCreated.$Shape} RunCreated
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RunCreated.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.RunCreated();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.run_id = reader.stringVerify();
                            message._run_id = "run_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.url = reader.stringVerify();
                            message._url = "url";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a RunCreated message.
             * @function verify
             * @memberof northstar.research.RunCreated
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RunCreated.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.run_id != null && $Object.hasOwnProperty.call(message, "run_id")) {
                    properties._run_id = 1;
                    if (!$util.isString(message.run_id))
                        return "run_id: string expected";
                }
                if (message.url != null && $Object.hasOwnProperty.call(message, "url")) {
                    properties._url = 1;
                    if (!$util.isString(message.url))
                        return "url: string expected";
                }
                return null;
            };

            /**
             * Creates a RunCreated message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.RunCreated
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.RunCreated} RunCreated
             */
            RunCreated.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.RunCreated)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.RunCreated: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.RunCreated();
                if (object.run_id != null)
                    message.run_id = $String(object.run_id);
                if (object.url != null)
                    message.url = $String(object.url);
                return message;
            };

            /**
             * Creates a plain object from a RunCreated message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.RunCreated
             * @static
             * @param {northstar.research.RunCreated} message RunCreated
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RunCreated.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.run_id != null && $Object.hasOwnProperty.call(message, "run_id")) {
                    object.run_id = message.run_id;
                    if (options.oneofs)
                        object._run_id = "run_id";
                }
                if (message.url != null && $Object.hasOwnProperty.call(message, "url")) {
                    object.url = message.url;
                    if (options.oneofs)
                        object._url = "url";
                }
                return object;
            };

            /**
             * Converts this RunCreated to JSON.
             * @function toJSON
             * @memberof northstar.research.RunCreated
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RunCreated.prototype.toJSON = function() {
                return RunCreated.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for RunCreated
             * @function getTypeUrl
             * @memberof northstar.research.RunCreated
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            RunCreated.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.RunCreated";
            };

            return RunCreated;
        })();

        research.RunDetail = (function() {

            /**
             * Properties of a RunDetail.
             * @typedef {Object} northstar.research.RunDetail.$Properties
             * @property {string|null} [code_revision] RunDetail code_revision
             * @property {boolean|null} [committed_code] RunDetail committed_code
             * @property {northstar.research.ResearchConfiguration.$Properties|null} [config] RunDetail config
             * @property {string|null} [created_at] RunDetail created_at
             * @property {northstar.research.ResearchResultDocument.$Properties|null} [result] RunDetail result
             * @property {string|null} [run_id] RunDetail run_id
             * @property {northstar.research.SnapshotReference.$Properties|null} [snapshot] RunDetail snapshot
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] RunDetail evidence_fields
             * @property {"code_revision"} [_code_revision] RunDetail _code_revision
             * @property {"committed_code"} [_committed_code] RunDetail _committed_code
             * @property {"config"} [_config] RunDetail _config
             * @property {"created_at"} [_created_at] RunDetail _created_at
             * @property {"result"} [_result] RunDetail _result
             * @property {"run_id"} [_run_id] RunDetail _run_id
             * @property {"snapshot"} [_snapshot] RunDetail _snapshot
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a RunDetail.
             * @memberof northstar.research
             * @interface IRunDetail
             * @augments northstar.research.RunDetail.$Properties
             * @deprecated Use northstar.research.RunDetail.$Properties instead.
             */

            /**
             * Narrowed shape of a RunDetail.
             * @typedef {{
             *   code_revision?: string|null;
             *   committed_code?: boolean|null;
             *   config?: northstar.research.ResearchConfiguration.$Shape|null;
             *   created_at?: string|null;
             *   result?: northstar.research.ResearchResultDocument.$Shape|null;
             *   run_id?: string|null;
             *   snapshot?: northstar.research.SnapshotReference.$Shape|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _code_revision?: undefined; code_revision?: null }|{ _code_revision?: "code_revision"; code_revision: string })
             * ) & (
             *   ({ _committed_code?: undefined; committed_code?: null }|{ _committed_code?: "committed_code"; committed_code: boolean })
             * ) & (
             *   ({ _config?: undefined; config?: null }|{ _config?: "config"; config: northstar.research.ResearchConfiguration.$Shape })
             * ) & (
             *   ({ _created_at?: undefined; created_at?: null }|{ _created_at?: "created_at"; created_at: string })
             * ) & (
             *   ({ _result?: undefined; result?: null }|{ _result?: "result"; result: northstar.research.ResearchResultDocument.$Shape })
             * ) & (
             *   ({ _run_id?: undefined; run_id?: null }|{ _run_id?: "run_id"; run_id: string })
             * ) & (
             *   ({ _snapshot?: undefined; snapshot?: null }|{ _snapshot?: "snapshot"; snapshot: northstar.research.SnapshotReference.$Shape })
             * )} northstar.research.RunDetail.$Shape
             */

            /**
             * Constructs a new RunDetail.
             * @memberof northstar.research
             * @classdesc Represents a RunDetail.
             * @constructor
             * @param {northstar.research.RunDetail.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const RunDetail = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * RunDetail code_revision.
             * @member {string|null|undefined} code_revision
             * @memberof northstar.research.RunDetail
             * @instance
             */
            RunDetail.prototype.code_revision = null;

            /**
             * RunDetail committed_code.
             * @member {boolean|null|undefined} committed_code
             * @memberof northstar.research.RunDetail
             * @instance
             */
            RunDetail.prototype.committed_code = null;

            /**
             * RunDetail config.
             * @member {northstar.research.ResearchConfiguration.$Properties|null|undefined} config
             * @memberof northstar.research.RunDetail
             * @instance
             */
            RunDetail.prototype.config = null;

            /**
             * RunDetail created_at.
             * @member {string|null|undefined} created_at
             * @memberof northstar.research.RunDetail
             * @instance
             */
            RunDetail.prototype.created_at = null;

            /**
             * RunDetail result.
             * @member {northstar.research.ResearchResultDocument.$Properties|null|undefined} result
             * @memberof northstar.research.RunDetail
             * @instance
             */
            RunDetail.prototype.result = null;

            /**
             * RunDetail run_id.
             * @member {string|null|undefined} run_id
             * @memberof northstar.research.RunDetail
             * @instance
             */
            RunDetail.prototype.run_id = null;

            /**
             * RunDetail snapshot.
             * @member {northstar.research.SnapshotReference.$Properties|null|undefined} snapshot
             * @memberof northstar.research.RunDetail
             * @instance
             */
            RunDetail.prototype.snapshot = null;

            /**
             * RunDetail evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.research.RunDetail
             * @instance
             */
            RunDetail.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * RunDetail _code_revision.
             * @member {"code_revision"|undefined} _code_revision
             * @memberof northstar.research.RunDetail
             * @instance
             */
            $Object.defineProperty(RunDetail.prototype, "_code_revision", {
                get: $util.oneOfGetter($oneOfFields = ["code_revision"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RunDetail _committed_code.
             * @member {"committed_code"|undefined} _committed_code
             * @memberof northstar.research.RunDetail
             * @instance
             */
            $Object.defineProperty(RunDetail.prototype, "_committed_code", {
                get: $util.oneOfGetter($oneOfFields = ["committed_code"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RunDetail _config.
             * @member {"config"|undefined} _config
             * @memberof northstar.research.RunDetail
             * @instance
             */
            $Object.defineProperty(RunDetail.prototype, "_config", {
                get: $util.oneOfGetter($oneOfFields = ["config"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RunDetail _created_at.
             * @member {"created_at"|undefined} _created_at
             * @memberof northstar.research.RunDetail
             * @instance
             */
            $Object.defineProperty(RunDetail.prototype, "_created_at", {
                get: $util.oneOfGetter($oneOfFields = ["created_at"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RunDetail _result.
             * @member {"result"|undefined} _result
             * @memberof northstar.research.RunDetail
             * @instance
             */
            $Object.defineProperty(RunDetail.prototype, "_result", {
                get: $util.oneOfGetter($oneOfFields = ["result"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RunDetail _run_id.
             * @member {"run_id"|undefined} _run_id
             * @memberof northstar.research.RunDetail
             * @instance
             */
            $Object.defineProperty(RunDetail.prototype, "_run_id", {
                get: $util.oneOfGetter($oneOfFields = ["run_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RunDetail _snapshot.
             * @member {"snapshot"|undefined} _snapshot
             * @memberof northstar.research.RunDetail
             * @instance
             */
            $Object.defineProperty(RunDetail.prototype, "_snapshot", {
                get: $util.oneOfGetter($oneOfFields = ["snapshot"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new RunDetail instance using the specified properties.
             * @function create
             * @memberof northstar.research.RunDetail
             * @static
             * @param {northstar.research.RunDetail.$Properties=} [properties] Properties to set
             * @returns {northstar.research.RunDetail} RunDetail instance
             * @type {{
             *   (properties: northstar.research.RunDetail.$Shape): northstar.research.RunDetail & northstar.research.RunDetail.$Shape;
             *   (properties?: northstar.research.RunDetail.$Properties): northstar.research.RunDetail;
             * }}
             */
            RunDetail.create = function(properties) {
                return new RunDetail(properties);
            };

            /**
             * Encodes the specified RunDetail message. Does not implicitly {@link northstar.research.RunDetail.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.RunDetail
             * @static
             * @param {northstar.research.RunDetail.$Properties} message RunDetail message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RunDetail.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.code_revision);
                if (message.committed_code != null && $Object.hasOwnProperty.call(message, "committed_code"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.committed_code);
                if (message.config != null && $Object.hasOwnProperty.call(message, "config"))
                    $root.northstar.research.ResearchConfiguration.encode(message.config, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.created_at != null && $Object.hasOwnProperty.call(message, "created_at"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.created_at);
                if (message.result != null && $Object.hasOwnProperty.call(message, "result"))
                    $root.northstar.research.ResearchResultDocument.encode(message.result, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                if (message.run_id != null && $Object.hasOwnProperty.call(message, "run_id"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.run_id);
                if (message.snapshot != null && $Object.hasOwnProperty.call(message, "snapshot"))
                    $root.northstar.research.SnapshotReference.encode(message.snapshot, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a RunDetail message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.RunDetail
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.RunDetail & northstar.research.RunDetail.$Shape} RunDetail
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RunDetail.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.RunDetail();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.code_revision = reader.stringVerify();
                            message._code_revision = "code_revision";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            message.committed_code = reader.bool();
                            message._committed_code = "committed_code";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.config = $root.northstar.research.ResearchConfiguration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.config);
                            message._config = "config";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.created_at = reader.stringVerify();
                            message._created_at = "created_at";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.result = $root.northstar.research.ResearchResultDocument.decode(reader, reader.uint32(), $undefined, _depth + 1, message.result);
                            message._result = "result";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.run_id = reader.stringVerify();
                            message._run_id = "run_id";
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            message.snapshot = $root.northstar.research.SnapshotReference.decode(reader, reader.uint32(), $undefined, _depth + 1, message.snapshot);
                            message._snapshot = "snapshot";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a RunDetail message.
             * @function verify
             * @memberof northstar.research.RunDetail
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RunDetail.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision")) {
                    properties._code_revision = 1;
                    if (!$util.isString(message.code_revision))
                        return "code_revision: string expected";
                }
                if (message.committed_code != null && $Object.hasOwnProperty.call(message, "committed_code")) {
                    properties._committed_code = 1;
                    if (typeof message.committed_code !== "boolean")
                        return "committed_code: boolean expected";
                }
                if (message.config != null && $Object.hasOwnProperty.call(message, "config")) {
                    properties._config = 1;
                    {
                        let error = $root.northstar.research.ResearchConfiguration.verify(message.config, _depth + 1);
                        if (error)
                            return "config." + error;
                    }
                }
                if (message.created_at != null && $Object.hasOwnProperty.call(message, "created_at")) {
                    properties._created_at = 1;
                    if (!$util.isString(message.created_at))
                        return "created_at: string expected";
                }
                if (message.result != null && $Object.hasOwnProperty.call(message, "result")) {
                    properties._result = 1;
                    {
                        let error = $root.northstar.research.ResearchResultDocument.verify(message.result, _depth + 1);
                        if (error)
                            return "result." + error;
                    }
                }
                if (message.run_id != null && $Object.hasOwnProperty.call(message, "run_id")) {
                    properties._run_id = 1;
                    if (!$util.isString(message.run_id))
                        return "run_id: string expected";
                }
                if (message.snapshot != null && $Object.hasOwnProperty.call(message, "snapshot")) {
                    properties._snapshot = 1;
                    {
                        let error = $root.northstar.research.SnapshotReference.verify(message.snapshot, _depth + 1);
                        if (error)
                            return "snapshot." + error;
                    }
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a RunDetail message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.RunDetail
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.RunDetail} RunDetail
             */
            RunDetail.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.RunDetail)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.RunDetail: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.RunDetail();
                if (object.code_revision != null)
                    message.code_revision = $String(object.code_revision);
                if (object.committed_code != null)
                    message.committed_code = $Boolean(object.committed_code);
                if (object.config != null) {
                    if (!$util.isObject(object.config))
                        throw $TypeError(".northstar.research.RunDetail.config: object expected");
                    message.config = $root.northstar.research.ResearchConfiguration.fromObject(object.config, _depth + 1);
                }
                if (object.created_at != null)
                    message.created_at = $String(object.created_at);
                if (object.result != null) {
                    if (!$util.isObject(object.result))
                        throw $TypeError(".northstar.research.RunDetail.result: object expected");
                    message.result = $root.northstar.research.ResearchResultDocument.fromObject(object.result, _depth + 1);
                }
                if (object.run_id != null)
                    message.run_id = $String(object.run_id);
                if (object.snapshot != null) {
                    if (!$util.isObject(object.snapshot))
                        throw $TypeError(".northstar.research.RunDetail.snapshot: object expected");
                    message.snapshot = $root.northstar.research.SnapshotReference.fromObject(object.snapshot, _depth + 1);
                }
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.research.RunDetail.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.research.RunDetail.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a RunDetail message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.RunDetail
             * @static
             * @param {northstar.research.RunDetail} message RunDetail
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RunDetail.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision")) {
                    object.code_revision = message.code_revision;
                    if (options.oneofs)
                        object._code_revision = "code_revision";
                }
                if (message.committed_code != null && $Object.hasOwnProperty.call(message, "committed_code")) {
                    object.committed_code = message.committed_code;
                    if (options.oneofs)
                        object._committed_code = "committed_code";
                }
                if (message.config != null && $Object.hasOwnProperty.call(message, "config")) {
                    object.config = $root.northstar.research.ResearchConfiguration.toObject(message.config, options, _depth + 1);
                    if (options.oneofs)
                        object._config = "config";
                }
                if (message.created_at != null && $Object.hasOwnProperty.call(message, "created_at")) {
                    object.created_at = message.created_at;
                    if (options.oneofs)
                        object._created_at = "created_at";
                }
                if (message.result != null && $Object.hasOwnProperty.call(message, "result")) {
                    object.result = $root.northstar.research.ResearchResultDocument.toObject(message.result, options, _depth + 1);
                    if (options.oneofs)
                        object._result = "result";
                }
                if (message.run_id != null && $Object.hasOwnProperty.call(message, "run_id")) {
                    object.run_id = message.run_id;
                    if (options.oneofs)
                        object._run_id = "run_id";
                }
                if (message.snapshot != null && $Object.hasOwnProperty.call(message, "snapshot")) {
                    object.snapshot = $root.northstar.research.SnapshotReference.toObject(message.snapshot, options, _depth + 1);
                    if (options.oneofs)
                        object._snapshot = "snapshot";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this RunDetail to JSON.
             * @function toJSON
             * @memberof northstar.research.RunDetail
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RunDetail.prototype.toJSON = function() {
                return RunDetail.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for RunDetail
             * @function getTypeUrl
             * @memberof northstar.research.RunDetail
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            RunDetail.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.RunDetail";
            };

            return RunDetail;
        })();

        research.RunRequest = (function() {

            /**
             * Properties of a RunRequest.
             * @typedef {Object} northstar.research.RunRequest.$Properties
             * @property {northstar.research.ResearchConfigurationInput.$Properties|null} [config] RunRequest config
             * @property {string|null} [snapshot_id] RunRequest snapshot_id
             * @property {"config"} [_config] RunRequest _config
             * @property {"snapshot_id"} [_snapshot_id] RunRequest _snapshot_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a RunRequest.
             * @memberof northstar.research
             * @interface IRunRequest
             * @augments northstar.research.RunRequest.$Properties
             * @deprecated Use northstar.research.RunRequest.$Properties instead.
             */

            /**
             * Narrowed shape of a RunRequest.
             * @typedef {{
             *   config?: northstar.research.ResearchConfigurationInput.$Shape|null;
             *   snapshot_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _config?: undefined; config?: null }|{ _config?: "config"; config: northstar.research.ResearchConfigurationInput.$Shape })
             * ) & (
             *   ({ _snapshot_id?: undefined; snapshot_id?: null }|{ _snapshot_id?: "snapshot_id"; snapshot_id: string })
             * )} northstar.research.RunRequest.$Shape
             */

            /**
             * Constructs a new RunRequest.
             * @memberof northstar.research
             * @classdesc Represents a RunRequest.
             * @constructor
             * @param {northstar.research.RunRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const RunRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * RunRequest config.
             * @member {northstar.research.ResearchConfigurationInput.$Properties|null|undefined} config
             * @memberof northstar.research.RunRequest
             * @instance
             */
            RunRequest.prototype.config = null;

            /**
             * RunRequest snapshot_id.
             * @member {string|null|undefined} snapshot_id
             * @memberof northstar.research.RunRequest
             * @instance
             */
            RunRequest.prototype.snapshot_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * RunRequest _config.
             * @member {"config"|undefined} _config
             * @memberof northstar.research.RunRequest
             * @instance
             */
            $Object.defineProperty(RunRequest.prototype, "_config", {
                get: $util.oneOfGetter($oneOfFields = ["config"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RunRequest _snapshot_id.
             * @member {"snapshot_id"|undefined} _snapshot_id
             * @memberof northstar.research.RunRequest
             * @instance
             */
            $Object.defineProperty(RunRequest.prototype, "_snapshot_id", {
                get: $util.oneOfGetter($oneOfFields = ["snapshot_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new RunRequest instance using the specified properties.
             * @function create
             * @memberof northstar.research.RunRequest
             * @static
             * @param {northstar.research.RunRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.research.RunRequest} RunRequest instance
             * @type {{
             *   (properties: northstar.research.RunRequest.$Shape): northstar.research.RunRequest & northstar.research.RunRequest.$Shape;
             *   (properties?: northstar.research.RunRequest.$Properties): northstar.research.RunRequest;
             * }}
             */
            RunRequest.create = function(properties) {
                return new RunRequest(properties);
            };

            /**
             * Encodes the specified RunRequest message. Does not implicitly {@link northstar.research.RunRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.RunRequest
             * @static
             * @param {northstar.research.RunRequest.$Properties} message RunRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RunRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.config != null && $Object.hasOwnProperty.call(message, "config"))
                    $root.northstar.research.ResearchConfigurationInput.encode(message.config, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.snapshot_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a RunRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.RunRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.RunRequest & northstar.research.RunRequest.$Shape} RunRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RunRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.RunRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.config = $root.northstar.research.ResearchConfigurationInput.decode(reader, reader.uint32(), $undefined, _depth + 1, message.config);
                            message._config = "config";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.snapshot_id = reader.stringVerify();
                            message._snapshot_id = "snapshot_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a RunRequest message.
             * @function verify
             * @memberof northstar.research.RunRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RunRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.config != null && $Object.hasOwnProperty.call(message, "config")) {
                    properties._config = 1;
                    {
                        let error = $root.northstar.research.ResearchConfigurationInput.verify(message.config, _depth + 1);
                        if (error)
                            return "config." + error;
                    }
                }
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id")) {
                    properties._snapshot_id = 1;
                    if (!$util.isString(message.snapshot_id))
                        return "snapshot_id: string expected";
                }
                return null;
            };

            /**
             * Creates a RunRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.RunRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.RunRequest} RunRequest
             */
            RunRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.RunRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.RunRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.RunRequest();
                if (object.config != null) {
                    if (!$util.isObject(object.config))
                        throw $TypeError(".northstar.research.RunRequest.config: object expected");
                    message.config = $root.northstar.research.ResearchConfigurationInput.fromObject(object.config, _depth + 1);
                }
                if (object.snapshot_id != null)
                    message.snapshot_id = $String(object.snapshot_id);
                return message;
            };

            /**
             * Creates a plain object from a RunRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.RunRequest
             * @static
             * @param {northstar.research.RunRequest} message RunRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RunRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.config != null && $Object.hasOwnProperty.call(message, "config")) {
                    object.config = $root.northstar.research.ResearchConfigurationInput.toObject(message.config, options, _depth + 1);
                    if (options.oneofs)
                        object._config = "config";
                }
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id")) {
                    object.snapshot_id = message.snapshot_id;
                    if (options.oneofs)
                        object._snapshot_id = "snapshot_id";
                }
                return object;
            };

            /**
             * Converts this RunRequest to JSON.
             * @function toJSON
             * @memberof northstar.research.RunRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RunRequest.prototype.toJSON = function() {
                return RunRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for RunRequest
             * @function getTypeUrl
             * @memberof northstar.research.RunRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            RunRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.RunRequest";
            };

            return RunRequest;
        })();

        research.RunSummary = (function() {

            /**
             * Properties of a RunSummary.
             * @typedef {Object} northstar.research.RunSummary.$Properties
             * @property {string|null} [code_revision] RunSummary code_revision
             * @property {boolean|null} [committed_code] RunSummary committed_code
             * @property {northstar.research.ResearchConfiguration.$Properties|null} [config] RunSummary config
             * @property {string|null} [created_at] RunSummary created_at
             * @property {google.protobuf.Struct.$Properties|null} [market] RunSummary market
             * @property {string|null} [run_id] RunSummary run_id
             * @property {northstar.research.SnapshotReference.$Properties|null} [snapshot] RunSummary snapshot
             * @property {northstar.research.ResearchSummary.$Properties|null} [summary] RunSummary summary
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] RunSummary evidence_fields
             * @property {"code_revision"} [_code_revision] RunSummary _code_revision
             * @property {"committed_code"} [_committed_code] RunSummary _committed_code
             * @property {"config"} [_config] RunSummary _config
             * @property {"created_at"} [_created_at] RunSummary _created_at
             * @property {"market"} [_market] RunSummary _market
             * @property {"run_id"} [_run_id] RunSummary _run_id
             * @property {"snapshot"} [_snapshot] RunSummary _snapshot
             * @property {"summary"} [_summary] RunSummary _summary
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a RunSummary.
             * @memberof northstar.research
             * @interface IRunSummary
             * @augments northstar.research.RunSummary.$Properties
             * @deprecated Use northstar.research.RunSummary.$Properties instead.
             */

            /**
             * Narrowed shape of a RunSummary.
             * @typedef {{
             *   code_revision?: string|null;
             *   committed_code?: boolean|null;
             *   config?: northstar.research.ResearchConfiguration.$Shape|null;
             *   created_at?: string|null;
             *   market?: google.protobuf.Struct.$Shape|null;
             *   run_id?: string|null;
             *   snapshot?: northstar.research.SnapshotReference.$Shape|null;
             *   summary?: northstar.research.ResearchSummary.$Shape|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _code_revision?: undefined; code_revision?: null }|{ _code_revision?: "code_revision"; code_revision: string })
             * ) & (
             *   ({ _committed_code?: undefined; committed_code?: null }|{ _committed_code?: "committed_code"; committed_code: boolean })
             * ) & (
             *   ({ _config?: undefined; config?: null }|{ _config?: "config"; config: northstar.research.ResearchConfiguration.$Shape })
             * ) & (
             *   ({ _created_at?: undefined; created_at?: null }|{ _created_at?: "created_at"; created_at: string })
             * ) & (
             *   ({ _market?: undefined; market?: null }|{ _market?: "market"; market: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _run_id?: undefined; run_id?: null }|{ _run_id?: "run_id"; run_id: string })
             * ) & (
             *   ({ _snapshot?: undefined; snapshot?: null }|{ _snapshot?: "snapshot"; snapshot: northstar.research.SnapshotReference.$Shape })
             * ) & (
             *   ({ _summary?: undefined; summary?: null }|{ _summary?: "summary"; summary: northstar.research.ResearchSummary.$Shape })
             * )} northstar.research.RunSummary.$Shape
             */

            /**
             * Constructs a new RunSummary.
             * @memberof northstar.research
             * @classdesc Represents a RunSummary.
             * @constructor
             * @param {northstar.research.RunSummary.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const RunSummary = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * RunSummary code_revision.
             * @member {string|null|undefined} code_revision
             * @memberof northstar.research.RunSummary
             * @instance
             */
            RunSummary.prototype.code_revision = null;

            /**
             * RunSummary committed_code.
             * @member {boolean|null|undefined} committed_code
             * @memberof northstar.research.RunSummary
             * @instance
             */
            RunSummary.prototype.committed_code = null;

            /**
             * RunSummary config.
             * @member {northstar.research.ResearchConfiguration.$Properties|null|undefined} config
             * @memberof northstar.research.RunSummary
             * @instance
             */
            RunSummary.prototype.config = null;

            /**
             * RunSummary created_at.
             * @member {string|null|undefined} created_at
             * @memberof northstar.research.RunSummary
             * @instance
             */
            RunSummary.prototype.created_at = null;

            /**
             * RunSummary market.
             * @member {google.protobuf.Struct.$Properties|null|undefined} market
             * @memberof northstar.research.RunSummary
             * @instance
             */
            RunSummary.prototype.market = null;

            /**
             * RunSummary run_id.
             * @member {string|null|undefined} run_id
             * @memberof northstar.research.RunSummary
             * @instance
             */
            RunSummary.prototype.run_id = null;

            /**
             * RunSummary snapshot.
             * @member {northstar.research.SnapshotReference.$Properties|null|undefined} snapshot
             * @memberof northstar.research.RunSummary
             * @instance
             */
            RunSummary.prototype.snapshot = null;

            /**
             * RunSummary summary.
             * @member {northstar.research.ResearchSummary.$Properties|null|undefined} summary
             * @memberof northstar.research.RunSummary
             * @instance
             */
            RunSummary.prototype.summary = null;

            /**
             * RunSummary evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.research.RunSummary
             * @instance
             */
            RunSummary.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * RunSummary _code_revision.
             * @member {"code_revision"|undefined} _code_revision
             * @memberof northstar.research.RunSummary
             * @instance
             */
            $Object.defineProperty(RunSummary.prototype, "_code_revision", {
                get: $util.oneOfGetter($oneOfFields = ["code_revision"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RunSummary _committed_code.
             * @member {"committed_code"|undefined} _committed_code
             * @memberof northstar.research.RunSummary
             * @instance
             */
            $Object.defineProperty(RunSummary.prototype, "_committed_code", {
                get: $util.oneOfGetter($oneOfFields = ["committed_code"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RunSummary _config.
             * @member {"config"|undefined} _config
             * @memberof northstar.research.RunSummary
             * @instance
             */
            $Object.defineProperty(RunSummary.prototype, "_config", {
                get: $util.oneOfGetter($oneOfFields = ["config"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RunSummary _created_at.
             * @member {"created_at"|undefined} _created_at
             * @memberof northstar.research.RunSummary
             * @instance
             */
            $Object.defineProperty(RunSummary.prototype, "_created_at", {
                get: $util.oneOfGetter($oneOfFields = ["created_at"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RunSummary _market.
             * @member {"market"|undefined} _market
             * @memberof northstar.research.RunSummary
             * @instance
             */
            $Object.defineProperty(RunSummary.prototype, "_market", {
                get: $util.oneOfGetter($oneOfFields = ["market"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RunSummary _run_id.
             * @member {"run_id"|undefined} _run_id
             * @memberof northstar.research.RunSummary
             * @instance
             */
            $Object.defineProperty(RunSummary.prototype, "_run_id", {
                get: $util.oneOfGetter($oneOfFields = ["run_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RunSummary _snapshot.
             * @member {"snapshot"|undefined} _snapshot
             * @memberof northstar.research.RunSummary
             * @instance
             */
            $Object.defineProperty(RunSummary.prototype, "_snapshot", {
                get: $util.oneOfGetter($oneOfFields = ["snapshot"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RunSummary _summary.
             * @member {"summary"|undefined} _summary
             * @memberof northstar.research.RunSummary
             * @instance
             */
            $Object.defineProperty(RunSummary.prototype, "_summary", {
                get: $util.oneOfGetter($oneOfFields = ["summary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new RunSummary instance using the specified properties.
             * @function create
             * @memberof northstar.research.RunSummary
             * @static
             * @param {northstar.research.RunSummary.$Properties=} [properties] Properties to set
             * @returns {northstar.research.RunSummary} RunSummary instance
             * @type {{
             *   (properties: northstar.research.RunSummary.$Shape): northstar.research.RunSummary & northstar.research.RunSummary.$Shape;
             *   (properties?: northstar.research.RunSummary.$Properties): northstar.research.RunSummary;
             * }}
             */
            RunSummary.create = function(properties) {
                return new RunSummary(properties);
            };

            /**
             * Encodes the specified RunSummary message. Does not implicitly {@link northstar.research.RunSummary.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.RunSummary
             * @static
             * @param {northstar.research.RunSummary.$Properties} message RunSummary message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RunSummary.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.code_revision);
                if (message.committed_code != null && $Object.hasOwnProperty.call(message, "committed_code"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.committed_code);
                if (message.config != null && $Object.hasOwnProperty.call(message, "config"))
                    $root.northstar.research.ResearchConfiguration.encode(message.config, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.created_at != null && $Object.hasOwnProperty.call(message, "created_at"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.created_at);
                if (message.market != null && $Object.hasOwnProperty.call(message, "market"))
                    $root.google.protobuf.Struct.encode(message.market, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                if (message.run_id != null && $Object.hasOwnProperty.call(message, "run_id"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.run_id);
                if (message.snapshot != null && $Object.hasOwnProperty.call(message, "snapshot"))
                    $root.northstar.research.SnapshotReference.encode(message.snapshot, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
                if (message.summary != null && $Object.hasOwnProperty.call(message, "summary"))
                    $root.northstar.research.ResearchSummary.encode(message.summary, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a RunSummary message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.RunSummary
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.RunSummary & northstar.research.RunSummary.$Shape} RunSummary
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RunSummary.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.RunSummary();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.code_revision = reader.stringVerify();
                            message._code_revision = "code_revision";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            message.committed_code = reader.bool();
                            message._committed_code = "committed_code";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.config = $root.northstar.research.ResearchConfiguration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.config);
                            message._config = "config";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.created_at = reader.stringVerify();
                            message._created_at = "created_at";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.market = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.market);
                            message._market = "market";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.run_id = reader.stringVerify();
                            message._run_id = "run_id";
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            message.snapshot = $root.northstar.research.SnapshotReference.decode(reader, reader.uint32(), $undefined, _depth + 1, message.snapshot);
                            message._snapshot = "snapshot";
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            message.summary = $root.northstar.research.ResearchSummary.decode(reader, reader.uint32(), $undefined, _depth + 1, message.summary);
                            message._summary = "summary";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a RunSummary message.
             * @function verify
             * @memberof northstar.research.RunSummary
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RunSummary.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision")) {
                    properties._code_revision = 1;
                    if (!$util.isString(message.code_revision))
                        return "code_revision: string expected";
                }
                if (message.committed_code != null && $Object.hasOwnProperty.call(message, "committed_code")) {
                    properties._committed_code = 1;
                    if (typeof message.committed_code !== "boolean")
                        return "committed_code: boolean expected";
                }
                if (message.config != null && $Object.hasOwnProperty.call(message, "config")) {
                    properties._config = 1;
                    {
                        let error = $root.northstar.research.ResearchConfiguration.verify(message.config, _depth + 1);
                        if (error)
                            return "config." + error;
                    }
                }
                if (message.created_at != null && $Object.hasOwnProperty.call(message, "created_at")) {
                    properties._created_at = 1;
                    if (!$util.isString(message.created_at))
                        return "created_at: string expected";
                }
                if (message.market != null && $Object.hasOwnProperty.call(message, "market")) {
                    properties._market = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.market, _depth + 1);
                        if (error)
                            return "market." + error;
                    }
                }
                if (message.run_id != null && $Object.hasOwnProperty.call(message, "run_id")) {
                    properties._run_id = 1;
                    if (!$util.isString(message.run_id))
                        return "run_id: string expected";
                }
                if (message.snapshot != null && $Object.hasOwnProperty.call(message, "snapshot")) {
                    properties._snapshot = 1;
                    {
                        let error = $root.northstar.research.SnapshotReference.verify(message.snapshot, _depth + 1);
                        if (error)
                            return "snapshot." + error;
                    }
                }
                if (message.summary != null && $Object.hasOwnProperty.call(message, "summary")) {
                    properties._summary = 1;
                    {
                        let error = $root.northstar.research.ResearchSummary.verify(message.summary, _depth + 1);
                        if (error)
                            return "summary." + error;
                    }
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a RunSummary message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.RunSummary
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.RunSummary} RunSummary
             */
            RunSummary.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.RunSummary)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.RunSummary: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.RunSummary();
                if (object.code_revision != null)
                    message.code_revision = $String(object.code_revision);
                if (object.committed_code != null)
                    message.committed_code = $Boolean(object.committed_code);
                if (object.config != null) {
                    if (!$util.isObject(object.config))
                        throw $TypeError(".northstar.research.RunSummary.config: object expected");
                    message.config = $root.northstar.research.ResearchConfiguration.fromObject(object.config, _depth + 1);
                }
                if (object.created_at != null)
                    message.created_at = $String(object.created_at);
                if (object.market != null) {
                    if (!$util.isObject(object.market))
                        throw $TypeError(".northstar.research.RunSummary.market: object expected");
                    message.market = $root.google.protobuf.Struct.fromObject(object.market, _depth + 1);
                }
                if (object.run_id != null)
                    message.run_id = $String(object.run_id);
                if (object.snapshot != null) {
                    if (!$util.isObject(object.snapshot))
                        throw $TypeError(".northstar.research.RunSummary.snapshot: object expected");
                    message.snapshot = $root.northstar.research.SnapshotReference.fromObject(object.snapshot, _depth + 1);
                }
                if (object.summary != null) {
                    if (!$util.isObject(object.summary))
                        throw $TypeError(".northstar.research.RunSummary.summary: object expected");
                    message.summary = $root.northstar.research.ResearchSummary.fromObject(object.summary, _depth + 1);
                }
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.research.RunSummary.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.research.RunSummary.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a RunSummary message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.RunSummary
             * @static
             * @param {northstar.research.RunSummary} message RunSummary
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RunSummary.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision")) {
                    object.code_revision = message.code_revision;
                    if (options.oneofs)
                        object._code_revision = "code_revision";
                }
                if (message.committed_code != null && $Object.hasOwnProperty.call(message, "committed_code")) {
                    object.committed_code = message.committed_code;
                    if (options.oneofs)
                        object._committed_code = "committed_code";
                }
                if (message.config != null && $Object.hasOwnProperty.call(message, "config")) {
                    object.config = $root.northstar.research.ResearchConfiguration.toObject(message.config, options, _depth + 1);
                    if (options.oneofs)
                        object._config = "config";
                }
                if (message.created_at != null && $Object.hasOwnProperty.call(message, "created_at")) {
                    object.created_at = message.created_at;
                    if (options.oneofs)
                        object._created_at = "created_at";
                }
                if (message.market != null && $Object.hasOwnProperty.call(message, "market")) {
                    object.market = $root.google.protobuf.Struct.toObject(message.market, options, _depth + 1);
                    if (options.oneofs)
                        object._market = "market";
                }
                if (message.run_id != null && $Object.hasOwnProperty.call(message, "run_id")) {
                    object.run_id = message.run_id;
                    if (options.oneofs)
                        object._run_id = "run_id";
                }
                if (message.snapshot != null && $Object.hasOwnProperty.call(message, "snapshot")) {
                    object.snapshot = $root.northstar.research.SnapshotReference.toObject(message.snapshot, options, _depth + 1);
                    if (options.oneofs)
                        object._snapshot = "snapshot";
                }
                if (message.summary != null && $Object.hasOwnProperty.call(message, "summary")) {
                    object.summary = $root.northstar.research.ResearchSummary.toObject(message.summary, options, _depth + 1);
                    if (options.oneofs)
                        object._summary = "summary";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this RunSummary to JSON.
             * @function toJSON
             * @memberof northstar.research.RunSummary
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RunSummary.prototype.toJSON = function() {
                return RunSummary.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for RunSummary
             * @function getTypeUrl
             * @memberof northstar.research.RunSummary
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            RunSummary.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.RunSummary";
            };

            return RunSummary;
        })();

        research.SavedConfiguration = (function() {

            /**
             * Properties of a SavedConfiguration.
             * @typedef {Object} northstar.research.SavedConfiguration.$Properties
             * @property {northstar.research.ResearchConfiguration.$Properties|null} [config] SavedConfiguration config
             * @property {string|null} [configuration_id] SavedConfiguration configuration_id
             * @property {string|null} [created_at] SavedConfiguration created_at
             * @property {string|null} [name] SavedConfiguration name
             * @property {string|null} [risk_hash] SavedConfiguration risk_hash
             * @property {string|null} [strategy_hash] SavedConfiguration strategy_hash
             * @property {"config"} [_config] SavedConfiguration _config
             * @property {"configuration_id"} [_configuration_id] SavedConfiguration _configuration_id
             * @property {"created_at"} [_created_at] SavedConfiguration _created_at
             * @property {"name"} [_name] SavedConfiguration _name
             * @property {"risk_hash"} [_risk_hash] SavedConfiguration _risk_hash
             * @property {"strategy_hash"} [_strategy_hash] SavedConfiguration _strategy_hash
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SavedConfiguration.
             * @memberof northstar.research
             * @interface ISavedConfiguration
             * @augments northstar.research.SavedConfiguration.$Properties
             * @deprecated Use northstar.research.SavedConfiguration.$Properties instead.
             */

            /**
             * Narrowed shape of a SavedConfiguration.
             * @typedef {{
             *   config?: northstar.research.ResearchConfiguration.$Shape|null;
             *   configuration_id?: string|null;
             *   created_at?: string|null;
             *   name?: string|null;
             *   risk_hash?: string|null;
             *   strategy_hash?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _config?: undefined; config?: null }|{ _config?: "config"; config: northstar.research.ResearchConfiguration.$Shape })
             * ) & (
             *   ({ _configuration_id?: undefined; configuration_id?: null }|{ _configuration_id?: "configuration_id"; configuration_id: string })
             * ) & (
             *   ({ _created_at?: undefined; created_at?: null }|{ _created_at?: "created_at"; created_at: string })
             * ) & (
             *   ({ _name?: undefined; name?: null }|{ _name?: "name"; name: string })
             * ) & (
             *   ({ _risk_hash?: undefined; risk_hash?: null }|{ _risk_hash?: "risk_hash"; risk_hash: string })
             * ) & (
             *   ({ _strategy_hash?: undefined; strategy_hash?: null }|{ _strategy_hash?: "strategy_hash"; strategy_hash: string })
             * )} northstar.research.SavedConfiguration.$Shape
             */

            /**
             * Constructs a new SavedConfiguration.
             * @memberof northstar.research
             * @classdesc Represents a SavedConfiguration.
             * @constructor
             * @param {northstar.research.SavedConfiguration.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const SavedConfiguration = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SavedConfiguration config.
             * @member {northstar.research.ResearchConfiguration.$Properties|null|undefined} config
             * @memberof northstar.research.SavedConfiguration
             * @instance
             */
            SavedConfiguration.prototype.config = null;

            /**
             * SavedConfiguration configuration_id.
             * @member {string|null|undefined} configuration_id
             * @memberof northstar.research.SavedConfiguration
             * @instance
             */
            SavedConfiguration.prototype.configuration_id = null;

            /**
             * SavedConfiguration created_at.
             * @member {string|null|undefined} created_at
             * @memberof northstar.research.SavedConfiguration
             * @instance
             */
            SavedConfiguration.prototype.created_at = null;

            /**
             * SavedConfiguration name.
             * @member {string|null|undefined} name
             * @memberof northstar.research.SavedConfiguration
             * @instance
             */
            SavedConfiguration.prototype.name = null;

            /**
             * SavedConfiguration risk_hash.
             * @member {string|null|undefined} risk_hash
             * @memberof northstar.research.SavedConfiguration
             * @instance
             */
            SavedConfiguration.prototype.risk_hash = null;

            /**
             * SavedConfiguration strategy_hash.
             * @member {string|null|undefined} strategy_hash
             * @memberof northstar.research.SavedConfiguration
             * @instance
             */
            SavedConfiguration.prototype.strategy_hash = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * SavedConfiguration _config.
             * @member {"config"|undefined} _config
             * @memberof northstar.research.SavedConfiguration
             * @instance
             */
            $Object.defineProperty(SavedConfiguration.prototype, "_config", {
                get: $util.oneOfGetter($oneOfFields = ["config"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * SavedConfiguration _configuration_id.
             * @member {"configuration_id"|undefined} _configuration_id
             * @memberof northstar.research.SavedConfiguration
             * @instance
             */
            $Object.defineProperty(SavedConfiguration.prototype, "_configuration_id", {
                get: $util.oneOfGetter($oneOfFields = ["configuration_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * SavedConfiguration _created_at.
             * @member {"created_at"|undefined} _created_at
             * @memberof northstar.research.SavedConfiguration
             * @instance
             */
            $Object.defineProperty(SavedConfiguration.prototype, "_created_at", {
                get: $util.oneOfGetter($oneOfFields = ["created_at"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * SavedConfiguration _name.
             * @member {"name"|undefined} _name
             * @memberof northstar.research.SavedConfiguration
             * @instance
             */
            $Object.defineProperty(SavedConfiguration.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * SavedConfiguration _risk_hash.
             * @member {"risk_hash"|undefined} _risk_hash
             * @memberof northstar.research.SavedConfiguration
             * @instance
             */
            $Object.defineProperty(SavedConfiguration.prototype, "_risk_hash", {
                get: $util.oneOfGetter($oneOfFields = ["risk_hash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * SavedConfiguration _strategy_hash.
             * @member {"strategy_hash"|undefined} _strategy_hash
             * @memberof northstar.research.SavedConfiguration
             * @instance
             */
            $Object.defineProperty(SavedConfiguration.prototype, "_strategy_hash", {
                get: $util.oneOfGetter($oneOfFields = ["strategy_hash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new SavedConfiguration instance using the specified properties.
             * @function create
             * @memberof northstar.research.SavedConfiguration
             * @static
             * @param {northstar.research.SavedConfiguration.$Properties=} [properties] Properties to set
             * @returns {northstar.research.SavedConfiguration} SavedConfiguration instance
             * @type {{
             *   (properties: northstar.research.SavedConfiguration.$Shape): northstar.research.SavedConfiguration & northstar.research.SavedConfiguration.$Shape;
             *   (properties?: northstar.research.SavedConfiguration.$Properties): northstar.research.SavedConfiguration;
             * }}
             */
            SavedConfiguration.create = function(properties) {
                return new SavedConfiguration(properties);
            };

            /**
             * Encodes the specified SavedConfiguration message. Does not implicitly {@link northstar.research.SavedConfiguration.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.SavedConfiguration
             * @static
             * @param {northstar.research.SavedConfiguration.$Properties} message SavedConfiguration message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SavedConfiguration.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.config != null && $Object.hasOwnProperty.call(message, "config"))
                    $root.northstar.research.ResearchConfiguration.encode(message.config, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.configuration_id != null && $Object.hasOwnProperty.call(message, "configuration_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.configuration_id);
                if (message.created_at != null && $Object.hasOwnProperty.call(message, "created_at"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.created_at);
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.name);
                if (message.risk_hash != null && $Object.hasOwnProperty.call(message, "risk_hash"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.risk_hash);
                if (message.strategy_hash != null && $Object.hasOwnProperty.call(message, "strategy_hash"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.strategy_hash);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a SavedConfiguration message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.SavedConfiguration
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.SavedConfiguration & northstar.research.SavedConfiguration.$Shape} SavedConfiguration
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SavedConfiguration.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.SavedConfiguration();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.config = $root.northstar.research.ResearchConfiguration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.config);
                            message._config = "config";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.configuration_id = reader.stringVerify();
                            message._configuration_id = "configuration_id";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.created_at = reader.stringVerify();
                            message._created_at = "created_at";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.risk_hash = reader.stringVerify();
                            message._risk_hash = "risk_hash";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.strategy_hash = reader.stringVerify();
                            message._strategy_hash = "strategy_hash";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a SavedConfiguration message.
             * @function verify
             * @memberof northstar.research.SavedConfiguration
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SavedConfiguration.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.config != null && $Object.hasOwnProperty.call(message, "config")) {
                    properties._config = 1;
                    {
                        let error = $root.northstar.research.ResearchConfiguration.verify(message.config, _depth + 1);
                        if (error)
                            return "config." + error;
                    }
                }
                if (message.configuration_id != null && $Object.hasOwnProperty.call(message, "configuration_id")) {
                    properties._configuration_id = 1;
                    if (!$util.isString(message.configuration_id))
                        return "configuration_id: string expected";
                }
                if (message.created_at != null && $Object.hasOwnProperty.call(message, "created_at")) {
                    properties._created_at = 1;
                    if (!$util.isString(message.created_at))
                        return "created_at: string expected";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                if (message.risk_hash != null && $Object.hasOwnProperty.call(message, "risk_hash")) {
                    properties._risk_hash = 1;
                    if (!$util.isString(message.risk_hash))
                        return "risk_hash: string expected";
                }
                if (message.strategy_hash != null && $Object.hasOwnProperty.call(message, "strategy_hash")) {
                    properties._strategy_hash = 1;
                    if (!$util.isString(message.strategy_hash))
                        return "strategy_hash: string expected";
                }
                return null;
            };

            /**
             * Creates a SavedConfiguration message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.SavedConfiguration
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.SavedConfiguration} SavedConfiguration
             */
            SavedConfiguration.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.SavedConfiguration)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.SavedConfiguration: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.SavedConfiguration();
                if (object.config != null) {
                    if (!$util.isObject(object.config))
                        throw $TypeError(".northstar.research.SavedConfiguration.config: object expected");
                    message.config = $root.northstar.research.ResearchConfiguration.fromObject(object.config, _depth + 1);
                }
                if (object.configuration_id != null)
                    message.configuration_id = $String(object.configuration_id);
                if (object.created_at != null)
                    message.created_at = $String(object.created_at);
                if (object.name != null)
                    message.name = $String(object.name);
                if (object.risk_hash != null)
                    message.risk_hash = $String(object.risk_hash);
                if (object.strategy_hash != null)
                    message.strategy_hash = $String(object.strategy_hash);
                return message;
            };

            /**
             * Creates a plain object from a SavedConfiguration message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.SavedConfiguration
             * @static
             * @param {northstar.research.SavedConfiguration} message SavedConfiguration
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SavedConfiguration.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.config != null && $Object.hasOwnProperty.call(message, "config")) {
                    object.config = $root.northstar.research.ResearchConfiguration.toObject(message.config, options, _depth + 1);
                    if (options.oneofs)
                        object._config = "config";
                }
                if (message.configuration_id != null && $Object.hasOwnProperty.call(message, "configuration_id")) {
                    object.configuration_id = message.configuration_id;
                    if (options.oneofs)
                        object._configuration_id = "configuration_id";
                }
                if (message.created_at != null && $Object.hasOwnProperty.call(message, "created_at")) {
                    object.created_at = message.created_at;
                    if (options.oneofs)
                        object._created_at = "created_at";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    object.name = message.name;
                    if (options.oneofs)
                        object._name = "name";
                }
                if (message.risk_hash != null && $Object.hasOwnProperty.call(message, "risk_hash")) {
                    object.risk_hash = message.risk_hash;
                    if (options.oneofs)
                        object._risk_hash = "risk_hash";
                }
                if (message.strategy_hash != null && $Object.hasOwnProperty.call(message, "strategy_hash")) {
                    object.strategy_hash = message.strategy_hash;
                    if (options.oneofs)
                        object._strategy_hash = "strategy_hash";
                }
                return object;
            };

            /**
             * Converts this SavedConfiguration to JSON.
             * @function toJSON
             * @memberof northstar.research.SavedConfiguration
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SavedConfiguration.prototype.toJSON = function() {
                return SavedConfiguration.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SavedConfiguration
             * @function getTypeUrl
             * @memberof northstar.research.SavedConfiguration
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SavedConfiguration.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.SavedConfiguration";
            };

            return SavedConfiguration;
        })();

        research.SimulationInput = (function() {

            /**
             * Properties of a SimulationInput.
             * @typedef {Object} northstar.research.SimulationInput.$Properties
             * @property {string|null} [fee_per_lot] SimulationInput fee_per_lot
             * @property {string|null} [initial_cash] SimulationInput initial_cash
             * @property {number|Long|null} [slippage_ticks] SimulationInput slippage_ticks
             * @property {"fee_per_lot"} [_fee_per_lot] SimulationInput _fee_per_lot
             * @property {"initial_cash"} [_initial_cash] SimulationInput _initial_cash
             * @property {"slippage_ticks"} [_slippage_ticks] SimulationInput _slippage_ticks
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SimulationInput.
             * @memberof northstar.research
             * @interface ISimulationInput
             * @augments northstar.research.SimulationInput.$Properties
             * @deprecated Use northstar.research.SimulationInput.$Properties instead.
             */

            /**
             * Narrowed shape of a SimulationInput.
             * @typedef {{
             *   fee_per_lot?: string|null;
             *   initial_cash?: string|null;
             *   slippage_ticks?: number|Long|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _fee_per_lot?: undefined; fee_per_lot?: null }|{ _fee_per_lot?: "fee_per_lot"; fee_per_lot: string })
             * ) & (
             *   ({ _initial_cash?: undefined; initial_cash?: null }|{ _initial_cash?: "initial_cash"; initial_cash: string })
             * ) & (
             *   ({ _slippage_ticks?: undefined; slippage_ticks?: null }|{ _slippage_ticks?: "slippage_ticks"; slippage_ticks: number|Long })
             * )} northstar.research.SimulationInput.$Shape
             */

            /**
             * Constructs a new SimulationInput.
             * @memberof northstar.research
             * @classdesc Represents a SimulationInput.
             * @constructor
             * @param {northstar.research.SimulationInput.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const SimulationInput = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SimulationInput fee_per_lot.
             * @member {string|null|undefined} fee_per_lot
             * @memberof northstar.research.SimulationInput
             * @instance
             */
            SimulationInput.prototype.fee_per_lot = null;

            /**
             * SimulationInput initial_cash.
             * @member {string|null|undefined} initial_cash
             * @memberof northstar.research.SimulationInput
             * @instance
             */
            SimulationInput.prototype.initial_cash = null;

            /**
             * SimulationInput slippage_ticks.
             * @member {number|Long|null|undefined} slippage_ticks
             * @memberof northstar.research.SimulationInput
             * @instance
             */
            SimulationInput.prototype.slippage_ticks = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * SimulationInput _fee_per_lot.
             * @member {"fee_per_lot"|undefined} _fee_per_lot
             * @memberof northstar.research.SimulationInput
             * @instance
             */
            $Object.defineProperty(SimulationInput.prototype, "_fee_per_lot", {
                get: $util.oneOfGetter($oneOfFields = ["fee_per_lot"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * SimulationInput _initial_cash.
             * @member {"initial_cash"|undefined} _initial_cash
             * @memberof northstar.research.SimulationInput
             * @instance
             */
            $Object.defineProperty(SimulationInput.prototype, "_initial_cash", {
                get: $util.oneOfGetter($oneOfFields = ["initial_cash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * SimulationInput _slippage_ticks.
             * @member {"slippage_ticks"|undefined} _slippage_ticks
             * @memberof northstar.research.SimulationInput
             * @instance
             */
            $Object.defineProperty(SimulationInput.prototype, "_slippage_ticks", {
                get: $util.oneOfGetter($oneOfFields = ["slippage_ticks"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new SimulationInput instance using the specified properties.
             * @function create
             * @memberof northstar.research.SimulationInput
             * @static
             * @param {northstar.research.SimulationInput.$Properties=} [properties] Properties to set
             * @returns {northstar.research.SimulationInput} SimulationInput instance
             * @type {{
             *   (properties: northstar.research.SimulationInput.$Shape): northstar.research.SimulationInput & northstar.research.SimulationInput.$Shape;
             *   (properties?: northstar.research.SimulationInput.$Properties): northstar.research.SimulationInput;
             * }}
             */
            SimulationInput.create = function(properties) {
                return new SimulationInput(properties);
            };

            /**
             * Encodes the specified SimulationInput message. Does not implicitly {@link northstar.research.SimulationInput.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.SimulationInput
             * @static
             * @param {northstar.research.SimulationInput.$Properties} message SimulationInput message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SimulationInput.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.fee_per_lot != null && $Object.hasOwnProperty.call(message, "fee_per_lot"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.fee_per_lot);
                if (message.initial_cash != null && $Object.hasOwnProperty.call(message, "initial_cash"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.initial_cash);
                if (message.slippage_ticks != null && $Object.hasOwnProperty.call(message, "slippage_ticks"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.slippage_ticks);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a SimulationInput message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.SimulationInput
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.SimulationInput & northstar.research.SimulationInput.$Shape} SimulationInput
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SimulationInput.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.SimulationInput();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.fee_per_lot = reader.stringVerify();
                            message._fee_per_lot = "fee_per_lot";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.initial_cash = reader.stringVerify();
                            message._initial_cash = "initial_cash";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            message.slippage_ticks = reader.int64();
                            message._slippage_ticks = "slippage_ticks";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a SimulationInput message.
             * @function verify
             * @memberof northstar.research.SimulationInput
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SimulationInput.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.fee_per_lot != null && $Object.hasOwnProperty.call(message, "fee_per_lot")) {
                    properties._fee_per_lot = 1;
                    if (!$util.isString(message.fee_per_lot))
                        return "fee_per_lot: string expected";
                }
                if (message.initial_cash != null && $Object.hasOwnProperty.call(message, "initial_cash")) {
                    properties._initial_cash = 1;
                    if (!$util.isString(message.initial_cash))
                        return "initial_cash: string expected";
                }
                if (message.slippage_ticks != null && $Object.hasOwnProperty.call(message, "slippage_ticks")) {
                    properties._slippage_ticks = 1;
                    if (!$util.isInteger(message.slippage_ticks) && !(message.slippage_ticks && $util.isInteger(message.slippage_ticks.low) && $util.isInteger(message.slippage_ticks.high)))
                        return "slippage_ticks: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a SimulationInput message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.SimulationInput
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.SimulationInput} SimulationInput
             */
            SimulationInput.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.SimulationInput)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.SimulationInput: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.SimulationInput();
                if (object.fee_per_lot != null)
                    message.fee_per_lot = $String(object.fee_per_lot);
                if (object.initial_cash != null)
                    message.initial_cash = $String(object.initial_cash);
                if (object.slippage_ticks != null)
                    if ($util.Long)
                        message.slippage_ticks = $util.Long.fromValue(object.slippage_ticks, false);
                    else if (typeof object.slippage_ticks === "string")
                        message.slippage_ticks = $parseInt(object.slippage_ticks, 10);
                    else if (typeof object.slippage_ticks === "number")
                        message.slippage_ticks = object.slippage_ticks;
                    else if (typeof object.slippage_ticks === "object")
                        message.slippage_ticks = new $util.LongBits(object.slippage_ticks.low >>> 0, object.slippage_ticks.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a SimulationInput message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.SimulationInput
             * @static
             * @param {northstar.research.SimulationInput} message SimulationInput
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SimulationInput.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.fee_per_lot != null && $Object.hasOwnProperty.call(message, "fee_per_lot")) {
                    object.fee_per_lot = message.fee_per_lot;
                    if (options.oneofs)
                        object._fee_per_lot = "fee_per_lot";
                }
                if (message.initial_cash != null && $Object.hasOwnProperty.call(message, "initial_cash")) {
                    object.initial_cash = message.initial_cash;
                    if (options.oneofs)
                        object._initial_cash = "initial_cash";
                }
                if (message.slippage_ticks != null && $Object.hasOwnProperty.call(message, "slippage_ticks")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.slippage_ticks = typeof message.slippage_ticks === "number" ? $BigInt(message.slippage_ticks) : $util.Long.fromBits(message.slippage_ticks.low >>> 0, message.slippage_ticks.high >>> 0, false).toBigInt();
                    else if (typeof message.slippage_ticks === "number")
                        object.slippage_ticks = options.longs === $String ? $String(message.slippage_ticks) : message.slippage_ticks;
                    else
                        object.slippage_ticks = options.longs === $String ? $util.Long.prototype.toString.call(message.slippage_ticks) : options.longs === $Number ? new $util.LongBits(message.slippage_ticks.low >>> 0, message.slippage_ticks.high >>> 0).toNumber() : message.slippage_ticks;
                    if (options.oneofs)
                        object._slippage_ticks = "slippage_ticks";
                }
                return object;
            };

            /**
             * Converts this SimulationInput to JSON.
             * @function toJSON
             * @memberof northstar.research.SimulationInput
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SimulationInput.prototype.toJSON = function() {
                return SimulationInput.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SimulationInput
             * @function getTypeUrl
             * @memberof northstar.research.SimulationInput
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SimulationInput.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.SimulationInput";
            };

            return SimulationInput;
        })();

        research.SnapshotReference = (function() {

            /**
             * Properties of a SnapshotReference.
             * @typedef {Object} northstar.research.SnapshotReference.$Properties
             * @property {string|null} [content_hash] SnapshotReference content_hash
             * @property {string|null} [id] SnapshotReference id
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] SnapshotReference evidence_fields
             * @property {"content_hash"} [_content_hash] SnapshotReference _content_hash
             * @property {"id"} [_id] SnapshotReference _id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SnapshotReference.
             * @memberof northstar.research
             * @interface ISnapshotReference
             * @augments northstar.research.SnapshotReference.$Properties
             * @deprecated Use northstar.research.SnapshotReference.$Properties instead.
             */

            /**
             * Narrowed shape of a SnapshotReference.
             * @typedef {{
             *   content_hash?: string|null;
             *   id?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _content_hash?: undefined; content_hash?: null }|{ _content_hash?: "content_hash"; content_hash: string })
             * ) & (
             *   ({ _id?: undefined; id?: null }|{ _id?: "id"; id: string })
             * )} northstar.research.SnapshotReference.$Shape
             */

            /**
             * Constructs a new SnapshotReference.
             * @memberof northstar.research
             * @classdesc Represents a SnapshotReference.
             * @constructor
             * @param {northstar.research.SnapshotReference.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const SnapshotReference = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SnapshotReference content_hash.
             * @member {string|null|undefined} content_hash
             * @memberof northstar.research.SnapshotReference
             * @instance
             */
            SnapshotReference.prototype.content_hash = null;

            /**
             * SnapshotReference id.
             * @member {string|null|undefined} id
             * @memberof northstar.research.SnapshotReference
             * @instance
             */
            SnapshotReference.prototype.id = null;

            /**
             * SnapshotReference evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.research.SnapshotReference
             * @instance
             */
            SnapshotReference.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * SnapshotReference _content_hash.
             * @member {"content_hash"|undefined} _content_hash
             * @memberof northstar.research.SnapshotReference
             * @instance
             */
            $Object.defineProperty(SnapshotReference.prototype, "_content_hash", {
                get: $util.oneOfGetter($oneOfFields = ["content_hash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * SnapshotReference _id.
             * @member {"id"|undefined} _id
             * @memberof northstar.research.SnapshotReference
             * @instance
             */
            $Object.defineProperty(SnapshotReference.prototype, "_id", {
                get: $util.oneOfGetter($oneOfFields = ["id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new SnapshotReference instance using the specified properties.
             * @function create
             * @memberof northstar.research.SnapshotReference
             * @static
             * @param {northstar.research.SnapshotReference.$Properties=} [properties] Properties to set
             * @returns {northstar.research.SnapshotReference} SnapshotReference instance
             * @type {{
             *   (properties: northstar.research.SnapshotReference.$Shape): northstar.research.SnapshotReference & northstar.research.SnapshotReference.$Shape;
             *   (properties?: northstar.research.SnapshotReference.$Properties): northstar.research.SnapshotReference;
             * }}
             */
            SnapshotReference.create = function(properties) {
                return new SnapshotReference(properties);
            };

            /**
             * Encodes the specified SnapshotReference message. Does not implicitly {@link northstar.research.SnapshotReference.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.SnapshotReference
             * @static
             * @param {northstar.research.SnapshotReference.$Properties} message SnapshotReference message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SnapshotReference.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.content_hash != null && $Object.hasOwnProperty.call(message, "content_hash"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.content_hash);
                if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.id);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a SnapshotReference message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.SnapshotReference
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.SnapshotReference & northstar.research.SnapshotReference.$Shape} SnapshotReference
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SnapshotReference.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.SnapshotReference();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.content_hash = reader.stringVerify();
                            message._content_hash = "content_hash";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.id = reader.stringVerify();
                            message._id = "id";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a SnapshotReference message.
             * @function verify
             * @memberof northstar.research.SnapshotReference
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SnapshotReference.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.content_hash != null && $Object.hasOwnProperty.call(message, "content_hash")) {
                    properties._content_hash = 1;
                    if (!$util.isString(message.content_hash))
                        return "content_hash: string expected";
                }
                if (message.id != null && $Object.hasOwnProperty.call(message, "id")) {
                    properties._id = 1;
                    if (!$util.isString(message.id))
                        return "id: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a SnapshotReference message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.SnapshotReference
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.SnapshotReference} SnapshotReference
             */
            SnapshotReference.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.SnapshotReference)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.SnapshotReference: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.SnapshotReference();
                if (object.content_hash != null)
                    message.content_hash = $String(object.content_hash);
                if (object.id != null)
                    message.id = $String(object.id);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.research.SnapshotReference.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.research.SnapshotReference.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a SnapshotReference message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.SnapshotReference
             * @static
             * @param {northstar.research.SnapshotReference} message SnapshotReference
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SnapshotReference.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.content_hash != null && $Object.hasOwnProperty.call(message, "content_hash")) {
                    object.content_hash = message.content_hash;
                    if (options.oneofs)
                        object._content_hash = "content_hash";
                }
                if (message.id != null && $Object.hasOwnProperty.call(message, "id")) {
                    object.id = message.id;
                    if (options.oneofs)
                        object._id = "id";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this SnapshotReference to JSON.
             * @function toJSON
             * @memberof northstar.research.SnapshotReference
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SnapshotReference.prototype.toJSON = function() {
                return SnapshotReference.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SnapshotReference
             * @function getTypeUrl
             * @memberof northstar.research.SnapshotReference
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SnapshotReference.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.SnapshotReference";
            };

            return SnapshotReference;
        })();

        research.StrategyCandidate = (function() {

            /**
             * Properties of a StrategyCandidate.
             * @typedef {Object} northstar.research.StrategyCandidate.$Properties
             * @property {string|null} [candidate_id] StrategyCandidate candidate_id
             * @property {northstar.research.VersionDocument.$Properties|null} [document] StrategyCandidate document
             * @property {number|Long|null} [format] StrategyCandidate format
             * @property {boolean|null} [production_eligible] StrategyCandidate production_eligible
             * @property {string|null} [version_id] StrategyCandidate version_id
             * @property {"candidate_id"} [_candidate_id] StrategyCandidate _candidate_id
             * @property {"document"} [_document] StrategyCandidate _document
             * @property {"format"} [_format] StrategyCandidate _format
             * @property {"production_eligible"} [_production_eligible] StrategyCandidate _production_eligible
             * @property {"version_id"} [_version_id] StrategyCandidate _version_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StrategyCandidate.
             * @memberof northstar.research
             * @interface IStrategyCandidate
             * @augments northstar.research.StrategyCandidate.$Properties
             * @deprecated Use northstar.research.StrategyCandidate.$Properties instead.
             */

            /**
             * Narrowed shape of a StrategyCandidate.
             * @typedef {{
             *   candidate_id?: string|null;
             *   document?: northstar.research.VersionDocument.$Shape|null;
             *   format?: number|Long|null;
             *   production_eligible?: boolean|null;
             *   version_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _candidate_id?: undefined; candidate_id?: null }|{ _candidate_id?: "candidate_id"; candidate_id: string })
             * ) & (
             *   ({ _document?: undefined; document?: null }|{ _document?: "document"; document: northstar.research.VersionDocument.$Shape })
             * ) & (
             *   ({ _format?: undefined; format?: null }|{ _format?: "format"; format: number|Long })
             * ) & (
             *   ({ _production_eligible?: undefined; production_eligible?: null }|{ _production_eligible?: "production_eligible"; production_eligible: boolean })
             * ) & (
             *   ({ _version_id?: undefined; version_id?: null }|{ _version_id?: "version_id"; version_id: string })
             * )} northstar.research.StrategyCandidate.$Shape
             */

            /**
             * Constructs a new StrategyCandidate.
             * @memberof northstar.research
             * @classdesc Represents a StrategyCandidate.
             * @constructor
             * @param {northstar.research.StrategyCandidate.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StrategyCandidate = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StrategyCandidate candidate_id.
             * @member {string|null|undefined} candidate_id
             * @memberof northstar.research.StrategyCandidate
             * @instance
             */
            StrategyCandidate.prototype.candidate_id = null;

            /**
             * StrategyCandidate document.
             * @member {northstar.research.VersionDocument.$Properties|null|undefined} document
             * @memberof northstar.research.StrategyCandidate
             * @instance
             */
            StrategyCandidate.prototype.document = null;

            /**
             * StrategyCandidate format.
             * @member {number|Long|null|undefined} format
             * @memberof northstar.research.StrategyCandidate
             * @instance
             */
            StrategyCandidate.prototype.format = null;

            /**
             * StrategyCandidate production_eligible.
             * @member {boolean|null|undefined} production_eligible
             * @memberof northstar.research.StrategyCandidate
             * @instance
             */
            StrategyCandidate.prototype.production_eligible = null;

            /**
             * StrategyCandidate version_id.
             * @member {string|null|undefined} version_id
             * @memberof northstar.research.StrategyCandidate
             * @instance
             */
            StrategyCandidate.prototype.version_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * StrategyCandidate _candidate_id.
             * @member {"candidate_id"|undefined} _candidate_id
             * @memberof northstar.research.StrategyCandidate
             * @instance
             */
            $Object.defineProperty(StrategyCandidate.prototype, "_candidate_id", {
                get: $util.oneOfGetter($oneOfFields = ["candidate_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StrategyCandidate _document.
             * @member {"document"|undefined} _document
             * @memberof northstar.research.StrategyCandidate
             * @instance
             */
            $Object.defineProperty(StrategyCandidate.prototype, "_document", {
                get: $util.oneOfGetter($oneOfFields = ["document"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StrategyCandidate _format.
             * @member {"format"|undefined} _format
             * @memberof northstar.research.StrategyCandidate
             * @instance
             */
            $Object.defineProperty(StrategyCandidate.prototype, "_format", {
                get: $util.oneOfGetter($oneOfFields = ["format"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StrategyCandidate _production_eligible.
             * @member {"production_eligible"|undefined} _production_eligible
             * @memberof northstar.research.StrategyCandidate
             * @instance
             */
            $Object.defineProperty(StrategyCandidate.prototype, "_production_eligible", {
                get: $util.oneOfGetter($oneOfFields = ["production_eligible"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StrategyCandidate _version_id.
             * @member {"version_id"|undefined} _version_id
             * @memberof northstar.research.StrategyCandidate
             * @instance
             */
            $Object.defineProperty(StrategyCandidate.prototype, "_version_id", {
                get: $util.oneOfGetter($oneOfFields = ["version_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StrategyCandidate instance using the specified properties.
             * @function create
             * @memberof northstar.research.StrategyCandidate
             * @static
             * @param {northstar.research.StrategyCandidate.$Properties=} [properties] Properties to set
             * @returns {northstar.research.StrategyCandidate} StrategyCandidate instance
             * @type {{
             *   (properties: northstar.research.StrategyCandidate.$Shape): northstar.research.StrategyCandidate & northstar.research.StrategyCandidate.$Shape;
             *   (properties?: northstar.research.StrategyCandidate.$Properties): northstar.research.StrategyCandidate;
             * }}
             */
            StrategyCandidate.create = function(properties) {
                return new StrategyCandidate(properties);
            };

            /**
             * Encodes the specified StrategyCandidate message. Does not implicitly {@link northstar.research.StrategyCandidate.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.StrategyCandidate
             * @static
             * @param {northstar.research.StrategyCandidate.$Properties} message StrategyCandidate message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StrategyCandidate.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.candidate_id != null && $Object.hasOwnProperty.call(message, "candidate_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.candidate_id);
                if (message.document != null && $Object.hasOwnProperty.call(message, "document"))
                    $root.northstar.research.VersionDocument.encode(message.document, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.format != null && $Object.hasOwnProperty.call(message, "format"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.format);
                if (message.production_eligible != null && $Object.hasOwnProperty.call(message, "production_eligible"))
                    writer.uint32(/* id 4, wireType 0 =*/32).bool(message.production_eligible);
                if (message.version_id != null && $Object.hasOwnProperty.call(message, "version_id"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.version_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a StrategyCandidate message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.StrategyCandidate
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.StrategyCandidate & northstar.research.StrategyCandidate.$Shape} StrategyCandidate
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StrategyCandidate.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.StrategyCandidate();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.candidate_id = reader.stringVerify();
                            message._candidate_id = "candidate_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.document = $root.northstar.research.VersionDocument.decode(reader, reader.uint32(), $undefined, _depth + 1, message.document);
                            message._document = "document";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            message.format = reader.int64();
                            message._format = "format";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            message.production_eligible = reader.bool();
                            message._production_eligible = "production_eligible";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.version_id = reader.stringVerify();
                            message._version_id = "version_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a StrategyCandidate message.
             * @function verify
             * @memberof northstar.research.StrategyCandidate
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StrategyCandidate.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.candidate_id != null && $Object.hasOwnProperty.call(message, "candidate_id")) {
                    properties._candidate_id = 1;
                    if (!$util.isString(message.candidate_id))
                        return "candidate_id: string expected";
                }
                if (message.document != null && $Object.hasOwnProperty.call(message, "document")) {
                    properties._document = 1;
                    {
                        let error = $root.northstar.research.VersionDocument.verify(message.document, _depth + 1);
                        if (error)
                            return "document." + error;
                    }
                }
                if (message.format != null && $Object.hasOwnProperty.call(message, "format")) {
                    properties._format = 1;
                    if (!$util.isInteger(message.format) && !(message.format && $util.isInteger(message.format.low) && $util.isInteger(message.format.high)))
                        return "format: integer|Long expected";
                }
                if (message.production_eligible != null && $Object.hasOwnProperty.call(message, "production_eligible")) {
                    properties._production_eligible = 1;
                    if (typeof message.production_eligible !== "boolean")
                        return "production_eligible: boolean expected";
                }
                if (message.version_id != null && $Object.hasOwnProperty.call(message, "version_id")) {
                    properties._version_id = 1;
                    if (!$util.isString(message.version_id))
                        return "version_id: string expected";
                }
                return null;
            };

            /**
             * Creates a StrategyCandidate message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.StrategyCandidate
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.StrategyCandidate} StrategyCandidate
             */
            StrategyCandidate.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.StrategyCandidate)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.StrategyCandidate: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.StrategyCandidate();
                if (object.candidate_id != null)
                    message.candidate_id = $String(object.candidate_id);
                if (object.document != null) {
                    if (!$util.isObject(object.document))
                        throw $TypeError(".northstar.research.StrategyCandidate.document: object expected");
                    message.document = $root.northstar.research.VersionDocument.fromObject(object.document, _depth + 1);
                }
                if (object.format != null)
                    if ($util.Long)
                        message.format = $util.Long.fromValue(object.format, false);
                    else if (typeof object.format === "string")
                        message.format = $parseInt(object.format, 10);
                    else if (typeof object.format === "number")
                        message.format = object.format;
                    else if (typeof object.format === "object")
                        message.format = new $util.LongBits(object.format.low >>> 0, object.format.high >>> 0).toNumber();
                if (object.production_eligible != null)
                    message.production_eligible = $Boolean(object.production_eligible);
                if (object.version_id != null)
                    message.version_id = $String(object.version_id);
                return message;
            };

            /**
             * Creates a plain object from a StrategyCandidate message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.StrategyCandidate
             * @static
             * @param {northstar.research.StrategyCandidate} message StrategyCandidate
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StrategyCandidate.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.candidate_id != null && $Object.hasOwnProperty.call(message, "candidate_id")) {
                    object.candidate_id = message.candidate_id;
                    if (options.oneofs)
                        object._candidate_id = "candidate_id";
                }
                if (message.document != null && $Object.hasOwnProperty.call(message, "document")) {
                    object.document = $root.northstar.research.VersionDocument.toObject(message.document, options, _depth + 1);
                    if (options.oneofs)
                        object._document = "document";
                }
                if (message.format != null && $Object.hasOwnProperty.call(message, "format")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.format = typeof message.format === "number" ? $BigInt(message.format) : $util.Long.fromBits(message.format.low >>> 0, message.format.high >>> 0, false).toBigInt();
                    else if (typeof message.format === "number")
                        object.format = options.longs === $String ? $String(message.format) : message.format;
                    else
                        object.format = options.longs === $String ? $util.Long.prototype.toString.call(message.format) : options.longs === $Number ? new $util.LongBits(message.format.low >>> 0, message.format.high >>> 0).toNumber() : message.format;
                    if (options.oneofs)
                        object._format = "format";
                }
                if (message.production_eligible != null && $Object.hasOwnProperty.call(message, "production_eligible")) {
                    object.production_eligible = message.production_eligible;
                    if (options.oneofs)
                        object._production_eligible = "production_eligible";
                }
                if (message.version_id != null && $Object.hasOwnProperty.call(message, "version_id")) {
                    object.version_id = message.version_id;
                    if (options.oneofs)
                        object._version_id = "version_id";
                }
                return object;
            };

            /**
             * Converts this StrategyCandidate to JSON.
             * @function toJSON
             * @memberof northstar.research.StrategyCandidate
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StrategyCandidate.prototype.toJSON = function() {
                return StrategyCandidate.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StrategyCandidate
             * @function getTypeUrl
             * @memberof northstar.research.StrategyCandidate
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StrategyCandidate.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.StrategyCandidate";
            };

            return StrategyCandidate;
        })();

        research.StrategyDescription = (function() {

            /**
             * Properties of a StrategyDescription.
             * @typedef {Object} northstar.research.StrategyDescription.$Properties
             * @property {string|null} [category] StrategyDescription category
             * @property {string|null} [description] StrategyDescription description
             * @property {Object.<string,string>|null} [factor_slots] StrategyDescription factor_slots
             * @property {string|null} [name] StrategyDescription name
             * @property {Array.<northstar.research.ParameterDescription.$Properties>|null} [parameters] StrategyDescription parameters
             * @property {string|null} [revision] StrategyDescription revision
             * @property {string|null} [strategy_id] StrategyDescription strategy_id
             * @property {"category"} [_category] StrategyDescription _category
             * @property {"description"} [_description] StrategyDescription _description
             * @property {"name"} [_name] StrategyDescription _name
             * @property {"revision"} [_revision] StrategyDescription _revision
             * @property {"strategy_id"} [_strategy_id] StrategyDescription _strategy_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StrategyDescription.
             * @memberof northstar.research
             * @interface IStrategyDescription
             * @augments northstar.research.StrategyDescription.$Properties
             * @deprecated Use northstar.research.StrategyDescription.$Properties instead.
             */

            /**
             * Narrowed shape of a StrategyDescription.
             * @typedef {{
             *   category?: string|null;
             *   description?: string|null;
             *   factor_slots?: Object.<string,string>|null;
             *   name?: string|null;
             *   parameters?: Array.<northstar.research.ParameterDescription.$Shape>|null;
             *   revision?: string|null;
             *   strategy_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _category?: undefined; category?: null }|{ _category?: "category"; category: string })
             * ) & (
             *   ({ _description?: undefined; description?: null }|{ _description?: "description"; description: string })
             * ) & (
             *   ({ _name?: undefined; name?: null }|{ _name?: "name"; name: string })
             * ) & (
             *   ({ _revision?: undefined; revision?: null }|{ _revision?: "revision"; revision: string })
             * ) & (
             *   ({ _strategy_id?: undefined; strategy_id?: null }|{ _strategy_id?: "strategy_id"; strategy_id: string })
             * )} northstar.research.StrategyDescription.$Shape
             */

            /**
             * Constructs a new StrategyDescription.
             * @memberof northstar.research
             * @classdesc Represents a StrategyDescription.
             * @constructor
             * @param {northstar.research.StrategyDescription.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StrategyDescription = function (properties) {
                this.factor_slots = {};
                this.parameters = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StrategyDescription category.
             * @member {string|null|undefined} category
             * @memberof northstar.research.StrategyDescription
             * @instance
             */
            StrategyDescription.prototype.category = null;

            /**
             * StrategyDescription description.
             * @member {string|null|undefined} description
             * @memberof northstar.research.StrategyDescription
             * @instance
             */
            StrategyDescription.prototype.description = null;

            /**
             * StrategyDescription factor_slots.
             * @member {Object.<string,string>} factor_slots
             * @memberof northstar.research.StrategyDescription
             * @instance
             */
            StrategyDescription.prototype.factor_slots = $util.emptyObject;

            /**
             * StrategyDescription name.
             * @member {string|null|undefined} name
             * @memberof northstar.research.StrategyDescription
             * @instance
             */
            StrategyDescription.prototype.name = null;

            /**
             * StrategyDescription parameters.
             * @member {Array.<northstar.research.ParameterDescription.$Properties>} parameters
             * @memberof northstar.research.StrategyDescription
             * @instance
             */
            StrategyDescription.prototype.parameters = $util.emptyArray;

            /**
             * StrategyDescription revision.
             * @member {string|null|undefined} revision
             * @memberof northstar.research.StrategyDescription
             * @instance
             */
            StrategyDescription.prototype.revision = null;

            /**
             * StrategyDescription strategy_id.
             * @member {string|null|undefined} strategy_id
             * @memberof northstar.research.StrategyDescription
             * @instance
             */
            StrategyDescription.prototype.strategy_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * StrategyDescription _category.
             * @member {"category"|undefined} _category
             * @memberof northstar.research.StrategyDescription
             * @instance
             */
            $Object.defineProperty(StrategyDescription.prototype, "_category", {
                get: $util.oneOfGetter($oneOfFields = ["category"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StrategyDescription _description.
             * @member {"description"|undefined} _description
             * @memberof northstar.research.StrategyDescription
             * @instance
             */
            $Object.defineProperty(StrategyDescription.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StrategyDescription _name.
             * @member {"name"|undefined} _name
             * @memberof northstar.research.StrategyDescription
             * @instance
             */
            $Object.defineProperty(StrategyDescription.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StrategyDescription _revision.
             * @member {"revision"|undefined} _revision
             * @memberof northstar.research.StrategyDescription
             * @instance
             */
            $Object.defineProperty(StrategyDescription.prototype, "_revision", {
                get: $util.oneOfGetter($oneOfFields = ["revision"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StrategyDescription _strategy_id.
             * @member {"strategy_id"|undefined} _strategy_id
             * @memberof northstar.research.StrategyDescription
             * @instance
             */
            $Object.defineProperty(StrategyDescription.prototype, "_strategy_id", {
                get: $util.oneOfGetter($oneOfFields = ["strategy_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StrategyDescription instance using the specified properties.
             * @function create
             * @memberof northstar.research.StrategyDescription
             * @static
             * @param {northstar.research.StrategyDescription.$Properties=} [properties] Properties to set
             * @returns {northstar.research.StrategyDescription} StrategyDescription instance
             * @type {{
             *   (properties: northstar.research.StrategyDescription.$Shape): northstar.research.StrategyDescription & northstar.research.StrategyDescription.$Shape;
             *   (properties?: northstar.research.StrategyDescription.$Properties): northstar.research.StrategyDescription;
             * }}
             */
            StrategyDescription.create = function(properties) {
                return new StrategyDescription(properties);
            };

            /**
             * Encodes the specified StrategyDescription message. Does not implicitly {@link northstar.research.StrategyDescription.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.StrategyDescription
             * @static
             * @param {northstar.research.StrategyDescription.$Properties} message StrategyDescription message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StrategyDescription.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.category != null && $Object.hasOwnProperty.call(message, "category"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.category);
                if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.description);
                if (message.factor_slots != null && $Object.hasOwnProperty.call(message, "factor_slots"))
                    for (let keys = $Object.keys(message.factor_slots), i = 0; i < keys.length; ++i)
                        writer.uint32(/* id 3, wireType 2 =*/26).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.factor_slots[keys[i]]).ldelim();
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.name);
                if (message.parameters != null && message.parameters.length)
                    for (let i = 0; i < message.parameters.length; ++i)
                        $root.northstar.research.ParameterDescription.encode(message.parameters[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                if (message.revision != null && $Object.hasOwnProperty.call(message, "revision"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.revision);
                if (message.strategy_id != null && $Object.hasOwnProperty.call(message, "strategy_id"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.strategy_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a StrategyDescription message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.StrategyDescription
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.StrategyDescription & northstar.research.StrategyDescription.$Shape} StrategyDescription
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StrategyDescription.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.StrategyDescription();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.category = reader.stringVerify();
                            message._category = "category";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.description = reader.stringVerify();
                            message._description = "description";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if (message.factor_slots === $util.emptyObject)
                                message.factor_slots = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = "";
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = reader.stringVerify();
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.factor_slots, key);
                            message.factor_slots[key] = value;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            if (!(message.parameters && message.parameters.length))
                                message.parameters = [];
                            message.parameters.push($root.northstar.research.ParameterDescription.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.revision = reader.stringVerify();
                            message._revision = "revision";
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            message.strategy_id = reader.stringVerify();
                            message._strategy_id = "strategy_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a StrategyDescription message.
             * @function verify
             * @memberof northstar.research.StrategyDescription
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StrategyDescription.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.category != null && $Object.hasOwnProperty.call(message, "category")) {
                    properties._category = 1;
                    if (!$util.isString(message.category))
                        return "category: string expected";
                }
                if (message.description != null && $Object.hasOwnProperty.call(message, "description")) {
                    properties._description = 1;
                    if (!$util.isString(message.description))
                        return "description: string expected";
                }
                if (message.factor_slots != null && $Object.hasOwnProperty.call(message, "factor_slots")) {
                    if (!$util.isObject(message.factor_slots))
                        return "factor_slots: object expected";
                    let key = $Object.keys(message.factor_slots);
                    for (let i = 0; i < key.length; ++i)
                        if (!$util.isString(message.factor_slots[key[i]]))
                            return "factor_slots: string{k:string} expected";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters")) {
                    if (!$Array.isArray(message.parameters))
                        return "parameters: array expected";
                    for (let i = 0; i < message.parameters.length; ++i) {
                        let error = $root.northstar.research.ParameterDescription.verify(message.parameters[i], _depth + 1);
                        if (error)
                            return "parameters." + error;
                    }
                }
                if (message.revision != null && $Object.hasOwnProperty.call(message, "revision")) {
                    properties._revision = 1;
                    if (!$util.isString(message.revision))
                        return "revision: string expected";
                }
                if (message.strategy_id != null && $Object.hasOwnProperty.call(message, "strategy_id")) {
                    properties._strategy_id = 1;
                    if (!$util.isString(message.strategy_id))
                        return "strategy_id: string expected";
                }
                return null;
            };

            /**
             * Creates a StrategyDescription message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.StrategyDescription
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.StrategyDescription} StrategyDescription
             */
            StrategyDescription.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.StrategyDescription)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.StrategyDescription: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.StrategyDescription();
                if (object.category != null)
                    message.category = $String(object.category);
                if (object.description != null)
                    message.description = $String(object.description);
                if (object.factor_slots) {
                    if (!$util.isObject(object.factor_slots))
                        throw $TypeError(".northstar.research.StrategyDescription.factor_slots: object expected");
                    message.factor_slots = {};
                    for (let keys = $Object.keys(object.factor_slots), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.factor_slots, keys[i]);
                        message.factor_slots[keys[i]] = $String(object.factor_slots[keys[i]]);
                    }
                }
                if (object.name != null)
                    message.name = $String(object.name);
                if (object.parameters) {
                    if (!$Array.isArray(object.parameters))
                        throw $TypeError(".northstar.research.StrategyDescription.parameters: array expected");
                    message.parameters = $Array(object.parameters.length);
                    for (let i = 0; i < object.parameters.length; ++i) {
                        if (!$util.isObject(object.parameters[i]))
                            throw $TypeError(".northstar.research.StrategyDescription.parameters: object expected");
                        message.parameters[i] = $root.northstar.research.ParameterDescription.fromObject(object.parameters[i], _depth + 1);
                    }
                }
                if (object.revision != null)
                    message.revision = $String(object.revision);
                if (object.strategy_id != null)
                    message.strategy_id = $String(object.strategy_id);
                return message;
            };

            /**
             * Creates a plain object from a StrategyDescription message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.StrategyDescription
             * @static
             * @param {northstar.research.StrategyDescription} message StrategyDescription
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StrategyDescription.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.parameters = [];
                if (options.objects || options.defaults)
                    object.factor_slots = {};
                if (message.category != null && $Object.hasOwnProperty.call(message, "category")) {
                    object.category = message.category;
                    if (options.oneofs)
                        object._category = "category";
                }
                if (message.description != null && $Object.hasOwnProperty.call(message, "description")) {
                    object.description = message.description;
                    if (options.oneofs)
                        object._description = "description";
                }
                let keys2;
                if (message.factor_slots && (keys2 = $Object.keys(message.factor_slots)).length) {
                    object.factor_slots = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.factor_slots, keys2[j]);
                        object.factor_slots[keys2[j]] = message.factor_slots[keys2[j]];
                    }
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    object.name = message.name;
                    if (options.oneofs)
                        object._name = "name";
                }
                if (message.parameters && message.parameters.length) {
                    object.parameters = $Array(message.parameters.length);
                    for (let j = 0; j < message.parameters.length; ++j)
                        object.parameters[j] = $root.northstar.research.ParameterDescription.toObject(message.parameters[j], options, _depth + 1);
                }
                if (message.revision != null && $Object.hasOwnProperty.call(message, "revision")) {
                    object.revision = message.revision;
                    if (options.oneofs)
                        object._revision = "revision";
                }
                if (message.strategy_id != null && $Object.hasOwnProperty.call(message, "strategy_id")) {
                    object.strategy_id = message.strategy_id;
                    if (options.oneofs)
                        object._strategy_id = "strategy_id";
                }
                return object;
            };

            /**
             * Converts this StrategyDescription to JSON.
             * @function toJSON
             * @memberof northstar.research.StrategyDescription
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StrategyDescription.prototype.toJSON = function() {
                return StrategyDescription.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StrategyDescription
             * @function getTypeUrl
             * @memberof northstar.research.StrategyDescription
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StrategyDescription.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.StrategyDescription";
            };

            return StrategyDescription;
        })();

        research.StrategyInput = (function() {

            /**
             * Properties of a StrategyInput.
             * @typedef {Object} northstar.research.StrategyInput.$Properties
             * @property {Object.<string,northstar.research.FactorInput.$Properties>|null} [factor_bindings] StrategyInput factor_bindings
             * @property {google.protobuf.Struct.$Properties|null} [parameters] StrategyInput parameters
             * @property {string|null} [strategy_id] StrategyInput strategy_id
             * @property {Array.<string>|null} [null_fields] StrategyInput null_fields
             * @property {"parameters"} [_parameters] StrategyInput _parameters
             * @property {"strategy_id"} [_strategy_id] StrategyInput _strategy_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StrategyInput.
             * @memberof northstar.research
             * @interface IStrategyInput
             * @augments northstar.research.StrategyInput.$Properties
             * @deprecated Use northstar.research.StrategyInput.$Properties instead.
             */

            /**
             * Narrowed shape of a StrategyInput.
             * @typedef {{
             *   factor_bindings?: Object.<string,northstar.research.FactorInput.$Shape>|null;
             *   parameters?: google.protobuf.Struct.$Shape|null;
             *   strategy_id?: string|null;
             *   null_fields?: Array.<string>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _parameters?: undefined; parameters?: null }|{ _parameters?: "parameters"; parameters: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _strategy_id?: undefined; strategy_id?: null }|{ _strategy_id?: "strategy_id"; strategy_id: string })
             * )} northstar.research.StrategyInput.$Shape
             */

            /**
             * Constructs a new StrategyInput.
             * @memberof northstar.research
             * @classdesc Represents a StrategyInput.
             * @constructor
             * @param {northstar.research.StrategyInput.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StrategyInput = function (properties) {
                this.factor_bindings = {};
                this.null_fields = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StrategyInput factor_bindings.
             * @member {Object.<string,northstar.research.FactorInput.$Properties>} factor_bindings
             * @memberof northstar.research.StrategyInput
             * @instance
             */
            StrategyInput.prototype.factor_bindings = $util.emptyObject;

            /**
             * StrategyInput parameters.
             * @member {google.protobuf.Struct.$Properties|null|undefined} parameters
             * @memberof northstar.research.StrategyInput
             * @instance
             */
            StrategyInput.prototype.parameters = null;

            /**
             * StrategyInput strategy_id.
             * @member {string|null|undefined} strategy_id
             * @memberof northstar.research.StrategyInput
             * @instance
             */
            StrategyInput.prototype.strategy_id = null;

            /**
             * StrategyInput null_fields.
             * @member {Array.<string>} null_fields
             * @memberof northstar.research.StrategyInput
             * @instance
             */
            StrategyInput.prototype.null_fields = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * StrategyInput _parameters.
             * @member {"parameters"|undefined} _parameters
             * @memberof northstar.research.StrategyInput
             * @instance
             */
            $Object.defineProperty(StrategyInput.prototype, "_parameters", {
                get: $util.oneOfGetter($oneOfFields = ["parameters"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StrategyInput _strategy_id.
             * @member {"strategy_id"|undefined} _strategy_id
             * @memberof northstar.research.StrategyInput
             * @instance
             */
            $Object.defineProperty(StrategyInput.prototype, "_strategy_id", {
                get: $util.oneOfGetter($oneOfFields = ["strategy_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StrategyInput instance using the specified properties.
             * @function create
             * @memberof northstar.research.StrategyInput
             * @static
             * @param {northstar.research.StrategyInput.$Properties=} [properties] Properties to set
             * @returns {northstar.research.StrategyInput} StrategyInput instance
             * @type {{
             *   (properties: northstar.research.StrategyInput.$Shape): northstar.research.StrategyInput & northstar.research.StrategyInput.$Shape;
             *   (properties?: northstar.research.StrategyInput.$Properties): northstar.research.StrategyInput;
             * }}
             */
            StrategyInput.create = function(properties) {
                return new StrategyInput(properties);
            };

            /**
             * Encodes the specified StrategyInput message. Does not implicitly {@link northstar.research.StrategyInput.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.StrategyInput
             * @static
             * @param {northstar.research.StrategyInput.$Properties} message StrategyInput message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StrategyInput.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.factor_bindings != null && $Object.hasOwnProperty.call(message, "factor_bindings"))
                    for (let keys = $Object.keys(message.factor_bindings), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.northstar.research.FactorInput.encode(message.factor_bindings[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters"))
                    $root.google.protobuf.Struct.encode(message.parameters, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.strategy_id != null && $Object.hasOwnProperty.call(message, "strategy_id"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.strategy_id);
                if (message.null_fields != null && message.null_fields.length)
                    for (let i = 0; i < message.null_fields.length; ++i)
                        writer.uint32(/* id 2046, wireType 2 =*/16370).string(message.null_fields[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a StrategyInput message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.StrategyInput
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.StrategyInput & northstar.research.StrategyInput.$Shape} StrategyInput
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StrategyInput.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.StrategyInput();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (message.factor_bindings === $util.emptyObject)
                                message.factor_bindings = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.northstar.research.FactorInput.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.factor_bindings, key);
                            message.factor_bindings[key] = value || new $root.northstar.research.FactorInput();
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.parameters = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.parameters);
                            message._parameters = "parameters";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.strategy_id = reader.stringVerify();
                            message._strategy_id = "strategy_id";
                            continue;
                        }
                    case 2046: {
                            if (wireType !== 2)
                                break;
                            if (!(message.null_fields && message.null_fields.length))
                                message.null_fields = [];
                            message.null_fields.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a StrategyInput message.
             * @function verify
             * @memberof northstar.research.StrategyInput
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StrategyInput.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.factor_bindings != null && $Object.hasOwnProperty.call(message, "factor_bindings")) {
                    if (!$util.isObject(message.factor_bindings))
                        return "factor_bindings: object expected";
                    let key = $Object.keys(message.factor_bindings);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.northstar.research.FactorInput.verify(message.factor_bindings[key[i]], _depth + 1);
                        if (error)
                            return "factor_bindings." + error;
                    }
                }
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters")) {
                    properties._parameters = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.parameters, _depth + 1);
                        if (error)
                            return "parameters." + error;
                    }
                }
                if (message.strategy_id != null && $Object.hasOwnProperty.call(message, "strategy_id")) {
                    properties._strategy_id = 1;
                    if (!$util.isString(message.strategy_id))
                        return "strategy_id: string expected";
                }
                if (message.null_fields != null && $Object.hasOwnProperty.call(message, "null_fields")) {
                    if (!$Array.isArray(message.null_fields))
                        return "null_fields: array expected";
                    for (let i = 0; i < message.null_fields.length; ++i)
                        if (!$util.isString(message.null_fields[i]))
                            return "null_fields: string[] expected";
                }
                return null;
            };

            /**
             * Creates a StrategyInput message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.StrategyInput
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.StrategyInput} StrategyInput
             */
            StrategyInput.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.StrategyInput)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.StrategyInput: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.StrategyInput();
                if (object.factor_bindings) {
                    if (!$util.isObject(object.factor_bindings))
                        throw $TypeError(".northstar.research.StrategyInput.factor_bindings: object expected");
                    message.factor_bindings = {};
                    for (let keys = $Object.keys(object.factor_bindings), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.factor_bindings, keys[i]);
                        if (!$util.isObject(object.factor_bindings[keys[i]]))
                            throw $TypeError(".northstar.research.StrategyInput.factor_bindings: object expected");
                        message.factor_bindings[keys[i]] = $root.northstar.research.FactorInput.fromObject(object.factor_bindings[keys[i]], _depth + 1);
                    }
                }
                if (object.parameters != null) {
                    if (!$util.isObject(object.parameters))
                        throw $TypeError(".northstar.research.StrategyInput.parameters: object expected");
                    message.parameters = $root.google.protobuf.Struct.fromObject(object.parameters, _depth + 1);
                }
                if (object.strategy_id != null)
                    message.strategy_id = $String(object.strategy_id);
                if (object.null_fields) {
                    if (!$Array.isArray(object.null_fields))
                        throw $TypeError(".northstar.research.StrategyInput.null_fields: array expected");
                    message.null_fields = $Array(object.null_fields.length);
                    for (let i = 0; i < object.null_fields.length; ++i)
                        message.null_fields[i] = $String(object.null_fields[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a StrategyInput message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.StrategyInput
             * @static
             * @param {northstar.research.StrategyInput} message StrategyInput
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StrategyInput.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.null_fields = [];
                if (options.objects || options.defaults)
                    object.factor_bindings = {};
                let keys2;
                if (message.factor_bindings && (keys2 = $Object.keys(message.factor_bindings)).length) {
                    object.factor_bindings = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.factor_bindings, keys2[j]);
                        object.factor_bindings[keys2[j]] = $root.northstar.research.FactorInput.toObject(message.factor_bindings[keys2[j]], options, _depth + 1);
                    }
                }
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters")) {
                    object.parameters = $root.google.protobuf.Struct.toObject(message.parameters, options, _depth + 1);
                    if (options.oneofs)
                        object._parameters = "parameters";
                }
                if (message.strategy_id != null && $Object.hasOwnProperty.call(message, "strategy_id")) {
                    object.strategy_id = message.strategy_id;
                    if (options.oneofs)
                        object._strategy_id = "strategy_id";
                }
                if (message.null_fields && message.null_fields.length) {
                    object.null_fields = $Array(message.null_fields.length);
                    for (let j = 0; j < message.null_fields.length; ++j)
                        object.null_fields[j] = message.null_fields[j];
                }
                return object;
            };

            /**
             * Converts this StrategyInput to JSON.
             * @function toJSON
             * @memberof northstar.research.StrategyInput
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StrategyInput.prototype.toJSON = function() {
                return StrategyInput.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StrategyInput
             * @function getTypeUrl
             * @memberof northstar.research.StrategyInput
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StrategyInput.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.StrategyInput";
            };

            return StrategyInput;
        })();

        research.StrategySummary = (function() {

            /**
             * Properties of a StrategySummary.
             * @typedef {Object} northstar.research.StrategySummary.$Properties
             * @property {string|null} [category] StrategySummary category
             * @property {string|null} [description] StrategySummary description
             * @property {string|null} [name] StrategySummary name
             * @property {string|null} [strategy_id] StrategySummary strategy_id
             * @property {"category"} [_category] StrategySummary _category
             * @property {"description"} [_description] StrategySummary _description
             * @property {"name"} [_name] StrategySummary _name
             * @property {"strategy_id"} [_strategy_id] StrategySummary _strategy_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StrategySummary.
             * @memberof northstar.research
             * @interface IStrategySummary
             * @augments northstar.research.StrategySummary.$Properties
             * @deprecated Use northstar.research.StrategySummary.$Properties instead.
             */

            /**
             * Narrowed shape of a StrategySummary.
             * @typedef {{
             *   category?: string|null;
             *   description?: string|null;
             *   name?: string|null;
             *   strategy_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _category?: undefined; category?: null }|{ _category?: "category"; category: string })
             * ) & (
             *   ({ _description?: undefined; description?: null }|{ _description?: "description"; description: string })
             * ) & (
             *   ({ _name?: undefined; name?: null }|{ _name?: "name"; name: string })
             * ) & (
             *   ({ _strategy_id?: undefined; strategy_id?: null }|{ _strategy_id?: "strategy_id"; strategy_id: string })
             * )} northstar.research.StrategySummary.$Shape
             */

            /**
             * Constructs a new StrategySummary.
             * @memberof northstar.research
             * @classdesc Represents a StrategySummary.
             * @constructor
             * @param {northstar.research.StrategySummary.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StrategySummary = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StrategySummary category.
             * @member {string|null|undefined} category
             * @memberof northstar.research.StrategySummary
             * @instance
             */
            StrategySummary.prototype.category = null;

            /**
             * StrategySummary description.
             * @member {string|null|undefined} description
             * @memberof northstar.research.StrategySummary
             * @instance
             */
            StrategySummary.prototype.description = null;

            /**
             * StrategySummary name.
             * @member {string|null|undefined} name
             * @memberof northstar.research.StrategySummary
             * @instance
             */
            StrategySummary.prototype.name = null;

            /**
             * StrategySummary strategy_id.
             * @member {string|null|undefined} strategy_id
             * @memberof northstar.research.StrategySummary
             * @instance
             */
            StrategySummary.prototype.strategy_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * StrategySummary _category.
             * @member {"category"|undefined} _category
             * @memberof northstar.research.StrategySummary
             * @instance
             */
            $Object.defineProperty(StrategySummary.prototype, "_category", {
                get: $util.oneOfGetter($oneOfFields = ["category"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StrategySummary _description.
             * @member {"description"|undefined} _description
             * @memberof northstar.research.StrategySummary
             * @instance
             */
            $Object.defineProperty(StrategySummary.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StrategySummary _name.
             * @member {"name"|undefined} _name
             * @memberof northstar.research.StrategySummary
             * @instance
             */
            $Object.defineProperty(StrategySummary.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StrategySummary _strategy_id.
             * @member {"strategy_id"|undefined} _strategy_id
             * @memberof northstar.research.StrategySummary
             * @instance
             */
            $Object.defineProperty(StrategySummary.prototype, "_strategy_id", {
                get: $util.oneOfGetter($oneOfFields = ["strategy_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StrategySummary instance using the specified properties.
             * @function create
             * @memberof northstar.research.StrategySummary
             * @static
             * @param {northstar.research.StrategySummary.$Properties=} [properties] Properties to set
             * @returns {northstar.research.StrategySummary} StrategySummary instance
             * @type {{
             *   (properties: northstar.research.StrategySummary.$Shape): northstar.research.StrategySummary & northstar.research.StrategySummary.$Shape;
             *   (properties?: northstar.research.StrategySummary.$Properties): northstar.research.StrategySummary;
             * }}
             */
            StrategySummary.create = function(properties) {
                return new StrategySummary(properties);
            };

            /**
             * Encodes the specified StrategySummary message. Does not implicitly {@link northstar.research.StrategySummary.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.StrategySummary
             * @static
             * @param {northstar.research.StrategySummary.$Properties} message StrategySummary message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StrategySummary.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.category != null && $Object.hasOwnProperty.call(message, "category"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.category);
                if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.description);
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.name);
                if (message.strategy_id != null && $Object.hasOwnProperty.call(message, "strategy_id"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.strategy_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a StrategySummary message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.StrategySummary
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.StrategySummary & northstar.research.StrategySummary.$Shape} StrategySummary
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StrategySummary.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.StrategySummary();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.category = reader.stringVerify();
                            message._category = "category";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.description = reader.stringVerify();
                            message._description = "description";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.strategy_id = reader.stringVerify();
                            message._strategy_id = "strategy_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a StrategySummary message.
             * @function verify
             * @memberof northstar.research.StrategySummary
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StrategySummary.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.category != null && $Object.hasOwnProperty.call(message, "category")) {
                    properties._category = 1;
                    if (!$util.isString(message.category))
                        return "category: string expected";
                }
                if (message.description != null && $Object.hasOwnProperty.call(message, "description")) {
                    properties._description = 1;
                    if (!$util.isString(message.description))
                        return "description: string expected";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                if (message.strategy_id != null && $Object.hasOwnProperty.call(message, "strategy_id")) {
                    properties._strategy_id = 1;
                    if (!$util.isString(message.strategy_id))
                        return "strategy_id: string expected";
                }
                return null;
            };

            /**
             * Creates a StrategySummary message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.StrategySummary
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.StrategySummary} StrategySummary
             */
            StrategySummary.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.StrategySummary)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.StrategySummary: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.StrategySummary();
                if (object.category != null)
                    message.category = $String(object.category);
                if (object.description != null)
                    message.description = $String(object.description);
                if (object.name != null)
                    message.name = $String(object.name);
                if (object.strategy_id != null)
                    message.strategy_id = $String(object.strategy_id);
                return message;
            };

            /**
             * Creates a plain object from a StrategySummary message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.StrategySummary
             * @static
             * @param {northstar.research.StrategySummary} message StrategySummary
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StrategySummary.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.category != null && $Object.hasOwnProperty.call(message, "category")) {
                    object.category = message.category;
                    if (options.oneofs)
                        object._category = "category";
                }
                if (message.description != null && $Object.hasOwnProperty.call(message, "description")) {
                    object.description = message.description;
                    if (options.oneofs)
                        object._description = "description";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    object.name = message.name;
                    if (options.oneofs)
                        object._name = "name";
                }
                if (message.strategy_id != null && $Object.hasOwnProperty.call(message, "strategy_id")) {
                    object.strategy_id = message.strategy_id;
                    if (options.oneofs)
                        object._strategy_id = "strategy_id";
                }
                return object;
            };

            /**
             * Converts this StrategySummary to JSON.
             * @function toJSON
             * @memberof northstar.research.StrategySummary
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StrategySummary.prototype.toJSON = function() {
                return StrategySummary.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StrategySummary
             * @function getTypeUrl
             * @memberof northstar.research.StrategySummary
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StrategySummary.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.StrategySummary";
            };

            return StrategySummary;
        })();

        research.StrategyVersion = (function() {

            /**
             * Properties of a StrategyVersion.
             * @typedef {Object} northstar.research.StrategyVersion.$Properties
             * @property {string|null} [created_at] StrategyVersion created_at
             * @property {northstar.research.VersionDocument.$Properties|null} [document] StrategyVersion document
             * @property {string|null} [name] StrategyVersion name
             * @property {string|null} [version_id] StrategyVersion version_id
             * @property {"created_at"} [_created_at] StrategyVersion _created_at
             * @property {"document"} [_document] StrategyVersion _document
             * @property {"name"} [_name] StrategyVersion _name
             * @property {"version_id"} [_version_id] StrategyVersion _version_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StrategyVersion.
             * @memberof northstar.research
             * @interface IStrategyVersion
             * @augments northstar.research.StrategyVersion.$Properties
             * @deprecated Use northstar.research.StrategyVersion.$Properties instead.
             */

            /**
             * Narrowed shape of a StrategyVersion.
             * @typedef {{
             *   created_at?: string|null;
             *   document?: northstar.research.VersionDocument.$Shape|null;
             *   name?: string|null;
             *   version_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _created_at?: undefined; created_at?: null }|{ _created_at?: "created_at"; created_at: string })
             * ) & (
             *   ({ _document?: undefined; document?: null }|{ _document?: "document"; document: northstar.research.VersionDocument.$Shape })
             * ) & (
             *   ({ _name?: undefined; name?: null }|{ _name?: "name"; name: string })
             * ) & (
             *   ({ _version_id?: undefined; version_id?: null }|{ _version_id?: "version_id"; version_id: string })
             * )} northstar.research.StrategyVersion.$Shape
             */

            /**
             * Constructs a new StrategyVersion.
             * @memberof northstar.research
             * @classdesc Represents a StrategyVersion.
             * @constructor
             * @param {northstar.research.StrategyVersion.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StrategyVersion = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StrategyVersion created_at.
             * @member {string|null|undefined} created_at
             * @memberof northstar.research.StrategyVersion
             * @instance
             */
            StrategyVersion.prototype.created_at = null;

            /**
             * StrategyVersion document.
             * @member {northstar.research.VersionDocument.$Properties|null|undefined} document
             * @memberof northstar.research.StrategyVersion
             * @instance
             */
            StrategyVersion.prototype.document = null;

            /**
             * StrategyVersion name.
             * @member {string|null|undefined} name
             * @memberof northstar.research.StrategyVersion
             * @instance
             */
            StrategyVersion.prototype.name = null;

            /**
             * StrategyVersion version_id.
             * @member {string|null|undefined} version_id
             * @memberof northstar.research.StrategyVersion
             * @instance
             */
            StrategyVersion.prototype.version_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * StrategyVersion _created_at.
             * @member {"created_at"|undefined} _created_at
             * @memberof northstar.research.StrategyVersion
             * @instance
             */
            $Object.defineProperty(StrategyVersion.prototype, "_created_at", {
                get: $util.oneOfGetter($oneOfFields = ["created_at"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StrategyVersion _document.
             * @member {"document"|undefined} _document
             * @memberof northstar.research.StrategyVersion
             * @instance
             */
            $Object.defineProperty(StrategyVersion.prototype, "_document", {
                get: $util.oneOfGetter($oneOfFields = ["document"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StrategyVersion _name.
             * @member {"name"|undefined} _name
             * @memberof northstar.research.StrategyVersion
             * @instance
             */
            $Object.defineProperty(StrategyVersion.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StrategyVersion _version_id.
             * @member {"version_id"|undefined} _version_id
             * @memberof northstar.research.StrategyVersion
             * @instance
             */
            $Object.defineProperty(StrategyVersion.prototype, "_version_id", {
                get: $util.oneOfGetter($oneOfFields = ["version_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StrategyVersion instance using the specified properties.
             * @function create
             * @memberof northstar.research.StrategyVersion
             * @static
             * @param {northstar.research.StrategyVersion.$Properties=} [properties] Properties to set
             * @returns {northstar.research.StrategyVersion} StrategyVersion instance
             * @type {{
             *   (properties: northstar.research.StrategyVersion.$Shape): northstar.research.StrategyVersion & northstar.research.StrategyVersion.$Shape;
             *   (properties?: northstar.research.StrategyVersion.$Properties): northstar.research.StrategyVersion;
             * }}
             */
            StrategyVersion.create = function(properties) {
                return new StrategyVersion(properties);
            };

            /**
             * Encodes the specified StrategyVersion message. Does not implicitly {@link northstar.research.StrategyVersion.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.StrategyVersion
             * @static
             * @param {northstar.research.StrategyVersion.$Properties} message StrategyVersion message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StrategyVersion.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.created_at != null && $Object.hasOwnProperty.call(message, "created_at"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.created_at);
                if (message.document != null && $Object.hasOwnProperty.call(message, "document"))
                    $root.northstar.research.VersionDocument.encode(message.document, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.name);
                if (message.version_id != null && $Object.hasOwnProperty.call(message, "version_id"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.version_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a StrategyVersion message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.StrategyVersion
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.StrategyVersion & northstar.research.StrategyVersion.$Shape} StrategyVersion
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StrategyVersion.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.StrategyVersion();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.created_at = reader.stringVerify();
                            message._created_at = "created_at";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.document = $root.northstar.research.VersionDocument.decode(reader, reader.uint32(), $undefined, _depth + 1, message.document);
                            message._document = "document";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.version_id = reader.stringVerify();
                            message._version_id = "version_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a StrategyVersion message.
             * @function verify
             * @memberof northstar.research.StrategyVersion
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StrategyVersion.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.created_at != null && $Object.hasOwnProperty.call(message, "created_at")) {
                    properties._created_at = 1;
                    if (!$util.isString(message.created_at))
                        return "created_at: string expected";
                }
                if (message.document != null && $Object.hasOwnProperty.call(message, "document")) {
                    properties._document = 1;
                    {
                        let error = $root.northstar.research.VersionDocument.verify(message.document, _depth + 1);
                        if (error)
                            return "document." + error;
                    }
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                if (message.version_id != null && $Object.hasOwnProperty.call(message, "version_id")) {
                    properties._version_id = 1;
                    if (!$util.isString(message.version_id))
                        return "version_id: string expected";
                }
                return null;
            };

            /**
             * Creates a StrategyVersion message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.StrategyVersion
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.StrategyVersion} StrategyVersion
             */
            StrategyVersion.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.StrategyVersion)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.StrategyVersion: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.StrategyVersion();
                if (object.created_at != null)
                    message.created_at = $String(object.created_at);
                if (object.document != null) {
                    if (!$util.isObject(object.document))
                        throw $TypeError(".northstar.research.StrategyVersion.document: object expected");
                    message.document = $root.northstar.research.VersionDocument.fromObject(object.document, _depth + 1);
                }
                if (object.name != null)
                    message.name = $String(object.name);
                if (object.version_id != null)
                    message.version_id = $String(object.version_id);
                return message;
            };

            /**
             * Creates a plain object from a StrategyVersion message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.StrategyVersion
             * @static
             * @param {northstar.research.StrategyVersion} message StrategyVersion
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StrategyVersion.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.created_at != null && $Object.hasOwnProperty.call(message, "created_at")) {
                    object.created_at = message.created_at;
                    if (options.oneofs)
                        object._created_at = "created_at";
                }
                if (message.document != null && $Object.hasOwnProperty.call(message, "document")) {
                    object.document = $root.northstar.research.VersionDocument.toObject(message.document, options, _depth + 1);
                    if (options.oneofs)
                        object._document = "document";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    object.name = message.name;
                    if (options.oneofs)
                        object._name = "name";
                }
                if (message.version_id != null && $Object.hasOwnProperty.call(message, "version_id")) {
                    object.version_id = message.version_id;
                    if (options.oneofs)
                        object._version_id = "version_id";
                }
                return object;
            };

            /**
             * Converts this StrategyVersion to JSON.
             * @function toJSON
             * @memberof northstar.research.StrategyVersion
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StrategyVersion.prototype.toJSON = function() {
                return StrategyVersion.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StrategyVersion
             * @function getTypeUrl
             * @memberof northstar.research.StrategyVersion
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StrategyVersion.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.StrategyVersion";
            };

            return StrategyVersion;
        })();

        research.StrategyVersionRequest = (function() {

            /**
             * Properties of a StrategyVersionRequest.
             * @typedef {Object} northstar.research.StrategyVersionRequest.$Properties
             * @property {string|null} [configuration_id] StrategyVersionRequest configuration_id
             * @property {string|null} [name] StrategyVersionRequest name
             * @property {Array.<string>|null} [run_ids] StrategyVersionRequest run_ids
             * @property {"configuration_id"} [_configuration_id] StrategyVersionRequest _configuration_id
             * @property {"name"} [_name] StrategyVersionRequest _name
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StrategyVersionRequest.
             * @memberof northstar.research
             * @interface IStrategyVersionRequest
             * @augments northstar.research.StrategyVersionRequest.$Properties
             * @deprecated Use northstar.research.StrategyVersionRequest.$Properties instead.
             */

            /**
             * Narrowed shape of a StrategyVersionRequest.
             * @typedef {{
             *   configuration_id?: string|null;
             *   name?: string|null;
             *   run_ids?: Array.<string>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _configuration_id?: undefined; configuration_id?: null }|{ _configuration_id?: "configuration_id"; configuration_id: string })
             * ) & (
             *   ({ _name?: undefined; name?: null }|{ _name?: "name"; name: string })
             * )} northstar.research.StrategyVersionRequest.$Shape
             */

            /**
             * Constructs a new StrategyVersionRequest.
             * @memberof northstar.research
             * @classdesc Represents a StrategyVersionRequest.
             * @constructor
             * @param {northstar.research.StrategyVersionRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StrategyVersionRequest = function (properties) {
                this.run_ids = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StrategyVersionRequest configuration_id.
             * @member {string|null|undefined} configuration_id
             * @memberof northstar.research.StrategyVersionRequest
             * @instance
             */
            StrategyVersionRequest.prototype.configuration_id = null;

            /**
             * StrategyVersionRequest name.
             * @member {string|null|undefined} name
             * @memberof northstar.research.StrategyVersionRequest
             * @instance
             */
            StrategyVersionRequest.prototype.name = null;

            /**
             * StrategyVersionRequest run_ids.
             * @member {Array.<string>} run_ids
             * @memberof northstar.research.StrategyVersionRequest
             * @instance
             */
            StrategyVersionRequest.prototype.run_ids = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * StrategyVersionRequest _configuration_id.
             * @member {"configuration_id"|undefined} _configuration_id
             * @memberof northstar.research.StrategyVersionRequest
             * @instance
             */
            $Object.defineProperty(StrategyVersionRequest.prototype, "_configuration_id", {
                get: $util.oneOfGetter($oneOfFields = ["configuration_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StrategyVersionRequest _name.
             * @member {"name"|undefined} _name
             * @memberof northstar.research.StrategyVersionRequest
             * @instance
             */
            $Object.defineProperty(StrategyVersionRequest.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StrategyVersionRequest instance using the specified properties.
             * @function create
             * @memberof northstar.research.StrategyVersionRequest
             * @static
             * @param {northstar.research.StrategyVersionRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.research.StrategyVersionRequest} StrategyVersionRequest instance
             * @type {{
             *   (properties: northstar.research.StrategyVersionRequest.$Shape): northstar.research.StrategyVersionRequest & northstar.research.StrategyVersionRequest.$Shape;
             *   (properties?: northstar.research.StrategyVersionRequest.$Properties): northstar.research.StrategyVersionRequest;
             * }}
             */
            StrategyVersionRequest.create = function(properties) {
                return new StrategyVersionRequest(properties);
            };

            /**
             * Encodes the specified StrategyVersionRequest message. Does not implicitly {@link northstar.research.StrategyVersionRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.StrategyVersionRequest
             * @static
             * @param {northstar.research.StrategyVersionRequest.$Properties} message StrategyVersionRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StrategyVersionRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.configuration_id != null && $Object.hasOwnProperty.call(message, "configuration_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.configuration_id);
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
                if (message.run_ids != null && message.run_ids.length)
                    for (let i = 0; i < message.run_ids.length; ++i)
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.run_ids[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a StrategyVersionRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.StrategyVersionRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.StrategyVersionRequest & northstar.research.StrategyVersionRequest.$Shape} StrategyVersionRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StrategyVersionRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.StrategyVersionRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.configuration_id = reader.stringVerify();
                            message._configuration_id = "configuration_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if (!(message.run_ids && message.run_ids.length))
                                message.run_ids = [];
                            message.run_ids.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a StrategyVersionRequest message.
             * @function verify
             * @memberof northstar.research.StrategyVersionRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StrategyVersionRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.configuration_id != null && $Object.hasOwnProperty.call(message, "configuration_id")) {
                    properties._configuration_id = 1;
                    if (!$util.isString(message.configuration_id))
                        return "configuration_id: string expected";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                if (message.run_ids != null && $Object.hasOwnProperty.call(message, "run_ids")) {
                    if (!$Array.isArray(message.run_ids))
                        return "run_ids: array expected";
                    for (let i = 0; i < message.run_ids.length; ++i)
                        if (!$util.isString(message.run_ids[i]))
                            return "run_ids: string[] expected";
                }
                return null;
            };

            /**
             * Creates a StrategyVersionRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.StrategyVersionRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.StrategyVersionRequest} StrategyVersionRequest
             */
            StrategyVersionRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.StrategyVersionRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.StrategyVersionRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.StrategyVersionRequest();
                if (object.configuration_id != null)
                    message.configuration_id = $String(object.configuration_id);
                if (object.name != null)
                    message.name = $String(object.name);
                if (object.run_ids) {
                    if (!$Array.isArray(object.run_ids))
                        throw $TypeError(".northstar.research.StrategyVersionRequest.run_ids: array expected");
                    message.run_ids = $Array(object.run_ids.length);
                    for (let i = 0; i < object.run_ids.length; ++i)
                        message.run_ids[i] = $String(object.run_ids[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a StrategyVersionRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.StrategyVersionRequest
             * @static
             * @param {northstar.research.StrategyVersionRequest} message StrategyVersionRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StrategyVersionRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.run_ids = [];
                if (message.configuration_id != null && $Object.hasOwnProperty.call(message, "configuration_id")) {
                    object.configuration_id = message.configuration_id;
                    if (options.oneofs)
                        object._configuration_id = "configuration_id";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    object.name = message.name;
                    if (options.oneofs)
                        object._name = "name";
                }
                if (message.run_ids && message.run_ids.length) {
                    object.run_ids = $Array(message.run_ids.length);
                    for (let j = 0; j < message.run_ids.length; ++j)
                        object.run_ids[j] = message.run_ids[j];
                }
                return object;
            };

            /**
             * Converts this StrategyVersionRequest to JSON.
             * @function toJSON
             * @memberof northstar.research.StrategyVersionRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StrategyVersionRequest.prototype.toJSON = function() {
                return StrategyVersionRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StrategyVersionRequest
             * @function getTypeUrl
             * @memberof northstar.research.StrategyVersionRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StrategyVersionRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.StrategyVersionRequest";
            };

            return StrategyVersionRequest;
        })();

        research.VersionCreated = (function() {

            /**
             * Properties of a VersionCreated.
             * @typedef {Object} northstar.research.VersionCreated.$Properties
             * @property {string|null} [version_id] VersionCreated version_id
             * @property {"version_id"} [_version_id] VersionCreated _version_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a VersionCreated.
             * @memberof northstar.research
             * @interface IVersionCreated
             * @augments northstar.research.VersionCreated.$Properties
             * @deprecated Use northstar.research.VersionCreated.$Properties instead.
             */

            /**
             * Narrowed shape of a VersionCreated.
             * @typedef {{
             *   version_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _version_id?: undefined; version_id?: null }|{ _version_id?: "version_id"; version_id: string })
             * )} northstar.research.VersionCreated.$Shape
             */

            /**
             * Constructs a new VersionCreated.
             * @memberof northstar.research
             * @classdesc Represents a VersionCreated.
             * @constructor
             * @param {northstar.research.VersionCreated.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const VersionCreated = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * VersionCreated version_id.
             * @member {string|null|undefined} version_id
             * @memberof northstar.research.VersionCreated
             * @instance
             */
            VersionCreated.prototype.version_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * VersionCreated _version_id.
             * @member {"version_id"|undefined} _version_id
             * @memberof northstar.research.VersionCreated
             * @instance
             */
            $Object.defineProperty(VersionCreated.prototype, "_version_id", {
                get: $util.oneOfGetter($oneOfFields = ["version_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new VersionCreated instance using the specified properties.
             * @function create
             * @memberof northstar.research.VersionCreated
             * @static
             * @param {northstar.research.VersionCreated.$Properties=} [properties] Properties to set
             * @returns {northstar.research.VersionCreated} VersionCreated instance
             * @type {{
             *   (properties: northstar.research.VersionCreated.$Shape): northstar.research.VersionCreated & northstar.research.VersionCreated.$Shape;
             *   (properties?: northstar.research.VersionCreated.$Properties): northstar.research.VersionCreated;
             * }}
             */
            VersionCreated.create = function(properties) {
                return new VersionCreated(properties);
            };

            /**
             * Encodes the specified VersionCreated message. Does not implicitly {@link northstar.research.VersionCreated.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.VersionCreated
             * @static
             * @param {northstar.research.VersionCreated.$Properties} message VersionCreated message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            VersionCreated.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.version_id != null && $Object.hasOwnProperty.call(message, "version_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.version_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a VersionCreated message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.VersionCreated
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.VersionCreated & northstar.research.VersionCreated.$Shape} VersionCreated
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            VersionCreated.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.VersionCreated();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.version_id = reader.stringVerify();
                            message._version_id = "version_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a VersionCreated message.
             * @function verify
             * @memberof northstar.research.VersionCreated
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            VersionCreated.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.version_id != null && $Object.hasOwnProperty.call(message, "version_id")) {
                    properties._version_id = 1;
                    if (!$util.isString(message.version_id))
                        return "version_id: string expected";
                }
                return null;
            };

            /**
             * Creates a VersionCreated message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.VersionCreated
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.VersionCreated} VersionCreated
             */
            VersionCreated.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.VersionCreated)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.VersionCreated: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.VersionCreated();
                if (object.version_id != null)
                    message.version_id = $String(object.version_id);
                return message;
            };

            /**
             * Creates a plain object from a VersionCreated message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.VersionCreated
             * @static
             * @param {northstar.research.VersionCreated} message VersionCreated
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            VersionCreated.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.version_id != null && $Object.hasOwnProperty.call(message, "version_id")) {
                    object.version_id = message.version_id;
                    if (options.oneofs)
                        object._version_id = "version_id";
                }
                return object;
            };

            /**
             * Converts this VersionCreated to JSON.
             * @function toJSON
             * @memberof northstar.research.VersionCreated
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            VersionCreated.prototype.toJSON = function() {
                return VersionCreated.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for VersionCreated
             * @function getTypeUrl
             * @memberof northstar.research.VersionCreated
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            VersionCreated.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.VersionCreated";
            };

            return VersionCreated;
        })();

        research.VersionDocument = (function() {

            /**
             * Properties of a VersionDocument.
             * @typedef {Object} northstar.research.VersionDocument.$Properties
             * @property {string|null} [code_revision] VersionDocument code_revision
             * @property {northstar.research.SavedConfiguration.$Properties|null} [configuration] VersionDocument configuration
             * @property {Array.<northstar.research.RunDetail.$Properties>|null} [evidence] VersionDocument evidence
             * @property {google.protobuf.Struct.$Properties|null} [validation] VersionDocument validation
             * @property {"code_revision"} [_code_revision] VersionDocument _code_revision
             * @property {"configuration"} [_configuration] VersionDocument _configuration
             * @property {"validation"} [_validation] VersionDocument _validation
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a VersionDocument.
             * @memberof northstar.research
             * @interface IVersionDocument
             * @augments northstar.research.VersionDocument.$Properties
             * @deprecated Use northstar.research.VersionDocument.$Properties instead.
             */

            /**
             * Narrowed shape of a VersionDocument.
             * @typedef {{
             *   code_revision?: string|null;
             *   configuration?: northstar.research.SavedConfiguration.$Shape|null;
             *   evidence?: Array.<northstar.research.RunDetail.$Shape>|null;
             *   validation?: google.protobuf.Struct.$Shape|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _code_revision?: undefined; code_revision?: null }|{ _code_revision?: "code_revision"; code_revision: string })
             * ) & (
             *   ({ _configuration?: undefined; configuration?: null }|{ _configuration?: "configuration"; configuration: northstar.research.SavedConfiguration.$Shape })
             * ) & (
             *   ({ _validation?: undefined; validation?: null }|{ _validation?: "validation"; validation: google.protobuf.Struct.$Shape })
             * )} northstar.research.VersionDocument.$Shape
             */

            /**
             * Constructs a new VersionDocument.
             * @memberof northstar.research
             * @classdesc Represents a VersionDocument.
             * @constructor
             * @param {northstar.research.VersionDocument.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const VersionDocument = function (properties) {
                this.evidence = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * VersionDocument code_revision.
             * @member {string|null|undefined} code_revision
             * @memberof northstar.research.VersionDocument
             * @instance
             */
            VersionDocument.prototype.code_revision = null;

            /**
             * VersionDocument configuration.
             * @member {northstar.research.SavedConfiguration.$Properties|null|undefined} configuration
             * @memberof northstar.research.VersionDocument
             * @instance
             */
            VersionDocument.prototype.configuration = null;

            /**
             * VersionDocument evidence.
             * @member {Array.<northstar.research.RunDetail.$Properties>} evidence
             * @memberof northstar.research.VersionDocument
             * @instance
             */
            VersionDocument.prototype.evidence = $util.emptyArray;

            /**
             * VersionDocument validation.
             * @member {google.protobuf.Struct.$Properties|null|undefined} validation
             * @memberof northstar.research.VersionDocument
             * @instance
             */
            VersionDocument.prototype.validation = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * VersionDocument _code_revision.
             * @member {"code_revision"|undefined} _code_revision
             * @memberof northstar.research.VersionDocument
             * @instance
             */
            $Object.defineProperty(VersionDocument.prototype, "_code_revision", {
                get: $util.oneOfGetter($oneOfFields = ["code_revision"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * VersionDocument _configuration.
             * @member {"configuration"|undefined} _configuration
             * @memberof northstar.research.VersionDocument
             * @instance
             */
            $Object.defineProperty(VersionDocument.prototype, "_configuration", {
                get: $util.oneOfGetter($oneOfFields = ["configuration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * VersionDocument _validation.
             * @member {"validation"|undefined} _validation
             * @memberof northstar.research.VersionDocument
             * @instance
             */
            $Object.defineProperty(VersionDocument.prototype, "_validation", {
                get: $util.oneOfGetter($oneOfFields = ["validation"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new VersionDocument instance using the specified properties.
             * @function create
             * @memberof northstar.research.VersionDocument
             * @static
             * @param {northstar.research.VersionDocument.$Properties=} [properties] Properties to set
             * @returns {northstar.research.VersionDocument} VersionDocument instance
             * @type {{
             *   (properties: northstar.research.VersionDocument.$Shape): northstar.research.VersionDocument & northstar.research.VersionDocument.$Shape;
             *   (properties?: northstar.research.VersionDocument.$Properties): northstar.research.VersionDocument;
             * }}
             */
            VersionDocument.create = function(properties) {
                return new VersionDocument(properties);
            };

            /**
             * Encodes the specified VersionDocument message. Does not implicitly {@link northstar.research.VersionDocument.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.VersionDocument
             * @static
             * @param {northstar.research.VersionDocument.$Properties} message VersionDocument message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            VersionDocument.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.code_revision);
                if (message.configuration != null && $Object.hasOwnProperty.call(message, "configuration"))
                    $root.northstar.research.SavedConfiguration.encode(message.configuration, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.evidence != null && message.evidence.length)
                    for (let i = 0; i < message.evidence.length; ++i)
                        $root.northstar.research.RunDetail.encode(message.evidence[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.validation != null && $Object.hasOwnProperty.call(message, "validation"))
                    $root.google.protobuf.Struct.encode(message.validation, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a VersionDocument message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.VersionDocument
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.VersionDocument & northstar.research.VersionDocument.$Shape} VersionDocument
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            VersionDocument.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.VersionDocument();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.code_revision = reader.stringVerify();
                            message._code_revision = "code_revision";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.configuration = $root.northstar.research.SavedConfiguration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.configuration);
                            message._configuration = "configuration";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if (!(message.evidence && message.evidence.length))
                                message.evidence = [];
                            message.evidence.push($root.northstar.research.RunDetail.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.validation = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.validation);
                            message._validation = "validation";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a VersionDocument message.
             * @function verify
             * @memberof northstar.research.VersionDocument
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            VersionDocument.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision")) {
                    properties._code_revision = 1;
                    if (!$util.isString(message.code_revision))
                        return "code_revision: string expected";
                }
                if (message.configuration != null && $Object.hasOwnProperty.call(message, "configuration")) {
                    properties._configuration = 1;
                    {
                        let error = $root.northstar.research.SavedConfiguration.verify(message.configuration, _depth + 1);
                        if (error)
                            return "configuration." + error;
                    }
                }
                if (message.evidence != null && $Object.hasOwnProperty.call(message, "evidence")) {
                    if (!$Array.isArray(message.evidence))
                        return "evidence: array expected";
                    for (let i = 0; i < message.evidence.length; ++i) {
                        let error = $root.northstar.research.RunDetail.verify(message.evidence[i], _depth + 1);
                        if (error)
                            return "evidence." + error;
                    }
                }
                if (message.validation != null && $Object.hasOwnProperty.call(message, "validation")) {
                    properties._validation = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.validation, _depth + 1);
                        if (error)
                            return "validation." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a VersionDocument message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.VersionDocument
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.VersionDocument} VersionDocument
             */
            VersionDocument.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.VersionDocument)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.VersionDocument: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.VersionDocument();
                if (object.code_revision != null)
                    message.code_revision = $String(object.code_revision);
                if (object.configuration != null) {
                    if (!$util.isObject(object.configuration))
                        throw $TypeError(".northstar.research.VersionDocument.configuration: object expected");
                    message.configuration = $root.northstar.research.SavedConfiguration.fromObject(object.configuration, _depth + 1);
                }
                if (object.evidence) {
                    if (!$Array.isArray(object.evidence))
                        throw $TypeError(".northstar.research.VersionDocument.evidence: array expected");
                    message.evidence = $Array(object.evidence.length);
                    for (let i = 0; i < object.evidence.length; ++i) {
                        if (!$util.isObject(object.evidence[i]))
                            throw $TypeError(".northstar.research.VersionDocument.evidence: object expected");
                        message.evidence[i] = $root.northstar.research.RunDetail.fromObject(object.evidence[i], _depth + 1);
                    }
                }
                if (object.validation != null) {
                    if (!$util.isObject(object.validation))
                        throw $TypeError(".northstar.research.VersionDocument.validation: object expected");
                    message.validation = $root.google.protobuf.Struct.fromObject(object.validation, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a VersionDocument message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.VersionDocument
             * @static
             * @param {northstar.research.VersionDocument} message VersionDocument
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            VersionDocument.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.evidence = [];
                if (message.code_revision != null && $Object.hasOwnProperty.call(message, "code_revision")) {
                    object.code_revision = message.code_revision;
                    if (options.oneofs)
                        object._code_revision = "code_revision";
                }
                if (message.configuration != null && $Object.hasOwnProperty.call(message, "configuration")) {
                    object.configuration = $root.northstar.research.SavedConfiguration.toObject(message.configuration, options, _depth + 1);
                    if (options.oneofs)
                        object._configuration = "configuration";
                }
                if (message.evidence && message.evidence.length) {
                    object.evidence = $Array(message.evidence.length);
                    for (let j = 0; j < message.evidence.length; ++j)
                        object.evidence[j] = $root.northstar.research.RunDetail.toObject(message.evidence[j], options, _depth + 1);
                }
                if (message.validation != null && $Object.hasOwnProperty.call(message, "validation")) {
                    object.validation = $root.google.protobuf.Struct.toObject(message.validation, options, _depth + 1);
                    if (options.oneofs)
                        object._validation = "validation";
                }
                return object;
            };

            /**
             * Converts this VersionDocument to JSON.
             * @function toJSON
             * @memberof northstar.research.VersionDocument
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            VersionDocument.prototype.toJSON = function() {
                return VersionDocument.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for VersionDocument
             * @function getTypeUrl
             * @memberof northstar.research.VersionDocument
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            VersionDocument.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.VersionDocument";
            };

            return VersionDocument;
        })();

        research.GetApiConfigurationsResponse = (function() {

            /**
             * Properties of a GetApiConfigurationsResponse.
             * @typedef {Object} northstar.research.GetApiConfigurationsResponse.$Properties
             * @property {Array.<northstar.research.SavedConfiguration.$Properties>|null} [items] GetApiConfigurationsResponse items
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetApiConfigurationsResponse.
             * @memberof northstar.research
             * @interface IGetApiConfigurationsResponse
             * @augments northstar.research.GetApiConfigurationsResponse.$Properties
             * @deprecated Use northstar.research.GetApiConfigurationsResponse.$Properties instead.
             */

            /**
             * Shape of a GetApiConfigurationsResponse.
             * @typedef {{
             *   items?: Array.<northstar.research.SavedConfiguration.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} northstar.research.GetApiConfigurationsResponse.$Shape
             */

            /**
             * Constructs a new GetApiConfigurationsResponse.
             * @memberof northstar.research
             * @classdesc Represents a GetApiConfigurationsResponse.
             * @constructor
             * @param {northstar.research.GetApiConfigurationsResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const GetApiConfigurationsResponse = function (properties) {
                this.items = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetApiConfigurationsResponse items.
             * @member {Array.<northstar.research.SavedConfiguration.$Properties>} items
             * @memberof northstar.research.GetApiConfigurationsResponse
             * @instance
             */
            GetApiConfigurationsResponse.prototype.items = $util.emptyArray;

            /**
             * Creates a new GetApiConfigurationsResponse instance using the specified properties.
             * @function create
             * @memberof northstar.research.GetApiConfigurationsResponse
             * @static
             * @param {northstar.research.GetApiConfigurationsResponse.$Properties=} [properties] Properties to set
             * @returns {northstar.research.GetApiConfigurationsResponse} GetApiConfigurationsResponse instance
             * @type {{
             *   (properties: northstar.research.GetApiConfigurationsResponse.$Shape): northstar.research.GetApiConfigurationsResponse & northstar.research.GetApiConfigurationsResponse.$Shape;
             *   (properties?: northstar.research.GetApiConfigurationsResponse.$Properties): northstar.research.GetApiConfigurationsResponse;
             * }}
             */
            GetApiConfigurationsResponse.create = function(properties) {
                return new GetApiConfigurationsResponse(properties);
            };

            /**
             * Encodes the specified GetApiConfigurationsResponse message. Does not implicitly {@link northstar.research.GetApiConfigurationsResponse.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.GetApiConfigurationsResponse
             * @static
             * @param {northstar.research.GetApiConfigurationsResponse.$Properties} message GetApiConfigurationsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetApiConfigurationsResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.items != null && message.items.length)
                    for (let i = 0; i < message.items.length; ++i)
                        $root.northstar.research.SavedConfiguration.encode(message.items[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a GetApiConfigurationsResponse message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.GetApiConfigurationsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.GetApiConfigurationsResponse & northstar.research.GetApiConfigurationsResponse.$Shape} GetApiConfigurationsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetApiConfigurationsResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.GetApiConfigurationsResponse();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.items && message.items.length))
                                message.items = [];
                            message.items.push($root.northstar.research.SavedConfiguration.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a GetApiConfigurationsResponse message.
             * @function verify
             * @memberof northstar.research.GetApiConfigurationsResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetApiConfigurationsResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.items != null && $Object.hasOwnProperty.call(message, "items")) {
                    if (!$Array.isArray(message.items))
                        return "items: array expected";
                    for (let i = 0; i < message.items.length; ++i) {
                        let error = $root.northstar.research.SavedConfiguration.verify(message.items[i], _depth + 1);
                        if (error)
                            return "items." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a GetApiConfigurationsResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.GetApiConfigurationsResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.GetApiConfigurationsResponse} GetApiConfigurationsResponse
             */
            GetApiConfigurationsResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.GetApiConfigurationsResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.GetApiConfigurationsResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.GetApiConfigurationsResponse();
                if (object.items) {
                    if (!$Array.isArray(object.items))
                        throw $TypeError(".northstar.research.GetApiConfigurationsResponse.items: array expected");
                    message.items = $Array(object.items.length);
                    for (let i = 0; i < object.items.length; ++i) {
                        if (!$util.isObject(object.items[i]))
                            throw $TypeError(".northstar.research.GetApiConfigurationsResponse.items: object expected");
                        message.items[i] = $root.northstar.research.SavedConfiguration.fromObject(object.items[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a GetApiConfigurationsResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.GetApiConfigurationsResponse
             * @static
             * @param {northstar.research.GetApiConfigurationsResponse} message GetApiConfigurationsResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetApiConfigurationsResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.items = [];
                if (message.items && message.items.length) {
                    object.items = $Array(message.items.length);
                    for (let j = 0; j < message.items.length; ++j)
                        object.items[j] = $root.northstar.research.SavedConfiguration.toObject(message.items[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this GetApiConfigurationsResponse to JSON.
             * @function toJSON
             * @memberof northstar.research.GetApiConfigurationsResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetApiConfigurationsResponse.prototype.toJSON = function() {
                return GetApiConfigurationsResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetApiConfigurationsResponse
             * @function getTypeUrl
             * @memberof northstar.research.GetApiConfigurationsResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetApiConfigurationsResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.GetApiConfigurationsResponse";
            };

            return GetApiConfigurationsResponse;
        })();

        research.GetApiDatasetsResponse = (function() {

            /**
             * Properties of a GetApiDatasetsResponse.
             * @typedef {Object} northstar.research.GetApiDatasetsResponse.$Properties
             * @property {Array.<northstar.research.DatasetSummary.$Properties>|null} [items] GetApiDatasetsResponse items
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetApiDatasetsResponse.
             * @memberof northstar.research
             * @interface IGetApiDatasetsResponse
             * @augments northstar.research.GetApiDatasetsResponse.$Properties
             * @deprecated Use northstar.research.GetApiDatasetsResponse.$Properties instead.
             */

            /**
             * Shape of a GetApiDatasetsResponse.
             * @typedef {{
             *   items?: Array.<northstar.research.DatasetSummary.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} northstar.research.GetApiDatasetsResponse.$Shape
             */

            /**
             * Constructs a new GetApiDatasetsResponse.
             * @memberof northstar.research
             * @classdesc Represents a GetApiDatasetsResponse.
             * @constructor
             * @param {northstar.research.GetApiDatasetsResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const GetApiDatasetsResponse = function (properties) {
                this.items = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetApiDatasetsResponse items.
             * @member {Array.<northstar.research.DatasetSummary.$Properties>} items
             * @memberof northstar.research.GetApiDatasetsResponse
             * @instance
             */
            GetApiDatasetsResponse.prototype.items = $util.emptyArray;

            /**
             * Creates a new GetApiDatasetsResponse instance using the specified properties.
             * @function create
             * @memberof northstar.research.GetApiDatasetsResponse
             * @static
             * @param {northstar.research.GetApiDatasetsResponse.$Properties=} [properties] Properties to set
             * @returns {northstar.research.GetApiDatasetsResponse} GetApiDatasetsResponse instance
             * @type {{
             *   (properties: northstar.research.GetApiDatasetsResponse.$Shape): northstar.research.GetApiDatasetsResponse & northstar.research.GetApiDatasetsResponse.$Shape;
             *   (properties?: northstar.research.GetApiDatasetsResponse.$Properties): northstar.research.GetApiDatasetsResponse;
             * }}
             */
            GetApiDatasetsResponse.create = function(properties) {
                return new GetApiDatasetsResponse(properties);
            };

            /**
             * Encodes the specified GetApiDatasetsResponse message. Does not implicitly {@link northstar.research.GetApiDatasetsResponse.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.GetApiDatasetsResponse
             * @static
             * @param {northstar.research.GetApiDatasetsResponse.$Properties} message GetApiDatasetsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetApiDatasetsResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.items != null && message.items.length)
                    for (let i = 0; i < message.items.length; ++i)
                        $root.northstar.research.DatasetSummary.encode(message.items[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a GetApiDatasetsResponse message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.GetApiDatasetsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.GetApiDatasetsResponse & northstar.research.GetApiDatasetsResponse.$Shape} GetApiDatasetsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetApiDatasetsResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.GetApiDatasetsResponse();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.items && message.items.length))
                                message.items = [];
                            message.items.push($root.northstar.research.DatasetSummary.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a GetApiDatasetsResponse message.
             * @function verify
             * @memberof northstar.research.GetApiDatasetsResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetApiDatasetsResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.items != null && $Object.hasOwnProperty.call(message, "items")) {
                    if (!$Array.isArray(message.items))
                        return "items: array expected";
                    for (let i = 0; i < message.items.length; ++i) {
                        let error = $root.northstar.research.DatasetSummary.verify(message.items[i], _depth + 1);
                        if (error)
                            return "items." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a GetApiDatasetsResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.GetApiDatasetsResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.GetApiDatasetsResponse} GetApiDatasetsResponse
             */
            GetApiDatasetsResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.GetApiDatasetsResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.GetApiDatasetsResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.GetApiDatasetsResponse();
                if (object.items) {
                    if (!$Array.isArray(object.items))
                        throw $TypeError(".northstar.research.GetApiDatasetsResponse.items: array expected");
                    message.items = $Array(object.items.length);
                    for (let i = 0; i < object.items.length; ++i) {
                        if (!$util.isObject(object.items[i]))
                            throw $TypeError(".northstar.research.GetApiDatasetsResponse.items: object expected");
                        message.items[i] = $root.northstar.research.DatasetSummary.fromObject(object.items[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a GetApiDatasetsResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.GetApiDatasetsResponse
             * @static
             * @param {northstar.research.GetApiDatasetsResponse} message GetApiDatasetsResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetApiDatasetsResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.items = [];
                if (message.items && message.items.length) {
                    object.items = $Array(message.items.length);
                    for (let j = 0; j < message.items.length; ++j)
                        object.items[j] = $root.northstar.research.DatasetSummary.toObject(message.items[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this GetApiDatasetsResponse to JSON.
             * @function toJSON
             * @memberof northstar.research.GetApiDatasetsResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetApiDatasetsResponse.prototype.toJSON = function() {
                return GetApiDatasetsResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetApiDatasetsResponse
             * @function getTypeUrl
             * @memberof northstar.research.GetApiDatasetsResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetApiDatasetsResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.GetApiDatasetsResponse";
            };

            return GetApiDatasetsResponse;
        })();

        research.GetApiFactorRevisionsResponse = (function() {

            /**
             * Properties of a GetApiFactorRevisionsResponse.
             * @typedef {Object} northstar.research.GetApiFactorRevisionsResponse.$Properties
             * @property {Array.<northstar.research.FactorRevision.$Properties>|null} [items] GetApiFactorRevisionsResponse items
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetApiFactorRevisionsResponse.
             * @memberof northstar.research
             * @interface IGetApiFactorRevisionsResponse
             * @augments northstar.research.GetApiFactorRevisionsResponse.$Properties
             * @deprecated Use northstar.research.GetApiFactorRevisionsResponse.$Properties instead.
             */

            /**
             * Shape of a GetApiFactorRevisionsResponse.
             * @typedef {{
             *   items?: Array.<northstar.research.FactorRevision.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} northstar.research.GetApiFactorRevisionsResponse.$Shape
             */

            /**
             * Constructs a new GetApiFactorRevisionsResponse.
             * @memberof northstar.research
             * @classdesc Represents a GetApiFactorRevisionsResponse.
             * @constructor
             * @param {northstar.research.GetApiFactorRevisionsResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const GetApiFactorRevisionsResponse = function (properties) {
                this.items = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetApiFactorRevisionsResponse items.
             * @member {Array.<northstar.research.FactorRevision.$Properties>} items
             * @memberof northstar.research.GetApiFactorRevisionsResponse
             * @instance
             */
            GetApiFactorRevisionsResponse.prototype.items = $util.emptyArray;

            /**
             * Creates a new GetApiFactorRevisionsResponse instance using the specified properties.
             * @function create
             * @memberof northstar.research.GetApiFactorRevisionsResponse
             * @static
             * @param {northstar.research.GetApiFactorRevisionsResponse.$Properties=} [properties] Properties to set
             * @returns {northstar.research.GetApiFactorRevisionsResponse} GetApiFactorRevisionsResponse instance
             * @type {{
             *   (properties: northstar.research.GetApiFactorRevisionsResponse.$Shape): northstar.research.GetApiFactorRevisionsResponse & northstar.research.GetApiFactorRevisionsResponse.$Shape;
             *   (properties?: northstar.research.GetApiFactorRevisionsResponse.$Properties): northstar.research.GetApiFactorRevisionsResponse;
             * }}
             */
            GetApiFactorRevisionsResponse.create = function(properties) {
                return new GetApiFactorRevisionsResponse(properties);
            };

            /**
             * Encodes the specified GetApiFactorRevisionsResponse message. Does not implicitly {@link northstar.research.GetApiFactorRevisionsResponse.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.GetApiFactorRevisionsResponse
             * @static
             * @param {northstar.research.GetApiFactorRevisionsResponse.$Properties} message GetApiFactorRevisionsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetApiFactorRevisionsResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.items != null && message.items.length)
                    for (let i = 0; i < message.items.length; ++i)
                        $root.northstar.research.FactorRevision.encode(message.items[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a GetApiFactorRevisionsResponse message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.GetApiFactorRevisionsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.GetApiFactorRevisionsResponse & northstar.research.GetApiFactorRevisionsResponse.$Shape} GetApiFactorRevisionsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetApiFactorRevisionsResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.GetApiFactorRevisionsResponse();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.items && message.items.length))
                                message.items = [];
                            message.items.push($root.northstar.research.FactorRevision.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a GetApiFactorRevisionsResponse message.
             * @function verify
             * @memberof northstar.research.GetApiFactorRevisionsResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetApiFactorRevisionsResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.items != null && $Object.hasOwnProperty.call(message, "items")) {
                    if (!$Array.isArray(message.items))
                        return "items: array expected";
                    for (let i = 0; i < message.items.length; ++i) {
                        let error = $root.northstar.research.FactorRevision.verify(message.items[i], _depth + 1);
                        if (error)
                            return "items." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a GetApiFactorRevisionsResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.GetApiFactorRevisionsResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.GetApiFactorRevisionsResponse} GetApiFactorRevisionsResponse
             */
            GetApiFactorRevisionsResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.GetApiFactorRevisionsResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.GetApiFactorRevisionsResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.GetApiFactorRevisionsResponse();
                if (object.items) {
                    if (!$Array.isArray(object.items))
                        throw $TypeError(".northstar.research.GetApiFactorRevisionsResponse.items: array expected");
                    message.items = $Array(object.items.length);
                    for (let i = 0; i < object.items.length; ++i) {
                        if (!$util.isObject(object.items[i]))
                            throw $TypeError(".northstar.research.GetApiFactorRevisionsResponse.items: object expected");
                        message.items[i] = $root.northstar.research.FactorRevision.fromObject(object.items[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a GetApiFactorRevisionsResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.GetApiFactorRevisionsResponse
             * @static
             * @param {northstar.research.GetApiFactorRevisionsResponse} message GetApiFactorRevisionsResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetApiFactorRevisionsResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.items = [];
                if (message.items && message.items.length) {
                    object.items = $Array(message.items.length);
                    for (let j = 0; j < message.items.length; ++j)
                        object.items[j] = $root.northstar.research.FactorRevision.toObject(message.items[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this GetApiFactorRevisionsResponse to JSON.
             * @function toJSON
             * @memberof northstar.research.GetApiFactorRevisionsResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetApiFactorRevisionsResponse.prototype.toJSON = function() {
                return GetApiFactorRevisionsResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetApiFactorRevisionsResponse
             * @function getTypeUrl
             * @memberof northstar.research.GetApiFactorRevisionsResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetApiFactorRevisionsResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.GetApiFactorRevisionsResponse";
            };

            return GetApiFactorRevisionsResponse;
        })();

        research.GetApiFactorRunsResponse = (function() {

            /**
             * Properties of a GetApiFactorRunsResponse.
             * @typedef {Object} northstar.research.GetApiFactorRunsResponse.$Properties
             * @property {Array.<northstar.research.FactorRun.$Properties>|null} [items] GetApiFactorRunsResponse items
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetApiFactorRunsResponse.
             * @memberof northstar.research
             * @interface IGetApiFactorRunsResponse
             * @augments northstar.research.GetApiFactorRunsResponse.$Properties
             * @deprecated Use northstar.research.GetApiFactorRunsResponse.$Properties instead.
             */

            /**
             * Shape of a GetApiFactorRunsResponse.
             * @typedef {{
             *   items?: Array.<northstar.research.FactorRun.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} northstar.research.GetApiFactorRunsResponse.$Shape
             */

            /**
             * Constructs a new GetApiFactorRunsResponse.
             * @memberof northstar.research
             * @classdesc Represents a GetApiFactorRunsResponse.
             * @constructor
             * @param {northstar.research.GetApiFactorRunsResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const GetApiFactorRunsResponse = function (properties) {
                this.items = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetApiFactorRunsResponse items.
             * @member {Array.<northstar.research.FactorRun.$Properties>} items
             * @memberof northstar.research.GetApiFactorRunsResponse
             * @instance
             */
            GetApiFactorRunsResponse.prototype.items = $util.emptyArray;

            /**
             * Creates a new GetApiFactorRunsResponse instance using the specified properties.
             * @function create
             * @memberof northstar.research.GetApiFactorRunsResponse
             * @static
             * @param {northstar.research.GetApiFactorRunsResponse.$Properties=} [properties] Properties to set
             * @returns {northstar.research.GetApiFactorRunsResponse} GetApiFactorRunsResponse instance
             * @type {{
             *   (properties: northstar.research.GetApiFactorRunsResponse.$Shape): northstar.research.GetApiFactorRunsResponse & northstar.research.GetApiFactorRunsResponse.$Shape;
             *   (properties?: northstar.research.GetApiFactorRunsResponse.$Properties): northstar.research.GetApiFactorRunsResponse;
             * }}
             */
            GetApiFactorRunsResponse.create = function(properties) {
                return new GetApiFactorRunsResponse(properties);
            };

            /**
             * Encodes the specified GetApiFactorRunsResponse message. Does not implicitly {@link northstar.research.GetApiFactorRunsResponse.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.GetApiFactorRunsResponse
             * @static
             * @param {northstar.research.GetApiFactorRunsResponse.$Properties} message GetApiFactorRunsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetApiFactorRunsResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.items != null && message.items.length)
                    for (let i = 0; i < message.items.length; ++i)
                        $root.northstar.research.FactorRun.encode(message.items[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a GetApiFactorRunsResponse message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.GetApiFactorRunsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.GetApiFactorRunsResponse & northstar.research.GetApiFactorRunsResponse.$Shape} GetApiFactorRunsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetApiFactorRunsResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.GetApiFactorRunsResponse();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.items && message.items.length))
                                message.items = [];
                            message.items.push($root.northstar.research.FactorRun.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a GetApiFactorRunsResponse message.
             * @function verify
             * @memberof northstar.research.GetApiFactorRunsResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetApiFactorRunsResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.items != null && $Object.hasOwnProperty.call(message, "items")) {
                    if (!$Array.isArray(message.items))
                        return "items: array expected";
                    for (let i = 0; i < message.items.length; ++i) {
                        let error = $root.northstar.research.FactorRun.verify(message.items[i], _depth + 1);
                        if (error)
                            return "items." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a GetApiFactorRunsResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.GetApiFactorRunsResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.GetApiFactorRunsResponse} GetApiFactorRunsResponse
             */
            GetApiFactorRunsResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.GetApiFactorRunsResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.GetApiFactorRunsResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.GetApiFactorRunsResponse();
                if (object.items) {
                    if (!$Array.isArray(object.items))
                        throw $TypeError(".northstar.research.GetApiFactorRunsResponse.items: array expected");
                    message.items = $Array(object.items.length);
                    for (let i = 0; i < object.items.length; ++i) {
                        if (!$util.isObject(object.items[i]))
                            throw $TypeError(".northstar.research.GetApiFactorRunsResponse.items: object expected");
                        message.items[i] = $root.northstar.research.FactorRun.fromObject(object.items[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a GetApiFactorRunsResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.GetApiFactorRunsResponse
             * @static
             * @param {northstar.research.GetApiFactorRunsResponse} message GetApiFactorRunsResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetApiFactorRunsResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.items = [];
                if (message.items && message.items.length) {
                    object.items = $Array(message.items.length);
                    for (let j = 0; j < message.items.length; ++j)
                        object.items[j] = $root.northstar.research.FactorRun.toObject(message.items[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this GetApiFactorRunsResponse to JSON.
             * @function toJSON
             * @memberof northstar.research.GetApiFactorRunsResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetApiFactorRunsResponse.prototype.toJSON = function() {
                return GetApiFactorRunsResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetApiFactorRunsResponse
             * @function getTypeUrl
             * @memberof northstar.research.GetApiFactorRunsResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetApiFactorRunsResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.GetApiFactorRunsResponse";
            };

            return GetApiFactorRunsResponse;
        })();

        research.GetApiPaperResponse = (function() {

            /**
             * Properties of a GetApiPaperResponse.
             * @typedef {Object} northstar.research.GetApiPaperResponse.$Properties
             * @property {Array.<northstar.research.PaperSession.$Properties>|null} [items] GetApiPaperResponse items
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetApiPaperResponse.
             * @memberof northstar.research
             * @interface IGetApiPaperResponse
             * @augments northstar.research.GetApiPaperResponse.$Properties
             * @deprecated Use northstar.research.GetApiPaperResponse.$Properties instead.
             */

            /**
             * Shape of a GetApiPaperResponse.
             * @typedef {{
             *   items?: Array.<northstar.research.PaperSession.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} northstar.research.GetApiPaperResponse.$Shape
             */

            /**
             * Constructs a new GetApiPaperResponse.
             * @memberof northstar.research
             * @classdesc Represents a GetApiPaperResponse.
             * @constructor
             * @param {northstar.research.GetApiPaperResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const GetApiPaperResponse = function (properties) {
                this.items = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetApiPaperResponse items.
             * @member {Array.<northstar.research.PaperSession.$Properties>} items
             * @memberof northstar.research.GetApiPaperResponse
             * @instance
             */
            GetApiPaperResponse.prototype.items = $util.emptyArray;

            /**
             * Creates a new GetApiPaperResponse instance using the specified properties.
             * @function create
             * @memberof northstar.research.GetApiPaperResponse
             * @static
             * @param {northstar.research.GetApiPaperResponse.$Properties=} [properties] Properties to set
             * @returns {northstar.research.GetApiPaperResponse} GetApiPaperResponse instance
             * @type {{
             *   (properties: northstar.research.GetApiPaperResponse.$Shape): northstar.research.GetApiPaperResponse & northstar.research.GetApiPaperResponse.$Shape;
             *   (properties?: northstar.research.GetApiPaperResponse.$Properties): northstar.research.GetApiPaperResponse;
             * }}
             */
            GetApiPaperResponse.create = function(properties) {
                return new GetApiPaperResponse(properties);
            };

            /**
             * Encodes the specified GetApiPaperResponse message. Does not implicitly {@link northstar.research.GetApiPaperResponse.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.GetApiPaperResponse
             * @static
             * @param {northstar.research.GetApiPaperResponse.$Properties} message GetApiPaperResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetApiPaperResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.items != null && message.items.length)
                    for (let i = 0; i < message.items.length; ++i)
                        $root.northstar.research.PaperSession.encode(message.items[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a GetApiPaperResponse message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.GetApiPaperResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.GetApiPaperResponse & northstar.research.GetApiPaperResponse.$Shape} GetApiPaperResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetApiPaperResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.GetApiPaperResponse();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.items && message.items.length))
                                message.items = [];
                            message.items.push($root.northstar.research.PaperSession.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a GetApiPaperResponse message.
             * @function verify
             * @memberof northstar.research.GetApiPaperResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetApiPaperResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.items != null && $Object.hasOwnProperty.call(message, "items")) {
                    if (!$Array.isArray(message.items))
                        return "items: array expected";
                    for (let i = 0; i < message.items.length; ++i) {
                        let error = $root.northstar.research.PaperSession.verify(message.items[i], _depth + 1);
                        if (error)
                            return "items." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a GetApiPaperResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.GetApiPaperResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.GetApiPaperResponse} GetApiPaperResponse
             */
            GetApiPaperResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.GetApiPaperResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.GetApiPaperResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.GetApiPaperResponse();
                if (object.items) {
                    if (!$Array.isArray(object.items))
                        throw $TypeError(".northstar.research.GetApiPaperResponse.items: array expected");
                    message.items = $Array(object.items.length);
                    for (let i = 0; i < object.items.length; ++i) {
                        if (!$util.isObject(object.items[i]))
                            throw $TypeError(".northstar.research.GetApiPaperResponse.items: object expected");
                        message.items[i] = $root.northstar.research.PaperSession.fromObject(object.items[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a GetApiPaperResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.GetApiPaperResponse
             * @static
             * @param {northstar.research.GetApiPaperResponse} message GetApiPaperResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetApiPaperResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.items = [];
                if (message.items && message.items.length) {
                    object.items = $Array(message.items.length);
                    for (let j = 0; j < message.items.length; ++j)
                        object.items[j] = $root.northstar.research.PaperSession.toObject(message.items[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this GetApiPaperResponse to JSON.
             * @function toJSON
             * @memberof northstar.research.GetApiPaperResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetApiPaperResponse.prototype.toJSON = function() {
                return GetApiPaperResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetApiPaperResponse
             * @function getTypeUrl
             * @memberof northstar.research.GetApiPaperResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetApiPaperResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.GetApiPaperResponse";
            };

            return GetApiPaperResponse;
        })();

        research.GetApiResearchAttemptsResponse = (function() {

            /**
             * Properties of a GetApiResearchAttemptsResponse.
             * @typedef {Object} northstar.research.GetApiResearchAttemptsResponse.$Properties
             * @property {Array.<northstar.research.ResearchAttempt.$Properties>|null} [items] GetApiResearchAttemptsResponse items
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetApiResearchAttemptsResponse.
             * @memberof northstar.research
             * @interface IGetApiResearchAttemptsResponse
             * @augments northstar.research.GetApiResearchAttemptsResponse.$Properties
             * @deprecated Use northstar.research.GetApiResearchAttemptsResponse.$Properties instead.
             */

            /**
             * Shape of a GetApiResearchAttemptsResponse.
             * @typedef {{
             *   items?: Array.<northstar.research.ResearchAttempt.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} northstar.research.GetApiResearchAttemptsResponse.$Shape
             */

            /**
             * Constructs a new GetApiResearchAttemptsResponse.
             * @memberof northstar.research
             * @classdesc Represents a GetApiResearchAttemptsResponse.
             * @constructor
             * @param {northstar.research.GetApiResearchAttemptsResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const GetApiResearchAttemptsResponse = function (properties) {
                this.items = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetApiResearchAttemptsResponse items.
             * @member {Array.<northstar.research.ResearchAttempt.$Properties>} items
             * @memberof northstar.research.GetApiResearchAttemptsResponse
             * @instance
             */
            GetApiResearchAttemptsResponse.prototype.items = $util.emptyArray;

            /**
             * Creates a new GetApiResearchAttemptsResponse instance using the specified properties.
             * @function create
             * @memberof northstar.research.GetApiResearchAttemptsResponse
             * @static
             * @param {northstar.research.GetApiResearchAttemptsResponse.$Properties=} [properties] Properties to set
             * @returns {northstar.research.GetApiResearchAttemptsResponse} GetApiResearchAttemptsResponse instance
             * @type {{
             *   (properties: northstar.research.GetApiResearchAttemptsResponse.$Shape): northstar.research.GetApiResearchAttemptsResponse & northstar.research.GetApiResearchAttemptsResponse.$Shape;
             *   (properties?: northstar.research.GetApiResearchAttemptsResponse.$Properties): northstar.research.GetApiResearchAttemptsResponse;
             * }}
             */
            GetApiResearchAttemptsResponse.create = function(properties) {
                return new GetApiResearchAttemptsResponse(properties);
            };

            /**
             * Encodes the specified GetApiResearchAttemptsResponse message. Does not implicitly {@link northstar.research.GetApiResearchAttemptsResponse.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.GetApiResearchAttemptsResponse
             * @static
             * @param {northstar.research.GetApiResearchAttemptsResponse.$Properties} message GetApiResearchAttemptsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetApiResearchAttemptsResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.items != null && message.items.length)
                    for (let i = 0; i < message.items.length; ++i)
                        $root.northstar.research.ResearchAttempt.encode(message.items[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a GetApiResearchAttemptsResponse message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.GetApiResearchAttemptsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.GetApiResearchAttemptsResponse & northstar.research.GetApiResearchAttemptsResponse.$Shape} GetApiResearchAttemptsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetApiResearchAttemptsResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.GetApiResearchAttemptsResponse();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.items && message.items.length))
                                message.items = [];
                            message.items.push($root.northstar.research.ResearchAttempt.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a GetApiResearchAttemptsResponse message.
             * @function verify
             * @memberof northstar.research.GetApiResearchAttemptsResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetApiResearchAttemptsResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.items != null && $Object.hasOwnProperty.call(message, "items")) {
                    if (!$Array.isArray(message.items))
                        return "items: array expected";
                    for (let i = 0; i < message.items.length; ++i) {
                        let error = $root.northstar.research.ResearchAttempt.verify(message.items[i], _depth + 1);
                        if (error)
                            return "items." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a GetApiResearchAttemptsResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.GetApiResearchAttemptsResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.GetApiResearchAttemptsResponse} GetApiResearchAttemptsResponse
             */
            GetApiResearchAttemptsResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.GetApiResearchAttemptsResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.GetApiResearchAttemptsResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.GetApiResearchAttemptsResponse();
                if (object.items) {
                    if (!$Array.isArray(object.items))
                        throw $TypeError(".northstar.research.GetApiResearchAttemptsResponse.items: array expected");
                    message.items = $Array(object.items.length);
                    for (let i = 0; i < object.items.length; ++i) {
                        if (!$util.isObject(object.items[i]))
                            throw $TypeError(".northstar.research.GetApiResearchAttemptsResponse.items: object expected");
                        message.items[i] = $root.northstar.research.ResearchAttempt.fromObject(object.items[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a GetApiResearchAttemptsResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.GetApiResearchAttemptsResponse
             * @static
             * @param {northstar.research.GetApiResearchAttemptsResponse} message GetApiResearchAttemptsResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetApiResearchAttemptsResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.items = [];
                if (message.items && message.items.length) {
                    object.items = $Array(message.items.length);
                    for (let j = 0; j < message.items.length; ++j)
                        object.items[j] = $root.northstar.research.ResearchAttempt.toObject(message.items[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this GetApiResearchAttemptsResponse to JSON.
             * @function toJSON
             * @memberof northstar.research.GetApiResearchAttemptsResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetApiResearchAttemptsResponse.prototype.toJSON = function() {
                return GetApiResearchAttemptsResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetApiResearchAttemptsResponse
             * @function getTypeUrl
             * @memberof northstar.research.GetApiResearchAttemptsResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetApiResearchAttemptsResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.GetApiResearchAttemptsResponse";
            };

            return GetApiResearchAttemptsResponse;
        })();

        research.PostApiRunComparisonsResponse = (function() {

            /**
             * Properties of a PostApiRunComparisonsResponse.
             * @typedef {Object} northstar.research.PostApiRunComparisonsResponse.$Properties
             * @property {Array.<northstar.research.Comparison.$Properties>|null} [items] PostApiRunComparisonsResponse items
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a PostApiRunComparisonsResponse.
             * @memberof northstar.research
             * @interface IPostApiRunComparisonsResponse
             * @augments northstar.research.PostApiRunComparisonsResponse.$Properties
             * @deprecated Use northstar.research.PostApiRunComparisonsResponse.$Properties instead.
             */

            /**
             * Shape of a PostApiRunComparisonsResponse.
             * @typedef {{
             *   items?: Array.<northstar.research.Comparison.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} northstar.research.PostApiRunComparisonsResponse.$Shape
             */

            /**
             * Constructs a new PostApiRunComparisonsResponse.
             * @memberof northstar.research
             * @classdesc Represents a PostApiRunComparisonsResponse.
             * @constructor
             * @param {northstar.research.PostApiRunComparisonsResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const PostApiRunComparisonsResponse = function (properties) {
                this.items = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * PostApiRunComparisonsResponse items.
             * @member {Array.<northstar.research.Comparison.$Properties>} items
             * @memberof northstar.research.PostApiRunComparisonsResponse
             * @instance
             */
            PostApiRunComparisonsResponse.prototype.items = $util.emptyArray;

            /**
             * Creates a new PostApiRunComparisonsResponse instance using the specified properties.
             * @function create
             * @memberof northstar.research.PostApiRunComparisonsResponse
             * @static
             * @param {northstar.research.PostApiRunComparisonsResponse.$Properties=} [properties] Properties to set
             * @returns {northstar.research.PostApiRunComparisonsResponse} PostApiRunComparisonsResponse instance
             * @type {{
             *   (properties: northstar.research.PostApiRunComparisonsResponse.$Shape): northstar.research.PostApiRunComparisonsResponse & northstar.research.PostApiRunComparisonsResponse.$Shape;
             *   (properties?: northstar.research.PostApiRunComparisonsResponse.$Properties): northstar.research.PostApiRunComparisonsResponse;
             * }}
             */
            PostApiRunComparisonsResponse.create = function(properties) {
                return new PostApiRunComparisonsResponse(properties);
            };

            /**
             * Encodes the specified PostApiRunComparisonsResponse message. Does not implicitly {@link northstar.research.PostApiRunComparisonsResponse.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.PostApiRunComparisonsResponse
             * @static
             * @param {northstar.research.PostApiRunComparisonsResponse.$Properties} message PostApiRunComparisonsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PostApiRunComparisonsResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.items != null && message.items.length)
                    for (let i = 0; i < message.items.length; ++i)
                        $root.northstar.research.Comparison.encode(message.items[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a PostApiRunComparisonsResponse message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.PostApiRunComparisonsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.PostApiRunComparisonsResponse & northstar.research.PostApiRunComparisonsResponse.$Shape} PostApiRunComparisonsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PostApiRunComparisonsResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.PostApiRunComparisonsResponse();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.items && message.items.length))
                                message.items = [];
                            message.items.push($root.northstar.research.Comparison.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a PostApiRunComparisonsResponse message.
             * @function verify
             * @memberof northstar.research.PostApiRunComparisonsResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PostApiRunComparisonsResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.items != null && $Object.hasOwnProperty.call(message, "items")) {
                    if (!$Array.isArray(message.items))
                        return "items: array expected";
                    for (let i = 0; i < message.items.length; ++i) {
                        let error = $root.northstar.research.Comparison.verify(message.items[i], _depth + 1);
                        if (error)
                            return "items." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a PostApiRunComparisonsResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.PostApiRunComparisonsResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.PostApiRunComparisonsResponse} PostApiRunComparisonsResponse
             */
            PostApiRunComparisonsResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.PostApiRunComparisonsResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.PostApiRunComparisonsResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.PostApiRunComparisonsResponse();
                if (object.items) {
                    if (!$Array.isArray(object.items))
                        throw $TypeError(".northstar.research.PostApiRunComparisonsResponse.items: array expected");
                    message.items = $Array(object.items.length);
                    for (let i = 0; i < object.items.length; ++i) {
                        if (!$util.isObject(object.items[i]))
                            throw $TypeError(".northstar.research.PostApiRunComparisonsResponse.items: object expected");
                        message.items[i] = $root.northstar.research.Comparison.fromObject(object.items[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a PostApiRunComparisonsResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.PostApiRunComparisonsResponse
             * @static
             * @param {northstar.research.PostApiRunComparisonsResponse} message PostApiRunComparisonsResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PostApiRunComparisonsResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.items = [];
                if (message.items && message.items.length) {
                    object.items = $Array(message.items.length);
                    for (let j = 0; j < message.items.length; ++j)
                        object.items[j] = $root.northstar.research.Comparison.toObject(message.items[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this PostApiRunComparisonsResponse to JSON.
             * @function toJSON
             * @memberof northstar.research.PostApiRunComparisonsResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PostApiRunComparisonsResponse.prototype.toJSON = function() {
                return PostApiRunComparisonsResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for PostApiRunComparisonsResponse
             * @function getTypeUrl
             * @memberof northstar.research.PostApiRunComparisonsResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            PostApiRunComparisonsResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.PostApiRunComparisonsResponse";
            };

            return PostApiRunComparisonsResponse;
        })();

        research.GetApiRunsResponse = (function() {

            /**
             * Properties of a GetApiRunsResponse.
             * @typedef {Object} northstar.research.GetApiRunsResponse.$Properties
             * @property {Array.<northstar.research.RunSummary.$Properties>|null} [items] GetApiRunsResponse items
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetApiRunsResponse.
             * @memberof northstar.research
             * @interface IGetApiRunsResponse
             * @augments northstar.research.GetApiRunsResponse.$Properties
             * @deprecated Use northstar.research.GetApiRunsResponse.$Properties instead.
             */

            /**
             * Shape of a GetApiRunsResponse.
             * @typedef {{
             *   items?: Array.<northstar.research.RunSummary.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} northstar.research.GetApiRunsResponse.$Shape
             */

            /**
             * Constructs a new GetApiRunsResponse.
             * @memberof northstar.research
             * @classdesc Represents a GetApiRunsResponse.
             * @constructor
             * @param {northstar.research.GetApiRunsResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const GetApiRunsResponse = function (properties) {
                this.items = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetApiRunsResponse items.
             * @member {Array.<northstar.research.RunSummary.$Properties>} items
             * @memberof northstar.research.GetApiRunsResponse
             * @instance
             */
            GetApiRunsResponse.prototype.items = $util.emptyArray;

            /**
             * Creates a new GetApiRunsResponse instance using the specified properties.
             * @function create
             * @memberof northstar.research.GetApiRunsResponse
             * @static
             * @param {northstar.research.GetApiRunsResponse.$Properties=} [properties] Properties to set
             * @returns {northstar.research.GetApiRunsResponse} GetApiRunsResponse instance
             * @type {{
             *   (properties: northstar.research.GetApiRunsResponse.$Shape): northstar.research.GetApiRunsResponse & northstar.research.GetApiRunsResponse.$Shape;
             *   (properties?: northstar.research.GetApiRunsResponse.$Properties): northstar.research.GetApiRunsResponse;
             * }}
             */
            GetApiRunsResponse.create = function(properties) {
                return new GetApiRunsResponse(properties);
            };

            /**
             * Encodes the specified GetApiRunsResponse message. Does not implicitly {@link northstar.research.GetApiRunsResponse.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.GetApiRunsResponse
             * @static
             * @param {northstar.research.GetApiRunsResponse.$Properties} message GetApiRunsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetApiRunsResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.items != null && message.items.length)
                    for (let i = 0; i < message.items.length; ++i)
                        $root.northstar.research.RunSummary.encode(message.items[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a GetApiRunsResponse message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.GetApiRunsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.GetApiRunsResponse & northstar.research.GetApiRunsResponse.$Shape} GetApiRunsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetApiRunsResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.GetApiRunsResponse();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.items && message.items.length))
                                message.items = [];
                            message.items.push($root.northstar.research.RunSummary.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a GetApiRunsResponse message.
             * @function verify
             * @memberof northstar.research.GetApiRunsResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetApiRunsResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.items != null && $Object.hasOwnProperty.call(message, "items")) {
                    if (!$Array.isArray(message.items))
                        return "items: array expected";
                    for (let i = 0; i < message.items.length; ++i) {
                        let error = $root.northstar.research.RunSummary.verify(message.items[i], _depth + 1);
                        if (error)
                            return "items." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a GetApiRunsResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.GetApiRunsResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.GetApiRunsResponse} GetApiRunsResponse
             */
            GetApiRunsResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.GetApiRunsResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.GetApiRunsResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.GetApiRunsResponse();
                if (object.items) {
                    if (!$Array.isArray(object.items))
                        throw $TypeError(".northstar.research.GetApiRunsResponse.items: array expected");
                    message.items = $Array(object.items.length);
                    for (let i = 0; i < object.items.length; ++i) {
                        if (!$util.isObject(object.items[i]))
                            throw $TypeError(".northstar.research.GetApiRunsResponse.items: object expected");
                        message.items[i] = $root.northstar.research.RunSummary.fromObject(object.items[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a GetApiRunsResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.GetApiRunsResponse
             * @static
             * @param {northstar.research.GetApiRunsResponse} message GetApiRunsResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetApiRunsResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.items = [];
                if (message.items && message.items.length) {
                    object.items = $Array(message.items.length);
                    for (let j = 0; j < message.items.length; ++j)
                        object.items[j] = $root.northstar.research.RunSummary.toObject(message.items[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this GetApiRunsResponse to JSON.
             * @function toJSON
             * @memberof northstar.research.GetApiRunsResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetApiRunsResponse.prototype.toJSON = function() {
                return GetApiRunsResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetApiRunsResponse
             * @function getTypeUrl
             * @memberof northstar.research.GetApiRunsResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetApiRunsResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.GetApiRunsResponse";
            };

            return GetApiRunsResponse;
        })();

        research.GetApiStrategyCandidatesResponse = (function() {

            /**
             * Properties of a GetApiStrategyCandidatesResponse.
             * @typedef {Object} northstar.research.GetApiStrategyCandidatesResponse.$Properties
             * @property {Array.<northstar.research.StrategyCandidate.$Properties>|null} [items] GetApiStrategyCandidatesResponse items
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetApiStrategyCandidatesResponse.
             * @memberof northstar.research
             * @interface IGetApiStrategyCandidatesResponse
             * @augments northstar.research.GetApiStrategyCandidatesResponse.$Properties
             * @deprecated Use northstar.research.GetApiStrategyCandidatesResponse.$Properties instead.
             */

            /**
             * Shape of a GetApiStrategyCandidatesResponse.
             * @typedef {{
             *   items?: Array.<northstar.research.StrategyCandidate.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} northstar.research.GetApiStrategyCandidatesResponse.$Shape
             */

            /**
             * Constructs a new GetApiStrategyCandidatesResponse.
             * @memberof northstar.research
             * @classdesc Represents a GetApiStrategyCandidatesResponse.
             * @constructor
             * @param {northstar.research.GetApiStrategyCandidatesResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const GetApiStrategyCandidatesResponse = function (properties) {
                this.items = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetApiStrategyCandidatesResponse items.
             * @member {Array.<northstar.research.StrategyCandidate.$Properties>} items
             * @memberof northstar.research.GetApiStrategyCandidatesResponse
             * @instance
             */
            GetApiStrategyCandidatesResponse.prototype.items = $util.emptyArray;

            /**
             * Creates a new GetApiStrategyCandidatesResponse instance using the specified properties.
             * @function create
             * @memberof northstar.research.GetApiStrategyCandidatesResponse
             * @static
             * @param {northstar.research.GetApiStrategyCandidatesResponse.$Properties=} [properties] Properties to set
             * @returns {northstar.research.GetApiStrategyCandidatesResponse} GetApiStrategyCandidatesResponse instance
             * @type {{
             *   (properties: northstar.research.GetApiStrategyCandidatesResponse.$Shape): northstar.research.GetApiStrategyCandidatesResponse & northstar.research.GetApiStrategyCandidatesResponse.$Shape;
             *   (properties?: northstar.research.GetApiStrategyCandidatesResponse.$Properties): northstar.research.GetApiStrategyCandidatesResponse;
             * }}
             */
            GetApiStrategyCandidatesResponse.create = function(properties) {
                return new GetApiStrategyCandidatesResponse(properties);
            };

            /**
             * Encodes the specified GetApiStrategyCandidatesResponse message. Does not implicitly {@link northstar.research.GetApiStrategyCandidatesResponse.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.GetApiStrategyCandidatesResponse
             * @static
             * @param {northstar.research.GetApiStrategyCandidatesResponse.$Properties} message GetApiStrategyCandidatesResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetApiStrategyCandidatesResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.items != null && message.items.length)
                    for (let i = 0; i < message.items.length; ++i)
                        $root.northstar.research.StrategyCandidate.encode(message.items[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a GetApiStrategyCandidatesResponse message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.GetApiStrategyCandidatesResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.GetApiStrategyCandidatesResponse & northstar.research.GetApiStrategyCandidatesResponse.$Shape} GetApiStrategyCandidatesResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetApiStrategyCandidatesResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.GetApiStrategyCandidatesResponse();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.items && message.items.length))
                                message.items = [];
                            message.items.push($root.northstar.research.StrategyCandidate.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a GetApiStrategyCandidatesResponse message.
             * @function verify
             * @memberof northstar.research.GetApiStrategyCandidatesResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetApiStrategyCandidatesResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.items != null && $Object.hasOwnProperty.call(message, "items")) {
                    if (!$Array.isArray(message.items))
                        return "items: array expected";
                    for (let i = 0; i < message.items.length; ++i) {
                        let error = $root.northstar.research.StrategyCandidate.verify(message.items[i], _depth + 1);
                        if (error)
                            return "items." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a GetApiStrategyCandidatesResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.GetApiStrategyCandidatesResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.GetApiStrategyCandidatesResponse} GetApiStrategyCandidatesResponse
             */
            GetApiStrategyCandidatesResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.GetApiStrategyCandidatesResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.GetApiStrategyCandidatesResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.GetApiStrategyCandidatesResponse();
                if (object.items) {
                    if (!$Array.isArray(object.items))
                        throw $TypeError(".northstar.research.GetApiStrategyCandidatesResponse.items: array expected");
                    message.items = $Array(object.items.length);
                    for (let i = 0; i < object.items.length; ++i) {
                        if (!$util.isObject(object.items[i]))
                            throw $TypeError(".northstar.research.GetApiStrategyCandidatesResponse.items: object expected");
                        message.items[i] = $root.northstar.research.StrategyCandidate.fromObject(object.items[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a GetApiStrategyCandidatesResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.GetApiStrategyCandidatesResponse
             * @static
             * @param {northstar.research.GetApiStrategyCandidatesResponse} message GetApiStrategyCandidatesResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetApiStrategyCandidatesResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.items = [];
                if (message.items && message.items.length) {
                    object.items = $Array(message.items.length);
                    for (let j = 0; j < message.items.length; ++j)
                        object.items[j] = $root.northstar.research.StrategyCandidate.toObject(message.items[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this GetApiStrategyCandidatesResponse to JSON.
             * @function toJSON
             * @memberof northstar.research.GetApiStrategyCandidatesResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetApiStrategyCandidatesResponse.prototype.toJSON = function() {
                return GetApiStrategyCandidatesResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetApiStrategyCandidatesResponse
             * @function getTypeUrl
             * @memberof northstar.research.GetApiStrategyCandidatesResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetApiStrategyCandidatesResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.GetApiStrategyCandidatesResponse";
            };

            return GetApiStrategyCandidatesResponse;
        })();

        research.GetApiStrategyVersionsResponse = (function() {

            /**
             * Properties of a GetApiStrategyVersionsResponse.
             * @typedef {Object} northstar.research.GetApiStrategyVersionsResponse.$Properties
             * @property {Array.<northstar.research.StrategyVersion.$Properties>|null} [items] GetApiStrategyVersionsResponse items
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetApiStrategyVersionsResponse.
             * @memberof northstar.research
             * @interface IGetApiStrategyVersionsResponse
             * @augments northstar.research.GetApiStrategyVersionsResponse.$Properties
             * @deprecated Use northstar.research.GetApiStrategyVersionsResponse.$Properties instead.
             */

            /**
             * Shape of a GetApiStrategyVersionsResponse.
             * @typedef {{
             *   items?: Array.<northstar.research.StrategyVersion.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} northstar.research.GetApiStrategyVersionsResponse.$Shape
             */

            /**
             * Constructs a new GetApiStrategyVersionsResponse.
             * @memberof northstar.research
             * @classdesc Represents a GetApiStrategyVersionsResponse.
             * @constructor
             * @param {northstar.research.GetApiStrategyVersionsResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const GetApiStrategyVersionsResponse = function (properties) {
                this.items = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetApiStrategyVersionsResponse items.
             * @member {Array.<northstar.research.StrategyVersion.$Properties>} items
             * @memberof northstar.research.GetApiStrategyVersionsResponse
             * @instance
             */
            GetApiStrategyVersionsResponse.prototype.items = $util.emptyArray;

            /**
             * Creates a new GetApiStrategyVersionsResponse instance using the specified properties.
             * @function create
             * @memberof northstar.research.GetApiStrategyVersionsResponse
             * @static
             * @param {northstar.research.GetApiStrategyVersionsResponse.$Properties=} [properties] Properties to set
             * @returns {northstar.research.GetApiStrategyVersionsResponse} GetApiStrategyVersionsResponse instance
             * @type {{
             *   (properties: northstar.research.GetApiStrategyVersionsResponse.$Shape): northstar.research.GetApiStrategyVersionsResponse & northstar.research.GetApiStrategyVersionsResponse.$Shape;
             *   (properties?: northstar.research.GetApiStrategyVersionsResponse.$Properties): northstar.research.GetApiStrategyVersionsResponse;
             * }}
             */
            GetApiStrategyVersionsResponse.create = function(properties) {
                return new GetApiStrategyVersionsResponse(properties);
            };

            /**
             * Encodes the specified GetApiStrategyVersionsResponse message. Does not implicitly {@link northstar.research.GetApiStrategyVersionsResponse.verify|verify} messages.
             * @function encode
             * @memberof northstar.research.GetApiStrategyVersionsResponse
             * @static
             * @param {northstar.research.GetApiStrategyVersionsResponse.$Properties} message GetApiStrategyVersionsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetApiStrategyVersionsResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.items != null && message.items.length)
                    for (let i = 0; i < message.items.length; ++i)
                        $root.northstar.research.StrategyVersion.encode(message.items[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a GetApiStrategyVersionsResponse message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.research.GetApiStrategyVersionsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.research.GetApiStrategyVersionsResponse & northstar.research.GetApiStrategyVersionsResponse.$Shape} GetApiStrategyVersionsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetApiStrategyVersionsResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.research.GetApiStrategyVersionsResponse();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.items && message.items.length))
                                message.items = [];
                            message.items.push($root.northstar.research.StrategyVersion.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a GetApiStrategyVersionsResponse message.
             * @function verify
             * @memberof northstar.research.GetApiStrategyVersionsResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetApiStrategyVersionsResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.items != null && $Object.hasOwnProperty.call(message, "items")) {
                    if (!$Array.isArray(message.items))
                        return "items: array expected";
                    for (let i = 0; i < message.items.length; ++i) {
                        let error = $root.northstar.research.StrategyVersion.verify(message.items[i], _depth + 1);
                        if (error)
                            return "items." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a GetApiStrategyVersionsResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.research.GetApiStrategyVersionsResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.research.GetApiStrategyVersionsResponse} GetApiStrategyVersionsResponse
             */
            GetApiStrategyVersionsResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.research.GetApiStrategyVersionsResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.research.GetApiStrategyVersionsResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.research.GetApiStrategyVersionsResponse();
                if (object.items) {
                    if (!$Array.isArray(object.items))
                        throw $TypeError(".northstar.research.GetApiStrategyVersionsResponse.items: array expected");
                    message.items = $Array(object.items.length);
                    for (let i = 0; i < object.items.length; ++i) {
                        if (!$util.isObject(object.items[i]))
                            throw $TypeError(".northstar.research.GetApiStrategyVersionsResponse.items: object expected");
                        message.items[i] = $root.northstar.research.StrategyVersion.fromObject(object.items[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a GetApiStrategyVersionsResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.research.GetApiStrategyVersionsResponse
             * @static
             * @param {northstar.research.GetApiStrategyVersionsResponse} message GetApiStrategyVersionsResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetApiStrategyVersionsResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.items = [];
                if (message.items && message.items.length) {
                    object.items = $Array(message.items.length);
                    for (let j = 0; j < message.items.length; ++j)
                        object.items[j] = $root.northstar.research.StrategyVersion.toObject(message.items[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this GetApiStrategyVersionsResponse to JSON.
             * @function toJSON
             * @memberof northstar.research.GetApiStrategyVersionsResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetApiStrategyVersionsResponse.prototype.toJSON = function() {
                return GetApiStrategyVersionsResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetApiStrategyVersionsResponse
             * @function getTypeUrl
             * @memberof northstar.research.GetApiStrategyVersionsResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetApiStrategyVersionsResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.research.GetApiStrategyVersionsResponse";
            };

            return GetApiStrategyVersionsResponse;
        })();

        return research;
    })();

    northstar.web = (function() {

        /**
         * Namespace web.
         * @memberof northstar
         * @namespace
         */
        const web = {};

        web.Empty = (function() {

            /**
             * Properties of an Empty.
             * @typedef {Object} northstar.web.Empty.$Properties
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an Empty.
             * @memberof northstar.web
             * @interface IEmpty
             * @augments northstar.web.Empty.$Properties
             * @deprecated Use northstar.web.Empty.$Properties instead.
             */

            /**
             * Shape of an Empty.
             * @typedef {northstar.web.Empty.$Properties} northstar.web.Empty.$Shape
             */

            /**
             * Constructs a new Empty.
             * @memberof northstar.web
             * @classdesc Represents an Empty.
             * @constructor
             * @param {northstar.web.Empty.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const Empty = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Creates a new Empty instance using the specified properties.
             * @function create
             * @memberof northstar.web.Empty
             * @static
             * @param {northstar.web.Empty.$Properties=} [properties] Properties to set
             * @returns {northstar.web.Empty} Empty instance
             * @type {{
             *   (properties: northstar.web.Empty.$Shape): northstar.web.Empty & northstar.web.Empty.$Shape;
             *   (properties?: northstar.web.Empty.$Properties): northstar.web.Empty;
             * }}
             */
            Empty.create = function(properties) {
                return new Empty(properties);
            };

            /**
             * Encodes the specified Empty message. Does not implicitly {@link northstar.web.Empty.verify|verify} messages.
             * @function encode
             * @memberof northstar.web.Empty
             * @static
             * @param {northstar.web.Empty.$Properties} message Empty message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Empty.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes an Empty message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.web.Empty
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.web.Empty & northstar.web.Empty.$Shape} Empty
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Empty.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.web.Empty();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    reader.skipType(tag & 7, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies an Empty message.
             * @function verify
             * @memberof northstar.web.Empty
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Empty.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                return null;
            };

            /**
             * Creates an Empty message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.web.Empty
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.web.Empty} Empty
             */
            Empty.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.web.Empty)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.web.Empty: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                return new $root.northstar.web.Empty();
            };

            /**
             * Creates a plain object from an Empty message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.web.Empty
             * @static
             * @param {northstar.web.Empty} message Empty
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Empty.toObject = function () {
                return {};
            };

            /**
             * Converts this Empty to JSON.
             * @function toJSON
             * @memberof northstar.web.Empty
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Empty.prototype.toJSON = function() {
                return Empty.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Empty
             * @function getTypeUrl
             * @memberof northstar.web.Empty
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Empty.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.web.Empty";
            };

            return Empty;
        })();

        web.Error = (function() {

            /**
             * Properties of an Error.
             * @typedef {Object} northstar.web.Error.$Properties
             * @property {string|null} [detail] Error detail
             * @property {string|null} [status] Error status
             * @property {string|null} [request_id] Error request_id
             * @property {string|null} [runtime_id] Error runtime_id
             * @property {string|null} [url] Error url
             * @property {string|null} [rejection_id] Error rejection_id
             * @property {"detail"} [_detail] Error _detail
             * @property {"status"} [_status] Error _status
             * @property {"request_id"} [_request_id] Error _request_id
             * @property {"runtime_id"} [_runtime_id] Error _runtime_id
             * @property {"url"} [_url] Error _url
             * @property {"rejection_id"} [_rejection_id] Error _rejection_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an Error.
             * @memberof northstar.web
             * @interface IError
             * @augments northstar.web.Error.$Properties
             * @deprecated Use northstar.web.Error.$Properties instead.
             */

            /**
             * Narrowed shape of an Error.
             * @typedef {{
             *   detail?: string|null;
             *   status?: string|null;
             *   request_id?: string|null;
             *   runtime_id?: string|null;
             *   url?: string|null;
             *   rejection_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _detail?: undefined; detail?: null }|{ _detail?: "detail"; detail: string })
             * ) & (
             *   ({ _status?: undefined; status?: null }|{ _status?: "status"; status: string })
             * ) & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * ) & (
             *   ({ _runtime_id?: undefined; runtime_id?: null }|{ _runtime_id?: "runtime_id"; runtime_id: string })
             * ) & (
             *   ({ _url?: undefined; url?: null }|{ _url?: "url"; url: string })
             * ) & (
             *   ({ _rejection_id?: undefined; rejection_id?: null }|{ _rejection_id?: "rejection_id"; rejection_id: string })
             * )} northstar.web.Error.$Shape
             */

            /**
             * Constructs a new Error.
             * @memberof northstar.web
             * @classdesc Represents an Error.
             * @constructor
             * @param {northstar.web.Error.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const Error = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Error detail.
             * @member {string|null|undefined} detail
             * @memberof northstar.web.Error
             * @instance
             */
            Error.prototype.detail = null;

            /**
             * Error status.
             * @member {string|null|undefined} status
             * @memberof northstar.web.Error
             * @instance
             */
            Error.prototype.status = null;

            /**
             * Error request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.web.Error
             * @instance
             */
            Error.prototype.request_id = null;

            /**
             * Error runtime_id.
             * @member {string|null|undefined} runtime_id
             * @memberof northstar.web.Error
             * @instance
             */
            Error.prototype.runtime_id = null;

            /**
             * Error url.
             * @member {string|null|undefined} url
             * @memberof northstar.web.Error
             * @instance
             */
            Error.prototype.url = null;

            /**
             * Error rejection_id.
             * @member {string|null|undefined} rejection_id
             * @memberof northstar.web.Error
             * @instance
             */
            Error.prototype.rejection_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * Error _detail.
             * @member {"detail"|undefined} _detail
             * @memberof northstar.web.Error
             * @instance
             */
            $Object.defineProperty(Error.prototype, "_detail", {
                get: $util.oneOfGetter($oneOfFields = ["detail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Error _status.
             * @member {"status"|undefined} _status
             * @memberof northstar.web.Error
             * @instance
             */
            $Object.defineProperty(Error.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Error _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.web.Error
             * @instance
             */
            $Object.defineProperty(Error.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Error _runtime_id.
             * @member {"runtime_id"|undefined} _runtime_id
             * @memberof northstar.web.Error
             * @instance
             */
            $Object.defineProperty(Error.prototype, "_runtime_id", {
                get: $util.oneOfGetter($oneOfFields = ["runtime_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Error _url.
             * @member {"url"|undefined} _url
             * @memberof northstar.web.Error
             * @instance
             */
            $Object.defineProperty(Error.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Error _rejection_id.
             * @member {"rejection_id"|undefined} _rejection_id
             * @memberof northstar.web.Error
             * @instance
             */
            $Object.defineProperty(Error.prototype, "_rejection_id", {
                get: $util.oneOfGetter($oneOfFields = ["rejection_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new Error instance using the specified properties.
             * @function create
             * @memberof northstar.web.Error
             * @static
             * @param {northstar.web.Error.$Properties=} [properties] Properties to set
             * @returns {northstar.web.Error} Error instance
             * @type {{
             *   (properties: northstar.web.Error.$Shape): northstar.web.Error & northstar.web.Error.$Shape;
             *   (properties?: northstar.web.Error.$Properties): northstar.web.Error;
             * }}
             */
            Error.create = function(properties) {
                return new Error(properties);
            };

            /**
             * Encodes the specified Error message. Does not implicitly {@link northstar.web.Error.verify|verify} messages.
             * @function encode
             * @memberof northstar.web.Error
             * @static
             * @param {northstar.web.Error.$Properties} message Error message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Error.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.detail != null && $Object.hasOwnProperty.call(message, "detail"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.detail);
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.status);
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.request_id);
                if (message.runtime_id != null && $Object.hasOwnProperty.call(message, "runtime_id"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.runtime_id);
                if (message.url != null && $Object.hasOwnProperty.call(message, "url"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.url);
                if (message.rejection_id != null && $Object.hasOwnProperty.call(message, "rejection_id"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.rejection_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes an Error message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.web.Error
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.web.Error & northstar.web.Error.$Shape} Error
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Error.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.web.Error();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.detail = reader.stringVerify();
                            message._detail = "detail";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.status = reader.stringVerify();
                            message._status = "status";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.runtime_id = reader.stringVerify();
                            message._runtime_id = "runtime_id";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.url = reader.stringVerify();
                            message._url = "url";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.rejection_id = reader.stringVerify();
                            message._rejection_id = "rejection_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies an Error message.
             * @function verify
             * @memberof northstar.web.Error
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Error.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.detail != null && $Object.hasOwnProperty.call(message, "detail")) {
                    properties._detail = 1;
                    if (!$util.isString(message.detail))
                        return "detail: string expected";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (!$util.isString(message.status))
                        return "status: string expected";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                if (message.runtime_id != null && $Object.hasOwnProperty.call(message, "runtime_id")) {
                    properties._runtime_id = 1;
                    if (!$util.isString(message.runtime_id))
                        return "runtime_id: string expected";
                }
                if (message.url != null && $Object.hasOwnProperty.call(message, "url")) {
                    properties._url = 1;
                    if (!$util.isString(message.url))
                        return "url: string expected";
                }
                if (message.rejection_id != null && $Object.hasOwnProperty.call(message, "rejection_id")) {
                    properties._rejection_id = 1;
                    if (!$util.isString(message.rejection_id))
                        return "rejection_id: string expected";
                }
                return null;
            };

            /**
             * Creates an Error message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.web.Error
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.web.Error} Error
             */
            Error.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.web.Error)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.web.Error: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.web.Error();
                if (object.detail != null)
                    message.detail = $String(object.detail);
                if (object.status != null)
                    message.status = $String(object.status);
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                if (object.runtime_id != null)
                    message.runtime_id = $String(object.runtime_id);
                if (object.url != null)
                    message.url = $String(object.url);
                if (object.rejection_id != null)
                    message.rejection_id = $String(object.rejection_id);
                return message;
            };

            /**
             * Creates a plain object from an Error message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.web.Error
             * @static
             * @param {northstar.web.Error} message Error
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Error.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.detail != null && $Object.hasOwnProperty.call(message, "detail")) {
                    object.detail = message.detail;
                    if (options.oneofs)
                        object._detail = "detail";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    object.status = message.status;
                    if (options.oneofs)
                        object._status = "status";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                if (message.runtime_id != null && $Object.hasOwnProperty.call(message, "runtime_id")) {
                    object.runtime_id = message.runtime_id;
                    if (options.oneofs)
                        object._runtime_id = "runtime_id";
                }
                if (message.url != null && $Object.hasOwnProperty.call(message, "url")) {
                    object.url = message.url;
                    if (options.oneofs)
                        object._url = "url";
                }
                if (message.rejection_id != null && $Object.hasOwnProperty.call(message, "rejection_id")) {
                    object.rejection_id = message.rejection_id;
                    if (options.oneofs)
                        object._rejection_id = "rejection_id";
                }
                return object;
            };

            /**
             * Converts this Error to JSON.
             * @function toJSON
             * @memberof northstar.web.Error
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Error.prototype.toJSON = function() {
                return Error.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Error
             * @function getTypeUrl
             * @memberof northstar.web.Error
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Error.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.web.Error";
            };

            return Error;
        })();

        return web;
    })();

    return northstar;
})();

export {
  $root as default
};

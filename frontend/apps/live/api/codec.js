/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-mixed-operators, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars, default-case, jsdoc/require-param*/
import $protobuf from "protobufjs/minimal.js";

// Common aliases
const $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;
const $Object = $util.global.Object, $undefined = $util.global.undefined, $Error = $util.global.Error, $RangeError = $util.global.RangeError, $TypeError = $util.global.TypeError, $Number = $util.global.Number, $String = $util.global.String, $Boolean = $util.global.Boolean, $isFinite = $util.global.isFinite, $Array = $util.global.Array, $parseInt = $util.global.parseInt, $BigInt = $util.global.BigInt;

// Exported root namespace
const $root = $protobuf.roots["northstar_live"] || ($protobuf.roots["northstar_live"] = {});

export const google = $root.google = (() => {

    /**
     * Namespace google.
     * @exports google
     * @namespace
     */
    const google = {};

    google.protobuf = (function() {

        /**
         * Namespace protobuf.
         * @memberof google
         * @namespace
         */
        const protobuf = {};

        protobuf.Struct = (function() {

            /**
             * Properties of a Struct.
             * @typedef {Object} google.protobuf.Struct.$Properties
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [fields] Struct fields
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Struct.
             * @memberof google.protobuf
             * @interface IStruct
             * @augments google.protobuf.Struct.$Properties
             * @deprecated Use google.protobuf.Struct.$Properties instead.
             */

            /**
             * Shape of a Struct.
             * @typedef {{
             *   fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} google.protobuf.Struct.$Shape
             */

            /**
             * Constructs a new Struct.
             * @memberof google.protobuf
             * @classdesc Represents a Struct.
             * @constructor
             * @param {google.protobuf.Struct.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const Struct = function (properties) {
                this.fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Struct fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} fields
             * @memberof google.protobuf.Struct
             * @instance
             */
            Struct.prototype.fields = $util.emptyObject;

            /**
             * Creates a new Struct instance using the specified properties.
             * @function create
             * @memberof google.protobuf.Struct
             * @static
             * @param {google.protobuf.Struct.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.Struct} Struct instance
             * @type {{
             *   (properties: google.protobuf.Struct.$Shape): google.protobuf.Struct & google.protobuf.Struct.$Shape;
             *   (properties?: google.protobuf.Struct.$Properties): google.protobuf.Struct;
             * }}
             */
            Struct.create = function(properties) {
                return new Struct(properties);
            };

            /**
             * Encodes the specified Struct message. Does not implicitly {@link google.protobuf.Struct.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.Struct
             * @static
             * @param {google.protobuf.Struct.$Properties} message Struct message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Struct.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.fields != null && $Object.hasOwnProperty.call(message, "fields"))
                    for (let keys = $Object.keys(message.fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a Struct message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.Struct
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.Struct & google.protobuf.Struct.$Shape} Struct
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Struct.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.google.protobuf.Struct();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (message.fields === $util.emptyObject)
                                message.fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.fields, key);
                            message.fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a Struct message.
             * @function verify
             * @memberof google.protobuf.Struct
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Struct.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.fields != null && $Object.hasOwnProperty.call(message, "fields")) {
                    if (!$util.isObject(message.fields))
                        return "fields: object expected";
                    let key = $Object.keys(message.fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.fields[key[i]], _depth + 1);
                        if (error)
                            return "fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a Struct message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.Struct
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.Struct} Struct
             */
            Struct.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.Struct)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.Struct: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.Struct();
                if (object.fields) {
                    if (!$util.isObject(object.fields))
                        throw $TypeError(".google.protobuf.Struct.fields: object expected");
                    message.fields = {};
                    for (let keys = $Object.keys(object.fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.fields, keys[i]);
                        if (!$util.isObject(object.fields[keys[i]]))
                            throw $TypeError(".google.protobuf.Struct.fields: object expected");
                        message.fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a Struct message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.Struct
             * @static
             * @param {google.protobuf.Struct} message Struct
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Struct.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.fields = {};
                let keys2;
                if (message.fields && (keys2 = $Object.keys(message.fields)).length) {
                    object.fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.fields, keys2[j]);
                        object.fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this Struct to JSON.
             * @function toJSON
             * @memberof google.protobuf.Struct
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Struct.prototype.toJSON = function() {
                return Struct.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Struct
             * @function getTypeUrl
             * @memberof google.protobuf.Struct
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Struct.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.Struct";
            };

            return Struct;
        })();

        protobuf.Value = (function() {

            /**
             * Properties of a Value.
             * @typedef {Object} google.protobuf.Value.$Properties
             * @property {google.protobuf.NullValue|null} [nullValue] Value nullValue
             * @property {number|null} [numberValue] Value numberValue
             * @property {string|null} [stringValue] Value stringValue
             * @property {boolean|null} [boolValue] Value boolValue
             * @property {google.protobuf.Struct.$Properties|null} [structValue] Value structValue
             * @property {google.protobuf.ListValue.$Properties|null} [listValue] Value listValue
             * @property {"nullValue"|"numberValue"|"stringValue"|"boolValue"|"structValue"|"listValue"} [kind] Value kind
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Value.
             * @memberof google.protobuf
             * @interface IValue
             * @augments google.protobuf.Value.$Properties
             * @deprecated Use google.protobuf.Value.$Properties instead.
             */

            /**
             * Narrowed shape of a Value.
             * @typedef {{
             *   nullValue?: google.protobuf.NullValue|null;
             *   numberValue?: number|null;
             *   stringValue?: string|null;
             *   boolValue?: boolean|null;
             *   structValue?: google.protobuf.Struct.$Shape|null;
             *   listValue?: google.protobuf.ListValue.$Shape|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ kind?: undefined; nullValue?: null; numberValue?: null; stringValue?: null; boolValue?: null; structValue?: null; listValue?: null }|{ kind?: "nullValue"; nullValue: google.protobuf.NullValue; numberValue?: null; stringValue?: null; boolValue?: null; structValue?: null; listValue?: null }|{ kind?: "numberValue"; nullValue?: null; numberValue: number; stringValue?: null; boolValue?: null; structValue?: null; listValue?: null }|{ kind?: "stringValue"; nullValue?: null; numberValue?: null; stringValue: string; boolValue?: null; structValue?: null; listValue?: null }|{ kind?: "boolValue"; nullValue?: null; numberValue?: null; stringValue?: null; boolValue: boolean; structValue?: null; listValue?: null }|{ kind?: "structValue"; nullValue?: null; numberValue?: null; stringValue?: null; boolValue?: null; structValue: google.protobuf.Struct.$Shape; listValue?: null }|{ kind?: "listValue"; nullValue?: null; numberValue?: null; stringValue?: null; boolValue?: null; structValue?: null; listValue: google.protobuf.ListValue.$Shape })
             * )} google.protobuf.Value.$Shape
             */

            /**
             * Constructs a new Value.
             * @memberof google.protobuf
             * @classdesc Represents a Value.
             * @constructor
             * @param {google.protobuf.Value.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const Value = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Value nullValue.
             * @member {google.protobuf.NullValue|null|undefined} nullValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.nullValue = null;

            /**
             * Value numberValue.
             * @member {number|null|undefined} numberValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.numberValue = null;

            /**
             * Value stringValue.
             * @member {string|null|undefined} stringValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.stringValue = null;

            /**
             * Value boolValue.
             * @member {boolean|null|undefined} boolValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.boolValue = null;

            /**
             * Value structValue.
             * @member {google.protobuf.Struct.$Properties|null|undefined} structValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.structValue = null;

            /**
             * Value listValue.
             * @member {google.protobuf.ListValue.$Properties|null|undefined} listValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.listValue = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * Value kind.
             * @member {"nullValue"|"numberValue"|"stringValue"|"boolValue"|"structValue"|"listValue"|undefined} kind
             * @memberof google.protobuf.Value
             * @instance
             */
            $Object.defineProperty(Value.prototype, "kind", {
                get: $util.oneOfGetter($oneOfFields = ["nullValue", "numberValue", "stringValue", "boolValue", "structValue", "listValue"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new Value instance using the specified properties.
             * @function create
             * @memberof google.protobuf.Value
             * @static
             * @param {google.protobuf.Value.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.Value} Value instance
             * @type {{
             *   (properties: google.protobuf.Value.$Shape): google.protobuf.Value & google.protobuf.Value.$Shape;
             *   (properties?: google.protobuf.Value.$Properties): google.protobuf.Value;
             * }}
             */
            Value.create = function(properties) {
                return new Value(properties);
            };

            /**
             * Encodes the specified Value message. Does not implicitly {@link google.protobuf.Value.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.Value
             * @static
             * @param {google.protobuf.Value.$Properties} message Value message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Value.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.nullValue != null && $Object.hasOwnProperty.call(message, "nullValue"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.nullValue);
                if (message.numberValue != null && $Object.hasOwnProperty.call(message, "numberValue"))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.numberValue);
                if (message.stringValue != null && $Object.hasOwnProperty.call(message, "stringValue"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.stringValue);
                if (message.boolValue != null && $Object.hasOwnProperty.call(message, "boolValue"))
                    writer.uint32(/* id 4, wireType 0 =*/32).bool(message.boolValue);
                if (message.structValue != null && $Object.hasOwnProperty.call(message, "structValue"))
                    $root.google.protobuf.Struct.encode(message.structValue, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                if (message.listValue != null && $Object.hasOwnProperty.call(message, "listValue"))
                    $root.google.protobuf.ListValue.encode(message.listValue, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a Value message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.Value
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.Value & google.protobuf.Value.$Shape} Value
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Value.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.google.protobuf.Value();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            message.nullValue = reader.int32();
                            message.kind = "nullValue";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 1)
                                break;
                            message.numberValue = reader.double();
                            message.kind = "numberValue";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.stringValue = reader.stringVerify();
                            message.kind = "stringValue";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            message.boolValue = reader.bool();
                            message.kind = "boolValue";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.structValue = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.structValue);
                            message.kind = "structValue";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.listValue = $root.google.protobuf.ListValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.listValue);
                            message.kind = "listValue";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a Value message.
             * @function verify
             * @memberof google.protobuf.Value
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Value.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.nullValue != null && $Object.hasOwnProperty.call(message, "nullValue")) {
                    properties.kind = 1;
                    if (typeof message.nullValue !== "number" || (message.nullValue | 0) !== message.nullValue)
                        return "nullValue: enum value expected";
                }
                if (message.numberValue != null && $Object.hasOwnProperty.call(message, "numberValue")) {
                    if (properties.kind === 1)
                        return "kind: multiple values";
                    properties.kind = 1;
                    if (typeof message.numberValue !== "number")
                        return "numberValue: number expected";
                }
                if (message.stringValue != null && $Object.hasOwnProperty.call(message, "stringValue")) {
                    if (properties.kind === 1)
                        return "kind: multiple values";
                    properties.kind = 1;
                    if (!$util.isString(message.stringValue))
                        return "stringValue: string expected";
                }
                if (message.boolValue != null && $Object.hasOwnProperty.call(message, "boolValue")) {
                    if (properties.kind === 1)
                        return "kind: multiple values";
                    properties.kind = 1;
                    if (typeof message.boolValue !== "boolean")
                        return "boolValue: boolean expected";
                }
                if (message.structValue != null && $Object.hasOwnProperty.call(message, "structValue")) {
                    if (properties.kind === 1)
                        return "kind: multiple values";
                    properties.kind = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.structValue, _depth + 1);
                        if (error)
                            return "structValue." + error;
                    }
                }
                if (message.listValue != null && $Object.hasOwnProperty.call(message, "listValue")) {
                    if (properties.kind === 1)
                        return "kind: multiple values";
                    properties.kind = 1;
                    {
                        let error = $root.google.protobuf.ListValue.verify(message.listValue, _depth + 1);
                        if (error)
                            return "listValue." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a Value message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.Value
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.Value} Value
             */
            Value.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.Value)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.Value: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.Value();
                switch (object.nullValue) {
                case "NULL_VALUE":
                case 0:
                    message.nullValue = 0;
                    break;
                default:
                    if (typeof object.nullValue === "number" && (object.nullValue | 0) === object.nullValue)
                        message.nullValue = object.nullValue;
                }
                if (object.numberValue != null)
                    message.numberValue = $Number(object.numberValue);
                if (object.stringValue != null)
                    message.stringValue = $String(object.stringValue);
                if (object.boolValue != null)
                    message.boolValue = $Boolean(object.boolValue);
                if (object.structValue != null) {
                    if (!$util.isObject(object.structValue))
                        throw $TypeError(".google.protobuf.Value.structValue: object expected");
                    message.structValue = $root.google.protobuf.Struct.fromObject(object.structValue, _depth + 1);
                }
                if (object.listValue != null) {
                    if (!$util.isObject(object.listValue))
                        throw $TypeError(".google.protobuf.Value.listValue: object expected");
                    message.listValue = $root.google.protobuf.ListValue.fromObject(object.listValue, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a Value message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.Value
             * @static
             * @param {google.protobuf.Value} message Value
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Value.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.nullValue != null && $Object.hasOwnProperty.call(message, "nullValue")) {
                    object.nullValue = options.enums === $String ? $root.google.protobuf.NullValue[message.nullValue] === $undefined ? message.nullValue : $root.google.protobuf.NullValue[message.nullValue] : message.nullValue;
                    if (options.oneofs)
                        object.kind = "nullValue";
                }
                if (message.numberValue != null && $Object.hasOwnProperty.call(message, "numberValue")) {
                    object.numberValue = options.json && !$isFinite(message.numberValue) ? $String(message.numberValue) : message.numberValue;
                    if (options.oneofs)
                        object.kind = "numberValue";
                }
                if (message.stringValue != null && $Object.hasOwnProperty.call(message, "stringValue")) {
                    object.stringValue = message.stringValue;
                    if (options.oneofs)
                        object.kind = "stringValue";
                }
                if (message.boolValue != null && $Object.hasOwnProperty.call(message, "boolValue")) {
                    object.boolValue = message.boolValue;
                    if (options.oneofs)
                        object.kind = "boolValue";
                }
                if (message.structValue != null && $Object.hasOwnProperty.call(message, "structValue")) {
                    object.structValue = $root.google.protobuf.Struct.toObject(message.structValue, options, _depth + 1);
                    if (options.oneofs)
                        object.kind = "structValue";
                }
                if (message.listValue != null && $Object.hasOwnProperty.call(message, "listValue")) {
                    object.listValue = $root.google.protobuf.ListValue.toObject(message.listValue, options, _depth + 1);
                    if (options.oneofs)
                        object.kind = "listValue";
                }
                return object;
            };

            /**
             * Converts this Value to JSON.
             * @function toJSON
             * @memberof google.protobuf.Value
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Value.prototype.toJSON = function() {
                return Value.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Value
             * @function getTypeUrl
             * @memberof google.protobuf.Value
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Value.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.Value";
            };

            return Value;
        })();

        /**
         * NullValue enum.
         * @name google.protobuf.NullValue
         * @enum {number}
         * @property {number} NULL_VALUE=0 NULL_VALUE value
         */
        protobuf.NullValue = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "NULL_VALUE"] = 0;
            return values;
        })();

        protobuf.ListValue = (function() {

            /**
             * Properties of a ListValue.
             * @typedef {Object} google.protobuf.ListValue.$Properties
             * @property {Array.<google.protobuf.Value.$Properties>|null} [values] ListValue values
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ListValue.
             * @memberof google.protobuf
             * @interface IListValue
             * @augments google.protobuf.ListValue.$Properties
             * @deprecated Use google.protobuf.ListValue.$Properties instead.
             */

            /**
             * Shape of a ListValue.
             * @typedef {{
             *   values?: Array.<google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} google.protobuf.ListValue.$Shape
             */

            /**
             * Constructs a new ListValue.
             * @memberof google.protobuf
             * @classdesc Represents a ListValue.
             * @constructor
             * @param {google.protobuf.ListValue.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ListValue = function (properties) {
                this.values = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ListValue values.
             * @member {Array.<google.protobuf.Value.$Properties>} values
             * @memberof google.protobuf.ListValue
             * @instance
             */
            ListValue.prototype.values = $util.emptyArray;

            /**
             * Creates a new ListValue instance using the specified properties.
             * @function create
             * @memberof google.protobuf.ListValue
             * @static
             * @param {google.protobuf.ListValue.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.ListValue} ListValue instance
             * @type {{
             *   (properties: google.protobuf.ListValue.$Shape): google.protobuf.ListValue & google.protobuf.ListValue.$Shape;
             *   (properties?: google.protobuf.ListValue.$Properties): google.protobuf.ListValue;
             * }}
             */
            ListValue.create = function(properties) {
                return new ListValue(properties);
            };

            /**
             * Encodes the specified ListValue message. Does not implicitly {@link google.protobuf.ListValue.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.ListValue
             * @static
             * @param {google.protobuf.ListValue.$Properties} message ListValue message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ListValue.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.values != null && message.values.length)
                    for (let i = 0; i < message.values.length; ++i)
                        $root.google.protobuf.Value.encode(message.values[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a ListValue message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.ListValue
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.ListValue & google.protobuf.ListValue.$Shape} ListValue
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ListValue.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.google.protobuf.ListValue();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.values && message.values.length))
                                message.values = [];
                            message.values.push($root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a ListValue message.
             * @function verify
             * @memberof google.protobuf.ListValue
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ListValue.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.values != null && $Object.hasOwnProperty.call(message, "values")) {
                    if (!$Array.isArray(message.values))
                        return "values: array expected";
                    for (let i = 0; i < message.values.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.values[i], _depth + 1);
                        if (error)
                            return "values." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a ListValue message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.ListValue
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.ListValue} ListValue
             */
            ListValue.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.ListValue)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.ListValue: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.ListValue();
                if (object.values) {
                    if (!$Array.isArray(object.values))
                        throw $TypeError(".google.protobuf.ListValue.values: array expected");
                    message.values = $Array(object.values.length);
                    for (let i = 0; i < object.values.length; ++i) {
                        if (!$util.isObject(object.values[i]))
                            throw $TypeError(".google.protobuf.ListValue.values: object expected");
                        message.values[i] = $root.google.protobuf.Value.fromObject(object.values[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a ListValue message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.ListValue
             * @static
             * @param {google.protobuf.ListValue} message ListValue
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ListValue.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.values = [];
                if (message.values && message.values.length) {
                    object.values = $Array(message.values.length);
                    for (let j = 0; j < message.values.length; ++j)
                        object.values[j] = $root.google.protobuf.Value.toObject(message.values[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this ListValue to JSON.
             * @function toJSON
             * @memberof google.protobuf.ListValue
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ListValue.prototype.toJSON = function() {
                return ListValue.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ListValue
             * @function getTypeUrl
             * @memberof google.protobuf.ListValue
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ListValue.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.ListValue";
            };

            return ListValue;
        })();

        return protobuf;
    })();

    return google;
})();

export const northstar = $root.northstar = (() => {

    /**
     * Namespace northstar.
     * @exports northstar
     * @namespace
     */
    const northstar = {};

    northstar.live = (function() {

        /**
         * Namespace live.
         * @memberof northstar
         * @namespace
         */
        const live = {};

        live.AccountCatchupRequest = (function() {

            /**
             * Properties of an AccountCatchupRequest.
             * @typedef {Object} northstar.live.AccountCatchupRequest.$Properties
             * @property {string|null} [baseline_id] AccountCatchupRequest baseline_id
             * @property {string|null} [request_id] AccountCatchupRequest request_id
             * @property {number|Long|null} [through_sequence] AccountCatchupRequest through_sequence
             * @property {"baseline_id"} [_baseline_id] AccountCatchupRequest _baseline_id
             * @property {"request_id"} [_request_id] AccountCatchupRequest _request_id
             * @property {"through_sequence"} [_through_sequence] AccountCatchupRequest _through_sequence
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an AccountCatchupRequest.
             * @memberof northstar.live
             * @interface IAccountCatchupRequest
             * @augments northstar.live.AccountCatchupRequest.$Properties
             * @deprecated Use northstar.live.AccountCatchupRequest.$Properties instead.
             */

            /**
             * Narrowed shape of an AccountCatchupRequest.
             * @typedef {{
             *   baseline_id?: string|null;
             *   request_id?: string|null;
             *   through_sequence?: number|Long|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _baseline_id?: undefined; baseline_id?: null }|{ _baseline_id?: "baseline_id"; baseline_id: string })
             * ) & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * ) & (
             *   ({ _through_sequence?: undefined; through_sequence?: null }|{ _through_sequence?: "through_sequence"; through_sequence: number|Long })
             * )} northstar.live.AccountCatchupRequest.$Shape
             */

            /**
             * Constructs a new AccountCatchupRequest.
             * @memberof northstar.live
             * @classdesc Represents an AccountCatchupRequest.
             * @constructor
             * @param {northstar.live.AccountCatchupRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const AccountCatchupRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * AccountCatchupRequest baseline_id.
             * @member {string|null|undefined} baseline_id
             * @memberof northstar.live.AccountCatchupRequest
             * @instance
             */
            AccountCatchupRequest.prototype.baseline_id = null;

            /**
             * AccountCatchupRequest request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.live.AccountCatchupRequest
             * @instance
             */
            AccountCatchupRequest.prototype.request_id = null;

            /**
             * AccountCatchupRequest through_sequence.
             * @member {number|Long|null|undefined} through_sequence
             * @memberof northstar.live.AccountCatchupRequest
             * @instance
             */
            AccountCatchupRequest.prototype.through_sequence = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * AccountCatchupRequest _baseline_id.
             * @member {"baseline_id"|undefined} _baseline_id
             * @memberof northstar.live.AccountCatchupRequest
             * @instance
             */
            $Object.defineProperty(AccountCatchupRequest.prototype, "_baseline_id", {
                get: $util.oneOfGetter($oneOfFields = ["baseline_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * AccountCatchupRequest _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.live.AccountCatchupRequest
             * @instance
             */
            $Object.defineProperty(AccountCatchupRequest.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * AccountCatchupRequest _through_sequence.
             * @member {"through_sequence"|undefined} _through_sequence
             * @memberof northstar.live.AccountCatchupRequest
             * @instance
             */
            $Object.defineProperty(AccountCatchupRequest.prototype, "_through_sequence", {
                get: $util.oneOfGetter($oneOfFields = ["through_sequence"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AccountCatchupRequest instance using the specified properties.
             * @function create
             * @memberof northstar.live.AccountCatchupRequest
             * @static
             * @param {northstar.live.AccountCatchupRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.live.AccountCatchupRequest} AccountCatchupRequest instance
             * @type {{
             *   (properties: northstar.live.AccountCatchupRequest.$Shape): northstar.live.AccountCatchupRequest & northstar.live.AccountCatchupRequest.$Shape;
             *   (properties?: northstar.live.AccountCatchupRequest.$Properties): northstar.live.AccountCatchupRequest;
             * }}
             */
            AccountCatchupRequest.create = function(properties) {
                return new AccountCatchupRequest(properties);
            };

            /**
             * Encodes the specified AccountCatchupRequest message. Does not implicitly {@link northstar.live.AccountCatchupRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.AccountCatchupRequest
             * @static
             * @param {northstar.live.AccountCatchupRequest.$Properties} message AccountCatchupRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AccountCatchupRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.baseline_id);
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.request_id);
                if (message.through_sequence != null && $Object.hasOwnProperty.call(message, "through_sequence"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.through_sequence);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes an AccountCatchupRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.AccountCatchupRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.AccountCatchupRequest & northstar.live.AccountCatchupRequest.$Shape} AccountCatchupRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AccountCatchupRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.AccountCatchupRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.baseline_id = reader.stringVerify();
                            message._baseline_id = "baseline_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            message.through_sequence = reader.int64();
                            message._through_sequence = "through_sequence";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies an AccountCatchupRequest message.
             * @function verify
             * @memberof northstar.live.AccountCatchupRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AccountCatchupRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id")) {
                    properties._baseline_id = 1;
                    if (!$util.isString(message.baseline_id))
                        return "baseline_id: string expected";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                if (message.through_sequence != null && $Object.hasOwnProperty.call(message, "through_sequence")) {
                    properties._through_sequence = 1;
                    if (!$util.isInteger(message.through_sequence) && !(message.through_sequence && $util.isInteger(message.through_sequence.low) && $util.isInteger(message.through_sequence.high)))
                        return "through_sequence: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates an AccountCatchupRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.AccountCatchupRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.AccountCatchupRequest} AccountCatchupRequest
             */
            AccountCatchupRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.AccountCatchupRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.AccountCatchupRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.AccountCatchupRequest();
                if (object.baseline_id != null)
                    message.baseline_id = $String(object.baseline_id);
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                if (object.through_sequence != null)
                    if ($util.Long)
                        message.through_sequence = $util.Long.fromValue(object.through_sequence, false);
                    else if (typeof object.through_sequence === "string")
                        message.through_sequence = $parseInt(object.through_sequence, 10);
                    else if (typeof object.through_sequence === "number")
                        message.through_sequence = object.through_sequence;
                    else if (typeof object.through_sequence === "object")
                        message.through_sequence = new $util.LongBits(object.through_sequence.low >>> 0, object.through_sequence.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from an AccountCatchupRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.AccountCatchupRequest
             * @static
             * @param {northstar.live.AccountCatchupRequest} message AccountCatchupRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AccountCatchupRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id")) {
                    object.baseline_id = message.baseline_id;
                    if (options.oneofs)
                        object._baseline_id = "baseline_id";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                if (message.through_sequence != null && $Object.hasOwnProperty.call(message, "through_sequence")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.through_sequence = typeof message.through_sequence === "number" ? $BigInt(message.through_sequence) : $util.Long.fromBits(message.through_sequence.low >>> 0, message.through_sequence.high >>> 0, false).toBigInt();
                    else if (typeof message.through_sequence === "number")
                        object.through_sequence = options.longs === $String ? $String(message.through_sequence) : message.through_sequence;
                    else
                        object.through_sequence = options.longs === $String ? $util.Long.prototype.toString.call(message.through_sequence) : options.longs === $Number ? new $util.LongBits(message.through_sequence.low >>> 0, message.through_sequence.high >>> 0).toNumber() : message.through_sequence;
                    if (options.oneofs)
                        object._through_sequence = "through_sequence";
                }
                return object;
            };

            /**
             * Converts this AccountCatchupRequest to JSON.
             * @function toJSON
             * @memberof northstar.live.AccountCatchupRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AccountCatchupRequest.prototype.toJSON = function() {
                return AccountCatchupRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for AccountCatchupRequest
             * @function getTypeUrl
             * @memberof northstar.live.AccountCatchupRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            AccountCatchupRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.AccountCatchupRequest";
            };

            return AccountCatchupRequest;
        })();

        live.AccountProgress = (function() {

            /**
             * Properties of an AccountProgress.
             * @typedef {Object} northstar.live.AccountProgress.$Properties
             * @property {string|null} [status] AccountProgress status
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] AccountProgress evidence_fields
             * @property {"status"} [_status] AccountProgress _status
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an AccountProgress.
             * @memberof northstar.live
             * @interface IAccountProgress
             * @augments northstar.live.AccountProgress.$Properties
             * @deprecated Use northstar.live.AccountProgress.$Properties instead.
             */

            /**
             * Narrowed shape of an AccountProgress.
             * @typedef {{
             *   status?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _status?: undefined; status?: null }|{ _status?: "status"; status: string })
             * )} northstar.live.AccountProgress.$Shape
             */

            /**
             * Constructs a new AccountProgress.
             * @memberof northstar.live
             * @classdesc Represents an AccountProgress.
             * @constructor
             * @param {northstar.live.AccountProgress.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const AccountProgress = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * AccountProgress status.
             * @member {string|null|undefined} status
             * @memberof northstar.live.AccountProgress
             * @instance
             */
            AccountProgress.prototype.status = null;

            /**
             * AccountProgress evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.AccountProgress
             * @instance
             */
            AccountProgress.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * AccountProgress _status.
             * @member {"status"|undefined} _status
             * @memberof northstar.live.AccountProgress
             * @instance
             */
            $Object.defineProperty(AccountProgress.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AccountProgress instance using the specified properties.
             * @function create
             * @memberof northstar.live.AccountProgress
             * @static
             * @param {northstar.live.AccountProgress.$Properties=} [properties] Properties to set
             * @returns {northstar.live.AccountProgress} AccountProgress instance
             * @type {{
             *   (properties: northstar.live.AccountProgress.$Shape): northstar.live.AccountProgress & northstar.live.AccountProgress.$Shape;
             *   (properties?: northstar.live.AccountProgress.$Properties): northstar.live.AccountProgress;
             * }}
             */
            AccountProgress.create = function(properties) {
                return new AccountProgress(properties);
            };

            /**
             * Encodes the specified AccountProgress message. Does not implicitly {@link northstar.live.AccountProgress.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.AccountProgress
             * @static
             * @param {northstar.live.AccountProgress.$Properties} message AccountProgress message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AccountProgress.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes an AccountProgress message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.AccountProgress
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.AccountProgress & northstar.live.AccountProgress.$Shape} AccountProgress
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AccountProgress.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.AccountProgress();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.status = reader.stringVerify();
                            message._status = "status";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies an AccountProgress message.
             * @function verify
             * @memberof northstar.live.AccountProgress
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AccountProgress.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (!$util.isString(message.status))
                        return "status: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an AccountProgress message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.AccountProgress
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.AccountProgress} AccountProgress
             */
            AccountProgress.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.AccountProgress)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.AccountProgress: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.AccountProgress();
                if (object.status != null)
                    message.status = $String(object.status);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.AccountProgress.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.AccountProgress.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from an AccountProgress message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.AccountProgress
             * @static
             * @param {northstar.live.AccountProgress} message AccountProgress
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AccountProgress.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    object.status = message.status;
                    if (options.oneofs)
                        object._status = "status";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this AccountProgress to JSON.
             * @function toJSON
             * @memberof northstar.live.AccountProgress
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AccountProgress.prototype.toJSON = function() {
                return AccountProgress.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for AccountProgress
             * @function getTypeUrl
             * @memberof northstar.live.AccountProgress
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            AccountProgress.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.AccountProgress";
            };

            return AccountProgress;
        })();

        live.ArchiveAttempt = (function() {

            /**
             * Properties of an ArchiveAttempt.
             * @typedef {Object} northstar.live.ArchiveAttempt.$Properties
             * @property {string|null} [attempt_id] ArchiveAttempt attempt_id
             * @property {google.protobuf.Struct.$Properties|null} [parameters] ArchiveAttempt parameters
             * @property {string|null} [snapshot_id] ArchiveAttempt snapshot_id
             * @property {string|null} [source_id] ArchiveAttempt source_id
             * @property {string|null} [status] ArchiveAttempt status
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] ArchiveAttempt evidence_fields
             * @property {Array.<string>|null} [null_fields] ArchiveAttempt null_fields
             * @property {"attempt_id"} [_attempt_id] ArchiveAttempt _attempt_id
             * @property {"parameters"} [_parameters] ArchiveAttempt _parameters
             * @property {"snapshot_id"} [_snapshot_id] ArchiveAttempt _snapshot_id
             * @property {"source_id"} [_source_id] ArchiveAttempt _source_id
             * @property {"status"} [_status] ArchiveAttempt _status
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an ArchiveAttempt.
             * @memberof northstar.live
             * @interface IArchiveAttempt
             * @augments northstar.live.ArchiveAttempt.$Properties
             * @deprecated Use northstar.live.ArchiveAttempt.$Properties instead.
             */

            /**
             * Narrowed shape of an ArchiveAttempt.
             * @typedef {{
             *   attempt_id?: string|null;
             *   parameters?: google.protobuf.Struct.$Shape|null;
             *   snapshot_id?: string|null;
             *   source_id?: string|null;
             *   status?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   null_fields?: Array.<string>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _attempt_id?: undefined; attempt_id?: null }|{ _attempt_id?: "attempt_id"; attempt_id: string })
             * ) & (
             *   ({ _parameters?: undefined; parameters?: null }|{ _parameters?: "parameters"; parameters: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _snapshot_id?: undefined; snapshot_id?: null }|{ _snapshot_id?: "snapshot_id"; snapshot_id: string })
             * ) & (
             *   ({ _source_id?: undefined; source_id?: null }|{ _source_id?: "source_id"; source_id: string })
             * ) & (
             *   ({ _status?: undefined; status?: null }|{ _status?: "status"; status: string })
             * )} northstar.live.ArchiveAttempt.$Shape
             */

            /**
             * Constructs a new ArchiveAttempt.
             * @memberof northstar.live
             * @classdesc Represents an ArchiveAttempt.
             * @constructor
             * @param {northstar.live.ArchiveAttempt.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ArchiveAttempt = function (properties) {
                this.evidence_fields = {};
                this.null_fields = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ArchiveAttempt attempt_id.
             * @member {string|null|undefined} attempt_id
             * @memberof northstar.live.ArchiveAttempt
             * @instance
             */
            ArchiveAttempt.prototype.attempt_id = null;

            /**
             * ArchiveAttempt parameters.
             * @member {google.protobuf.Struct.$Properties|null|undefined} parameters
             * @memberof northstar.live.ArchiveAttempt
             * @instance
             */
            ArchiveAttempt.prototype.parameters = null;

            /**
             * ArchiveAttempt snapshot_id.
             * @member {string|null|undefined} snapshot_id
             * @memberof northstar.live.ArchiveAttempt
             * @instance
             */
            ArchiveAttempt.prototype.snapshot_id = null;

            /**
             * ArchiveAttempt source_id.
             * @member {string|null|undefined} source_id
             * @memberof northstar.live.ArchiveAttempt
             * @instance
             */
            ArchiveAttempt.prototype.source_id = null;

            /**
             * ArchiveAttempt status.
             * @member {string|null|undefined} status
             * @memberof northstar.live.ArchiveAttempt
             * @instance
             */
            ArchiveAttempt.prototype.status = null;

            /**
             * ArchiveAttempt evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.ArchiveAttempt
             * @instance
             */
            ArchiveAttempt.prototype.evidence_fields = $util.emptyObject;

            /**
             * ArchiveAttempt null_fields.
             * @member {Array.<string>} null_fields
             * @memberof northstar.live.ArchiveAttempt
             * @instance
             */
            ArchiveAttempt.prototype.null_fields = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * ArchiveAttempt _attempt_id.
             * @member {"attempt_id"|undefined} _attempt_id
             * @memberof northstar.live.ArchiveAttempt
             * @instance
             */
            $Object.defineProperty(ArchiveAttempt.prototype, "_attempt_id", {
                get: $util.oneOfGetter($oneOfFields = ["attempt_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveAttempt _parameters.
             * @member {"parameters"|undefined} _parameters
             * @memberof northstar.live.ArchiveAttempt
             * @instance
             */
            $Object.defineProperty(ArchiveAttempt.prototype, "_parameters", {
                get: $util.oneOfGetter($oneOfFields = ["parameters"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveAttempt _snapshot_id.
             * @member {"snapshot_id"|undefined} _snapshot_id
             * @memberof northstar.live.ArchiveAttempt
             * @instance
             */
            $Object.defineProperty(ArchiveAttempt.prototype, "_snapshot_id", {
                get: $util.oneOfGetter($oneOfFields = ["snapshot_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveAttempt _source_id.
             * @member {"source_id"|undefined} _source_id
             * @memberof northstar.live.ArchiveAttempt
             * @instance
             */
            $Object.defineProperty(ArchiveAttempt.prototype, "_source_id", {
                get: $util.oneOfGetter($oneOfFields = ["source_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveAttempt _status.
             * @member {"status"|undefined} _status
             * @memberof northstar.live.ArchiveAttempt
             * @instance
             */
            $Object.defineProperty(ArchiveAttempt.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ArchiveAttempt instance using the specified properties.
             * @function create
             * @memberof northstar.live.ArchiveAttempt
             * @static
             * @param {northstar.live.ArchiveAttempt.$Properties=} [properties] Properties to set
             * @returns {northstar.live.ArchiveAttempt} ArchiveAttempt instance
             * @type {{
             *   (properties: northstar.live.ArchiveAttempt.$Shape): northstar.live.ArchiveAttempt & northstar.live.ArchiveAttempt.$Shape;
             *   (properties?: northstar.live.ArchiveAttempt.$Properties): northstar.live.ArchiveAttempt;
             * }}
             */
            ArchiveAttempt.create = function(properties) {
                return new ArchiveAttempt(properties);
            };

            /**
             * Encodes the specified ArchiveAttempt message. Does not implicitly {@link northstar.live.ArchiveAttempt.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.ArchiveAttempt
             * @static
             * @param {northstar.live.ArchiveAttempt.$Properties} message ArchiveAttempt message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ArchiveAttempt.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.attempt_id != null && $Object.hasOwnProperty.call(message, "attempt_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.attempt_id);
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters"))
                    $root.google.protobuf.Struct.encode(message.parameters, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.snapshot_id);
                if (message.source_id != null && $Object.hasOwnProperty.call(message, "source_id"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.source_id);
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.status);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.null_fields != null && message.null_fields.length)
                    for (let i = 0; i < message.null_fields.length; ++i)
                        writer.uint32(/* id 2046, wireType 2 =*/16370).string(message.null_fields[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes an ArchiveAttempt message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.ArchiveAttempt
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.ArchiveAttempt & northstar.live.ArchiveAttempt.$Shape} ArchiveAttempt
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ArchiveAttempt.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.ArchiveAttempt();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.attempt_id = reader.stringVerify();
                            message._attempt_id = "attempt_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.parameters = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.parameters);
                            message._parameters = "parameters";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.snapshot_id = reader.stringVerify();
                            message._snapshot_id = "snapshot_id";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.source_id = reader.stringVerify();
                            message._source_id = "source_id";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.status = reader.stringVerify();
                            message._status = "status";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    case 2046: {
                            if (wireType !== 2)
                                break;
                            if (!(message.null_fields && message.null_fields.length))
                                message.null_fields = [];
                            message.null_fields.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies an ArchiveAttempt message.
             * @function verify
             * @memberof northstar.live.ArchiveAttempt
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ArchiveAttempt.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.attempt_id != null && $Object.hasOwnProperty.call(message, "attempt_id")) {
                    properties._attempt_id = 1;
                    if (!$util.isString(message.attempt_id))
                        return "attempt_id: string expected";
                }
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters")) {
                    properties._parameters = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.parameters, _depth + 1);
                        if (error)
                            return "parameters." + error;
                    }
                }
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id")) {
                    properties._snapshot_id = 1;
                    if (!$util.isString(message.snapshot_id))
                        return "snapshot_id: string expected";
                }
                if (message.source_id != null && $Object.hasOwnProperty.call(message, "source_id")) {
                    properties._source_id = 1;
                    if (!$util.isString(message.source_id))
                        return "source_id: string expected";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (!$util.isString(message.status))
                        return "status: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                if (message.null_fields != null && $Object.hasOwnProperty.call(message, "null_fields")) {
                    if (!$Array.isArray(message.null_fields))
                        return "null_fields: array expected";
                    for (let i = 0; i < message.null_fields.length; ++i)
                        if (!$util.isString(message.null_fields[i]))
                            return "null_fields: string[] expected";
                }
                return null;
            };

            /**
             * Creates an ArchiveAttempt message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.ArchiveAttempt
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.ArchiveAttempt} ArchiveAttempt
             */
            ArchiveAttempt.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.ArchiveAttempt)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.ArchiveAttempt: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.ArchiveAttempt();
                if (object.attempt_id != null)
                    message.attempt_id = $String(object.attempt_id);
                if (object.parameters != null) {
                    if (!$util.isObject(object.parameters))
                        throw $TypeError(".northstar.live.ArchiveAttempt.parameters: object expected");
                    message.parameters = $root.google.protobuf.Struct.fromObject(object.parameters, _depth + 1);
                }
                if (object.snapshot_id != null)
                    message.snapshot_id = $String(object.snapshot_id);
                if (object.source_id != null)
                    message.source_id = $String(object.source_id);
                if (object.status != null)
                    message.status = $String(object.status);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.ArchiveAttempt.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.ArchiveAttempt.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                if (object.null_fields) {
                    if (!$Array.isArray(object.null_fields))
                        throw $TypeError(".northstar.live.ArchiveAttempt.null_fields: array expected");
                    message.null_fields = $Array(object.null_fields.length);
                    for (let i = 0; i < object.null_fields.length; ++i)
                        message.null_fields[i] = $String(object.null_fields[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from an ArchiveAttempt message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.ArchiveAttempt
             * @static
             * @param {northstar.live.ArchiveAttempt} message ArchiveAttempt
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ArchiveAttempt.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.null_fields = [];
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.attempt_id != null && $Object.hasOwnProperty.call(message, "attempt_id")) {
                    object.attempt_id = message.attempt_id;
                    if (options.oneofs)
                        object._attempt_id = "attempt_id";
                }
                if (message.parameters != null && $Object.hasOwnProperty.call(message, "parameters")) {
                    object.parameters = $root.google.protobuf.Struct.toObject(message.parameters, options, _depth + 1);
                    if (options.oneofs)
                        object._parameters = "parameters";
                }
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id")) {
                    object.snapshot_id = message.snapshot_id;
                    if (options.oneofs)
                        object._snapshot_id = "snapshot_id";
                }
                if (message.source_id != null && $Object.hasOwnProperty.call(message, "source_id")) {
                    object.source_id = message.source_id;
                    if (options.oneofs)
                        object._source_id = "source_id";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    object.status = message.status;
                    if (options.oneofs)
                        object._status = "status";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                if (message.null_fields && message.null_fields.length) {
                    object.null_fields = $Array(message.null_fields.length);
                    for (let j = 0; j < message.null_fields.length; ++j)
                        object.null_fields[j] = message.null_fields[j];
                }
                return object;
            };

            /**
             * Converts this ArchiveAttempt to JSON.
             * @function toJSON
             * @memberof northstar.live.ArchiveAttempt
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ArchiveAttempt.prototype.toJSON = function() {
                return ArchiveAttempt.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ArchiveAttempt
             * @function getTypeUrl
             * @memberof northstar.live.ArchiveAttempt
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ArchiveAttempt.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.ArchiveAttempt";
            };

            return ArchiveAttempt;
        })();

        live.ArchiveDataset = (function() {

            /**
             * Properties of an ArchiveDataset.
             * @typedef {Object} northstar.live.ArchiveDataset.$Properties
             * @property {string|null} [availability_basis] ArchiveDataset availability_basis
             * @property {string|null} [availability_note] ArchiveDataset availability_note
             * @property {number|Long|null} [bar_count] ArchiveDataset bar_count
             * @property {string|null} [content_hash] ArchiveDataset content_hash
             * @property {string|null} [exchange] ArchiveDataset exchange
             * @property {northstar.live.ImportSpecification.$Properties|null} [import_spec] ArchiveDataset import_spec
             * @property {Array.<string>|null} [limitations] ArchiveDataset limitations
             * @property {google.protobuf.Struct.$Properties|null} [live_runtime] ArchiveDataset live_runtime
             * @property {google.protobuf.Struct.$Properties|null} [processing_provenance] ArchiveDataset processing_provenance
             * @property {string|null} [product] ArchiveDataset product
             * @property {string|null} [published_at] ArchiveDataset published_at
             * @property {google.protobuf.Struct.$Properties|null} [quality] ArchiveDataset quality
             * @property {google.protobuf.Struct.$Properties|null} [semantics] ArchiveDataset semantics
             * @property {string|null} [session_close] ArchiveDataset session_close
             * @property {string|null} [session_open] ArchiveDataset session_open
             * @property {string|null} [snapshot_id] ArchiveDataset snapshot_id
             * @property {string|null} [source_reference] ArchiveDataset source_reference
             * @property {Array.<google.protobuf.Struct.$Properties>|null} [sources] ArchiveDataset sources
             * @property {string|null} [symbol] ArchiveDataset symbol
             * @property {string|null} [trading_day] ArchiveDataset trading_day
             * @property {Array.<string>|null} [null_fields] ArchiveDataset null_fields
             * @property {"availability_basis"} [_availability_basis] ArchiveDataset _availability_basis
             * @property {"availability_note"} [_availability_note] ArchiveDataset _availability_note
             * @property {"bar_count"} [_bar_count] ArchiveDataset _bar_count
             * @property {"content_hash"} [_content_hash] ArchiveDataset _content_hash
             * @property {"exchange"} [_exchange] ArchiveDataset _exchange
             * @property {"import_spec"} [_import_spec] ArchiveDataset _import_spec
             * @property {"live_runtime"} [_live_runtime] ArchiveDataset _live_runtime
             * @property {"processing_provenance"} [_processing_provenance] ArchiveDataset _processing_provenance
             * @property {"product"} [_product] ArchiveDataset _product
             * @property {"published_at"} [_published_at] ArchiveDataset _published_at
             * @property {"quality"} [_quality] ArchiveDataset _quality
             * @property {"semantics"} [_semantics] ArchiveDataset _semantics
             * @property {"session_close"} [_session_close] ArchiveDataset _session_close
             * @property {"session_open"} [_session_open] ArchiveDataset _session_open
             * @property {"snapshot_id"} [_snapshot_id] ArchiveDataset _snapshot_id
             * @property {"source_reference"} [_source_reference] ArchiveDataset _source_reference
             * @property {"symbol"} [_symbol] ArchiveDataset _symbol
             * @property {"trading_day"} [_trading_day] ArchiveDataset _trading_day
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an ArchiveDataset.
             * @memberof northstar.live
             * @interface IArchiveDataset
             * @augments northstar.live.ArchiveDataset.$Properties
             * @deprecated Use northstar.live.ArchiveDataset.$Properties instead.
             */

            /**
             * Narrowed shape of an ArchiveDataset.
             * @typedef {{
             *   availability_basis?: string|null;
             *   availability_note?: string|null;
             *   bar_count?: number|Long|null;
             *   content_hash?: string|null;
             *   exchange?: string|null;
             *   import_spec?: northstar.live.ImportSpecification.$Shape|null;
             *   limitations?: Array.<string>|null;
             *   live_runtime?: google.protobuf.Struct.$Shape|null;
             *   processing_provenance?: google.protobuf.Struct.$Shape|null;
             *   product?: string|null;
             *   published_at?: string|null;
             *   quality?: google.protobuf.Struct.$Shape|null;
             *   semantics?: google.protobuf.Struct.$Shape|null;
             *   session_close?: string|null;
             *   session_open?: string|null;
             *   snapshot_id?: string|null;
             *   source_reference?: string|null;
             *   sources?: Array.<google.protobuf.Struct.$Shape>|null;
             *   symbol?: string|null;
             *   trading_day?: string|null;
             *   null_fields?: Array.<string>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _availability_basis?: undefined; availability_basis?: null }|{ _availability_basis?: "availability_basis"; availability_basis: string })
             * ) & (
             *   ({ _availability_note?: undefined; availability_note?: null }|{ _availability_note?: "availability_note"; availability_note: string })
             * ) & (
             *   ({ _bar_count?: undefined; bar_count?: null }|{ _bar_count?: "bar_count"; bar_count: number|Long })
             * ) & (
             *   ({ _content_hash?: undefined; content_hash?: null }|{ _content_hash?: "content_hash"; content_hash: string })
             * ) & (
             *   ({ _exchange?: undefined; exchange?: null }|{ _exchange?: "exchange"; exchange: string })
             * ) & (
             *   ({ _import_spec?: undefined; import_spec?: null }|{ _import_spec?: "import_spec"; import_spec: northstar.live.ImportSpecification.$Shape })
             * ) & (
             *   ({ _live_runtime?: undefined; live_runtime?: null }|{ _live_runtime?: "live_runtime"; live_runtime: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _processing_provenance?: undefined; processing_provenance?: null }|{ _processing_provenance?: "processing_provenance"; processing_provenance: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _product?: undefined; product?: null }|{ _product?: "product"; product: string })
             * ) & (
             *   ({ _published_at?: undefined; published_at?: null }|{ _published_at?: "published_at"; published_at: string })
             * ) & (
             *   ({ _quality?: undefined; quality?: null }|{ _quality?: "quality"; quality: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _semantics?: undefined; semantics?: null }|{ _semantics?: "semantics"; semantics: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _session_close?: undefined; session_close?: null }|{ _session_close?: "session_close"; session_close: string })
             * ) & (
             *   ({ _session_open?: undefined; session_open?: null }|{ _session_open?: "session_open"; session_open: string })
             * ) & (
             *   ({ _snapshot_id?: undefined; snapshot_id?: null }|{ _snapshot_id?: "snapshot_id"; snapshot_id: string })
             * ) & (
             *   ({ _source_reference?: undefined; source_reference?: null }|{ _source_reference?: "source_reference"; source_reference: string })
             * ) & (
             *   ({ _symbol?: undefined; symbol?: null }|{ _symbol?: "symbol"; symbol: string })
             * ) & (
             *   ({ _trading_day?: undefined; trading_day?: null }|{ _trading_day?: "trading_day"; trading_day: string })
             * )} northstar.live.ArchiveDataset.$Shape
             */

            /**
             * Constructs a new ArchiveDataset.
             * @memberof northstar.live
             * @classdesc Represents an ArchiveDataset.
             * @constructor
             * @param {northstar.live.ArchiveDataset.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ArchiveDataset = function (properties) {
                this.limitations = [];
                this.sources = [];
                this.null_fields = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ArchiveDataset availability_basis.
             * @member {string|null|undefined} availability_basis
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.availability_basis = null;

            /**
             * ArchiveDataset availability_note.
             * @member {string|null|undefined} availability_note
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.availability_note = null;

            /**
             * ArchiveDataset bar_count.
             * @member {number|Long|null|undefined} bar_count
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.bar_count = null;

            /**
             * ArchiveDataset content_hash.
             * @member {string|null|undefined} content_hash
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.content_hash = null;

            /**
             * ArchiveDataset exchange.
             * @member {string|null|undefined} exchange
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.exchange = null;

            /**
             * ArchiveDataset import_spec.
             * @member {northstar.live.ImportSpecification.$Properties|null|undefined} import_spec
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.import_spec = null;

            /**
             * ArchiveDataset limitations.
             * @member {Array.<string>} limitations
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.limitations = $util.emptyArray;

            /**
             * ArchiveDataset live_runtime.
             * @member {google.protobuf.Struct.$Properties|null|undefined} live_runtime
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.live_runtime = null;

            /**
             * ArchiveDataset processing_provenance.
             * @member {google.protobuf.Struct.$Properties|null|undefined} processing_provenance
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.processing_provenance = null;

            /**
             * ArchiveDataset product.
             * @member {string|null|undefined} product
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.product = null;

            /**
             * ArchiveDataset published_at.
             * @member {string|null|undefined} published_at
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.published_at = null;

            /**
             * ArchiveDataset quality.
             * @member {google.protobuf.Struct.$Properties|null|undefined} quality
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.quality = null;

            /**
             * ArchiveDataset semantics.
             * @member {google.protobuf.Struct.$Properties|null|undefined} semantics
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.semantics = null;

            /**
             * ArchiveDataset session_close.
             * @member {string|null|undefined} session_close
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.session_close = null;

            /**
             * ArchiveDataset session_open.
             * @member {string|null|undefined} session_open
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.session_open = null;

            /**
             * ArchiveDataset snapshot_id.
             * @member {string|null|undefined} snapshot_id
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.snapshot_id = null;

            /**
             * ArchiveDataset source_reference.
             * @member {string|null|undefined} source_reference
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.source_reference = null;

            /**
             * ArchiveDataset sources.
             * @member {Array.<google.protobuf.Struct.$Properties>} sources
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.sources = $util.emptyArray;

            /**
             * ArchiveDataset symbol.
             * @member {string|null|undefined} symbol
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.symbol = null;

            /**
             * ArchiveDataset trading_day.
             * @member {string|null|undefined} trading_day
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.trading_day = null;

            /**
             * ArchiveDataset null_fields.
             * @member {Array.<string>} null_fields
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            ArchiveDataset.prototype.null_fields = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * ArchiveDataset _availability_basis.
             * @member {"availability_basis"|undefined} _availability_basis
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            $Object.defineProperty(ArchiveDataset.prototype, "_availability_basis", {
                get: $util.oneOfGetter($oneOfFields = ["availability_basis"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveDataset _availability_note.
             * @member {"availability_note"|undefined} _availability_note
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            $Object.defineProperty(ArchiveDataset.prototype, "_availability_note", {
                get: $util.oneOfGetter($oneOfFields = ["availability_note"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveDataset _bar_count.
             * @member {"bar_count"|undefined} _bar_count
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            $Object.defineProperty(ArchiveDataset.prototype, "_bar_count", {
                get: $util.oneOfGetter($oneOfFields = ["bar_count"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveDataset _content_hash.
             * @member {"content_hash"|undefined} _content_hash
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            $Object.defineProperty(ArchiveDataset.prototype, "_content_hash", {
                get: $util.oneOfGetter($oneOfFields = ["content_hash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveDataset _exchange.
             * @member {"exchange"|undefined} _exchange
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            $Object.defineProperty(ArchiveDataset.prototype, "_exchange", {
                get: $util.oneOfGetter($oneOfFields = ["exchange"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveDataset _import_spec.
             * @member {"import_spec"|undefined} _import_spec
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            $Object.defineProperty(ArchiveDataset.prototype, "_import_spec", {
                get: $util.oneOfGetter($oneOfFields = ["import_spec"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveDataset _live_runtime.
             * @member {"live_runtime"|undefined} _live_runtime
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            $Object.defineProperty(ArchiveDataset.prototype, "_live_runtime", {
                get: $util.oneOfGetter($oneOfFields = ["live_runtime"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveDataset _processing_provenance.
             * @member {"processing_provenance"|undefined} _processing_provenance
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            $Object.defineProperty(ArchiveDataset.prototype, "_processing_provenance", {
                get: $util.oneOfGetter($oneOfFields = ["processing_provenance"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveDataset _product.
             * @member {"product"|undefined} _product
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            $Object.defineProperty(ArchiveDataset.prototype, "_product", {
                get: $util.oneOfGetter($oneOfFields = ["product"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveDataset _published_at.
             * @member {"published_at"|undefined} _published_at
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            $Object.defineProperty(ArchiveDataset.prototype, "_published_at", {
                get: $util.oneOfGetter($oneOfFields = ["published_at"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveDataset _quality.
             * @member {"quality"|undefined} _quality
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            $Object.defineProperty(ArchiveDataset.prototype, "_quality", {
                get: $util.oneOfGetter($oneOfFields = ["quality"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveDataset _semantics.
             * @member {"semantics"|undefined} _semantics
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            $Object.defineProperty(ArchiveDataset.prototype, "_semantics", {
                get: $util.oneOfGetter($oneOfFields = ["semantics"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveDataset _session_close.
             * @member {"session_close"|undefined} _session_close
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            $Object.defineProperty(ArchiveDataset.prototype, "_session_close", {
                get: $util.oneOfGetter($oneOfFields = ["session_close"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveDataset _session_open.
             * @member {"session_open"|undefined} _session_open
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            $Object.defineProperty(ArchiveDataset.prototype, "_session_open", {
                get: $util.oneOfGetter($oneOfFields = ["session_open"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveDataset _snapshot_id.
             * @member {"snapshot_id"|undefined} _snapshot_id
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            $Object.defineProperty(ArchiveDataset.prototype, "_snapshot_id", {
                get: $util.oneOfGetter($oneOfFields = ["snapshot_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveDataset _source_reference.
             * @member {"source_reference"|undefined} _source_reference
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            $Object.defineProperty(ArchiveDataset.prototype, "_source_reference", {
                get: $util.oneOfGetter($oneOfFields = ["source_reference"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveDataset _symbol.
             * @member {"symbol"|undefined} _symbol
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            $Object.defineProperty(ArchiveDataset.prototype, "_symbol", {
                get: $util.oneOfGetter($oneOfFields = ["symbol"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveDataset _trading_day.
             * @member {"trading_day"|undefined} _trading_day
             * @memberof northstar.live.ArchiveDataset
             * @instance
             */
            $Object.defineProperty(ArchiveDataset.prototype, "_trading_day", {
                get: $util.oneOfGetter($oneOfFields = ["trading_day"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ArchiveDataset instance using the specified properties.
             * @function create
             * @memberof northstar.live.ArchiveDataset
             * @static
             * @param {northstar.live.ArchiveDataset.$Properties=} [properties] Properties to set
             * @returns {northstar.live.ArchiveDataset} ArchiveDataset instance
             * @type {{
             *   (properties: northstar.live.ArchiveDataset.$Shape): northstar.live.ArchiveDataset & northstar.live.ArchiveDataset.$Shape;
             *   (properties?: northstar.live.ArchiveDataset.$Properties): northstar.live.ArchiveDataset;
             * }}
             */
            ArchiveDataset.create = function(properties) {
                return new ArchiveDataset(properties);
            };

            /**
             * Encodes the specified ArchiveDataset message. Does not implicitly {@link northstar.live.ArchiveDataset.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.ArchiveDataset
             * @static
             * @param {northstar.live.ArchiveDataset.$Properties} message ArchiveDataset message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ArchiveDataset.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.availability_basis != null && $Object.hasOwnProperty.call(message, "availability_basis"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.availability_basis);
                if (message.availability_note != null && $Object.hasOwnProperty.call(message, "availability_note"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.availability_note);
                if (message.bar_count != null && $Object.hasOwnProperty.call(message, "bar_count"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.bar_count);
                if (message.content_hash != null && $Object.hasOwnProperty.call(message, "content_hash"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.content_hash);
                if (message.exchange != null && $Object.hasOwnProperty.call(message, "exchange"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.exchange);
                if (message.import_spec != null && $Object.hasOwnProperty.call(message, "import_spec"))
                    $root.northstar.live.ImportSpecification.encode(message.import_spec, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
                if (message.limitations != null && message.limitations.length)
                    for (let i = 0; i < message.limitations.length; ++i)
                        writer.uint32(/* id 7, wireType 2 =*/58).string(message.limitations[i]);
                if (message.live_runtime != null && $Object.hasOwnProperty.call(message, "live_runtime"))
                    $root.google.protobuf.Struct.encode(message.live_runtime, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
                if (message.processing_provenance != null && $Object.hasOwnProperty.call(message, "processing_provenance"))
                    $root.google.protobuf.Struct.encode(message.processing_provenance, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
                if (message.product != null && $Object.hasOwnProperty.call(message, "product"))
                    writer.uint32(/* id 10, wireType 2 =*/82).string(message.product);
                if (message.published_at != null && $Object.hasOwnProperty.call(message, "published_at"))
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.published_at);
                if (message.quality != null && $Object.hasOwnProperty.call(message, "quality"))
                    $root.google.protobuf.Struct.encode(message.quality, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
                if (message.semantics != null && $Object.hasOwnProperty.call(message, "semantics"))
                    $root.google.protobuf.Struct.encode(message.semantics, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
                if (message.session_close != null && $Object.hasOwnProperty.call(message, "session_close"))
                    writer.uint32(/* id 14, wireType 2 =*/114).string(message.session_close);
                if (message.session_open != null && $Object.hasOwnProperty.call(message, "session_open"))
                    writer.uint32(/* id 15, wireType 2 =*/122).string(message.session_open);
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id"))
                    writer.uint32(/* id 16, wireType 2 =*/130).string(message.snapshot_id);
                if (message.source_reference != null && $Object.hasOwnProperty.call(message, "source_reference"))
                    writer.uint32(/* id 17, wireType 2 =*/138).string(message.source_reference);
                if (message.sources != null && message.sources.length)
                    for (let i = 0; i < message.sources.length; ++i)
                        $root.google.protobuf.Struct.encode(message.sources[i], writer.uint32(/* id 18, wireType 2 =*/146).fork(), _depth + 1).ldelim();
                if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol"))
                    writer.uint32(/* id 19, wireType 2 =*/154).string(message.symbol);
                if (message.trading_day != null && $Object.hasOwnProperty.call(message, "trading_day"))
                    writer.uint32(/* id 20, wireType 2 =*/162).string(message.trading_day);
                if (message.null_fields != null && message.null_fields.length)
                    for (let i = 0; i < message.null_fields.length; ++i)
                        writer.uint32(/* id 2046, wireType 2 =*/16370).string(message.null_fields[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes an ArchiveDataset message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.ArchiveDataset
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.ArchiveDataset & northstar.live.ArchiveDataset.$Shape} ArchiveDataset
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ArchiveDataset.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.ArchiveDataset();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.availability_basis = reader.stringVerify();
                            message._availability_basis = "availability_basis";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.availability_note = reader.stringVerify();
                            message._availability_note = "availability_note";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            message.bar_count = reader.int64();
                            message._bar_count = "bar_count";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.content_hash = reader.stringVerify();
                            message._content_hash = "content_hash";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.exchange = reader.stringVerify();
                            message._exchange = "exchange";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.import_spec = $root.northstar.live.ImportSpecification.decode(reader, reader.uint32(), $undefined, _depth + 1, message.import_spec);
                            message._import_spec = "import_spec";
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            if (!(message.limitations && message.limitations.length))
                                message.limitations = [];
                            message.limitations.push(reader.stringVerify());
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            message.live_runtime = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.live_runtime);
                            message._live_runtime = "live_runtime";
                            continue;
                        }
                    case 9: {
                            if (wireType !== 2)
                                break;
                            message.processing_provenance = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.processing_provenance);
                            message._processing_provenance = "processing_provenance";
                            continue;
                        }
                    case 10: {
                            if (wireType !== 2)
                                break;
                            message.product = reader.stringVerify();
                            message._product = "product";
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            message.published_at = reader.stringVerify();
                            message._published_at = "published_at";
                            continue;
                        }
                    case 12: {
                            if (wireType !== 2)
                                break;
                            message.quality = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.quality);
                            message._quality = "quality";
                            continue;
                        }
                    case 13: {
                            if (wireType !== 2)
                                break;
                            message.semantics = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.semantics);
                            message._semantics = "semantics";
                            continue;
                        }
                    case 14: {
                            if (wireType !== 2)
                                break;
                            message.session_close = reader.stringVerify();
                            message._session_close = "session_close";
                            continue;
                        }
                    case 15: {
                            if (wireType !== 2)
                                break;
                            message.session_open = reader.stringVerify();
                            message._session_open = "session_open";
                            continue;
                        }
                    case 16: {
                            if (wireType !== 2)
                                break;
                            message.snapshot_id = reader.stringVerify();
                            message._snapshot_id = "snapshot_id";
                            continue;
                        }
                    case 17: {
                            if (wireType !== 2)
                                break;
                            message.source_reference = reader.stringVerify();
                            message._source_reference = "source_reference";
                            continue;
                        }
                    case 18: {
                            if (wireType !== 2)
                                break;
                            if (!(message.sources && message.sources.length))
                                message.sources = [];
                            message.sources.push($root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 19: {
                            if (wireType !== 2)
                                break;
                            message.symbol = reader.stringVerify();
                            message._symbol = "symbol";
                            continue;
                        }
                    case 20: {
                            if (wireType !== 2)
                                break;
                            message.trading_day = reader.stringVerify();
                            message._trading_day = "trading_day";
                            continue;
                        }
                    case 2046: {
                            if (wireType !== 2)
                                break;
                            if (!(message.null_fields && message.null_fields.length))
                                message.null_fields = [];
                            message.null_fields.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies an ArchiveDataset message.
             * @function verify
             * @memberof northstar.live.ArchiveDataset
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ArchiveDataset.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.availability_basis != null && $Object.hasOwnProperty.call(message, "availability_basis")) {
                    properties._availability_basis = 1;
                    if (!$util.isString(message.availability_basis))
                        return "availability_basis: string expected";
                }
                if (message.availability_note != null && $Object.hasOwnProperty.call(message, "availability_note")) {
                    properties._availability_note = 1;
                    if (!$util.isString(message.availability_note))
                        return "availability_note: string expected";
                }
                if (message.bar_count != null && $Object.hasOwnProperty.call(message, "bar_count")) {
                    properties._bar_count = 1;
                    if (!$util.isInteger(message.bar_count) && !(message.bar_count && $util.isInteger(message.bar_count.low) && $util.isInteger(message.bar_count.high)))
                        return "bar_count: integer|Long expected";
                }
                if (message.content_hash != null && $Object.hasOwnProperty.call(message, "content_hash")) {
                    properties._content_hash = 1;
                    if (!$util.isString(message.content_hash))
                        return "content_hash: string expected";
                }
                if (message.exchange != null && $Object.hasOwnProperty.call(message, "exchange")) {
                    properties._exchange = 1;
                    if (!$util.isString(message.exchange))
                        return "exchange: string expected";
                }
                if (message.import_spec != null && $Object.hasOwnProperty.call(message, "import_spec")) {
                    properties._import_spec = 1;
                    {
                        let error = $root.northstar.live.ImportSpecification.verify(message.import_spec, _depth + 1);
                        if (error)
                            return "import_spec." + error;
                    }
                }
                if (message.limitations != null && $Object.hasOwnProperty.call(message, "limitations")) {
                    if (!$Array.isArray(message.limitations))
                        return "limitations: array expected";
                    for (let i = 0; i < message.limitations.length; ++i)
                        if (!$util.isString(message.limitations[i]))
                            return "limitations: string[] expected";
                }
                if (message.live_runtime != null && $Object.hasOwnProperty.call(message, "live_runtime")) {
                    properties._live_runtime = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.live_runtime, _depth + 1);
                        if (error)
                            return "live_runtime." + error;
                    }
                }
                if (message.processing_provenance != null && $Object.hasOwnProperty.call(message, "processing_provenance")) {
                    properties._processing_provenance = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.processing_provenance, _depth + 1);
                        if (error)
                            return "processing_provenance." + error;
                    }
                }
                if (message.product != null && $Object.hasOwnProperty.call(message, "product")) {
                    properties._product = 1;
                    if (!$util.isString(message.product))
                        return "product: string expected";
                }
                if (message.published_at != null && $Object.hasOwnProperty.call(message, "published_at")) {
                    properties._published_at = 1;
                    if (!$util.isString(message.published_at))
                        return "published_at: string expected";
                }
                if (message.quality != null && $Object.hasOwnProperty.call(message, "quality")) {
                    properties._quality = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.quality, _depth + 1);
                        if (error)
                            return "quality." + error;
                    }
                }
                if (message.semantics != null && $Object.hasOwnProperty.call(message, "semantics")) {
                    properties._semantics = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.semantics, _depth + 1);
                        if (error)
                            return "semantics." + error;
                    }
                }
                if (message.session_close != null && $Object.hasOwnProperty.call(message, "session_close")) {
                    properties._session_close = 1;
                    if (!$util.isString(message.session_close))
                        return "session_close: string expected";
                }
                if (message.session_open != null && $Object.hasOwnProperty.call(message, "session_open")) {
                    properties._session_open = 1;
                    if (!$util.isString(message.session_open))
                        return "session_open: string expected";
                }
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id")) {
                    properties._snapshot_id = 1;
                    if (!$util.isString(message.snapshot_id))
                        return "snapshot_id: string expected";
                }
                if (message.source_reference != null && $Object.hasOwnProperty.call(message, "source_reference")) {
                    properties._source_reference = 1;
                    if (!$util.isString(message.source_reference))
                        return "source_reference: string expected";
                }
                if (message.sources != null && $Object.hasOwnProperty.call(message, "sources")) {
                    if (!$Array.isArray(message.sources))
                        return "sources: array expected";
                    for (let i = 0; i < message.sources.length; ++i) {
                        let error = $root.google.protobuf.Struct.verify(message.sources[i], _depth + 1);
                        if (error)
                            return "sources." + error;
                    }
                }
                if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol")) {
                    properties._symbol = 1;
                    if (!$util.isString(message.symbol))
                        return "symbol: string expected";
                }
                if (message.trading_day != null && $Object.hasOwnProperty.call(message, "trading_day")) {
                    properties._trading_day = 1;
                    if (!$util.isString(message.trading_day))
                        return "trading_day: string expected";
                }
                if (message.null_fields != null && $Object.hasOwnProperty.call(message, "null_fields")) {
                    if (!$Array.isArray(message.null_fields))
                        return "null_fields: array expected";
                    for (let i = 0; i < message.null_fields.length; ++i)
                        if (!$util.isString(message.null_fields[i]))
                            return "null_fields: string[] expected";
                }
                return null;
            };

            /**
             * Creates an ArchiveDataset message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.ArchiveDataset
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.ArchiveDataset} ArchiveDataset
             */
            ArchiveDataset.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.ArchiveDataset)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.ArchiveDataset: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.ArchiveDataset();
                if (object.availability_basis != null)
                    message.availability_basis = $String(object.availability_basis);
                if (object.availability_note != null)
                    message.availability_note = $String(object.availability_note);
                if (object.bar_count != null)
                    if ($util.Long)
                        message.bar_count = $util.Long.fromValue(object.bar_count, false);
                    else if (typeof object.bar_count === "string")
                        message.bar_count = $parseInt(object.bar_count, 10);
                    else if (typeof object.bar_count === "number")
                        message.bar_count = object.bar_count;
                    else if (typeof object.bar_count === "object")
                        message.bar_count = new $util.LongBits(object.bar_count.low >>> 0, object.bar_count.high >>> 0).toNumber();
                if (object.content_hash != null)
                    message.content_hash = $String(object.content_hash);
                if (object.exchange != null)
                    message.exchange = $String(object.exchange);
                if (object.import_spec != null) {
                    if (!$util.isObject(object.import_spec))
                        throw $TypeError(".northstar.live.ArchiveDataset.import_spec: object expected");
                    message.import_spec = $root.northstar.live.ImportSpecification.fromObject(object.import_spec, _depth + 1);
                }
                if (object.limitations) {
                    if (!$Array.isArray(object.limitations))
                        throw $TypeError(".northstar.live.ArchiveDataset.limitations: array expected");
                    message.limitations = $Array(object.limitations.length);
                    for (let i = 0; i < object.limitations.length; ++i)
                        message.limitations[i] = $String(object.limitations[i]);
                }
                if (object.live_runtime != null) {
                    if (!$util.isObject(object.live_runtime))
                        throw $TypeError(".northstar.live.ArchiveDataset.live_runtime: object expected");
                    message.live_runtime = $root.google.protobuf.Struct.fromObject(object.live_runtime, _depth + 1);
                }
                if (object.processing_provenance != null) {
                    if (!$util.isObject(object.processing_provenance))
                        throw $TypeError(".northstar.live.ArchiveDataset.processing_provenance: object expected");
                    message.processing_provenance = $root.google.protobuf.Struct.fromObject(object.processing_provenance, _depth + 1);
                }
                if (object.product != null)
                    message.product = $String(object.product);
                if (object.published_at != null)
                    message.published_at = $String(object.published_at);
                if (object.quality != null) {
                    if (!$util.isObject(object.quality))
                        throw $TypeError(".northstar.live.ArchiveDataset.quality: object expected");
                    message.quality = $root.google.protobuf.Struct.fromObject(object.quality, _depth + 1);
                }
                if (object.semantics != null) {
                    if (!$util.isObject(object.semantics))
                        throw $TypeError(".northstar.live.ArchiveDataset.semantics: object expected");
                    message.semantics = $root.google.protobuf.Struct.fromObject(object.semantics, _depth + 1);
                }
                if (object.session_close != null)
                    message.session_close = $String(object.session_close);
                if (object.session_open != null)
                    message.session_open = $String(object.session_open);
                if (object.snapshot_id != null)
                    message.snapshot_id = $String(object.snapshot_id);
                if (object.source_reference != null)
                    message.source_reference = $String(object.source_reference);
                if (object.sources) {
                    if (!$Array.isArray(object.sources))
                        throw $TypeError(".northstar.live.ArchiveDataset.sources: array expected");
                    message.sources = $Array(object.sources.length);
                    for (let i = 0; i < object.sources.length; ++i) {
                        if (!$util.isObject(object.sources[i]))
                            throw $TypeError(".northstar.live.ArchiveDataset.sources: object expected");
                        message.sources[i] = $root.google.protobuf.Struct.fromObject(object.sources[i], _depth + 1);
                    }
                }
                if (object.symbol != null)
                    message.symbol = $String(object.symbol);
                if (object.trading_day != null)
                    message.trading_day = $String(object.trading_day);
                if (object.null_fields) {
                    if (!$Array.isArray(object.null_fields))
                        throw $TypeError(".northstar.live.ArchiveDataset.null_fields: array expected");
                    message.null_fields = $Array(object.null_fields.length);
                    for (let i = 0; i < object.null_fields.length; ++i)
                        message.null_fields[i] = $String(object.null_fields[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from an ArchiveDataset message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.ArchiveDataset
             * @static
             * @param {northstar.live.ArchiveDataset} message ArchiveDataset
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ArchiveDataset.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.limitations = [];
                    object.sources = [];
                    object.null_fields = [];
                }
                if (message.availability_basis != null && $Object.hasOwnProperty.call(message, "availability_basis")) {
                    object.availability_basis = message.availability_basis;
                    if (options.oneofs)
                        object._availability_basis = "availability_basis";
                }
                if (message.availability_note != null && $Object.hasOwnProperty.call(message, "availability_note")) {
                    object.availability_note = message.availability_note;
                    if (options.oneofs)
                        object._availability_note = "availability_note";
                }
                if (message.bar_count != null && $Object.hasOwnProperty.call(message, "bar_count")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.bar_count = typeof message.bar_count === "number" ? $BigInt(message.bar_count) : $util.Long.fromBits(message.bar_count.low >>> 0, message.bar_count.high >>> 0, false).toBigInt();
                    else if (typeof message.bar_count === "number")
                        object.bar_count = options.longs === $String ? $String(message.bar_count) : message.bar_count;
                    else
                        object.bar_count = options.longs === $String ? $util.Long.prototype.toString.call(message.bar_count) : options.longs === $Number ? new $util.LongBits(message.bar_count.low >>> 0, message.bar_count.high >>> 0).toNumber() : message.bar_count;
                    if (options.oneofs)
                        object._bar_count = "bar_count";
                }
                if (message.content_hash != null && $Object.hasOwnProperty.call(message, "content_hash")) {
                    object.content_hash = message.content_hash;
                    if (options.oneofs)
                        object._content_hash = "content_hash";
                }
                if (message.exchange != null && $Object.hasOwnProperty.call(message, "exchange")) {
                    object.exchange = message.exchange;
                    if (options.oneofs)
                        object._exchange = "exchange";
                }
                if (message.import_spec != null && $Object.hasOwnProperty.call(message, "import_spec")) {
                    object.import_spec = $root.northstar.live.ImportSpecification.toObject(message.import_spec, options, _depth + 1);
                    if (options.oneofs)
                        object._import_spec = "import_spec";
                }
                if (message.limitations && message.limitations.length) {
                    object.limitations = $Array(message.limitations.length);
                    for (let j = 0; j < message.limitations.length; ++j)
                        object.limitations[j] = message.limitations[j];
                }
                if (message.live_runtime != null && $Object.hasOwnProperty.call(message, "live_runtime")) {
                    object.live_runtime = $root.google.protobuf.Struct.toObject(message.live_runtime, options, _depth + 1);
                    if (options.oneofs)
                        object._live_runtime = "live_runtime";
                }
                if (message.processing_provenance != null && $Object.hasOwnProperty.call(message, "processing_provenance")) {
                    object.processing_provenance = $root.google.protobuf.Struct.toObject(message.processing_provenance, options, _depth + 1);
                    if (options.oneofs)
                        object._processing_provenance = "processing_provenance";
                }
                if (message.product != null && $Object.hasOwnProperty.call(message, "product")) {
                    object.product = message.product;
                    if (options.oneofs)
                        object._product = "product";
                }
                if (message.published_at != null && $Object.hasOwnProperty.call(message, "published_at")) {
                    object.published_at = message.published_at;
                    if (options.oneofs)
                        object._published_at = "published_at";
                }
                if (message.quality != null && $Object.hasOwnProperty.call(message, "quality")) {
                    object.quality = $root.google.protobuf.Struct.toObject(message.quality, options, _depth + 1);
                    if (options.oneofs)
                        object._quality = "quality";
                }
                if (message.semantics != null && $Object.hasOwnProperty.call(message, "semantics")) {
                    object.semantics = $root.google.protobuf.Struct.toObject(message.semantics, options, _depth + 1);
                    if (options.oneofs)
                        object._semantics = "semantics";
                }
                if (message.session_close != null && $Object.hasOwnProperty.call(message, "session_close")) {
                    object.session_close = message.session_close;
                    if (options.oneofs)
                        object._session_close = "session_close";
                }
                if (message.session_open != null && $Object.hasOwnProperty.call(message, "session_open")) {
                    object.session_open = message.session_open;
                    if (options.oneofs)
                        object._session_open = "session_open";
                }
                if (message.snapshot_id != null && $Object.hasOwnProperty.call(message, "snapshot_id")) {
                    object.snapshot_id = message.snapshot_id;
                    if (options.oneofs)
                        object._snapshot_id = "snapshot_id";
                }
                if (message.source_reference != null && $Object.hasOwnProperty.call(message, "source_reference")) {
                    object.source_reference = message.source_reference;
                    if (options.oneofs)
                        object._source_reference = "source_reference";
                }
                if (message.sources && message.sources.length) {
                    object.sources = $Array(message.sources.length);
                    for (let j = 0; j < message.sources.length; ++j)
                        object.sources[j] = $root.google.protobuf.Struct.toObject(message.sources[j], options, _depth + 1);
                }
                if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol")) {
                    object.symbol = message.symbol;
                    if (options.oneofs)
                        object._symbol = "symbol";
                }
                if (message.trading_day != null && $Object.hasOwnProperty.call(message, "trading_day")) {
                    object.trading_day = message.trading_day;
                    if (options.oneofs)
                        object._trading_day = "trading_day";
                }
                if (message.null_fields && message.null_fields.length) {
                    object.null_fields = $Array(message.null_fields.length);
                    for (let j = 0; j < message.null_fields.length; ++j)
                        object.null_fields[j] = message.null_fields[j];
                }
                return object;
            };

            /**
             * Converts this ArchiveDataset to JSON.
             * @function toJSON
             * @memberof northstar.live.ArchiveDataset
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ArchiveDataset.prototype.toJSON = function() {
                return ArchiveDataset.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ArchiveDataset
             * @function getTypeUrl
             * @memberof northstar.live.ArchiveDataset
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ArchiveDataset.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.ArchiveDataset";
            };

            return ArchiveDataset;
        })();

        live.ArchiveReprocessRequest = (function() {

            /**
             * Properties of an ArchiveReprocessRequest.
             * @typedef {Object} northstar.live.ArchiveReprocessRequest.$Properties
             * @property {string|null} [request_id] ArchiveReprocessRequest request_id
             * @property {google.protobuf.Struct.$Properties|null} [spec] ArchiveReprocessRequest spec
             * @property {"request_id"} [_request_id] ArchiveReprocessRequest _request_id
             * @property {"spec"} [_spec] ArchiveReprocessRequest _spec
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an ArchiveReprocessRequest.
             * @memberof northstar.live
             * @interface IArchiveReprocessRequest
             * @augments northstar.live.ArchiveReprocessRequest.$Properties
             * @deprecated Use northstar.live.ArchiveReprocessRequest.$Properties instead.
             */

            /**
             * Narrowed shape of an ArchiveReprocessRequest.
             * @typedef {{
             *   request_id?: string|null;
             *   spec?: google.protobuf.Struct.$Shape|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * ) & (
             *   ({ _spec?: undefined; spec?: null }|{ _spec?: "spec"; spec: google.protobuf.Struct.$Shape })
             * )} northstar.live.ArchiveReprocessRequest.$Shape
             */

            /**
             * Constructs a new ArchiveReprocessRequest.
             * @memberof northstar.live
             * @classdesc Represents an ArchiveReprocessRequest.
             * @constructor
             * @param {northstar.live.ArchiveReprocessRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ArchiveReprocessRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ArchiveReprocessRequest request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.live.ArchiveReprocessRequest
             * @instance
             */
            ArchiveReprocessRequest.prototype.request_id = null;

            /**
             * ArchiveReprocessRequest spec.
             * @member {google.protobuf.Struct.$Properties|null|undefined} spec
             * @memberof northstar.live.ArchiveReprocessRequest
             * @instance
             */
            ArchiveReprocessRequest.prototype.spec = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * ArchiveReprocessRequest _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.live.ArchiveReprocessRequest
             * @instance
             */
            $Object.defineProperty(ArchiveReprocessRequest.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveReprocessRequest _spec.
             * @member {"spec"|undefined} _spec
             * @memberof northstar.live.ArchiveReprocessRequest
             * @instance
             */
            $Object.defineProperty(ArchiveReprocessRequest.prototype, "_spec", {
                get: $util.oneOfGetter($oneOfFields = ["spec"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ArchiveReprocessRequest instance using the specified properties.
             * @function create
             * @memberof northstar.live.ArchiveReprocessRequest
             * @static
             * @param {northstar.live.ArchiveReprocessRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.live.ArchiveReprocessRequest} ArchiveReprocessRequest instance
             * @type {{
             *   (properties: northstar.live.ArchiveReprocessRequest.$Shape): northstar.live.ArchiveReprocessRequest & northstar.live.ArchiveReprocessRequest.$Shape;
             *   (properties?: northstar.live.ArchiveReprocessRequest.$Properties): northstar.live.ArchiveReprocessRequest;
             * }}
             */
            ArchiveReprocessRequest.create = function(properties) {
                return new ArchiveReprocessRequest(properties);
            };

            /**
             * Encodes the specified ArchiveReprocessRequest message. Does not implicitly {@link northstar.live.ArchiveReprocessRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.ArchiveReprocessRequest
             * @static
             * @param {northstar.live.ArchiveReprocessRequest.$Properties} message ArchiveReprocessRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ArchiveReprocessRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.request_id);
                if (message.spec != null && $Object.hasOwnProperty.call(message, "spec"))
                    $root.google.protobuf.Struct.encode(message.spec, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes an ArchiveReprocessRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.ArchiveReprocessRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.ArchiveReprocessRequest & northstar.live.ArchiveReprocessRequest.$Shape} ArchiveReprocessRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ArchiveReprocessRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.ArchiveReprocessRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.spec = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.spec);
                            message._spec = "spec";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies an ArchiveReprocessRequest message.
             * @function verify
             * @memberof northstar.live.ArchiveReprocessRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ArchiveReprocessRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                if (message.spec != null && $Object.hasOwnProperty.call(message, "spec")) {
                    properties._spec = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.spec, _depth + 1);
                        if (error)
                            return "spec." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an ArchiveReprocessRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.ArchiveReprocessRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.ArchiveReprocessRequest} ArchiveReprocessRequest
             */
            ArchiveReprocessRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.ArchiveReprocessRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.ArchiveReprocessRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.ArchiveReprocessRequest();
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                if (object.spec != null) {
                    if (!$util.isObject(object.spec))
                        throw $TypeError(".northstar.live.ArchiveReprocessRequest.spec: object expected");
                    message.spec = $root.google.protobuf.Struct.fromObject(object.spec, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from an ArchiveReprocessRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.ArchiveReprocessRequest
             * @static
             * @param {northstar.live.ArchiveReprocessRequest} message ArchiveReprocessRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ArchiveReprocessRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                if (message.spec != null && $Object.hasOwnProperty.call(message, "spec")) {
                    object.spec = $root.google.protobuf.Struct.toObject(message.spec, options, _depth + 1);
                    if (options.oneofs)
                        object._spec = "spec";
                }
                return object;
            };

            /**
             * Converts this ArchiveReprocessRequest to JSON.
             * @function toJSON
             * @memberof northstar.live.ArchiveReprocessRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ArchiveReprocessRequest.prototype.toJSON = function() {
                return ArchiveReprocessRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ArchiveReprocessRequest
             * @function getTypeUrl
             * @memberof northstar.live.ArchiveReprocessRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ArchiveReprocessRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.ArchiveReprocessRequest";
            };

            return ArchiveReprocessRequest;
        })();

        live.ArchiveRequest = (function() {

            /**
             * Properties of an ArchiveRequest.
             * @typedef {Object} northstar.live.ArchiveRequest.$Properties
             * @property {boolean|null} [allow_download] ArchiveRequest allow_download
             * @property {string|null} [request_id] ArchiveRequest request_id
             * @property {string|null} [session_close] ArchiveRequest session_close
             * @property {string|null} [session_open] ArchiveRequest session_open
             * @property {number|Long|null} [through_sequence] ArchiveRequest through_sequence
             * @property {"allow_download"} [_allow_download] ArchiveRequest _allow_download
             * @property {"request_id"} [_request_id] ArchiveRequest _request_id
             * @property {"session_close"} [_session_close] ArchiveRequest _session_close
             * @property {"session_open"} [_session_open] ArchiveRequest _session_open
             * @property {"through_sequence"} [_through_sequence] ArchiveRequest _through_sequence
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an ArchiveRequest.
             * @memberof northstar.live
             * @interface IArchiveRequest
             * @augments northstar.live.ArchiveRequest.$Properties
             * @deprecated Use northstar.live.ArchiveRequest.$Properties instead.
             */

            /**
             * Narrowed shape of an ArchiveRequest.
             * @typedef {{
             *   allow_download?: boolean|null;
             *   request_id?: string|null;
             *   session_close?: string|null;
             *   session_open?: string|null;
             *   through_sequence?: number|Long|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _allow_download?: undefined; allow_download?: null }|{ _allow_download?: "allow_download"; allow_download: boolean })
             * ) & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * ) & (
             *   ({ _session_close?: undefined; session_close?: null }|{ _session_close?: "session_close"; session_close: string })
             * ) & (
             *   ({ _session_open?: undefined; session_open?: null }|{ _session_open?: "session_open"; session_open: string })
             * ) & (
             *   ({ _through_sequence?: undefined; through_sequence?: null }|{ _through_sequence?: "through_sequence"; through_sequence: number|Long })
             * )} northstar.live.ArchiveRequest.$Shape
             */

            /**
             * Constructs a new ArchiveRequest.
             * @memberof northstar.live
             * @classdesc Represents an ArchiveRequest.
             * @constructor
             * @param {northstar.live.ArchiveRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ArchiveRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ArchiveRequest allow_download.
             * @member {boolean|null|undefined} allow_download
             * @memberof northstar.live.ArchiveRequest
             * @instance
             */
            ArchiveRequest.prototype.allow_download = null;

            /**
             * ArchiveRequest request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.live.ArchiveRequest
             * @instance
             */
            ArchiveRequest.prototype.request_id = null;

            /**
             * ArchiveRequest session_close.
             * @member {string|null|undefined} session_close
             * @memberof northstar.live.ArchiveRequest
             * @instance
             */
            ArchiveRequest.prototype.session_close = null;

            /**
             * ArchiveRequest session_open.
             * @member {string|null|undefined} session_open
             * @memberof northstar.live.ArchiveRequest
             * @instance
             */
            ArchiveRequest.prototype.session_open = null;

            /**
             * ArchiveRequest through_sequence.
             * @member {number|Long|null|undefined} through_sequence
             * @memberof northstar.live.ArchiveRequest
             * @instance
             */
            ArchiveRequest.prototype.through_sequence = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * ArchiveRequest _allow_download.
             * @member {"allow_download"|undefined} _allow_download
             * @memberof northstar.live.ArchiveRequest
             * @instance
             */
            $Object.defineProperty(ArchiveRequest.prototype, "_allow_download", {
                get: $util.oneOfGetter($oneOfFields = ["allow_download"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveRequest _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.live.ArchiveRequest
             * @instance
             */
            $Object.defineProperty(ArchiveRequest.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveRequest _session_close.
             * @member {"session_close"|undefined} _session_close
             * @memberof northstar.live.ArchiveRequest
             * @instance
             */
            $Object.defineProperty(ArchiveRequest.prototype, "_session_close", {
                get: $util.oneOfGetter($oneOfFields = ["session_close"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveRequest _session_open.
             * @member {"session_open"|undefined} _session_open
             * @memberof northstar.live.ArchiveRequest
             * @instance
             */
            $Object.defineProperty(ArchiveRequest.prototype, "_session_open", {
                get: $util.oneOfGetter($oneOfFields = ["session_open"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveRequest _through_sequence.
             * @member {"through_sequence"|undefined} _through_sequence
             * @memberof northstar.live.ArchiveRequest
             * @instance
             */
            $Object.defineProperty(ArchiveRequest.prototype, "_through_sequence", {
                get: $util.oneOfGetter($oneOfFields = ["through_sequence"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ArchiveRequest instance using the specified properties.
             * @function create
             * @memberof northstar.live.ArchiveRequest
             * @static
             * @param {northstar.live.ArchiveRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.live.ArchiveRequest} ArchiveRequest instance
             * @type {{
             *   (properties: northstar.live.ArchiveRequest.$Shape): northstar.live.ArchiveRequest & northstar.live.ArchiveRequest.$Shape;
             *   (properties?: northstar.live.ArchiveRequest.$Properties): northstar.live.ArchiveRequest;
             * }}
             */
            ArchiveRequest.create = function(properties) {
                return new ArchiveRequest(properties);
            };

            /**
             * Encodes the specified ArchiveRequest message. Does not implicitly {@link northstar.live.ArchiveRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.ArchiveRequest
             * @static
             * @param {northstar.live.ArchiveRequest.$Properties} message ArchiveRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ArchiveRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.allow_download != null && $Object.hasOwnProperty.call(message, "allow_download"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.allow_download);
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.request_id);
                if (message.session_close != null && $Object.hasOwnProperty.call(message, "session_close"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.session_close);
                if (message.session_open != null && $Object.hasOwnProperty.call(message, "session_open"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.session_open);
                if (message.through_sequence != null && $Object.hasOwnProperty.call(message, "through_sequence"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.through_sequence);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes an ArchiveRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.ArchiveRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.ArchiveRequest & northstar.live.ArchiveRequest.$Shape} ArchiveRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ArchiveRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.ArchiveRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            message.allow_download = reader.bool();
                            message._allow_download = "allow_download";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.session_close = reader.stringVerify();
                            message._session_close = "session_close";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.session_open = reader.stringVerify();
                            message._session_open = "session_open";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            message.through_sequence = reader.int64();
                            message._through_sequence = "through_sequence";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies an ArchiveRequest message.
             * @function verify
             * @memberof northstar.live.ArchiveRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ArchiveRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.allow_download != null && $Object.hasOwnProperty.call(message, "allow_download")) {
                    properties._allow_download = 1;
                    if (typeof message.allow_download !== "boolean")
                        return "allow_download: boolean expected";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                if (message.session_close != null && $Object.hasOwnProperty.call(message, "session_close")) {
                    properties._session_close = 1;
                    if (!$util.isString(message.session_close))
                        return "session_close: string expected";
                }
                if (message.session_open != null && $Object.hasOwnProperty.call(message, "session_open")) {
                    properties._session_open = 1;
                    if (!$util.isString(message.session_open))
                        return "session_open: string expected";
                }
                if (message.through_sequence != null && $Object.hasOwnProperty.call(message, "through_sequence")) {
                    properties._through_sequence = 1;
                    if (!$util.isInteger(message.through_sequence) && !(message.through_sequence && $util.isInteger(message.through_sequence.low) && $util.isInteger(message.through_sequence.high)))
                        return "through_sequence: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates an ArchiveRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.ArchiveRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.ArchiveRequest} ArchiveRequest
             */
            ArchiveRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.ArchiveRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.ArchiveRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.ArchiveRequest();
                if (object.allow_download != null)
                    message.allow_download = $Boolean(object.allow_download);
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                if (object.session_close != null)
                    message.session_close = $String(object.session_close);
                if (object.session_open != null)
                    message.session_open = $String(object.session_open);
                if (object.through_sequence != null)
                    if ($util.Long)
                        message.through_sequence = $util.Long.fromValue(object.through_sequence, false);
                    else if (typeof object.through_sequence === "string")
                        message.through_sequence = $parseInt(object.through_sequence, 10);
                    else if (typeof object.through_sequence === "number")
                        message.through_sequence = object.through_sequence;
                    else if (typeof object.through_sequence === "object")
                        message.through_sequence = new $util.LongBits(object.through_sequence.low >>> 0, object.through_sequence.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from an ArchiveRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.ArchiveRequest
             * @static
             * @param {northstar.live.ArchiveRequest} message ArchiveRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ArchiveRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.allow_download != null && $Object.hasOwnProperty.call(message, "allow_download")) {
                    object.allow_download = message.allow_download;
                    if (options.oneofs)
                        object._allow_download = "allow_download";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                if (message.session_close != null && $Object.hasOwnProperty.call(message, "session_close")) {
                    object.session_close = message.session_close;
                    if (options.oneofs)
                        object._session_close = "session_close";
                }
                if (message.session_open != null && $Object.hasOwnProperty.call(message, "session_open")) {
                    object.session_open = message.session_open;
                    if (options.oneofs)
                        object._session_open = "session_open";
                }
                if (message.through_sequence != null && $Object.hasOwnProperty.call(message, "through_sequence")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.through_sequence = typeof message.through_sequence === "number" ? $BigInt(message.through_sequence) : $util.Long.fromBits(message.through_sequence.low >>> 0, message.through_sequence.high >>> 0, false).toBigInt();
                    else if (typeof message.through_sequence === "number")
                        object.through_sequence = options.longs === $String ? $String(message.through_sequence) : message.through_sequence;
                    else
                        object.through_sequence = options.longs === $String ? $util.Long.prototype.toString.call(message.through_sequence) : options.longs === $Number ? new $util.LongBits(message.through_sequence.low >>> 0, message.through_sequence.high >>> 0).toNumber() : message.through_sequence;
                    if (options.oneofs)
                        object._through_sequence = "through_sequence";
                }
                return object;
            };

            /**
             * Converts this ArchiveRequest to JSON.
             * @function toJSON
             * @memberof northstar.live.ArchiveRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ArchiveRequest.prototype.toJSON = function() {
                return ArchiveRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ArchiveRequest
             * @function getTypeUrl
             * @memberof northstar.live.ArchiveRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ArchiveRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.ArchiveRequest";
            };

            return ArchiveRequest;
        })();

        live.ArchiveSource = (function() {

            /**
             * Properties of an ArchiveSource.
             * @typedef {Object} northstar.live.ArchiveSource.$Properties
             * @property {boolean|null} [allow_download] ArchiveSource allow_download
             * @property {string|null} [filename] ArchiveSource filename
             * @property {string|null} [source_id] ArchiveSource source_id
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] ArchiveSource evidence_fields
             * @property {"allow_download"} [_allow_download] ArchiveSource _allow_download
             * @property {"filename"} [_filename] ArchiveSource _filename
             * @property {"source_id"} [_source_id] ArchiveSource _source_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an ArchiveSource.
             * @memberof northstar.live
             * @interface IArchiveSource
             * @augments northstar.live.ArchiveSource.$Properties
             * @deprecated Use northstar.live.ArchiveSource.$Properties instead.
             */

            /**
             * Narrowed shape of an ArchiveSource.
             * @typedef {{
             *   allow_download?: boolean|null;
             *   filename?: string|null;
             *   source_id?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _allow_download?: undefined; allow_download?: null }|{ _allow_download?: "allow_download"; allow_download: boolean })
             * ) & (
             *   ({ _filename?: undefined; filename?: null }|{ _filename?: "filename"; filename: string })
             * ) & (
             *   ({ _source_id?: undefined; source_id?: null }|{ _source_id?: "source_id"; source_id: string })
             * )} northstar.live.ArchiveSource.$Shape
             */

            /**
             * Constructs a new ArchiveSource.
             * @memberof northstar.live
             * @classdesc Represents an ArchiveSource.
             * @constructor
             * @param {northstar.live.ArchiveSource.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ArchiveSource = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ArchiveSource allow_download.
             * @member {boolean|null|undefined} allow_download
             * @memberof northstar.live.ArchiveSource
             * @instance
             */
            ArchiveSource.prototype.allow_download = null;

            /**
             * ArchiveSource filename.
             * @member {string|null|undefined} filename
             * @memberof northstar.live.ArchiveSource
             * @instance
             */
            ArchiveSource.prototype.filename = null;

            /**
             * ArchiveSource source_id.
             * @member {string|null|undefined} source_id
             * @memberof northstar.live.ArchiveSource
             * @instance
             */
            ArchiveSource.prototype.source_id = null;

            /**
             * ArchiveSource evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.ArchiveSource
             * @instance
             */
            ArchiveSource.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * ArchiveSource _allow_download.
             * @member {"allow_download"|undefined} _allow_download
             * @memberof northstar.live.ArchiveSource
             * @instance
             */
            $Object.defineProperty(ArchiveSource.prototype, "_allow_download", {
                get: $util.oneOfGetter($oneOfFields = ["allow_download"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveSource _filename.
             * @member {"filename"|undefined} _filename
             * @memberof northstar.live.ArchiveSource
             * @instance
             */
            $Object.defineProperty(ArchiveSource.prototype, "_filename", {
                get: $util.oneOfGetter($oneOfFields = ["filename"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ArchiveSource _source_id.
             * @member {"source_id"|undefined} _source_id
             * @memberof northstar.live.ArchiveSource
             * @instance
             */
            $Object.defineProperty(ArchiveSource.prototype, "_source_id", {
                get: $util.oneOfGetter($oneOfFields = ["source_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ArchiveSource instance using the specified properties.
             * @function create
             * @memberof northstar.live.ArchiveSource
             * @static
             * @param {northstar.live.ArchiveSource.$Properties=} [properties] Properties to set
             * @returns {northstar.live.ArchiveSource} ArchiveSource instance
             * @type {{
             *   (properties: northstar.live.ArchiveSource.$Shape): northstar.live.ArchiveSource & northstar.live.ArchiveSource.$Shape;
             *   (properties?: northstar.live.ArchiveSource.$Properties): northstar.live.ArchiveSource;
             * }}
             */
            ArchiveSource.create = function(properties) {
                return new ArchiveSource(properties);
            };

            /**
             * Encodes the specified ArchiveSource message. Does not implicitly {@link northstar.live.ArchiveSource.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.ArchiveSource
             * @static
             * @param {northstar.live.ArchiveSource.$Properties} message ArchiveSource message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ArchiveSource.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.allow_download != null && $Object.hasOwnProperty.call(message, "allow_download"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.allow_download);
                if (message.filename != null && $Object.hasOwnProperty.call(message, "filename"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.filename);
                if (message.source_id != null && $Object.hasOwnProperty.call(message, "source_id"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.source_id);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes an ArchiveSource message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.ArchiveSource
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.ArchiveSource & northstar.live.ArchiveSource.$Shape} ArchiveSource
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ArchiveSource.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.ArchiveSource();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            message.allow_download = reader.bool();
                            message._allow_download = "allow_download";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.filename = reader.stringVerify();
                            message._filename = "filename";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.source_id = reader.stringVerify();
                            message._source_id = "source_id";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies an ArchiveSource message.
             * @function verify
             * @memberof northstar.live.ArchiveSource
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ArchiveSource.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.allow_download != null && $Object.hasOwnProperty.call(message, "allow_download")) {
                    properties._allow_download = 1;
                    if (typeof message.allow_download !== "boolean")
                        return "allow_download: boolean expected";
                }
                if (message.filename != null && $Object.hasOwnProperty.call(message, "filename")) {
                    properties._filename = 1;
                    if (!$util.isString(message.filename))
                        return "filename: string expected";
                }
                if (message.source_id != null && $Object.hasOwnProperty.call(message, "source_id")) {
                    properties._source_id = 1;
                    if (!$util.isString(message.source_id))
                        return "source_id: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an ArchiveSource message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.ArchiveSource
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.ArchiveSource} ArchiveSource
             */
            ArchiveSource.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.ArchiveSource)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.ArchiveSource: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.ArchiveSource();
                if (object.allow_download != null)
                    message.allow_download = $Boolean(object.allow_download);
                if (object.filename != null)
                    message.filename = $String(object.filename);
                if (object.source_id != null)
                    message.source_id = $String(object.source_id);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.ArchiveSource.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.ArchiveSource.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from an ArchiveSource message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.ArchiveSource
             * @static
             * @param {northstar.live.ArchiveSource} message ArchiveSource
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ArchiveSource.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.allow_download != null && $Object.hasOwnProperty.call(message, "allow_download")) {
                    object.allow_download = message.allow_download;
                    if (options.oneofs)
                        object._allow_download = "allow_download";
                }
                if (message.filename != null && $Object.hasOwnProperty.call(message, "filename")) {
                    object.filename = message.filename;
                    if (options.oneofs)
                        object._filename = "filename";
                }
                if (message.source_id != null && $Object.hasOwnProperty.call(message, "source_id")) {
                    object.source_id = message.source_id;
                    if (options.oneofs)
                        object._source_id = "source_id";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this ArchiveSource to JSON.
             * @function toJSON
             * @memberof northstar.live.ArchiveSource
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ArchiveSource.prototype.toJSON = function() {
                return ArchiveSource.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ArchiveSource
             * @function getTypeUrl
             * @memberof northstar.live.ArchiveSource
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ArchiveSource.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.ArchiveSource";
            };

            return ArchiveSource;
        })();

        live.BaselineCheckRequest = (function() {

            /**
             * Properties of a BaselineCheckRequest.
             * @typedef {Object} northstar.live.BaselineCheckRequest.$Properties
             * @property {string|null} [baseline_id] BaselineCheckRequest baseline_id
             * @property {string|null} [query_batch_id] BaselineCheckRequest query_batch_id
             * @property {string|null} [request_id] BaselineCheckRequest request_id
             * @property {"baseline_id"} [_baseline_id] BaselineCheckRequest _baseline_id
             * @property {"query_batch_id"} [_query_batch_id] BaselineCheckRequest _query_batch_id
             * @property {"request_id"} [_request_id] BaselineCheckRequest _request_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a BaselineCheckRequest.
             * @memberof northstar.live
             * @interface IBaselineCheckRequest
             * @augments northstar.live.BaselineCheckRequest.$Properties
             * @deprecated Use northstar.live.BaselineCheckRequest.$Properties instead.
             */

            /**
             * Narrowed shape of a BaselineCheckRequest.
             * @typedef {{
             *   baseline_id?: string|null;
             *   query_batch_id?: string|null;
             *   request_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _baseline_id?: undefined; baseline_id?: null }|{ _baseline_id?: "baseline_id"; baseline_id: string })
             * ) & (
             *   ({ _query_batch_id?: undefined; query_batch_id?: null }|{ _query_batch_id?: "query_batch_id"; query_batch_id: string })
             * ) & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * )} northstar.live.BaselineCheckRequest.$Shape
             */

            /**
             * Constructs a new BaselineCheckRequest.
             * @memberof northstar.live
             * @classdesc Represents a BaselineCheckRequest.
             * @constructor
             * @param {northstar.live.BaselineCheckRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const BaselineCheckRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * BaselineCheckRequest baseline_id.
             * @member {string|null|undefined} baseline_id
             * @memberof northstar.live.BaselineCheckRequest
             * @instance
             */
            BaselineCheckRequest.prototype.baseline_id = null;

            /**
             * BaselineCheckRequest query_batch_id.
             * @member {string|null|undefined} query_batch_id
             * @memberof northstar.live.BaselineCheckRequest
             * @instance
             */
            BaselineCheckRequest.prototype.query_batch_id = null;

            /**
             * BaselineCheckRequest request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.live.BaselineCheckRequest
             * @instance
             */
            BaselineCheckRequest.prototype.request_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * BaselineCheckRequest _baseline_id.
             * @member {"baseline_id"|undefined} _baseline_id
             * @memberof northstar.live.BaselineCheckRequest
             * @instance
             */
            $Object.defineProperty(BaselineCheckRequest.prototype, "_baseline_id", {
                get: $util.oneOfGetter($oneOfFields = ["baseline_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * BaselineCheckRequest _query_batch_id.
             * @member {"query_batch_id"|undefined} _query_batch_id
             * @memberof northstar.live.BaselineCheckRequest
             * @instance
             */
            $Object.defineProperty(BaselineCheckRequest.prototype, "_query_batch_id", {
                get: $util.oneOfGetter($oneOfFields = ["query_batch_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * BaselineCheckRequest _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.live.BaselineCheckRequest
             * @instance
             */
            $Object.defineProperty(BaselineCheckRequest.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BaselineCheckRequest instance using the specified properties.
             * @function create
             * @memberof northstar.live.BaselineCheckRequest
             * @static
             * @param {northstar.live.BaselineCheckRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.live.BaselineCheckRequest} BaselineCheckRequest instance
             * @type {{
             *   (properties: northstar.live.BaselineCheckRequest.$Shape): northstar.live.BaselineCheckRequest & northstar.live.BaselineCheckRequest.$Shape;
             *   (properties?: northstar.live.BaselineCheckRequest.$Properties): northstar.live.BaselineCheckRequest;
             * }}
             */
            BaselineCheckRequest.create = function(properties) {
                return new BaselineCheckRequest(properties);
            };

            /**
             * Encodes the specified BaselineCheckRequest message. Does not implicitly {@link northstar.live.BaselineCheckRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.BaselineCheckRequest
             * @static
             * @param {northstar.live.BaselineCheckRequest.$Properties} message BaselineCheckRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BaselineCheckRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.baseline_id);
                if (message.query_batch_id != null && $Object.hasOwnProperty.call(message, "query_batch_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.query_batch_id);
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.request_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a BaselineCheckRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.BaselineCheckRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.BaselineCheckRequest & northstar.live.BaselineCheckRequest.$Shape} BaselineCheckRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BaselineCheckRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.BaselineCheckRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.baseline_id = reader.stringVerify();
                            message._baseline_id = "baseline_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.query_batch_id = reader.stringVerify();
                            message._query_batch_id = "query_batch_id";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a BaselineCheckRequest message.
             * @function verify
             * @memberof northstar.live.BaselineCheckRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BaselineCheckRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id")) {
                    properties._baseline_id = 1;
                    if (!$util.isString(message.baseline_id))
                        return "baseline_id: string expected";
                }
                if (message.query_batch_id != null && $Object.hasOwnProperty.call(message, "query_batch_id")) {
                    properties._query_batch_id = 1;
                    if (!$util.isString(message.query_batch_id))
                        return "query_batch_id: string expected";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                return null;
            };

            /**
             * Creates a BaselineCheckRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.BaselineCheckRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.BaselineCheckRequest} BaselineCheckRequest
             */
            BaselineCheckRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.BaselineCheckRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.BaselineCheckRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.BaselineCheckRequest();
                if (object.baseline_id != null)
                    message.baseline_id = $String(object.baseline_id);
                if (object.query_batch_id != null)
                    message.query_batch_id = $String(object.query_batch_id);
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                return message;
            };

            /**
             * Creates a plain object from a BaselineCheckRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.BaselineCheckRequest
             * @static
             * @param {northstar.live.BaselineCheckRequest} message BaselineCheckRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BaselineCheckRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id")) {
                    object.baseline_id = message.baseline_id;
                    if (options.oneofs)
                        object._baseline_id = "baseline_id";
                }
                if (message.query_batch_id != null && $Object.hasOwnProperty.call(message, "query_batch_id")) {
                    object.query_batch_id = message.query_batch_id;
                    if (options.oneofs)
                        object._query_batch_id = "query_batch_id";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                return object;
            };

            /**
             * Converts this BaselineCheckRequest to JSON.
             * @function toJSON
             * @memberof northstar.live.BaselineCheckRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BaselineCheckRequest.prototype.toJSON = function() {
                return BaselineCheckRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for BaselineCheckRequest
             * @function getTypeUrl
             * @memberof northstar.live.BaselineCheckRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            BaselineCheckRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.BaselineCheckRequest";
            };

            return BaselineCheckRequest;
        })();

        live.BaselineContext = (function() {

            /**
             * Properties of a BaselineContext.
             * @typedef {Object} northstar.live.BaselineContext.$Properties
             * @property {northstar.live.BaselineRecord.$Properties|null} [baseline] BaselineContext baseline
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] BaselineContext evidence_fields
             * @property {Array.<string>|null} [null_fields] BaselineContext null_fields
             * @property {"baseline"} [_baseline] BaselineContext _baseline
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a BaselineContext.
             * @memberof northstar.live
             * @interface IBaselineContext
             * @augments northstar.live.BaselineContext.$Properties
             * @deprecated Use northstar.live.BaselineContext.$Properties instead.
             */

            /**
             * Narrowed shape of a BaselineContext.
             * @typedef {{
             *   baseline?: northstar.live.BaselineRecord.$Shape|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   null_fields?: Array.<string>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _baseline?: undefined; baseline?: null }|{ _baseline?: "baseline"; baseline: northstar.live.BaselineRecord.$Shape })
             * )} northstar.live.BaselineContext.$Shape
             */

            /**
             * Constructs a new BaselineContext.
             * @memberof northstar.live
             * @classdesc Represents a BaselineContext.
             * @constructor
             * @param {northstar.live.BaselineContext.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const BaselineContext = function (properties) {
                this.evidence_fields = {};
                this.null_fields = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * BaselineContext baseline.
             * @member {northstar.live.BaselineRecord.$Properties|null|undefined} baseline
             * @memberof northstar.live.BaselineContext
             * @instance
             */
            BaselineContext.prototype.baseline = null;

            /**
             * BaselineContext evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.BaselineContext
             * @instance
             */
            BaselineContext.prototype.evidence_fields = $util.emptyObject;

            /**
             * BaselineContext null_fields.
             * @member {Array.<string>} null_fields
             * @memberof northstar.live.BaselineContext
             * @instance
             */
            BaselineContext.prototype.null_fields = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * BaselineContext _baseline.
             * @member {"baseline"|undefined} _baseline
             * @memberof northstar.live.BaselineContext
             * @instance
             */
            $Object.defineProperty(BaselineContext.prototype, "_baseline", {
                get: $util.oneOfGetter($oneOfFields = ["baseline"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BaselineContext instance using the specified properties.
             * @function create
             * @memberof northstar.live.BaselineContext
             * @static
             * @param {northstar.live.BaselineContext.$Properties=} [properties] Properties to set
             * @returns {northstar.live.BaselineContext} BaselineContext instance
             * @type {{
             *   (properties: northstar.live.BaselineContext.$Shape): northstar.live.BaselineContext & northstar.live.BaselineContext.$Shape;
             *   (properties?: northstar.live.BaselineContext.$Properties): northstar.live.BaselineContext;
             * }}
             */
            BaselineContext.create = function(properties) {
                return new BaselineContext(properties);
            };

            /**
             * Encodes the specified BaselineContext message. Does not implicitly {@link northstar.live.BaselineContext.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.BaselineContext
             * @static
             * @param {northstar.live.BaselineContext.$Properties} message BaselineContext message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BaselineContext.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.baseline != null && $Object.hasOwnProperty.call(message, "baseline"))
                    $root.northstar.live.BaselineRecord.encode(message.baseline, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.null_fields != null && message.null_fields.length)
                    for (let i = 0; i < message.null_fields.length; ++i)
                        writer.uint32(/* id 2046, wireType 2 =*/16370).string(message.null_fields[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a BaselineContext message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.BaselineContext
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.BaselineContext & northstar.live.BaselineContext.$Shape} BaselineContext
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BaselineContext.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.BaselineContext();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.baseline = $root.northstar.live.BaselineRecord.decode(reader, reader.uint32(), $undefined, _depth + 1, message.baseline);
                            message._baseline = "baseline";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    case 2046: {
                            if (wireType !== 2)
                                break;
                            if (!(message.null_fields && message.null_fields.length))
                                message.null_fields = [];
                            message.null_fields.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a BaselineContext message.
             * @function verify
             * @memberof northstar.live.BaselineContext
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BaselineContext.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.baseline != null && $Object.hasOwnProperty.call(message, "baseline")) {
                    properties._baseline = 1;
                    {
                        let error = $root.northstar.live.BaselineRecord.verify(message.baseline, _depth + 1);
                        if (error)
                            return "baseline." + error;
                    }
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                if (message.null_fields != null && $Object.hasOwnProperty.call(message, "null_fields")) {
                    if (!$Array.isArray(message.null_fields))
                        return "null_fields: array expected";
                    for (let i = 0; i < message.null_fields.length; ++i)
                        if (!$util.isString(message.null_fields[i]))
                            return "null_fields: string[] expected";
                }
                return null;
            };

            /**
             * Creates a BaselineContext message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.BaselineContext
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.BaselineContext} BaselineContext
             */
            BaselineContext.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.BaselineContext)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.BaselineContext: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.BaselineContext();
                if (object.baseline != null) {
                    if (!$util.isObject(object.baseline))
                        throw $TypeError(".northstar.live.BaselineContext.baseline: object expected");
                    message.baseline = $root.northstar.live.BaselineRecord.fromObject(object.baseline, _depth + 1);
                }
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.BaselineContext.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.BaselineContext.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                if (object.null_fields) {
                    if (!$Array.isArray(object.null_fields))
                        throw $TypeError(".northstar.live.BaselineContext.null_fields: array expected");
                    message.null_fields = $Array(object.null_fields.length);
                    for (let i = 0; i < object.null_fields.length; ++i)
                        message.null_fields[i] = $String(object.null_fields[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a BaselineContext message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.BaselineContext
             * @static
             * @param {northstar.live.BaselineContext} message BaselineContext
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BaselineContext.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.null_fields = [];
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.baseline != null && $Object.hasOwnProperty.call(message, "baseline")) {
                    object.baseline = $root.northstar.live.BaselineRecord.toObject(message.baseline, options, _depth + 1);
                    if (options.oneofs)
                        object._baseline = "baseline";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                if (message.null_fields && message.null_fields.length) {
                    object.null_fields = $Array(message.null_fields.length);
                    for (let j = 0; j < message.null_fields.length; ++j)
                        object.null_fields[j] = message.null_fields[j];
                }
                return object;
            };

            /**
             * Converts this BaselineContext to JSON.
             * @function toJSON
             * @memberof northstar.live.BaselineContext
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BaselineContext.prototype.toJSON = function() {
                return BaselineContext.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for BaselineContext
             * @function getTypeUrl
             * @memberof northstar.live.BaselineContext
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            BaselineContext.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.BaselineContext";
            };

            return BaselineContext;
        })();

        live.BaselineRecord = (function() {

            /**
             * Properties of a BaselineRecord.
             * @typedef {Object} northstar.live.BaselineRecord.$Properties
             * @property {string|null} [baseline_id] BaselineRecord baseline_id
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] BaselineRecord evidence_fields
             * @property {"baseline_id"} [_baseline_id] BaselineRecord _baseline_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a BaselineRecord.
             * @memberof northstar.live
             * @interface IBaselineRecord
             * @augments northstar.live.BaselineRecord.$Properties
             * @deprecated Use northstar.live.BaselineRecord.$Properties instead.
             */

            /**
             * Narrowed shape of a BaselineRecord.
             * @typedef {{
             *   baseline_id?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _baseline_id?: undefined; baseline_id?: null }|{ _baseline_id?: "baseline_id"; baseline_id: string })
             * )} northstar.live.BaselineRecord.$Shape
             */

            /**
             * Constructs a new BaselineRecord.
             * @memberof northstar.live
             * @classdesc Represents a BaselineRecord.
             * @constructor
             * @param {northstar.live.BaselineRecord.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const BaselineRecord = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * BaselineRecord baseline_id.
             * @member {string|null|undefined} baseline_id
             * @memberof northstar.live.BaselineRecord
             * @instance
             */
            BaselineRecord.prototype.baseline_id = null;

            /**
             * BaselineRecord evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.BaselineRecord
             * @instance
             */
            BaselineRecord.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * BaselineRecord _baseline_id.
             * @member {"baseline_id"|undefined} _baseline_id
             * @memberof northstar.live.BaselineRecord
             * @instance
             */
            $Object.defineProperty(BaselineRecord.prototype, "_baseline_id", {
                get: $util.oneOfGetter($oneOfFields = ["baseline_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BaselineRecord instance using the specified properties.
             * @function create
             * @memberof northstar.live.BaselineRecord
             * @static
             * @param {northstar.live.BaselineRecord.$Properties=} [properties] Properties to set
             * @returns {northstar.live.BaselineRecord} BaselineRecord instance
             * @type {{
             *   (properties: northstar.live.BaselineRecord.$Shape): northstar.live.BaselineRecord & northstar.live.BaselineRecord.$Shape;
             *   (properties?: northstar.live.BaselineRecord.$Properties): northstar.live.BaselineRecord;
             * }}
             */
            BaselineRecord.create = function(properties) {
                return new BaselineRecord(properties);
            };

            /**
             * Encodes the specified BaselineRecord message. Does not implicitly {@link northstar.live.BaselineRecord.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.BaselineRecord
             * @static
             * @param {northstar.live.BaselineRecord.$Properties} message BaselineRecord message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BaselineRecord.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.baseline_id);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a BaselineRecord message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.BaselineRecord
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.BaselineRecord & northstar.live.BaselineRecord.$Shape} BaselineRecord
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BaselineRecord.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.BaselineRecord();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.baseline_id = reader.stringVerify();
                            message._baseline_id = "baseline_id";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a BaselineRecord message.
             * @function verify
             * @memberof northstar.live.BaselineRecord
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BaselineRecord.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id")) {
                    properties._baseline_id = 1;
                    if (!$util.isString(message.baseline_id))
                        return "baseline_id: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a BaselineRecord message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.BaselineRecord
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.BaselineRecord} BaselineRecord
             */
            BaselineRecord.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.BaselineRecord)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.BaselineRecord: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.BaselineRecord();
                if (object.baseline_id != null)
                    message.baseline_id = $String(object.baseline_id);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.BaselineRecord.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.BaselineRecord.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a BaselineRecord message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.BaselineRecord
             * @static
             * @param {northstar.live.BaselineRecord} message BaselineRecord
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BaselineRecord.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id")) {
                    object.baseline_id = message.baseline_id;
                    if (options.oneofs)
                        object._baseline_id = "baseline_id";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this BaselineRecord to JSON.
             * @function toJSON
             * @memberof northstar.live.BaselineRecord
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BaselineRecord.prototype.toJSON = function() {
                return BaselineRecord.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for BaselineRecord
             * @function getTypeUrl
             * @memberof northstar.live.BaselineRecord
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            BaselineRecord.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.BaselineRecord";
            };

            return BaselineRecord;
        })();

        live.BaselineRequest = (function() {

            /**
             * Properties of a BaselineRequest.
             * @typedef {Object} northstar.live.BaselineRequest.$Properties
             * @property {string|null} [request_id] BaselineRequest request_id
             * @property {string|null} [source_batch_id] BaselineRequest source_batch_id
             * @property {"request_id"} [_request_id] BaselineRequest _request_id
             * @property {"source_batch_id"} [_source_batch_id] BaselineRequest _source_batch_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a BaselineRequest.
             * @memberof northstar.live
             * @interface IBaselineRequest
             * @augments northstar.live.BaselineRequest.$Properties
             * @deprecated Use northstar.live.BaselineRequest.$Properties instead.
             */

            /**
             * Narrowed shape of a BaselineRequest.
             * @typedef {{
             *   request_id?: string|null;
             *   source_batch_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * ) & (
             *   ({ _source_batch_id?: undefined; source_batch_id?: null }|{ _source_batch_id?: "source_batch_id"; source_batch_id: string })
             * )} northstar.live.BaselineRequest.$Shape
             */

            /**
             * Constructs a new BaselineRequest.
             * @memberof northstar.live
             * @classdesc Represents a BaselineRequest.
             * @constructor
             * @param {northstar.live.BaselineRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const BaselineRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * BaselineRequest request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.live.BaselineRequest
             * @instance
             */
            BaselineRequest.prototype.request_id = null;

            /**
             * BaselineRequest source_batch_id.
             * @member {string|null|undefined} source_batch_id
             * @memberof northstar.live.BaselineRequest
             * @instance
             */
            BaselineRequest.prototype.source_batch_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * BaselineRequest _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.live.BaselineRequest
             * @instance
             */
            $Object.defineProperty(BaselineRequest.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * BaselineRequest _source_batch_id.
             * @member {"source_batch_id"|undefined} _source_batch_id
             * @memberof northstar.live.BaselineRequest
             * @instance
             */
            $Object.defineProperty(BaselineRequest.prototype, "_source_batch_id", {
                get: $util.oneOfGetter($oneOfFields = ["source_batch_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BaselineRequest instance using the specified properties.
             * @function create
             * @memberof northstar.live.BaselineRequest
             * @static
             * @param {northstar.live.BaselineRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.live.BaselineRequest} BaselineRequest instance
             * @type {{
             *   (properties: northstar.live.BaselineRequest.$Shape): northstar.live.BaselineRequest & northstar.live.BaselineRequest.$Shape;
             *   (properties?: northstar.live.BaselineRequest.$Properties): northstar.live.BaselineRequest;
             * }}
             */
            BaselineRequest.create = function(properties) {
                return new BaselineRequest(properties);
            };

            /**
             * Encodes the specified BaselineRequest message. Does not implicitly {@link northstar.live.BaselineRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.BaselineRequest
             * @static
             * @param {northstar.live.BaselineRequest.$Properties} message BaselineRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BaselineRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.request_id);
                if (message.source_batch_id != null && $Object.hasOwnProperty.call(message, "source_batch_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.source_batch_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a BaselineRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.BaselineRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.BaselineRequest & northstar.live.BaselineRequest.$Shape} BaselineRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BaselineRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.BaselineRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.source_batch_id = reader.stringVerify();
                            message._source_batch_id = "source_batch_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a BaselineRequest message.
             * @function verify
             * @memberof northstar.live.BaselineRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BaselineRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                if (message.source_batch_id != null && $Object.hasOwnProperty.call(message, "source_batch_id")) {
                    properties._source_batch_id = 1;
                    if (!$util.isString(message.source_batch_id))
                        return "source_batch_id: string expected";
                }
                return null;
            };

            /**
             * Creates a BaselineRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.BaselineRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.BaselineRequest} BaselineRequest
             */
            BaselineRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.BaselineRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.BaselineRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.BaselineRequest();
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                if (object.source_batch_id != null)
                    message.source_batch_id = $String(object.source_batch_id);
                return message;
            };

            /**
             * Creates a plain object from a BaselineRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.BaselineRequest
             * @static
             * @param {northstar.live.BaselineRequest} message BaselineRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BaselineRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                if (message.source_batch_id != null && $Object.hasOwnProperty.call(message, "source_batch_id")) {
                    object.source_batch_id = message.source_batch_id;
                    if (options.oneofs)
                        object._source_batch_id = "source_batch_id";
                }
                return object;
            };

            /**
             * Converts this BaselineRequest to JSON.
             * @function toJSON
             * @memberof northstar.live.BaselineRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BaselineRequest.prototype.toJSON = function() {
                return BaselineRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for BaselineRequest
             * @function getTypeUrl
             * @memberof northstar.live.BaselineRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            BaselineRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.BaselineRequest";
            };

            return BaselineRequest;
        })();

        live.BrokerStatus = (function() {

            /**
             * Properties of a BrokerStatus.
             * @typedef {Object} northstar.live.BrokerStatus.$Properties
             * @property {string|null} [connection] BrokerStatus connection
             * @property {google.protobuf.Struct.$Properties|null} [credentials] BrokerStatus credentials
             * @property {Object.<string,string>|null} [execution] BrokerStatus execution
             * @property {Array.<google.protobuf.Struct.$Properties>|null} [profiles] BrokerStatus profiles
             * @property {google.protobuf.Struct.$Properties|null} [sdk] BrokerStatus sdk
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] BrokerStatus evidence_fields
             * @property {"connection"} [_connection] BrokerStatus _connection
             * @property {"credentials"} [_credentials] BrokerStatus _credentials
             * @property {"sdk"} [_sdk] BrokerStatus _sdk
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a BrokerStatus.
             * @memberof northstar.live
             * @interface IBrokerStatus
             * @augments northstar.live.BrokerStatus.$Properties
             * @deprecated Use northstar.live.BrokerStatus.$Properties instead.
             */

            /**
             * Narrowed shape of a BrokerStatus.
             * @typedef {{
             *   connection?: string|null;
             *   credentials?: google.protobuf.Struct.$Shape|null;
             *   execution?: Object.<string,string>|null;
             *   profiles?: Array.<google.protobuf.Struct.$Shape>|null;
             *   sdk?: google.protobuf.Struct.$Shape|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _connection?: undefined; connection?: null }|{ _connection?: "connection"; connection: string })
             * ) & (
             *   ({ _credentials?: undefined; credentials?: null }|{ _credentials?: "credentials"; credentials: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _sdk?: undefined; sdk?: null }|{ _sdk?: "sdk"; sdk: google.protobuf.Struct.$Shape })
             * )} northstar.live.BrokerStatus.$Shape
             */

            /**
             * Constructs a new BrokerStatus.
             * @memberof northstar.live
             * @classdesc Represents a BrokerStatus.
             * @constructor
             * @param {northstar.live.BrokerStatus.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const BrokerStatus = function (properties) {
                this.execution = {};
                this.profiles = [];
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * BrokerStatus connection.
             * @member {string|null|undefined} connection
             * @memberof northstar.live.BrokerStatus
             * @instance
             */
            BrokerStatus.prototype.connection = null;

            /**
             * BrokerStatus credentials.
             * @member {google.protobuf.Struct.$Properties|null|undefined} credentials
             * @memberof northstar.live.BrokerStatus
             * @instance
             */
            BrokerStatus.prototype.credentials = null;

            /**
             * BrokerStatus execution.
             * @member {Object.<string,string>} execution
             * @memberof northstar.live.BrokerStatus
             * @instance
             */
            BrokerStatus.prototype.execution = $util.emptyObject;

            /**
             * BrokerStatus profiles.
             * @member {Array.<google.protobuf.Struct.$Properties>} profiles
             * @memberof northstar.live.BrokerStatus
             * @instance
             */
            BrokerStatus.prototype.profiles = $util.emptyArray;

            /**
             * BrokerStatus sdk.
             * @member {google.protobuf.Struct.$Properties|null|undefined} sdk
             * @memberof northstar.live.BrokerStatus
             * @instance
             */
            BrokerStatus.prototype.sdk = null;

            /**
             * BrokerStatus evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.BrokerStatus
             * @instance
             */
            BrokerStatus.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * BrokerStatus _connection.
             * @member {"connection"|undefined} _connection
             * @memberof northstar.live.BrokerStatus
             * @instance
             */
            $Object.defineProperty(BrokerStatus.prototype, "_connection", {
                get: $util.oneOfGetter($oneOfFields = ["connection"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * BrokerStatus _credentials.
             * @member {"credentials"|undefined} _credentials
             * @memberof northstar.live.BrokerStatus
             * @instance
             */
            $Object.defineProperty(BrokerStatus.prototype, "_credentials", {
                get: $util.oneOfGetter($oneOfFields = ["credentials"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * BrokerStatus _sdk.
             * @member {"sdk"|undefined} _sdk
             * @memberof northstar.live.BrokerStatus
             * @instance
             */
            $Object.defineProperty(BrokerStatus.prototype, "_sdk", {
                get: $util.oneOfGetter($oneOfFields = ["sdk"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BrokerStatus instance using the specified properties.
             * @function create
             * @memberof northstar.live.BrokerStatus
             * @static
             * @param {northstar.live.BrokerStatus.$Properties=} [properties] Properties to set
             * @returns {northstar.live.BrokerStatus} BrokerStatus instance
             * @type {{
             *   (properties: northstar.live.BrokerStatus.$Shape): northstar.live.BrokerStatus & northstar.live.BrokerStatus.$Shape;
             *   (properties?: northstar.live.BrokerStatus.$Properties): northstar.live.BrokerStatus;
             * }}
             */
            BrokerStatus.create = function(properties) {
                return new BrokerStatus(properties);
            };

            /**
             * Encodes the specified BrokerStatus message. Does not implicitly {@link northstar.live.BrokerStatus.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.BrokerStatus
             * @static
             * @param {northstar.live.BrokerStatus.$Properties} message BrokerStatus message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BrokerStatus.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.connection != null && $Object.hasOwnProperty.call(message, "connection"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.connection);
                if (message.credentials != null && $Object.hasOwnProperty.call(message, "credentials"))
                    $root.google.protobuf.Struct.encode(message.credentials, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.execution != null && $Object.hasOwnProperty.call(message, "execution"))
                    for (let keys = $Object.keys(message.execution), i = 0; i < keys.length; ++i)
                        writer.uint32(/* id 3, wireType 2 =*/26).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.execution[keys[i]]).ldelim();
                if (message.profiles != null && message.profiles.length)
                    for (let i = 0; i < message.profiles.length; ++i)
                        $root.google.protobuf.Struct.encode(message.profiles[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                if (message.sdk != null && $Object.hasOwnProperty.call(message, "sdk"))
                    $root.google.protobuf.Struct.encode(message.sdk, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a BrokerStatus message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.BrokerStatus
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.BrokerStatus & northstar.live.BrokerStatus.$Shape} BrokerStatus
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BrokerStatus.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.BrokerStatus();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.connection = reader.stringVerify();
                            message._connection = "connection";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.credentials = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.credentials);
                            message._credentials = "credentials";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if (message.execution === $util.emptyObject)
                                message.execution = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = "";
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = reader.stringVerify();
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.execution, key);
                            message.execution[key] = value;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            if (!(message.profiles && message.profiles.length))
                                message.profiles = [];
                            message.profiles.push($root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.sdk = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.sdk);
                            message._sdk = "sdk";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a BrokerStatus message.
             * @function verify
             * @memberof northstar.live.BrokerStatus
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BrokerStatus.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.connection != null && $Object.hasOwnProperty.call(message, "connection")) {
                    properties._connection = 1;
                    if (!$util.isString(message.connection))
                        return "connection: string expected";
                }
                if (message.credentials != null && $Object.hasOwnProperty.call(message, "credentials")) {
                    properties._credentials = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.credentials, _depth + 1);
                        if (error)
                            return "credentials." + error;
                    }
                }
                if (message.execution != null && $Object.hasOwnProperty.call(message, "execution")) {
                    if (!$util.isObject(message.execution))
                        return "execution: object expected";
                    let key = $Object.keys(message.execution);
                    for (let i = 0; i < key.length; ++i)
                        if (!$util.isString(message.execution[key[i]]))
                            return "execution: string{k:string} expected";
                }
                if (message.profiles != null && $Object.hasOwnProperty.call(message, "profiles")) {
                    if (!$Array.isArray(message.profiles))
                        return "profiles: array expected";
                    for (let i = 0; i < message.profiles.length; ++i) {
                        let error = $root.google.protobuf.Struct.verify(message.profiles[i], _depth + 1);
                        if (error)
                            return "profiles." + error;
                    }
                }
                if (message.sdk != null && $Object.hasOwnProperty.call(message, "sdk")) {
                    properties._sdk = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.sdk, _depth + 1);
                        if (error)
                            return "sdk." + error;
                    }
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a BrokerStatus message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.BrokerStatus
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.BrokerStatus} BrokerStatus
             */
            BrokerStatus.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.BrokerStatus)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.BrokerStatus: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.BrokerStatus();
                if (object.connection != null)
                    message.connection = $String(object.connection);
                if (object.credentials != null) {
                    if (!$util.isObject(object.credentials))
                        throw $TypeError(".northstar.live.BrokerStatus.credentials: object expected");
                    message.credentials = $root.google.protobuf.Struct.fromObject(object.credentials, _depth + 1);
                }
                if (object.execution) {
                    if (!$util.isObject(object.execution))
                        throw $TypeError(".northstar.live.BrokerStatus.execution: object expected");
                    message.execution = {};
                    for (let keys = $Object.keys(object.execution), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.execution, keys[i]);
                        message.execution[keys[i]] = $String(object.execution[keys[i]]);
                    }
                }
                if (object.profiles) {
                    if (!$Array.isArray(object.profiles))
                        throw $TypeError(".northstar.live.BrokerStatus.profiles: array expected");
                    message.profiles = $Array(object.profiles.length);
                    for (let i = 0; i < object.profiles.length; ++i) {
                        if (!$util.isObject(object.profiles[i]))
                            throw $TypeError(".northstar.live.BrokerStatus.profiles: object expected");
                        message.profiles[i] = $root.google.protobuf.Struct.fromObject(object.profiles[i], _depth + 1);
                    }
                }
                if (object.sdk != null) {
                    if (!$util.isObject(object.sdk))
                        throw $TypeError(".northstar.live.BrokerStatus.sdk: object expected");
                    message.sdk = $root.google.protobuf.Struct.fromObject(object.sdk, _depth + 1);
                }
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.BrokerStatus.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.BrokerStatus.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a BrokerStatus message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.BrokerStatus
             * @static
             * @param {northstar.live.BrokerStatus} message BrokerStatus
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BrokerStatus.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.profiles = [];
                if (options.objects || options.defaults) {
                    object.execution = {};
                    object.evidence_fields = {};
                }
                if (message.connection != null && $Object.hasOwnProperty.call(message, "connection")) {
                    object.connection = message.connection;
                    if (options.oneofs)
                        object._connection = "connection";
                }
                if (message.credentials != null && $Object.hasOwnProperty.call(message, "credentials")) {
                    object.credentials = $root.google.protobuf.Struct.toObject(message.credentials, options, _depth + 1);
                    if (options.oneofs)
                        object._credentials = "credentials";
                }
                let keys2;
                if (message.execution && (keys2 = $Object.keys(message.execution)).length) {
                    object.execution = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.execution, keys2[j]);
                        object.execution[keys2[j]] = message.execution[keys2[j]];
                    }
                }
                if (message.profiles && message.profiles.length) {
                    object.profiles = $Array(message.profiles.length);
                    for (let j = 0; j < message.profiles.length; ++j)
                        object.profiles[j] = $root.google.protobuf.Struct.toObject(message.profiles[j], options, _depth + 1);
                }
                if (message.sdk != null && $Object.hasOwnProperty.call(message, "sdk")) {
                    object.sdk = $root.google.protobuf.Struct.toObject(message.sdk, options, _depth + 1);
                    if (options.oneofs)
                        object._sdk = "sdk";
                }
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this BrokerStatus to JSON.
             * @function toJSON
             * @memberof northstar.live.BrokerStatus
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BrokerStatus.prototype.toJSON = function() {
                return BrokerStatus.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for BrokerStatus
             * @function getTypeUrl
             * @memberof northstar.live.BrokerStatus
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            BrokerStatus.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.BrokerStatus";
            };

            return BrokerStatus;
        })();

        live.BrowserSession = (function() {

            /**
             * Properties of a BrowserSession.
             * @typedef {Object} northstar.live.BrowserSession.$Properties
             * @property {string|null} [csrf] BrowserSession csrf
             * @property {"csrf"} [_csrf] BrowserSession _csrf
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a BrowserSession.
             * @memberof northstar.live
             * @interface IBrowserSession
             * @augments northstar.live.BrowserSession.$Properties
             * @deprecated Use northstar.live.BrowserSession.$Properties instead.
             */

            /**
             * Narrowed shape of a BrowserSession.
             * @typedef {{
             *   csrf?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _csrf?: undefined; csrf?: null }|{ _csrf?: "csrf"; csrf: string })
             * )} northstar.live.BrowserSession.$Shape
             */

            /**
             * Constructs a new BrowserSession.
             * @memberof northstar.live
             * @classdesc Represents a BrowserSession.
             * @constructor
             * @param {northstar.live.BrowserSession.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const BrowserSession = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * BrowserSession csrf.
             * @member {string|null|undefined} csrf
             * @memberof northstar.live.BrowserSession
             * @instance
             */
            BrowserSession.prototype.csrf = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * BrowserSession _csrf.
             * @member {"csrf"|undefined} _csrf
             * @memberof northstar.live.BrowserSession
             * @instance
             */
            $Object.defineProperty(BrowserSession.prototype, "_csrf", {
                get: $util.oneOfGetter($oneOfFields = ["csrf"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BrowserSession instance using the specified properties.
             * @function create
             * @memberof northstar.live.BrowserSession
             * @static
             * @param {northstar.live.BrowserSession.$Properties=} [properties] Properties to set
             * @returns {northstar.live.BrowserSession} BrowserSession instance
             * @type {{
             *   (properties: northstar.live.BrowserSession.$Shape): northstar.live.BrowserSession & northstar.live.BrowserSession.$Shape;
             *   (properties?: northstar.live.BrowserSession.$Properties): northstar.live.BrowserSession;
             * }}
             */
            BrowserSession.create = function(properties) {
                return new BrowserSession(properties);
            };

            /**
             * Encodes the specified BrowserSession message. Does not implicitly {@link northstar.live.BrowserSession.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.BrowserSession
             * @static
             * @param {northstar.live.BrowserSession.$Properties} message BrowserSession message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BrowserSession.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.csrf != null && $Object.hasOwnProperty.call(message, "csrf"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.csrf);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a BrowserSession message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.BrowserSession
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.BrowserSession & northstar.live.BrowserSession.$Shape} BrowserSession
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BrowserSession.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.BrowserSession();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.csrf = reader.stringVerify();
                            message._csrf = "csrf";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a BrowserSession message.
             * @function verify
             * @memberof northstar.live.BrowserSession
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BrowserSession.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.csrf != null && $Object.hasOwnProperty.call(message, "csrf")) {
                    properties._csrf = 1;
                    if (!$util.isString(message.csrf))
                        return "csrf: string expected";
                }
                return null;
            };

            /**
             * Creates a BrowserSession message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.BrowserSession
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.BrowserSession} BrowserSession
             */
            BrowserSession.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.BrowserSession)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.BrowserSession: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.BrowserSession();
                if (object.csrf != null)
                    message.csrf = $String(object.csrf);
                return message;
            };

            /**
             * Creates a plain object from a BrowserSession message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.BrowserSession
             * @static
             * @param {northstar.live.BrowserSession} message BrowserSession
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BrowserSession.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.csrf != null && $Object.hasOwnProperty.call(message, "csrf")) {
                    object.csrf = message.csrf;
                    if (options.oneofs)
                        object._csrf = "csrf";
                }
                return object;
            };

            /**
             * Converts this BrowserSession to JSON.
             * @function toJSON
             * @memberof northstar.live.BrowserSession
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BrowserSession.prototype.toJSON = function() {
                return BrowserSession.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for BrowserSession
             * @function getTypeUrl
             * @memberof northstar.live.BrowserSession
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            BrowserSession.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.BrowserSession";
            };

            return BrowserSession;
        })();

        live.BudgetContext = (function() {

            /**
             * Properties of a BudgetContext.
             * @typedef {Object} northstar.live.BudgetContext.$Properties
             * @property {Array.<google.protobuf.Struct.$Properties>|null} [budgets] BudgetContext budgets
             * @property {google.protobuf.Struct.$Properties|null} [live_runtime] BudgetContext live_runtime
             * @property {Array.<northstar.live.CheckRecord.$Properties>|null} [order_checks] BudgetContext order_checks
             * @property {Array.<string>|null} [null_fields] BudgetContext null_fields
             * @property {"live_runtime"} [_live_runtime] BudgetContext _live_runtime
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a BudgetContext.
             * @memberof northstar.live
             * @interface IBudgetContext
             * @augments northstar.live.BudgetContext.$Properties
             * @deprecated Use northstar.live.BudgetContext.$Properties instead.
             */

            /**
             * Narrowed shape of a BudgetContext.
             * @typedef {{
             *   budgets?: Array.<google.protobuf.Struct.$Shape>|null;
             *   live_runtime?: google.protobuf.Struct.$Shape|null;
             *   order_checks?: Array.<northstar.live.CheckRecord.$Shape>|null;
             *   null_fields?: Array.<string>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _live_runtime?: undefined; live_runtime?: null }|{ _live_runtime?: "live_runtime"; live_runtime: google.protobuf.Struct.$Shape })
             * )} northstar.live.BudgetContext.$Shape
             */

            /**
             * Constructs a new BudgetContext.
             * @memberof northstar.live
             * @classdesc Represents a BudgetContext.
             * @constructor
             * @param {northstar.live.BudgetContext.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const BudgetContext = function (properties) {
                this.budgets = [];
                this.order_checks = [];
                this.null_fields = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * BudgetContext budgets.
             * @member {Array.<google.protobuf.Struct.$Properties>} budgets
             * @memberof northstar.live.BudgetContext
             * @instance
             */
            BudgetContext.prototype.budgets = $util.emptyArray;

            /**
             * BudgetContext live_runtime.
             * @member {google.protobuf.Struct.$Properties|null|undefined} live_runtime
             * @memberof northstar.live.BudgetContext
             * @instance
             */
            BudgetContext.prototype.live_runtime = null;

            /**
             * BudgetContext order_checks.
             * @member {Array.<northstar.live.CheckRecord.$Properties>} order_checks
             * @memberof northstar.live.BudgetContext
             * @instance
             */
            BudgetContext.prototype.order_checks = $util.emptyArray;

            /**
             * BudgetContext null_fields.
             * @member {Array.<string>} null_fields
             * @memberof northstar.live.BudgetContext
             * @instance
             */
            BudgetContext.prototype.null_fields = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * BudgetContext _live_runtime.
             * @member {"live_runtime"|undefined} _live_runtime
             * @memberof northstar.live.BudgetContext
             * @instance
             */
            $Object.defineProperty(BudgetContext.prototype, "_live_runtime", {
                get: $util.oneOfGetter($oneOfFields = ["live_runtime"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BudgetContext instance using the specified properties.
             * @function create
             * @memberof northstar.live.BudgetContext
             * @static
             * @param {northstar.live.BudgetContext.$Properties=} [properties] Properties to set
             * @returns {northstar.live.BudgetContext} BudgetContext instance
             * @type {{
             *   (properties: northstar.live.BudgetContext.$Shape): northstar.live.BudgetContext & northstar.live.BudgetContext.$Shape;
             *   (properties?: northstar.live.BudgetContext.$Properties): northstar.live.BudgetContext;
             * }}
             */
            BudgetContext.create = function(properties) {
                return new BudgetContext(properties);
            };

            /**
             * Encodes the specified BudgetContext message. Does not implicitly {@link northstar.live.BudgetContext.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.BudgetContext
             * @static
             * @param {northstar.live.BudgetContext.$Properties} message BudgetContext message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BudgetContext.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.budgets != null && message.budgets.length)
                    for (let i = 0; i < message.budgets.length; ++i)
                        $root.google.protobuf.Struct.encode(message.budgets[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.live_runtime != null && $Object.hasOwnProperty.call(message, "live_runtime"))
                    $root.google.protobuf.Struct.encode(message.live_runtime, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.order_checks != null && message.order_checks.length)
                    for (let i = 0; i < message.order_checks.length; ++i)
                        $root.northstar.live.CheckRecord.encode(message.order_checks[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.null_fields != null && message.null_fields.length)
                    for (let i = 0; i < message.null_fields.length; ++i)
                        writer.uint32(/* id 2046, wireType 2 =*/16370).string(message.null_fields[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a BudgetContext message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.BudgetContext
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.BudgetContext & northstar.live.BudgetContext.$Shape} BudgetContext
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BudgetContext.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.BudgetContext();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.budgets && message.budgets.length))
                                message.budgets = [];
                            message.budgets.push($root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.live_runtime = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.live_runtime);
                            message._live_runtime = "live_runtime";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if (!(message.order_checks && message.order_checks.length))
                                message.order_checks = [];
                            message.order_checks.push($root.northstar.live.CheckRecord.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 2046: {
                            if (wireType !== 2)
                                break;
                            if (!(message.null_fields && message.null_fields.length))
                                message.null_fields = [];
                            message.null_fields.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a BudgetContext message.
             * @function verify
             * @memberof northstar.live.BudgetContext
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BudgetContext.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.budgets != null && $Object.hasOwnProperty.call(message, "budgets")) {
                    if (!$Array.isArray(message.budgets))
                        return "budgets: array expected";
                    for (let i = 0; i < message.budgets.length; ++i) {
                        let error = $root.google.protobuf.Struct.verify(message.budgets[i], _depth + 1);
                        if (error)
                            return "budgets." + error;
                    }
                }
                if (message.live_runtime != null && $Object.hasOwnProperty.call(message, "live_runtime")) {
                    properties._live_runtime = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.live_runtime, _depth + 1);
                        if (error)
                            return "live_runtime." + error;
                    }
                }
                if (message.order_checks != null && $Object.hasOwnProperty.call(message, "order_checks")) {
                    if (!$Array.isArray(message.order_checks))
                        return "order_checks: array expected";
                    for (let i = 0; i < message.order_checks.length; ++i) {
                        let error = $root.northstar.live.CheckRecord.verify(message.order_checks[i], _depth + 1);
                        if (error)
                            return "order_checks." + error;
                    }
                }
                if (message.null_fields != null && $Object.hasOwnProperty.call(message, "null_fields")) {
                    if (!$Array.isArray(message.null_fields))
                        return "null_fields: array expected";
                    for (let i = 0; i < message.null_fields.length; ++i)
                        if (!$util.isString(message.null_fields[i]))
                            return "null_fields: string[] expected";
                }
                return null;
            };

            /**
             * Creates a BudgetContext message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.BudgetContext
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.BudgetContext} BudgetContext
             */
            BudgetContext.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.BudgetContext)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.BudgetContext: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.BudgetContext();
                if (object.budgets) {
                    if (!$Array.isArray(object.budgets))
                        throw $TypeError(".northstar.live.BudgetContext.budgets: array expected");
                    message.budgets = $Array(object.budgets.length);
                    for (let i = 0; i < object.budgets.length; ++i) {
                        if (!$util.isObject(object.budgets[i]))
                            throw $TypeError(".northstar.live.BudgetContext.budgets: object expected");
                        message.budgets[i] = $root.google.protobuf.Struct.fromObject(object.budgets[i], _depth + 1);
                    }
                }
                if (object.live_runtime != null) {
                    if (!$util.isObject(object.live_runtime))
                        throw $TypeError(".northstar.live.BudgetContext.live_runtime: object expected");
                    message.live_runtime = $root.google.protobuf.Struct.fromObject(object.live_runtime, _depth + 1);
                }
                if (object.order_checks) {
                    if (!$Array.isArray(object.order_checks))
                        throw $TypeError(".northstar.live.BudgetContext.order_checks: array expected");
                    message.order_checks = $Array(object.order_checks.length);
                    for (let i = 0; i < object.order_checks.length; ++i) {
                        if (!$util.isObject(object.order_checks[i]))
                            throw $TypeError(".northstar.live.BudgetContext.order_checks: object expected");
                        message.order_checks[i] = $root.northstar.live.CheckRecord.fromObject(object.order_checks[i], _depth + 1);
                    }
                }
                if (object.null_fields) {
                    if (!$Array.isArray(object.null_fields))
                        throw $TypeError(".northstar.live.BudgetContext.null_fields: array expected");
                    message.null_fields = $Array(object.null_fields.length);
                    for (let i = 0; i < object.null_fields.length; ++i)
                        message.null_fields[i] = $String(object.null_fields[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a BudgetContext message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.BudgetContext
             * @static
             * @param {northstar.live.BudgetContext} message BudgetContext
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BudgetContext.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.budgets = [];
                    object.order_checks = [];
                    object.null_fields = [];
                }
                if (message.budgets && message.budgets.length) {
                    object.budgets = $Array(message.budgets.length);
                    for (let j = 0; j < message.budgets.length; ++j)
                        object.budgets[j] = $root.google.protobuf.Struct.toObject(message.budgets[j], options, _depth + 1);
                }
                if (message.live_runtime != null && $Object.hasOwnProperty.call(message, "live_runtime")) {
                    object.live_runtime = $root.google.protobuf.Struct.toObject(message.live_runtime, options, _depth + 1);
                    if (options.oneofs)
                        object._live_runtime = "live_runtime";
                }
                if (message.order_checks && message.order_checks.length) {
                    object.order_checks = $Array(message.order_checks.length);
                    for (let j = 0; j < message.order_checks.length; ++j)
                        object.order_checks[j] = $root.northstar.live.CheckRecord.toObject(message.order_checks[j], options, _depth + 1);
                }
                if (message.null_fields && message.null_fields.length) {
                    object.null_fields = $Array(message.null_fields.length);
                    for (let j = 0; j < message.null_fields.length; ++j)
                        object.null_fields[j] = message.null_fields[j];
                }
                return object;
            };

            /**
             * Converts this BudgetContext to JSON.
             * @function toJSON
             * @memberof northstar.live.BudgetContext
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BudgetContext.prototype.toJSON = function() {
                return BudgetContext.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for BudgetContext
             * @function getTypeUrl
             * @memberof northstar.live.BudgetContext
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            BudgetContext.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.BudgetContext";
            };

            return BudgetContext;
        })();

        live.CheckRecord = (function() {

            /**
             * Properties of a CheckRecord.
             * @typedef {Object} northstar.live.CheckRecord.$Properties
             * @property {string|null} [check_id] CheckRecord check_id
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] CheckRecord evidence_fields
             * @property {"check_id"} [_check_id] CheckRecord _check_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a CheckRecord.
             * @memberof northstar.live
             * @interface ICheckRecord
             * @augments northstar.live.CheckRecord.$Properties
             * @deprecated Use northstar.live.CheckRecord.$Properties instead.
             */

            /**
             * Narrowed shape of a CheckRecord.
             * @typedef {{
             *   check_id?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _check_id?: undefined; check_id?: null }|{ _check_id?: "check_id"; check_id: string })
             * )} northstar.live.CheckRecord.$Shape
             */

            /**
             * Constructs a new CheckRecord.
             * @memberof northstar.live
             * @classdesc Represents a CheckRecord.
             * @constructor
             * @param {northstar.live.CheckRecord.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const CheckRecord = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * CheckRecord check_id.
             * @member {string|null|undefined} check_id
             * @memberof northstar.live.CheckRecord
             * @instance
             */
            CheckRecord.prototype.check_id = null;

            /**
             * CheckRecord evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.CheckRecord
             * @instance
             */
            CheckRecord.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * CheckRecord _check_id.
             * @member {"check_id"|undefined} _check_id
             * @memberof northstar.live.CheckRecord
             * @instance
             */
            $Object.defineProperty(CheckRecord.prototype, "_check_id", {
                get: $util.oneOfGetter($oneOfFields = ["check_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new CheckRecord instance using the specified properties.
             * @function create
             * @memberof northstar.live.CheckRecord
             * @static
             * @param {northstar.live.CheckRecord.$Properties=} [properties] Properties to set
             * @returns {northstar.live.CheckRecord} CheckRecord instance
             * @type {{
             *   (properties: northstar.live.CheckRecord.$Shape): northstar.live.CheckRecord & northstar.live.CheckRecord.$Shape;
             *   (properties?: northstar.live.CheckRecord.$Properties): northstar.live.CheckRecord;
             * }}
             */
            CheckRecord.create = function(properties) {
                return new CheckRecord(properties);
            };

            /**
             * Encodes the specified CheckRecord message. Does not implicitly {@link northstar.live.CheckRecord.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.CheckRecord
             * @static
             * @param {northstar.live.CheckRecord.$Properties} message CheckRecord message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CheckRecord.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.check_id != null && $Object.hasOwnProperty.call(message, "check_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.check_id);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a CheckRecord message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.CheckRecord
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.CheckRecord & northstar.live.CheckRecord.$Shape} CheckRecord
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CheckRecord.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.CheckRecord();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.check_id = reader.stringVerify();
                            message._check_id = "check_id";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a CheckRecord message.
             * @function verify
             * @memberof northstar.live.CheckRecord
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CheckRecord.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.check_id != null && $Object.hasOwnProperty.call(message, "check_id")) {
                    properties._check_id = 1;
                    if (!$util.isString(message.check_id))
                        return "check_id: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a CheckRecord message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.CheckRecord
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.CheckRecord} CheckRecord
             */
            CheckRecord.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.CheckRecord)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.CheckRecord: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.CheckRecord();
                if (object.check_id != null)
                    message.check_id = $String(object.check_id);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.CheckRecord.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.CheckRecord.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a CheckRecord message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.CheckRecord
             * @static
             * @param {northstar.live.CheckRecord} message CheckRecord
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CheckRecord.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.check_id != null && $Object.hasOwnProperty.call(message, "check_id")) {
                    object.check_id = message.check_id;
                    if (options.oneofs)
                        object._check_id = "check_id";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this CheckRecord to JSON.
             * @function toJSON
             * @memberof northstar.live.CheckRecord
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CheckRecord.prototype.toJSON = function() {
                return CheckRecord.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for CheckRecord
             * @function getTypeUrl
             * @memberof northstar.live.CheckRecord
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            CheckRecord.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.CheckRecord";
            };

            return CheckRecord;
        })();

        live.CommandRecord = (function() {

            /**
             * Properties of a CommandRecord.
             * @typedef {Object} northstar.live.CommandRecord.$Properties
             * @property {string|null} [request_id] CommandRecord request_id
             * @property {string|null} [status] CommandRecord status
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] CommandRecord evidence_fields
             * @property {"request_id"} [_request_id] CommandRecord _request_id
             * @property {"status"} [_status] CommandRecord _status
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a CommandRecord.
             * @memberof northstar.live
             * @interface ICommandRecord
             * @augments northstar.live.CommandRecord.$Properties
             * @deprecated Use northstar.live.CommandRecord.$Properties instead.
             */

            /**
             * Narrowed shape of a CommandRecord.
             * @typedef {{
             *   request_id?: string|null;
             *   status?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * ) & (
             *   ({ _status?: undefined; status?: null }|{ _status?: "status"; status: string })
             * )} northstar.live.CommandRecord.$Shape
             */

            /**
             * Constructs a new CommandRecord.
             * @memberof northstar.live
             * @classdesc Represents a CommandRecord.
             * @constructor
             * @param {northstar.live.CommandRecord.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const CommandRecord = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * CommandRecord request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.live.CommandRecord
             * @instance
             */
            CommandRecord.prototype.request_id = null;

            /**
             * CommandRecord status.
             * @member {string|null|undefined} status
             * @memberof northstar.live.CommandRecord
             * @instance
             */
            CommandRecord.prototype.status = null;

            /**
             * CommandRecord evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.CommandRecord
             * @instance
             */
            CommandRecord.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * CommandRecord _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.live.CommandRecord
             * @instance
             */
            $Object.defineProperty(CommandRecord.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * CommandRecord _status.
             * @member {"status"|undefined} _status
             * @memberof northstar.live.CommandRecord
             * @instance
             */
            $Object.defineProperty(CommandRecord.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new CommandRecord instance using the specified properties.
             * @function create
             * @memberof northstar.live.CommandRecord
             * @static
             * @param {northstar.live.CommandRecord.$Properties=} [properties] Properties to set
             * @returns {northstar.live.CommandRecord} CommandRecord instance
             * @type {{
             *   (properties: northstar.live.CommandRecord.$Shape): northstar.live.CommandRecord & northstar.live.CommandRecord.$Shape;
             *   (properties?: northstar.live.CommandRecord.$Properties): northstar.live.CommandRecord;
             * }}
             */
            CommandRecord.create = function(properties) {
                return new CommandRecord(properties);
            };

            /**
             * Encodes the specified CommandRecord message. Does not implicitly {@link northstar.live.CommandRecord.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.CommandRecord
             * @static
             * @param {northstar.live.CommandRecord.$Properties} message CommandRecord message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CommandRecord.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.request_id);
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.status);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a CommandRecord message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.CommandRecord
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.CommandRecord & northstar.live.CommandRecord.$Shape} CommandRecord
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CommandRecord.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.CommandRecord();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.status = reader.stringVerify();
                            message._status = "status";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a CommandRecord message.
             * @function verify
             * @memberof northstar.live.CommandRecord
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CommandRecord.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (!$util.isString(message.status))
                        return "status: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a CommandRecord message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.CommandRecord
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.CommandRecord} CommandRecord
             */
            CommandRecord.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.CommandRecord)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.CommandRecord: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.CommandRecord();
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                if (object.status != null)
                    message.status = $String(object.status);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.CommandRecord.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.CommandRecord.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a CommandRecord message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.CommandRecord
             * @static
             * @param {northstar.live.CommandRecord} message CommandRecord
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CommandRecord.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    object.status = message.status;
                    if (options.oneofs)
                        object._status = "status";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this CommandRecord to JSON.
             * @function toJSON
             * @memberof northstar.live.CommandRecord
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CommandRecord.prototype.toJSON = function() {
                return CommandRecord.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for CommandRecord
             * @function getTypeUrl
             * @memberof northstar.live.CommandRecord
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            CommandRecord.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.CommandRecord";
            };

            return CommandRecord;
        })();

        live.CompletedMinute = (function() {

            /**
             * Properties of a CompletedMinute.
             * @typedef {Object} northstar.live.CompletedMinute.$Properties
             * @property {string|null} [close] CompletedMinute close
             * @property {string|null} [completed_at] CompletedMinute completed_at
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] CompletedMinute evidence_fields
             * @property {"close"} [_close] CompletedMinute _close
             * @property {"completed_at"} [_completed_at] CompletedMinute _completed_at
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a CompletedMinute.
             * @memberof northstar.live
             * @interface ICompletedMinute
             * @augments northstar.live.CompletedMinute.$Properties
             * @deprecated Use northstar.live.CompletedMinute.$Properties instead.
             */

            /**
             * Narrowed shape of a CompletedMinute.
             * @typedef {{
             *   close?: string|null;
             *   completed_at?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _close?: undefined; close?: null }|{ _close?: "close"; close: string })
             * ) & (
             *   ({ _completed_at?: undefined; completed_at?: null }|{ _completed_at?: "completed_at"; completed_at: string })
             * )} northstar.live.CompletedMinute.$Shape
             */

            /**
             * Constructs a new CompletedMinute.
             * @memberof northstar.live
             * @classdesc Represents a CompletedMinute.
             * @constructor
             * @param {northstar.live.CompletedMinute.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const CompletedMinute = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * CompletedMinute close.
             * @member {string|null|undefined} close
             * @memberof northstar.live.CompletedMinute
             * @instance
             */
            CompletedMinute.prototype.close = null;

            /**
             * CompletedMinute completed_at.
             * @member {string|null|undefined} completed_at
             * @memberof northstar.live.CompletedMinute
             * @instance
             */
            CompletedMinute.prototype.completed_at = null;

            /**
             * CompletedMinute evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.CompletedMinute
             * @instance
             */
            CompletedMinute.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * CompletedMinute _close.
             * @member {"close"|undefined} _close
             * @memberof northstar.live.CompletedMinute
             * @instance
             */
            $Object.defineProperty(CompletedMinute.prototype, "_close", {
                get: $util.oneOfGetter($oneOfFields = ["close"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * CompletedMinute _completed_at.
             * @member {"completed_at"|undefined} _completed_at
             * @memberof northstar.live.CompletedMinute
             * @instance
             */
            $Object.defineProperty(CompletedMinute.prototype, "_completed_at", {
                get: $util.oneOfGetter($oneOfFields = ["completed_at"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new CompletedMinute instance using the specified properties.
             * @function create
             * @memberof northstar.live.CompletedMinute
             * @static
             * @param {northstar.live.CompletedMinute.$Properties=} [properties] Properties to set
             * @returns {northstar.live.CompletedMinute} CompletedMinute instance
             * @type {{
             *   (properties: northstar.live.CompletedMinute.$Shape): northstar.live.CompletedMinute & northstar.live.CompletedMinute.$Shape;
             *   (properties?: northstar.live.CompletedMinute.$Properties): northstar.live.CompletedMinute;
             * }}
             */
            CompletedMinute.create = function(properties) {
                return new CompletedMinute(properties);
            };

            /**
             * Encodes the specified CompletedMinute message. Does not implicitly {@link northstar.live.CompletedMinute.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.CompletedMinute
             * @static
             * @param {northstar.live.CompletedMinute.$Properties} message CompletedMinute message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CompletedMinute.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.close != null && $Object.hasOwnProperty.call(message, "close"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.close);
                if (message.completed_at != null && $Object.hasOwnProperty.call(message, "completed_at"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.completed_at);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a CompletedMinute message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.CompletedMinute
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.CompletedMinute & northstar.live.CompletedMinute.$Shape} CompletedMinute
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CompletedMinute.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.CompletedMinute();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.close = reader.stringVerify();
                            message._close = "close";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.completed_at = reader.stringVerify();
                            message._completed_at = "completed_at";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a CompletedMinute message.
             * @function verify
             * @memberof northstar.live.CompletedMinute
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CompletedMinute.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.close != null && $Object.hasOwnProperty.call(message, "close")) {
                    properties._close = 1;
                    if (!$util.isString(message.close))
                        return "close: string expected";
                }
                if (message.completed_at != null && $Object.hasOwnProperty.call(message, "completed_at")) {
                    properties._completed_at = 1;
                    if (!$util.isString(message.completed_at))
                        return "completed_at: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a CompletedMinute message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.CompletedMinute
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.CompletedMinute} CompletedMinute
             */
            CompletedMinute.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.CompletedMinute)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.CompletedMinute: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.CompletedMinute();
                if (object.close != null)
                    message.close = $String(object.close);
                if (object.completed_at != null)
                    message.completed_at = $String(object.completed_at);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.CompletedMinute.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.CompletedMinute.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a CompletedMinute message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.CompletedMinute
             * @static
             * @param {northstar.live.CompletedMinute} message CompletedMinute
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CompletedMinute.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.close != null && $Object.hasOwnProperty.call(message, "close")) {
                    object.close = message.close;
                    if (options.oneofs)
                        object._close = "close";
                }
                if (message.completed_at != null && $Object.hasOwnProperty.call(message, "completed_at")) {
                    object.completed_at = message.completed_at;
                    if (options.oneofs)
                        object._completed_at = "completed_at";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this CompletedMinute to JSON.
             * @function toJSON
             * @memberof northstar.live.CompletedMinute
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CompletedMinute.prototype.toJSON = function() {
                return CompletedMinute.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for CompletedMinute
             * @function getTypeUrl
             * @memberof northstar.live.CompletedMinute
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            CompletedMinute.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.CompletedMinute";
            };

            return CompletedMinute;
        })();

        live.ControlRequest = (function() {

            /**
             * Properties of a ControlRequest.
             * @typedef {Object} northstar.live.ControlRequest.$Properties
             * @property {string|null} [action] ControlRequest action
             * @property {string|null} [request_id] ControlRequest request_id
             * @property {"action"} [_action] ControlRequest _action
             * @property {"request_id"} [_request_id] ControlRequest _request_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ControlRequest.
             * @memberof northstar.live
             * @interface IControlRequest
             * @augments northstar.live.ControlRequest.$Properties
             * @deprecated Use northstar.live.ControlRequest.$Properties instead.
             */

            /**
             * Narrowed shape of a ControlRequest.
             * @typedef {{
             *   action?: string|null;
             *   request_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _action?: undefined; action?: null }|{ _action?: "action"; action: string })
             * ) & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * )} northstar.live.ControlRequest.$Shape
             */

            /**
             * Constructs a new ControlRequest.
             * @memberof northstar.live
             * @classdesc Represents a ControlRequest.
             * @constructor
             * @param {northstar.live.ControlRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ControlRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ControlRequest action.
             * @member {string|null|undefined} action
             * @memberof northstar.live.ControlRequest
             * @instance
             */
            ControlRequest.prototype.action = null;

            /**
             * ControlRequest request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.live.ControlRequest
             * @instance
             */
            ControlRequest.prototype.request_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * ControlRequest _action.
             * @member {"action"|undefined} _action
             * @memberof northstar.live.ControlRequest
             * @instance
             */
            $Object.defineProperty(ControlRequest.prototype, "_action", {
                get: $util.oneOfGetter($oneOfFields = ["action"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ControlRequest _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.live.ControlRequest
             * @instance
             */
            $Object.defineProperty(ControlRequest.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ControlRequest instance using the specified properties.
             * @function create
             * @memberof northstar.live.ControlRequest
             * @static
             * @param {northstar.live.ControlRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.live.ControlRequest} ControlRequest instance
             * @type {{
             *   (properties: northstar.live.ControlRequest.$Shape): northstar.live.ControlRequest & northstar.live.ControlRequest.$Shape;
             *   (properties?: northstar.live.ControlRequest.$Properties): northstar.live.ControlRequest;
             * }}
             */
            ControlRequest.create = function(properties) {
                return new ControlRequest(properties);
            };

            /**
             * Encodes the specified ControlRequest message. Does not implicitly {@link northstar.live.ControlRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.ControlRequest
             * @static
             * @param {northstar.live.ControlRequest.$Properties} message ControlRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ControlRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.action != null && $Object.hasOwnProperty.call(message, "action"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.action);
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.request_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a ControlRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.ControlRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.ControlRequest & northstar.live.ControlRequest.$Shape} ControlRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ControlRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.ControlRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.action = reader.stringVerify();
                            message._action = "action";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a ControlRequest message.
             * @function verify
             * @memberof northstar.live.ControlRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ControlRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.action != null && $Object.hasOwnProperty.call(message, "action")) {
                    properties._action = 1;
                    if (!$util.isString(message.action))
                        return "action: string expected";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                return null;
            };

            /**
             * Creates a ControlRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.ControlRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.ControlRequest} ControlRequest
             */
            ControlRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.ControlRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.ControlRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.ControlRequest();
                if (object.action != null)
                    message.action = $String(object.action);
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                return message;
            };

            /**
             * Creates a plain object from a ControlRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.ControlRequest
             * @static
             * @param {northstar.live.ControlRequest} message ControlRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ControlRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.action != null && $Object.hasOwnProperty.call(message, "action")) {
                    object.action = message.action;
                    if (options.oneofs)
                        object._action = "action";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                return object;
            };

            /**
             * Converts this ControlRequest to JSON.
             * @function toJSON
             * @memberof northstar.live.ControlRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ControlRequest.prototype.toJSON = function() {
                return ControlRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ControlRequest
             * @function getTypeUrl
             * @memberof northstar.live.ControlRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ControlRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.ControlRequest";
            };

            return ControlRequest;
        })();

        live.Diagnostics = (function() {

            /**
             * Properties of a Diagnostics.
             * @typedef {Object} northstar.live.Diagnostics.$Properties
             * @property {google.protobuf.Struct.$Properties|null} [database] Diagnostics database
             * @property {number|Long|null} [duration_ms] Diagnostics duration_ms
             * @property {string|null} [observed_at] Diagnostics observed_at
             * @property {string|null} [scope] Diagnostics scope
             * @property {google.protobuf.Struct.$Properties|null} [source_filesystem] Diagnostics source_filesystem
             * @property {string|null} [status] Diagnostics status
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] Diagnostics evidence_fields
             * @property {"database"} [_database] Diagnostics _database
             * @property {"duration_ms"} [_duration_ms] Diagnostics _duration_ms
             * @property {"observed_at"} [_observed_at] Diagnostics _observed_at
             * @property {"scope"} [_scope] Diagnostics _scope
             * @property {"source_filesystem"} [_source_filesystem] Diagnostics _source_filesystem
             * @property {"status"} [_status] Diagnostics _status
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Diagnostics.
             * @memberof northstar.live
             * @interface IDiagnostics
             * @augments northstar.live.Diagnostics.$Properties
             * @deprecated Use northstar.live.Diagnostics.$Properties instead.
             */

            /**
             * Narrowed shape of a Diagnostics.
             * @typedef {{
             *   database?: google.protobuf.Struct.$Shape|null;
             *   duration_ms?: number|Long|null;
             *   observed_at?: string|null;
             *   scope?: string|null;
             *   source_filesystem?: google.protobuf.Struct.$Shape|null;
             *   status?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _database?: undefined; database?: null }|{ _database?: "database"; database: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _duration_ms?: undefined; duration_ms?: null }|{ _duration_ms?: "duration_ms"; duration_ms: number|Long })
             * ) & (
             *   ({ _observed_at?: undefined; observed_at?: null }|{ _observed_at?: "observed_at"; observed_at: string })
             * ) & (
             *   ({ _scope?: undefined; scope?: null }|{ _scope?: "scope"; scope: string })
             * ) & (
             *   ({ _source_filesystem?: undefined; source_filesystem?: null }|{ _source_filesystem?: "source_filesystem"; source_filesystem: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _status?: undefined; status?: null }|{ _status?: "status"; status: string })
             * )} northstar.live.Diagnostics.$Shape
             */

            /**
             * Constructs a new Diagnostics.
             * @memberof northstar.live
             * @classdesc Represents a Diagnostics.
             * @constructor
             * @param {northstar.live.Diagnostics.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const Diagnostics = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Diagnostics database.
             * @member {google.protobuf.Struct.$Properties|null|undefined} database
             * @memberof northstar.live.Diagnostics
             * @instance
             */
            Diagnostics.prototype.database = null;

            /**
             * Diagnostics duration_ms.
             * @member {number|Long|null|undefined} duration_ms
             * @memberof northstar.live.Diagnostics
             * @instance
             */
            Diagnostics.prototype.duration_ms = null;

            /**
             * Diagnostics observed_at.
             * @member {string|null|undefined} observed_at
             * @memberof northstar.live.Diagnostics
             * @instance
             */
            Diagnostics.prototype.observed_at = null;

            /**
             * Diagnostics scope.
             * @member {string|null|undefined} scope
             * @memberof northstar.live.Diagnostics
             * @instance
             */
            Diagnostics.prototype.scope = null;

            /**
             * Diagnostics source_filesystem.
             * @member {google.protobuf.Struct.$Properties|null|undefined} source_filesystem
             * @memberof northstar.live.Diagnostics
             * @instance
             */
            Diagnostics.prototype.source_filesystem = null;

            /**
             * Diagnostics status.
             * @member {string|null|undefined} status
             * @memberof northstar.live.Diagnostics
             * @instance
             */
            Diagnostics.prototype.status = null;

            /**
             * Diagnostics evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.Diagnostics
             * @instance
             */
            Diagnostics.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * Diagnostics _database.
             * @member {"database"|undefined} _database
             * @memberof northstar.live.Diagnostics
             * @instance
             */
            $Object.defineProperty(Diagnostics.prototype, "_database", {
                get: $util.oneOfGetter($oneOfFields = ["database"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Diagnostics _duration_ms.
             * @member {"duration_ms"|undefined} _duration_ms
             * @memberof northstar.live.Diagnostics
             * @instance
             */
            $Object.defineProperty(Diagnostics.prototype, "_duration_ms", {
                get: $util.oneOfGetter($oneOfFields = ["duration_ms"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Diagnostics _observed_at.
             * @member {"observed_at"|undefined} _observed_at
             * @memberof northstar.live.Diagnostics
             * @instance
             */
            $Object.defineProperty(Diagnostics.prototype, "_observed_at", {
                get: $util.oneOfGetter($oneOfFields = ["observed_at"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Diagnostics _scope.
             * @member {"scope"|undefined} _scope
             * @memberof northstar.live.Diagnostics
             * @instance
             */
            $Object.defineProperty(Diagnostics.prototype, "_scope", {
                get: $util.oneOfGetter($oneOfFields = ["scope"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Diagnostics _source_filesystem.
             * @member {"source_filesystem"|undefined} _source_filesystem
             * @memberof northstar.live.Diagnostics
             * @instance
             */
            $Object.defineProperty(Diagnostics.prototype, "_source_filesystem", {
                get: $util.oneOfGetter($oneOfFields = ["source_filesystem"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Diagnostics _status.
             * @member {"status"|undefined} _status
             * @memberof northstar.live.Diagnostics
             * @instance
             */
            $Object.defineProperty(Diagnostics.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new Diagnostics instance using the specified properties.
             * @function create
             * @memberof northstar.live.Diagnostics
             * @static
             * @param {northstar.live.Diagnostics.$Properties=} [properties] Properties to set
             * @returns {northstar.live.Diagnostics} Diagnostics instance
             * @type {{
             *   (properties: northstar.live.Diagnostics.$Shape): northstar.live.Diagnostics & northstar.live.Diagnostics.$Shape;
             *   (properties?: northstar.live.Diagnostics.$Properties): northstar.live.Diagnostics;
             * }}
             */
            Diagnostics.create = function(properties) {
                return new Diagnostics(properties);
            };

            /**
             * Encodes the specified Diagnostics message. Does not implicitly {@link northstar.live.Diagnostics.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.Diagnostics
             * @static
             * @param {northstar.live.Diagnostics.$Properties} message Diagnostics message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Diagnostics.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.database != null && $Object.hasOwnProperty.call(message, "database"))
                    $root.google.protobuf.Struct.encode(message.database, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.duration_ms != null && $Object.hasOwnProperty.call(message, "duration_ms"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.duration_ms);
                if (message.observed_at != null && $Object.hasOwnProperty.call(message, "observed_at"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.observed_at);
                if (message.scope != null && $Object.hasOwnProperty.call(message, "scope"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.scope);
                if (message.source_filesystem != null && $Object.hasOwnProperty.call(message, "source_filesystem"))
                    $root.google.protobuf.Struct.encode(message.source_filesystem, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.status);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a Diagnostics message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.Diagnostics
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.Diagnostics & northstar.live.Diagnostics.$Shape} Diagnostics
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Diagnostics.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.Diagnostics();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.database = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.database);
                            message._database = "database";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            message.duration_ms = reader.int64();
                            message._duration_ms = "duration_ms";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.observed_at = reader.stringVerify();
                            message._observed_at = "observed_at";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.scope = reader.stringVerify();
                            message._scope = "scope";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.source_filesystem = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.source_filesystem);
                            message._source_filesystem = "source_filesystem";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.status = reader.stringVerify();
                            message._status = "status";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a Diagnostics message.
             * @function verify
             * @memberof northstar.live.Diagnostics
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Diagnostics.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.database != null && $Object.hasOwnProperty.call(message, "database")) {
                    properties._database = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.database, _depth + 1);
                        if (error)
                            return "database." + error;
                    }
                }
                if (message.duration_ms != null && $Object.hasOwnProperty.call(message, "duration_ms")) {
                    properties._duration_ms = 1;
                    if (!$util.isInteger(message.duration_ms) && !(message.duration_ms && $util.isInteger(message.duration_ms.low) && $util.isInteger(message.duration_ms.high)))
                        return "duration_ms: integer|Long expected";
                }
                if (message.observed_at != null && $Object.hasOwnProperty.call(message, "observed_at")) {
                    properties._observed_at = 1;
                    if (!$util.isString(message.observed_at))
                        return "observed_at: string expected";
                }
                if (message.scope != null && $Object.hasOwnProperty.call(message, "scope")) {
                    properties._scope = 1;
                    if (!$util.isString(message.scope))
                        return "scope: string expected";
                }
                if (message.source_filesystem != null && $Object.hasOwnProperty.call(message, "source_filesystem")) {
                    properties._source_filesystem = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.source_filesystem, _depth + 1);
                        if (error)
                            return "source_filesystem." + error;
                    }
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (!$util.isString(message.status))
                        return "status: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a Diagnostics message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.Diagnostics
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.Diagnostics} Diagnostics
             */
            Diagnostics.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.Diagnostics)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.Diagnostics: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.Diagnostics();
                if (object.database != null) {
                    if (!$util.isObject(object.database))
                        throw $TypeError(".northstar.live.Diagnostics.database: object expected");
                    message.database = $root.google.protobuf.Struct.fromObject(object.database, _depth + 1);
                }
                if (object.duration_ms != null)
                    if ($util.Long)
                        message.duration_ms = $util.Long.fromValue(object.duration_ms, false);
                    else if (typeof object.duration_ms === "string")
                        message.duration_ms = $parseInt(object.duration_ms, 10);
                    else if (typeof object.duration_ms === "number")
                        message.duration_ms = object.duration_ms;
                    else if (typeof object.duration_ms === "object")
                        message.duration_ms = new $util.LongBits(object.duration_ms.low >>> 0, object.duration_ms.high >>> 0).toNumber();
                if (object.observed_at != null)
                    message.observed_at = $String(object.observed_at);
                if (object.scope != null)
                    message.scope = $String(object.scope);
                if (object.source_filesystem != null) {
                    if (!$util.isObject(object.source_filesystem))
                        throw $TypeError(".northstar.live.Diagnostics.source_filesystem: object expected");
                    message.source_filesystem = $root.google.protobuf.Struct.fromObject(object.source_filesystem, _depth + 1);
                }
                if (object.status != null)
                    message.status = $String(object.status);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.Diagnostics.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.Diagnostics.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a Diagnostics message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.Diagnostics
             * @static
             * @param {northstar.live.Diagnostics} message Diagnostics
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Diagnostics.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.database != null && $Object.hasOwnProperty.call(message, "database")) {
                    object.database = $root.google.protobuf.Struct.toObject(message.database, options, _depth + 1);
                    if (options.oneofs)
                        object._database = "database";
                }
                if (message.duration_ms != null && $Object.hasOwnProperty.call(message, "duration_ms")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.duration_ms = typeof message.duration_ms === "number" ? $BigInt(message.duration_ms) : $util.Long.fromBits(message.duration_ms.low >>> 0, message.duration_ms.high >>> 0, false).toBigInt();
                    else if (typeof message.duration_ms === "number")
                        object.duration_ms = options.longs === $String ? $String(message.duration_ms) : message.duration_ms;
                    else
                        object.duration_ms = options.longs === $String ? $util.Long.prototype.toString.call(message.duration_ms) : options.longs === $Number ? new $util.LongBits(message.duration_ms.low >>> 0, message.duration_ms.high >>> 0).toNumber() : message.duration_ms;
                    if (options.oneofs)
                        object._duration_ms = "duration_ms";
                }
                if (message.observed_at != null && $Object.hasOwnProperty.call(message, "observed_at")) {
                    object.observed_at = message.observed_at;
                    if (options.oneofs)
                        object._observed_at = "observed_at";
                }
                if (message.scope != null && $Object.hasOwnProperty.call(message, "scope")) {
                    object.scope = message.scope;
                    if (options.oneofs)
                        object._scope = "scope";
                }
                if (message.source_filesystem != null && $Object.hasOwnProperty.call(message, "source_filesystem")) {
                    object.source_filesystem = $root.google.protobuf.Struct.toObject(message.source_filesystem, options, _depth + 1);
                    if (options.oneofs)
                        object._source_filesystem = "source_filesystem";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    object.status = message.status;
                    if (options.oneofs)
                        object._status = "status";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this Diagnostics to JSON.
             * @function toJSON
             * @memberof northstar.live.Diagnostics
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Diagnostics.prototype.toJSON = function() {
                return Diagnostics.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Diagnostics
             * @function getTypeUrl
             * @memberof northstar.live.Diagnostics
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Diagnostics.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.Diagnostics";
            };

            return Diagnostics;
        })();

        live.FundsContext = (function() {

            /**
             * Properties of a FundsContext.
             * @typedef {Object} northstar.live.FundsContext.$Properties
             * @property {Array.<google.protobuf.Struct.$Properties>|null} [entries] FundsContext entries
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] FundsContext evidence_fields
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FundsContext.
             * @memberof northstar.live
             * @interface IFundsContext
             * @augments northstar.live.FundsContext.$Properties
             * @deprecated Use northstar.live.FundsContext.$Properties instead.
             */

            /**
             * Shape of a FundsContext.
             * @typedef {{
             *   entries?: Array.<google.protobuf.Struct.$Shape>|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} northstar.live.FundsContext.$Shape
             */

            /**
             * Constructs a new FundsContext.
             * @memberof northstar.live
             * @classdesc Represents a FundsContext.
             * @constructor
             * @param {northstar.live.FundsContext.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FundsContext = function (properties) {
                this.entries = [];
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FundsContext entries.
             * @member {Array.<google.protobuf.Struct.$Properties>} entries
             * @memberof northstar.live.FundsContext
             * @instance
             */
            FundsContext.prototype.entries = $util.emptyArray;

            /**
             * FundsContext evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.FundsContext
             * @instance
             */
            FundsContext.prototype.evidence_fields = $util.emptyObject;

            /**
             * Creates a new FundsContext instance using the specified properties.
             * @function create
             * @memberof northstar.live.FundsContext
             * @static
             * @param {northstar.live.FundsContext.$Properties=} [properties] Properties to set
             * @returns {northstar.live.FundsContext} FundsContext instance
             * @type {{
             *   (properties: northstar.live.FundsContext.$Shape): northstar.live.FundsContext & northstar.live.FundsContext.$Shape;
             *   (properties?: northstar.live.FundsContext.$Properties): northstar.live.FundsContext;
             * }}
             */
            FundsContext.create = function(properties) {
                return new FundsContext(properties);
            };

            /**
             * Encodes the specified FundsContext message. Does not implicitly {@link northstar.live.FundsContext.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.FundsContext
             * @static
             * @param {northstar.live.FundsContext.$Properties} message FundsContext message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FundsContext.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.entries != null && message.entries.length)
                    for (let i = 0; i < message.entries.length; ++i)
                        $root.google.protobuf.Struct.encode(message.entries[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a FundsContext message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.FundsContext
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.FundsContext & northstar.live.FundsContext.$Shape} FundsContext
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FundsContext.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.FundsContext();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.entries && message.entries.length))
                                message.entries = [];
                            message.entries.push($root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a FundsContext message.
             * @function verify
             * @memberof northstar.live.FundsContext
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FundsContext.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.entries != null && $Object.hasOwnProperty.call(message, "entries")) {
                    if (!$Array.isArray(message.entries))
                        return "entries: array expected";
                    for (let i = 0; i < message.entries.length; ++i) {
                        let error = $root.google.protobuf.Struct.verify(message.entries[i], _depth + 1);
                        if (error)
                            return "entries." + error;
                    }
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a FundsContext message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.FundsContext
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.FundsContext} FundsContext
             */
            FundsContext.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.FundsContext)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.FundsContext: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.FundsContext();
                if (object.entries) {
                    if (!$Array.isArray(object.entries))
                        throw $TypeError(".northstar.live.FundsContext.entries: array expected");
                    message.entries = $Array(object.entries.length);
                    for (let i = 0; i < object.entries.length; ++i) {
                        if (!$util.isObject(object.entries[i]))
                            throw $TypeError(".northstar.live.FundsContext.entries: object expected");
                        message.entries[i] = $root.google.protobuf.Struct.fromObject(object.entries[i], _depth + 1);
                    }
                }
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.FundsContext.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.FundsContext.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a FundsContext message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.FundsContext
             * @static
             * @param {northstar.live.FundsContext} message FundsContext
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FundsContext.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.entries = [];
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.entries && message.entries.length) {
                    object.entries = $Array(message.entries.length);
                    for (let j = 0; j < message.entries.length; ++j)
                        object.entries[j] = $root.google.protobuf.Struct.toObject(message.entries[j], options, _depth + 1);
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this FundsContext to JSON.
             * @function toJSON
             * @memberof northstar.live.FundsContext
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FundsContext.prototype.toJSON = function() {
                return FundsContext.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FundsContext
             * @function getTypeUrl
             * @memberof northstar.live.FundsContext
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FundsContext.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.FundsContext";
            };

            return FundsContext;
        })();

        live.FundsEntry = (function() {

            /**
             * Properties of a FundsEntry.
             * @typedef {Object} northstar.live.FundsEntry.$Properties
             * @property {string|null} [entry_id] FundsEntry entry_id
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] FundsEntry evidence_fields
             * @property {"entry_id"} [_entry_id] FundsEntry _entry_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FundsEntry.
             * @memberof northstar.live
             * @interface IFundsEntry
             * @augments northstar.live.FundsEntry.$Properties
             * @deprecated Use northstar.live.FundsEntry.$Properties instead.
             */

            /**
             * Narrowed shape of a FundsEntry.
             * @typedef {{
             *   entry_id?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _entry_id?: undefined; entry_id?: null }|{ _entry_id?: "entry_id"; entry_id: string })
             * )} northstar.live.FundsEntry.$Shape
             */

            /**
             * Constructs a new FundsEntry.
             * @memberof northstar.live
             * @classdesc Represents a FundsEntry.
             * @constructor
             * @param {northstar.live.FundsEntry.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FundsEntry = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FundsEntry entry_id.
             * @member {string|null|undefined} entry_id
             * @memberof northstar.live.FundsEntry
             * @instance
             */
            FundsEntry.prototype.entry_id = null;

            /**
             * FundsEntry evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.FundsEntry
             * @instance
             */
            FundsEntry.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * FundsEntry _entry_id.
             * @member {"entry_id"|undefined} _entry_id
             * @memberof northstar.live.FundsEntry
             * @instance
             */
            $Object.defineProperty(FundsEntry.prototype, "_entry_id", {
                get: $util.oneOfGetter($oneOfFields = ["entry_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new FundsEntry instance using the specified properties.
             * @function create
             * @memberof northstar.live.FundsEntry
             * @static
             * @param {northstar.live.FundsEntry.$Properties=} [properties] Properties to set
             * @returns {northstar.live.FundsEntry} FundsEntry instance
             * @type {{
             *   (properties: northstar.live.FundsEntry.$Shape): northstar.live.FundsEntry & northstar.live.FundsEntry.$Shape;
             *   (properties?: northstar.live.FundsEntry.$Properties): northstar.live.FundsEntry;
             * }}
             */
            FundsEntry.create = function(properties) {
                return new FundsEntry(properties);
            };

            /**
             * Encodes the specified FundsEntry message. Does not implicitly {@link northstar.live.FundsEntry.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.FundsEntry
             * @static
             * @param {northstar.live.FundsEntry.$Properties} message FundsEntry message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FundsEntry.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.entry_id != null && $Object.hasOwnProperty.call(message, "entry_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.entry_id);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a FundsEntry message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.FundsEntry
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.FundsEntry & northstar.live.FundsEntry.$Shape} FundsEntry
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FundsEntry.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.FundsEntry();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.entry_id = reader.stringVerify();
                            message._entry_id = "entry_id";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a FundsEntry message.
             * @function verify
             * @memberof northstar.live.FundsEntry
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FundsEntry.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.entry_id != null && $Object.hasOwnProperty.call(message, "entry_id")) {
                    properties._entry_id = 1;
                    if (!$util.isString(message.entry_id))
                        return "entry_id: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a FundsEntry message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.FundsEntry
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.FundsEntry} FundsEntry
             */
            FundsEntry.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.FundsEntry)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.FundsEntry: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.FundsEntry();
                if (object.entry_id != null)
                    message.entry_id = $String(object.entry_id);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.FundsEntry.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.FundsEntry.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a FundsEntry message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.FundsEntry
             * @static
             * @param {northstar.live.FundsEntry} message FundsEntry
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FundsEntry.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.entry_id != null && $Object.hasOwnProperty.call(message, "entry_id")) {
                    object.entry_id = message.entry_id;
                    if (options.oneofs)
                        object._entry_id = "entry_id";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this FundsEntry to JSON.
             * @function toJSON
             * @memberof northstar.live.FundsEntry
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FundsEntry.prototype.toJSON = function() {
                return FundsEntry.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FundsEntry
             * @function getTypeUrl
             * @memberof northstar.live.FundsEntry
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FundsEntry.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.FundsEntry";
            };

            return FundsEntry;
        })();

        live.FundsEntryRequest = (function() {

            /**
             * Properties of a FundsEntryRequest.
             * @typedef {Object} northstar.live.FundsEntryRequest.$Properties
             * @property {string|null} [baseline_id] FundsEntryRequest baseline_id
             * @property {string|null} [request_id] FundsEntryRequest request_id
             * @property {string|null} [source_batch_id] FundsEntryRequest source_batch_id
             * @property {"baseline_id"} [_baseline_id] FundsEntryRequest _baseline_id
             * @property {"request_id"} [_request_id] FundsEntryRequest _request_id
             * @property {"source_batch_id"} [_source_batch_id] FundsEntryRequest _source_batch_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FundsEntryRequest.
             * @memberof northstar.live
             * @interface IFundsEntryRequest
             * @augments northstar.live.FundsEntryRequest.$Properties
             * @deprecated Use northstar.live.FundsEntryRequest.$Properties instead.
             */

            /**
             * Narrowed shape of a FundsEntryRequest.
             * @typedef {{
             *   baseline_id?: string|null;
             *   request_id?: string|null;
             *   source_batch_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _baseline_id?: undefined; baseline_id?: null }|{ _baseline_id?: "baseline_id"; baseline_id: string })
             * ) & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * ) & (
             *   ({ _source_batch_id?: undefined; source_batch_id?: null }|{ _source_batch_id?: "source_batch_id"; source_batch_id: string })
             * )} northstar.live.FundsEntryRequest.$Shape
             */

            /**
             * Constructs a new FundsEntryRequest.
             * @memberof northstar.live
             * @classdesc Represents a FundsEntryRequest.
             * @constructor
             * @param {northstar.live.FundsEntryRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FundsEntryRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FundsEntryRequest baseline_id.
             * @member {string|null|undefined} baseline_id
             * @memberof northstar.live.FundsEntryRequest
             * @instance
             */
            FundsEntryRequest.prototype.baseline_id = null;

            /**
             * FundsEntryRequest request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.live.FundsEntryRequest
             * @instance
             */
            FundsEntryRequest.prototype.request_id = null;

            /**
             * FundsEntryRequest source_batch_id.
             * @member {string|null|undefined} source_batch_id
             * @memberof northstar.live.FundsEntryRequest
             * @instance
             */
            FundsEntryRequest.prototype.source_batch_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * FundsEntryRequest _baseline_id.
             * @member {"baseline_id"|undefined} _baseline_id
             * @memberof northstar.live.FundsEntryRequest
             * @instance
             */
            $Object.defineProperty(FundsEntryRequest.prototype, "_baseline_id", {
                get: $util.oneOfGetter($oneOfFields = ["baseline_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FundsEntryRequest _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.live.FundsEntryRequest
             * @instance
             */
            $Object.defineProperty(FundsEntryRequest.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * FundsEntryRequest _source_batch_id.
             * @member {"source_batch_id"|undefined} _source_batch_id
             * @memberof northstar.live.FundsEntryRequest
             * @instance
             */
            $Object.defineProperty(FundsEntryRequest.prototype, "_source_batch_id", {
                get: $util.oneOfGetter($oneOfFields = ["source_batch_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new FundsEntryRequest instance using the specified properties.
             * @function create
             * @memberof northstar.live.FundsEntryRequest
             * @static
             * @param {northstar.live.FundsEntryRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.live.FundsEntryRequest} FundsEntryRequest instance
             * @type {{
             *   (properties: northstar.live.FundsEntryRequest.$Shape): northstar.live.FundsEntryRequest & northstar.live.FundsEntryRequest.$Shape;
             *   (properties?: northstar.live.FundsEntryRequest.$Properties): northstar.live.FundsEntryRequest;
             * }}
             */
            FundsEntryRequest.create = function(properties) {
                return new FundsEntryRequest(properties);
            };

            /**
             * Encodes the specified FundsEntryRequest message. Does not implicitly {@link northstar.live.FundsEntryRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.FundsEntryRequest
             * @static
             * @param {northstar.live.FundsEntryRequest.$Properties} message FundsEntryRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FundsEntryRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.baseline_id);
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.request_id);
                if (message.source_batch_id != null && $Object.hasOwnProperty.call(message, "source_batch_id"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.source_batch_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a FundsEntryRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.FundsEntryRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.FundsEntryRequest & northstar.live.FundsEntryRequest.$Shape} FundsEntryRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FundsEntryRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.FundsEntryRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.baseline_id = reader.stringVerify();
                            message._baseline_id = "baseline_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.source_batch_id = reader.stringVerify();
                            message._source_batch_id = "source_batch_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a FundsEntryRequest message.
             * @function verify
             * @memberof northstar.live.FundsEntryRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FundsEntryRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id")) {
                    properties._baseline_id = 1;
                    if (!$util.isString(message.baseline_id))
                        return "baseline_id: string expected";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                if (message.source_batch_id != null && $Object.hasOwnProperty.call(message, "source_batch_id")) {
                    properties._source_batch_id = 1;
                    if (!$util.isString(message.source_batch_id))
                        return "source_batch_id: string expected";
                }
                return null;
            };

            /**
             * Creates a FundsEntryRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.FundsEntryRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.FundsEntryRequest} FundsEntryRequest
             */
            FundsEntryRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.FundsEntryRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.FundsEntryRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.FundsEntryRequest();
                if (object.baseline_id != null)
                    message.baseline_id = $String(object.baseline_id);
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                if (object.source_batch_id != null)
                    message.source_batch_id = $String(object.source_batch_id);
                return message;
            };

            /**
             * Creates a plain object from a FundsEntryRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.FundsEntryRequest
             * @static
             * @param {northstar.live.FundsEntryRequest} message FundsEntryRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FundsEntryRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id")) {
                    object.baseline_id = message.baseline_id;
                    if (options.oneofs)
                        object._baseline_id = "baseline_id";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                if (message.source_batch_id != null && $Object.hasOwnProperty.call(message, "source_batch_id")) {
                    object.source_batch_id = message.source_batch_id;
                    if (options.oneofs)
                        object._source_batch_id = "source_batch_id";
                }
                return object;
            };

            /**
             * Converts this FundsEntryRequest to JSON.
             * @function toJSON
             * @memberof northstar.live.FundsEntryRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FundsEntryRequest.prototype.toJSON = function() {
                return FundsEntryRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FundsEntryRequest
             * @function getTypeUrl
             * @memberof northstar.live.FundsEntryRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FundsEntryRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.FundsEntryRequest";
            };

            return FundsEntryRequest;
        })();

        live.HttpError = (function() {

            /**
             * Properties of a HttpError.
             * @typedef {Object} northstar.live.HttpError.$Properties
             * @property {string|null} [detail] HttpError detail
             * @property {string|null} [rejection_id] HttpError rejection_id
             * @property {string|null} [request_id] HttpError request_id
             * @property {string|null} [runtime_id] HttpError runtime_id
             * @property {string|null} [status] HttpError status
             * @property {string|null} [url] HttpError url
             * @property {Array.<string>|null} [null_fields] HttpError null_fields
             * @property {"detail"} [_detail] HttpError _detail
             * @property {"rejection_id"} [_rejection_id] HttpError _rejection_id
             * @property {"request_id"} [_request_id] HttpError _request_id
             * @property {"runtime_id"} [_runtime_id] HttpError _runtime_id
             * @property {"status"} [_status] HttpError _status
             * @property {"url"} [_url] HttpError _url
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a HttpError.
             * @memberof northstar.live
             * @interface IHttpError
             * @augments northstar.live.HttpError.$Properties
             * @deprecated Use northstar.live.HttpError.$Properties instead.
             */

            /**
             * Narrowed shape of a HttpError.
             * @typedef {{
             *   detail?: string|null;
             *   rejection_id?: string|null;
             *   request_id?: string|null;
             *   runtime_id?: string|null;
             *   status?: string|null;
             *   url?: string|null;
             *   null_fields?: Array.<string>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _detail?: undefined; detail?: null }|{ _detail?: "detail"; detail: string })
             * ) & (
             *   ({ _rejection_id?: undefined; rejection_id?: null }|{ _rejection_id?: "rejection_id"; rejection_id: string })
             * ) & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * ) & (
             *   ({ _runtime_id?: undefined; runtime_id?: null }|{ _runtime_id?: "runtime_id"; runtime_id: string })
             * ) & (
             *   ({ _status?: undefined; status?: null }|{ _status?: "status"; status: string })
             * ) & (
             *   ({ _url?: undefined; url?: null }|{ _url?: "url"; url: string })
             * )} northstar.live.HttpError.$Shape
             */

            /**
             * Constructs a new HttpError.
             * @memberof northstar.live
             * @classdesc Represents a HttpError.
             * @constructor
             * @param {northstar.live.HttpError.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const HttpError = function (properties) {
                this.null_fields = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * HttpError detail.
             * @member {string|null|undefined} detail
             * @memberof northstar.live.HttpError
             * @instance
             */
            HttpError.prototype.detail = null;

            /**
             * HttpError rejection_id.
             * @member {string|null|undefined} rejection_id
             * @memberof northstar.live.HttpError
             * @instance
             */
            HttpError.prototype.rejection_id = null;

            /**
             * HttpError request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.live.HttpError
             * @instance
             */
            HttpError.prototype.request_id = null;

            /**
             * HttpError runtime_id.
             * @member {string|null|undefined} runtime_id
             * @memberof northstar.live.HttpError
             * @instance
             */
            HttpError.prototype.runtime_id = null;

            /**
             * HttpError status.
             * @member {string|null|undefined} status
             * @memberof northstar.live.HttpError
             * @instance
             */
            HttpError.prototype.status = null;

            /**
             * HttpError url.
             * @member {string|null|undefined} url
             * @memberof northstar.live.HttpError
             * @instance
             */
            HttpError.prototype.url = null;

            /**
             * HttpError null_fields.
             * @member {Array.<string>} null_fields
             * @memberof northstar.live.HttpError
             * @instance
             */
            HttpError.prototype.null_fields = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * HttpError _detail.
             * @member {"detail"|undefined} _detail
             * @memberof northstar.live.HttpError
             * @instance
             */
            $Object.defineProperty(HttpError.prototype, "_detail", {
                get: $util.oneOfGetter($oneOfFields = ["detail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * HttpError _rejection_id.
             * @member {"rejection_id"|undefined} _rejection_id
             * @memberof northstar.live.HttpError
             * @instance
             */
            $Object.defineProperty(HttpError.prototype, "_rejection_id", {
                get: $util.oneOfGetter($oneOfFields = ["rejection_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * HttpError _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.live.HttpError
             * @instance
             */
            $Object.defineProperty(HttpError.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * HttpError _runtime_id.
             * @member {"runtime_id"|undefined} _runtime_id
             * @memberof northstar.live.HttpError
             * @instance
             */
            $Object.defineProperty(HttpError.prototype, "_runtime_id", {
                get: $util.oneOfGetter($oneOfFields = ["runtime_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * HttpError _status.
             * @member {"status"|undefined} _status
             * @memberof northstar.live.HttpError
             * @instance
             */
            $Object.defineProperty(HttpError.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * HttpError _url.
             * @member {"url"|undefined} _url
             * @memberof northstar.live.HttpError
             * @instance
             */
            $Object.defineProperty(HttpError.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new HttpError instance using the specified properties.
             * @function create
             * @memberof northstar.live.HttpError
             * @static
             * @param {northstar.live.HttpError.$Properties=} [properties] Properties to set
             * @returns {northstar.live.HttpError} HttpError instance
             * @type {{
             *   (properties: northstar.live.HttpError.$Shape): northstar.live.HttpError & northstar.live.HttpError.$Shape;
             *   (properties?: northstar.live.HttpError.$Properties): northstar.live.HttpError;
             * }}
             */
            HttpError.create = function(properties) {
                return new HttpError(properties);
            };

            /**
             * Encodes the specified HttpError message. Does not implicitly {@link northstar.live.HttpError.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.HttpError
             * @static
             * @param {northstar.live.HttpError.$Properties} message HttpError message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HttpError.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.detail != null && $Object.hasOwnProperty.call(message, "detail"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.detail);
                if (message.rejection_id != null && $Object.hasOwnProperty.call(message, "rejection_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.rejection_id);
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.request_id);
                if (message.runtime_id != null && $Object.hasOwnProperty.call(message, "runtime_id"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.runtime_id);
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.status);
                if (message.url != null && $Object.hasOwnProperty.call(message, "url"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.url);
                if (message.null_fields != null && message.null_fields.length)
                    for (let i = 0; i < message.null_fields.length; ++i)
                        writer.uint32(/* id 2046, wireType 2 =*/16370).string(message.null_fields[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a HttpError message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.HttpError
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.HttpError & northstar.live.HttpError.$Shape} HttpError
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HttpError.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.HttpError();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.detail = reader.stringVerify();
                            message._detail = "detail";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.rejection_id = reader.stringVerify();
                            message._rejection_id = "rejection_id";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.runtime_id = reader.stringVerify();
                            message._runtime_id = "runtime_id";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.status = reader.stringVerify();
                            message._status = "status";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.url = reader.stringVerify();
                            message._url = "url";
                            continue;
                        }
                    case 2046: {
                            if (wireType !== 2)
                                break;
                            if (!(message.null_fields && message.null_fields.length))
                                message.null_fields = [];
                            message.null_fields.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a HttpError message.
             * @function verify
             * @memberof northstar.live.HttpError
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            HttpError.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.detail != null && $Object.hasOwnProperty.call(message, "detail")) {
                    properties._detail = 1;
                    if (!$util.isString(message.detail))
                        return "detail: string expected";
                }
                if (message.rejection_id != null && $Object.hasOwnProperty.call(message, "rejection_id")) {
                    properties._rejection_id = 1;
                    if (!$util.isString(message.rejection_id))
                        return "rejection_id: string expected";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                if (message.runtime_id != null && $Object.hasOwnProperty.call(message, "runtime_id")) {
                    properties._runtime_id = 1;
                    if (!$util.isString(message.runtime_id))
                        return "runtime_id: string expected";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (!$util.isString(message.status))
                        return "status: string expected";
                }
                if (message.url != null && $Object.hasOwnProperty.call(message, "url")) {
                    properties._url = 1;
                    if (!$util.isString(message.url))
                        return "url: string expected";
                }
                if (message.null_fields != null && $Object.hasOwnProperty.call(message, "null_fields")) {
                    if (!$Array.isArray(message.null_fields))
                        return "null_fields: array expected";
                    for (let i = 0; i < message.null_fields.length; ++i)
                        if (!$util.isString(message.null_fields[i]))
                            return "null_fields: string[] expected";
                }
                return null;
            };

            /**
             * Creates a HttpError message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.HttpError
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.HttpError} HttpError
             */
            HttpError.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.HttpError)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.HttpError: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.HttpError();
                if (object.detail != null)
                    message.detail = $String(object.detail);
                if (object.rejection_id != null)
                    message.rejection_id = $String(object.rejection_id);
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                if (object.runtime_id != null)
                    message.runtime_id = $String(object.runtime_id);
                if (object.status != null)
                    message.status = $String(object.status);
                if (object.url != null)
                    message.url = $String(object.url);
                if (object.null_fields) {
                    if (!$Array.isArray(object.null_fields))
                        throw $TypeError(".northstar.live.HttpError.null_fields: array expected");
                    message.null_fields = $Array(object.null_fields.length);
                    for (let i = 0; i < object.null_fields.length; ++i)
                        message.null_fields[i] = $String(object.null_fields[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a HttpError message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.HttpError
             * @static
             * @param {northstar.live.HttpError} message HttpError
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            HttpError.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.null_fields = [];
                if (message.detail != null && $Object.hasOwnProperty.call(message, "detail")) {
                    object.detail = message.detail;
                    if (options.oneofs)
                        object._detail = "detail";
                }
                if (message.rejection_id != null && $Object.hasOwnProperty.call(message, "rejection_id")) {
                    object.rejection_id = message.rejection_id;
                    if (options.oneofs)
                        object._rejection_id = "rejection_id";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                if (message.runtime_id != null && $Object.hasOwnProperty.call(message, "runtime_id")) {
                    object.runtime_id = message.runtime_id;
                    if (options.oneofs)
                        object._runtime_id = "runtime_id";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    object.status = message.status;
                    if (options.oneofs)
                        object._status = "status";
                }
                if (message.url != null && $Object.hasOwnProperty.call(message, "url")) {
                    object.url = message.url;
                    if (options.oneofs)
                        object._url = "url";
                }
                if (message.null_fields && message.null_fields.length) {
                    object.null_fields = $Array(message.null_fields.length);
                    for (let j = 0; j < message.null_fields.length; ++j)
                        object.null_fields[j] = message.null_fields[j];
                }
                return object;
            };

            /**
             * Converts this HttpError to JSON.
             * @function toJSON
             * @memberof northstar.live.HttpError
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            HttpError.prototype.toJSON = function() {
                return HttpError.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for HttpError
             * @function getTypeUrl
             * @memberof northstar.live.HttpError
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            HttpError.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.HttpError";
            };

            return HttpError;
        })();

        live.ImportSpecification = (function() {

            /**
             * Properties of an ImportSpecification.
             * @typedef {Object} northstar.live.ImportSpecification.$Properties
             * @property {string|null} [availability_basis] ImportSpecification availability_basis
             * @property {string|null} [availability_note] ImportSpecification availability_note
             * @property {string|null} [currency] ImportSpecification currency
             * @property {string|null} [exchange] ImportSpecification exchange
             * @property {string|null} [multiplier] ImportSpecification multiplier
             * @property {string|null} [price_tick] ImportSpecification price_tick
             * @property {string|null} [product] ImportSpecification product
             * @property {string|null} [quantity_unit] ImportSpecification quantity_unit
             * @property {string|null} [session_close] ImportSpecification session_close
             * @property {string|null} [session_open] ImportSpecification session_open
             * @property {string|null} [source_name] ImportSpecification source_name
             * @property {string|null} [source_reference] ImportSpecification source_reference
             * @property {string|null} [symbol] ImportSpecification symbol
             * @property {string|null} [timezone] ImportSpecification timezone
             * @property {string|null} [trading_day] ImportSpecification trading_day
             * @property {"availability_basis"} [_availability_basis] ImportSpecification _availability_basis
             * @property {"availability_note"} [_availability_note] ImportSpecification _availability_note
             * @property {"currency"} [_currency] ImportSpecification _currency
             * @property {"exchange"} [_exchange] ImportSpecification _exchange
             * @property {"multiplier"} [_multiplier] ImportSpecification _multiplier
             * @property {"price_tick"} [_price_tick] ImportSpecification _price_tick
             * @property {"product"} [_product] ImportSpecification _product
             * @property {"quantity_unit"} [_quantity_unit] ImportSpecification _quantity_unit
             * @property {"session_close"} [_session_close] ImportSpecification _session_close
             * @property {"session_open"} [_session_open] ImportSpecification _session_open
             * @property {"source_name"} [_source_name] ImportSpecification _source_name
             * @property {"source_reference"} [_source_reference] ImportSpecification _source_reference
             * @property {"symbol"} [_symbol] ImportSpecification _symbol
             * @property {"timezone"} [_timezone] ImportSpecification _timezone
             * @property {"trading_day"} [_trading_day] ImportSpecification _trading_day
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an ImportSpecification.
             * @memberof northstar.live
             * @interface IImportSpecification
             * @augments northstar.live.ImportSpecification.$Properties
             * @deprecated Use northstar.live.ImportSpecification.$Properties instead.
             */

            /**
             * Narrowed shape of an ImportSpecification.
             * @typedef {{
             *   availability_basis?: string|null;
             *   availability_note?: string|null;
             *   currency?: string|null;
             *   exchange?: string|null;
             *   multiplier?: string|null;
             *   price_tick?: string|null;
             *   product?: string|null;
             *   quantity_unit?: string|null;
             *   session_close?: string|null;
             *   session_open?: string|null;
             *   source_name?: string|null;
             *   source_reference?: string|null;
             *   symbol?: string|null;
             *   timezone?: string|null;
             *   trading_day?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _availability_basis?: undefined; availability_basis?: null }|{ _availability_basis?: "availability_basis"; availability_basis: string })
             * ) & (
             *   ({ _availability_note?: undefined; availability_note?: null }|{ _availability_note?: "availability_note"; availability_note: string })
             * ) & (
             *   ({ _currency?: undefined; currency?: null }|{ _currency?: "currency"; currency: string })
             * ) & (
             *   ({ _exchange?: undefined; exchange?: null }|{ _exchange?: "exchange"; exchange: string })
             * ) & (
             *   ({ _multiplier?: undefined; multiplier?: null }|{ _multiplier?: "multiplier"; multiplier: string })
             * ) & (
             *   ({ _price_tick?: undefined; price_tick?: null }|{ _price_tick?: "price_tick"; price_tick: string })
             * ) & (
             *   ({ _product?: undefined; product?: null }|{ _product?: "product"; product: string })
             * ) & (
             *   ({ _quantity_unit?: undefined; quantity_unit?: null }|{ _quantity_unit?: "quantity_unit"; quantity_unit: string })
             * ) & (
             *   ({ _session_close?: undefined; session_close?: null }|{ _session_close?: "session_close"; session_close: string })
             * ) & (
             *   ({ _session_open?: undefined; session_open?: null }|{ _session_open?: "session_open"; session_open: string })
             * ) & (
             *   ({ _source_name?: undefined; source_name?: null }|{ _source_name?: "source_name"; source_name: string })
             * ) & (
             *   ({ _source_reference?: undefined; source_reference?: null }|{ _source_reference?: "source_reference"; source_reference: string })
             * ) & (
             *   ({ _symbol?: undefined; symbol?: null }|{ _symbol?: "symbol"; symbol: string })
             * ) & (
             *   ({ _timezone?: undefined; timezone?: null }|{ _timezone?: "timezone"; timezone: string })
             * ) & (
             *   ({ _trading_day?: undefined; trading_day?: null }|{ _trading_day?: "trading_day"; trading_day: string })
             * )} northstar.live.ImportSpecification.$Shape
             */

            /**
             * Constructs a new ImportSpecification.
             * @memberof northstar.live
             * @classdesc Represents an ImportSpecification.
             * @constructor
             * @param {northstar.live.ImportSpecification.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ImportSpecification = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ImportSpecification availability_basis.
             * @member {string|null|undefined} availability_basis
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.availability_basis = null;

            /**
             * ImportSpecification availability_note.
             * @member {string|null|undefined} availability_note
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.availability_note = null;

            /**
             * ImportSpecification currency.
             * @member {string|null|undefined} currency
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.currency = null;

            /**
             * ImportSpecification exchange.
             * @member {string|null|undefined} exchange
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.exchange = null;

            /**
             * ImportSpecification multiplier.
             * @member {string|null|undefined} multiplier
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.multiplier = null;

            /**
             * ImportSpecification price_tick.
             * @member {string|null|undefined} price_tick
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.price_tick = null;

            /**
             * ImportSpecification product.
             * @member {string|null|undefined} product
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.product = null;

            /**
             * ImportSpecification quantity_unit.
             * @member {string|null|undefined} quantity_unit
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.quantity_unit = null;

            /**
             * ImportSpecification session_close.
             * @member {string|null|undefined} session_close
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.session_close = null;

            /**
             * ImportSpecification session_open.
             * @member {string|null|undefined} session_open
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.session_open = null;

            /**
             * ImportSpecification source_name.
             * @member {string|null|undefined} source_name
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.source_name = null;

            /**
             * ImportSpecification source_reference.
             * @member {string|null|undefined} source_reference
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.source_reference = null;

            /**
             * ImportSpecification symbol.
             * @member {string|null|undefined} symbol
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.symbol = null;

            /**
             * ImportSpecification timezone.
             * @member {string|null|undefined} timezone
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.timezone = null;

            /**
             * ImportSpecification trading_day.
             * @member {string|null|undefined} trading_day
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            ImportSpecification.prototype.trading_day = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * ImportSpecification _availability_basis.
             * @member {"availability_basis"|undefined} _availability_basis
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_availability_basis", {
                get: $util.oneOfGetter($oneOfFields = ["availability_basis"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _availability_note.
             * @member {"availability_note"|undefined} _availability_note
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_availability_note", {
                get: $util.oneOfGetter($oneOfFields = ["availability_note"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _currency.
             * @member {"currency"|undefined} _currency
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_currency", {
                get: $util.oneOfGetter($oneOfFields = ["currency"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _exchange.
             * @member {"exchange"|undefined} _exchange
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_exchange", {
                get: $util.oneOfGetter($oneOfFields = ["exchange"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _multiplier.
             * @member {"multiplier"|undefined} _multiplier
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_multiplier", {
                get: $util.oneOfGetter($oneOfFields = ["multiplier"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _price_tick.
             * @member {"price_tick"|undefined} _price_tick
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_price_tick", {
                get: $util.oneOfGetter($oneOfFields = ["price_tick"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _product.
             * @member {"product"|undefined} _product
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_product", {
                get: $util.oneOfGetter($oneOfFields = ["product"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _quantity_unit.
             * @member {"quantity_unit"|undefined} _quantity_unit
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_quantity_unit", {
                get: $util.oneOfGetter($oneOfFields = ["quantity_unit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _session_close.
             * @member {"session_close"|undefined} _session_close
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_session_close", {
                get: $util.oneOfGetter($oneOfFields = ["session_close"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _session_open.
             * @member {"session_open"|undefined} _session_open
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_session_open", {
                get: $util.oneOfGetter($oneOfFields = ["session_open"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _source_name.
             * @member {"source_name"|undefined} _source_name
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_source_name", {
                get: $util.oneOfGetter($oneOfFields = ["source_name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _source_reference.
             * @member {"source_reference"|undefined} _source_reference
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_source_reference", {
                get: $util.oneOfGetter($oneOfFields = ["source_reference"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _symbol.
             * @member {"symbol"|undefined} _symbol
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_symbol", {
                get: $util.oneOfGetter($oneOfFields = ["symbol"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _timezone.
             * @member {"timezone"|undefined} _timezone
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_timezone", {
                get: $util.oneOfGetter($oneOfFields = ["timezone"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ImportSpecification _trading_day.
             * @member {"trading_day"|undefined} _trading_day
             * @memberof northstar.live.ImportSpecification
             * @instance
             */
            $Object.defineProperty(ImportSpecification.prototype, "_trading_day", {
                get: $util.oneOfGetter($oneOfFields = ["trading_day"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ImportSpecification instance using the specified properties.
             * @function create
             * @memberof northstar.live.ImportSpecification
             * @static
             * @param {northstar.live.ImportSpecification.$Properties=} [properties] Properties to set
             * @returns {northstar.live.ImportSpecification} ImportSpecification instance
             * @type {{
             *   (properties: northstar.live.ImportSpecification.$Shape): northstar.live.ImportSpecification & northstar.live.ImportSpecification.$Shape;
             *   (properties?: northstar.live.ImportSpecification.$Properties): northstar.live.ImportSpecification;
             * }}
             */
            ImportSpecification.create = function(properties) {
                return new ImportSpecification(properties);
            };

            /**
             * Encodes the specified ImportSpecification message. Does not implicitly {@link northstar.live.ImportSpecification.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.ImportSpecification
             * @static
             * @param {northstar.live.ImportSpecification.$Properties} message ImportSpecification message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ImportSpecification.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.availability_basis != null && $Object.hasOwnProperty.call(message, "availability_basis"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.availability_basis);
                if (message.availability_note != null && $Object.hasOwnProperty.call(message, "availability_note"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.availability_note);
                if (message.currency != null && $Object.hasOwnProperty.call(message, "currency"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.currency);
                if (message.exchange != null && $Object.hasOwnProperty.call(message, "exchange"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.exchange);
                if (message.multiplier != null && $Object.hasOwnProperty.call(message, "multiplier"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.multiplier);
                if (message.price_tick != null && $Object.hasOwnProperty.call(message, "price_tick"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.price_tick);
                if (message.product != null && $Object.hasOwnProperty.call(message, "product"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.product);
                if (message.quantity_unit != null && $Object.hasOwnProperty.call(message, "quantity_unit"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.quantity_unit);
                if (message.session_close != null && $Object.hasOwnProperty.call(message, "session_close"))
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.session_close);
                if (message.session_open != null && $Object.hasOwnProperty.call(message, "session_open"))
                    writer.uint32(/* id 10, wireType 2 =*/82).string(message.session_open);
                if (message.source_name != null && $Object.hasOwnProperty.call(message, "source_name"))
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.source_name);
                if (message.source_reference != null && $Object.hasOwnProperty.call(message, "source_reference"))
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.source_reference);
                if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol"))
                    writer.uint32(/* id 13, wireType 2 =*/106).string(message.symbol);
                if (message.timezone != null && $Object.hasOwnProperty.call(message, "timezone"))
                    writer.uint32(/* id 14, wireType 2 =*/114).string(message.timezone);
                if (message.trading_day != null && $Object.hasOwnProperty.call(message, "trading_day"))
                    writer.uint32(/* id 15, wireType 2 =*/122).string(message.trading_day);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes an ImportSpecification message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.ImportSpecification
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.ImportSpecification & northstar.live.ImportSpecification.$Shape} ImportSpecification
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ImportSpecification.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.ImportSpecification();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.availability_basis = reader.stringVerify();
                            message._availability_basis = "availability_basis";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.availability_note = reader.stringVerify();
                            message._availability_note = "availability_note";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.currency = reader.stringVerify();
                            message._currency = "currency";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.exchange = reader.stringVerify();
                            message._exchange = "exchange";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.multiplier = reader.stringVerify();
                            message._multiplier = "multiplier";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.price_tick = reader.stringVerify();
                            message._price_tick = "price_tick";
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            message.product = reader.stringVerify();
                            message._product = "product";
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            message.quantity_unit = reader.stringVerify();
                            message._quantity_unit = "quantity_unit";
                            continue;
                        }
                    case 9: {
                            if (wireType !== 2)
                                break;
                            message.session_close = reader.stringVerify();
                            message._session_close = "session_close";
                            continue;
                        }
                    case 10: {
                            if (wireType !== 2)
                                break;
                            message.session_open = reader.stringVerify();
                            message._session_open = "session_open";
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            message.source_name = reader.stringVerify();
                            message._source_name = "source_name";
                            continue;
                        }
                    case 12: {
                            if (wireType !== 2)
                                break;
                            message.source_reference = reader.stringVerify();
                            message._source_reference = "source_reference";
                            continue;
                        }
                    case 13: {
                            if (wireType !== 2)
                                break;
                            message.symbol = reader.stringVerify();
                            message._symbol = "symbol";
                            continue;
                        }
                    case 14: {
                            if (wireType !== 2)
                                break;
                            message.timezone = reader.stringVerify();
                            message._timezone = "timezone";
                            continue;
                        }
                    case 15: {
                            if (wireType !== 2)
                                break;
                            message.trading_day = reader.stringVerify();
                            message._trading_day = "trading_day";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies an ImportSpecification message.
             * @function verify
             * @memberof northstar.live.ImportSpecification
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ImportSpecification.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.availability_basis != null && $Object.hasOwnProperty.call(message, "availability_basis")) {
                    properties._availability_basis = 1;
                    if (!$util.isString(message.availability_basis))
                        return "availability_basis: string expected";
                }
                if (message.availability_note != null && $Object.hasOwnProperty.call(message, "availability_note")) {
                    properties._availability_note = 1;
                    if (!$util.isString(message.availability_note))
                        return "availability_note: string expected";
                }
                if (message.currency != null && $Object.hasOwnProperty.call(message, "currency")) {
                    properties._currency = 1;
                    if (!$util.isString(message.currency))
                        return "currency: string expected";
                }
                if (message.exchange != null && $Object.hasOwnProperty.call(message, "exchange")) {
                    properties._exchange = 1;
                    if (!$util.isString(message.exchange))
                        return "exchange: string expected";
                }
                if (message.multiplier != null && $Object.hasOwnProperty.call(message, "multiplier")) {
                    properties._multiplier = 1;
                    if (!$util.isString(message.multiplier))
                        return "multiplier: string expected";
                }
                if (message.price_tick != null && $Object.hasOwnProperty.call(message, "price_tick")) {
                    properties._price_tick = 1;
                    if (!$util.isString(message.price_tick))
                        return "price_tick: string expected";
                }
                if (message.product != null && $Object.hasOwnProperty.call(message, "product")) {
                    properties._product = 1;
                    if (!$util.isString(message.product))
                        return "product: string expected";
                }
                if (message.quantity_unit != null && $Object.hasOwnProperty.call(message, "quantity_unit")) {
                    properties._quantity_unit = 1;
                    if (!$util.isString(message.quantity_unit))
                        return "quantity_unit: string expected";
                }
                if (message.session_close != null && $Object.hasOwnProperty.call(message, "session_close")) {
                    properties._session_close = 1;
                    if (!$util.isString(message.session_close))
                        return "session_close: string expected";
                }
                if (message.session_open != null && $Object.hasOwnProperty.call(message, "session_open")) {
                    properties._session_open = 1;
                    if (!$util.isString(message.session_open))
                        return "session_open: string expected";
                }
                if (message.source_name != null && $Object.hasOwnProperty.call(message, "source_name")) {
                    properties._source_name = 1;
                    if (!$util.isString(message.source_name))
                        return "source_name: string expected";
                }
                if (message.source_reference != null && $Object.hasOwnProperty.call(message, "source_reference")) {
                    properties._source_reference = 1;
                    if (!$util.isString(message.source_reference))
                        return "source_reference: string expected";
                }
                if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol")) {
                    properties._symbol = 1;
                    if (!$util.isString(message.symbol))
                        return "symbol: string expected";
                }
                if (message.timezone != null && $Object.hasOwnProperty.call(message, "timezone")) {
                    properties._timezone = 1;
                    if (!$util.isString(message.timezone))
                        return "timezone: string expected";
                }
                if (message.trading_day != null && $Object.hasOwnProperty.call(message, "trading_day")) {
                    properties._trading_day = 1;
                    if (!$util.isString(message.trading_day))
                        return "trading_day: string expected";
                }
                return null;
            };

            /**
             * Creates an ImportSpecification message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.ImportSpecification
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.ImportSpecification} ImportSpecification
             */
            ImportSpecification.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.ImportSpecification)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.ImportSpecification: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.ImportSpecification();
                if (object.availability_basis != null)
                    message.availability_basis = $String(object.availability_basis);
                if (object.availability_note != null)
                    message.availability_note = $String(object.availability_note);
                if (object.currency != null)
                    message.currency = $String(object.currency);
                if (object.exchange != null)
                    message.exchange = $String(object.exchange);
                if (object.multiplier != null)
                    message.multiplier = $String(object.multiplier);
                if (object.price_tick != null)
                    message.price_tick = $String(object.price_tick);
                if (object.product != null)
                    message.product = $String(object.product);
                if (object.quantity_unit != null)
                    message.quantity_unit = $String(object.quantity_unit);
                if (object.session_close != null)
                    message.session_close = $String(object.session_close);
                if (object.session_open != null)
                    message.session_open = $String(object.session_open);
                if (object.source_name != null)
                    message.source_name = $String(object.source_name);
                if (object.source_reference != null)
                    message.source_reference = $String(object.source_reference);
                if (object.symbol != null)
                    message.symbol = $String(object.symbol);
                if (object.timezone != null)
                    message.timezone = $String(object.timezone);
                if (object.trading_day != null)
                    message.trading_day = $String(object.trading_day);
                return message;
            };

            /**
             * Creates a plain object from an ImportSpecification message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.ImportSpecification
             * @static
             * @param {northstar.live.ImportSpecification} message ImportSpecification
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ImportSpecification.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.availability_basis != null && $Object.hasOwnProperty.call(message, "availability_basis")) {
                    object.availability_basis = message.availability_basis;
                    if (options.oneofs)
                        object._availability_basis = "availability_basis";
                }
                if (message.availability_note != null && $Object.hasOwnProperty.call(message, "availability_note")) {
                    object.availability_note = message.availability_note;
                    if (options.oneofs)
                        object._availability_note = "availability_note";
                }
                if (message.currency != null && $Object.hasOwnProperty.call(message, "currency")) {
                    object.currency = message.currency;
                    if (options.oneofs)
                        object._currency = "currency";
                }
                if (message.exchange != null && $Object.hasOwnProperty.call(message, "exchange")) {
                    object.exchange = message.exchange;
                    if (options.oneofs)
                        object._exchange = "exchange";
                }
                if (message.multiplier != null && $Object.hasOwnProperty.call(message, "multiplier")) {
                    object.multiplier = message.multiplier;
                    if (options.oneofs)
                        object._multiplier = "multiplier";
                }
                if (message.price_tick != null && $Object.hasOwnProperty.call(message, "price_tick")) {
                    object.price_tick = message.price_tick;
                    if (options.oneofs)
                        object._price_tick = "price_tick";
                }
                if (message.product != null && $Object.hasOwnProperty.call(message, "product")) {
                    object.product = message.product;
                    if (options.oneofs)
                        object._product = "product";
                }
                if (message.quantity_unit != null && $Object.hasOwnProperty.call(message, "quantity_unit")) {
                    object.quantity_unit = message.quantity_unit;
                    if (options.oneofs)
                        object._quantity_unit = "quantity_unit";
                }
                if (message.session_close != null && $Object.hasOwnProperty.call(message, "session_close")) {
                    object.session_close = message.session_close;
                    if (options.oneofs)
                        object._session_close = "session_close";
                }
                if (message.session_open != null && $Object.hasOwnProperty.call(message, "session_open")) {
                    object.session_open = message.session_open;
                    if (options.oneofs)
                        object._session_open = "session_open";
                }
                if (message.source_name != null && $Object.hasOwnProperty.call(message, "source_name")) {
                    object.source_name = message.source_name;
                    if (options.oneofs)
                        object._source_name = "source_name";
                }
                if (message.source_reference != null && $Object.hasOwnProperty.call(message, "source_reference")) {
                    object.source_reference = message.source_reference;
                    if (options.oneofs)
                        object._source_reference = "source_reference";
                }
                if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol")) {
                    object.symbol = message.symbol;
                    if (options.oneofs)
                        object._symbol = "symbol";
                }
                if (message.timezone != null && $Object.hasOwnProperty.call(message, "timezone")) {
                    object.timezone = message.timezone;
                    if (options.oneofs)
                        object._timezone = "timezone";
                }
                if (message.trading_day != null && $Object.hasOwnProperty.call(message, "trading_day")) {
                    object.trading_day = message.trading_day;
                    if (options.oneofs)
                        object._trading_day = "trading_day";
                }
                return object;
            };

            /**
             * Converts this ImportSpecification to JSON.
             * @function toJSON
             * @memberof northstar.live.ImportSpecification
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ImportSpecification.prototype.toJSON = function() {
                return ImportSpecification.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ImportSpecification
             * @function getTypeUrl
             * @memberof northstar.live.ImportSpecification
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ImportSpecification.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.ImportSpecification";
            };

            return ImportSpecification;
        })();

        live.LedgerContext = (function() {

            /**
             * Properties of a LedgerContext.
             * @typedef {Object} northstar.live.LedgerContext.$Properties
             * @property {northstar.live.BaselineRecord.$Properties|null} [baseline] LedgerContext baseline
             * @property {string|null} [baseline_id] LedgerContext baseline_id
             * @property {Array.<northstar.live.CheckRecord.$Properties>|null} [checks] LedgerContext checks
             * @property {Array.<northstar.live.PositionEntry.$Properties>|null} [entries] LedgerContext entries
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] LedgerContext evidence_fields
             * @property {Array.<string>|null} [null_fields] LedgerContext null_fields
             * @property {"baseline"} [_baseline] LedgerContext _baseline
             * @property {"baseline_id"} [_baseline_id] LedgerContext _baseline_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a LedgerContext.
             * @memberof northstar.live
             * @interface ILedgerContext
             * @augments northstar.live.LedgerContext.$Properties
             * @deprecated Use northstar.live.LedgerContext.$Properties instead.
             */

            /**
             * Narrowed shape of a LedgerContext.
             * @typedef {{
             *   baseline?: northstar.live.BaselineRecord.$Shape|null;
             *   baseline_id?: string|null;
             *   checks?: Array.<northstar.live.CheckRecord.$Shape>|null;
             *   entries?: Array.<northstar.live.PositionEntry.$Shape>|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   null_fields?: Array.<string>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _baseline?: undefined; baseline?: null }|{ _baseline?: "baseline"; baseline: northstar.live.BaselineRecord.$Shape })
             * ) & (
             *   ({ _baseline_id?: undefined; baseline_id?: null }|{ _baseline_id?: "baseline_id"; baseline_id: string })
             * )} northstar.live.LedgerContext.$Shape
             */

            /**
             * Constructs a new LedgerContext.
             * @memberof northstar.live
             * @classdesc Represents a LedgerContext.
             * @constructor
             * @param {northstar.live.LedgerContext.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const LedgerContext = function (properties) {
                this.checks = [];
                this.entries = [];
                this.evidence_fields = {};
                this.null_fields = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * LedgerContext baseline.
             * @member {northstar.live.BaselineRecord.$Properties|null|undefined} baseline
             * @memberof northstar.live.LedgerContext
             * @instance
             */
            LedgerContext.prototype.baseline = null;

            /**
             * LedgerContext baseline_id.
             * @member {string|null|undefined} baseline_id
             * @memberof northstar.live.LedgerContext
             * @instance
             */
            LedgerContext.prototype.baseline_id = null;

            /**
             * LedgerContext checks.
             * @member {Array.<northstar.live.CheckRecord.$Properties>} checks
             * @memberof northstar.live.LedgerContext
             * @instance
             */
            LedgerContext.prototype.checks = $util.emptyArray;

            /**
             * LedgerContext entries.
             * @member {Array.<northstar.live.PositionEntry.$Properties>} entries
             * @memberof northstar.live.LedgerContext
             * @instance
             */
            LedgerContext.prototype.entries = $util.emptyArray;

            /**
             * LedgerContext evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.LedgerContext
             * @instance
             */
            LedgerContext.prototype.evidence_fields = $util.emptyObject;

            /**
             * LedgerContext null_fields.
             * @member {Array.<string>} null_fields
             * @memberof northstar.live.LedgerContext
             * @instance
             */
            LedgerContext.prototype.null_fields = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * LedgerContext _baseline.
             * @member {"baseline"|undefined} _baseline
             * @memberof northstar.live.LedgerContext
             * @instance
             */
            $Object.defineProperty(LedgerContext.prototype, "_baseline", {
                get: $util.oneOfGetter($oneOfFields = ["baseline"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * LedgerContext _baseline_id.
             * @member {"baseline_id"|undefined} _baseline_id
             * @memberof northstar.live.LedgerContext
             * @instance
             */
            $Object.defineProperty(LedgerContext.prototype, "_baseline_id", {
                get: $util.oneOfGetter($oneOfFields = ["baseline_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new LedgerContext instance using the specified properties.
             * @function create
             * @memberof northstar.live.LedgerContext
             * @static
             * @param {northstar.live.LedgerContext.$Properties=} [properties] Properties to set
             * @returns {northstar.live.LedgerContext} LedgerContext instance
             * @type {{
             *   (properties: northstar.live.LedgerContext.$Shape): northstar.live.LedgerContext & northstar.live.LedgerContext.$Shape;
             *   (properties?: northstar.live.LedgerContext.$Properties): northstar.live.LedgerContext;
             * }}
             */
            LedgerContext.create = function(properties) {
                return new LedgerContext(properties);
            };

            /**
             * Encodes the specified LedgerContext message. Does not implicitly {@link northstar.live.LedgerContext.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.LedgerContext
             * @static
             * @param {northstar.live.LedgerContext.$Properties} message LedgerContext message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LedgerContext.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.baseline != null && $Object.hasOwnProperty.call(message, "baseline"))
                    $root.northstar.live.BaselineRecord.encode(message.baseline, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.baseline_id);
                if (message.checks != null && message.checks.length)
                    for (let i = 0; i < message.checks.length; ++i)
                        $root.northstar.live.CheckRecord.encode(message.checks[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.entries != null && message.entries.length)
                    for (let i = 0; i < message.entries.length; ++i)
                        $root.northstar.live.PositionEntry.encode(message.entries[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.null_fields != null && message.null_fields.length)
                    for (let i = 0; i < message.null_fields.length; ++i)
                        writer.uint32(/* id 2046, wireType 2 =*/16370).string(message.null_fields[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a LedgerContext message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.LedgerContext
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.LedgerContext & northstar.live.LedgerContext.$Shape} LedgerContext
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LedgerContext.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.LedgerContext();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.baseline = $root.northstar.live.BaselineRecord.decode(reader, reader.uint32(), $undefined, _depth + 1, message.baseline);
                            message._baseline = "baseline";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.baseline_id = reader.stringVerify();
                            message._baseline_id = "baseline_id";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if (!(message.checks && message.checks.length))
                                message.checks = [];
                            message.checks.push($root.northstar.live.CheckRecord.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            if (!(message.entries && message.entries.length))
                                message.entries = [];
                            message.entries.push($root.northstar.live.PositionEntry.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    case 2046: {
                            if (wireType !== 2)
                                break;
                            if (!(message.null_fields && message.null_fields.length))
                                message.null_fields = [];
                            message.null_fields.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a LedgerContext message.
             * @function verify
             * @memberof northstar.live.LedgerContext
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LedgerContext.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.baseline != null && $Object.hasOwnProperty.call(message, "baseline")) {
                    properties._baseline = 1;
                    {
                        let error = $root.northstar.live.BaselineRecord.verify(message.baseline, _depth + 1);
                        if (error)
                            return "baseline." + error;
                    }
                }
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id")) {
                    properties._baseline_id = 1;
                    if (!$util.isString(message.baseline_id))
                        return "baseline_id: string expected";
                }
                if (message.checks != null && $Object.hasOwnProperty.call(message, "checks")) {
                    if (!$Array.isArray(message.checks))
                        return "checks: array expected";
                    for (let i = 0; i < message.checks.length; ++i) {
                        let error = $root.northstar.live.CheckRecord.verify(message.checks[i], _depth + 1);
                        if (error)
                            return "checks." + error;
                    }
                }
                if (message.entries != null && $Object.hasOwnProperty.call(message, "entries")) {
                    if (!$Array.isArray(message.entries))
                        return "entries: array expected";
                    for (let i = 0; i < message.entries.length; ++i) {
                        let error = $root.northstar.live.PositionEntry.verify(message.entries[i], _depth + 1);
                        if (error)
                            return "entries." + error;
                    }
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                if (message.null_fields != null && $Object.hasOwnProperty.call(message, "null_fields")) {
                    if (!$Array.isArray(message.null_fields))
                        return "null_fields: array expected";
                    for (let i = 0; i < message.null_fields.length; ++i)
                        if (!$util.isString(message.null_fields[i]))
                            return "null_fields: string[] expected";
                }
                return null;
            };

            /**
             * Creates a LedgerContext message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.LedgerContext
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.LedgerContext} LedgerContext
             */
            LedgerContext.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.LedgerContext)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.LedgerContext: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.LedgerContext();
                if (object.baseline != null) {
                    if (!$util.isObject(object.baseline))
                        throw $TypeError(".northstar.live.LedgerContext.baseline: object expected");
                    message.baseline = $root.northstar.live.BaselineRecord.fromObject(object.baseline, _depth + 1);
                }
                if (object.baseline_id != null)
                    message.baseline_id = $String(object.baseline_id);
                if (object.checks) {
                    if (!$Array.isArray(object.checks))
                        throw $TypeError(".northstar.live.LedgerContext.checks: array expected");
                    message.checks = $Array(object.checks.length);
                    for (let i = 0; i < object.checks.length; ++i) {
                        if (!$util.isObject(object.checks[i]))
                            throw $TypeError(".northstar.live.LedgerContext.checks: object expected");
                        message.checks[i] = $root.northstar.live.CheckRecord.fromObject(object.checks[i], _depth + 1);
                    }
                }
                if (object.entries) {
                    if (!$Array.isArray(object.entries))
                        throw $TypeError(".northstar.live.LedgerContext.entries: array expected");
                    message.entries = $Array(object.entries.length);
                    for (let i = 0; i < object.entries.length; ++i) {
                        if (!$util.isObject(object.entries[i]))
                            throw $TypeError(".northstar.live.LedgerContext.entries: object expected");
                        message.entries[i] = $root.northstar.live.PositionEntry.fromObject(object.entries[i], _depth + 1);
                    }
                }
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.LedgerContext.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.LedgerContext.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                if (object.null_fields) {
                    if (!$Array.isArray(object.null_fields))
                        throw $TypeError(".northstar.live.LedgerContext.null_fields: array expected");
                    message.null_fields = $Array(object.null_fields.length);
                    for (let i = 0; i < object.null_fields.length; ++i)
                        message.null_fields[i] = $String(object.null_fields[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a LedgerContext message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.LedgerContext
             * @static
             * @param {northstar.live.LedgerContext} message LedgerContext
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LedgerContext.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.checks = [];
                    object.entries = [];
                    object.null_fields = [];
                }
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.baseline != null && $Object.hasOwnProperty.call(message, "baseline")) {
                    object.baseline = $root.northstar.live.BaselineRecord.toObject(message.baseline, options, _depth + 1);
                    if (options.oneofs)
                        object._baseline = "baseline";
                }
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id")) {
                    object.baseline_id = message.baseline_id;
                    if (options.oneofs)
                        object._baseline_id = "baseline_id";
                }
                if (message.checks && message.checks.length) {
                    object.checks = $Array(message.checks.length);
                    for (let j = 0; j < message.checks.length; ++j)
                        object.checks[j] = $root.northstar.live.CheckRecord.toObject(message.checks[j], options, _depth + 1);
                }
                if (message.entries && message.entries.length) {
                    object.entries = $Array(message.entries.length);
                    for (let j = 0; j < message.entries.length; ++j)
                        object.entries[j] = $root.northstar.live.PositionEntry.toObject(message.entries[j], options, _depth + 1);
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                if (message.null_fields && message.null_fields.length) {
                    object.null_fields = $Array(message.null_fields.length);
                    for (let j = 0; j < message.null_fields.length; ++j)
                        object.null_fields[j] = message.null_fields[j];
                }
                return object;
            };

            /**
             * Converts this LedgerContext to JSON.
             * @function toJSON
             * @memberof northstar.live.LedgerContext
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LedgerContext.prototype.toJSON = function() {
                return LedgerContext.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for LedgerContext
             * @function getTypeUrl
             * @memberof northstar.live.LedgerContext
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            LedgerContext.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.LedgerContext";
            };

            return LedgerContext;
        })();

        live.LiveConfiguration = (function() {

            /**
             * Properties of a LiveConfiguration.
             * @typedef {Object} northstar.live.LiveConfiguration.$Properties
             * @property {google.protobuf.Struct.$Properties|null} [config] LiveConfiguration config
             * @property {string|null} [configuration_id] LiveConfiguration configuration_id
             * @property {string|null} [name] LiveConfiguration name
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] LiveConfiguration evidence_fields
             * @property {"config"} [_config] LiveConfiguration _config
             * @property {"configuration_id"} [_configuration_id] LiveConfiguration _configuration_id
             * @property {"name"} [_name] LiveConfiguration _name
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a LiveConfiguration.
             * @memberof northstar.live
             * @interface ILiveConfiguration
             * @augments northstar.live.LiveConfiguration.$Properties
             * @deprecated Use northstar.live.LiveConfiguration.$Properties instead.
             */

            /**
             * Narrowed shape of a LiveConfiguration.
             * @typedef {{
             *   config?: google.protobuf.Struct.$Shape|null;
             *   configuration_id?: string|null;
             *   name?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _config?: undefined; config?: null }|{ _config?: "config"; config: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _configuration_id?: undefined; configuration_id?: null }|{ _configuration_id?: "configuration_id"; configuration_id: string })
             * ) & (
             *   ({ _name?: undefined; name?: null }|{ _name?: "name"; name: string })
             * )} northstar.live.LiveConfiguration.$Shape
             */

            /**
             * Constructs a new LiveConfiguration.
             * @memberof northstar.live
             * @classdesc Represents a LiveConfiguration.
             * @constructor
             * @param {northstar.live.LiveConfiguration.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const LiveConfiguration = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * LiveConfiguration config.
             * @member {google.protobuf.Struct.$Properties|null|undefined} config
             * @memberof northstar.live.LiveConfiguration
             * @instance
             */
            LiveConfiguration.prototype.config = null;

            /**
             * LiveConfiguration configuration_id.
             * @member {string|null|undefined} configuration_id
             * @memberof northstar.live.LiveConfiguration
             * @instance
             */
            LiveConfiguration.prototype.configuration_id = null;

            /**
             * LiveConfiguration name.
             * @member {string|null|undefined} name
             * @memberof northstar.live.LiveConfiguration
             * @instance
             */
            LiveConfiguration.prototype.name = null;

            /**
             * LiveConfiguration evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.LiveConfiguration
             * @instance
             */
            LiveConfiguration.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * LiveConfiguration _config.
             * @member {"config"|undefined} _config
             * @memberof northstar.live.LiveConfiguration
             * @instance
             */
            $Object.defineProperty(LiveConfiguration.prototype, "_config", {
                get: $util.oneOfGetter($oneOfFields = ["config"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * LiveConfiguration _configuration_id.
             * @member {"configuration_id"|undefined} _configuration_id
             * @memberof northstar.live.LiveConfiguration
             * @instance
             */
            $Object.defineProperty(LiveConfiguration.prototype, "_configuration_id", {
                get: $util.oneOfGetter($oneOfFields = ["configuration_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * LiveConfiguration _name.
             * @member {"name"|undefined} _name
             * @memberof northstar.live.LiveConfiguration
             * @instance
             */
            $Object.defineProperty(LiveConfiguration.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new LiveConfiguration instance using the specified properties.
             * @function create
             * @memberof northstar.live.LiveConfiguration
             * @static
             * @param {northstar.live.LiveConfiguration.$Properties=} [properties] Properties to set
             * @returns {northstar.live.LiveConfiguration} LiveConfiguration instance
             * @type {{
             *   (properties: northstar.live.LiveConfiguration.$Shape): northstar.live.LiveConfiguration & northstar.live.LiveConfiguration.$Shape;
             *   (properties?: northstar.live.LiveConfiguration.$Properties): northstar.live.LiveConfiguration;
             * }}
             */
            LiveConfiguration.create = function(properties) {
                return new LiveConfiguration(properties);
            };

            /**
             * Encodes the specified LiveConfiguration message. Does not implicitly {@link northstar.live.LiveConfiguration.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.LiveConfiguration
             * @static
             * @param {northstar.live.LiveConfiguration.$Properties} message LiveConfiguration message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LiveConfiguration.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.config != null && $Object.hasOwnProperty.call(message, "config"))
                    $root.google.protobuf.Struct.encode(message.config, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.configuration_id != null && $Object.hasOwnProperty.call(message, "configuration_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.configuration_id);
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.name);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a LiveConfiguration message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.LiveConfiguration
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.LiveConfiguration & northstar.live.LiveConfiguration.$Shape} LiveConfiguration
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LiveConfiguration.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.LiveConfiguration();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.config = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.config);
                            message._config = "config";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.configuration_id = reader.stringVerify();
                            message._configuration_id = "configuration_id";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a LiveConfiguration message.
             * @function verify
             * @memberof northstar.live.LiveConfiguration
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LiveConfiguration.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.config != null && $Object.hasOwnProperty.call(message, "config")) {
                    properties._config = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.config, _depth + 1);
                        if (error)
                            return "config." + error;
                    }
                }
                if (message.configuration_id != null && $Object.hasOwnProperty.call(message, "configuration_id")) {
                    properties._configuration_id = 1;
                    if (!$util.isString(message.configuration_id))
                        return "configuration_id: string expected";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a LiveConfiguration message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.LiveConfiguration
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.LiveConfiguration} LiveConfiguration
             */
            LiveConfiguration.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.LiveConfiguration)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.LiveConfiguration: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.LiveConfiguration();
                if (object.config != null) {
                    if (!$util.isObject(object.config))
                        throw $TypeError(".northstar.live.LiveConfiguration.config: object expected");
                    message.config = $root.google.protobuf.Struct.fromObject(object.config, _depth + 1);
                }
                if (object.configuration_id != null)
                    message.configuration_id = $String(object.configuration_id);
                if (object.name != null)
                    message.name = $String(object.name);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.LiveConfiguration.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.LiveConfiguration.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a LiveConfiguration message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.LiveConfiguration
             * @static
             * @param {northstar.live.LiveConfiguration} message LiveConfiguration
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LiveConfiguration.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.config != null && $Object.hasOwnProperty.call(message, "config")) {
                    object.config = $root.google.protobuf.Struct.toObject(message.config, options, _depth + 1);
                    if (options.oneofs)
                        object._config = "config";
                }
                if (message.configuration_id != null && $Object.hasOwnProperty.call(message, "configuration_id")) {
                    object.configuration_id = message.configuration_id;
                    if (options.oneofs)
                        object._configuration_id = "configuration_id";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    object.name = message.name;
                    if (options.oneofs)
                        object._name = "name";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this LiveConfiguration to JSON.
             * @function toJSON
             * @memberof northstar.live.LiveConfiguration
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LiveConfiguration.prototype.toJSON = function() {
                return LiveConfiguration.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for LiveConfiguration
             * @function getTypeUrl
             * @memberof northstar.live.LiveConfiguration
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            LiveConfiguration.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.LiveConfiguration";
            };

            return LiveConfiguration;
        })();

        live.MaterialRequest = (function() {

            /**
             * Properties of a MaterialRequest.
             * @typedef {Object} northstar.live.MaterialRequest.$Properties
             * @property {google.protobuf.Struct.$Properties|null} [candidate] MaterialRequest candidate
             * @property {string|null} [request_id] MaterialRequest request_id
             * @property {"candidate"} [_candidate] MaterialRequest _candidate
             * @property {"request_id"} [_request_id] MaterialRequest _request_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a MaterialRequest.
             * @memberof northstar.live
             * @interface IMaterialRequest
             * @augments northstar.live.MaterialRequest.$Properties
             * @deprecated Use northstar.live.MaterialRequest.$Properties instead.
             */

            /**
             * Narrowed shape of a MaterialRequest.
             * @typedef {{
             *   candidate?: google.protobuf.Struct.$Shape|null;
             *   request_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _candidate?: undefined; candidate?: null }|{ _candidate?: "candidate"; candidate: google.protobuf.Struct.$Shape })
             * ) & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * )} northstar.live.MaterialRequest.$Shape
             */

            /**
             * Constructs a new MaterialRequest.
             * @memberof northstar.live
             * @classdesc Represents a MaterialRequest.
             * @constructor
             * @param {northstar.live.MaterialRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const MaterialRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * MaterialRequest candidate.
             * @member {google.protobuf.Struct.$Properties|null|undefined} candidate
             * @memberof northstar.live.MaterialRequest
             * @instance
             */
            MaterialRequest.prototype.candidate = null;

            /**
             * MaterialRequest request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.live.MaterialRequest
             * @instance
             */
            MaterialRequest.prototype.request_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * MaterialRequest _candidate.
             * @member {"candidate"|undefined} _candidate
             * @memberof northstar.live.MaterialRequest
             * @instance
             */
            $Object.defineProperty(MaterialRequest.prototype, "_candidate", {
                get: $util.oneOfGetter($oneOfFields = ["candidate"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * MaterialRequest _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.live.MaterialRequest
             * @instance
             */
            $Object.defineProperty(MaterialRequest.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new MaterialRequest instance using the specified properties.
             * @function create
             * @memberof northstar.live.MaterialRequest
             * @static
             * @param {northstar.live.MaterialRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.live.MaterialRequest} MaterialRequest instance
             * @type {{
             *   (properties: northstar.live.MaterialRequest.$Shape): northstar.live.MaterialRequest & northstar.live.MaterialRequest.$Shape;
             *   (properties?: northstar.live.MaterialRequest.$Properties): northstar.live.MaterialRequest;
             * }}
             */
            MaterialRequest.create = function(properties) {
                return new MaterialRequest(properties);
            };

            /**
             * Encodes the specified MaterialRequest message. Does not implicitly {@link northstar.live.MaterialRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.MaterialRequest
             * @static
             * @param {northstar.live.MaterialRequest.$Properties} message MaterialRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MaterialRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.candidate != null && $Object.hasOwnProperty.call(message, "candidate"))
                    $root.google.protobuf.Struct.encode(message.candidate, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.request_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a MaterialRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.MaterialRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.MaterialRequest & northstar.live.MaterialRequest.$Shape} MaterialRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MaterialRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.MaterialRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.candidate = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.candidate);
                            message._candidate = "candidate";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a MaterialRequest message.
             * @function verify
             * @memberof northstar.live.MaterialRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MaterialRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.candidate != null && $Object.hasOwnProperty.call(message, "candidate")) {
                    properties._candidate = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.candidate, _depth + 1);
                        if (error)
                            return "candidate." + error;
                    }
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                return null;
            };

            /**
             * Creates a MaterialRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.MaterialRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.MaterialRequest} MaterialRequest
             */
            MaterialRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.MaterialRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.MaterialRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.MaterialRequest();
                if (object.candidate != null) {
                    if (!$util.isObject(object.candidate))
                        throw $TypeError(".northstar.live.MaterialRequest.candidate: object expected");
                    message.candidate = $root.google.protobuf.Struct.fromObject(object.candidate, _depth + 1);
                }
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                return message;
            };

            /**
             * Creates a plain object from a MaterialRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.MaterialRequest
             * @static
             * @param {northstar.live.MaterialRequest} message MaterialRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MaterialRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.candidate != null && $Object.hasOwnProperty.call(message, "candidate")) {
                    object.candidate = $root.google.protobuf.Struct.toObject(message.candidate, options, _depth + 1);
                    if (options.oneofs)
                        object._candidate = "candidate";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                return object;
            };

            /**
             * Converts this MaterialRequest to JSON.
             * @function toJSON
             * @memberof northstar.live.MaterialRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MaterialRequest.prototype.toJSON = function() {
                return MaterialRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for MaterialRequest
             * @function getTypeUrl
             * @memberof northstar.live.MaterialRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            MaterialRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.MaterialRequest";
            };

            return MaterialRequest;
        })();

        live.OpeningBudget = (function() {

            /**
             * Properties of an OpeningBudget.
             * @typedef {Object} northstar.live.OpeningBudget.$Properties
             * @property {string|null} [budget_id] OpeningBudget budget_id
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] OpeningBudget evidence_fields
             * @property {"budget_id"} [_budget_id] OpeningBudget _budget_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an OpeningBudget.
             * @memberof northstar.live
             * @interface IOpeningBudget
             * @augments northstar.live.OpeningBudget.$Properties
             * @deprecated Use northstar.live.OpeningBudget.$Properties instead.
             */

            /**
             * Narrowed shape of an OpeningBudget.
             * @typedef {{
             *   budget_id?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _budget_id?: undefined; budget_id?: null }|{ _budget_id?: "budget_id"; budget_id: string })
             * )} northstar.live.OpeningBudget.$Shape
             */

            /**
             * Constructs a new OpeningBudget.
             * @memberof northstar.live
             * @classdesc Represents an OpeningBudget.
             * @constructor
             * @param {northstar.live.OpeningBudget.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const OpeningBudget = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * OpeningBudget budget_id.
             * @member {string|null|undefined} budget_id
             * @memberof northstar.live.OpeningBudget
             * @instance
             */
            OpeningBudget.prototype.budget_id = null;

            /**
             * OpeningBudget evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.OpeningBudget
             * @instance
             */
            OpeningBudget.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * OpeningBudget _budget_id.
             * @member {"budget_id"|undefined} _budget_id
             * @memberof northstar.live.OpeningBudget
             * @instance
             */
            $Object.defineProperty(OpeningBudget.prototype, "_budget_id", {
                get: $util.oneOfGetter($oneOfFields = ["budget_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new OpeningBudget instance using the specified properties.
             * @function create
             * @memberof northstar.live.OpeningBudget
             * @static
             * @param {northstar.live.OpeningBudget.$Properties=} [properties] Properties to set
             * @returns {northstar.live.OpeningBudget} OpeningBudget instance
             * @type {{
             *   (properties: northstar.live.OpeningBudget.$Shape): northstar.live.OpeningBudget & northstar.live.OpeningBudget.$Shape;
             *   (properties?: northstar.live.OpeningBudget.$Properties): northstar.live.OpeningBudget;
             * }}
             */
            OpeningBudget.create = function(properties) {
                return new OpeningBudget(properties);
            };

            /**
             * Encodes the specified OpeningBudget message. Does not implicitly {@link northstar.live.OpeningBudget.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.OpeningBudget
             * @static
             * @param {northstar.live.OpeningBudget.$Properties} message OpeningBudget message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OpeningBudget.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.budget_id != null && $Object.hasOwnProperty.call(message, "budget_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.budget_id);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes an OpeningBudget message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.OpeningBudget
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.OpeningBudget & northstar.live.OpeningBudget.$Shape} OpeningBudget
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OpeningBudget.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.OpeningBudget();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.budget_id = reader.stringVerify();
                            message._budget_id = "budget_id";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies an OpeningBudget message.
             * @function verify
             * @memberof northstar.live.OpeningBudget
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            OpeningBudget.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.budget_id != null && $Object.hasOwnProperty.call(message, "budget_id")) {
                    properties._budget_id = 1;
                    if (!$util.isString(message.budget_id))
                        return "budget_id: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an OpeningBudget message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.OpeningBudget
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.OpeningBudget} OpeningBudget
             */
            OpeningBudget.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.OpeningBudget)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.OpeningBudget: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.OpeningBudget();
                if (object.budget_id != null)
                    message.budget_id = $String(object.budget_id);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.OpeningBudget.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.OpeningBudget.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from an OpeningBudget message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.OpeningBudget
             * @static
             * @param {northstar.live.OpeningBudget} message OpeningBudget
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            OpeningBudget.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.budget_id != null && $Object.hasOwnProperty.call(message, "budget_id")) {
                    object.budget_id = message.budget_id;
                    if (options.oneofs)
                        object._budget_id = "budget_id";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this OpeningBudget to JSON.
             * @function toJSON
             * @memberof northstar.live.OpeningBudget
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            OpeningBudget.prototype.toJSON = function() {
                return OpeningBudget.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for OpeningBudget
             * @function getTypeUrl
             * @memberof northstar.live.OpeningBudget
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            OpeningBudget.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.OpeningBudget";
            };

            return OpeningBudget;
        })();

        live.OpeningBudgetRequest = (function() {

            /**
             * Properties of an OpeningBudgetRequest.
             * @typedef {Object} northstar.live.OpeningBudgetRequest.$Properties
             * @property {string|null} [limit_price] OpeningBudgetRequest limit_price
             * @property {string|null} [order_check_id] OpeningBudgetRequest order_check_id
             * @property {string|null} [request_id] OpeningBudgetRequest request_id
             * @property {number|Long|null} [sequence] OpeningBudgetRequest sequence
             * @property {"limit_price"} [_limit_price] OpeningBudgetRequest _limit_price
             * @property {"order_check_id"} [_order_check_id] OpeningBudgetRequest _order_check_id
             * @property {"request_id"} [_request_id] OpeningBudgetRequest _request_id
             * @property {"sequence"} [_sequence] OpeningBudgetRequest _sequence
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an OpeningBudgetRequest.
             * @memberof northstar.live
             * @interface IOpeningBudgetRequest
             * @augments northstar.live.OpeningBudgetRequest.$Properties
             * @deprecated Use northstar.live.OpeningBudgetRequest.$Properties instead.
             */

            /**
             * Narrowed shape of an OpeningBudgetRequest.
             * @typedef {{
             *   limit_price?: string|null;
             *   order_check_id?: string|null;
             *   request_id?: string|null;
             *   sequence?: number|Long|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _limit_price?: undefined; limit_price?: null }|{ _limit_price?: "limit_price"; limit_price: string })
             * ) & (
             *   ({ _order_check_id?: undefined; order_check_id?: null }|{ _order_check_id?: "order_check_id"; order_check_id: string })
             * ) & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * ) & (
             *   ({ _sequence?: undefined; sequence?: null }|{ _sequence?: "sequence"; sequence: number|Long })
             * )} northstar.live.OpeningBudgetRequest.$Shape
             */

            /**
             * Constructs a new OpeningBudgetRequest.
             * @memberof northstar.live
             * @classdesc Represents an OpeningBudgetRequest.
             * @constructor
             * @param {northstar.live.OpeningBudgetRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const OpeningBudgetRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * OpeningBudgetRequest limit_price.
             * @member {string|null|undefined} limit_price
             * @memberof northstar.live.OpeningBudgetRequest
             * @instance
             */
            OpeningBudgetRequest.prototype.limit_price = null;

            /**
             * OpeningBudgetRequest order_check_id.
             * @member {string|null|undefined} order_check_id
             * @memberof northstar.live.OpeningBudgetRequest
             * @instance
             */
            OpeningBudgetRequest.prototype.order_check_id = null;

            /**
             * OpeningBudgetRequest request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.live.OpeningBudgetRequest
             * @instance
             */
            OpeningBudgetRequest.prototype.request_id = null;

            /**
             * OpeningBudgetRequest sequence.
             * @member {number|Long|null|undefined} sequence
             * @memberof northstar.live.OpeningBudgetRequest
             * @instance
             */
            OpeningBudgetRequest.prototype.sequence = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * OpeningBudgetRequest _limit_price.
             * @member {"limit_price"|undefined} _limit_price
             * @memberof northstar.live.OpeningBudgetRequest
             * @instance
             */
            $Object.defineProperty(OpeningBudgetRequest.prototype, "_limit_price", {
                get: $util.oneOfGetter($oneOfFields = ["limit_price"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * OpeningBudgetRequest _order_check_id.
             * @member {"order_check_id"|undefined} _order_check_id
             * @memberof northstar.live.OpeningBudgetRequest
             * @instance
             */
            $Object.defineProperty(OpeningBudgetRequest.prototype, "_order_check_id", {
                get: $util.oneOfGetter($oneOfFields = ["order_check_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * OpeningBudgetRequest _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.live.OpeningBudgetRequest
             * @instance
             */
            $Object.defineProperty(OpeningBudgetRequest.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * OpeningBudgetRequest _sequence.
             * @member {"sequence"|undefined} _sequence
             * @memberof northstar.live.OpeningBudgetRequest
             * @instance
             */
            $Object.defineProperty(OpeningBudgetRequest.prototype, "_sequence", {
                get: $util.oneOfGetter($oneOfFields = ["sequence"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new OpeningBudgetRequest instance using the specified properties.
             * @function create
             * @memberof northstar.live.OpeningBudgetRequest
             * @static
             * @param {northstar.live.OpeningBudgetRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.live.OpeningBudgetRequest} OpeningBudgetRequest instance
             * @type {{
             *   (properties: northstar.live.OpeningBudgetRequest.$Shape): northstar.live.OpeningBudgetRequest & northstar.live.OpeningBudgetRequest.$Shape;
             *   (properties?: northstar.live.OpeningBudgetRequest.$Properties): northstar.live.OpeningBudgetRequest;
             * }}
             */
            OpeningBudgetRequest.create = function(properties) {
                return new OpeningBudgetRequest(properties);
            };

            /**
             * Encodes the specified OpeningBudgetRequest message. Does not implicitly {@link northstar.live.OpeningBudgetRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.OpeningBudgetRequest
             * @static
             * @param {northstar.live.OpeningBudgetRequest.$Properties} message OpeningBudgetRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OpeningBudgetRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.limit_price != null && $Object.hasOwnProperty.call(message, "limit_price"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.limit_price);
                if (message.order_check_id != null && $Object.hasOwnProperty.call(message, "order_check_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.order_check_id);
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.request_id);
                if (message.sequence != null && $Object.hasOwnProperty.call(message, "sequence"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int64(message.sequence);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes an OpeningBudgetRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.OpeningBudgetRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.OpeningBudgetRequest & northstar.live.OpeningBudgetRequest.$Shape} OpeningBudgetRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OpeningBudgetRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.OpeningBudgetRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.limit_price = reader.stringVerify();
                            message._limit_price = "limit_price";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.order_check_id = reader.stringVerify();
                            message._order_check_id = "order_check_id";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            message.sequence = reader.int64();
                            message._sequence = "sequence";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies an OpeningBudgetRequest message.
             * @function verify
             * @memberof northstar.live.OpeningBudgetRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            OpeningBudgetRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.limit_price != null && $Object.hasOwnProperty.call(message, "limit_price")) {
                    properties._limit_price = 1;
                    if (!$util.isString(message.limit_price))
                        return "limit_price: string expected";
                }
                if (message.order_check_id != null && $Object.hasOwnProperty.call(message, "order_check_id")) {
                    properties._order_check_id = 1;
                    if (!$util.isString(message.order_check_id))
                        return "order_check_id: string expected";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                if (message.sequence != null && $Object.hasOwnProperty.call(message, "sequence")) {
                    properties._sequence = 1;
                    if (!$util.isInteger(message.sequence) && !(message.sequence && $util.isInteger(message.sequence.low) && $util.isInteger(message.sequence.high)))
                        return "sequence: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates an OpeningBudgetRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.OpeningBudgetRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.OpeningBudgetRequest} OpeningBudgetRequest
             */
            OpeningBudgetRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.OpeningBudgetRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.OpeningBudgetRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.OpeningBudgetRequest();
                if (object.limit_price != null)
                    message.limit_price = $String(object.limit_price);
                if (object.order_check_id != null)
                    message.order_check_id = $String(object.order_check_id);
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                if (object.sequence != null)
                    if ($util.Long)
                        message.sequence = $util.Long.fromValue(object.sequence, false);
                    else if (typeof object.sequence === "string")
                        message.sequence = $parseInt(object.sequence, 10);
                    else if (typeof object.sequence === "number")
                        message.sequence = object.sequence;
                    else if (typeof object.sequence === "object")
                        message.sequence = new $util.LongBits(object.sequence.low >>> 0, object.sequence.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from an OpeningBudgetRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.OpeningBudgetRequest
             * @static
             * @param {northstar.live.OpeningBudgetRequest} message OpeningBudgetRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            OpeningBudgetRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.limit_price != null && $Object.hasOwnProperty.call(message, "limit_price")) {
                    object.limit_price = message.limit_price;
                    if (options.oneofs)
                        object._limit_price = "limit_price";
                }
                if (message.order_check_id != null && $Object.hasOwnProperty.call(message, "order_check_id")) {
                    object.order_check_id = message.order_check_id;
                    if (options.oneofs)
                        object._order_check_id = "order_check_id";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                if (message.sequence != null && $Object.hasOwnProperty.call(message, "sequence")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.sequence = typeof message.sequence === "number" ? $BigInt(message.sequence) : $util.Long.fromBits(message.sequence.low >>> 0, message.sequence.high >>> 0, false).toBigInt();
                    else if (typeof message.sequence === "number")
                        object.sequence = options.longs === $String ? $String(message.sequence) : message.sequence;
                    else
                        object.sequence = options.longs === $String ? $util.Long.prototype.toString.call(message.sequence) : options.longs === $Number ? new $util.LongBits(message.sequence.low >>> 0, message.sequence.high >>> 0).toNumber() : message.sequence;
                    if (options.oneofs)
                        object._sequence = "sequence";
                }
                return object;
            };

            /**
             * Converts this OpeningBudgetRequest to JSON.
             * @function toJSON
             * @memberof northstar.live.OpeningBudgetRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            OpeningBudgetRequest.prototype.toJSON = function() {
                return OpeningBudgetRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for OpeningBudgetRequest
             * @function getTypeUrl
             * @memberof northstar.live.OpeningBudgetRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            OpeningBudgetRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.OpeningBudgetRequest";
            };

            return OpeningBudgetRequest;
        })();

        live.OrderCheckRequest = (function() {

            /**
             * Properties of an OrderCheckRequest.
             * @typedef {Object} northstar.live.OrderCheckRequest.$Properties
             * @property {string|null} [position_check_id] OrderCheckRequest position_check_id
             * @property {string|null} [request_id] OrderCheckRequest request_id
             * @property {"position_check_id"} [_position_check_id] OrderCheckRequest _position_check_id
             * @property {"request_id"} [_request_id] OrderCheckRequest _request_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an OrderCheckRequest.
             * @memberof northstar.live
             * @interface IOrderCheckRequest
             * @augments northstar.live.OrderCheckRequest.$Properties
             * @deprecated Use northstar.live.OrderCheckRequest.$Properties instead.
             */

            /**
             * Narrowed shape of an OrderCheckRequest.
             * @typedef {{
             *   position_check_id?: string|null;
             *   request_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _position_check_id?: undefined; position_check_id?: null }|{ _position_check_id?: "position_check_id"; position_check_id: string })
             * ) & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * )} northstar.live.OrderCheckRequest.$Shape
             */

            /**
             * Constructs a new OrderCheckRequest.
             * @memberof northstar.live
             * @classdesc Represents an OrderCheckRequest.
             * @constructor
             * @param {northstar.live.OrderCheckRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const OrderCheckRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * OrderCheckRequest position_check_id.
             * @member {string|null|undefined} position_check_id
             * @memberof northstar.live.OrderCheckRequest
             * @instance
             */
            OrderCheckRequest.prototype.position_check_id = null;

            /**
             * OrderCheckRequest request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.live.OrderCheckRequest
             * @instance
             */
            OrderCheckRequest.prototype.request_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * OrderCheckRequest _position_check_id.
             * @member {"position_check_id"|undefined} _position_check_id
             * @memberof northstar.live.OrderCheckRequest
             * @instance
             */
            $Object.defineProperty(OrderCheckRequest.prototype, "_position_check_id", {
                get: $util.oneOfGetter($oneOfFields = ["position_check_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * OrderCheckRequest _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.live.OrderCheckRequest
             * @instance
             */
            $Object.defineProperty(OrderCheckRequest.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new OrderCheckRequest instance using the specified properties.
             * @function create
             * @memberof northstar.live.OrderCheckRequest
             * @static
             * @param {northstar.live.OrderCheckRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.live.OrderCheckRequest} OrderCheckRequest instance
             * @type {{
             *   (properties: northstar.live.OrderCheckRequest.$Shape): northstar.live.OrderCheckRequest & northstar.live.OrderCheckRequest.$Shape;
             *   (properties?: northstar.live.OrderCheckRequest.$Properties): northstar.live.OrderCheckRequest;
             * }}
             */
            OrderCheckRequest.create = function(properties) {
                return new OrderCheckRequest(properties);
            };

            /**
             * Encodes the specified OrderCheckRequest message. Does not implicitly {@link northstar.live.OrderCheckRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.OrderCheckRequest
             * @static
             * @param {northstar.live.OrderCheckRequest.$Properties} message OrderCheckRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OrderCheckRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.position_check_id != null && $Object.hasOwnProperty.call(message, "position_check_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.position_check_id);
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.request_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes an OrderCheckRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.OrderCheckRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.OrderCheckRequest & northstar.live.OrderCheckRequest.$Shape} OrderCheckRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OrderCheckRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.OrderCheckRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.position_check_id = reader.stringVerify();
                            message._position_check_id = "position_check_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies an OrderCheckRequest message.
             * @function verify
             * @memberof northstar.live.OrderCheckRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            OrderCheckRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.position_check_id != null && $Object.hasOwnProperty.call(message, "position_check_id")) {
                    properties._position_check_id = 1;
                    if (!$util.isString(message.position_check_id))
                        return "position_check_id: string expected";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                return null;
            };

            /**
             * Creates an OrderCheckRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.OrderCheckRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.OrderCheckRequest} OrderCheckRequest
             */
            OrderCheckRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.OrderCheckRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.OrderCheckRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.OrderCheckRequest();
                if (object.position_check_id != null)
                    message.position_check_id = $String(object.position_check_id);
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                return message;
            };

            /**
             * Creates a plain object from an OrderCheckRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.OrderCheckRequest
             * @static
             * @param {northstar.live.OrderCheckRequest} message OrderCheckRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            OrderCheckRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.position_check_id != null && $Object.hasOwnProperty.call(message, "position_check_id")) {
                    object.position_check_id = message.position_check_id;
                    if (options.oneofs)
                        object._position_check_id = "position_check_id";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                return object;
            };

            /**
             * Converts this OrderCheckRequest to JSON.
             * @function toJSON
             * @memberof northstar.live.OrderCheckRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            OrderCheckRequest.prototype.toJSON = function() {
                return OrderCheckRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for OrderCheckRequest
             * @function getTypeUrl
             * @memberof northstar.live.OrderCheckRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            OrderCheckRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.OrderCheckRequest";
            };

            return OrderCheckRequest;
        })();

        live.PositionCheckRequest = (function() {

            /**
             * Properties of a PositionCheckRequest.
             * @typedef {Object} northstar.live.PositionCheckRequest.$Properties
             * @property {string|null} [entry_id] PositionCheckRequest entry_id
             * @property {string|null} [query_batch_id] PositionCheckRequest query_batch_id
             * @property {string|null} [request_id] PositionCheckRequest request_id
             * @property {"entry_id"} [_entry_id] PositionCheckRequest _entry_id
             * @property {"query_batch_id"} [_query_batch_id] PositionCheckRequest _query_batch_id
             * @property {"request_id"} [_request_id] PositionCheckRequest _request_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a PositionCheckRequest.
             * @memberof northstar.live
             * @interface IPositionCheckRequest
             * @augments northstar.live.PositionCheckRequest.$Properties
             * @deprecated Use northstar.live.PositionCheckRequest.$Properties instead.
             */

            /**
             * Narrowed shape of a PositionCheckRequest.
             * @typedef {{
             *   entry_id?: string|null;
             *   query_batch_id?: string|null;
             *   request_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _entry_id?: undefined; entry_id?: null }|{ _entry_id?: "entry_id"; entry_id: string })
             * ) & (
             *   ({ _query_batch_id?: undefined; query_batch_id?: null }|{ _query_batch_id?: "query_batch_id"; query_batch_id: string })
             * ) & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * )} northstar.live.PositionCheckRequest.$Shape
             */

            /**
             * Constructs a new PositionCheckRequest.
             * @memberof northstar.live
             * @classdesc Represents a PositionCheckRequest.
             * @constructor
             * @param {northstar.live.PositionCheckRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const PositionCheckRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * PositionCheckRequest entry_id.
             * @member {string|null|undefined} entry_id
             * @memberof northstar.live.PositionCheckRequest
             * @instance
             */
            PositionCheckRequest.prototype.entry_id = null;

            /**
             * PositionCheckRequest query_batch_id.
             * @member {string|null|undefined} query_batch_id
             * @memberof northstar.live.PositionCheckRequest
             * @instance
             */
            PositionCheckRequest.prototype.query_batch_id = null;

            /**
             * PositionCheckRequest request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.live.PositionCheckRequest
             * @instance
             */
            PositionCheckRequest.prototype.request_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * PositionCheckRequest _entry_id.
             * @member {"entry_id"|undefined} _entry_id
             * @memberof northstar.live.PositionCheckRequest
             * @instance
             */
            $Object.defineProperty(PositionCheckRequest.prototype, "_entry_id", {
                get: $util.oneOfGetter($oneOfFields = ["entry_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * PositionCheckRequest _query_batch_id.
             * @member {"query_batch_id"|undefined} _query_batch_id
             * @memberof northstar.live.PositionCheckRequest
             * @instance
             */
            $Object.defineProperty(PositionCheckRequest.prototype, "_query_batch_id", {
                get: $util.oneOfGetter($oneOfFields = ["query_batch_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * PositionCheckRequest _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.live.PositionCheckRequest
             * @instance
             */
            $Object.defineProperty(PositionCheckRequest.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PositionCheckRequest instance using the specified properties.
             * @function create
             * @memberof northstar.live.PositionCheckRequest
             * @static
             * @param {northstar.live.PositionCheckRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.live.PositionCheckRequest} PositionCheckRequest instance
             * @type {{
             *   (properties: northstar.live.PositionCheckRequest.$Shape): northstar.live.PositionCheckRequest & northstar.live.PositionCheckRequest.$Shape;
             *   (properties?: northstar.live.PositionCheckRequest.$Properties): northstar.live.PositionCheckRequest;
             * }}
             */
            PositionCheckRequest.create = function(properties) {
                return new PositionCheckRequest(properties);
            };

            /**
             * Encodes the specified PositionCheckRequest message. Does not implicitly {@link northstar.live.PositionCheckRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.PositionCheckRequest
             * @static
             * @param {northstar.live.PositionCheckRequest.$Properties} message PositionCheckRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PositionCheckRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.entry_id != null && $Object.hasOwnProperty.call(message, "entry_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.entry_id);
                if (message.query_batch_id != null && $Object.hasOwnProperty.call(message, "query_batch_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.query_batch_id);
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.request_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a PositionCheckRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.PositionCheckRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.PositionCheckRequest & northstar.live.PositionCheckRequest.$Shape} PositionCheckRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PositionCheckRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.PositionCheckRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.entry_id = reader.stringVerify();
                            message._entry_id = "entry_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.query_batch_id = reader.stringVerify();
                            message._query_batch_id = "query_batch_id";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a PositionCheckRequest message.
             * @function verify
             * @memberof northstar.live.PositionCheckRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PositionCheckRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.entry_id != null && $Object.hasOwnProperty.call(message, "entry_id")) {
                    properties._entry_id = 1;
                    if (!$util.isString(message.entry_id))
                        return "entry_id: string expected";
                }
                if (message.query_batch_id != null && $Object.hasOwnProperty.call(message, "query_batch_id")) {
                    properties._query_batch_id = 1;
                    if (!$util.isString(message.query_batch_id))
                        return "query_batch_id: string expected";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                return null;
            };

            /**
             * Creates a PositionCheckRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.PositionCheckRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.PositionCheckRequest} PositionCheckRequest
             */
            PositionCheckRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.PositionCheckRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.PositionCheckRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.PositionCheckRequest();
                if (object.entry_id != null)
                    message.entry_id = $String(object.entry_id);
                if (object.query_batch_id != null)
                    message.query_batch_id = $String(object.query_batch_id);
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                return message;
            };

            /**
             * Creates a plain object from a PositionCheckRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.PositionCheckRequest
             * @static
             * @param {northstar.live.PositionCheckRequest} message PositionCheckRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PositionCheckRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.entry_id != null && $Object.hasOwnProperty.call(message, "entry_id")) {
                    object.entry_id = message.entry_id;
                    if (options.oneofs)
                        object._entry_id = "entry_id";
                }
                if (message.query_batch_id != null && $Object.hasOwnProperty.call(message, "query_batch_id")) {
                    object.query_batch_id = message.query_batch_id;
                    if (options.oneofs)
                        object._query_batch_id = "query_batch_id";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                return object;
            };

            /**
             * Converts this PositionCheckRequest to JSON.
             * @function toJSON
             * @memberof northstar.live.PositionCheckRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PositionCheckRequest.prototype.toJSON = function() {
                return PositionCheckRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for PositionCheckRequest
             * @function getTypeUrl
             * @memberof northstar.live.PositionCheckRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            PositionCheckRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.PositionCheckRequest";
            };

            return PositionCheckRequest;
        })();

        live.PositionEntry = (function() {

            /**
             * Properties of a PositionEntry.
             * @typedef {Object} northstar.live.PositionEntry.$Properties
             * @property {string|null} [entry_id] PositionEntry entry_id
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] PositionEntry evidence_fields
             * @property {"entry_id"} [_entry_id] PositionEntry _entry_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a PositionEntry.
             * @memberof northstar.live
             * @interface IPositionEntry
             * @augments northstar.live.PositionEntry.$Properties
             * @deprecated Use northstar.live.PositionEntry.$Properties instead.
             */

            /**
             * Narrowed shape of a PositionEntry.
             * @typedef {{
             *   entry_id?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _entry_id?: undefined; entry_id?: null }|{ _entry_id?: "entry_id"; entry_id: string })
             * )} northstar.live.PositionEntry.$Shape
             */

            /**
             * Constructs a new PositionEntry.
             * @memberof northstar.live
             * @classdesc Represents a PositionEntry.
             * @constructor
             * @param {northstar.live.PositionEntry.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const PositionEntry = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * PositionEntry entry_id.
             * @member {string|null|undefined} entry_id
             * @memberof northstar.live.PositionEntry
             * @instance
             */
            PositionEntry.prototype.entry_id = null;

            /**
             * PositionEntry evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.PositionEntry
             * @instance
             */
            PositionEntry.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * PositionEntry _entry_id.
             * @member {"entry_id"|undefined} _entry_id
             * @memberof northstar.live.PositionEntry
             * @instance
             */
            $Object.defineProperty(PositionEntry.prototype, "_entry_id", {
                get: $util.oneOfGetter($oneOfFields = ["entry_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PositionEntry instance using the specified properties.
             * @function create
             * @memberof northstar.live.PositionEntry
             * @static
             * @param {northstar.live.PositionEntry.$Properties=} [properties] Properties to set
             * @returns {northstar.live.PositionEntry} PositionEntry instance
             * @type {{
             *   (properties: northstar.live.PositionEntry.$Shape): northstar.live.PositionEntry & northstar.live.PositionEntry.$Shape;
             *   (properties?: northstar.live.PositionEntry.$Properties): northstar.live.PositionEntry;
             * }}
             */
            PositionEntry.create = function(properties) {
                return new PositionEntry(properties);
            };

            /**
             * Encodes the specified PositionEntry message. Does not implicitly {@link northstar.live.PositionEntry.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.PositionEntry
             * @static
             * @param {northstar.live.PositionEntry.$Properties} message PositionEntry message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PositionEntry.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.entry_id != null && $Object.hasOwnProperty.call(message, "entry_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.entry_id);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a PositionEntry message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.PositionEntry
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.PositionEntry & northstar.live.PositionEntry.$Shape} PositionEntry
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PositionEntry.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.PositionEntry();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.entry_id = reader.stringVerify();
                            message._entry_id = "entry_id";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a PositionEntry message.
             * @function verify
             * @memberof northstar.live.PositionEntry
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PositionEntry.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.entry_id != null && $Object.hasOwnProperty.call(message, "entry_id")) {
                    properties._entry_id = 1;
                    if (!$util.isString(message.entry_id))
                        return "entry_id: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a PositionEntry message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.PositionEntry
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.PositionEntry} PositionEntry
             */
            PositionEntry.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.PositionEntry)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.PositionEntry: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.PositionEntry();
                if (object.entry_id != null)
                    message.entry_id = $String(object.entry_id);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.PositionEntry.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.PositionEntry.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a PositionEntry message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.PositionEntry
             * @static
             * @param {northstar.live.PositionEntry} message PositionEntry
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PositionEntry.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.entry_id != null && $Object.hasOwnProperty.call(message, "entry_id")) {
                    object.entry_id = message.entry_id;
                    if (options.oneofs)
                        object._entry_id = "entry_id";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this PositionEntry to JSON.
             * @function toJSON
             * @memberof northstar.live.PositionEntry
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PositionEntry.prototype.toJSON = function() {
                return PositionEntry.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for PositionEntry
             * @function getTypeUrl
             * @memberof northstar.live.PositionEntry
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            PositionEntry.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.PositionEntry";
            };

            return PositionEntry;
        })();

        live.PositionEntryRequest = (function() {

            /**
             * Properties of a PositionEntryRequest.
             * @typedef {Object} northstar.live.PositionEntryRequest.$Properties
             * @property {string|null} [baseline_id] PositionEntryRequest baseline_id
             * @property {string|null} [request_id] PositionEntryRequest request_id
             * @property {string|null} [source_batch_id] PositionEntryRequest source_batch_id
             * @property {"baseline_id"} [_baseline_id] PositionEntryRequest _baseline_id
             * @property {"request_id"} [_request_id] PositionEntryRequest _request_id
             * @property {"source_batch_id"} [_source_batch_id] PositionEntryRequest _source_batch_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a PositionEntryRequest.
             * @memberof northstar.live
             * @interface IPositionEntryRequest
             * @augments northstar.live.PositionEntryRequest.$Properties
             * @deprecated Use northstar.live.PositionEntryRequest.$Properties instead.
             */

            /**
             * Narrowed shape of a PositionEntryRequest.
             * @typedef {{
             *   baseline_id?: string|null;
             *   request_id?: string|null;
             *   source_batch_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _baseline_id?: undefined; baseline_id?: null }|{ _baseline_id?: "baseline_id"; baseline_id: string })
             * ) & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * ) & (
             *   ({ _source_batch_id?: undefined; source_batch_id?: null }|{ _source_batch_id?: "source_batch_id"; source_batch_id: string })
             * )} northstar.live.PositionEntryRequest.$Shape
             */

            /**
             * Constructs a new PositionEntryRequest.
             * @memberof northstar.live
             * @classdesc Represents a PositionEntryRequest.
             * @constructor
             * @param {northstar.live.PositionEntryRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const PositionEntryRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * PositionEntryRequest baseline_id.
             * @member {string|null|undefined} baseline_id
             * @memberof northstar.live.PositionEntryRequest
             * @instance
             */
            PositionEntryRequest.prototype.baseline_id = null;

            /**
             * PositionEntryRequest request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.live.PositionEntryRequest
             * @instance
             */
            PositionEntryRequest.prototype.request_id = null;

            /**
             * PositionEntryRequest source_batch_id.
             * @member {string|null|undefined} source_batch_id
             * @memberof northstar.live.PositionEntryRequest
             * @instance
             */
            PositionEntryRequest.prototype.source_batch_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * PositionEntryRequest _baseline_id.
             * @member {"baseline_id"|undefined} _baseline_id
             * @memberof northstar.live.PositionEntryRequest
             * @instance
             */
            $Object.defineProperty(PositionEntryRequest.prototype, "_baseline_id", {
                get: $util.oneOfGetter($oneOfFields = ["baseline_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * PositionEntryRequest _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.live.PositionEntryRequest
             * @instance
             */
            $Object.defineProperty(PositionEntryRequest.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * PositionEntryRequest _source_batch_id.
             * @member {"source_batch_id"|undefined} _source_batch_id
             * @memberof northstar.live.PositionEntryRequest
             * @instance
             */
            $Object.defineProperty(PositionEntryRequest.prototype, "_source_batch_id", {
                get: $util.oneOfGetter($oneOfFields = ["source_batch_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PositionEntryRequest instance using the specified properties.
             * @function create
             * @memberof northstar.live.PositionEntryRequest
             * @static
             * @param {northstar.live.PositionEntryRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.live.PositionEntryRequest} PositionEntryRequest instance
             * @type {{
             *   (properties: northstar.live.PositionEntryRequest.$Shape): northstar.live.PositionEntryRequest & northstar.live.PositionEntryRequest.$Shape;
             *   (properties?: northstar.live.PositionEntryRequest.$Properties): northstar.live.PositionEntryRequest;
             * }}
             */
            PositionEntryRequest.create = function(properties) {
                return new PositionEntryRequest(properties);
            };

            /**
             * Encodes the specified PositionEntryRequest message. Does not implicitly {@link northstar.live.PositionEntryRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.PositionEntryRequest
             * @static
             * @param {northstar.live.PositionEntryRequest.$Properties} message PositionEntryRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PositionEntryRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.baseline_id);
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.request_id);
                if (message.source_batch_id != null && $Object.hasOwnProperty.call(message, "source_batch_id"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.source_batch_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a PositionEntryRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.PositionEntryRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.PositionEntryRequest & northstar.live.PositionEntryRequest.$Shape} PositionEntryRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PositionEntryRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.PositionEntryRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.baseline_id = reader.stringVerify();
                            message._baseline_id = "baseline_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.source_batch_id = reader.stringVerify();
                            message._source_batch_id = "source_batch_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a PositionEntryRequest message.
             * @function verify
             * @memberof northstar.live.PositionEntryRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PositionEntryRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id")) {
                    properties._baseline_id = 1;
                    if (!$util.isString(message.baseline_id))
                        return "baseline_id: string expected";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                if (message.source_batch_id != null && $Object.hasOwnProperty.call(message, "source_batch_id")) {
                    properties._source_batch_id = 1;
                    if (!$util.isString(message.source_batch_id))
                        return "source_batch_id: string expected";
                }
                return null;
            };

            /**
             * Creates a PositionEntryRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.PositionEntryRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.PositionEntryRequest} PositionEntryRequest
             */
            PositionEntryRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.PositionEntryRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.PositionEntryRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.PositionEntryRequest();
                if (object.baseline_id != null)
                    message.baseline_id = $String(object.baseline_id);
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                if (object.source_batch_id != null)
                    message.source_batch_id = $String(object.source_batch_id);
                return message;
            };

            /**
             * Creates a plain object from a PositionEntryRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.PositionEntryRequest
             * @static
             * @param {northstar.live.PositionEntryRequest} message PositionEntryRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PositionEntryRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id")) {
                    object.baseline_id = message.baseline_id;
                    if (options.oneofs)
                        object._baseline_id = "baseline_id";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                if (message.source_batch_id != null && $Object.hasOwnProperty.call(message, "source_batch_id")) {
                    object.source_batch_id = message.source_batch_id;
                    if (options.oneofs)
                        object._source_batch_id = "source_batch_id";
                }
                return object;
            };

            /**
             * Converts this PositionEntryRequest to JSON.
             * @function toJSON
             * @memberof northstar.live.PositionEntryRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PositionEntryRequest.prototype.toJSON = function() {
                return PositionEntryRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for PositionEntryRequest
             * @function getTypeUrl
             * @memberof northstar.live.PositionEntryRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            PositionEntryRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.PositionEntryRequest";
            };

            return PositionEntryRequest;
        })();

        live.QueryRecord = (function() {

            /**
             * Properties of a QueryRecord.
             * @typedef {Object} northstar.live.QueryRecord.$Properties
             * @property {string|null} [batch_id] QueryRecord batch_id
             * @property {string|null} [instrument] QueryRecord instrument
             * @property {string|null} [status] QueryRecord status
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] QueryRecord evidence_fields
             * @property {"batch_id"} [_batch_id] QueryRecord _batch_id
             * @property {"instrument"} [_instrument] QueryRecord _instrument
             * @property {"status"} [_status] QueryRecord _status
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a QueryRecord.
             * @memberof northstar.live
             * @interface IQueryRecord
             * @augments northstar.live.QueryRecord.$Properties
             * @deprecated Use northstar.live.QueryRecord.$Properties instead.
             */

            /**
             * Narrowed shape of a QueryRecord.
             * @typedef {{
             *   batch_id?: string|null;
             *   instrument?: string|null;
             *   status?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _batch_id?: undefined; batch_id?: null }|{ _batch_id?: "batch_id"; batch_id: string })
             * ) & (
             *   ({ _instrument?: undefined; instrument?: null }|{ _instrument?: "instrument"; instrument: string })
             * ) & (
             *   ({ _status?: undefined; status?: null }|{ _status?: "status"; status: string })
             * )} northstar.live.QueryRecord.$Shape
             */

            /**
             * Constructs a new QueryRecord.
             * @memberof northstar.live
             * @classdesc Represents a QueryRecord.
             * @constructor
             * @param {northstar.live.QueryRecord.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const QueryRecord = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * QueryRecord batch_id.
             * @member {string|null|undefined} batch_id
             * @memberof northstar.live.QueryRecord
             * @instance
             */
            QueryRecord.prototype.batch_id = null;

            /**
             * QueryRecord instrument.
             * @member {string|null|undefined} instrument
             * @memberof northstar.live.QueryRecord
             * @instance
             */
            QueryRecord.prototype.instrument = null;

            /**
             * QueryRecord status.
             * @member {string|null|undefined} status
             * @memberof northstar.live.QueryRecord
             * @instance
             */
            QueryRecord.prototype.status = null;

            /**
             * QueryRecord evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.QueryRecord
             * @instance
             */
            QueryRecord.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * QueryRecord _batch_id.
             * @member {"batch_id"|undefined} _batch_id
             * @memberof northstar.live.QueryRecord
             * @instance
             */
            $Object.defineProperty(QueryRecord.prototype, "_batch_id", {
                get: $util.oneOfGetter($oneOfFields = ["batch_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * QueryRecord _instrument.
             * @member {"instrument"|undefined} _instrument
             * @memberof northstar.live.QueryRecord
             * @instance
             */
            $Object.defineProperty(QueryRecord.prototype, "_instrument", {
                get: $util.oneOfGetter($oneOfFields = ["instrument"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * QueryRecord _status.
             * @member {"status"|undefined} _status
             * @memberof northstar.live.QueryRecord
             * @instance
             */
            $Object.defineProperty(QueryRecord.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new QueryRecord instance using the specified properties.
             * @function create
             * @memberof northstar.live.QueryRecord
             * @static
             * @param {northstar.live.QueryRecord.$Properties=} [properties] Properties to set
             * @returns {northstar.live.QueryRecord} QueryRecord instance
             * @type {{
             *   (properties: northstar.live.QueryRecord.$Shape): northstar.live.QueryRecord & northstar.live.QueryRecord.$Shape;
             *   (properties?: northstar.live.QueryRecord.$Properties): northstar.live.QueryRecord;
             * }}
             */
            QueryRecord.create = function(properties) {
                return new QueryRecord(properties);
            };

            /**
             * Encodes the specified QueryRecord message. Does not implicitly {@link northstar.live.QueryRecord.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.QueryRecord
             * @static
             * @param {northstar.live.QueryRecord.$Properties} message QueryRecord message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            QueryRecord.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.batch_id != null && $Object.hasOwnProperty.call(message, "batch_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.batch_id);
                if (message.instrument != null && $Object.hasOwnProperty.call(message, "instrument"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.instrument);
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.status);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a QueryRecord message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.QueryRecord
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.QueryRecord & northstar.live.QueryRecord.$Shape} QueryRecord
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            QueryRecord.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.QueryRecord();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.batch_id = reader.stringVerify();
                            message._batch_id = "batch_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.instrument = reader.stringVerify();
                            message._instrument = "instrument";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.status = reader.stringVerify();
                            message._status = "status";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a QueryRecord message.
             * @function verify
             * @memberof northstar.live.QueryRecord
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            QueryRecord.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.batch_id != null && $Object.hasOwnProperty.call(message, "batch_id")) {
                    properties._batch_id = 1;
                    if (!$util.isString(message.batch_id))
                        return "batch_id: string expected";
                }
                if (message.instrument != null && $Object.hasOwnProperty.call(message, "instrument")) {
                    properties._instrument = 1;
                    if (!$util.isString(message.instrument))
                        return "instrument: string expected";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (!$util.isString(message.status))
                        return "status: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a QueryRecord message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.QueryRecord
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.QueryRecord} QueryRecord
             */
            QueryRecord.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.QueryRecord)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.QueryRecord: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.QueryRecord();
                if (object.batch_id != null)
                    message.batch_id = $String(object.batch_id);
                if (object.instrument != null)
                    message.instrument = $String(object.instrument);
                if (object.status != null)
                    message.status = $String(object.status);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.QueryRecord.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.QueryRecord.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a QueryRecord message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.QueryRecord
             * @static
             * @param {northstar.live.QueryRecord} message QueryRecord
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            QueryRecord.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.batch_id != null && $Object.hasOwnProperty.call(message, "batch_id")) {
                    object.batch_id = message.batch_id;
                    if (options.oneofs)
                        object._batch_id = "batch_id";
                }
                if (message.instrument != null && $Object.hasOwnProperty.call(message, "instrument")) {
                    object.instrument = message.instrument;
                    if (options.oneofs)
                        object._instrument = "instrument";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    object.status = message.status;
                    if (options.oneofs)
                        object._status = "status";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this QueryRecord to JSON.
             * @function toJSON
             * @memberof northstar.live.QueryRecord
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            QueryRecord.prototype.toJSON = function() {
                return QueryRecord.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for QueryRecord
             * @function getTypeUrl
             * @memberof northstar.live.QueryRecord
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            QueryRecord.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.QueryRecord";
            };

            return QueryRecord;
        })();

        live.QueryRequest = (function() {

            /**
             * Properties of a QueryRequest.
             * @typedef {Object} northstar.live.QueryRequest.$Properties
             * @property {string|null} [instrument] QueryRequest instrument
             * @property {string|null} [request_id] QueryRequest request_id
             * @property {"instrument"} [_instrument] QueryRequest _instrument
             * @property {"request_id"} [_request_id] QueryRequest _request_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a QueryRequest.
             * @memberof northstar.live
             * @interface IQueryRequest
             * @augments northstar.live.QueryRequest.$Properties
             * @deprecated Use northstar.live.QueryRequest.$Properties instead.
             */

            /**
             * Narrowed shape of a QueryRequest.
             * @typedef {{
             *   instrument?: string|null;
             *   request_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _instrument?: undefined; instrument?: null }|{ _instrument?: "instrument"; instrument: string })
             * ) & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * )} northstar.live.QueryRequest.$Shape
             */

            /**
             * Constructs a new QueryRequest.
             * @memberof northstar.live
             * @classdesc Represents a QueryRequest.
             * @constructor
             * @param {northstar.live.QueryRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const QueryRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * QueryRequest instrument.
             * @member {string|null|undefined} instrument
             * @memberof northstar.live.QueryRequest
             * @instance
             */
            QueryRequest.prototype.instrument = null;

            /**
             * QueryRequest request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.live.QueryRequest
             * @instance
             */
            QueryRequest.prototype.request_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * QueryRequest _instrument.
             * @member {"instrument"|undefined} _instrument
             * @memberof northstar.live.QueryRequest
             * @instance
             */
            $Object.defineProperty(QueryRequest.prototype, "_instrument", {
                get: $util.oneOfGetter($oneOfFields = ["instrument"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * QueryRequest _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.live.QueryRequest
             * @instance
             */
            $Object.defineProperty(QueryRequest.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new QueryRequest instance using the specified properties.
             * @function create
             * @memberof northstar.live.QueryRequest
             * @static
             * @param {northstar.live.QueryRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.live.QueryRequest} QueryRequest instance
             * @type {{
             *   (properties: northstar.live.QueryRequest.$Shape): northstar.live.QueryRequest & northstar.live.QueryRequest.$Shape;
             *   (properties?: northstar.live.QueryRequest.$Properties): northstar.live.QueryRequest;
             * }}
             */
            QueryRequest.create = function(properties) {
                return new QueryRequest(properties);
            };

            /**
             * Encodes the specified QueryRequest message. Does not implicitly {@link northstar.live.QueryRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.QueryRequest
             * @static
             * @param {northstar.live.QueryRequest.$Properties} message QueryRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            QueryRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.instrument != null && $Object.hasOwnProperty.call(message, "instrument"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.instrument);
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.request_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a QueryRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.QueryRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.QueryRequest & northstar.live.QueryRequest.$Shape} QueryRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            QueryRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.QueryRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.instrument = reader.stringVerify();
                            message._instrument = "instrument";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a QueryRequest message.
             * @function verify
             * @memberof northstar.live.QueryRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            QueryRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.instrument != null && $Object.hasOwnProperty.call(message, "instrument")) {
                    properties._instrument = 1;
                    if (!$util.isString(message.instrument))
                        return "instrument: string expected";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                return null;
            };

            /**
             * Creates a QueryRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.QueryRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.QueryRequest} QueryRequest
             */
            QueryRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.QueryRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.QueryRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.QueryRequest();
                if (object.instrument != null)
                    message.instrument = $String(object.instrument);
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                return message;
            };

            /**
             * Creates a plain object from a QueryRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.QueryRequest
             * @static
             * @param {northstar.live.QueryRequest} message QueryRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            QueryRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.instrument != null && $Object.hasOwnProperty.call(message, "instrument")) {
                    object.instrument = message.instrument;
                    if (options.oneofs)
                        object._instrument = "instrument";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                return object;
            };

            /**
             * Converts this QueryRequest to JSON.
             * @function toJSON
             * @memberof northstar.live.QueryRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            QueryRequest.prototype.toJSON = function() {
                return QueryRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for QueryRequest
             * @function getTypeUrl
             * @memberof northstar.live.QueryRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            QueryRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.QueryRequest";
            };

            return QueryRequest;
        })();

        live.Readiness = (function() {

            /**
             * Properties of a Readiness.
             * @typedef {Object} northstar.live.Readiness.$Properties
             * @property {string|null} [status] Readiness status
             * @property {"status"} [_status] Readiness _status
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Readiness.
             * @memberof northstar.live
             * @interface IReadiness
             * @augments northstar.live.Readiness.$Properties
             * @deprecated Use northstar.live.Readiness.$Properties instead.
             */

            /**
             * Narrowed shape of a Readiness.
             * @typedef {{
             *   status?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _status?: undefined; status?: null }|{ _status?: "status"; status: string })
             * )} northstar.live.Readiness.$Shape
             */

            /**
             * Constructs a new Readiness.
             * @memberof northstar.live
             * @classdesc Represents a Readiness.
             * @constructor
             * @param {northstar.live.Readiness.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const Readiness = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Readiness status.
             * @member {string|null|undefined} status
             * @memberof northstar.live.Readiness
             * @instance
             */
            Readiness.prototype.status = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * Readiness _status.
             * @member {"status"|undefined} _status
             * @memberof northstar.live.Readiness
             * @instance
             */
            $Object.defineProperty(Readiness.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new Readiness instance using the specified properties.
             * @function create
             * @memberof northstar.live.Readiness
             * @static
             * @param {northstar.live.Readiness.$Properties=} [properties] Properties to set
             * @returns {northstar.live.Readiness} Readiness instance
             * @type {{
             *   (properties: northstar.live.Readiness.$Shape): northstar.live.Readiness & northstar.live.Readiness.$Shape;
             *   (properties?: northstar.live.Readiness.$Properties): northstar.live.Readiness;
             * }}
             */
            Readiness.create = function(properties) {
                return new Readiness(properties);
            };

            /**
             * Encodes the specified Readiness message. Does not implicitly {@link northstar.live.Readiness.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.Readiness
             * @static
             * @param {northstar.live.Readiness.$Properties} message Readiness message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Readiness.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a Readiness message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.Readiness
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.Readiness & northstar.live.Readiness.$Shape} Readiness
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Readiness.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.Readiness();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.status = reader.stringVerify();
                            message._status = "status";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a Readiness message.
             * @function verify
             * @memberof northstar.live.Readiness
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Readiness.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (!$util.isString(message.status))
                        return "status: string expected";
                }
                return null;
            };

            /**
             * Creates a Readiness message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.Readiness
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.Readiness} Readiness
             */
            Readiness.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.Readiness)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.Readiness: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.Readiness();
                if (object.status != null)
                    message.status = $String(object.status);
                return message;
            };

            /**
             * Creates a plain object from a Readiness message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.Readiness
             * @static
             * @param {northstar.live.Readiness} message Readiness
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Readiness.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    object.status = message.status;
                    if (options.oneofs)
                        object._status = "status";
                }
                return object;
            };

            /**
             * Converts this Readiness to JSON.
             * @function toJSON
             * @memberof northstar.live.Readiness
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Readiness.prototype.toJSON = function() {
                return Readiness.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Readiness
             * @function getTypeUrl
             * @memberof northstar.live.Readiness
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Readiness.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.Readiness";
            };

            return Readiness;
        })();

        live.RuntimeStatus = (function() {

            /**
             * Properties of a RuntimeStatus.
             * @typedef {Object} northstar.live.RuntimeStatus.$Properties
             * @property {boolean|null} [cancel_sending] RuntimeStatus cancel_sending
             * @property {boolean|null} [control_available] RuntimeStatus control_available
             * @property {string|null} [observed_at] RuntimeStatus observed_at
             * @property {boolean|null} [order_sending] RuntimeStatus order_sending
             * @property {number|Long|null} [pid] RuntimeStatus pid
             * @property {string|null} [protocol] RuntimeStatus protocol
             * @property {string|null} [release] RuntimeStatus release
             * @property {string|null} [runtime_id] RuntimeStatus runtime_id
             * @property {string|null} [started_at] RuntimeStatus started_at
             * @property {string|null} [status] RuntimeStatus status
             * @property {"cancel_sending"} [_cancel_sending] RuntimeStatus _cancel_sending
             * @property {"control_available"} [_control_available] RuntimeStatus _control_available
             * @property {"observed_at"} [_observed_at] RuntimeStatus _observed_at
             * @property {"order_sending"} [_order_sending] RuntimeStatus _order_sending
             * @property {"pid"} [_pid] RuntimeStatus _pid
             * @property {"protocol"} [_protocol] RuntimeStatus _protocol
             * @property {"release"} [_release] RuntimeStatus _release
             * @property {"runtime_id"} [_runtime_id] RuntimeStatus _runtime_id
             * @property {"started_at"} [_started_at] RuntimeStatus _started_at
             * @property {"status"} [_status] RuntimeStatus _status
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a RuntimeStatus.
             * @memberof northstar.live
             * @interface IRuntimeStatus
             * @augments northstar.live.RuntimeStatus.$Properties
             * @deprecated Use northstar.live.RuntimeStatus.$Properties instead.
             */

            /**
             * Narrowed shape of a RuntimeStatus.
             * @typedef {{
             *   cancel_sending?: boolean|null;
             *   control_available?: boolean|null;
             *   observed_at?: string|null;
             *   order_sending?: boolean|null;
             *   pid?: number|Long|null;
             *   protocol?: string|null;
             *   release?: string|null;
             *   runtime_id?: string|null;
             *   started_at?: string|null;
             *   status?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _cancel_sending?: undefined; cancel_sending?: null }|{ _cancel_sending?: "cancel_sending"; cancel_sending: boolean })
             * ) & (
             *   ({ _control_available?: undefined; control_available?: null }|{ _control_available?: "control_available"; control_available: boolean })
             * ) & (
             *   ({ _observed_at?: undefined; observed_at?: null }|{ _observed_at?: "observed_at"; observed_at: string })
             * ) & (
             *   ({ _order_sending?: undefined; order_sending?: null }|{ _order_sending?: "order_sending"; order_sending: boolean })
             * ) & (
             *   ({ _pid?: undefined; pid?: null }|{ _pid?: "pid"; pid: number|Long })
             * ) & (
             *   ({ _protocol?: undefined; protocol?: null }|{ _protocol?: "protocol"; protocol: string })
             * ) & (
             *   ({ _release?: undefined; release?: null }|{ _release?: "release"; release: string })
             * ) & (
             *   ({ _runtime_id?: undefined; runtime_id?: null }|{ _runtime_id?: "runtime_id"; runtime_id: string })
             * ) & (
             *   ({ _started_at?: undefined; started_at?: null }|{ _started_at?: "started_at"; started_at: string })
             * ) & (
             *   ({ _status?: undefined; status?: null }|{ _status?: "status"; status: string })
             * )} northstar.live.RuntimeStatus.$Shape
             */

            /**
             * Constructs a new RuntimeStatus.
             * @memberof northstar.live
             * @classdesc Represents a RuntimeStatus.
             * @constructor
             * @param {northstar.live.RuntimeStatus.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const RuntimeStatus = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * RuntimeStatus cancel_sending.
             * @member {boolean|null|undefined} cancel_sending
             * @memberof northstar.live.RuntimeStatus
             * @instance
             */
            RuntimeStatus.prototype.cancel_sending = null;

            /**
             * RuntimeStatus control_available.
             * @member {boolean|null|undefined} control_available
             * @memberof northstar.live.RuntimeStatus
             * @instance
             */
            RuntimeStatus.prototype.control_available = null;

            /**
             * RuntimeStatus observed_at.
             * @member {string|null|undefined} observed_at
             * @memberof northstar.live.RuntimeStatus
             * @instance
             */
            RuntimeStatus.prototype.observed_at = null;

            /**
             * RuntimeStatus order_sending.
             * @member {boolean|null|undefined} order_sending
             * @memberof northstar.live.RuntimeStatus
             * @instance
             */
            RuntimeStatus.prototype.order_sending = null;

            /**
             * RuntimeStatus pid.
             * @member {number|Long|null|undefined} pid
             * @memberof northstar.live.RuntimeStatus
             * @instance
             */
            RuntimeStatus.prototype.pid = null;

            /**
             * RuntimeStatus protocol.
             * @member {string|null|undefined} protocol
             * @memberof northstar.live.RuntimeStatus
             * @instance
             */
            RuntimeStatus.prototype.protocol = null;

            /**
             * RuntimeStatus release.
             * @member {string|null|undefined} release
             * @memberof northstar.live.RuntimeStatus
             * @instance
             */
            RuntimeStatus.prototype.release = null;

            /**
             * RuntimeStatus runtime_id.
             * @member {string|null|undefined} runtime_id
             * @memberof northstar.live.RuntimeStatus
             * @instance
             */
            RuntimeStatus.prototype.runtime_id = null;

            /**
             * RuntimeStatus started_at.
             * @member {string|null|undefined} started_at
             * @memberof northstar.live.RuntimeStatus
             * @instance
             */
            RuntimeStatus.prototype.started_at = null;

            /**
             * RuntimeStatus status.
             * @member {string|null|undefined} status
             * @memberof northstar.live.RuntimeStatus
             * @instance
             */
            RuntimeStatus.prototype.status = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * RuntimeStatus _cancel_sending.
             * @member {"cancel_sending"|undefined} _cancel_sending
             * @memberof northstar.live.RuntimeStatus
             * @instance
             */
            $Object.defineProperty(RuntimeStatus.prototype, "_cancel_sending", {
                get: $util.oneOfGetter($oneOfFields = ["cancel_sending"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RuntimeStatus _control_available.
             * @member {"control_available"|undefined} _control_available
             * @memberof northstar.live.RuntimeStatus
             * @instance
             */
            $Object.defineProperty(RuntimeStatus.prototype, "_control_available", {
                get: $util.oneOfGetter($oneOfFields = ["control_available"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RuntimeStatus _observed_at.
             * @member {"observed_at"|undefined} _observed_at
             * @memberof northstar.live.RuntimeStatus
             * @instance
             */
            $Object.defineProperty(RuntimeStatus.prototype, "_observed_at", {
                get: $util.oneOfGetter($oneOfFields = ["observed_at"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RuntimeStatus _order_sending.
             * @member {"order_sending"|undefined} _order_sending
             * @memberof northstar.live.RuntimeStatus
             * @instance
             */
            $Object.defineProperty(RuntimeStatus.prototype, "_order_sending", {
                get: $util.oneOfGetter($oneOfFields = ["order_sending"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RuntimeStatus _pid.
             * @member {"pid"|undefined} _pid
             * @memberof northstar.live.RuntimeStatus
             * @instance
             */
            $Object.defineProperty(RuntimeStatus.prototype, "_pid", {
                get: $util.oneOfGetter($oneOfFields = ["pid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RuntimeStatus _protocol.
             * @member {"protocol"|undefined} _protocol
             * @memberof northstar.live.RuntimeStatus
             * @instance
             */
            $Object.defineProperty(RuntimeStatus.prototype, "_protocol", {
                get: $util.oneOfGetter($oneOfFields = ["protocol"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RuntimeStatus _release.
             * @member {"release"|undefined} _release
             * @memberof northstar.live.RuntimeStatus
             * @instance
             */
            $Object.defineProperty(RuntimeStatus.prototype, "_release", {
                get: $util.oneOfGetter($oneOfFields = ["release"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RuntimeStatus _runtime_id.
             * @member {"runtime_id"|undefined} _runtime_id
             * @memberof northstar.live.RuntimeStatus
             * @instance
             */
            $Object.defineProperty(RuntimeStatus.prototype, "_runtime_id", {
                get: $util.oneOfGetter($oneOfFields = ["runtime_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RuntimeStatus _started_at.
             * @member {"started_at"|undefined} _started_at
             * @memberof northstar.live.RuntimeStatus
             * @instance
             */
            $Object.defineProperty(RuntimeStatus.prototype, "_started_at", {
                get: $util.oneOfGetter($oneOfFields = ["started_at"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * RuntimeStatus _status.
             * @member {"status"|undefined} _status
             * @memberof northstar.live.RuntimeStatus
             * @instance
             */
            $Object.defineProperty(RuntimeStatus.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new RuntimeStatus instance using the specified properties.
             * @function create
             * @memberof northstar.live.RuntimeStatus
             * @static
             * @param {northstar.live.RuntimeStatus.$Properties=} [properties] Properties to set
             * @returns {northstar.live.RuntimeStatus} RuntimeStatus instance
             * @type {{
             *   (properties: northstar.live.RuntimeStatus.$Shape): northstar.live.RuntimeStatus & northstar.live.RuntimeStatus.$Shape;
             *   (properties?: northstar.live.RuntimeStatus.$Properties): northstar.live.RuntimeStatus;
             * }}
             */
            RuntimeStatus.create = function(properties) {
                return new RuntimeStatus(properties);
            };

            /**
             * Encodes the specified RuntimeStatus message. Does not implicitly {@link northstar.live.RuntimeStatus.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.RuntimeStatus
             * @static
             * @param {northstar.live.RuntimeStatus.$Properties} message RuntimeStatus message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RuntimeStatus.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.cancel_sending != null && $Object.hasOwnProperty.call(message, "cancel_sending"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.cancel_sending);
                if (message.control_available != null && $Object.hasOwnProperty.call(message, "control_available"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.control_available);
                if (message.observed_at != null && $Object.hasOwnProperty.call(message, "observed_at"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.observed_at);
                if (message.order_sending != null && $Object.hasOwnProperty.call(message, "order_sending"))
                    writer.uint32(/* id 4, wireType 0 =*/32).bool(message.order_sending);
                if (message.pid != null && $Object.hasOwnProperty.call(message, "pid"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.pid);
                if (message.protocol != null && $Object.hasOwnProperty.call(message, "protocol"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.protocol);
                if (message.release != null && $Object.hasOwnProperty.call(message, "release"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.release);
                if (message.runtime_id != null && $Object.hasOwnProperty.call(message, "runtime_id"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.runtime_id);
                if (message.started_at != null && $Object.hasOwnProperty.call(message, "started_at"))
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.started_at);
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 10, wireType 2 =*/82).string(message.status);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a RuntimeStatus message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.RuntimeStatus
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.RuntimeStatus & northstar.live.RuntimeStatus.$Shape} RuntimeStatus
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RuntimeStatus.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.RuntimeStatus();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            message.cancel_sending = reader.bool();
                            message._cancel_sending = "cancel_sending";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            message.control_available = reader.bool();
                            message._control_available = "control_available";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.observed_at = reader.stringVerify();
                            message._observed_at = "observed_at";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            message.order_sending = reader.bool();
                            message._order_sending = "order_sending";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            message.pid = reader.int64();
                            message._pid = "pid";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.protocol = reader.stringVerify();
                            message._protocol = "protocol";
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            message.release = reader.stringVerify();
                            message._release = "release";
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            message.runtime_id = reader.stringVerify();
                            message._runtime_id = "runtime_id";
                            continue;
                        }
                    case 9: {
                            if (wireType !== 2)
                                break;
                            message.started_at = reader.stringVerify();
                            message._started_at = "started_at";
                            continue;
                        }
                    case 10: {
                            if (wireType !== 2)
                                break;
                            message.status = reader.stringVerify();
                            message._status = "status";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a RuntimeStatus message.
             * @function verify
             * @memberof northstar.live.RuntimeStatus
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RuntimeStatus.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.cancel_sending != null && $Object.hasOwnProperty.call(message, "cancel_sending")) {
                    properties._cancel_sending = 1;
                    if (typeof message.cancel_sending !== "boolean")
                        return "cancel_sending: boolean expected";
                }
                if (message.control_available != null && $Object.hasOwnProperty.call(message, "control_available")) {
                    properties._control_available = 1;
                    if (typeof message.control_available !== "boolean")
                        return "control_available: boolean expected";
                }
                if (message.observed_at != null && $Object.hasOwnProperty.call(message, "observed_at")) {
                    properties._observed_at = 1;
                    if (!$util.isString(message.observed_at))
                        return "observed_at: string expected";
                }
                if (message.order_sending != null && $Object.hasOwnProperty.call(message, "order_sending")) {
                    properties._order_sending = 1;
                    if (typeof message.order_sending !== "boolean")
                        return "order_sending: boolean expected";
                }
                if (message.pid != null && $Object.hasOwnProperty.call(message, "pid")) {
                    properties._pid = 1;
                    if (!$util.isInteger(message.pid) && !(message.pid && $util.isInteger(message.pid.low) && $util.isInteger(message.pid.high)))
                        return "pid: integer|Long expected";
                }
                if (message.protocol != null && $Object.hasOwnProperty.call(message, "protocol")) {
                    properties._protocol = 1;
                    if (!$util.isString(message.protocol))
                        return "protocol: string expected";
                }
                if (message.release != null && $Object.hasOwnProperty.call(message, "release")) {
                    properties._release = 1;
                    if (!$util.isString(message.release))
                        return "release: string expected";
                }
                if (message.runtime_id != null && $Object.hasOwnProperty.call(message, "runtime_id")) {
                    properties._runtime_id = 1;
                    if (!$util.isString(message.runtime_id))
                        return "runtime_id: string expected";
                }
                if (message.started_at != null && $Object.hasOwnProperty.call(message, "started_at")) {
                    properties._started_at = 1;
                    if (!$util.isString(message.started_at))
                        return "started_at: string expected";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (!$util.isString(message.status))
                        return "status: string expected";
                }
                return null;
            };

            /**
             * Creates a RuntimeStatus message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.RuntimeStatus
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.RuntimeStatus} RuntimeStatus
             */
            RuntimeStatus.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.RuntimeStatus)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.RuntimeStatus: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.RuntimeStatus();
                if (object.cancel_sending != null)
                    message.cancel_sending = $Boolean(object.cancel_sending);
                if (object.control_available != null)
                    message.control_available = $Boolean(object.control_available);
                if (object.observed_at != null)
                    message.observed_at = $String(object.observed_at);
                if (object.order_sending != null)
                    message.order_sending = $Boolean(object.order_sending);
                if (object.pid != null)
                    if ($util.Long)
                        message.pid = $util.Long.fromValue(object.pid, false);
                    else if (typeof object.pid === "string")
                        message.pid = $parseInt(object.pid, 10);
                    else if (typeof object.pid === "number")
                        message.pid = object.pid;
                    else if (typeof object.pid === "object")
                        message.pid = new $util.LongBits(object.pid.low >>> 0, object.pid.high >>> 0).toNumber();
                if (object.protocol != null)
                    message.protocol = $String(object.protocol);
                if (object.release != null)
                    message.release = $String(object.release);
                if (object.runtime_id != null)
                    message.runtime_id = $String(object.runtime_id);
                if (object.started_at != null)
                    message.started_at = $String(object.started_at);
                if (object.status != null)
                    message.status = $String(object.status);
                return message;
            };

            /**
             * Creates a plain object from a RuntimeStatus message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.RuntimeStatus
             * @static
             * @param {northstar.live.RuntimeStatus} message RuntimeStatus
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RuntimeStatus.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.cancel_sending != null && $Object.hasOwnProperty.call(message, "cancel_sending")) {
                    object.cancel_sending = message.cancel_sending;
                    if (options.oneofs)
                        object._cancel_sending = "cancel_sending";
                }
                if (message.control_available != null && $Object.hasOwnProperty.call(message, "control_available")) {
                    object.control_available = message.control_available;
                    if (options.oneofs)
                        object._control_available = "control_available";
                }
                if (message.observed_at != null && $Object.hasOwnProperty.call(message, "observed_at")) {
                    object.observed_at = message.observed_at;
                    if (options.oneofs)
                        object._observed_at = "observed_at";
                }
                if (message.order_sending != null && $Object.hasOwnProperty.call(message, "order_sending")) {
                    object.order_sending = message.order_sending;
                    if (options.oneofs)
                        object._order_sending = "order_sending";
                }
                if (message.pid != null && $Object.hasOwnProperty.call(message, "pid")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.pid = typeof message.pid === "number" ? $BigInt(message.pid) : $util.Long.fromBits(message.pid.low >>> 0, message.pid.high >>> 0, false).toBigInt();
                    else if (typeof message.pid === "number")
                        object.pid = options.longs === $String ? $String(message.pid) : message.pid;
                    else
                        object.pid = options.longs === $String ? $util.Long.prototype.toString.call(message.pid) : options.longs === $Number ? new $util.LongBits(message.pid.low >>> 0, message.pid.high >>> 0).toNumber() : message.pid;
                    if (options.oneofs)
                        object._pid = "pid";
                }
                if (message.protocol != null && $Object.hasOwnProperty.call(message, "protocol")) {
                    object.protocol = message.protocol;
                    if (options.oneofs)
                        object._protocol = "protocol";
                }
                if (message.release != null && $Object.hasOwnProperty.call(message, "release")) {
                    object.release = message.release;
                    if (options.oneofs)
                        object._release = "release";
                }
                if (message.runtime_id != null && $Object.hasOwnProperty.call(message, "runtime_id")) {
                    object.runtime_id = message.runtime_id;
                    if (options.oneofs)
                        object._runtime_id = "runtime_id";
                }
                if (message.started_at != null && $Object.hasOwnProperty.call(message, "started_at")) {
                    object.started_at = message.started_at;
                    if (options.oneofs)
                        object._started_at = "started_at";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    object.status = message.status;
                    if (options.oneofs)
                        object._status = "status";
                }
                return object;
            };

            /**
             * Converts this RuntimeStatus to JSON.
             * @function toJSON
             * @memberof northstar.live.RuntimeStatus
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RuntimeStatus.prototype.toJSON = function() {
                return RuntimeStatus.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for RuntimeStatus
             * @function getTypeUrl
             * @memberof northstar.live.RuntimeStatus
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            RuntimeStatus.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.RuntimeStatus";
            };

            return RuntimeStatus;
        })();

        live.StrategyMaterial = (function() {

            /**
             * Properties of a StrategyMaterial.
             * @typedef {Object} northstar.live.StrategyMaterial.$Properties
             * @property {string|null} [candidate_id] StrategyMaterial candidate_id
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] StrategyMaterial evidence_fields
             * @property {"candidate_id"} [_candidate_id] StrategyMaterial _candidate_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StrategyMaterial.
             * @memberof northstar.live
             * @interface IStrategyMaterial
             * @augments northstar.live.StrategyMaterial.$Properties
             * @deprecated Use northstar.live.StrategyMaterial.$Properties instead.
             */

            /**
             * Narrowed shape of a StrategyMaterial.
             * @typedef {{
             *   candidate_id?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _candidate_id?: undefined; candidate_id?: null }|{ _candidate_id?: "candidate_id"; candidate_id: string })
             * )} northstar.live.StrategyMaterial.$Shape
             */

            /**
             * Constructs a new StrategyMaterial.
             * @memberof northstar.live
             * @classdesc Represents a StrategyMaterial.
             * @constructor
             * @param {northstar.live.StrategyMaterial.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StrategyMaterial = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StrategyMaterial candidate_id.
             * @member {string|null|undefined} candidate_id
             * @memberof northstar.live.StrategyMaterial
             * @instance
             */
            StrategyMaterial.prototype.candidate_id = null;

            /**
             * StrategyMaterial evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.StrategyMaterial
             * @instance
             */
            StrategyMaterial.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * StrategyMaterial _candidate_id.
             * @member {"candidate_id"|undefined} _candidate_id
             * @memberof northstar.live.StrategyMaterial
             * @instance
             */
            $Object.defineProperty(StrategyMaterial.prototype, "_candidate_id", {
                get: $util.oneOfGetter($oneOfFields = ["candidate_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StrategyMaterial instance using the specified properties.
             * @function create
             * @memberof northstar.live.StrategyMaterial
             * @static
             * @param {northstar.live.StrategyMaterial.$Properties=} [properties] Properties to set
             * @returns {northstar.live.StrategyMaterial} StrategyMaterial instance
             * @type {{
             *   (properties: northstar.live.StrategyMaterial.$Shape): northstar.live.StrategyMaterial & northstar.live.StrategyMaterial.$Shape;
             *   (properties?: northstar.live.StrategyMaterial.$Properties): northstar.live.StrategyMaterial;
             * }}
             */
            StrategyMaterial.create = function(properties) {
                return new StrategyMaterial(properties);
            };

            /**
             * Encodes the specified StrategyMaterial message. Does not implicitly {@link northstar.live.StrategyMaterial.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.StrategyMaterial
             * @static
             * @param {northstar.live.StrategyMaterial.$Properties} message StrategyMaterial message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StrategyMaterial.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.candidate_id != null && $Object.hasOwnProperty.call(message, "candidate_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.candidate_id);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a StrategyMaterial message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.StrategyMaterial
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.StrategyMaterial & northstar.live.StrategyMaterial.$Shape} StrategyMaterial
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StrategyMaterial.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.StrategyMaterial();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.candidate_id = reader.stringVerify();
                            message._candidate_id = "candidate_id";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a StrategyMaterial message.
             * @function verify
             * @memberof northstar.live.StrategyMaterial
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StrategyMaterial.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.candidate_id != null && $Object.hasOwnProperty.call(message, "candidate_id")) {
                    properties._candidate_id = 1;
                    if (!$util.isString(message.candidate_id))
                        return "candidate_id: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a StrategyMaterial message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.StrategyMaterial
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.StrategyMaterial} StrategyMaterial
             */
            StrategyMaterial.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.StrategyMaterial)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.StrategyMaterial: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.StrategyMaterial();
                if (object.candidate_id != null)
                    message.candidate_id = $String(object.candidate_id);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.StrategyMaterial.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.StrategyMaterial.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a StrategyMaterial message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.StrategyMaterial
             * @static
             * @param {northstar.live.StrategyMaterial} message StrategyMaterial
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StrategyMaterial.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.candidate_id != null && $Object.hasOwnProperty.call(message, "candidate_id")) {
                    object.candidate_id = message.candidate_id;
                    if (options.oneofs)
                        object._candidate_id = "candidate_id";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this StrategyMaterial to JSON.
             * @function toJSON
             * @memberof northstar.live.StrategyMaterial
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StrategyMaterial.prototype.toJSON = function() {
                return StrategyMaterial.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StrategyMaterial
             * @function getTypeUrl
             * @memberof northstar.live.StrategyMaterial
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StrategyMaterial.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.StrategyMaterial";
            };

            return StrategyMaterial;
        })();

        live.StreamAccountProgress = (function() {

            /**
             * Properties of a StreamAccountProgress.
             * @typedef {Object} northstar.live.StreamAccountProgress.$Properties
             * @property {string|null} [status] StreamAccountProgress status
             * @property {number|Long|null} [through_sequence] StreamAccountProgress through_sequence
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] StreamAccountProgress evidence_fields
             * @property {Array.<string>|null} [null_fields] StreamAccountProgress null_fields
             * @property {"status"} [_status] StreamAccountProgress _status
             * @property {"through_sequence"} [_through_sequence] StreamAccountProgress _through_sequence
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StreamAccountProgress.
             * @memberof northstar.live
             * @interface IStreamAccountProgress
             * @augments northstar.live.StreamAccountProgress.$Properties
             * @deprecated Use northstar.live.StreamAccountProgress.$Properties instead.
             */

            /**
             * Narrowed shape of a StreamAccountProgress.
             * @typedef {{
             *   status?: string|null;
             *   through_sequence?: number|Long|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   null_fields?: Array.<string>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _status?: undefined; status?: null }|{ _status?: "status"; status: string })
             * ) & (
             *   ({ _through_sequence?: undefined; through_sequence?: null }|{ _through_sequence?: "through_sequence"; through_sequence: number|Long })
             * )} northstar.live.StreamAccountProgress.$Shape
             */

            /**
             * Constructs a new StreamAccountProgress.
             * @memberof northstar.live
             * @classdesc Represents a StreamAccountProgress.
             * @constructor
             * @param {northstar.live.StreamAccountProgress.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StreamAccountProgress = function (properties) {
                this.evidence_fields = {};
                this.null_fields = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StreamAccountProgress status.
             * @member {string|null|undefined} status
             * @memberof northstar.live.StreamAccountProgress
             * @instance
             */
            StreamAccountProgress.prototype.status = null;

            /**
             * StreamAccountProgress through_sequence.
             * @member {number|Long|null|undefined} through_sequence
             * @memberof northstar.live.StreamAccountProgress
             * @instance
             */
            StreamAccountProgress.prototype.through_sequence = null;

            /**
             * StreamAccountProgress evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.StreamAccountProgress
             * @instance
             */
            StreamAccountProgress.prototype.evidence_fields = $util.emptyObject;

            /**
             * StreamAccountProgress null_fields.
             * @member {Array.<string>} null_fields
             * @memberof northstar.live.StreamAccountProgress
             * @instance
             */
            StreamAccountProgress.prototype.null_fields = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * StreamAccountProgress _status.
             * @member {"status"|undefined} _status
             * @memberof northstar.live.StreamAccountProgress
             * @instance
             */
            $Object.defineProperty(StreamAccountProgress.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StreamAccountProgress _through_sequence.
             * @member {"through_sequence"|undefined} _through_sequence
             * @memberof northstar.live.StreamAccountProgress
             * @instance
             */
            $Object.defineProperty(StreamAccountProgress.prototype, "_through_sequence", {
                get: $util.oneOfGetter($oneOfFields = ["through_sequence"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StreamAccountProgress instance using the specified properties.
             * @function create
             * @memberof northstar.live.StreamAccountProgress
             * @static
             * @param {northstar.live.StreamAccountProgress.$Properties=} [properties] Properties to set
             * @returns {northstar.live.StreamAccountProgress} StreamAccountProgress instance
             * @type {{
             *   (properties: northstar.live.StreamAccountProgress.$Shape): northstar.live.StreamAccountProgress & northstar.live.StreamAccountProgress.$Shape;
             *   (properties?: northstar.live.StreamAccountProgress.$Properties): northstar.live.StreamAccountProgress;
             * }}
             */
            StreamAccountProgress.create = function(properties) {
                return new StreamAccountProgress(properties);
            };

            /**
             * Encodes the specified StreamAccountProgress message. Does not implicitly {@link northstar.live.StreamAccountProgress.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.StreamAccountProgress
             * @static
             * @param {northstar.live.StreamAccountProgress.$Properties} message StreamAccountProgress message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StreamAccountProgress.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
                if (message.through_sequence != null && $Object.hasOwnProperty.call(message, "through_sequence"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.through_sequence);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.null_fields != null && message.null_fields.length)
                    for (let i = 0; i < message.null_fields.length; ++i)
                        writer.uint32(/* id 2046, wireType 2 =*/16370).string(message.null_fields[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a StreamAccountProgress message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.StreamAccountProgress
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.StreamAccountProgress & northstar.live.StreamAccountProgress.$Shape} StreamAccountProgress
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StreamAccountProgress.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.StreamAccountProgress();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.status = reader.stringVerify();
                            message._status = "status";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            message.through_sequence = reader.int64();
                            message._through_sequence = "through_sequence";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    case 2046: {
                            if (wireType !== 2)
                                break;
                            if (!(message.null_fields && message.null_fields.length))
                                message.null_fields = [];
                            message.null_fields.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a StreamAccountProgress message.
             * @function verify
             * @memberof northstar.live.StreamAccountProgress
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StreamAccountProgress.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (!$util.isString(message.status))
                        return "status: string expected";
                }
                if (message.through_sequence != null && $Object.hasOwnProperty.call(message, "through_sequence")) {
                    properties._through_sequence = 1;
                    if (!$util.isInteger(message.through_sequence) && !(message.through_sequence && $util.isInteger(message.through_sequence.low) && $util.isInteger(message.through_sequence.high)))
                        return "through_sequence: integer|Long expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                if (message.null_fields != null && $Object.hasOwnProperty.call(message, "null_fields")) {
                    if (!$Array.isArray(message.null_fields))
                        return "null_fields: array expected";
                    for (let i = 0; i < message.null_fields.length; ++i)
                        if (!$util.isString(message.null_fields[i]))
                            return "null_fields: string[] expected";
                }
                return null;
            };

            /**
             * Creates a StreamAccountProgress message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.StreamAccountProgress
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.StreamAccountProgress} StreamAccountProgress
             */
            StreamAccountProgress.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.StreamAccountProgress)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.StreamAccountProgress: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.StreamAccountProgress();
                if (object.status != null)
                    message.status = $String(object.status);
                if (object.through_sequence != null)
                    if ($util.Long)
                        message.through_sequence = $util.Long.fromValue(object.through_sequence, false);
                    else if (typeof object.through_sequence === "string")
                        message.through_sequence = $parseInt(object.through_sequence, 10);
                    else if (typeof object.through_sequence === "number")
                        message.through_sequence = object.through_sequence;
                    else if (typeof object.through_sequence === "object")
                        message.through_sequence = new $util.LongBits(object.through_sequence.low >>> 0, object.through_sequence.high >>> 0).toNumber();
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.StreamAccountProgress.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.StreamAccountProgress.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                if (object.null_fields) {
                    if (!$Array.isArray(object.null_fields))
                        throw $TypeError(".northstar.live.StreamAccountProgress.null_fields: array expected");
                    message.null_fields = $Array(object.null_fields.length);
                    for (let i = 0; i < object.null_fields.length; ++i)
                        message.null_fields[i] = $String(object.null_fields[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a StreamAccountProgress message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.StreamAccountProgress
             * @static
             * @param {northstar.live.StreamAccountProgress} message StreamAccountProgress
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StreamAccountProgress.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.null_fields = [];
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    object.status = message.status;
                    if (options.oneofs)
                        object._status = "status";
                }
                if (message.through_sequence != null && $Object.hasOwnProperty.call(message, "through_sequence")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.through_sequence = typeof message.through_sequence === "number" ? $BigInt(message.through_sequence) : $util.Long.fromBits(message.through_sequence.low >>> 0, message.through_sequence.high >>> 0, false).toBigInt();
                    else if (typeof message.through_sequence === "number")
                        object.through_sequence = options.longs === $String ? $String(message.through_sequence) : message.through_sequence;
                    else
                        object.through_sequence = options.longs === $String ? $util.Long.prototype.toString.call(message.through_sequence) : options.longs === $Number ? new $util.LongBits(message.through_sequence.low >>> 0, message.through_sequence.high >>> 0).toNumber() : message.through_sequence;
                    if (options.oneofs)
                        object._through_sequence = "through_sequence";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                if (message.null_fields && message.null_fields.length) {
                    object.null_fields = $Array(message.null_fields.length);
                    for (let j = 0; j < message.null_fields.length; ++j)
                        object.null_fields[j] = message.null_fields[j];
                }
                return object;
            };

            /**
             * Converts this StreamAccountProgress to JSON.
             * @function toJSON
             * @memberof northstar.live.StreamAccountProgress
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StreamAccountProgress.prototype.toJSON = function() {
                return StreamAccountProgress.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StreamAccountProgress
             * @function getTypeUrl
             * @memberof northstar.live.StreamAccountProgress
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StreamAccountProgress.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.StreamAccountProgress";
            };

            return StreamAccountProgress;
        })();

        live.StreamArchive = (function() {

            /**
             * Properties of a StreamArchive.
             * @typedef {Object} northstar.live.StreamArchive.$Properties
             * @property {string|null} [source_id] StreamArchive source_id
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] StreamArchive evidence_fields
             * @property {"source_id"} [_source_id] StreamArchive _source_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StreamArchive.
             * @memberof northstar.live
             * @interface IStreamArchive
             * @augments northstar.live.StreamArchive.$Properties
             * @deprecated Use northstar.live.StreamArchive.$Properties instead.
             */

            /**
             * Narrowed shape of a StreamArchive.
             * @typedef {{
             *   source_id?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _source_id?: undefined; source_id?: null }|{ _source_id?: "source_id"; source_id: string })
             * )} northstar.live.StreamArchive.$Shape
             */

            /**
             * Constructs a new StreamArchive.
             * @memberof northstar.live
             * @classdesc Represents a StreamArchive.
             * @constructor
             * @param {northstar.live.StreamArchive.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StreamArchive = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StreamArchive source_id.
             * @member {string|null|undefined} source_id
             * @memberof northstar.live.StreamArchive
             * @instance
             */
            StreamArchive.prototype.source_id = null;

            /**
             * StreamArchive evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.StreamArchive
             * @instance
             */
            StreamArchive.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * StreamArchive _source_id.
             * @member {"source_id"|undefined} _source_id
             * @memberof northstar.live.StreamArchive
             * @instance
             */
            $Object.defineProperty(StreamArchive.prototype, "_source_id", {
                get: $util.oneOfGetter($oneOfFields = ["source_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StreamArchive instance using the specified properties.
             * @function create
             * @memberof northstar.live.StreamArchive
             * @static
             * @param {northstar.live.StreamArchive.$Properties=} [properties] Properties to set
             * @returns {northstar.live.StreamArchive} StreamArchive instance
             * @type {{
             *   (properties: northstar.live.StreamArchive.$Shape): northstar.live.StreamArchive & northstar.live.StreamArchive.$Shape;
             *   (properties?: northstar.live.StreamArchive.$Properties): northstar.live.StreamArchive;
             * }}
             */
            StreamArchive.create = function(properties) {
                return new StreamArchive(properties);
            };

            /**
             * Encodes the specified StreamArchive message. Does not implicitly {@link northstar.live.StreamArchive.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.StreamArchive
             * @static
             * @param {northstar.live.StreamArchive.$Properties} message StreamArchive message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StreamArchive.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.source_id != null && $Object.hasOwnProperty.call(message, "source_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.source_id);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a StreamArchive message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.StreamArchive
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.StreamArchive & northstar.live.StreamArchive.$Shape} StreamArchive
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StreamArchive.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.StreamArchive();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.source_id = reader.stringVerify();
                            message._source_id = "source_id";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a StreamArchive message.
             * @function verify
             * @memberof northstar.live.StreamArchive
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StreamArchive.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.source_id != null && $Object.hasOwnProperty.call(message, "source_id")) {
                    properties._source_id = 1;
                    if (!$util.isString(message.source_id))
                        return "source_id: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a StreamArchive message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.StreamArchive
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.StreamArchive} StreamArchive
             */
            StreamArchive.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.StreamArchive)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.StreamArchive: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.StreamArchive();
                if (object.source_id != null)
                    message.source_id = $String(object.source_id);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.StreamArchive.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.StreamArchive.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a StreamArchive message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.StreamArchive
             * @static
             * @param {northstar.live.StreamArchive} message StreamArchive
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StreamArchive.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.source_id != null && $Object.hasOwnProperty.call(message, "source_id")) {
                    object.source_id = message.source_id;
                    if (options.oneofs)
                        object._source_id = "source_id";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this StreamArchive to JSON.
             * @function toJSON
             * @memberof northstar.live.StreamArchive
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StreamArchive.prototype.toJSON = function() {
                return StreamArchive.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StreamArchive
             * @function getTypeUrl
             * @memberof northstar.live.StreamArchive
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StreamArchive.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.StreamArchive";
            };

            return StreamArchive;
        })();

        live.StreamBinding = (function() {

            /**
             * Properties of a StreamBinding.
             * @typedef {Object} northstar.live.StreamBinding.$Properties
             * @property {northstar.live.StreamBindingRequest.$Properties|null} [request] StreamBinding request
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] StreamBinding evidence_fields
             * @property {"request"} [_request] StreamBinding _request
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StreamBinding.
             * @memberof northstar.live
             * @interface IStreamBinding
             * @augments northstar.live.StreamBinding.$Properties
             * @deprecated Use northstar.live.StreamBinding.$Properties instead.
             */

            /**
             * Narrowed shape of a StreamBinding.
             * @typedef {{
             *   request?: northstar.live.StreamBindingRequest.$Shape|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _request?: undefined; request?: null }|{ _request?: "request"; request: northstar.live.StreamBindingRequest.$Shape })
             * )} northstar.live.StreamBinding.$Shape
             */

            /**
             * Constructs a new StreamBinding.
             * @memberof northstar.live
             * @classdesc Represents a StreamBinding.
             * @constructor
             * @param {northstar.live.StreamBinding.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StreamBinding = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StreamBinding request.
             * @member {northstar.live.StreamBindingRequest.$Properties|null|undefined} request
             * @memberof northstar.live.StreamBinding
             * @instance
             */
            StreamBinding.prototype.request = null;

            /**
             * StreamBinding evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.StreamBinding
             * @instance
             */
            StreamBinding.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * StreamBinding _request.
             * @member {"request"|undefined} _request
             * @memberof northstar.live.StreamBinding
             * @instance
             */
            $Object.defineProperty(StreamBinding.prototype, "_request", {
                get: $util.oneOfGetter($oneOfFields = ["request"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StreamBinding instance using the specified properties.
             * @function create
             * @memberof northstar.live.StreamBinding
             * @static
             * @param {northstar.live.StreamBinding.$Properties=} [properties] Properties to set
             * @returns {northstar.live.StreamBinding} StreamBinding instance
             * @type {{
             *   (properties: northstar.live.StreamBinding.$Shape): northstar.live.StreamBinding & northstar.live.StreamBinding.$Shape;
             *   (properties?: northstar.live.StreamBinding.$Properties): northstar.live.StreamBinding;
             * }}
             */
            StreamBinding.create = function(properties) {
                return new StreamBinding(properties);
            };

            /**
             * Encodes the specified StreamBinding message. Does not implicitly {@link northstar.live.StreamBinding.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.StreamBinding
             * @static
             * @param {northstar.live.StreamBinding.$Properties} message StreamBinding message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StreamBinding.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.request != null && $Object.hasOwnProperty.call(message, "request"))
                    $root.northstar.live.StreamBindingRequest.encode(message.request, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a StreamBinding message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.StreamBinding
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.StreamBinding & northstar.live.StreamBinding.$Shape} StreamBinding
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StreamBinding.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.StreamBinding();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.request = $root.northstar.live.StreamBindingRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.request);
                            message._request = "request";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a StreamBinding message.
             * @function verify
             * @memberof northstar.live.StreamBinding
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StreamBinding.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.request != null && $Object.hasOwnProperty.call(message, "request")) {
                    properties._request = 1;
                    {
                        let error = $root.northstar.live.StreamBindingRequest.verify(message.request, _depth + 1);
                        if (error)
                            return "request." + error;
                    }
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a StreamBinding message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.StreamBinding
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.StreamBinding} StreamBinding
             */
            StreamBinding.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.StreamBinding)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.StreamBinding: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.StreamBinding();
                if (object.request != null) {
                    if (!$util.isObject(object.request))
                        throw $TypeError(".northstar.live.StreamBinding.request: object expected");
                    message.request = $root.northstar.live.StreamBindingRequest.fromObject(object.request, _depth + 1);
                }
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.StreamBinding.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.StreamBinding.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a StreamBinding message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.StreamBinding
             * @static
             * @param {northstar.live.StreamBinding} message StreamBinding
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StreamBinding.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.request != null && $Object.hasOwnProperty.call(message, "request")) {
                    object.request = $root.northstar.live.StreamBindingRequest.toObject(message.request, options, _depth + 1);
                    if (options.oneofs)
                        object._request = "request";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this StreamBinding to JSON.
             * @function toJSON
             * @memberof northstar.live.StreamBinding
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StreamBinding.prototype.toJSON = function() {
                return StreamBinding.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StreamBinding
             * @function getTypeUrl
             * @memberof northstar.live.StreamBinding
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StreamBinding.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.StreamBinding";
            };

            return StreamBinding;
        })();

        live.StreamBindingRequest = (function() {

            /**
             * Properties of a StreamBindingRequest.
             * @typedef {Object} northstar.live.StreamBindingRequest.$Properties
             * @property {string|null} [query_batch_id] StreamBindingRequest query_batch_id
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] StreamBindingRequest evidence_fields
             * @property {"query_batch_id"} [_query_batch_id] StreamBindingRequest _query_batch_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StreamBindingRequest.
             * @memberof northstar.live
             * @interface IStreamBindingRequest
             * @augments northstar.live.StreamBindingRequest.$Properties
             * @deprecated Use northstar.live.StreamBindingRequest.$Properties instead.
             */

            /**
             * Narrowed shape of a StreamBindingRequest.
             * @typedef {{
             *   query_batch_id?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _query_batch_id?: undefined; query_batch_id?: null }|{ _query_batch_id?: "query_batch_id"; query_batch_id: string })
             * )} northstar.live.StreamBindingRequest.$Shape
             */

            /**
             * Constructs a new StreamBindingRequest.
             * @memberof northstar.live
             * @classdesc Represents a StreamBindingRequest.
             * @constructor
             * @param {northstar.live.StreamBindingRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StreamBindingRequest = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StreamBindingRequest query_batch_id.
             * @member {string|null|undefined} query_batch_id
             * @memberof northstar.live.StreamBindingRequest
             * @instance
             */
            StreamBindingRequest.prototype.query_batch_id = null;

            /**
             * StreamBindingRequest evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.StreamBindingRequest
             * @instance
             */
            StreamBindingRequest.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * StreamBindingRequest _query_batch_id.
             * @member {"query_batch_id"|undefined} _query_batch_id
             * @memberof northstar.live.StreamBindingRequest
             * @instance
             */
            $Object.defineProperty(StreamBindingRequest.prototype, "_query_batch_id", {
                get: $util.oneOfGetter($oneOfFields = ["query_batch_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StreamBindingRequest instance using the specified properties.
             * @function create
             * @memberof northstar.live.StreamBindingRequest
             * @static
             * @param {northstar.live.StreamBindingRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.live.StreamBindingRequest} StreamBindingRequest instance
             * @type {{
             *   (properties: northstar.live.StreamBindingRequest.$Shape): northstar.live.StreamBindingRequest & northstar.live.StreamBindingRequest.$Shape;
             *   (properties?: northstar.live.StreamBindingRequest.$Properties): northstar.live.StreamBindingRequest;
             * }}
             */
            StreamBindingRequest.create = function(properties) {
                return new StreamBindingRequest(properties);
            };

            /**
             * Encodes the specified StreamBindingRequest message. Does not implicitly {@link northstar.live.StreamBindingRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.StreamBindingRequest
             * @static
             * @param {northstar.live.StreamBindingRequest.$Properties} message StreamBindingRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StreamBindingRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.query_batch_id != null && $Object.hasOwnProperty.call(message, "query_batch_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.query_batch_id);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a StreamBindingRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.StreamBindingRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.StreamBindingRequest & northstar.live.StreamBindingRequest.$Shape} StreamBindingRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StreamBindingRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.StreamBindingRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.query_batch_id = reader.stringVerify();
                            message._query_batch_id = "query_batch_id";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a StreamBindingRequest message.
             * @function verify
             * @memberof northstar.live.StreamBindingRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StreamBindingRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.query_batch_id != null && $Object.hasOwnProperty.call(message, "query_batch_id")) {
                    properties._query_batch_id = 1;
                    if (!$util.isString(message.query_batch_id))
                        return "query_batch_id: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a StreamBindingRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.StreamBindingRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.StreamBindingRequest} StreamBindingRequest
             */
            StreamBindingRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.StreamBindingRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.StreamBindingRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.StreamBindingRequest();
                if (object.query_batch_id != null)
                    message.query_batch_id = $String(object.query_batch_id);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.StreamBindingRequest.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.StreamBindingRequest.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a StreamBindingRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.StreamBindingRequest
             * @static
             * @param {northstar.live.StreamBindingRequest} message StreamBindingRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StreamBindingRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.query_batch_id != null && $Object.hasOwnProperty.call(message, "query_batch_id")) {
                    object.query_batch_id = message.query_batch_id;
                    if (options.oneofs)
                        object._query_batch_id = "query_batch_id";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this StreamBindingRequest to JSON.
             * @function toJSON
             * @memberof northstar.live.StreamBindingRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StreamBindingRequest.prototype.toJSON = function() {
                return StreamBindingRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StreamBindingRequest
             * @function getTypeUrl
             * @memberof northstar.live.StreamBindingRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StreamBindingRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.StreamBindingRequest";
            };

            return StreamBindingRequest;
        })();

        live.StreamControl = (function() {

            /**
             * Properties of a StreamControl.
             * @typedef {Object} northstar.live.StreamControl.$Properties
             * @property {string|null} [stream_id] StreamControl stream_id
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] StreamControl evidence_fields
             * @property {"stream_id"} [_stream_id] StreamControl _stream_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StreamControl.
             * @memberof northstar.live
             * @interface IStreamControl
             * @augments northstar.live.StreamControl.$Properties
             * @deprecated Use northstar.live.StreamControl.$Properties instead.
             */

            /**
             * Narrowed shape of a StreamControl.
             * @typedef {{
             *   stream_id?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _stream_id?: undefined; stream_id?: null }|{ _stream_id?: "stream_id"; stream_id: string })
             * )} northstar.live.StreamControl.$Shape
             */

            /**
             * Constructs a new StreamControl.
             * @memberof northstar.live
             * @classdesc Represents a StreamControl.
             * @constructor
             * @param {northstar.live.StreamControl.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StreamControl = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StreamControl stream_id.
             * @member {string|null|undefined} stream_id
             * @memberof northstar.live.StreamControl
             * @instance
             */
            StreamControl.prototype.stream_id = null;

            /**
             * StreamControl evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.StreamControl
             * @instance
             */
            StreamControl.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * StreamControl _stream_id.
             * @member {"stream_id"|undefined} _stream_id
             * @memberof northstar.live.StreamControl
             * @instance
             */
            $Object.defineProperty(StreamControl.prototype, "_stream_id", {
                get: $util.oneOfGetter($oneOfFields = ["stream_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StreamControl instance using the specified properties.
             * @function create
             * @memberof northstar.live.StreamControl
             * @static
             * @param {northstar.live.StreamControl.$Properties=} [properties] Properties to set
             * @returns {northstar.live.StreamControl} StreamControl instance
             * @type {{
             *   (properties: northstar.live.StreamControl.$Shape): northstar.live.StreamControl & northstar.live.StreamControl.$Shape;
             *   (properties?: northstar.live.StreamControl.$Properties): northstar.live.StreamControl;
             * }}
             */
            StreamControl.create = function(properties) {
                return new StreamControl(properties);
            };

            /**
             * Encodes the specified StreamControl message. Does not implicitly {@link northstar.live.StreamControl.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.StreamControl
             * @static
             * @param {northstar.live.StreamControl.$Properties} message StreamControl message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StreamControl.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.stream_id != null && $Object.hasOwnProperty.call(message, "stream_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.stream_id);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a StreamControl message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.StreamControl
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.StreamControl & northstar.live.StreamControl.$Shape} StreamControl
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StreamControl.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.StreamControl();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.stream_id = reader.stringVerify();
                            message._stream_id = "stream_id";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a StreamControl message.
             * @function verify
             * @memberof northstar.live.StreamControl
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StreamControl.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.stream_id != null && $Object.hasOwnProperty.call(message, "stream_id")) {
                    properties._stream_id = 1;
                    if (!$util.isString(message.stream_id))
                        return "stream_id: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a StreamControl message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.StreamControl
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.StreamControl} StreamControl
             */
            StreamControl.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.StreamControl)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.StreamControl: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.StreamControl();
                if (object.stream_id != null)
                    message.stream_id = $String(object.stream_id);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.StreamControl.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.StreamControl.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a StreamControl message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.StreamControl
             * @static
             * @param {northstar.live.StreamControl} message StreamControl
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StreamControl.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.stream_id != null && $Object.hasOwnProperty.call(message, "stream_id")) {
                    object.stream_id = message.stream_id;
                    if (options.oneofs)
                        object._stream_id = "stream_id";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this StreamControl to JSON.
             * @function toJSON
             * @memberof northstar.live.StreamControl
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StreamControl.prototype.toJSON = function() {
                return StreamControl.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StreamControl
             * @function getTypeUrl
             * @memberof northstar.live.StreamControl
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StreamControl.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.StreamControl";
            };

            return StreamControl;
        })();

        live.StreamDecision = (function() {

            /**
             * Properties of a StreamDecision.
             * @typedef {Object} northstar.live.StreamDecision.$Properties
             * @property {number|Long|null} [sequence] StreamDecision sequence
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] StreamDecision evidence_fields
             * @property {"sequence"} [_sequence] StreamDecision _sequence
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StreamDecision.
             * @memberof northstar.live
             * @interface IStreamDecision
             * @augments northstar.live.StreamDecision.$Properties
             * @deprecated Use northstar.live.StreamDecision.$Properties instead.
             */

            /**
             * Narrowed shape of a StreamDecision.
             * @typedef {{
             *   sequence?: number|Long|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _sequence?: undefined; sequence?: null }|{ _sequence?: "sequence"; sequence: number|Long })
             * )} northstar.live.StreamDecision.$Shape
             */

            /**
             * Constructs a new StreamDecision.
             * @memberof northstar.live
             * @classdesc Represents a StreamDecision.
             * @constructor
             * @param {northstar.live.StreamDecision.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StreamDecision = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StreamDecision sequence.
             * @member {number|Long|null|undefined} sequence
             * @memberof northstar.live.StreamDecision
             * @instance
             */
            StreamDecision.prototype.sequence = null;

            /**
             * StreamDecision evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.StreamDecision
             * @instance
             */
            StreamDecision.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * StreamDecision _sequence.
             * @member {"sequence"|undefined} _sequence
             * @memberof northstar.live.StreamDecision
             * @instance
             */
            $Object.defineProperty(StreamDecision.prototype, "_sequence", {
                get: $util.oneOfGetter($oneOfFields = ["sequence"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StreamDecision instance using the specified properties.
             * @function create
             * @memberof northstar.live.StreamDecision
             * @static
             * @param {northstar.live.StreamDecision.$Properties=} [properties] Properties to set
             * @returns {northstar.live.StreamDecision} StreamDecision instance
             * @type {{
             *   (properties: northstar.live.StreamDecision.$Shape): northstar.live.StreamDecision & northstar.live.StreamDecision.$Shape;
             *   (properties?: northstar.live.StreamDecision.$Properties): northstar.live.StreamDecision;
             * }}
             */
            StreamDecision.create = function(properties) {
                return new StreamDecision(properties);
            };

            /**
             * Encodes the specified StreamDecision message. Does not implicitly {@link northstar.live.StreamDecision.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.StreamDecision
             * @static
             * @param {northstar.live.StreamDecision.$Properties} message StreamDecision message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StreamDecision.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.sequence != null && $Object.hasOwnProperty.call(message, "sequence"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int64(message.sequence);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a StreamDecision message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.StreamDecision
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.StreamDecision & northstar.live.StreamDecision.$Shape} StreamDecision
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StreamDecision.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.StreamDecision();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            message.sequence = reader.int64();
                            message._sequence = "sequence";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a StreamDecision message.
             * @function verify
             * @memberof northstar.live.StreamDecision
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StreamDecision.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.sequence != null && $Object.hasOwnProperty.call(message, "sequence")) {
                    properties._sequence = 1;
                    if (!$util.isInteger(message.sequence) && !(message.sequence && $util.isInteger(message.sequence.low) && $util.isInteger(message.sequence.high)))
                        return "sequence: integer|Long expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a StreamDecision message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.StreamDecision
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.StreamDecision} StreamDecision
             */
            StreamDecision.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.StreamDecision)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.StreamDecision: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.StreamDecision();
                if (object.sequence != null)
                    if ($util.Long)
                        message.sequence = $util.Long.fromValue(object.sequence, false);
                    else if (typeof object.sequence === "string")
                        message.sequence = $parseInt(object.sequence, 10);
                    else if (typeof object.sequence === "number")
                        message.sequence = object.sequence;
                    else if (typeof object.sequence === "object")
                        message.sequence = new $util.LongBits(object.sequence.low >>> 0, object.sequence.high >>> 0).toNumber();
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.StreamDecision.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.StreamDecision.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a StreamDecision message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.StreamDecision
             * @static
             * @param {northstar.live.StreamDecision} message StreamDecision
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StreamDecision.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.sequence != null && $Object.hasOwnProperty.call(message, "sequence")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.sequence = typeof message.sequence === "number" ? $BigInt(message.sequence) : $util.Long.fromBits(message.sequence.low >>> 0, message.sequence.high >>> 0, false).toBigInt();
                    else if (typeof message.sequence === "number")
                        object.sequence = options.longs === $String ? $String(message.sequence) : message.sequence;
                    else
                        object.sequence = options.longs === $String ? $util.Long.prototype.toString.call(message.sequence) : options.longs === $Number ? new $util.LongBits(message.sequence.low >>> 0, message.sequence.high >>> 0).toNumber() : message.sequence;
                    if (options.oneofs)
                        object._sequence = "sequence";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this StreamDecision to JSON.
             * @function toJSON
             * @memberof northstar.live.StreamDecision
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StreamDecision.prototype.toJSON = function() {
                return StreamDecision.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StreamDecision
             * @function getTypeUrl
             * @memberof northstar.live.StreamDecision
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StreamDecision.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.StreamDecision";
            };

            return StreamDecision;
        })();

        live.StreamDetail = (function() {

            /**
             * Properties of a StreamDetail.
             * @typedef {Object} northstar.live.StreamDetail.$Properties
             * @property {northstar.live.StreamAccountProgress.$Properties|null} [account_progress] StreamDetail account_progress
             * @property {Array.<google.protobuf.Struct.$Properties>|null} [archives] StreamDetail archives
             * @property {northstar.live.StreamBinding.$Properties|null} [binding] StreamDetail binding
             * @property {string|null} [connection] StreamDetail connection
             * @property {number|Long|null} [cursor] StreamDetail cursor
             * @property {boolean|null} [paused] StreamDetail paused
             * @property {number|Long|null} [received] StreamDetail received
             * @property {Array.<northstar.live.StreamStep.$Properties>|null} [steps] StreamDetail steps
             * @property {string|null} [stream_id] StreamDetail stream_id
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] StreamDetail evidence_fields
             * @property {"account_progress"} [_account_progress] StreamDetail _account_progress
             * @property {"binding"} [_binding] StreamDetail _binding
             * @property {"connection"} [_connection] StreamDetail _connection
             * @property {"cursor"} [_cursor] StreamDetail _cursor
             * @property {"paused"} [_paused] StreamDetail _paused
             * @property {"received"} [_received] StreamDetail _received
             * @property {"stream_id"} [_stream_id] StreamDetail _stream_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StreamDetail.
             * @memberof northstar.live
             * @interface IStreamDetail
             * @augments northstar.live.StreamDetail.$Properties
             * @deprecated Use northstar.live.StreamDetail.$Properties instead.
             */

            /**
             * Narrowed shape of a StreamDetail.
             * @typedef {{
             *   account_progress?: northstar.live.StreamAccountProgress.$Shape|null;
             *   archives?: Array.<google.protobuf.Struct.$Shape>|null;
             *   binding?: northstar.live.StreamBinding.$Shape|null;
             *   connection?: string|null;
             *   cursor?: number|Long|null;
             *   paused?: boolean|null;
             *   received?: number|Long|null;
             *   steps?: Array.<northstar.live.StreamStep.$Shape>|null;
             *   stream_id?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _account_progress?: undefined; account_progress?: null }|{ _account_progress?: "account_progress"; account_progress: northstar.live.StreamAccountProgress.$Shape })
             * ) & (
             *   ({ _binding?: undefined; binding?: null }|{ _binding?: "binding"; binding: northstar.live.StreamBinding.$Shape })
             * ) & (
             *   ({ _connection?: undefined; connection?: null }|{ _connection?: "connection"; connection: string })
             * ) & (
             *   ({ _cursor?: undefined; cursor?: null }|{ _cursor?: "cursor"; cursor: number|Long })
             * ) & (
             *   ({ _paused?: undefined; paused?: null }|{ _paused?: "paused"; paused: boolean })
             * ) & (
             *   ({ _received?: undefined; received?: null }|{ _received?: "received"; received: number|Long })
             * ) & (
             *   ({ _stream_id?: undefined; stream_id?: null }|{ _stream_id?: "stream_id"; stream_id: string })
             * )} northstar.live.StreamDetail.$Shape
             */

            /**
             * Constructs a new StreamDetail.
             * @memberof northstar.live
             * @classdesc Represents a StreamDetail.
             * @constructor
             * @param {northstar.live.StreamDetail.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StreamDetail = function (properties) {
                this.archives = [];
                this.steps = [];
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StreamDetail account_progress.
             * @member {northstar.live.StreamAccountProgress.$Properties|null|undefined} account_progress
             * @memberof northstar.live.StreamDetail
             * @instance
             */
            StreamDetail.prototype.account_progress = null;

            /**
             * StreamDetail archives.
             * @member {Array.<google.protobuf.Struct.$Properties>} archives
             * @memberof northstar.live.StreamDetail
             * @instance
             */
            StreamDetail.prototype.archives = $util.emptyArray;

            /**
             * StreamDetail binding.
             * @member {northstar.live.StreamBinding.$Properties|null|undefined} binding
             * @memberof northstar.live.StreamDetail
             * @instance
             */
            StreamDetail.prototype.binding = null;

            /**
             * StreamDetail connection.
             * @member {string|null|undefined} connection
             * @memberof northstar.live.StreamDetail
             * @instance
             */
            StreamDetail.prototype.connection = null;

            /**
             * StreamDetail cursor.
             * @member {number|Long|null|undefined} cursor
             * @memberof northstar.live.StreamDetail
             * @instance
             */
            StreamDetail.prototype.cursor = null;

            /**
             * StreamDetail paused.
             * @member {boolean|null|undefined} paused
             * @memberof northstar.live.StreamDetail
             * @instance
             */
            StreamDetail.prototype.paused = null;

            /**
             * StreamDetail received.
             * @member {number|Long|null|undefined} received
             * @memberof northstar.live.StreamDetail
             * @instance
             */
            StreamDetail.prototype.received = null;

            /**
             * StreamDetail steps.
             * @member {Array.<northstar.live.StreamStep.$Properties>} steps
             * @memberof northstar.live.StreamDetail
             * @instance
             */
            StreamDetail.prototype.steps = $util.emptyArray;

            /**
             * StreamDetail stream_id.
             * @member {string|null|undefined} stream_id
             * @memberof northstar.live.StreamDetail
             * @instance
             */
            StreamDetail.prototype.stream_id = null;

            /**
             * StreamDetail evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.StreamDetail
             * @instance
             */
            StreamDetail.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * StreamDetail _account_progress.
             * @member {"account_progress"|undefined} _account_progress
             * @memberof northstar.live.StreamDetail
             * @instance
             */
            $Object.defineProperty(StreamDetail.prototype, "_account_progress", {
                get: $util.oneOfGetter($oneOfFields = ["account_progress"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StreamDetail _binding.
             * @member {"binding"|undefined} _binding
             * @memberof northstar.live.StreamDetail
             * @instance
             */
            $Object.defineProperty(StreamDetail.prototype, "_binding", {
                get: $util.oneOfGetter($oneOfFields = ["binding"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StreamDetail _connection.
             * @member {"connection"|undefined} _connection
             * @memberof northstar.live.StreamDetail
             * @instance
             */
            $Object.defineProperty(StreamDetail.prototype, "_connection", {
                get: $util.oneOfGetter($oneOfFields = ["connection"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StreamDetail _cursor.
             * @member {"cursor"|undefined} _cursor
             * @memberof northstar.live.StreamDetail
             * @instance
             */
            $Object.defineProperty(StreamDetail.prototype, "_cursor", {
                get: $util.oneOfGetter($oneOfFields = ["cursor"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StreamDetail _paused.
             * @member {"paused"|undefined} _paused
             * @memberof northstar.live.StreamDetail
             * @instance
             */
            $Object.defineProperty(StreamDetail.prototype, "_paused", {
                get: $util.oneOfGetter($oneOfFields = ["paused"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StreamDetail _received.
             * @member {"received"|undefined} _received
             * @memberof northstar.live.StreamDetail
             * @instance
             */
            $Object.defineProperty(StreamDetail.prototype, "_received", {
                get: $util.oneOfGetter($oneOfFields = ["received"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StreamDetail _stream_id.
             * @member {"stream_id"|undefined} _stream_id
             * @memberof northstar.live.StreamDetail
             * @instance
             */
            $Object.defineProperty(StreamDetail.prototype, "_stream_id", {
                get: $util.oneOfGetter($oneOfFields = ["stream_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StreamDetail instance using the specified properties.
             * @function create
             * @memberof northstar.live.StreamDetail
             * @static
             * @param {northstar.live.StreamDetail.$Properties=} [properties] Properties to set
             * @returns {northstar.live.StreamDetail} StreamDetail instance
             * @type {{
             *   (properties: northstar.live.StreamDetail.$Shape): northstar.live.StreamDetail & northstar.live.StreamDetail.$Shape;
             *   (properties?: northstar.live.StreamDetail.$Properties): northstar.live.StreamDetail;
             * }}
             */
            StreamDetail.create = function(properties) {
                return new StreamDetail(properties);
            };

            /**
             * Encodes the specified StreamDetail message. Does not implicitly {@link northstar.live.StreamDetail.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.StreamDetail
             * @static
             * @param {northstar.live.StreamDetail.$Properties} message StreamDetail message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StreamDetail.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.account_progress != null && $Object.hasOwnProperty.call(message, "account_progress"))
                    $root.northstar.live.StreamAccountProgress.encode(message.account_progress, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.archives != null && message.archives.length)
                    for (let i = 0; i < message.archives.length; ++i)
                        $root.google.protobuf.Struct.encode(message.archives[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.binding != null && $Object.hasOwnProperty.call(message, "binding"))
                    $root.northstar.live.StreamBinding.encode(message.binding, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.connection != null && $Object.hasOwnProperty.call(message, "connection"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.connection);
                if (message.cursor != null && $Object.hasOwnProperty.call(message, "cursor"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.cursor);
                if (message.paused != null && $Object.hasOwnProperty.call(message, "paused"))
                    writer.uint32(/* id 6, wireType 0 =*/48).bool(message.paused);
                if (message.received != null && $Object.hasOwnProperty.call(message, "received"))
                    writer.uint32(/* id 7, wireType 0 =*/56).int64(message.received);
                if (message.steps != null && message.steps.length)
                    for (let i = 0; i < message.steps.length; ++i)
                        $root.northstar.live.StreamStep.encode(message.steps[i], writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
                if (message.stream_id != null && $Object.hasOwnProperty.call(message, "stream_id"))
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.stream_id);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a StreamDetail message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.StreamDetail
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.StreamDetail & northstar.live.StreamDetail.$Shape} StreamDetail
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StreamDetail.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.StreamDetail();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.account_progress = $root.northstar.live.StreamAccountProgress.decode(reader, reader.uint32(), $undefined, _depth + 1, message.account_progress);
                            message._account_progress = "account_progress";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.archives && message.archives.length))
                                message.archives = [];
                            message.archives.push($root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.binding = $root.northstar.live.StreamBinding.decode(reader, reader.uint32(), $undefined, _depth + 1, message.binding);
                            message._binding = "binding";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.connection = reader.stringVerify();
                            message._connection = "connection";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            message.cursor = reader.int64();
                            message._cursor = "cursor";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            message.paused = reader.bool();
                            message._paused = "paused";
                            continue;
                        }
                    case 7: {
                            if (wireType !== 0)
                                break;
                            message.received = reader.int64();
                            message._received = "received";
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            if (!(message.steps && message.steps.length))
                                message.steps = [];
                            message.steps.push($root.northstar.live.StreamStep.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 9: {
                            if (wireType !== 2)
                                break;
                            message.stream_id = reader.stringVerify();
                            message._stream_id = "stream_id";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a StreamDetail message.
             * @function verify
             * @memberof northstar.live.StreamDetail
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StreamDetail.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.account_progress != null && $Object.hasOwnProperty.call(message, "account_progress")) {
                    properties._account_progress = 1;
                    {
                        let error = $root.northstar.live.StreamAccountProgress.verify(message.account_progress, _depth + 1);
                        if (error)
                            return "account_progress." + error;
                    }
                }
                if (message.archives != null && $Object.hasOwnProperty.call(message, "archives")) {
                    if (!$Array.isArray(message.archives))
                        return "archives: array expected";
                    for (let i = 0; i < message.archives.length; ++i) {
                        let error = $root.google.protobuf.Struct.verify(message.archives[i], _depth + 1);
                        if (error)
                            return "archives." + error;
                    }
                }
                if (message.binding != null && $Object.hasOwnProperty.call(message, "binding")) {
                    properties._binding = 1;
                    {
                        let error = $root.northstar.live.StreamBinding.verify(message.binding, _depth + 1);
                        if (error)
                            return "binding." + error;
                    }
                }
                if (message.connection != null && $Object.hasOwnProperty.call(message, "connection")) {
                    properties._connection = 1;
                    if (!$util.isString(message.connection))
                        return "connection: string expected";
                }
                if (message.cursor != null && $Object.hasOwnProperty.call(message, "cursor")) {
                    properties._cursor = 1;
                    if (!$util.isInteger(message.cursor) && !(message.cursor && $util.isInteger(message.cursor.low) && $util.isInteger(message.cursor.high)))
                        return "cursor: integer|Long expected";
                }
                if (message.paused != null && $Object.hasOwnProperty.call(message, "paused")) {
                    properties._paused = 1;
                    if (typeof message.paused !== "boolean")
                        return "paused: boolean expected";
                }
                if (message.received != null && $Object.hasOwnProperty.call(message, "received")) {
                    properties._received = 1;
                    if (!$util.isInteger(message.received) && !(message.received && $util.isInteger(message.received.low) && $util.isInteger(message.received.high)))
                        return "received: integer|Long expected";
                }
                if (message.steps != null && $Object.hasOwnProperty.call(message, "steps")) {
                    if (!$Array.isArray(message.steps))
                        return "steps: array expected";
                    for (let i = 0; i < message.steps.length; ++i) {
                        let error = $root.northstar.live.StreamStep.verify(message.steps[i], _depth + 1);
                        if (error)
                            return "steps." + error;
                    }
                }
                if (message.stream_id != null && $Object.hasOwnProperty.call(message, "stream_id")) {
                    properties._stream_id = 1;
                    if (!$util.isString(message.stream_id))
                        return "stream_id: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a StreamDetail message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.StreamDetail
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.StreamDetail} StreamDetail
             */
            StreamDetail.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.StreamDetail)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.StreamDetail: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.StreamDetail();
                if (object.account_progress != null) {
                    if (!$util.isObject(object.account_progress))
                        throw $TypeError(".northstar.live.StreamDetail.account_progress: object expected");
                    message.account_progress = $root.northstar.live.StreamAccountProgress.fromObject(object.account_progress, _depth + 1);
                }
                if (object.archives) {
                    if (!$Array.isArray(object.archives))
                        throw $TypeError(".northstar.live.StreamDetail.archives: array expected");
                    message.archives = $Array(object.archives.length);
                    for (let i = 0; i < object.archives.length; ++i) {
                        if (!$util.isObject(object.archives[i]))
                            throw $TypeError(".northstar.live.StreamDetail.archives: object expected");
                        message.archives[i] = $root.google.protobuf.Struct.fromObject(object.archives[i], _depth + 1);
                    }
                }
                if (object.binding != null) {
                    if (!$util.isObject(object.binding))
                        throw $TypeError(".northstar.live.StreamDetail.binding: object expected");
                    message.binding = $root.northstar.live.StreamBinding.fromObject(object.binding, _depth + 1);
                }
                if (object.connection != null)
                    message.connection = $String(object.connection);
                if (object.cursor != null)
                    if ($util.Long)
                        message.cursor = $util.Long.fromValue(object.cursor, false);
                    else if (typeof object.cursor === "string")
                        message.cursor = $parseInt(object.cursor, 10);
                    else if (typeof object.cursor === "number")
                        message.cursor = object.cursor;
                    else if (typeof object.cursor === "object")
                        message.cursor = new $util.LongBits(object.cursor.low >>> 0, object.cursor.high >>> 0).toNumber();
                if (object.paused != null)
                    message.paused = $Boolean(object.paused);
                if (object.received != null)
                    if ($util.Long)
                        message.received = $util.Long.fromValue(object.received, false);
                    else if (typeof object.received === "string")
                        message.received = $parseInt(object.received, 10);
                    else if (typeof object.received === "number")
                        message.received = object.received;
                    else if (typeof object.received === "object")
                        message.received = new $util.LongBits(object.received.low >>> 0, object.received.high >>> 0).toNumber();
                if (object.steps) {
                    if (!$Array.isArray(object.steps))
                        throw $TypeError(".northstar.live.StreamDetail.steps: array expected");
                    message.steps = $Array(object.steps.length);
                    for (let i = 0; i < object.steps.length; ++i) {
                        if (!$util.isObject(object.steps[i]))
                            throw $TypeError(".northstar.live.StreamDetail.steps: object expected");
                        message.steps[i] = $root.northstar.live.StreamStep.fromObject(object.steps[i], _depth + 1);
                    }
                }
                if (object.stream_id != null)
                    message.stream_id = $String(object.stream_id);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.StreamDetail.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.StreamDetail.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a StreamDetail message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.StreamDetail
             * @static
             * @param {northstar.live.StreamDetail} message StreamDetail
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StreamDetail.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.archives = [];
                    object.steps = [];
                }
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.account_progress != null && $Object.hasOwnProperty.call(message, "account_progress")) {
                    object.account_progress = $root.northstar.live.StreamAccountProgress.toObject(message.account_progress, options, _depth + 1);
                    if (options.oneofs)
                        object._account_progress = "account_progress";
                }
                if (message.archives && message.archives.length) {
                    object.archives = $Array(message.archives.length);
                    for (let j = 0; j < message.archives.length; ++j)
                        object.archives[j] = $root.google.protobuf.Struct.toObject(message.archives[j], options, _depth + 1);
                }
                if (message.binding != null && $Object.hasOwnProperty.call(message, "binding")) {
                    object.binding = $root.northstar.live.StreamBinding.toObject(message.binding, options, _depth + 1);
                    if (options.oneofs)
                        object._binding = "binding";
                }
                if (message.connection != null && $Object.hasOwnProperty.call(message, "connection")) {
                    object.connection = message.connection;
                    if (options.oneofs)
                        object._connection = "connection";
                }
                if (message.cursor != null && $Object.hasOwnProperty.call(message, "cursor")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.cursor = typeof message.cursor === "number" ? $BigInt(message.cursor) : $util.Long.fromBits(message.cursor.low >>> 0, message.cursor.high >>> 0, false).toBigInt();
                    else if (typeof message.cursor === "number")
                        object.cursor = options.longs === $String ? $String(message.cursor) : message.cursor;
                    else
                        object.cursor = options.longs === $String ? $util.Long.prototype.toString.call(message.cursor) : options.longs === $Number ? new $util.LongBits(message.cursor.low >>> 0, message.cursor.high >>> 0).toNumber() : message.cursor;
                    if (options.oneofs)
                        object._cursor = "cursor";
                }
                if (message.paused != null && $Object.hasOwnProperty.call(message, "paused")) {
                    object.paused = message.paused;
                    if (options.oneofs)
                        object._paused = "paused";
                }
                if (message.received != null && $Object.hasOwnProperty.call(message, "received")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.received = typeof message.received === "number" ? $BigInt(message.received) : $util.Long.fromBits(message.received.low >>> 0, message.received.high >>> 0, false).toBigInt();
                    else if (typeof message.received === "number")
                        object.received = options.longs === $String ? $String(message.received) : message.received;
                    else
                        object.received = options.longs === $String ? $util.Long.prototype.toString.call(message.received) : options.longs === $Number ? new $util.LongBits(message.received.low >>> 0, message.received.high >>> 0).toNumber() : message.received;
                    if (options.oneofs)
                        object._received = "received";
                }
                if (message.steps && message.steps.length) {
                    object.steps = $Array(message.steps.length);
                    for (let j = 0; j < message.steps.length; ++j)
                        object.steps[j] = $root.northstar.live.StreamStep.toObject(message.steps[j], options, _depth + 1);
                }
                if (message.stream_id != null && $Object.hasOwnProperty.call(message, "stream_id")) {
                    object.stream_id = message.stream_id;
                    if (options.oneofs)
                        object._stream_id = "stream_id";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this StreamDetail to JSON.
             * @function toJSON
             * @memberof northstar.live.StreamDetail
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StreamDetail.prototype.toJSON = function() {
                return StreamDetail.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StreamDetail
             * @function getTypeUrl
             * @memberof northstar.live.StreamDetail
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StreamDetail.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.StreamDetail";
            };

            return StreamDetail;
        })();

        live.StreamEvent = (function() {

            /**
             * Properties of a StreamEvent.
             * @typedef {Object} northstar.live.StreamEvent.$Properties
             * @property {string|null} [committed_at] StreamEvent committed_at
             * @property {google.protobuf.Struct.$Properties|null} [event] StreamEvent event
             * @property {"committed_at"} [_committed_at] StreamEvent _committed_at
             * @property {"event"} [_event] StreamEvent _event
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StreamEvent.
             * @memberof northstar.live
             * @interface IStreamEvent
             * @augments northstar.live.StreamEvent.$Properties
             * @deprecated Use northstar.live.StreamEvent.$Properties instead.
             */

            /**
             * Narrowed shape of a StreamEvent.
             * @typedef {{
             *   committed_at?: string|null;
             *   event?: google.protobuf.Struct.$Shape|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _committed_at?: undefined; committed_at?: null }|{ _committed_at?: "committed_at"; committed_at: string })
             * ) & (
             *   ({ _event?: undefined; event?: null }|{ _event?: "event"; event: google.protobuf.Struct.$Shape })
             * )} northstar.live.StreamEvent.$Shape
             */

            /**
             * Constructs a new StreamEvent.
             * @memberof northstar.live
             * @classdesc Represents a StreamEvent.
             * @constructor
             * @param {northstar.live.StreamEvent.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StreamEvent = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StreamEvent committed_at.
             * @member {string|null|undefined} committed_at
             * @memberof northstar.live.StreamEvent
             * @instance
             */
            StreamEvent.prototype.committed_at = null;

            /**
             * StreamEvent event.
             * @member {google.protobuf.Struct.$Properties|null|undefined} event
             * @memberof northstar.live.StreamEvent
             * @instance
             */
            StreamEvent.prototype.event = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * StreamEvent _committed_at.
             * @member {"committed_at"|undefined} _committed_at
             * @memberof northstar.live.StreamEvent
             * @instance
             */
            $Object.defineProperty(StreamEvent.prototype, "_committed_at", {
                get: $util.oneOfGetter($oneOfFields = ["committed_at"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StreamEvent _event.
             * @member {"event"|undefined} _event
             * @memberof northstar.live.StreamEvent
             * @instance
             */
            $Object.defineProperty(StreamEvent.prototype, "_event", {
                get: $util.oneOfGetter($oneOfFields = ["event"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StreamEvent instance using the specified properties.
             * @function create
             * @memberof northstar.live.StreamEvent
             * @static
             * @param {northstar.live.StreamEvent.$Properties=} [properties] Properties to set
             * @returns {northstar.live.StreamEvent} StreamEvent instance
             * @type {{
             *   (properties: northstar.live.StreamEvent.$Shape): northstar.live.StreamEvent & northstar.live.StreamEvent.$Shape;
             *   (properties?: northstar.live.StreamEvent.$Properties): northstar.live.StreamEvent;
             * }}
             */
            StreamEvent.create = function(properties) {
                return new StreamEvent(properties);
            };

            /**
             * Encodes the specified StreamEvent message. Does not implicitly {@link northstar.live.StreamEvent.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.StreamEvent
             * @static
             * @param {northstar.live.StreamEvent.$Properties} message StreamEvent message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StreamEvent.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.committed_at != null && $Object.hasOwnProperty.call(message, "committed_at"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.committed_at);
                if (message.event != null && $Object.hasOwnProperty.call(message, "event"))
                    $root.google.protobuf.Struct.encode(message.event, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a StreamEvent message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.StreamEvent
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.StreamEvent & northstar.live.StreamEvent.$Shape} StreamEvent
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StreamEvent.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.StreamEvent();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.committed_at = reader.stringVerify();
                            message._committed_at = "committed_at";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.event = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.event);
                            message._event = "event";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a StreamEvent message.
             * @function verify
             * @memberof northstar.live.StreamEvent
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StreamEvent.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.committed_at != null && $Object.hasOwnProperty.call(message, "committed_at")) {
                    properties._committed_at = 1;
                    if (!$util.isString(message.committed_at))
                        return "committed_at: string expected";
                }
                if (message.event != null && $Object.hasOwnProperty.call(message, "event")) {
                    properties._event = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.event, _depth + 1);
                        if (error)
                            return "event." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a StreamEvent message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.StreamEvent
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.StreamEvent} StreamEvent
             */
            StreamEvent.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.StreamEvent)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.StreamEvent: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.StreamEvent();
                if (object.committed_at != null)
                    message.committed_at = $String(object.committed_at);
                if (object.event != null) {
                    if (!$util.isObject(object.event))
                        throw $TypeError(".northstar.live.StreamEvent.event: object expected");
                    message.event = $root.google.protobuf.Struct.fromObject(object.event, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a StreamEvent message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.StreamEvent
             * @static
             * @param {northstar.live.StreamEvent} message StreamEvent
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StreamEvent.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.committed_at != null && $Object.hasOwnProperty.call(message, "committed_at")) {
                    object.committed_at = message.committed_at;
                    if (options.oneofs)
                        object._committed_at = "committed_at";
                }
                if (message.event != null && $Object.hasOwnProperty.call(message, "event")) {
                    object.event = $root.google.protobuf.Struct.toObject(message.event, options, _depth + 1);
                    if (options.oneofs)
                        object._event = "event";
                }
                return object;
            };

            /**
             * Converts this StreamEvent to JSON.
             * @function toJSON
             * @memberof northstar.live.StreamEvent
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StreamEvent.prototype.toJSON = function() {
                return StreamEvent.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StreamEvent
             * @function getTypeUrl
             * @memberof northstar.live.StreamEvent
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StreamEvent.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.StreamEvent";
            };

            return StreamEvent;
        })();

        live.StreamPositionsRequest = (function() {

            /**
             * Properties of a StreamPositionsRequest.
             * @typedef {Object} northstar.live.StreamPositionsRequest.$Properties
             * @property {string|null} [baseline_id] StreamPositionsRequest baseline_id
             * @property {string|null} [request_id] StreamPositionsRequest request_id
             * @property {number|Long|null} [through_sequence] StreamPositionsRequest through_sequence
             * @property {"baseline_id"} [_baseline_id] StreamPositionsRequest _baseline_id
             * @property {"request_id"} [_request_id] StreamPositionsRequest _request_id
             * @property {"through_sequence"} [_through_sequence] StreamPositionsRequest _through_sequence
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StreamPositionsRequest.
             * @memberof northstar.live
             * @interface IStreamPositionsRequest
             * @augments northstar.live.StreamPositionsRequest.$Properties
             * @deprecated Use northstar.live.StreamPositionsRequest.$Properties instead.
             */

            /**
             * Narrowed shape of a StreamPositionsRequest.
             * @typedef {{
             *   baseline_id?: string|null;
             *   request_id?: string|null;
             *   through_sequence?: number|Long|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _baseline_id?: undefined; baseline_id?: null }|{ _baseline_id?: "baseline_id"; baseline_id: string })
             * ) & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * ) & (
             *   ({ _through_sequence?: undefined; through_sequence?: null }|{ _through_sequence?: "through_sequence"; through_sequence: number|Long })
             * )} northstar.live.StreamPositionsRequest.$Shape
             */

            /**
             * Constructs a new StreamPositionsRequest.
             * @memberof northstar.live
             * @classdesc Represents a StreamPositionsRequest.
             * @constructor
             * @param {northstar.live.StreamPositionsRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StreamPositionsRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StreamPositionsRequest baseline_id.
             * @member {string|null|undefined} baseline_id
             * @memberof northstar.live.StreamPositionsRequest
             * @instance
             */
            StreamPositionsRequest.prototype.baseline_id = null;

            /**
             * StreamPositionsRequest request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.live.StreamPositionsRequest
             * @instance
             */
            StreamPositionsRequest.prototype.request_id = null;

            /**
             * StreamPositionsRequest through_sequence.
             * @member {number|Long|null|undefined} through_sequence
             * @memberof northstar.live.StreamPositionsRequest
             * @instance
             */
            StreamPositionsRequest.prototype.through_sequence = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * StreamPositionsRequest _baseline_id.
             * @member {"baseline_id"|undefined} _baseline_id
             * @memberof northstar.live.StreamPositionsRequest
             * @instance
             */
            $Object.defineProperty(StreamPositionsRequest.prototype, "_baseline_id", {
                get: $util.oneOfGetter($oneOfFields = ["baseline_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StreamPositionsRequest _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.live.StreamPositionsRequest
             * @instance
             */
            $Object.defineProperty(StreamPositionsRequest.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StreamPositionsRequest _through_sequence.
             * @member {"through_sequence"|undefined} _through_sequence
             * @memberof northstar.live.StreamPositionsRequest
             * @instance
             */
            $Object.defineProperty(StreamPositionsRequest.prototype, "_through_sequence", {
                get: $util.oneOfGetter($oneOfFields = ["through_sequence"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StreamPositionsRequest instance using the specified properties.
             * @function create
             * @memberof northstar.live.StreamPositionsRequest
             * @static
             * @param {northstar.live.StreamPositionsRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.live.StreamPositionsRequest} StreamPositionsRequest instance
             * @type {{
             *   (properties: northstar.live.StreamPositionsRequest.$Shape): northstar.live.StreamPositionsRequest & northstar.live.StreamPositionsRequest.$Shape;
             *   (properties?: northstar.live.StreamPositionsRequest.$Properties): northstar.live.StreamPositionsRequest;
             * }}
             */
            StreamPositionsRequest.create = function(properties) {
                return new StreamPositionsRequest(properties);
            };

            /**
             * Encodes the specified StreamPositionsRequest message. Does not implicitly {@link northstar.live.StreamPositionsRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.StreamPositionsRequest
             * @static
             * @param {northstar.live.StreamPositionsRequest.$Properties} message StreamPositionsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StreamPositionsRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.baseline_id);
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.request_id);
                if (message.through_sequence != null && $Object.hasOwnProperty.call(message, "through_sequence"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.through_sequence);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a StreamPositionsRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.StreamPositionsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.StreamPositionsRequest & northstar.live.StreamPositionsRequest.$Shape} StreamPositionsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StreamPositionsRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.StreamPositionsRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.baseline_id = reader.stringVerify();
                            message._baseline_id = "baseline_id";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            message.through_sequence = reader.int64();
                            message._through_sequence = "through_sequence";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a StreamPositionsRequest message.
             * @function verify
             * @memberof northstar.live.StreamPositionsRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StreamPositionsRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id")) {
                    properties._baseline_id = 1;
                    if (!$util.isString(message.baseline_id))
                        return "baseline_id: string expected";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                if (message.through_sequence != null && $Object.hasOwnProperty.call(message, "through_sequence")) {
                    properties._through_sequence = 1;
                    if (!$util.isInteger(message.through_sequence) && !(message.through_sequence && $util.isInteger(message.through_sequence.low) && $util.isInteger(message.through_sequence.high)))
                        return "through_sequence: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a StreamPositionsRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.StreamPositionsRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.StreamPositionsRequest} StreamPositionsRequest
             */
            StreamPositionsRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.StreamPositionsRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.StreamPositionsRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.StreamPositionsRequest();
                if (object.baseline_id != null)
                    message.baseline_id = $String(object.baseline_id);
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                if (object.through_sequence != null)
                    if ($util.Long)
                        message.through_sequence = $util.Long.fromValue(object.through_sequence, false);
                    else if (typeof object.through_sequence === "string")
                        message.through_sequence = $parseInt(object.through_sequence, 10);
                    else if (typeof object.through_sequence === "number")
                        message.through_sequence = object.through_sequence;
                    else if (typeof object.through_sequence === "object")
                        message.through_sequence = new $util.LongBits(object.through_sequence.low >>> 0, object.through_sequence.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a StreamPositionsRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.StreamPositionsRequest
             * @static
             * @param {northstar.live.StreamPositionsRequest} message StreamPositionsRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StreamPositionsRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.baseline_id != null && $Object.hasOwnProperty.call(message, "baseline_id")) {
                    object.baseline_id = message.baseline_id;
                    if (options.oneofs)
                        object._baseline_id = "baseline_id";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                if (message.through_sequence != null && $Object.hasOwnProperty.call(message, "through_sequence")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.through_sequence = typeof message.through_sequence === "number" ? $BigInt(message.through_sequence) : $util.Long.fromBits(message.through_sequence.low >>> 0, message.through_sequence.high >>> 0, false).toBigInt();
                    else if (typeof message.through_sequence === "number")
                        object.through_sequence = options.longs === $String ? $String(message.through_sequence) : message.through_sequence;
                    else
                        object.through_sequence = options.longs === $String ? $util.Long.prototype.toString.call(message.through_sequence) : options.longs === $Number ? new $util.LongBits(message.through_sequence.low >>> 0, message.through_sequence.high >>> 0).toNumber() : message.through_sequence;
                    if (options.oneofs)
                        object._through_sequence = "through_sequence";
                }
                return object;
            };

            /**
             * Converts this StreamPositionsRequest to JSON.
             * @function toJSON
             * @memberof northstar.live.StreamPositionsRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StreamPositionsRequest.prototype.toJSON = function() {
                return StreamPositionsRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StreamPositionsRequest
             * @function getTypeUrl
             * @memberof northstar.live.StreamPositionsRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StreamPositionsRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.StreamPositionsRequest";
            };

            return StreamPositionsRequest;
        })();

        live.StreamRequest = (function() {

            /**
             * Properties of a StreamRequest.
             * @typedef {Object} northstar.live.StreamRequest.$Properties
             * @property {boolean|null} [allow_retention] StreamRequest allow_retention
             * @property {string|null} [configuration_id] StreamRequest configuration_id
             * @property {number|Long|null} [duration_seconds] StreamRequest duration_seconds
             * @property {string|null} [query_batch_id] StreamRequest query_batch_id
             * @property {string|null} [request_id] StreamRequest request_id
             * @property {string|null} [use_basis] StreamRequest use_basis
             * @property {"allow_retention"} [_allow_retention] StreamRequest _allow_retention
             * @property {"configuration_id"} [_configuration_id] StreamRequest _configuration_id
             * @property {"duration_seconds"} [_duration_seconds] StreamRequest _duration_seconds
             * @property {"query_batch_id"} [_query_batch_id] StreamRequest _query_batch_id
             * @property {"request_id"} [_request_id] StreamRequest _request_id
             * @property {"use_basis"} [_use_basis] StreamRequest _use_basis
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StreamRequest.
             * @memberof northstar.live
             * @interface IStreamRequest
             * @augments northstar.live.StreamRequest.$Properties
             * @deprecated Use northstar.live.StreamRequest.$Properties instead.
             */

            /**
             * Narrowed shape of a StreamRequest.
             * @typedef {{
             *   allow_retention?: boolean|null;
             *   configuration_id?: string|null;
             *   duration_seconds?: number|Long|null;
             *   query_batch_id?: string|null;
             *   request_id?: string|null;
             *   use_basis?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _allow_retention?: undefined; allow_retention?: null }|{ _allow_retention?: "allow_retention"; allow_retention: boolean })
             * ) & (
             *   ({ _configuration_id?: undefined; configuration_id?: null }|{ _configuration_id?: "configuration_id"; configuration_id: string })
             * ) & (
             *   ({ _duration_seconds?: undefined; duration_seconds?: null }|{ _duration_seconds?: "duration_seconds"; duration_seconds: number|Long })
             * ) & (
             *   ({ _query_batch_id?: undefined; query_batch_id?: null }|{ _query_batch_id?: "query_batch_id"; query_batch_id: string })
             * ) & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * ) & (
             *   ({ _use_basis?: undefined; use_basis?: null }|{ _use_basis?: "use_basis"; use_basis: string })
             * )} northstar.live.StreamRequest.$Shape
             */

            /**
             * Constructs a new StreamRequest.
             * @memberof northstar.live
             * @classdesc Represents a StreamRequest.
             * @constructor
             * @param {northstar.live.StreamRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StreamRequest = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StreamRequest allow_retention.
             * @member {boolean|null|undefined} allow_retention
             * @memberof northstar.live.StreamRequest
             * @instance
             */
            StreamRequest.prototype.allow_retention = null;

            /**
             * StreamRequest configuration_id.
             * @member {string|null|undefined} configuration_id
             * @memberof northstar.live.StreamRequest
             * @instance
             */
            StreamRequest.prototype.configuration_id = null;

            /**
             * StreamRequest duration_seconds.
             * @member {number|Long|null|undefined} duration_seconds
             * @memberof northstar.live.StreamRequest
             * @instance
             */
            StreamRequest.prototype.duration_seconds = null;

            /**
             * StreamRequest query_batch_id.
             * @member {string|null|undefined} query_batch_id
             * @memberof northstar.live.StreamRequest
             * @instance
             */
            StreamRequest.prototype.query_batch_id = null;

            /**
             * StreamRequest request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.live.StreamRequest
             * @instance
             */
            StreamRequest.prototype.request_id = null;

            /**
             * StreamRequest use_basis.
             * @member {string|null|undefined} use_basis
             * @memberof northstar.live.StreamRequest
             * @instance
             */
            StreamRequest.prototype.use_basis = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * StreamRequest _allow_retention.
             * @member {"allow_retention"|undefined} _allow_retention
             * @memberof northstar.live.StreamRequest
             * @instance
             */
            $Object.defineProperty(StreamRequest.prototype, "_allow_retention", {
                get: $util.oneOfGetter($oneOfFields = ["allow_retention"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StreamRequest _configuration_id.
             * @member {"configuration_id"|undefined} _configuration_id
             * @memberof northstar.live.StreamRequest
             * @instance
             */
            $Object.defineProperty(StreamRequest.prototype, "_configuration_id", {
                get: $util.oneOfGetter($oneOfFields = ["configuration_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StreamRequest _duration_seconds.
             * @member {"duration_seconds"|undefined} _duration_seconds
             * @memberof northstar.live.StreamRequest
             * @instance
             */
            $Object.defineProperty(StreamRequest.prototype, "_duration_seconds", {
                get: $util.oneOfGetter($oneOfFields = ["duration_seconds"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StreamRequest _query_batch_id.
             * @member {"query_batch_id"|undefined} _query_batch_id
             * @memberof northstar.live.StreamRequest
             * @instance
             */
            $Object.defineProperty(StreamRequest.prototype, "_query_batch_id", {
                get: $util.oneOfGetter($oneOfFields = ["query_batch_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StreamRequest _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.live.StreamRequest
             * @instance
             */
            $Object.defineProperty(StreamRequest.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StreamRequest _use_basis.
             * @member {"use_basis"|undefined} _use_basis
             * @memberof northstar.live.StreamRequest
             * @instance
             */
            $Object.defineProperty(StreamRequest.prototype, "_use_basis", {
                get: $util.oneOfGetter($oneOfFields = ["use_basis"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StreamRequest instance using the specified properties.
             * @function create
             * @memberof northstar.live.StreamRequest
             * @static
             * @param {northstar.live.StreamRequest.$Properties=} [properties] Properties to set
             * @returns {northstar.live.StreamRequest} StreamRequest instance
             * @type {{
             *   (properties: northstar.live.StreamRequest.$Shape): northstar.live.StreamRequest & northstar.live.StreamRequest.$Shape;
             *   (properties?: northstar.live.StreamRequest.$Properties): northstar.live.StreamRequest;
             * }}
             */
            StreamRequest.create = function(properties) {
                return new StreamRequest(properties);
            };

            /**
             * Encodes the specified StreamRequest message. Does not implicitly {@link northstar.live.StreamRequest.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.StreamRequest
             * @static
             * @param {northstar.live.StreamRequest.$Properties} message StreamRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StreamRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.allow_retention != null && $Object.hasOwnProperty.call(message, "allow_retention"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.allow_retention);
                if (message.configuration_id != null && $Object.hasOwnProperty.call(message, "configuration_id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.configuration_id);
                if (message.duration_seconds != null && $Object.hasOwnProperty.call(message, "duration_seconds"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.duration_seconds);
                if (message.query_batch_id != null && $Object.hasOwnProperty.call(message, "query_batch_id"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.query_batch_id);
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.request_id);
                if (message.use_basis != null && $Object.hasOwnProperty.call(message, "use_basis"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.use_basis);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a StreamRequest message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.StreamRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.StreamRequest & northstar.live.StreamRequest.$Shape} StreamRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StreamRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.StreamRequest();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            message.allow_retention = reader.bool();
                            message._allow_retention = "allow_retention";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.configuration_id = reader.stringVerify();
                            message._configuration_id = "configuration_id";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            message.duration_seconds = reader.int64();
                            message._duration_seconds = "duration_seconds";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.query_batch_id = reader.stringVerify();
                            message._query_batch_id = "query_batch_id";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.use_basis = reader.stringVerify();
                            message._use_basis = "use_basis";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a StreamRequest message.
             * @function verify
             * @memberof northstar.live.StreamRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StreamRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.allow_retention != null && $Object.hasOwnProperty.call(message, "allow_retention")) {
                    properties._allow_retention = 1;
                    if (typeof message.allow_retention !== "boolean")
                        return "allow_retention: boolean expected";
                }
                if (message.configuration_id != null && $Object.hasOwnProperty.call(message, "configuration_id")) {
                    properties._configuration_id = 1;
                    if (!$util.isString(message.configuration_id))
                        return "configuration_id: string expected";
                }
                if (message.duration_seconds != null && $Object.hasOwnProperty.call(message, "duration_seconds")) {
                    properties._duration_seconds = 1;
                    if (!$util.isInteger(message.duration_seconds) && !(message.duration_seconds && $util.isInteger(message.duration_seconds.low) && $util.isInteger(message.duration_seconds.high)))
                        return "duration_seconds: integer|Long expected";
                }
                if (message.query_batch_id != null && $Object.hasOwnProperty.call(message, "query_batch_id")) {
                    properties._query_batch_id = 1;
                    if (!$util.isString(message.query_batch_id))
                        return "query_batch_id: string expected";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                if (message.use_basis != null && $Object.hasOwnProperty.call(message, "use_basis")) {
                    properties._use_basis = 1;
                    if (!$util.isString(message.use_basis))
                        return "use_basis: string expected";
                }
                return null;
            };

            /**
             * Creates a StreamRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.StreamRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.StreamRequest} StreamRequest
             */
            StreamRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.StreamRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.StreamRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.StreamRequest();
                if (object.allow_retention != null)
                    message.allow_retention = $Boolean(object.allow_retention);
                if (object.configuration_id != null)
                    message.configuration_id = $String(object.configuration_id);
                if (object.duration_seconds != null)
                    if ($util.Long)
                        message.duration_seconds = $util.Long.fromValue(object.duration_seconds, false);
                    else if (typeof object.duration_seconds === "string")
                        message.duration_seconds = $parseInt(object.duration_seconds, 10);
                    else if (typeof object.duration_seconds === "number")
                        message.duration_seconds = object.duration_seconds;
                    else if (typeof object.duration_seconds === "object")
                        message.duration_seconds = new $util.LongBits(object.duration_seconds.low >>> 0, object.duration_seconds.high >>> 0).toNumber();
                if (object.query_batch_id != null)
                    message.query_batch_id = $String(object.query_batch_id);
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                if (object.use_basis != null)
                    message.use_basis = $String(object.use_basis);
                return message;
            };

            /**
             * Creates a plain object from a StreamRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.StreamRequest
             * @static
             * @param {northstar.live.StreamRequest} message StreamRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StreamRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.allow_retention != null && $Object.hasOwnProperty.call(message, "allow_retention")) {
                    object.allow_retention = message.allow_retention;
                    if (options.oneofs)
                        object._allow_retention = "allow_retention";
                }
                if (message.configuration_id != null && $Object.hasOwnProperty.call(message, "configuration_id")) {
                    object.configuration_id = message.configuration_id;
                    if (options.oneofs)
                        object._configuration_id = "configuration_id";
                }
                if (message.duration_seconds != null && $Object.hasOwnProperty.call(message, "duration_seconds")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.duration_seconds = typeof message.duration_seconds === "number" ? $BigInt(message.duration_seconds) : $util.Long.fromBits(message.duration_seconds.low >>> 0, message.duration_seconds.high >>> 0, false).toBigInt();
                    else if (typeof message.duration_seconds === "number")
                        object.duration_seconds = options.longs === $String ? $String(message.duration_seconds) : message.duration_seconds;
                    else
                        object.duration_seconds = options.longs === $String ? $util.Long.prototype.toString.call(message.duration_seconds) : options.longs === $Number ? new $util.LongBits(message.duration_seconds.low >>> 0, message.duration_seconds.high >>> 0).toNumber() : message.duration_seconds;
                    if (options.oneofs)
                        object._duration_seconds = "duration_seconds";
                }
                if (message.query_batch_id != null && $Object.hasOwnProperty.call(message, "query_batch_id")) {
                    object.query_batch_id = message.query_batch_id;
                    if (options.oneofs)
                        object._query_batch_id = "query_batch_id";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                if (message.use_basis != null && $Object.hasOwnProperty.call(message, "use_basis")) {
                    object.use_basis = message.use_basis;
                    if (options.oneofs)
                        object._use_basis = "use_basis";
                }
                return object;
            };

            /**
             * Converts this StreamRequest to JSON.
             * @function toJSON
             * @memberof northstar.live.StreamRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StreamRequest.prototype.toJSON = function() {
                return StreamRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StreamRequest
             * @function getTypeUrl
             * @memberof northstar.live.StreamRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StreamRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.StreamRequest";
            };

            return StreamRequest;
        })();

        live.StreamStep = (function() {

            /**
             * Properties of a StreamStep.
             * @typedef {Object} northstar.live.StreamStep.$Properties
             * @property {string|null} [committed_at] StreamStep committed_at
             * @property {northstar.live.StreamStepResult.$Properties|null} [result] StreamStep result
             * @property {number|Long|null} [sequence] StreamStep sequence
             * @property {"committed_at"} [_committed_at] StreamStep _committed_at
             * @property {"result"} [_result] StreamStep _result
             * @property {"sequence"} [_sequence] StreamStep _sequence
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StreamStep.
             * @memberof northstar.live
             * @interface IStreamStep
             * @augments northstar.live.StreamStep.$Properties
             * @deprecated Use northstar.live.StreamStep.$Properties instead.
             */

            /**
             * Narrowed shape of a StreamStep.
             * @typedef {{
             *   committed_at?: string|null;
             *   result?: northstar.live.StreamStepResult.$Shape|null;
             *   sequence?: number|Long|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _committed_at?: undefined; committed_at?: null }|{ _committed_at?: "committed_at"; committed_at: string })
             * ) & (
             *   ({ _result?: undefined; result?: null }|{ _result?: "result"; result: northstar.live.StreamStepResult.$Shape })
             * ) & (
             *   ({ _sequence?: undefined; sequence?: null }|{ _sequence?: "sequence"; sequence: number|Long })
             * )} northstar.live.StreamStep.$Shape
             */

            /**
             * Constructs a new StreamStep.
             * @memberof northstar.live
             * @classdesc Represents a StreamStep.
             * @constructor
             * @param {northstar.live.StreamStep.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StreamStep = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StreamStep committed_at.
             * @member {string|null|undefined} committed_at
             * @memberof northstar.live.StreamStep
             * @instance
             */
            StreamStep.prototype.committed_at = null;

            /**
             * StreamStep result.
             * @member {northstar.live.StreamStepResult.$Properties|null|undefined} result
             * @memberof northstar.live.StreamStep
             * @instance
             */
            StreamStep.prototype.result = null;

            /**
             * StreamStep sequence.
             * @member {number|Long|null|undefined} sequence
             * @memberof northstar.live.StreamStep
             * @instance
             */
            StreamStep.prototype.sequence = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * StreamStep _committed_at.
             * @member {"committed_at"|undefined} _committed_at
             * @memberof northstar.live.StreamStep
             * @instance
             */
            $Object.defineProperty(StreamStep.prototype, "_committed_at", {
                get: $util.oneOfGetter($oneOfFields = ["committed_at"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StreamStep _result.
             * @member {"result"|undefined} _result
             * @memberof northstar.live.StreamStep
             * @instance
             */
            $Object.defineProperty(StreamStep.prototype, "_result", {
                get: $util.oneOfGetter($oneOfFields = ["result"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StreamStep _sequence.
             * @member {"sequence"|undefined} _sequence
             * @memberof northstar.live.StreamStep
             * @instance
             */
            $Object.defineProperty(StreamStep.prototype, "_sequence", {
                get: $util.oneOfGetter($oneOfFields = ["sequence"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StreamStep instance using the specified properties.
             * @function create
             * @memberof northstar.live.StreamStep
             * @static
             * @param {northstar.live.StreamStep.$Properties=} [properties] Properties to set
             * @returns {northstar.live.StreamStep} StreamStep instance
             * @type {{
             *   (properties: northstar.live.StreamStep.$Shape): northstar.live.StreamStep & northstar.live.StreamStep.$Shape;
             *   (properties?: northstar.live.StreamStep.$Properties): northstar.live.StreamStep;
             * }}
             */
            StreamStep.create = function(properties) {
                return new StreamStep(properties);
            };

            /**
             * Encodes the specified StreamStep message. Does not implicitly {@link northstar.live.StreamStep.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.StreamStep
             * @static
             * @param {northstar.live.StreamStep.$Properties} message StreamStep message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StreamStep.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.committed_at != null && $Object.hasOwnProperty.call(message, "committed_at"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.committed_at);
                if (message.result != null && $Object.hasOwnProperty.call(message, "result"))
                    $root.northstar.live.StreamStepResult.encode(message.result, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.sequence != null && $Object.hasOwnProperty.call(message, "sequence"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.sequence);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a StreamStep message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.StreamStep
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.StreamStep & northstar.live.StreamStep.$Shape} StreamStep
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StreamStep.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.StreamStep();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.committed_at = reader.stringVerify();
                            message._committed_at = "committed_at";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.result = $root.northstar.live.StreamStepResult.decode(reader, reader.uint32(), $undefined, _depth + 1, message.result);
                            message._result = "result";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            message.sequence = reader.int64();
                            message._sequence = "sequence";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a StreamStep message.
             * @function verify
             * @memberof northstar.live.StreamStep
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StreamStep.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.committed_at != null && $Object.hasOwnProperty.call(message, "committed_at")) {
                    properties._committed_at = 1;
                    if (!$util.isString(message.committed_at))
                        return "committed_at: string expected";
                }
                if (message.result != null && $Object.hasOwnProperty.call(message, "result")) {
                    properties._result = 1;
                    {
                        let error = $root.northstar.live.StreamStepResult.verify(message.result, _depth + 1);
                        if (error)
                            return "result." + error;
                    }
                }
                if (message.sequence != null && $Object.hasOwnProperty.call(message, "sequence")) {
                    properties._sequence = 1;
                    if (!$util.isInteger(message.sequence) && !(message.sequence && $util.isInteger(message.sequence.low) && $util.isInteger(message.sequence.high)))
                        return "sequence: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a StreamStep message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.StreamStep
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.StreamStep} StreamStep
             */
            StreamStep.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.StreamStep)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.StreamStep: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.StreamStep();
                if (object.committed_at != null)
                    message.committed_at = $String(object.committed_at);
                if (object.result != null) {
                    if (!$util.isObject(object.result))
                        throw $TypeError(".northstar.live.StreamStep.result: object expected");
                    message.result = $root.northstar.live.StreamStepResult.fromObject(object.result, _depth + 1);
                }
                if (object.sequence != null)
                    if ($util.Long)
                        message.sequence = $util.Long.fromValue(object.sequence, false);
                    else if (typeof object.sequence === "string")
                        message.sequence = $parseInt(object.sequence, 10);
                    else if (typeof object.sequence === "number")
                        message.sequence = object.sequence;
                    else if (typeof object.sequence === "object")
                        message.sequence = new $util.LongBits(object.sequence.low >>> 0, object.sequence.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a StreamStep message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.StreamStep
             * @static
             * @param {northstar.live.StreamStep} message StreamStep
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StreamStep.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.committed_at != null && $Object.hasOwnProperty.call(message, "committed_at")) {
                    object.committed_at = message.committed_at;
                    if (options.oneofs)
                        object._committed_at = "committed_at";
                }
                if (message.result != null && $Object.hasOwnProperty.call(message, "result")) {
                    object.result = $root.northstar.live.StreamStepResult.toObject(message.result, options, _depth + 1);
                    if (options.oneofs)
                        object._result = "result";
                }
                if (message.sequence != null && $Object.hasOwnProperty.call(message, "sequence")) {
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.sequence = typeof message.sequence === "number" ? $BigInt(message.sequence) : $util.Long.fromBits(message.sequence.low >>> 0, message.sequence.high >>> 0, false).toBigInt();
                    else if (typeof message.sequence === "number")
                        object.sequence = options.longs === $String ? $String(message.sequence) : message.sequence;
                    else
                        object.sequence = options.longs === $String ? $util.Long.prototype.toString.call(message.sequence) : options.longs === $Number ? new $util.LongBits(message.sequence.low >>> 0, message.sequence.high >>> 0).toNumber() : message.sequence;
                    if (options.oneofs)
                        object._sequence = "sequence";
                }
                return object;
            };

            /**
             * Converts this StreamStep to JSON.
             * @function toJSON
             * @memberof northstar.live.StreamStep
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StreamStep.prototype.toJSON = function() {
                return StreamStep.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StreamStep
             * @function getTypeUrl
             * @memberof northstar.live.StreamStep
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StreamStep.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.StreamStep";
            };

            return StreamStep;
        })();

        live.StreamStepResult = (function() {

            /**
             * Properties of a StreamStepResult.
             * @typedef {Object} northstar.live.StreamStepResult.$Properties
             * @property {northstar.live.CompletedMinute.$Properties|null} [bar] StreamStepResult bar
             * @property {google.protobuf.Struct.$Properties|null} [intent] StreamStepResult intent
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] StreamStepResult evidence_fields
             * @property {Array.<string>|null} [null_fields] StreamStepResult null_fields
             * @property {"bar"} [_bar] StreamStepResult _bar
             * @property {"intent"} [_intent] StreamStepResult _intent
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StreamStepResult.
             * @memberof northstar.live
             * @interface IStreamStepResult
             * @augments northstar.live.StreamStepResult.$Properties
             * @deprecated Use northstar.live.StreamStepResult.$Properties instead.
             */

            /**
             * Narrowed shape of a StreamStepResult.
             * @typedef {{
             *   bar?: northstar.live.CompletedMinute.$Shape|null;
             *   intent?: google.protobuf.Struct.$Shape|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   null_fields?: Array.<string>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _bar?: undefined; bar?: null }|{ _bar?: "bar"; bar: northstar.live.CompletedMinute.$Shape })
             * ) & (
             *   ({ _intent?: undefined; intent?: null }|{ _intent?: "intent"; intent: google.protobuf.Struct.$Shape })
             * )} northstar.live.StreamStepResult.$Shape
             */

            /**
             * Constructs a new StreamStepResult.
             * @memberof northstar.live
             * @classdesc Represents a StreamStepResult.
             * @constructor
             * @param {northstar.live.StreamStepResult.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StreamStepResult = function (properties) {
                this.evidence_fields = {};
                this.null_fields = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StreamStepResult bar.
             * @member {northstar.live.CompletedMinute.$Properties|null|undefined} bar
             * @memberof northstar.live.StreamStepResult
             * @instance
             */
            StreamStepResult.prototype.bar = null;

            /**
             * StreamStepResult intent.
             * @member {google.protobuf.Struct.$Properties|null|undefined} intent
             * @memberof northstar.live.StreamStepResult
             * @instance
             */
            StreamStepResult.prototype.intent = null;

            /**
             * StreamStepResult evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.StreamStepResult
             * @instance
             */
            StreamStepResult.prototype.evidence_fields = $util.emptyObject;

            /**
             * StreamStepResult null_fields.
             * @member {Array.<string>} null_fields
             * @memberof northstar.live.StreamStepResult
             * @instance
             */
            StreamStepResult.prototype.null_fields = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * StreamStepResult _bar.
             * @member {"bar"|undefined} _bar
             * @memberof northstar.live.StreamStepResult
             * @instance
             */
            $Object.defineProperty(StreamStepResult.prototype, "_bar", {
                get: $util.oneOfGetter($oneOfFields = ["bar"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * StreamStepResult _intent.
             * @member {"intent"|undefined} _intent
             * @memberof northstar.live.StreamStepResult
             * @instance
             */
            $Object.defineProperty(StreamStepResult.prototype, "_intent", {
                get: $util.oneOfGetter($oneOfFields = ["intent"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StreamStepResult instance using the specified properties.
             * @function create
             * @memberof northstar.live.StreamStepResult
             * @static
             * @param {northstar.live.StreamStepResult.$Properties=} [properties] Properties to set
             * @returns {northstar.live.StreamStepResult} StreamStepResult instance
             * @type {{
             *   (properties: northstar.live.StreamStepResult.$Shape): northstar.live.StreamStepResult & northstar.live.StreamStepResult.$Shape;
             *   (properties?: northstar.live.StreamStepResult.$Properties): northstar.live.StreamStepResult;
             * }}
             */
            StreamStepResult.create = function(properties) {
                return new StreamStepResult(properties);
            };

            /**
             * Encodes the specified StreamStepResult message. Does not implicitly {@link northstar.live.StreamStepResult.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.StreamStepResult
             * @static
             * @param {northstar.live.StreamStepResult.$Properties} message StreamStepResult message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StreamStepResult.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.bar != null && $Object.hasOwnProperty.call(message, "bar"))
                    $root.northstar.live.CompletedMinute.encode(message.bar, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.intent != null && $Object.hasOwnProperty.call(message, "intent"))
                    $root.google.protobuf.Struct.encode(message.intent, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.null_fields != null && message.null_fields.length)
                    for (let i = 0; i < message.null_fields.length; ++i)
                        writer.uint32(/* id 2046, wireType 2 =*/16370).string(message.null_fields[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a StreamStepResult message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.StreamStepResult
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.StreamStepResult & northstar.live.StreamStepResult.$Shape} StreamStepResult
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StreamStepResult.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.StreamStepResult();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.bar = $root.northstar.live.CompletedMinute.decode(reader, reader.uint32(), $undefined, _depth + 1, message.bar);
                            message._bar = "bar";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.intent = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.intent);
                            message._intent = "intent";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    case 2046: {
                            if (wireType !== 2)
                                break;
                            if (!(message.null_fields && message.null_fields.length))
                                message.null_fields = [];
                            message.null_fields.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a StreamStepResult message.
             * @function verify
             * @memberof northstar.live.StreamStepResult
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StreamStepResult.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.bar != null && $Object.hasOwnProperty.call(message, "bar")) {
                    properties._bar = 1;
                    {
                        let error = $root.northstar.live.CompletedMinute.verify(message.bar, _depth + 1);
                        if (error)
                            return "bar." + error;
                    }
                }
                if (message.intent != null && $Object.hasOwnProperty.call(message, "intent")) {
                    properties._intent = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.intent, _depth + 1);
                        if (error)
                            return "intent." + error;
                    }
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                if (message.null_fields != null && $Object.hasOwnProperty.call(message, "null_fields")) {
                    if (!$Array.isArray(message.null_fields))
                        return "null_fields: array expected";
                    for (let i = 0; i < message.null_fields.length; ++i)
                        if (!$util.isString(message.null_fields[i]))
                            return "null_fields: string[] expected";
                }
                return null;
            };

            /**
             * Creates a StreamStepResult message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.StreamStepResult
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.StreamStepResult} StreamStepResult
             */
            StreamStepResult.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.StreamStepResult)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.StreamStepResult: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.StreamStepResult();
                if (object.bar != null) {
                    if (!$util.isObject(object.bar))
                        throw $TypeError(".northstar.live.StreamStepResult.bar: object expected");
                    message.bar = $root.northstar.live.CompletedMinute.fromObject(object.bar, _depth + 1);
                }
                if (object.intent != null) {
                    if (!$util.isObject(object.intent))
                        throw $TypeError(".northstar.live.StreamStepResult.intent: object expected");
                    message.intent = $root.google.protobuf.Struct.fromObject(object.intent, _depth + 1);
                }
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.StreamStepResult.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.StreamStepResult.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                if (object.null_fields) {
                    if (!$Array.isArray(object.null_fields))
                        throw $TypeError(".northstar.live.StreamStepResult.null_fields: array expected");
                    message.null_fields = $Array(object.null_fields.length);
                    for (let i = 0; i < object.null_fields.length; ++i)
                        message.null_fields[i] = $String(object.null_fields[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a StreamStepResult message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.StreamStepResult
             * @static
             * @param {northstar.live.StreamStepResult} message StreamStepResult
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StreamStepResult.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.null_fields = [];
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.bar != null && $Object.hasOwnProperty.call(message, "bar")) {
                    object.bar = $root.northstar.live.CompletedMinute.toObject(message.bar, options, _depth + 1);
                    if (options.oneofs)
                        object._bar = "bar";
                }
                if (message.intent != null && $Object.hasOwnProperty.call(message, "intent")) {
                    object.intent = $root.google.protobuf.Struct.toObject(message.intent, options, _depth + 1);
                    if (options.oneofs)
                        object._intent = "intent";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                if (message.null_fields && message.null_fields.length) {
                    object.null_fields = $Array(message.null_fields.length);
                    for (let j = 0; j < message.null_fields.length; ++j)
                        object.null_fields[j] = message.null_fields[j];
                }
                return object;
            };

            /**
             * Converts this StreamStepResult to JSON.
             * @function toJSON
             * @memberof northstar.live.StreamStepResult
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StreamStepResult.prototype.toJSON = function() {
                return StreamStepResult.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StreamStepResult
             * @function getTypeUrl
             * @memberof northstar.live.StreamStepResult
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StreamStepResult.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.StreamStepResult";
            };

            return StreamStepResult;
        })();

        live.StreamSummary = (function() {

            /**
             * Properties of a StreamSummary.
             * @typedef {Object} northstar.live.StreamSummary.$Properties
             * @property {string|null} [stream_id] StreamSummary stream_id
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [evidence_fields] StreamSummary evidence_fields
             * @property {"stream_id"} [_stream_id] StreamSummary _stream_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StreamSummary.
             * @memberof northstar.live
             * @interface IStreamSummary
             * @augments northstar.live.StreamSummary.$Properties
             * @deprecated Use northstar.live.StreamSummary.$Properties instead.
             */

            /**
             * Narrowed shape of a StreamSummary.
             * @typedef {{
             *   stream_id?: string|null;
             *   evidence_fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _stream_id?: undefined; stream_id?: null }|{ _stream_id?: "stream_id"; stream_id: string })
             * )} northstar.live.StreamSummary.$Shape
             */

            /**
             * Constructs a new StreamSummary.
             * @memberof northstar.live
             * @classdesc Represents a StreamSummary.
             * @constructor
             * @param {northstar.live.StreamSummary.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StreamSummary = function (properties) {
                this.evidence_fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StreamSummary stream_id.
             * @member {string|null|undefined} stream_id
             * @memberof northstar.live.StreamSummary
             * @instance
             */
            StreamSummary.prototype.stream_id = null;

            /**
             * StreamSummary evidence_fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} evidence_fields
             * @memberof northstar.live.StreamSummary
             * @instance
             */
            StreamSummary.prototype.evidence_fields = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * StreamSummary _stream_id.
             * @member {"stream_id"|undefined} _stream_id
             * @memberof northstar.live.StreamSummary
             * @instance
             */
            $Object.defineProperty(StreamSummary.prototype, "_stream_id", {
                get: $util.oneOfGetter($oneOfFields = ["stream_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StreamSummary instance using the specified properties.
             * @function create
             * @memberof northstar.live.StreamSummary
             * @static
             * @param {northstar.live.StreamSummary.$Properties=} [properties] Properties to set
             * @returns {northstar.live.StreamSummary} StreamSummary instance
             * @type {{
             *   (properties: northstar.live.StreamSummary.$Shape): northstar.live.StreamSummary & northstar.live.StreamSummary.$Shape;
             *   (properties?: northstar.live.StreamSummary.$Properties): northstar.live.StreamSummary;
             * }}
             */
            StreamSummary.create = function(properties) {
                return new StreamSummary(properties);
            };

            /**
             * Encodes the specified StreamSummary message. Does not implicitly {@link northstar.live.StreamSummary.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.StreamSummary
             * @static
             * @param {northstar.live.StreamSummary.$Properties} message StreamSummary message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StreamSummary.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.stream_id != null && $Object.hasOwnProperty.call(message, "stream_id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.stream_id);
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields"))
                    for (let keys = $Object.keys(message.evidence_fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1000, wireType 2 =*/8002).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.evidence_fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a StreamSummary message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.StreamSummary
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.StreamSummary & northstar.live.StreamSummary.$Shape} StreamSummary
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StreamSummary.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.StreamSummary();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.stream_id = reader.stringVerify();
                            message._stream_id = "stream_id";
                            continue;
                        }
                    case 1000: {
                            if (wireType !== 2)
                                break;
                            if (message.evidence_fields === $util.emptyObject)
                                message.evidence_fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.evidence_fields, key);
                            message.evidence_fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a StreamSummary message.
             * @function verify
             * @memberof northstar.live.StreamSummary
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StreamSummary.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.stream_id != null && $Object.hasOwnProperty.call(message, "stream_id")) {
                    properties._stream_id = 1;
                    if (!$util.isString(message.stream_id))
                        return "stream_id: string expected";
                }
                if (message.evidence_fields != null && $Object.hasOwnProperty.call(message, "evidence_fields")) {
                    if (!$util.isObject(message.evidence_fields))
                        return "evidence_fields: object expected";
                    let key = $Object.keys(message.evidence_fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.evidence_fields[key[i]], _depth + 1);
                        if (error)
                            return "evidence_fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a StreamSummary message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.StreamSummary
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.StreamSummary} StreamSummary
             */
            StreamSummary.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.StreamSummary)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.StreamSummary: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.StreamSummary();
                if (object.stream_id != null)
                    message.stream_id = $String(object.stream_id);
                if (object.evidence_fields) {
                    if (!$util.isObject(object.evidence_fields))
                        throw $TypeError(".northstar.live.StreamSummary.evidence_fields: object expected");
                    message.evidence_fields = {};
                    for (let keys = $Object.keys(object.evidence_fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.evidence_fields, keys[i]);
                        if (!$util.isObject(object.evidence_fields[keys[i]]))
                            throw $TypeError(".northstar.live.StreamSummary.evidence_fields: object expected");
                        message.evidence_fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.evidence_fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a StreamSummary message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.StreamSummary
             * @static
             * @param {northstar.live.StreamSummary} message StreamSummary
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StreamSummary.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.evidence_fields = {};
                if (message.stream_id != null && $Object.hasOwnProperty.call(message, "stream_id")) {
                    object.stream_id = message.stream_id;
                    if (options.oneofs)
                        object._stream_id = "stream_id";
                }
                let keys2;
                if (message.evidence_fields && (keys2 = $Object.keys(message.evidence_fields)).length) {
                    object.evidence_fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.evidence_fields, keys2[j]);
                        object.evidence_fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.evidence_fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this StreamSummary to JSON.
             * @function toJSON
             * @memberof northstar.live.StreamSummary
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StreamSummary.prototype.toJSON = function() {
                return StreamSummary.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StreamSummary
             * @function getTypeUrl
             * @memberof northstar.live.StreamSummary
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StreamSummary.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.StreamSummary";
            };

            return StreamSummary;
        })();

        live.GetApiBrokerQueriesResponse = (function() {

            /**
             * Properties of a GetApiBrokerQueriesResponse.
             * @typedef {Object} northstar.live.GetApiBrokerQueriesResponse.$Properties
             * @property {Array.<northstar.live.QueryRecord.$Properties>|null} [items] GetApiBrokerQueriesResponse items
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetApiBrokerQueriesResponse.
             * @memberof northstar.live
             * @interface IGetApiBrokerQueriesResponse
             * @augments northstar.live.GetApiBrokerQueriesResponse.$Properties
             * @deprecated Use northstar.live.GetApiBrokerQueriesResponse.$Properties instead.
             */

            /**
             * Shape of a GetApiBrokerQueriesResponse.
             * @typedef {{
             *   items?: Array.<northstar.live.QueryRecord.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} northstar.live.GetApiBrokerQueriesResponse.$Shape
             */

            /**
             * Constructs a new GetApiBrokerQueriesResponse.
             * @memberof northstar.live
             * @classdesc Represents a GetApiBrokerQueriesResponse.
             * @constructor
             * @param {northstar.live.GetApiBrokerQueriesResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const GetApiBrokerQueriesResponse = function (properties) {
                this.items = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetApiBrokerQueriesResponse items.
             * @member {Array.<northstar.live.QueryRecord.$Properties>} items
             * @memberof northstar.live.GetApiBrokerQueriesResponse
             * @instance
             */
            GetApiBrokerQueriesResponse.prototype.items = $util.emptyArray;

            /**
             * Creates a new GetApiBrokerQueriesResponse instance using the specified properties.
             * @function create
             * @memberof northstar.live.GetApiBrokerQueriesResponse
             * @static
             * @param {northstar.live.GetApiBrokerQueriesResponse.$Properties=} [properties] Properties to set
             * @returns {northstar.live.GetApiBrokerQueriesResponse} GetApiBrokerQueriesResponse instance
             * @type {{
             *   (properties: northstar.live.GetApiBrokerQueriesResponse.$Shape): northstar.live.GetApiBrokerQueriesResponse & northstar.live.GetApiBrokerQueriesResponse.$Shape;
             *   (properties?: northstar.live.GetApiBrokerQueriesResponse.$Properties): northstar.live.GetApiBrokerQueriesResponse;
             * }}
             */
            GetApiBrokerQueriesResponse.create = function(properties) {
                return new GetApiBrokerQueriesResponse(properties);
            };

            /**
             * Encodes the specified GetApiBrokerQueriesResponse message. Does not implicitly {@link northstar.live.GetApiBrokerQueriesResponse.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.GetApiBrokerQueriesResponse
             * @static
             * @param {northstar.live.GetApiBrokerQueriesResponse.$Properties} message GetApiBrokerQueriesResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetApiBrokerQueriesResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.items != null && message.items.length)
                    for (let i = 0; i < message.items.length; ++i)
                        $root.northstar.live.QueryRecord.encode(message.items[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a GetApiBrokerQueriesResponse message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.GetApiBrokerQueriesResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.GetApiBrokerQueriesResponse & northstar.live.GetApiBrokerQueriesResponse.$Shape} GetApiBrokerQueriesResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetApiBrokerQueriesResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.GetApiBrokerQueriesResponse();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.items && message.items.length))
                                message.items = [];
                            message.items.push($root.northstar.live.QueryRecord.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a GetApiBrokerQueriesResponse message.
             * @function verify
             * @memberof northstar.live.GetApiBrokerQueriesResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetApiBrokerQueriesResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.items != null && $Object.hasOwnProperty.call(message, "items")) {
                    if (!$Array.isArray(message.items))
                        return "items: array expected";
                    for (let i = 0; i < message.items.length; ++i) {
                        let error = $root.northstar.live.QueryRecord.verify(message.items[i], _depth + 1);
                        if (error)
                            return "items." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a GetApiBrokerQueriesResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.GetApiBrokerQueriesResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.GetApiBrokerQueriesResponse} GetApiBrokerQueriesResponse
             */
            GetApiBrokerQueriesResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.GetApiBrokerQueriesResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.GetApiBrokerQueriesResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.GetApiBrokerQueriesResponse();
                if (object.items) {
                    if (!$Array.isArray(object.items))
                        throw $TypeError(".northstar.live.GetApiBrokerQueriesResponse.items: array expected");
                    message.items = $Array(object.items.length);
                    for (let i = 0; i < object.items.length; ++i) {
                        if (!$util.isObject(object.items[i]))
                            throw $TypeError(".northstar.live.GetApiBrokerQueriesResponse.items: object expected");
                        message.items[i] = $root.northstar.live.QueryRecord.fromObject(object.items[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a GetApiBrokerQueriesResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.GetApiBrokerQueriesResponse
             * @static
             * @param {northstar.live.GetApiBrokerQueriesResponse} message GetApiBrokerQueriesResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetApiBrokerQueriesResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.items = [];
                if (message.items && message.items.length) {
                    object.items = $Array(message.items.length);
                    for (let j = 0; j < message.items.length; ++j)
                        object.items[j] = $root.northstar.live.QueryRecord.toObject(message.items[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this GetApiBrokerQueriesResponse to JSON.
             * @function toJSON
             * @memberof northstar.live.GetApiBrokerQueriesResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetApiBrokerQueriesResponse.prototype.toJSON = function() {
                return GetApiBrokerQueriesResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetApiBrokerQueriesResponse
             * @function getTypeUrl
             * @memberof northstar.live.GetApiBrokerQueriesResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetApiBrokerQueriesResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.GetApiBrokerQueriesResponse";
            };

            return GetApiBrokerQueriesResponse;
        })();

        live.GetApiConfigurationsResponse = (function() {

            /**
             * Properties of a GetApiConfigurationsResponse.
             * @typedef {Object} northstar.live.GetApiConfigurationsResponse.$Properties
             * @property {Array.<northstar.live.LiveConfiguration.$Properties>|null} [items] GetApiConfigurationsResponse items
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetApiConfigurationsResponse.
             * @memberof northstar.live
             * @interface IGetApiConfigurationsResponse
             * @augments northstar.live.GetApiConfigurationsResponse.$Properties
             * @deprecated Use northstar.live.GetApiConfigurationsResponse.$Properties instead.
             */

            /**
             * Shape of a GetApiConfigurationsResponse.
             * @typedef {{
             *   items?: Array.<northstar.live.LiveConfiguration.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} northstar.live.GetApiConfigurationsResponse.$Shape
             */

            /**
             * Constructs a new GetApiConfigurationsResponse.
             * @memberof northstar.live
             * @classdesc Represents a GetApiConfigurationsResponse.
             * @constructor
             * @param {northstar.live.GetApiConfigurationsResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const GetApiConfigurationsResponse = function (properties) {
                this.items = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetApiConfigurationsResponse items.
             * @member {Array.<northstar.live.LiveConfiguration.$Properties>} items
             * @memberof northstar.live.GetApiConfigurationsResponse
             * @instance
             */
            GetApiConfigurationsResponse.prototype.items = $util.emptyArray;

            /**
             * Creates a new GetApiConfigurationsResponse instance using the specified properties.
             * @function create
             * @memberof northstar.live.GetApiConfigurationsResponse
             * @static
             * @param {northstar.live.GetApiConfigurationsResponse.$Properties=} [properties] Properties to set
             * @returns {northstar.live.GetApiConfigurationsResponse} GetApiConfigurationsResponse instance
             * @type {{
             *   (properties: northstar.live.GetApiConfigurationsResponse.$Shape): northstar.live.GetApiConfigurationsResponse & northstar.live.GetApiConfigurationsResponse.$Shape;
             *   (properties?: northstar.live.GetApiConfigurationsResponse.$Properties): northstar.live.GetApiConfigurationsResponse;
             * }}
             */
            GetApiConfigurationsResponse.create = function(properties) {
                return new GetApiConfigurationsResponse(properties);
            };

            /**
             * Encodes the specified GetApiConfigurationsResponse message. Does not implicitly {@link northstar.live.GetApiConfigurationsResponse.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.GetApiConfigurationsResponse
             * @static
             * @param {northstar.live.GetApiConfigurationsResponse.$Properties} message GetApiConfigurationsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetApiConfigurationsResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.items != null && message.items.length)
                    for (let i = 0; i < message.items.length; ++i)
                        $root.northstar.live.LiveConfiguration.encode(message.items[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a GetApiConfigurationsResponse message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.GetApiConfigurationsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.GetApiConfigurationsResponse & northstar.live.GetApiConfigurationsResponse.$Shape} GetApiConfigurationsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetApiConfigurationsResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.GetApiConfigurationsResponse();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.items && message.items.length))
                                message.items = [];
                            message.items.push($root.northstar.live.LiveConfiguration.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a GetApiConfigurationsResponse message.
             * @function verify
             * @memberof northstar.live.GetApiConfigurationsResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetApiConfigurationsResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.items != null && $Object.hasOwnProperty.call(message, "items")) {
                    if (!$Array.isArray(message.items))
                        return "items: array expected";
                    for (let i = 0; i < message.items.length; ++i) {
                        let error = $root.northstar.live.LiveConfiguration.verify(message.items[i], _depth + 1);
                        if (error)
                            return "items." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a GetApiConfigurationsResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.GetApiConfigurationsResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.GetApiConfigurationsResponse} GetApiConfigurationsResponse
             */
            GetApiConfigurationsResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.GetApiConfigurationsResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.GetApiConfigurationsResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.GetApiConfigurationsResponse();
                if (object.items) {
                    if (!$Array.isArray(object.items))
                        throw $TypeError(".northstar.live.GetApiConfigurationsResponse.items: array expected");
                    message.items = $Array(object.items.length);
                    for (let i = 0; i < object.items.length; ++i) {
                        if (!$util.isObject(object.items[i]))
                            throw $TypeError(".northstar.live.GetApiConfigurationsResponse.items: object expected");
                        message.items[i] = $root.northstar.live.LiveConfiguration.fromObject(object.items[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a GetApiConfigurationsResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.GetApiConfigurationsResponse
             * @static
             * @param {northstar.live.GetApiConfigurationsResponse} message GetApiConfigurationsResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetApiConfigurationsResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.items = [];
                if (message.items && message.items.length) {
                    object.items = $Array(message.items.length);
                    for (let j = 0; j < message.items.length; ++j)
                        object.items[j] = $root.northstar.live.LiveConfiguration.toObject(message.items[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this GetApiConfigurationsResponse to JSON.
             * @function toJSON
             * @memberof northstar.live.GetApiConfigurationsResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetApiConfigurationsResponse.prototype.toJSON = function() {
                return GetApiConfigurationsResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetApiConfigurationsResponse
             * @function getTypeUrl
             * @memberof northstar.live.GetApiConfigurationsResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetApiConfigurationsResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.GetApiConfigurationsResponse";
            };

            return GetApiConfigurationsResponse;
        })();

        live.GetApiStrategyMaterialsResponse = (function() {

            /**
             * Properties of a GetApiStrategyMaterialsResponse.
             * @typedef {Object} northstar.live.GetApiStrategyMaterialsResponse.$Properties
             * @property {Array.<northstar.live.StrategyMaterial.$Properties>|null} [items] GetApiStrategyMaterialsResponse items
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetApiStrategyMaterialsResponse.
             * @memberof northstar.live
             * @interface IGetApiStrategyMaterialsResponse
             * @augments northstar.live.GetApiStrategyMaterialsResponse.$Properties
             * @deprecated Use northstar.live.GetApiStrategyMaterialsResponse.$Properties instead.
             */

            /**
             * Shape of a GetApiStrategyMaterialsResponse.
             * @typedef {{
             *   items?: Array.<northstar.live.StrategyMaterial.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} northstar.live.GetApiStrategyMaterialsResponse.$Shape
             */

            /**
             * Constructs a new GetApiStrategyMaterialsResponse.
             * @memberof northstar.live
             * @classdesc Represents a GetApiStrategyMaterialsResponse.
             * @constructor
             * @param {northstar.live.GetApiStrategyMaterialsResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const GetApiStrategyMaterialsResponse = function (properties) {
                this.items = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetApiStrategyMaterialsResponse items.
             * @member {Array.<northstar.live.StrategyMaterial.$Properties>} items
             * @memberof northstar.live.GetApiStrategyMaterialsResponse
             * @instance
             */
            GetApiStrategyMaterialsResponse.prototype.items = $util.emptyArray;

            /**
             * Creates a new GetApiStrategyMaterialsResponse instance using the specified properties.
             * @function create
             * @memberof northstar.live.GetApiStrategyMaterialsResponse
             * @static
             * @param {northstar.live.GetApiStrategyMaterialsResponse.$Properties=} [properties] Properties to set
             * @returns {northstar.live.GetApiStrategyMaterialsResponse} GetApiStrategyMaterialsResponse instance
             * @type {{
             *   (properties: northstar.live.GetApiStrategyMaterialsResponse.$Shape): northstar.live.GetApiStrategyMaterialsResponse & northstar.live.GetApiStrategyMaterialsResponse.$Shape;
             *   (properties?: northstar.live.GetApiStrategyMaterialsResponse.$Properties): northstar.live.GetApiStrategyMaterialsResponse;
             * }}
             */
            GetApiStrategyMaterialsResponse.create = function(properties) {
                return new GetApiStrategyMaterialsResponse(properties);
            };

            /**
             * Encodes the specified GetApiStrategyMaterialsResponse message. Does not implicitly {@link northstar.live.GetApiStrategyMaterialsResponse.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.GetApiStrategyMaterialsResponse
             * @static
             * @param {northstar.live.GetApiStrategyMaterialsResponse.$Properties} message GetApiStrategyMaterialsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetApiStrategyMaterialsResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.items != null && message.items.length)
                    for (let i = 0; i < message.items.length; ++i)
                        $root.northstar.live.StrategyMaterial.encode(message.items[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a GetApiStrategyMaterialsResponse message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.GetApiStrategyMaterialsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.GetApiStrategyMaterialsResponse & northstar.live.GetApiStrategyMaterialsResponse.$Shape} GetApiStrategyMaterialsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetApiStrategyMaterialsResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.GetApiStrategyMaterialsResponse();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.items && message.items.length))
                                message.items = [];
                            message.items.push($root.northstar.live.StrategyMaterial.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a GetApiStrategyMaterialsResponse message.
             * @function verify
             * @memberof northstar.live.GetApiStrategyMaterialsResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetApiStrategyMaterialsResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.items != null && $Object.hasOwnProperty.call(message, "items")) {
                    if (!$Array.isArray(message.items))
                        return "items: array expected";
                    for (let i = 0; i < message.items.length; ++i) {
                        let error = $root.northstar.live.StrategyMaterial.verify(message.items[i], _depth + 1);
                        if (error)
                            return "items." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a GetApiStrategyMaterialsResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.GetApiStrategyMaterialsResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.GetApiStrategyMaterialsResponse} GetApiStrategyMaterialsResponse
             */
            GetApiStrategyMaterialsResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.GetApiStrategyMaterialsResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.GetApiStrategyMaterialsResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.GetApiStrategyMaterialsResponse();
                if (object.items) {
                    if (!$Array.isArray(object.items))
                        throw $TypeError(".northstar.live.GetApiStrategyMaterialsResponse.items: array expected");
                    message.items = $Array(object.items.length);
                    for (let i = 0; i < object.items.length; ++i) {
                        if (!$util.isObject(object.items[i]))
                            throw $TypeError(".northstar.live.GetApiStrategyMaterialsResponse.items: object expected");
                        message.items[i] = $root.northstar.live.StrategyMaterial.fromObject(object.items[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a GetApiStrategyMaterialsResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.GetApiStrategyMaterialsResponse
             * @static
             * @param {northstar.live.GetApiStrategyMaterialsResponse} message GetApiStrategyMaterialsResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetApiStrategyMaterialsResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.items = [];
                if (message.items && message.items.length) {
                    object.items = $Array(message.items.length);
                    for (let j = 0; j < message.items.length; ++j)
                        object.items[j] = $root.northstar.live.StrategyMaterial.toObject(message.items[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this GetApiStrategyMaterialsResponse to JSON.
             * @function toJSON
             * @memberof northstar.live.GetApiStrategyMaterialsResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetApiStrategyMaterialsResponse.prototype.toJSON = function() {
                return GetApiStrategyMaterialsResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetApiStrategyMaterialsResponse
             * @function getTypeUrl
             * @memberof northstar.live.GetApiStrategyMaterialsResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetApiStrategyMaterialsResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.GetApiStrategyMaterialsResponse";
            };

            return GetApiStrategyMaterialsResponse;
        })();

        live.GetApiStreamsResponse = (function() {

            /**
             * Properties of a GetApiStreamsResponse.
             * @typedef {Object} northstar.live.GetApiStreamsResponse.$Properties
             * @property {Array.<northstar.live.StreamSummary.$Properties>|null} [items] GetApiStreamsResponse items
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetApiStreamsResponse.
             * @memberof northstar.live
             * @interface IGetApiStreamsResponse
             * @augments northstar.live.GetApiStreamsResponse.$Properties
             * @deprecated Use northstar.live.GetApiStreamsResponse.$Properties instead.
             */

            /**
             * Shape of a GetApiStreamsResponse.
             * @typedef {{
             *   items?: Array.<northstar.live.StreamSummary.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} northstar.live.GetApiStreamsResponse.$Shape
             */

            /**
             * Constructs a new GetApiStreamsResponse.
             * @memberof northstar.live
             * @classdesc Represents a GetApiStreamsResponse.
             * @constructor
             * @param {northstar.live.GetApiStreamsResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const GetApiStreamsResponse = function (properties) {
                this.items = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetApiStreamsResponse items.
             * @member {Array.<northstar.live.StreamSummary.$Properties>} items
             * @memberof northstar.live.GetApiStreamsResponse
             * @instance
             */
            GetApiStreamsResponse.prototype.items = $util.emptyArray;

            /**
             * Creates a new GetApiStreamsResponse instance using the specified properties.
             * @function create
             * @memberof northstar.live.GetApiStreamsResponse
             * @static
             * @param {northstar.live.GetApiStreamsResponse.$Properties=} [properties] Properties to set
             * @returns {northstar.live.GetApiStreamsResponse} GetApiStreamsResponse instance
             * @type {{
             *   (properties: northstar.live.GetApiStreamsResponse.$Shape): northstar.live.GetApiStreamsResponse & northstar.live.GetApiStreamsResponse.$Shape;
             *   (properties?: northstar.live.GetApiStreamsResponse.$Properties): northstar.live.GetApiStreamsResponse;
             * }}
             */
            GetApiStreamsResponse.create = function(properties) {
                return new GetApiStreamsResponse(properties);
            };

            /**
             * Encodes the specified GetApiStreamsResponse message. Does not implicitly {@link northstar.live.GetApiStreamsResponse.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.GetApiStreamsResponse
             * @static
             * @param {northstar.live.GetApiStreamsResponse.$Properties} message GetApiStreamsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetApiStreamsResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.items != null && message.items.length)
                    for (let i = 0; i < message.items.length; ++i)
                        $root.northstar.live.StreamSummary.encode(message.items[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a GetApiStreamsResponse message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.GetApiStreamsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.GetApiStreamsResponse & northstar.live.GetApiStreamsResponse.$Shape} GetApiStreamsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetApiStreamsResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.GetApiStreamsResponse();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.items && message.items.length))
                                message.items = [];
                            message.items.push($root.northstar.live.StreamSummary.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a GetApiStreamsResponse message.
             * @function verify
             * @memberof northstar.live.GetApiStreamsResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetApiStreamsResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.items != null && $Object.hasOwnProperty.call(message, "items")) {
                    if (!$Array.isArray(message.items))
                        return "items: array expected";
                    for (let i = 0; i < message.items.length; ++i) {
                        let error = $root.northstar.live.StreamSummary.verify(message.items[i], _depth + 1);
                        if (error)
                            return "items." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a GetApiStreamsResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.GetApiStreamsResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.GetApiStreamsResponse} GetApiStreamsResponse
             */
            GetApiStreamsResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.GetApiStreamsResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.GetApiStreamsResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.GetApiStreamsResponse();
                if (object.items) {
                    if (!$Array.isArray(object.items))
                        throw $TypeError(".northstar.live.GetApiStreamsResponse.items: array expected");
                    message.items = $Array(object.items.length);
                    for (let i = 0; i < object.items.length; ++i) {
                        if (!$util.isObject(object.items[i]))
                            throw $TypeError(".northstar.live.GetApiStreamsResponse.items: object expected");
                        message.items[i] = $root.northstar.live.StreamSummary.fromObject(object.items[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a GetApiStreamsResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.GetApiStreamsResponse
             * @static
             * @param {northstar.live.GetApiStreamsResponse} message GetApiStreamsResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetApiStreamsResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.items = [];
                if (message.items && message.items.length) {
                    object.items = $Array(message.items.length);
                    for (let j = 0; j < message.items.length; ++j)
                        object.items[j] = $root.northstar.live.StreamSummary.toObject(message.items[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this GetApiStreamsResponse to JSON.
             * @function toJSON
             * @memberof northstar.live.GetApiStreamsResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetApiStreamsResponse.prototype.toJSON = function() {
                return GetApiStreamsResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetApiStreamsResponse
             * @function getTypeUrl
             * @memberof northstar.live.GetApiStreamsResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetApiStreamsResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.GetApiStreamsResponse";
            };

            return GetApiStreamsResponse;
        })();

        live.GetApiStreamsStreamIdEventsResponse = (function() {

            /**
             * Properties of a GetApiStreamsStreamIdEventsResponse.
             * @typedef {Object} northstar.live.GetApiStreamsStreamIdEventsResponse.$Properties
             * @property {Array.<northstar.live.StreamEvent.$Properties>|null} [items] GetApiStreamsStreamIdEventsResponse items
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetApiStreamsStreamIdEventsResponse.
             * @memberof northstar.live
             * @interface IGetApiStreamsStreamIdEventsResponse
             * @augments northstar.live.GetApiStreamsStreamIdEventsResponse.$Properties
             * @deprecated Use northstar.live.GetApiStreamsStreamIdEventsResponse.$Properties instead.
             */

            /**
             * Shape of a GetApiStreamsStreamIdEventsResponse.
             * @typedef {{
             *   items?: Array.<northstar.live.StreamEvent.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} northstar.live.GetApiStreamsStreamIdEventsResponse.$Shape
             */

            /**
             * Constructs a new GetApiStreamsStreamIdEventsResponse.
             * @memberof northstar.live
             * @classdesc Represents a GetApiStreamsStreamIdEventsResponse.
             * @constructor
             * @param {northstar.live.GetApiStreamsStreamIdEventsResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const GetApiStreamsStreamIdEventsResponse = function (properties) {
                this.items = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetApiStreamsStreamIdEventsResponse items.
             * @member {Array.<northstar.live.StreamEvent.$Properties>} items
             * @memberof northstar.live.GetApiStreamsStreamIdEventsResponse
             * @instance
             */
            GetApiStreamsStreamIdEventsResponse.prototype.items = $util.emptyArray;

            /**
             * Creates a new GetApiStreamsStreamIdEventsResponse instance using the specified properties.
             * @function create
             * @memberof northstar.live.GetApiStreamsStreamIdEventsResponse
             * @static
             * @param {northstar.live.GetApiStreamsStreamIdEventsResponse.$Properties=} [properties] Properties to set
             * @returns {northstar.live.GetApiStreamsStreamIdEventsResponse} GetApiStreamsStreamIdEventsResponse instance
             * @type {{
             *   (properties: northstar.live.GetApiStreamsStreamIdEventsResponse.$Shape): northstar.live.GetApiStreamsStreamIdEventsResponse & northstar.live.GetApiStreamsStreamIdEventsResponse.$Shape;
             *   (properties?: northstar.live.GetApiStreamsStreamIdEventsResponse.$Properties): northstar.live.GetApiStreamsStreamIdEventsResponse;
             * }}
             */
            GetApiStreamsStreamIdEventsResponse.create = function(properties) {
                return new GetApiStreamsStreamIdEventsResponse(properties);
            };

            /**
             * Encodes the specified GetApiStreamsStreamIdEventsResponse message. Does not implicitly {@link northstar.live.GetApiStreamsStreamIdEventsResponse.verify|verify} messages.
             * @function encode
             * @memberof northstar.live.GetApiStreamsStreamIdEventsResponse
             * @static
             * @param {northstar.live.GetApiStreamsStreamIdEventsResponse.$Properties} message GetApiStreamsStreamIdEventsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetApiStreamsStreamIdEventsResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.items != null && message.items.length)
                    for (let i = 0; i < message.items.length; ++i)
                        $root.northstar.live.StreamEvent.encode(message.items[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes a GetApiStreamsStreamIdEventsResponse message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.live.GetApiStreamsStreamIdEventsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.live.GetApiStreamsStreamIdEventsResponse & northstar.live.GetApiStreamsStreamIdEventsResponse.$Shape} GetApiStreamsStreamIdEventsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetApiStreamsStreamIdEventsResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.live.GetApiStreamsStreamIdEventsResponse();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.items && message.items.length))
                                message.items = [];
                            message.items.push($root.northstar.live.StreamEvent.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies a GetApiStreamsStreamIdEventsResponse message.
             * @function verify
             * @memberof northstar.live.GetApiStreamsStreamIdEventsResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetApiStreamsStreamIdEventsResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.items != null && $Object.hasOwnProperty.call(message, "items")) {
                    if (!$Array.isArray(message.items))
                        return "items: array expected";
                    for (let i = 0; i < message.items.length; ++i) {
                        let error = $root.northstar.live.StreamEvent.verify(message.items[i], _depth + 1);
                        if (error)
                            return "items." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a GetApiStreamsStreamIdEventsResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.live.GetApiStreamsStreamIdEventsResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.live.GetApiStreamsStreamIdEventsResponse} GetApiStreamsStreamIdEventsResponse
             */
            GetApiStreamsStreamIdEventsResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.live.GetApiStreamsStreamIdEventsResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.live.GetApiStreamsStreamIdEventsResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.live.GetApiStreamsStreamIdEventsResponse();
                if (object.items) {
                    if (!$Array.isArray(object.items))
                        throw $TypeError(".northstar.live.GetApiStreamsStreamIdEventsResponse.items: array expected");
                    message.items = $Array(object.items.length);
                    for (let i = 0; i < object.items.length; ++i) {
                        if (!$util.isObject(object.items[i]))
                            throw $TypeError(".northstar.live.GetApiStreamsStreamIdEventsResponse.items: object expected");
                        message.items[i] = $root.northstar.live.StreamEvent.fromObject(object.items[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a GetApiStreamsStreamIdEventsResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.live.GetApiStreamsStreamIdEventsResponse
             * @static
             * @param {northstar.live.GetApiStreamsStreamIdEventsResponse} message GetApiStreamsStreamIdEventsResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetApiStreamsStreamIdEventsResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.items = [];
                if (message.items && message.items.length) {
                    object.items = $Array(message.items.length);
                    for (let j = 0; j < message.items.length; ++j)
                        object.items[j] = $root.northstar.live.StreamEvent.toObject(message.items[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this GetApiStreamsStreamIdEventsResponse to JSON.
             * @function toJSON
             * @memberof northstar.live.GetApiStreamsStreamIdEventsResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetApiStreamsStreamIdEventsResponse.prototype.toJSON = function() {
                return GetApiStreamsStreamIdEventsResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetApiStreamsStreamIdEventsResponse
             * @function getTypeUrl
             * @memberof northstar.live.GetApiStreamsStreamIdEventsResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetApiStreamsStreamIdEventsResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.live.GetApiStreamsStreamIdEventsResponse";
            };

            return GetApiStreamsStreamIdEventsResponse;
        })();

        return live;
    })();

    northstar.web = (function() {

        /**
         * Namespace web.
         * @memberof northstar
         * @namespace
         */
        const web = {};

        web.Empty = (function() {

            /**
             * Properties of an Empty.
             * @typedef {Object} northstar.web.Empty.$Properties
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an Empty.
             * @memberof northstar.web
             * @interface IEmpty
             * @augments northstar.web.Empty.$Properties
             * @deprecated Use northstar.web.Empty.$Properties instead.
             */

            /**
             * Shape of an Empty.
             * @typedef {northstar.web.Empty.$Properties} northstar.web.Empty.$Shape
             */

            /**
             * Constructs a new Empty.
             * @memberof northstar.web
             * @classdesc Represents an Empty.
             * @constructor
             * @param {northstar.web.Empty.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const Empty = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Creates a new Empty instance using the specified properties.
             * @function create
             * @memberof northstar.web.Empty
             * @static
             * @param {northstar.web.Empty.$Properties=} [properties] Properties to set
             * @returns {northstar.web.Empty} Empty instance
             * @type {{
             *   (properties: northstar.web.Empty.$Shape): northstar.web.Empty & northstar.web.Empty.$Shape;
             *   (properties?: northstar.web.Empty.$Properties): northstar.web.Empty;
             * }}
             */
            Empty.create = function(properties) {
                return new Empty(properties);
            };

            /**
             * Encodes the specified Empty message. Does not implicitly {@link northstar.web.Empty.verify|verify} messages.
             * @function encode
             * @memberof northstar.web.Empty
             * @static
             * @param {northstar.web.Empty.$Properties} message Empty message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Empty.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes an Empty message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.web.Empty
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.web.Empty & northstar.web.Empty.$Shape} Empty
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Empty.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.web.Empty();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    reader.skipType(tag & 7, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies an Empty message.
             * @function verify
             * @memberof northstar.web.Empty
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Empty.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                return null;
            };

            /**
             * Creates an Empty message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.web.Empty
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.web.Empty} Empty
             */
            Empty.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.web.Empty)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.web.Empty: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                return new $root.northstar.web.Empty();
            };

            /**
             * Creates a plain object from an Empty message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.web.Empty
             * @static
             * @param {northstar.web.Empty} message Empty
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Empty.toObject = function () {
                return {};
            };

            /**
             * Converts this Empty to JSON.
             * @function toJSON
             * @memberof northstar.web.Empty
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Empty.prototype.toJSON = function() {
                return Empty.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Empty
             * @function getTypeUrl
             * @memberof northstar.web.Empty
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Empty.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.web.Empty";
            };

            return Empty;
        })();

        web.Error = (function() {

            /**
             * Properties of an Error.
             * @typedef {Object} northstar.web.Error.$Properties
             * @property {string|null} [detail] Error detail
             * @property {string|null} [status] Error status
             * @property {string|null} [request_id] Error request_id
             * @property {string|null} [runtime_id] Error runtime_id
             * @property {string|null} [url] Error url
             * @property {string|null} [rejection_id] Error rejection_id
             * @property {"detail"} [_detail] Error _detail
             * @property {"status"} [_status] Error _status
             * @property {"request_id"} [_request_id] Error _request_id
             * @property {"runtime_id"} [_runtime_id] Error _runtime_id
             * @property {"url"} [_url] Error _url
             * @property {"rejection_id"} [_rejection_id] Error _rejection_id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an Error.
             * @memberof northstar.web
             * @interface IError
             * @augments northstar.web.Error.$Properties
             * @deprecated Use northstar.web.Error.$Properties instead.
             */

            /**
             * Narrowed shape of an Error.
             * @typedef {{
             *   detail?: string|null;
             *   status?: string|null;
             *   request_id?: string|null;
             *   runtime_id?: string|null;
             *   url?: string|null;
             *   rejection_id?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ _detail?: undefined; detail?: null }|{ _detail?: "detail"; detail: string })
             * ) & (
             *   ({ _status?: undefined; status?: null }|{ _status?: "status"; status: string })
             * ) & (
             *   ({ _request_id?: undefined; request_id?: null }|{ _request_id?: "request_id"; request_id: string })
             * ) & (
             *   ({ _runtime_id?: undefined; runtime_id?: null }|{ _runtime_id?: "runtime_id"; runtime_id: string })
             * ) & (
             *   ({ _url?: undefined; url?: null }|{ _url?: "url"; url: string })
             * ) & (
             *   ({ _rejection_id?: undefined; rejection_id?: null }|{ _rejection_id?: "rejection_id"; rejection_id: string })
             * )} northstar.web.Error.$Shape
             */

            /**
             * Constructs a new Error.
             * @memberof northstar.web
             * @classdesc Represents an Error.
             * @constructor
             * @param {northstar.web.Error.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const Error = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Error detail.
             * @member {string|null|undefined} detail
             * @memberof northstar.web.Error
             * @instance
             */
            Error.prototype.detail = null;

            /**
             * Error status.
             * @member {string|null|undefined} status
             * @memberof northstar.web.Error
             * @instance
             */
            Error.prototype.status = null;

            /**
             * Error request_id.
             * @member {string|null|undefined} request_id
             * @memberof northstar.web.Error
             * @instance
             */
            Error.prototype.request_id = null;

            /**
             * Error runtime_id.
             * @member {string|null|undefined} runtime_id
             * @memberof northstar.web.Error
             * @instance
             */
            Error.prototype.runtime_id = null;

            /**
             * Error url.
             * @member {string|null|undefined} url
             * @memberof northstar.web.Error
             * @instance
             */
            Error.prototype.url = null;

            /**
             * Error rejection_id.
             * @member {string|null|undefined} rejection_id
             * @memberof northstar.web.Error
             * @instance
             */
            Error.prototype.rejection_id = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * Error _detail.
             * @member {"detail"|undefined} _detail
             * @memberof northstar.web.Error
             * @instance
             */
            $Object.defineProperty(Error.prototype, "_detail", {
                get: $util.oneOfGetter($oneOfFields = ["detail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Error _status.
             * @member {"status"|undefined} _status
             * @memberof northstar.web.Error
             * @instance
             */
            $Object.defineProperty(Error.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Error _request_id.
             * @member {"request_id"|undefined} _request_id
             * @memberof northstar.web.Error
             * @instance
             */
            $Object.defineProperty(Error.prototype, "_request_id", {
                get: $util.oneOfGetter($oneOfFields = ["request_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Error _runtime_id.
             * @member {"runtime_id"|undefined} _runtime_id
             * @memberof northstar.web.Error
             * @instance
             */
            $Object.defineProperty(Error.prototype, "_runtime_id", {
                get: $util.oneOfGetter($oneOfFields = ["runtime_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Error _url.
             * @member {"url"|undefined} _url
             * @memberof northstar.web.Error
             * @instance
             */
            $Object.defineProperty(Error.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Error _rejection_id.
             * @member {"rejection_id"|undefined} _rejection_id
             * @memberof northstar.web.Error
             * @instance
             */
            $Object.defineProperty(Error.prototype, "_rejection_id", {
                get: $util.oneOfGetter($oneOfFields = ["rejection_id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new Error instance using the specified properties.
             * @function create
             * @memberof northstar.web.Error
             * @static
             * @param {northstar.web.Error.$Properties=} [properties] Properties to set
             * @returns {northstar.web.Error} Error instance
             * @type {{
             *   (properties: northstar.web.Error.$Shape): northstar.web.Error & northstar.web.Error.$Shape;
             *   (properties?: northstar.web.Error.$Properties): northstar.web.Error;
             * }}
             */
            Error.create = function(properties) {
                return new Error(properties);
            };

            /**
             * Encodes the specified Error message. Does not implicitly {@link northstar.web.Error.verify|verify} messages.
             * @function encode
             * @memberof northstar.web.Error
             * @static
             * @param {northstar.web.Error.$Properties} message Error message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Error.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.detail != null && $Object.hasOwnProperty.call(message, "detail"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.detail);
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.status);
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.request_id);
                if (message.runtime_id != null && $Object.hasOwnProperty.call(message, "runtime_id"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.runtime_id);
                if (message.url != null && $Object.hasOwnProperty.call(message, "url"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.url);
                if (message.rejection_id != null && $Object.hasOwnProperty.call(message, "rejection_id"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.rejection_id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Decodes an Error message from the specified reader or buffer.
             * @function decode
             * @memberof northstar.web.Error
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {northstar.web.Error & northstar.web.Error.$Shape} Error
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Error.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.northstar.web.Error();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.detail = reader.stringVerify();
                            message._detail = "detail";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.status = reader.stringVerify();
                            message._status = "status";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.request_id = reader.stringVerify();
                            message._request_id = "request_id";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.runtime_id = reader.stringVerify();
                            message._runtime_id = "runtime_id";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.url = reader.stringVerify();
                            message._url = "url";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.rejection_id = reader.stringVerify();
                            message._rejection_id = "rejection_id";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Verifies an Error message.
             * @function verify
             * @memberof northstar.web.Error
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Error.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.detail != null && $Object.hasOwnProperty.call(message, "detail")) {
                    properties._detail = 1;
                    if (!$util.isString(message.detail))
                        return "detail: string expected";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (!$util.isString(message.status))
                        return "status: string expected";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    properties._request_id = 1;
                    if (!$util.isString(message.request_id))
                        return "request_id: string expected";
                }
                if (message.runtime_id != null && $Object.hasOwnProperty.call(message, "runtime_id")) {
                    properties._runtime_id = 1;
                    if (!$util.isString(message.runtime_id))
                        return "runtime_id: string expected";
                }
                if (message.url != null && $Object.hasOwnProperty.call(message, "url")) {
                    properties._url = 1;
                    if (!$util.isString(message.url))
                        return "url: string expected";
                }
                if (message.rejection_id != null && $Object.hasOwnProperty.call(message, "rejection_id")) {
                    properties._rejection_id = 1;
                    if (!$util.isString(message.rejection_id))
                        return "rejection_id: string expected";
                }
                return null;
            };

            /**
             * Creates an Error message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof northstar.web.Error
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {northstar.web.Error} Error
             */
            Error.fromObject = function (object, _depth) {
                if (object instanceof $root.northstar.web.Error)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".northstar.web.Error: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.northstar.web.Error();
                if (object.detail != null)
                    message.detail = $String(object.detail);
                if (object.status != null)
                    message.status = $String(object.status);
                if (object.request_id != null)
                    message.request_id = $String(object.request_id);
                if (object.runtime_id != null)
                    message.runtime_id = $String(object.runtime_id);
                if (object.url != null)
                    message.url = $String(object.url);
                if (object.rejection_id != null)
                    message.rejection_id = $String(object.rejection_id);
                return message;
            };

            /**
             * Creates a plain object from an Error message. Also converts values to other types if specified.
             * @function toObject
             * @memberof northstar.web.Error
             * @static
             * @param {northstar.web.Error} message Error
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Error.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.detail != null && $Object.hasOwnProperty.call(message, "detail")) {
                    object.detail = message.detail;
                    if (options.oneofs)
                        object._detail = "detail";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    object.status = message.status;
                    if (options.oneofs)
                        object._status = "status";
                }
                if (message.request_id != null && $Object.hasOwnProperty.call(message, "request_id")) {
                    object.request_id = message.request_id;
                    if (options.oneofs)
                        object._request_id = "request_id";
                }
                if (message.runtime_id != null && $Object.hasOwnProperty.call(message, "runtime_id")) {
                    object.runtime_id = message.runtime_id;
                    if (options.oneofs)
                        object._runtime_id = "runtime_id";
                }
                if (message.url != null && $Object.hasOwnProperty.call(message, "url")) {
                    object.url = message.url;
                    if (options.oneofs)
                        object._url = "url";
                }
                if (message.rejection_id != null && $Object.hasOwnProperty.call(message, "rejection_id")) {
                    object.rejection_id = message.rejection_id;
                    if (options.oneofs)
                        object._rejection_id = "rejection_id";
                }
                return object;
            };

            /**
             * Converts this Error to JSON.
             * @function toJSON
             * @memberof northstar.web.Error
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Error.prototype.toJSON = function() {
                return Error.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Error
             * @function getTypeUrl
             * @memberof northstar.web.Error
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Error.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/northstar.web.Error";
            };

            return Error;
        })();

        return web;
    })();

    return northstar;
})();

export {
  $root as default
};
